# Zig build outputs
zig-out/
.zig-cache/

# CodeGraph index (regenerated locally)
.codegraph/

# Rust / Cargo (workspace — Cargo.lock is committed for binary crates per Cargo guidance)
target/
# Legacy standalone paths (now workspace members)
tests/ffi/target/
tests/ffi/Cargo.lock

# Editor / OS
.DS_Store

# Claude Code agent worktrees (isolation="worktree" scratch dirs)
.claude/worktrees/

# DEAL workspace symbol index (regenerated by `deal check <workspace>`)
.deal/

# DEAL build artifacts (regenerated by `deal build --target ...`)
build/
*.actual
