---
milestone: v2.1.0
audited: 2026-06-09
status: gaps_found
re_audit: true
prior_audit: 2026-06-09 (initial — 33/41, Phase-5 verify blockers OPEN)
scores:
  requirements: 35/41
  phases: 7/8 substantively complete (Phase 6 not started)
  integration: 8/8 flows intact
  flows: 8/8
gaps:
  requirements:
    - id: "REQ-phase-6-application"
      status: "unsatisfied"
      phase: "06-editor-first-platform"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 6 not started — only 06-SPEC.md exists; no plans, no source, no SUMMARY/VERIFICATION/VALIDATION. Expected mid-milestone gap (next build phase), not a defect."
    - id: "REQ-phase-6-1-desktop-editor"
      status: "unsatisfied"
      phase: "06-editor-first-platform"
      verification_status: "missing"
      evidence: "Tauri/Next.js desktop editor not started."
    - id: "REQ-phase-6-2-import-pipelines"
      status: "unsatisfied"
      phase: "06-editor-first-platform"
      verification_status: "missing"
      evidence: "deal import --from sysml-v2 / reqif not started."
    - id: "REQ-phase-6-3-doc-generation"
      status: "unsatisfied"
      phase: "06-editor-first-platform"
      verification_status: "missing"
      evidence: "deal build --target docs not started."
    - id: "REQ-phase-6-4-stdlib-expansion"
      status: "unsatisfied"
      phase: "06-editor-first-platform"
      verification_status: "missing"
      evidence: "protocols/standards/patterns stdlib expansion not started."
    - id: "REQ-phase-6-gate"
      status: "unsatisfied"
      phase: "06-editor-first-platform"
      verification_status: "missing"
      evidence: "Phase 6 exit gate (full vision) not started."
  integration: []   # all cross-phase blockers from the initial audit are CLOSED (commit 027a830)
  flows: []         # 8/8 flows intact (was 6/8) — re-verified by gsd-integration-checker 2026-06-09
  verification_artifacts:
    - phase: "03-editor-intelligence"
      severity: "non-blocking"
      issue: "No 03-VERIFICATION.md (execute-phase artifact). 03-VALIDATION.md exists (nyquist_compliant:true). ROADMAP+traceability mark Complete; phase-3-gate/phase-3-gate-fresh exit 0 — functionally corroborated, artifact missing."
    - phase: "05-simulation-integration"
      severity: "non-blocking"
      issue: "No 05-VERIFICATION.md. 05-VALIDATION.md exists (nyquist_compliant:true, status:approved, Validation Audit #2 records STALE-wiring closure); 05-SECURITY.md present (20/20); 05-LEARNINGS.md present. SC#5 human-verify APPROVED 2026-06-08. Functionally corroborated, artifact missing."
  traceability_drift:
    - severity: "non-blocking"
      issue: "REQUIREMENTS.md traceability table marks all 10 Phase-2 REQ-IDs as 'Pending' / unchecked `[ ]`, but Phase 2 is Complete (02-VERIFICATION.md GREEN, all 10 SATISFIED; ROADMAP [x] 2026-05-22). Checkboxes are stale."
    - severity: "non-blocking"
      issue: "Phase 1.5's 7 requirements (REQ-phase-1.5-*) are documented in the body but ABSENT from the traceability table and the 41-count coverage. They are verified complete (01.5-VERIFICATION.md passed-after-amendment 7/7)."
    - severity: "non-blocking"
      issue: "REQUIREMENTS.md line 132 'Phases 1–6 are not started' is stale (written 2026-05-19)."
    - severity: "non-blocking"
      issue: "04-VERIFICATION.md lists two 'partial' gaps now CLOSED — (a) load_reqif_schemas SHA256 IS called in production at reqif.rs:608; (b) CWE-22 path-traversal fix present at resolver.rs:354-366. Doc not yet refreshed."
tech_debt:
  - phase: 04-ecosystem
    items:
      - "DOORS / standards-grade ReqIF import (SC-2) SKIPPED — no tool access; only Python reqif v0.0.48 round-trip (D-58 fallback) validates. DOORS contract technically unvalidated."
      - "CLI cross-file E2500 dimensional check DEFERRED Phase 4 → Phase 5; wired in Phase 5 (D-88). Confirm closed."
  - phase: 05-simulation-integration
    items:
      - "deal_check_with_stdlib (10th C ABI export) has no safe:: wrapper in deal-ffi — CLI/verify call the raw unsafe interface, bypassing the clone-before-free encapsulation the other 9 exports get."
      - "E2503 dimensional pin (sema_dimensional.regression_pins) = accepted Phase-4 carryover; fails on bare `zig build test` without sibling deal-stdlib worktree. Inherited; documented in deferred-items.md."
      - "spec submodule re-pin for calc/constraint grammar (SD-21/22/23) deferred to Phase 6; current gitlink dd3bb78 carries sims/v0 + sim-input values only. Push spec commits 1fb43ca+dd3bb78 to remote before any fresh-worktree gate."
  - phase: cross-cutting
    items:
      - "SUMMARY.md requirements-completed frontmatter populated in only 8 of 41 plan SUMMARYs. 3-source cross-reference leans on VERIFICATION.md + ROADMAP because the SUMMARY source is unreliable. Populate going forward."
---

# Milestone v2.1.0 — Audit Report (Re-Audit)

**Audited:** 2026-06-09 (re-audit; supersedes the initial 2026-06-09 pass)
**Status:** ⚠ gaps_found — **single remaining gap class: Phase 6 not started (expected next build phase)**
**Project:** DEAL — Digital Engineering Authoring Language

## Verdict

The v2.1.0 milestone (Phases 0→6) has **not** reached its full definition of done, but
the **only** remaining gap is that **Phase 6 (Application) is unstarted** — 6 requirements
unsatisfied. That is the expected mid-milestone boundary (STATE.md `status: verifying`,
75%), not a defect to close inline.

**What changed since the initial audit:** the one genuine *regression* the first pass
found — Phase 5's evidence-freshness verification being functionally broken — is now
**fixed and independently re-verified**. Commit `027a830` wired `check_staleness` and the
`--run-sims` re-run into `run_verify`. The `gsd-integration-checker` re-confirmed both
flows are CONNECTED end-to-end with passing TDD tests, raising the integration score from
**6/8 → 8/8 flows intact, 0 blockers**.

The **full E2E chain through Phases 1→5 is intact**: parse → C ABI → CLI → IR → SysML/ReqIF
codegen → LSP → simulate → evidence → verify (including STALE detection and `--run-sims`
refresh). A program manager can ask "is REQ_BAT_001 verified?" and get a traceable answer
(REQ_BAT_001 = PASS on the real showcase).

Everything else flagged is **non-blocking documentation/artifact hygiene** (missing
`03-/05-VERIFICATION.md`, stale Phase-2 checkboxes, untabled Phase-1.5 reqs, a stale
`04-VERIFICATION.md`) — none affects functional completeness.

## Requirements Coverage (3-source cross-reference)

Denominator = the 41 requirements in the REQUIREMENTS.md traceability table.
(Phase 1.5's 7 verified requirements are omitted from that table — see Traceability Drift.)

| Phase | Reqs | VERIFICATION.md | Traceability table | Final status |
|-------|------|-----------------|--------------------|--------------|
| 0 Foundation | 1 | n/a (spec, collapsed) | `[x]` | ✅ satisfied |
| 1 Zig Compiler Core | 7 | passed 5/5 | `[x]` | ✅ satisfied |
| 1.5 Parser Strictness | (7, untabled) | passed-after-amendment 7/7 | **absent** | ✅ satisfied (table gap) |
| 2 Prove the Pipeline | 10 | GREEN, 10/10 SATISFIED | **`[ ]` Pending (STALE)** | ✅ satisfied (fix checkboxes) |
| 3 Editor Intelligence | 6 | **missing** (VALIDATION only) | `[x]` | ✅ satisfied, verification-artifact gap |
| 4 Ecosystem | 6 | verified 13/15 + human-resolved | `[x]` | ✅ satisfied (tech debt) |
| 5 Simulation Integration | 5 | **missing** (VALIDATION #2 records closure) | `[x]` | ✅ **satisfied** (STALE wiring closed) |
| 6 Application | 6 | missing (not started) | Pending | ❌ unsatisfied |

**Satisfied: 35/41** (↑ from 33/41 — Phase-5 `REQ-phase-5-3-evidence-verification` and
`REQ-phase-5-gate` moved partial → satisfied). Partial: 0. Unsatisfied: 6 (all Phase 6).

## Phase Verification Summary

| Phase | Artifact | Status | Score |
|-------|----------|--------|-------|
| 01 | 01-VERIFICATION.md | passed | 5/5 truths |
| 01.5 | 01.5-VERIFICATION.md | passed-after-amendment (false-GREEN remediated) | 7/7 |
| 02 | 02-VERIFICATION.md | GREEN (viewer-smoke = documented blocker per D-36) | 10/10 reqs |
| 03 | **VALIDATION only** | nyquist_compliant; phase-3-gate exits 0 | — |
| 04 | 04-VERIFICATION.md | verified + human-resolved (SC-1 accepted, SC-2 skipped) | 13/15 |
| 05 | **VALIDATION only (#2 = STALE closure)** | nyquist_compliant, status:approved; SC#5 human-APPROVED | — |
| 06 | none | NOT STARTED | 0 |

## Cross-Phase Integration (re-verified by gsd-integration-checker, 2026-06-09)

**Score: 8 intact / 8 flows. 0 blockers.** All C ABI exports consumed; Phase 6 confirmed
isolated (no Phase 1–5 code depends on it).

### Intact flows
- A. `deal parse` — Zig parse → C ABI → AST JSON (`main.rs:843` → `ffi::deal_parse`/`deal_ast_json`)
- B. `deal check` — stdlib-seeded `deal_check_with_stdlib` per file (`main.rs:366`)
- C. `deal fmt` — parse → format in place (`main.rs:708` → `ffi::deal_format`)
- D. `deal build --target sysml-v2` — IR → emit → schema validate (`main.rs:1775` → `sysml_v2::emit_from_bytes`)
- E. `deal build --target reqif` — IR → emit → SHA256-verified XSD load → `.reqifz` (`reqif.rs:607-624`)
- F. `deal simulate → evidence → deal check --verify` happy path (`simulate.rs:706` ↔ `verify.rs:972`)
- **G. `deal check --verify --run-sims` (stale refresh) — NOW CONNECTED** (`verify.rs:1345-1350` calls `simulate::run_simulate_in(&project_root, &[], true, false)`)
- **H. Per-REQ STALE verdict — NOW CONNECTED** (`discover_baseline_tag` v:560 → `build_stale_overrides` v:575 → `check_staleness` v:585 → `evaluate_from_bytes` v:1362 → STALE exit v:1384-1388)

### Closed blockers (were BLOCKERS in the initial audit)
- `verify::check_staleness → run_verify` — **CLOSED**. `stale_overrides` is now a genuinely
  populated `HashMap<String,bool>` (was hardcoded empty). Backed by 4 TDD tests in
  `cli/tests/verify_engine.rs` that write real filesystem artifacts and assert SHA-256 drift.
- `verify.rs:run_verify → simulate::run_simulate` — **CLOSED**. `--run-sims` now invokes
  `run_simulate_in` (best-effort; Python-dependent, tracked manual-only per the MATLAB carve-out).

### Doc corrections carried from the integration check
- Phase 4 `04-VERIFICATION.md` still claims `load_reqif_schemas` SHA256 is **not** in the
  production path → **incorrect**: it IS called at `reqif.rs:608`. Gap closed.
- Phase 4 CWE-22 path-traversal fix present at `resolver.rs:354-366`. Gap closed. Refresh the doc.

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete |
|-------|---------------|-------------------|-----------------|
| 01 | ✓ | true | true |
| 01.5 | ✓ | true | true |
| 02 | ✓ | true | true |
| 03 | ✓ | true | true |
| 04 | ✓ | true | true |
| 05 | ✓ | true | true |
| 06 | ✗ missing | — | — (not started) |

**Overall: 6 compliant / 1 missing (Phase 6, not started).** No PARTIAL phases.

## Tech Debt & Carryover (non-blocking)

- **Phase 4:** DOORS import (SC-2) unvalidated (skipped, no tool); `04-VERIFICATION.md` stale
  (two gaps now closed — ReqIF SHA256 + CWE-22); cross-file E2500 deferred Ph4→Ph5 (wired).
- **Phase 5:** `deal_check_with_stdlib` lacks a `safe::` wrapper; E2503 dimensional pin is an
  inherited Phase-4 carryover; spec-submodule calc/constraint re-pin deferred to Phase 6.
- **Cross-cutting:** SUMMARY `requirements-completed` frontmatter populated in only 8/41
  plans — weakens automated traceability; populate going forward.

## Recommended Actions

The functional blocker is gone. The remaining items are **doc hygiene** (do opportunistically)
plus the **next build phase**:

1. **Repair REQUIREMENTS.md traceability** — check off Phase 2's 10 reqs, add Phase 1.5's 7
   reqs to the table (and to the coverage count), and remove the stale "Phases 1–6 are not
   started" line.
2. **Backfill verification artifacts** for Phases 3 and 5 (`03-VERIFICATION.md`,
   `05-VERIFICATION.md`) so the verification trail is complete (both are functionally
   corroborated by gates + VALIDATION docs).
3. **Refresh `04-VERIFICATION.md`** to reflect the two now-closed gaps (ReqIF SHA256 + CWE-22).
4. **Phase 6 is the remaining milestone work** — not a defect, the next build phase. Proceed
   via the standard phase chain when ready.

## Initial-Audit Closure — Recommended Action #1 (2026-06-09, earlier same day)

Action #1 (fix the Phase 5 verify blockers) was executed via `/gsd:validate-phase 5` and
committed as `027a830`:

- **STALE detection (was: unreachable).** `run_verify` no longer hardcodes an empty
  `stale_overrides`. New seam in `cli/src/verify.rs` — `discover_baseline_tag()` +
  `build_stale_overrides()` (which calls the previously-dead `check_staleness`) — is wired
  into `run_verify`, so drifted evidence vs the baseline manifest now produces the STALE
  verdict and non-zero exit. Covered by 4 new TDD tests in `cli/tests/verify_engine.rs`.
- **`--run-sims` re-run (was: dead).** `run_verify` now invokes
  `simulate::run_simulate_in(.., all=true, stale=false)` to force-refresh evidence before
  staleness resolution. The re-run path is Python-dependent and tracked manual-only in
  `05-VALIDATION.md` (consistent with the MATLAB carve-out).

This re-audit independently re-verified both closures via `gsd-integration-checker`
(8/8 flows, 0 blockers). `REQ-phase-5-3-evidence-verification` and `REQ-phase-5-gate` are
now **satisfied**. Remaining milestone gaps (Phase 6 unstarted; doc/traceability drift;
backfill of `03-/05-VERIFICATION.md`) are non-blocking.
