---
phase: 05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com
plan: 02
type: execute
wave: 1
depends_on: ["05.2-01"]
files_modified:
  - src/lexer.zig
  - src/keywords.zig
  - tests/unit/lexer_keywords.zig
  - tests/unit/lexer_calc.zig
autonomous: true
requirements: [REQ-05.2-D03]
must_haves:
  truths:
    - "global_keywords.get(\"calc\") returns .kw_calc; get(\"return\") returns .kw_return; get(\"require\") returns .kw_require"
    - "global_keywords.get(\"sig\") returns null — sig stays IDENT everywhere (D-07)"
    - "The lexer emits a single .plus_minus token for the 2-byte UTF-8 sequence 0xC2 0xB1 (±)"
    - "Lexing a minimal `calc def F(x : Real) : Real { return x; }` produces kw_calc/kw_def/ident/.../kw_return token sequence with zero .unknown tokens"
    - "The 19 existing showcase token snapshots remain byte-stable (no showcase file uses the new keywords)"
  artifacts:
    - path: "src/lexer.zig"
      provides: "kw_calc/kw_return/kw_require Tag enum entries + plus_minus token + 0xC2 0xB1 dispatch"
      contains: "plus_minus"
    - path: "src/keywords.zig"
      provides: "calc/return/require global_keywords entries (NOT sig)"
      contains: "kw_calc"
    - path: "tests/unit/lexer_calc.zig"
      provides: "token-sequence assertion for a minimal calc def + ± lexing"
  key_links:
    - from: "src/lexer.zig scanPunctuation"
      to: ".plus_minus token"
      via: "0xC2 0xB1 multi-byte dispatch before the generic high-bit else path"
      pattern: "0xC2"
---

<objective>
Wave 1: teach the lexer the three new global reserved words (`calc`, `return`, `require`) and the `±` operator token, while keeping `sig` as a plain IDENT (D-07 contextual). `=>` already lexes as the existing `ARROW` token — no lexer change for it.

Purpose: The oracle files (calcs.deal, constraints.deal) currently fail to lex on `calc`/`±`. This wave makes them lex cleanly; parsing comes in Wave 2.

Output: 3 new keyword Tag entries + `plus_minus` token + 2-byte UTF-8 dispatch; keywords map updated (count comment 85→88); new lexer token-sequence test; existing token snapshots regenerated byte-stable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-CONTEXT.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-PATTERNS.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-RESEARCH.md
@src/lexer.zig
@src/keywords.zig

<interfaces>
<!-- Key anchors extracted from PATTERNS.md / RESEARCH.md — executor should use directly, no codebase scavenger hunt. -->
- src/lexer.zig Tag enum: lines 61–246. Insert kw_calc after kw_constraint in the Element section; kw_return/kw_require in a new "Calc/constraint body keywords" section; plus_minus in §7 Delimiters & Punctuation after .plus (~line 227).
- src/lexer.zig scanPunctuation: lines 725–895. The else branch at ~867–894 handles c0 >= 0x80 by computing UTF-8 length and emitting .unknown. INSERT the ± dispatch BEFORE that generic path.
- src/keywords.zig global_keywords map: lines 32–139. Count comment at line 27 (85 → 88). Element keywords section near `.{ "constraint", .kw_constraint }` (~line 45).
- self.peekByte(n) is the existing byte-peek helper used in scanPunctuation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add kw_calc/kw_return/kw_require to Tag enum + keywords map; add plus_minus token + ± dispatch</name>
  <read_first>
    - src/lexer.zig (Tag enum lines 61–246; scanPunctuation lines 725–895, especially the else branch 867–894)
    - src/keywords.zig (global_keywords map lines 32–139; count comment line 27)
    - .planning/phases/05.2-.../05.2-PATTERNS.md § "Wave 1: src/lexer.zig" and § "Wave 1: src/keywords.zig" (copy-ready excerpts)
    - .planning/phases/05.2-.../05.2-RESEARCH.md § "Wave 1: Lexer — Detailed Investigation"
    - `git -C spec show 7ce59bc:grammar/lexical.ebnf` §5 (the keyword categories source-of-truth that keywords.zig mirrors)
  </read_first>
  <behavior>
    - Test (lexer_keywords): global_keywords.get("calc") == .kw_calc; get("return") == .kw_return; get("require") == .kw_require.
    - Test (lexer_keywords, D-07 guard): global_keywords.get("sig") == null. This is the Pitfall-4 guard — sig must NOT be globally reserved.
    - Test (lexer_calc): lexing `±` (bytes 0xC2 0xB1) yields exactly one .plus_minus token spanning 2 bytes, NOT two .unknown tokens.
    - Test (lexer_calc): lexing `calc def F(x : Real) : Real { return x; }` yields a token stream beginning .kw_calc, .kw_def, .ident("F"), .l_paren, .ident("x"), .colon, .ident("Real")… and containing .kw_return, with ZERO .unknown tokens.
  </behavior>
  <action>
    In `src/lexer.zig` Tag enum: add `kw_calc` after `kw_constraint` (Element section); add `kw_return` and `kw_require` in a new "Calc/constraint body keywords (SD-21/22)" section; add `plus_minus` in §7 after `.plus`. DO NOT add `kw_sig` — D-07 forbids it (Pitfall 4).

    In `src/lexer.zig` scanPunctuation else branch (~867), INSERT before the generic `if (c0 >= 0x80)` path: detect `c0 == 0xC2 and self.peekByte(1) == 0xB1` → advance `self.pos = start + 2` and return `tokenAt(.plus_minus, start, self.pos)`. This MUST come before the generic high-bit handler (Pitfall 5 / threat T-W1: ± OOB read — the multi-byte dispatch is ADDED before, not replacing, the generic path). The `+/-` ASCII alias is NOT handled in the lexer — it is a parser-level token sequence (.plus .slash .minus) per RESEARCH assumption A1 (Wave 2/3 owns it; fmt normalizes to ±).

    In `src/keywords.zig` global_keywords: add `.{ "calc", .kw_calc }` (alongside kw_constraint), `.{ "return", .kw_return }`, `.{ "require", .kw_require }` in a new "Calc/constraint body keywords" section. DO NOT add `sig`. Update the count comment at line 27 from 85 to 88.

    If `json.zig`/`fmt.zig` carry a token-name map that switches over Tag, add `.plus_minus` arms there to keep exhaustive switches compiling. No fenced code in this action — copy the exact shapes from PATTERNS.md § Wave 1.
  </action>
  <verify>
    <automated>zig build test -Dtest-filter=lexer_keywords && zig build test -Dtest-filter=lexer_calc</automated>
  </verify>
  <acceptance_criteria>
    - `zig build test -Dtest-filter=lexer_keywords` passes, including the assertion that `global_keywords.get("sig")` is null
    - `zig build test -Dtest-filter=lexer_calc` passes (± → single .plus_minus; minimal calc def lexes with zero .unknown)
    - `zig build` compiles (any exhaustive Tag switch in json.zig/fmt.zig has a .plus_minus arm)
    - `grep -c 'kw_sig' src/keywords.zig` == 0 (D-07 guard, source assertion)
  </acceptance_criteria>
  <done>calc/return/require are lexed as keywords, sig stays IDENT, and ± lexes as one plus_minus token.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate token snapshots + confirm byte-stability of the 19 existing showcase files</name>
  <read_first>
    - tests/unit/lexer_keywords.zig (existing keyword-map test to extend)
    - tests/snapshots/tokens/ (existing per-file token snapshots)
    - .planning/phases/05.2-.../05.2-RESEARCH.md § "Impact on Token Snapshots"
    - .planning/phases/05.2-.../05.2-VALIDATION.md § "Wave 1 — Lexer"
  </read_first>
  <action>
    Regenerate token snapshots with `zig build test -Dtest-filter=lexer_snapshot -Dupdate-snapshots=true` (the existing snapshot-update mechanism — do NOT hand-edit JSON). Because none of the 19 existing showcase files use `calc`/`return`/`require`/`±`, their token snapshots MUST be byte-stable (a non-empty git diff on any of the 19 existing token snapshots is a regression and means an unintended tokenization change leaked in — investigate before committing). The two new analysis/*.deal files (present since Wave 0) will now lex; their token snapshots are NEW and expected.

    Re-run without the update flag to assert stability.
  </action>
  <verify>
    <automated>zig build test -Dtest-filter=lexer_snapshot</automated>
  </verify>
  <acceptance_criteria>
    - `zig build test -Dtest-filter=lexer_snapshot` passes (snapshots stable after regeneration)
    - `git diff --stat tests/snapshots/tokens/` shows changes ONLY for the two new analysis files (+ any newly-added snapshot), and NO content change to the 19 pre-existing showcase token snapshots
    - `zig build test && cargo test --workspace` green (wave-merge sampling per VALIDATION.md)
  </acceptance_criteria>
  <done>Token snapshots regenerated; existing 19 byte-stable; the two oracle files now lex cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw source bytes → lexer | Untrusted multi-byte UTF-8 (`±`) crosses into the byte-dispatch path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-W1-01 | Tampering | `±` 2-byte dispatch (0xC2 0xB1) | mitigate | The multi-byte dispatch is ADDED before the generic high-bit path, not replacing it; `peekByte(1)` is bounds-safe (returns sentinel past EOF) — guards against OOB read on a truncated 0xC2 at end-of-buffer (Pitfall 5) |
| T-05.2-W1-02 | Tampering | `sig` accidental global reservation | mitigate | Source-grep gate `grep -c 'kw_sig' src/keywords.zig == 0` + null-return unit assertion (Pitfall 4 / D-07) |
| T-05.2-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH audit: none) |
</threat_model>

<verification>
- `zig build test -Dtest-filter=lexer_keywords` and `=lexer_calc` pass.
- `zig build test -Dtest-filter=lexer_snapshot` stable; 19 existing snapshots unchanged.
- `zig build test && cargo test --workspace` green.
</verification>

<success_criteria>
- calc/return/require are global keywords; sig is not.
- ± lexes as a single plus_minus token via a bounds-safe 2-byte dispatch.
- A minimal calc def lexes with zero .unknown tokens.
- Existing token snapshots byte-stable.
</success_criteria>

<output>
Create `.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-02-SUMMARY.md` when done.
</output>
