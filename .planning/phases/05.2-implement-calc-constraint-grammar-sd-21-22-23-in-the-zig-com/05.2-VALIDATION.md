---
phase: 05.2
slug: implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-10
validated: 2026-06-11
---

# Phase 05.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `05.2-RESEARCH.md` § Validation Architecture (HIGH confidence, file:line anchored).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Zig built-in test runner + Rust `cargo test` |
| **Config file** | `build.zig` (Zig), workspace `Cargo.toml` (Rust) |
| **Quick run command** | `zig build test -Dtest-filter=calc` |
| **Full suite command** | `zig build test && cargo test --workspace` |
| **Estimated runtime** | ~30–60 seconds (quick filter ~5s) |

---

## Sampling Rate

- **After every task commit:** Run `zig build test -Dtest-filter=<wave-relevant-filter>`
- **After every plan wave:** Run `zig build test && cargo test --workspace`
- **Before `/gsd:verify-work`:** `zig build phase-05.2-gate` (+ `-fresh` variant) must be green
- **Max feedback latency:** ~60 seconds (full suite)

---

## Per-Task Verification Map

> Filled from executed plans + audited via live test execution 2026-06-11. Task ID = plan ID; GREEN commit anchored.

| Task ID | Plan | Wave | Decision | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| 05.2-01 (`11852e0`) | W0 | 0 | D-01/D-02/D-03 | — | Spec merge `fce94c7`; 19 showcase parse clean; 3 oracle files present | smoke/gate | `zig build phase-05.2-wave0-gate`; `=parser_deal_snapshot` | ✅ gate (8 refs build.zig) + 3 oracles | ✅ green |
| 05.2-02 (`1a224b2`) | W1 | 1 | D-07 | T-05.2-W1-01 (± OOB read) | `kw_calc`/`kw_return`/`kw_require` tokenize; `sig` stays IDENT; ± single-token | unit | `=lexer_calc`; `=lexer_keywords` | ✅ lexer_calc.zig | ✅ green |
| 05.2-03 (`4f23c18`) | W2 | 2 | D-04/D-05/D-09 | T-05.2-W2-01 (parser DoS) | 3 oracles parse exit 0; 9 NodeKinds + ConstraintDefinition; round-trip byte-stable | integration | `=parser_deal_snapshot`; `=determinism_parse_twice` | ✅ 3 AST snapshots | ✅ green |
| 05.2-04 (`7f560fa`/`a9b3ace`) | W3 | 3 | D-06/D-08/D-10 | T-05.2-W3-01 (ConstraintRef cycle) | E2600 purity / E2601 missing-return / E2610 non-boolean / E2611 cycle; `+/-`→`±`; zero E25xx regression | unit/regression | `=sema_calc` (10); `=fmt_roundtrip` (22); `=recovery_corpus` (m25/26/27 pins) | ✅ sema_calc.zig + m25/26/27 | ✅ green |
| 05.2-05 (`dc242d3`/`f82b4a0`) | W4 | 4 | D-06/D-08/D-12 | T-05.2-W4-01..04 | IR `calc_def` schema-validates; KerML Function/Predicate golden match byte-for-byte | golden/integration | `cargo test -p deal --test golden_sysml_v2` (09/10); `--test golden_fixture_schema_validity` (09/10); `zig build phase-05.2-gate` | ✅ 09/10 goldens + expected.json | ✅ green |
| 05.2-06 (`5191a83`/`dbfb169`/`6c359aa`) | W5 | 5 | D-04/D-07 | — | `calc_def` semantic token + signature hover + go-to-def; tree-sitter corpus + contextual `sig` | unit/manual | `cargo test -p deal-lsp --lib calc_def` (6); `tree-sitter test` | ✅ corpus + 6 lsp tests | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements (net-new test files/steps)

- [x] `zig build phase-05.2-wave0-gate` build step in `build.zig` — spec-merge verification (19 showcase parse clean + oracle files present + oracle parse FAILs pre-Wave-1) — *8 refs in build.zig*
- [x] `scripts/verify-fresh-worktree.sh` extended with `phase-05.2-wave0-gate` step
- [x] `tests/unit/sema_calc.zig` — E2600/E2601/E2610/E2611 assertions (10 tests, green)
- [x] `tests/malformed/m25_calc_out_param_expr.deal` + pin in `recovery_corpus.zig`
- [x] `tests/malformed/m26_calc_no_return.deal` + pin
- [x] `tests/malformed/m27_require_not_boolean.deal` + pin
- [x] `tests/golden/sysml-v2/09-calc-def.{deal,expected.json}` + `10-constraint-def.{deal,expected.json}` golden fixtures (delivered as numbered fixtures, not `calcs_sysml.json`)
- [x] `zig build phase-05.2-gate` + `phase-05.2-gate-fresh` build steps (6 refs in build.zig)

*Existing test infrastructure (parser_deal_snapshot, determinism_parse_twice, sema_dimensional, fmt_roundtrip, tests/golden, tree-sitter corpus, deal-lsp tests) covers all wave-level regression needs; the above are net-new.*

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| Spec submodule `git merge 7ce59bc` + conflict resolution + push + gitlink bump | D-01/D-02 | Git history mutation across submodule + parent; cannot be unit-tested | Resolve 3 additive conflicts (deal.ebnf, DESIGN-DECISIONS.md, README.md) by hand; push to spec origin/main; bump `deal` gitlink; then `phase-05.2-wave0-gate` verifies the result |
| VS Code highlighting smoke: `calc def`/`return`/`require`/`±` colorized | D-04 | Visual editor surface; LSP unit tests cover token emission but not render | Open `calcs.deal` in VS Code with vscode-deal; confirm keyword + operator highlighting + hover |

*All other phase behaviors have automated verification.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (net-new gate steps + fixtures) — all 8 net-new artifacts present on disk
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter (planner/checker sets after per-task map is complete)

**Approval:** verified 2026-06-10; **Nyquist audit:** 2026-06-11 — all 6 waves COVERED via live test execution

---

## Validation Audit 2026-06-11

Retroactive Nyquist audit of the executed phase (6 plans, Waves 0–5). Cross-referenced every requirement against on-disk test files and confirmed green via live test runs (not summary self-reports).

| Metric | Count |
|--------|-------|
| Requirements/waves audited | 6 |
| Gaps found (MISSING) | 0 |
| Resolved (tests generated) | 0 |
| Escalated to manual-only | 0 |

**Live verification executed:**

| Wave | Command | Result |
|------|---------|--------|
| W0 | `tests/showcase/.../analysis/{calcs,constraints,precision}.deal` + `phase-05.2-wave0-gate` (8 refs) | ✅ 3 oracles + gate present |
| W1 | `zig build test -Dtest-filter=lexer_calc` / `=lexer_keywords` | ✅ green |
| W2 | `=parser_deal_snapshot` / `=determinism_parse_twice` | ✅ green |
| W3 | `=sema_calc` (10) / `=fmt_roundtrip` (22) / `=recovery_corpus` (exit 0, 0 panics; m25/26/27 pin) | ✅ green |
| W4 | `cargo test -p deal --test golden_sysml_v2` (09/10) + `--test golden_fixture_schema_validity` (09/10, 11 passed) | ✅ green |
| W5 | `cargo test -p deal-lsp --lib calc_def` (6 passed) | ✅ green |

**Note:** `recovery_corpus` emits soft "0 diagnostics (expected ≥1)" notices for pre-existing `gen_*` fuzz files and `m01` — these are not Phase 05.2 fixtures and the test exits 0 with 0 panics. The phase's own m25/m26/m27 fixtures pin correctly. The known pre-existing `sema.dimensional.regression_pins` failure (deal-stdlib sibling absent) is filtered by the phase gate and is a documented Phase-4 carryover, not a Phase 05.2 gap.

**Verdict:** Phase 05.2 is **Nyquist-compliant**. Every automatable requirement has green automated verification; the two remaining manual-only items (spec submodule git merge, VS Code visual render) are inherently non-automatable.
