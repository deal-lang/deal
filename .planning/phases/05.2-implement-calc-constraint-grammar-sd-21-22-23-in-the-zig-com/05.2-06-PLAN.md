---
phase: 05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com
plan: 06
type: execute
wave: 3
depends_on: ["05.2-03"]
files_modified:
  - tree-sitter-deal/grammar.js
  - tree-sitter-deal/queries/highlights.scm
  - tree-sitter-deal/test/corpus/calc_constraint.txt
  - vscode-deal/syntaxes/deal.tmLanguage.json
  - lsp/src/semantic_tokens.rs
  - lsp/src/hover.rs
  - lsp/src/definition.rs
autonomous: false
requirements: [REQ-05.2-D07]
must_haves:
  truths:
    - "D-04: Wave 5 ships all three editor surfaces (tree-sitter-deal + vscode-deal + LSP) at rich depth — keyword/± highlighting PLUS LSP signature/return-type hover and go-to-def for calc/constraint invocations — with a clean Phase 6 seam so the broader LSP build-out is not redone; tree-sitter-deal parses calc def / constraint def / require / return / ± and `tree-sitter test` passes the new corpus"
    - "highlights.scm highlights calc def / return / require keywords and ± as an operator; sig is highlighted ONLY inside a return contract (contextual)"
    - "vscode-deal TextMate grammar highlights calc / return / require alongside the existing part / constraint / requirement keywords"
    - "LSP emits a calc_def semantic token (keyword + MOD_DECLARATION) alongside the existing constraint_def token"
    - "Hovering a calc invocation shows the calc signature (param directions/names/types + return type + precision contract); go-to-def jumps to the calc_def/constraint_def"
    - "The Phase 6 LSP seam is documented: Wave 5 adds only the calc/constraint arms to existing handlers; it does NOT refactor the hover/definition/semantic-token infrastructure"
  artifacts:
    - path: "tree-sitter-deal/grammar.js"
      provides: "calc_definition + parameter_list + return_contract + require_statement + plus_minus rules"
      contains: "calc_definition"
    - path: "lsp/src/semantic_tokens.rs"
      provides: "calc_def semantic-token arm"
      contains: "calc_def"
    - path: "lsp/src/hover.rs"
      provides: "calc signature hover + calc_def keyword mapping"
      contains: "calc def"
  key_links:
    - from: "lsp/src/semantic_tokens.rs"
      to: "calc_def arm"
      via: "copy of the constraint_def emit_keyword arm (:277)"
      pattern: "calc_def"
    - from: "lsp/src/hover.rs calc signature"
      to: "resolved calc_def node (params + return type + contract)"
      via: "kind_to_keyword + signature markdown"
      pattern: "calc def"
---

<objective>
Wave 5: surface calc/constraint in the editor at rich depth (D-04) — tree-sitter-deal highlighting, vscode-deal TextMate scopes, and LSP semantic tokens + signature/return-type hover + go-to-def. This wave depends only on Wave 2 (the AST shape is fixed once `05.2-03` lands) and is NOT gated by the showcase parse oracle, so it runs in parallel with Plan 04 (wave 3, no file overlap). It MUST define a clean seam so Phase 6's broader LSP build-out does not redo this work.

Purpose: D-04 rich editor surfaces for calc/constraint, with a documented Phase 6 handoff boundary.

Output: tree-sitter grammar + highlights + corpus; vscode TextMate keywords; LSP calc_def semantic token + calc signature hover + go-to-def; Phase 6 seam documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-CONTEXT.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-PATTERNS.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-RESEARCH.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-VALIDATION.md
@lsp/src/semantic_tokens.rs
@lsp/src/hover.rs

<interfaces>
<!-- Anchors extracted from PATTERNS.md / RESEARCH.md (live-verified). -->
- tree-sitter-deal/grammar.js:198–214 element_keyword choice (add `token(prec(2, seq('calc', /\s+/, 'def')))` arm); :186–193 element_def rule (analog for calc_definition)
- lsp/src/semantic_tokens.rs:277–279 constraint_def arm → copy verbatim for calc_def (emit_keyword, "calc def".len(), MOD_DECLARATION)
- lsp/src/hover.rs:174–193 kind_to_keyword map (:183 "constraint_def" => "constraint def"; add "calc_def" => "calc def")
- lsp/src/definition.rs:92–125 candidate_path_at / collect_containing — basic go-to-def already resolves any indexed definition; NO new code needed for calc/constraint invocation go-to-def (the Index already populates calc_def during the definition scan)
- vscode-deal/syntaxes/deal.tmLanguage.json — keyword patterns covering part/constraint/requirement (add calc/return/require)
- Phase 6 seam (RESEARCH § "Phase 6 LSP Seam (D-04)"): Wave 5 adds calc/constraint-specific ARMS to existing handlers; it does NOT refactor infrastructure. Phase 6 extends the same arm, not replaces it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tree-sitter-deal grammar + highlights.scm + corpus for calc/constraint/require/return/±</name>
  <files>tree-sitter-deal/grammar.js, tree-sitter-deal/queries/highlights.scm, tree-sitter-deal/test/corpus/calc_constraint.txt</files>
  <read_first>
    - tree-sitter-deal/grammar.js (element_keyword 198–214; element_def 186–193; existing parameter/action-def rules)
    - tree-sitter-deal/queries/highlights.scm (existing keyword/operator highlight patterns)
    - tree-sitter-deal/test/corpus/ (existing corpus format)
    - .planning/phases/05.2-.../05.2-PATTERNS.md § "Wave 5: tree-sitter-deal/grammar.js" (copy-ready rules)
    - .planning/phases/05.2-.../05.2-RESEARCH.md § "tree-sitter-deal"
  </read_first>
  <action>
    In `tree-sitter-deal/grammar.js`: add `token(prec(2, seq('calc', /\s+/, 'def')))` to the `element_keyword` choice (analog: the `constraint def` arm at :210). Add structural rules `calc_definition`, `parameter_list`, `parameter_decl` (with optional `direction_keyword` in/out/inout), `return_contract` (`=>` + comma-sep `contract_item`), `contract_item` (`sig` INTEGER | `plus_minus` expression | qualified_name ConstraintRef [+ argument_list]), `plus_minus: _ => choice('±', '+/-')`, and `require_statement: $ => seq('require', $.expression, ';')`. Copy the exact rule shapes from PATTERNS.md § Wave 5.

    In `tree-sitter-deal/queries/highlights.scm`: highlight `calc def` keyword (`@keyword.control.element.deal`), `require`/`return` (`@keyword.control.deal`), `plus_minus` (`@operator.deal`), and `sig` ONLY inside `return_contract`/`precision_spec` (contextual — match within the contract node so a bare identifier `sig` elsewhere is not highlighted, honoring D-07).

    Add a `tree-sitter-deal/test/corpus/calc_constraint.txt` corpus file covering: a pure calc (MarginOfSafety), a calc with `=> ± percent(1), PositiveForce` and `=> sig 4`, and a multi-require constraint (ThermalWindow). Recompile with the pinned `tree-sitter-cli` 0.25.10.
  </action>
  <verify>
    <automated>cd tree-sitter-deal && npx tree-sitter generate && npx tree-sitter test</automated>
  </verify>
  <acceptance_criteria>
    - `tree-sitter generate && tree-sitter test` passes including the new `calc_constraint.txt` corpus
    - `grep -c 'calc_definition' tree-sitter-deal/grammar.js` ≥ 1 and `grep -c 'plus_minus' tree-sitter-deal/grammar.js` ≥ 1
    - highlights.scm highlights `sig` only within the return-contract context (source assertion: the `sig` capture is nested under return_contract/precision_spec, not a top-level identifier capture)
  </acceptance_criteria>
  <done>tree-sitter parses + highlights calc/constraint/require/return/±; corpus green; sig is contextual.</done>
</task>

<task type="auto">
  <name>Task 2: LSP calc_def semantic token + calc signature hover + go-to-def; vscode-deal TextMate keywords; Phase 6 seam doc</name>
  <files>lsp/src/semantic_tokens.rs, lsp/src/hover.rs, lsp/src/definition.rs, vscode-deal/syntaxes/deal.tmLanguage.json</files>
  <read_first>
    - lsp/src/semantic_tokens.rs (constraint_def arm 277–279)
    - lsp/src/hover.rs (kind_to_keyword 174–193, esp. :183)
    - lsp/src/definition.rs (candidate_path_at / collect_containing 92–125)
    - vscode-deal/syntaxes/deal.tmLanguage.json (keyword patterns)
    - .planning/phases/05.2-.../05.2-PATTERNS.md § "Wave 5: lsp/src/*" (exact copy patterns) + § "No Analog Found"
    - .planning/phases/05.2-.../05.2-RESEARCH.md § "LSP" + "Phase 6 LSP Seam (D-04)"
  </read_first>
  <action>
    In `lsp/src/semantic_tokens.rs`: add a `"calc_def"` arm copying the constraint_def arm verbatim (`emit_keyword(node, "calc def".len(), MOD_DECLARATION, out)`).

    In `lsp/src/hover.rs`: add `"calc_def" => "calc def"` to `kind_to_keyword` (:183 analog). Add calc signature hover: when the hover target resolves to a `calc_def` node, render a markdown `deal`-fenced block with the calc's signature — `calc def Name(in p1 : T1, in p2 : T2) : ReturnType => <contract>` — plus its doc comment if present. Pull param direction/name/type from the param_decl nodes, the return type from type_node, and the contract from the return_contract.

    In `lsp/src/definition.rs`: confirm go-to-def already resolves calc/constraint invocations (the Index populates calc_def/constraint_def during the definition scan; `pathstring_from_identifier` resolves the call callee). No new code is required for basic go-to-def; ADD a `pathstring_from_param_decl` helper ONLY if needed to make calc-invocation go-to-def land on the definition — otherwise document that the existing mechanism covers it (this is the Phase 6 seam point for richer "jump to parameter" go-to-def).

    In `vscode-deal/syntaxes/deal.tmLanguage.json`: add `calc`/`return`/`require` to the keyword patterns that already handle `part`/`constraint`/`requirement`.

    Document the Phase 6 LSP seam (D-04) in the SUMMARY and inline near the new arms: Phase 05.2 adds exactly the calc/constraint arms to the already-existing semantic-token/hover/definition handlers; it does NOT refactor the handler infrastructure. Phase 6's broader LSP build-out (workspace-wide index enrichment, cross-file completion for all kinds, arbitrary-kind hover) extends the SAME arm rather than replacing it.
  </action>
  <verify>
    <automated>cargo test -p deal-lsp</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p deal-lsp` passes, including a semantic-token test asserting the `calc_def` arm emits a keyword token
    - `grep -c 'calc_def' lsp/src/semantic_tokens.rs` ≥ 1 and `grep -c 'calc def' lsp/src/hover.rs` ≥ 1
    - `cargo test --workspace` green (no regression to existing LSP tests)
    - vscode-deal/syntaxes/deal.tmLanguage.json contains `calc`, `return`, `require` keyword entries
    - The Phase 6 LSP seam is documented in 05.2-06-SUMMARY.md (handoff boundary explicit)
  </acceptance_criteria>
  <done>LSP emits calc_def tokens, shows calc signature hover, resolves go-to-def; vscode highlights the new keywords; the Phase 6 seam is documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: VS Code highlighting smoke (manual)</name>
  <files>(none — visual verification only)</files>
  <what-built>tree-sitter + vscode-deal TextMate + LSP semantic-token/hover/go-to-def support for calc/constraint/require/return/±.</what-built>
  <action>Pause for human visual verification AFTER Tasks 1–2 have automated their work (this is the VALIDATION.md Manual-Only row 2: VS Code highlighting render cannot be unit-tested). Present the how-to-verify steps and wait for the resume signal. Do not auto-advance.</action>
  <how-to-verify>
    1. Open `tests/showcase/packages/analysis/calcs.deal` in VS Code with the vscode-deal extension active.
    2. Confirm `calc def` is highlighted as a keyword, `return` and `require` are highlighted, and `±` is highlighted as an operator.
    3. Hover over the `Drag` calc — confirm the signature (params + return type + `=> ± percent(1), PositiveForce` contract) appears.
    4. Open `constraints.deal`; in a file that invokes `PositiveForce`, ctrl/cmd-click the invocation and confirm go-to-def jumps to the `constraint def`.
    5. Confirm a plain identifier named `sig` (if present anywhere) is NOT highlighted as a keyword (D-07 contextual).
  </how-to-verify>
  <verify>
    <human-check>Reviewer confirms keyword + operator highlighting, calc signature hover, go-to-def, and contextual sig in VS Code.</human-check>
  </verify>
  <done>Human reviewer types "approved" after confirming highlighting, hover, go-to-def, and contextual sig render correctly.</done>
  <resume-signal>Type "approved" or describe the highlighting/hover/go-to-def issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor source buffer → tree-sitter/LSP | Untrusted in-editor source (incl. partial/malformed calc defs) is parsed for highlighting + tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-W5-01 | DoS | malformed calc def in editor crashes tree-sitter/LSP | mitigate | tree-sitter is error-tolerant by design (ERROR nodes, no panic); LSP arms only read resolved nodes and fall through on unknown kinds — no new unbounded recursion |
| T-05.2-W5-02 | Tampering | `sig` highlighted as keyword everywhere (D-07 violation) | mitigate | highlights.scm captures `sig` only nested under return_contract/precision_spec; manual smoke step 5 confirms a bare `sig` identifier is not keyword-colored |
| T-05.2-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; tree-sitter-cli pinned 0.25.10 (T-3-03), no version change (RESEARCH audit: none) |
</threat_model>

<verification>
- `tree-sitter generate && tree-sitter test` green.
- `cargo test -p deal-lsp` + `cargo test --workspace` green.
- Manual VS Code smoke approved.
</verification>

<success_criteria>
- tree-sitter + vscode + LSP surface calc/constraint/require/return/± at rich depth (D-04).
- sig stays contextual in highlighting (D-07).
- Phase 6 LSP seam documented (no infrastructure refactor; arms only).
</success_criteria>

<output>
Create `.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-06-SUMMARY.md` when done.
</output>
