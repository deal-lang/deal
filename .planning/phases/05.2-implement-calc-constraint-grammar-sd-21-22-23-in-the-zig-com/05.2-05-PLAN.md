---
phase: 05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com
plan: 05
type: execute
wave: 4
depends_on: ["05.2-04", "05.2-06"]
files_modified:
  - src/ir.zig
  - src/lowering.zig
  - spec/ir/v0/schema.json
  - cli/src/sysml_v2.rs
  - tests/golden/sysml-v2/calcs_sysml.json
  - tests/golden/sysml-v2/constraints_sysml.json
  - build.zig
autonomous: true
requirements: [REQ-05.2-D06, REQ-05.2-D09]
must_haves:
  truths:
    - "AST calc_def lowers to an IR calc_def node carrying directed params {name,type,direction}, return_type, a generic precision{kind,value} slot, and an is_statement_valued purity flag"
    - "The generic precision{kind,value} slot is attached at ALL THREE IR sites: calc return, attribute/feature value, and require threshold (D-06/D-08) — one shared schema definition, not three node kinds"
    - "spec/ir/v0/schema.json validates the calc_def IR node (calc_def in the NodeKind enum + CalcDefPayload $defs) AND references the shared precision $def from the calc-return, attribute-value, and require-threshold payloads"
    - "constraint_def lowers via the new ConstraintDefinition struct (not the removed ElementDef alias)"
    - "calc_def emits KerML Function (directed params + result feature); constraint_def emits KerML Predicate (not emit_generic_element)"
    - "IR calc_def directed-params shape coincides exactly with deal-sim's I/O→KerML mapping: in-params == sim inputs, out-params == sim outputs, order preserved (D-12)"
    - "Golden fixtures for calcs.deal and constraints.deal match byte-for-byte"
    - "phase-05.2-gate (+ -fresh) is green: all 21+ showcase files parse, IR validates, goldens match, E26xx corpus green, fmt idempotent; tree-sitter corpus green and the Plan 06 manual VS Code smoke is approved"
  artifacts:
    - path: "spec/ir/v0/schema.json"
      provides: "calc_def NodeKind + CalcDefPayload schema + a shared precision $def referenced at calc-return/attribute-value/require-threshold sites (D-06/D-08)"
      contains: "calc_def"
    - path: "cli/src/sysml_v2.rs"
      provides: "emit_calc_def (Function) + emit_constraint_def (Predicate)"
      contains: "emit_calc_def"
    - path: "build.zig"
      provides: "phase-05.2-gate + phase-05.2-gate-fresh build steps"
      contains: "phase-05.2-gate"
  key_links:
    - from: "src/lowering.zig lowerCalcDef"
      to: "IrCalcDefPayload {params[], return_type, precision, is_statement_valued}"
      via: "AST calc_def → IR calc_def directed-feature shape (D-12)"
      pattern: "IrParam"
    - from: "src/lowering.zig lowerAttributeUsage / lowerRequireStatement"
      to: "shared IrPrecision slot"
      via: "ElementUsage.precision / RequireStatement.precision → IR precision{kind,value} (D-06/D-08)"
      pattern: "IrPrecision"
    - from: "cli/src/sysml_v2.rs dispatch"
      to: "emit_calc_def / emit_constraint_def"
      via: "calc_def → Function, constraint_def → Predicate (removed from emit_generic_element arm)"
      pattern: "emit_calc_def"
---

<objective>
Wave 4: lower the calc/constraint AST into IR and emit KerML. Add a `calc_def` IR node whose directed-param shape is deliberately identical to how a deal-sim's I/O contract maps to KerML (D-12 — so a sim can later collapse into a calc with zero representational rework), extend `spec/ir/v0/schema.json` with ONE generic `precision{kind,value}` slot definition that is referenced at ALL THREE attach points (calc return, attribute value, require threshold — D-06/D-08), and replace `constraint_def → emit_generic_element` with dedicated `emit_constraint_def` (Predicate) + `emit_calc_def` (Function). Install the phase gate (depends on both the Wave-3 sema plan and the Wave-5 editor plan, so the gate's tree-sitter + manual-smoke acceptance is in scope).

Purpose: Close the pipeline end-to-end (parse → check → IR → SysML/KerML) and lock it with golden fixtures + the phase-05.2-gate.

Output: IR calc_def + IrCalcDefPayload; schema.json extension with a shared precision $def at three sites; lowerCalcDef/lowerConstraintDef + precision lowering on attribute/require; sysml_v2.rs emit_calc_def/emit_constraint_def; golden fixtures; phase-05.2-gate(-fresh).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-CONTEXT.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-PATTERNS.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-RESEARCH.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-VALIDATION.md
@src/ir.zig
@src/lowering.zig
@cli/src/sysml_v2.rs
@spec/ir/v0/schema.json

<interfaces>
<!-- Anchors extracted from PATTERNS.md / RESEARCH.md (live-verified). -->
- ir.zig:28–56 NodeKind enum (constraint_def at :37; add calc_def after, alphabetical)
- lowering.zig:644–662 elemDefOf (constraint_def removed in Wave 2; constraintDefOf() helper added); :688–706 astKindToIrKind (:701 constraint_def => .constraint_def; add .calc_def => .calc_def); :193–238 pass2LowerDef (add calc_def + constraint_def branches before generic path)
- lowering.zig attribute/require lowering — wherever ElementUsage and RequireStatement lower, map the `.precision` AST slot (added Wave 2) into the shared IrPrecision slot (D-06)
- cli/src/sysml_v2.rs:168–194 dispatch match (:190 constraint_def currently in the emit_generic_element arm — REMOVE it); :443–455 emit_requirement_def (Function/Predicate analog); :199–210 base_fields; :212–234 owned_member_refs
- IrCalcDefPayload {params: []IrParam, return_type: ?[]const u8, precision: ?IrPrecision, is_statement_valued: bool}; IrParam {name, type_name, direction:"in"|"out"|"inout"}; IrPrecision {kind, value} — full shapes in PATTERNS.md § "Wave 4: src/ir.zig". IrPrecision is the SINGLE shared shape reused at all three attach points (D-08), never duplicated per site.
- D-12 shape (RESEARCH § "D-12: IR Shape for deal-sim Compatibility" + Open Q3 RESOLVED): preserve per-parameter directional structure as an ORDERED JSON array of {name,type,direction} (matching KerML directed features); do NOT collapse in/out into a single inputs+outputs dict. deal-sim Shape A input = flat {key:value} (src/sim/deal_sim.zig:72–73,155–200) — in-params == its inputs, out-params == its outputs.
- KerML mapping (design §7): calc_def → Function (directed in params + result feature); constraint_def → Predicate (Boolean Function); precision → metadata feature.
- Golden infra: tests/golden/sysml-v2/ + cargo golden test (cli/tests/).
- Gate naming: build.zig phase-2-gate (:317) / phase-2-gate-fresh (:332) are the exact step analogs; verify-fresh-worktree.sh takes a gate-step argument.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IR calc_def node + IrCalcDefPayload; lowerCalcDef/lowerConstraintDef; lower the precision slot at all three sites; extend schema.json (D-06/D-08/D-12)</name>
  <read_first>
    - src/ir.zig (NodeKind enum 28–56; closest IrPayload struct shape)
    - src/lowering.zig (elemDefOf 644–662; astKindToIrKind 688–706; pass2LowerDef 193–238; ElementUsage + RequireStatement lowering sites)
    - spec/ir/v0/schema.json (existing NodeKind enum array + $defs structure)
    - src/sim/deal_sim.zig (lines 72–73, 155–200 — Shape A input/output envelope for the D-12 alignment)
    - .planning/phases/05.2-.../05.2-PATTERNS.md § "Wave 4: src/ir.zig" + "Wave 4: src/lowering.zig"
    - .planning/phases/05.2-.../05.2-RESEARCH.md § "Wave 4" + "D-12: IR Shape for deal-sim Compatibility" + Pitfall 7 + Open Q3 (RESOLVED)
  </read_first>
  <action>
    In `src/ir.zig`: add `calc_def` to `ir.NodeKind` after `constraint_def` (:37, alphabetical). Add `IrCalcDefPayload {params: []IrParam, return_type: ?[]const u8, precision: ?IrPrecision, is_statement_valued: bool}`, `IrParam {name, type_name, direction}`, `IrPrecision {kind, value}` — copy field lists from PATTERNS.md § Wave 4. `IrPrecision` is the SINGLE shared precision shape (D-08); it is referenced by the calc-return payload AND by the attribute/feature-value payload AND by the require-threshold payload — never duplicated as three distinct types.

    In `src/lowering.zig`: add `.calc_def => .calc_def` to astKindToIrKind (:701 analog). In pass2LowerDef (:193–238), add a dedicated `lowerCalcDef` branch and a `lowerConstraintDef` branch BEFORE the generic elemDefOf path (constraint_def is no longer an ElementDef after Wave 2). `lowerCalcDef` builds the IrParam slice from the param_list, mapping each param_decl's direction to "in"/"out"/"inout" via `directionStr`; sets `is_statement_valued` true iff any param is out/inout (purity flag, D7); builds the `precision` slot from the return_contract if present (D-08 generic slot, value carried not enforced).

    GENERALIZED PRECISION LOWERING (D-06 — locked scope): also lower the `.precision` AST slot at the attribute/feature-value and require-threshold sites into the SAME `IrPrecision` slot. Where `ElementUsage` lowers (attribute/feature value), if `ElementUsage.precision` is non-null, populate the lowered node's `precision: ?IrPrecision`. Where `RequireStatement` lowers, if `RequireStatement.precision` is non-null, populate the lowered require node's `precision: ?IrPrecision`. Same `IrPrecision` shape at all three sites — carried, not enforced.

    D-12 SHAPE GUARANTEE: store params as an ORDERED array of {name, type_name, direction}. Do NOT collapse in/out into a single inputs+outputs dict — the per-parameter directional structure must coincide exactly with deal-sim's I/O→KerML mapping (in-params == sim inputs, out-params == sim outputs, order preserved) so a sim collapses into a calc with zero rework.

    In `spec/ir/v0/schema.json`: add `"calc_def"` to the NodeKind enum array AND a `CalcDefPayload` `$defs` section (params array of {name,type,direction}, return_type, generic precision, is_statement_valued). Define ONE shared `Precision` `$def` ({kind, value}) and `$ref` it from the calc-return payload, the attribute/feature-value payload, AND the require-statement payload — so the generic precision slot is attached at all three sites via one definition (D-06/D-08). Extend the constraint_def payload schema with `params` + `requires` arrays, and the attribute-usage + require payloads with the optional precision `$ref` (Pitfall 7 — the Zig code compiles regardless, but schema validation fails if calc_def is absent from the enum).
  </action>
  <verify>
    <automated>zig build && zig build test -Dtest-filter=determinism_lower_twice</automated>
  </verify>
  <acceptance_criteria>
    - `zig build` compiles; `zig build test -Dtest-filter=determinism_lower_twice` passes with calcs.deal + constraints.deal + precision.deal added to its file list (IR lowering byte-deterministic across two runs)
    - `grep -c '"calc_def"' spec/ir/v0/schema.json` ≥ 1 (NodeKind enum entry) AND the schema has a CalcDefPayload $defs section
    - The schema defines ONE shared precision $def and `$ref`s it from at least the calc-return, attribute-value, and require payloads (`grep -c '#/$defs/Precision\|precision' spec/ir/v0/schema.json` ≥ 3 — D-06/D-08 shared-slot-at-three-sites source assertion)
    - IR JSON for calcs.deal stores params as an ordered array of {name,type,direction} (NOT a collapsed inputs/outputs dict) — D-12, asserted in the lowering test or a probe
    - IR JSON for precision.deal carries the precision slot on the attribute-value and require nodes (D-06, asserted in the lowering test or a probe)
    - `grep -c 'is_statement_valued' src/ir.zig` ≥ 1 (purity flag carried into IR)
  </acceptance_criteria>
  <done>IR calc_def node + payload land; the shared precision slot is lowered at all three sites; schema validates; lowering is deterministic and D-12-shaped.</done>
</task>

<task type="auto">
  <name>Task 2: sysml_v2.rs emit_calc_def (Function) + emit_constraint_def (Predicate) + golden fixtures</name>
  <read_first>
    - cli/src/sysml_v2.rs (dispatch match 168–194, esp. :190 constraint_def in emit_generic_element arm; emit_requirement_def 443–455; base_fields 199–210; owned_member_refs 212–234)
    - tests/golden/sysml-v2/ (existing golden fixtures + the cargo golden test format)
    - .planning/phases/05.2-.../05.2-PATTERNS.md § "Wave 4: cli/src/sysml_v2.rs" + § "Shared Patterns 4"
    - .planning/phases/05.2-.../05.2-RESEARCH.md § "SysML v2 Codegen" + "Golden Fixtures"
    - `git -C spec show <merge>:grammar/2026-06-08-calc-constraint-grammar-design.md` §7 (KerML mapping)
  </read_first>
  <action>
    In `cli/src/sysml_v2.rs`: REMOVE `"constraint_def"` from the `emit_generic_element` arm (:190) and add two dedicated dispatch arms: `"constraint_def" => emit_constraint_def(node)` and `"calc_def" => emit_calc_def(node, outgoing, node_map, all_edges)`.
      - `emit_calc_def`: `base_fields(node.id, "Function")` (KerML Function per design §7); build `ownedMembership` from directed-param members via `owned_member_refs`; add `result` feature for the return type; if a precision slot is present on the payload, attach it as a `metadata` feature. Preserve directed-feature ORDER (D-12).
      - `emit_constraint_def`: `base_fields(node.id, "Predicate")` (KerML Boolean Function per design §7); `dealKind = "constraint_def"`.

    Create golden fixtures `tests/golden/sysml-v2/calcs_sysml.json` (MarginOfSafety/Drag/KineticEnergy as Functions) and `constraints_sysml.json` (PositiveForce/BatteryLimits/ThermalWindow as Predicates). Generate them via the existing golden-generation mechanism (do NOT hand-author byte-fragile JSON if a generator exists); commit the byte-stable output and add/extend a `cargo test` golden assertion (e.g. `cli/tests/sysml_v2_calcs.rs`).
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace` passes, including the new golden assertion for calcs.deal + constraints.deal
    - `grep -c 'emit_calc_def' cli/src/sysml_v2.rs` ≥ 1 and `grep -c 'emit_constraint_def' cli/src/sysml_v2.rs` ≥ 1
    - `constraint_def` no longer appears in the `emit_generic_element` dispatch arm (source assertion: the :190 arm no longer lists constraint_def)
    - Golden fixtures `tests/golden/sysml-v2/calcs_sysml.json` and `constraints_sysml.json` exist and match byte-for-byte (Function for calc, Predicate for constraint)
  </acceptance_criteria>
  <done>calc_def emits a KerML Function, constraint_def a Predicate; goldens match byte-for-byte.</done>
</task>

<task type="auto">
  <name>Task 3: Add phase-05.2-gate (+ -fresh) and verify the full phase end-to-end</name>
  <read_first>
    - build.zig (phase-2-gate :317 / phase-2-gate-fresh :332; phase-5-gate step; phase-05.2-wave0-gate from Plan 01)
    - scripts/verify-fresh-worktree.sh (gate-step argument routing)
    - .planning/phases/05.2-.../05.2-VALIDATION.md § "Phase Gate (all waves)"
  </read_first>
  <action>
    Add a `phase-05.2-gate` build step in `build.zig` (analog: phase-2-gate at :317) that runs, in order:
    1. `zig build phase-5-gate` (all prior pipeline regression).
    2. `deal parse tests/showcase/packages/analysis/calcs.deal` — exit 0.
    3. `deal parse tests/showcase/packages/analysis/constraints.deal` — exit 0.
    4. `deal parse tests/showcase/packages/analysis/precision.deal` — exit 0 (D-06 attribute-value + require-value precision).
    5. `zig build test` (all new Zig unit tests: lexer_calc, sema_calc, recovery_corpus m25/m26/m27, parser/lowering determinism).
    6. `cargo test --workspace` (LSP + CLI + golden).
    7. The tree-sitter corpus from Plan 06: `cd tree-sitter-deal && npx tree-sitter generate && npx tree-sitter test` — the calc/constraint corpus is green (editor-surface regression in the gate).
    8. `deal fmt tests/showcase/packages/analysis/calcs.deal | diff - tests/showcase/packages/analysis/calcs.deal` AND the same for precision.deal — fmt is a no-op on canonical source (idempotency at all precision attach points).

    Add `phase-05.2-gate-fresh` shelling out to `scripts/verify-fresh-worktree.sh phase-05.2-gate` (analog: phase-2-gate-fresh at :332). Run both and confirm green.

    NOTE (Plan 06 dependency): this plan depends_on `05.2-06`, so Plan 06's tree-sitter corpus and LSP arms exist when the gate runs. Plan 06 Task 3's manual VS Code highlighting smoke is a blocking-human checkpoint; it MUST be approved before the phase gate is considered complete (the gate automates everything except that single visual render, which VALIDATION.md Manual-Only row 2 documents). Record the manual-smoke approval in the SUMMARY alongside the gate result.
  </action>
  <verify>
    <automated>zig build phase-05.2-gate && zig build phase-05.2-gate-fresh</automated>
  </verify>
  <acceptance_criteria>
    - `zig build phase-05.2-gate` exits 0 (all 21+ showcase parse incl. precision.deal, new unit tests green, cargo workspace green, tree-sitter corpus green, fmt idempotent)
    - `zig build phase-05.2-gate-fresh` exits 0 (same, inside an ephemeral worktree with submodule init against the pushed spec merge)
    - `grep -v '^#' build.zig | grep -c 'phase-05.2-gate'` ≥ 1
    - Plan 06 Task 3 manual VS Code smoke approval is recorded in 05.2-05-SUMMARY.md (the only non-automated phase-gate input)
  </acceptance_criteria>
  <done>The phase gate is green in both the main checkout and a fresh ephemeral worktree; the Plan 06 manual smoke is approved and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IR JSON → spec/ir/v0 schema validation | Lowered IR must conform to the published schema or downstream consumers reject it |
| IR → SysML/KerML emit | calc/constraint nodes cross into the Rust codegen tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-W4-01 | Tampering | calc_def IR omitted from schema.json NodeKind enum | mitigate | `cargo test --workspace` schema-validation step on the lowered calcs.deal IR fails fast if calc_def is missing from the enum (Pitfall 7); grep gate asserts presence |
| T-05.2-W4-02 | Tampering | constraint_def silently stays mistyped as generic element in KerML | mitigate | Dispatch arm REMOVED from emit_generic_element; golden fixture asserts Predicate type byte-for-byte |
| T-05.2-W4-03 | Tampering | gate passes in main checkout but fails fresh (unpushed dep) | mitigate | `phase-05.2-gate-fresh` runs in an ephemeral worktree with submodule init — inherits the Wave-0 push guarantee |
| T-05.2-W4-04 | Tampering | precision slot diverges in shape across the three attach points | mitigate | One shared `Precision` $def `$ref`d from all three payloads; schema validation rejects a malformed/duplicated slot (D-08) |
| T-05.2-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH audit: none) |
</threat_model>

<verification>
- `zig build && zig build test -Dtest-filter=determinism_lower_twice` green.
- `cargo test --workspace` green incl. new goldens.
- `zig build phase-05.2-gate` + `phase-05.2-gate-fresh` exit 0; Plan 06 manual smoke approved.
</verification>

<success_criteria>
- IR calc_def + generic precision slot (D-08) land; the slot is attached at all three sites (calc return, attribute value, require threshold — D-06) via one shared $def; D-12 directed-param shape preserved; schema validates.
- calc_def → Function, constraint_def → Predicate; goldens byte-match.
- phase-05.2-gate green in main + fresh worktree; Plan 06 tree-sitter corpus green and manual smoke approved.
</success_criteria>

<output>
Create `.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-05-SUMMARY.md` when done.
</output>
