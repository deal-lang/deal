---
phase: 05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com
plan: 03
type: execute
wave: 2
depends_on: ["05.2-02"]
files_modified:
  - src/ast.zig
  - src/json.zig
  - src/fmt.zig
  - src/parser_deal.zig
  - src/sema.zig
  - src/lowering.zig
  - tests/snapshots/ast/
autonomous: true
requirements: [REQ-05.2-D04, REQ-05.2-D06, REQ-05.2-D08]
must_haves:
  truths:
    - "`deal parse tests/showcase/packages/analysis/calcs.deal` exits 0 (parse oracle GREEN)"
    - "`deal parse tests/showcase/packages/analysis/constraints.deal` exits 0 (parse oracle GREEN)"
    - "`deal parse tests/showcase/packages/analysis/precision.deal` exits 0 — attribute-value precision (`attribute mass : Mass = kg(5) => sig 4;`) and require-value precision parse via the SAME ReturnContract/PrecisionSpec nodes (D-06 generalized, D-08 one generic slot)"
    - "`sig` after `=>` is recognized as a precision keyword contextually; `sig` elsewhere stays an ordinary identifier (D-07)"
    - "calc bodies parse as LocalBinding* + ReturnStatement; an out-param assignment-style body is a clean parse error E2602 (D-11)"
    - "parse → fmt → parse round-trip is byte-identical AST for all three oracle files (calcs, constraints, precision)"
    - "The ConstraintDef = ElementDef alias is replaced by ConstraintDefinition; all 5 dispatch sites updated atomically and the build compiles"
  artifacts:
    - path: "src/ast.zig"
      provides: "9 new NodeKinds + payload structs; ConstraintDefinition replacing the ElementDef alias; a generic `precision: ?*Node` slot on ElementUsage AND RequireStatement (D-06/D-08, no new node kinds)"
      contains: "calc_def"
    - path: "src/parser_deal.zig"
      provides: "parseCalcDef + parseConstraintDef + parseParamList/parseParamDecl/parseReturnContract/parseCalcBody/parseConstraintBody/parseRequireStatement; generalized `=> ReturnContract` suffix on parseAttributeUsage and parseRequireStatement (D-06)"
      contains: "parseCalcDef"
  key_links:
    - from: "src/parser_deal.zig dispatch (~line 926)"
      to: "parseCalcDef / parseConstraintDef"
      via: ".kw_calc / .kw_constraint dispatch arms"
      pattern: "parseCalcDef"
    - from: "parseReturnContract"
      to: "contextual sig recognition"
      via: "text comparison of .ident token after .arrow"
      pattern: "\"sig\""
    - from: "parseAttributeUsage (parser_deal.zig:1444) + parseRequireStatement"
      to: "parseReturnContract (shared)"
      via: "optional `=> ReturnContract` suffix before the terminating `;` on result-valued declarations (D-06)"
      pattern: "parseReturnContract"
---

<objective>
Wave 2 (the array_literal analog wave): add the calc/constraint AST nodes and the parser that builds them, so all THREE oracle files parse with exit 0. This is the heaviest wave — it introduces 9 new NodeKinds, replaces the `ConstraintDef = ElementDef` alias with a real `ConstraintDefinition` struct (touching 5 dispatch sites atomically), adds the parse functions including contextual `sig` recognition (D-07) and the `=>` return contract, AND wires the SAME `=> ReturnContract` suffix onto attribute/feature-value declarations and require thresholds (D-06 generalized precision — one generic `precision` slot, NO new node kinds, D-08).

Purpose: The showcase parse oracle (calcs.deal + constraints.deal + precision.deal must exit 0) gates this wave. D-06 is a LOCKED user decision: precision attaches to ANY result-valued declaration (calc return, attribute/feature value, require threshold), not just calc returns. Mirror the 05.1 array_literal end-to-end add exactly, then reuse the ReturnContract/PrecisionSpec nodes at all three attach points.

Output: 9 NodeKinds + payload structs through ast/json/fmt; new parse functions; the generalized `=> ReturnContract` suffix on parseAttributeUsage + parseRequireStatement; updated 5 alias-break dispatch sites; all three oracle parses GREEN; AST snapshots regenerated; round-trip byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-CONTEXT.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-PATTERNS.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-RESEARCH.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-VALIDATION.md
@src/ast.zig
@src/parser_deal.zig

<interfaces>
<!-- Anchors extracted from PATTERNS.md (live-verified key invariants). -->
- ast.zig:80  constraint_def in NodeKind enum (insert calc_def + 8 more AFTER it)
- ast.zig:305 ConstraintDef = ElementDef alias (REPLACE with ConstraintDefinition struct)
- ast.zig:310–325 ElementUsage struct (shared by AttributeUsage at :329) — ADD `precision: ?*Node = null` field (defaulted, non-breaking; carries the generic D-08 precision slot for attribute/feature values, D-06)
- ast.zig:600 constraint_def: ConstraintDef in Payload union (becomes constraint_def: ConstraintDefinition; add 9 arms in matching enum order)
- ast.zig:138,567 array_literal NodeKind + ArrayLiteral struct — the end-to-end analog
- json.zig:685–761 nodekindName() exhaustive switch (add 9 arms); json.zig:590,1309,1667 constraint_def field-access helpers (ElementDef → ConstraintDefinition fields)
- fmt.zig:81–171 writeNode() exhaustive switch (add 9 arms); fmt.zig:404 writeElementDef analog; fmt.zig:767 writeArrayLiteral analog
- parser_deal.zig:914–959 definition dispatch switch; :926 parseElementDef(p, .constraint_def, ...) → parseConstraintDef; :2166,:2183 STATEMENT_SYNC sets (add .kw_calc)
- parser_deal.zig:1004–1099 parseRequirementDef/parseNeedDef (dedicated-function analog); :1130–1228 parseDefinitionBody (body-loop analog); :1636 parseOptionalTypeAnnotation; direction parse block ~903–908
- parser_deal.zig:1444–1487 parseAttributeUsage — default_value parsed at :1457–1460; the `;` is consumed at :1463. The `=> ReturnContract` suffix goes BETWEEN those (after default_value, before the `;`) — RESEARCH Open Q1 placement.
- lowering.zig:657 elemDefOf constraint_def arm (remove); :701 astKindToIrKind constraint_def arm
- sema.zig:826–861 checkDefinition / elemDefOf insertion point

The 5 alias-break dispatch sites (PATTERNS.md § "ConstraintDef = ElementDef Alias Replacement"):
  1. lowering.zig:657 elemDefOf — remove constraint_def; add constraintDefOf()
  2. lowering.zig:199 pass2LowerDef — add constraint_def branch (full lowering is Wave 4; this plan only keeps it compiling)
  3. sema.zig checkDefinition — dispatch on node.payload for .constraint_def
  4. parser_deal.zig:926 — parseElementDef → parseConstraintDef
  5. json.zig:590,1309,1667 — field access ElementDef → ConstraintDefinition
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 9 NodeKinds + payload structs; generic precision slot on ElementUsage + RequireStatement; replace ConstraintDef alias; close all exhaustive switches + 5 dispatch sites (compile-green)</name>
  <read_first>
    - src/ast.zig (NodeKind enum lines 59–154; ConstraintDef alias line 305; ElementUsage struct 310–325; Payload union lines 579–668; ArrayLiteral struct lines 564–569 as the analog)
    - src/json.zig (nodekindName lines 685–761; constraint_def helpers 590/1309/1667)
    - src/fmt.zig (writeNode lines 81–171; writeElementDef 404; writeArrayLiteral 767)
    - src/lowering.zig (elemDefOf 644–662; astKindToIrKind 688–706; pass2LowerDef 193–238)
    - src/sema.zig (checkDefinition / elemDefOf insertion 826–861)
    - .planning/phases/05.2-.../05.2-PATTERNS.md § "Wave 2: src/ast.zig", § "Shared Patterns 1, 2, 5"
    - .planning/phases/05.2-.../05.2-RESEARCH.md § "Common Pitfalls" (Pitfalls 1, 2, 3) + Open Q1 (attribute-value `=>` placement, RESOLVED)
  </read_first>
  <action>
    Add 9 NodeKinds in `ast.zig` NodeKind enum AFTER `constraint_def` (line 80), in this order: `calc_def`, `param_list`, `param_decl`, `return_contract`, `precision_spec`, `constraint_ref`, `constraint_body`, `calc_body`, `require_statement`. Add the matching payload structs (CalcDefinition, ParameterList, ParameterDecl, ReturnContract, PrecisionSpec+PrecisionKind enum{sig_figures,tolerance_absolute,tolerance_relative}, ConstraintRef, ConstraintBody, CalcBody, ReturnStatement, RequireStatement) — copy the exact struct field lists from PATTERNS.md § "Wave 2: src/ast.zig".

    GENERIC PRECISION SLOT (D-06/D-08 — one slot, NO new node kinds):
      - Add `precision: ?*Node = null` to the `ElementUsage` struct (ast.zig:310, shared by AttributeUsage at :329). It points to a `return_contract` node — the SAME node kind used by calc returns. Defaulted to null so all existing usage constructors stay valid (non-breaking).
      - Add a `precision: ?*Node = null` field to the new `RequireStatement` payload struct (points to a `return_contract` node — the require threshold/result-value precision, D-06).
      Do NOT introduce a new node kind for attribute/require precision — both reuse `return_contract` + `precision_spec` (D-08: one generic AST/IR precision slot shared by ALL attach points).

    REPLACE the `ConstraintDef = ElementDef` alias (line 305) with the new `ConstraintDefinition` struct (params?, specializes?, body, …). In the Payload union (line 600), change `constraint_def: ConstraintDef` to `constraint_def: ConstraintDefinition` and add the 9 new union arms in the SAME relative order as the enum (Pitfall 1 — enum order MUST equal union order; add both in one edit, build immediately).

    Close both exhaustive switches (Pitfall 2): add 9 `.calc_def => "calc_def"`-style arms to `json.zig:nodekindName`, and 9 stub `writeNode` arms to `fmt.zig` (real write logic lands in Task 2 of this plan, but stubs must compile — use a placeholder that emits a recognizable token or `return error.Unimplemented` temporarily, replaced before the oracle gate).

    Update all 5 alias-break dispatch sites so the build compiles (Pitfall 3 — compile-time enforcement catches a missed site):
      - lowering.zig:657 — remove `.constraint_def => |e| e` from elemDefOf; add a `constraintDefOf()` helper.
      - lowering.zig:199 (pass2LowerDef) — add an `if (node.kind == .constraint_def)` branch (full lowering deferred to Wave 4; here it just needs to not crash/compile).
      - sema.zig checkDefinition — switch on `node.payload` so `.constraint_def` is handled via the new struct, not elemDefOf.
      - json.zig:590/1309/1667 — update field access from ElementDef fields to ConstraintDefinition fields.
      - parser_deal.zig:926 dispatch arm is rewired in Task 2.

    No fenced code in this action — copy shapes from PATTERNS.md.
  </action>
  <verify>
    <automated>zig build</automated>
  </verify>
  <acceptance_criteria>
    - `zig build` compiles green (all exhaustive switches closed; all 5 alias-break sites updated)
    - `grep -c 'constraint_def: ConstraintDefinition' src/ast.zig` == 1 (alias replaced, source assertion)
    - `grep -c 'ConstraintDef = ElementDef' src/ast.zig` == 0
    - `grep -c 'precision: ?\*Node' src/ast.zig` ≥ 2 (generic precision slot on ElementUsage AND RequireStatement — D-06/D-08 source assertion, one shared node kind)
    - `grep -cE '\.(calc_def|param_list|param_decl|return_contract|precision_spec|constraint_ref|constraint_body|calc_body|require_statement) =>' src/json.zig` == 9 (nodekindName arms present)
  </acceptance_criteria>
  <done>All 9 NodeKinds + payloads exist, the generic precision slot is on ElementUsage + RequireStatement, the alias is replaced, every exhaustive switch and dispatch site compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Implement parse functions (calc/constraint defs, params, return contract w/ contextual sig, bodies) + generalized `=>` on attribute/require + fmt writeNode logic</name>
  <read_first>
    - src/parser_deal.zig (dispatch 914–959; parseRequirementDef/parseNeedDef 1004–1099; parseDefinitionBody 1130–1228; parseAttributeUsage 1444–1487; parseOptionalTypeAnnotation 1636; direction block ~903–908; STATEMENT_SYNC 2166/2183)
    - src/fmt.zig (writeElementDef 404; writeArrayLiteral 767)
    - .planning/phases/05.2-.../05.2-PATTERNS.md § "Wave 2: src/parser_deal.zig" + § "Wave 2: src/fmt.zig" (copy-ready parse + write excerpts)
    - .planning/phases/05.2-.../05.2-RESEARCH.md § "Parser Changes" + "FIRST-set disjointness confirmation" + Pitfall 6 (arrow disambiguation) + Open Q1 (attribute-value `=>` placement, RESOLVED)
    - `git -C spec show <merge>:grammar/2026-06-08-calc-constraint-grammar-design.md` §3 (13 productions), §5 (FIRST-set/disambiguation), §8 (worked examples that MUST parse)
  </read_first>
  <action>
    In `parser_deal.zig`: rewire dispatch (line 926) `.kw_constraint => parseConstraintDef(...)` and add `.kw_calc => parseCalcDef(...)`. Add `.kw_calc` to the STATEMENT_SYNC sets at 2166 and 2183 (DoS recovery — threat T-W2).

    Implement the parse functions (copy shapes from PATTERNS.md § Wave 2 parser excerpts):
      - `parseCalcDef`: consume `calc`, expect `def`, name, `parseParamList`, `parseTypeAnnotation` (`: ReturnType`), optional `parseReturnContract` if peek == `.arrow`, then `parseCalcBody`.
      - `parseConstraintDef`: consume `constraint`, expect `def`, name, optional `parseParamList` (if peek == `.l_paren`), `parseOptionalStructuralRelationship` (reuse existing `specializes`), `parseConstraintBody`.
      - `parseParamList` / `parseParamDecl`: reuse existing direction parse block (`kw_in`/`kw_out`/`kw_inout`, default `.in`, D-11 accepts out/inout), `parseTypeAnnotation`, `parseOptionalMultiplicity`.
      - `parseReturnContract`: consume `.arrow`; loop `ContractItem` (comma-separated). Contextual `sig` (D-07): when peek is `.ident` with text `"sig"`, parse PrecisionSpec(sig_figures, INTEGER). When peek is `.plus_minus`, parse PrecisionSpec(tolerance). When peek is `.plus`+`.slash`+`.minus` (3-token `+/-` ASCII alias, RESEARCH A1), consume the 3 tokens and parse as tolerance (fmt normalizes to ±). Otherwise `.ident` → `parseConstraintRef` (QualifiedName + optional argument list — reuse existing call-argument production, do NOT invent a new one).
      - `parseCalcBody`: `{` then LocalBinding* (kw_derived/kw_attribute → reuse parseMemberDeclaration) / AnnotationStatement, then exactly one ReturnStatement (kw_return) which must be last; if no return found, emit E2601 and synthesize a recovery node. An out-param assignment-style body is a clean parse error E2602 (D-11) — calc bodies are LocalBinding* + ReturnStatement only.
      - `parseConstraintBody`: `{` then RequireStatement* (kw_require) / LocalBinding / AnnotationStatement.

    GENERALIZED `=> ReturnContract` SUFFIX (D-06 — LOCKED scope, implement, do NOT defer):
      - `parseAttributeUsage` (parser_deal.zig:1444): after `default_value` is parsed (:1457–1460) and BEFORE the terminating `;` is consumed (:1463), add: `if (p.peek().tag == .arrow) usage.precision = try parseReturnContract(p);`. This is the attribute/feature-value precision attach point (`attribute mass : Mass = kg(5) => sig 4;`). Placement is before the `;` per RESEARCH Open Q1 (RESOLVED). It REUSES `parseReturnContract` — same node, no new kind. FIRST-set disjointness holds: `=>` cannot start an expression and only appears between the value and `;` (Open Q1 proof). Set the `precision` field on the `ElementUsage` before constructing the node.
      - `parseRequireStatement`: consume `require`, `parseConditionExpression` (reuse existing expression parser), then an OPTIONAL `=> ReturnContract` on the require RESULT value (`if (p.peek().tag == .arrow) precision = try parseReturnContract(p);`), then expect `;`. The precision binds to the require's result value only, never to bare sub-literals inside the condition (D-06). Store it in the new RequireStatement payload's `precision` field.
      - At BOTH new attach points, honor the FIRST-set disjointness obligation already in the plan (after `=>`: `{` → .dealx satisfy block is NOT valid here; `sig`/`±`/IDENT-ConstraintRef → precision). A `{` after `=>` on an attribute/require is a contract error, not a body.

    Arrow disambiguation (Pitfall 6): in `.deal` mode after `: Type` (calc) or after a value/condition (attribute/require), `=>` opens a ReturnContract whose FIRST set is `sig`/`±`/IDENT (never `{`). Confirm the parser is in `.deal` mode (the `.dealx` satisfy `=>` is a separate mode/start symbol).

    In `src/fmt.zig`: replace the Task-1 stub arms with real `writeCalcDef`/`writeConstraintDef`/`writeParamList`/`writeParamDecl`/`writeReturnContract`/`writePrecisionSpec`/`writeConstraintRef`/`writeConstraintBody`/`writeCalcBody`/`writeRequireStatement`. `writePrecisionSpec` ALWAYS emits `±` (U+00B1) for tolerance kinds, never `+/-` (D-09 normalization point). ALSO update `writeAttributeUsage`/`writeElementUsage` (fmt.zig:404 area) and `writeRequireStatement` to emit the optional `=> ReturnContract` suffix when the `precision` slot is non-null — so the generalized precision round-trips through `deal fmt` at the attribute and require attach points too, with `+/-`→`±` normalization at every site (D-06/D-09). `deal fmt` also normalizes a leading `in` direction prefix away on ParameterDecl (D-06 style normalization).
  </action>
  <verify>
    <automated>zig build && zig build test -Dtest-filter=parser_deal</automated>
  </verify>
  <acceptance_criteria>
    - `zig build` compiles; `zig build test -Dtest-filter=parser_deal` passes
    - `grep -c '"sig"' src/parser_deal.zig` ≥ 1 (contextual sig via text comparison — source assertion for D-07)
    - `grep -c 'E2602' src/parser_deal.zig` ≥ 1 (out-param assignment body is a parse error per D-11)
    - `grep -c 'parseReturnContract' src/parser_deal.zig` ≥ 3 (called from calc return, attribute value, AND require — D-06 generalized attach, source assertion)
    - The §8 worked examples (Drag with `=> ± percent(1), PositiveForce`, KineticEnergy with `=> sig 4`, ThermalWindow multi-require) PLUS the attribute-value precision form (`attribute mass : Mass = kg(5) => sig 4;`) and require-value precision parse without diagnostics (covered by the oracle gate in Task 3)
  </acceptance_criteria>
  <done>parseCalcDef/parseConstraintDef and all helpers build the new AST; the `=> ReturnContract` suffix is wired onto attribute values and require thresholds via the shared parseReturnContract (D-06); fmt emits canonical DEAL with ± normalization at all three attach points.</done>
</task>

<task type="auto">
  <name>Task 3: Oracle parse gate GREEN (3 files) + regenerate AST snapshots + round-trip determinism</name>
  <read_first>
    - tests/unit/parser_deal_snapshot.zig (existing snapshot harness)
    - tests/unit/determinism_parse_twice.zig (existing parse-twice byte-identical test)
    - tests/snapshots/ast/ (existing AST snapshots)
    - .planning/phases/05.2-.../05.2-VALIDATION.md § "Wave 2 — AST + Parser"
  </read_first>
  <action>
    Run the parse oracle on all THREE analysis files: `deal parse tests/showcase/packages/analysis/calcs.deal`, `…/constraints.deal`, AND `…/precision.deal` — all MUST exit 0 (parse oracle GREEN; this gates the wave). precision.deal exercises the D-06 attribute-value + require-value precision attach points landed in Task 2.

    Regenerate AST snapshots with `zig build test -Dtest-filter=parser_deal_snapshot -Dupdate-snapshots=true` (mechanism — do NOT hand-edit JSON), producing EXACTLY 3 NEW snapshots for calcs.deal + constraints.deal + precision.deal (the three analysis files added by Wave 0; the snapshot scan covers the analysis package). Confirm the 19 pre-existing AST snapshots remain byte-stable (no unintended drift from the ConstraintDef alias replacement or the new `precision` field on ElementUsage — the field is defaulted null, so an attribute WITHOUT a precision contract must serialize identically to before; any change to an existing snapshot must be reviewed and justified).

    Re-run `zig build test -Dtest-filter=parser_deal_snapshot` (no update flag) for stability, and `zig build test -Dtest-filter=determinism_parse_twice` (the determinism harness covers all showcase files, now including the three oracle files — asserts byte-identical AST JSON across two parses, the D-18 invariant). Round-trip: parse → fmt → parse on all three oracle files yields byte-identical AST (the attribute/require `=> ReturnContract` suffix must survive fmt).
  </action>
  <verify>
    <automated>zig build test -Dtest-filter=parser_deal_snapshot && zig build test -Dtest-filter=determinism_parse_twice</automated>
  </verify>
  <acceptance_criteria>
    - `deal parse tests/showcase/packages/analysis/calcs.deal` exits 0
    - `deal parse tests/showcase/packages/analysis/constraints.deal` exits 0
    - `deal parse tests/showcase/packages/analysis/precision.deal` exits 0 (D-06 attribute-value + require-value precision parse)
    - `zig build test -Dtest-filter=parser_deal_snapshot` passes; `git diff --stat tests/snapshots/ast/` shows EXACTLY 3 new snapshots for the analysis files (calcs, constraints, precision) and no unjustified change to the other 19
    - `zig build test -Dtest-filter=determinism_parse_twice` passes (byte-identical AST across two parses, including the three oracle files)
    - `zig build test && cargo test --workspace` green (wave-merge sampling)
  </acceptance_criteria>
  <done>All three oracle files parse exit 0; exactly 3 new AST snapshots; existing 19 stable; parse-twice determinism holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| token stream → parser | Untrusted/malformed calc-def token sequences cross into recursive-descent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-W2-01 | DoS | malformed calc/constraint body → parser panic | mitigate | `parseCalcBody`/`parseConstraintBody` else arms use the three-tier forward-progress guard (`syncToStatement` → advance on `semicolon`/`comma`, break on `r_brace`/`eof`, force-advance when position is unchanged), so the body loop cannot spin regardless of the encountered token; arena allocator bounds body-loop allocation (no unbounded recursion) — RESEARCH Security Domain row 1 + row 3. NOTE: `.kw_calc` is NOT in `STATEMENT_SYNC` (punctuation-only set); the forward-progress guard alone provides the panic/loop protection (secure-phase 05.2 audit, 2026-06-11). |
| T-05.2-W2-02 | Tampering | arrow `=>` disambiguation between calc/attr/require ReturnContract and .dealx satisfy | mitigate | Mode-checked: `.deal` parser only treats `=>` (after `: Type` for calc, after value/condition for attr/require) as ReturnContract; `{` is not a valid ContractItem FIRST token at any of the three attach points (Pitfall 6) |
| T-05.2-W2-03 | Tampering | ConstraintDef alias break leaves a stale dispatch site | mitigate | Zig exhaustive-switch + union compile-time enforcement catches any of the 5 missed sites (Pitfall 3); `zig build` is the gate |
| T-05.2-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH audit: none) |
</threat_model>

<verification>
- `zig build` compiles; all three oracle files parse exit 0.
- AST snapshots regenerated (exactly 3 new), existing 19 stable; parse-twice determinism holds.
- `zig build test && cargo test --workspace` green.
</verification>

<success_criteria>
- 9 NodeKinds + payloads land end-to-end through ast/json/fmt; alias replaced; 5 sites updated atomically.
- calcs.deal + constraints.deal + precision.deal parse exit 0 (oracle GREEN — gates the wave).
- Generalized `=> ReturnContract` precision suffix on attribute values + require thresholds via the SAME shared nodes (D-06, no new node kinds, D-08).
- Contextual sig (D-07), out-param-body parse error E2602 (D-11), ± fmt normalization at all attach points (D-09) all in place.
- Round-trip + determinism byte-identical.
</success_criteria>

<output>
Create `.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-03-SUMMARY.md` when done.
</output>
