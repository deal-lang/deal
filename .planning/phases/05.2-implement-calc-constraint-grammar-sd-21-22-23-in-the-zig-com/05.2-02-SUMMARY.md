---
phase: 05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com
plan: "02"
subsystem: lexer
tags: [lexer, keywords, tokens, wave-1, sd-21, sd-22]
dependency_graph:
  requires: ["05.2-01"]
  provides: ["kw_calc", "kw_return", "kw_require", "plus_minus"]
  affects: ["src/lexer.zig", "src/keywords.zig", "src/json.zig", "tests/unit/lexer_keywords.zig", "tests/unit/lexer_calc.zig"]
tech_stack:
  added: []
  patterns: ["TDD RED/GREEN", "comptime StaticStringMap", "UTF-8 2-byte dispatch", "exhaustive switch stub"]
key_files:
  created:
    - tests/unit/lexer_calc.zig
  modified:
    - src/lexer.zig
    - src/keywords.zig
    - src/json.zig
    - tests/unit/lexer_keywords.zig
    - tests/unit/_all.zig
decisions:
  - "kw_calc placed in Element section (after kw_constraint), count 16→17"
  - "kw_return + kw_require placed in new Calc/constraint body section"
  - "plus_minus placed in §7 Delimiters & Punctuation after .plus"
  - "D-07 enforced: kw_sig not added; sig stays .ident globally"
  - "T-05.2-W1-01: 0xC2/0xB1 dispatch inserted BEFORE generic high-bit path; peekByte sentinel guards truncated EOF"
  - "Existing 19 showcase snapshots byte-stable (zero diff after regeneration)"
  - "sema.dimensional.regression_pins failure is pre-existing Phase-4 carryover; not caused by this plan"
metrics:
  duration: "11 minutes"
  completed: "2026-06-10"
  tasks: 2
  files_created: 1
  files_modified: 5
---

# Phase 05.2 Plan 02: Lexer Wave 1 — calc/return/require keywords + ± token Summary

Wave 1 teaches the lexer the three new global reserved words (`calc`, `return`, `require`) and the `±` operator token. `sig` stays a plain IDENT (D-07 contextual). `=>` already lexed as the existing `arrow` token — no change needed.

## What Was Built

**src/lexer.zig:**
- Added `kw_calc` to Tag enum Element section (16→17 entries)
- Added `kw_return` and `kw_require` in new "Calc / constraint body keywords (SD-21/22)" section
- Added `plus_minus` in §7 Delimiters & Punctuation after `.plus`
- Inserted 2-byte `±` dispatch in `scanPunctuation` `else` branch before the generic `c0 >= 0x80` path: `if (c0 == 0xC2 and self.peekByte(1) == 0xB1)` → advance 2 bytes, emit `.plus_minus` (T-05.2-W1-01 mitigation)

**src/keywords.zig:**
- `global_keywords` map: added `calc` (Element section), `return` + `require` (new Calc/constraint body section)
- Count comment updated 85→88 (17+10+3+3+4+10+8+7+11+4+9+2)
- D-07 note added: `sig` intentionally absent

**src/json.zig:**
- `tagName()` exhaustive switch updated with 4 new arms: `kw_calc`, `kw_return`, `kw_require`, `plus_minus`

**tests/unit/lexer_calc.zig (new):**
- 6 tests: plus_minus single-token, truncated 0xC2 safety, wrong-byte non-match, minimal calc def zero-unknown, return keyword, require keyword, sig stays ident

**tests/unit/lexer_keywords.zig (updated):**
- Count assertion updated 85→88
- Part E: explicit lookup assertions for calc/return/require
- Part F: D-07 guard — `global_keywords.get("sig") == null`

## Task Results

| Task | Name | Commit | Status |
|------|------|--------|--------|
| 1 | Add kw_calc/kw_return/kw_require to Tag enum + keywords map; add plus_minus + ± dispatch | 1a224b2 | DONE |
| 2 | Regenerate token snapshots + confirm byte-stability | (no commit needed — zero diff) | DONE |

### TDD Gate Compliance

- RED commit: `6358476` — test(05.2-02): failing tests (compilation error — kw_calc does not exist)
- GREEN commit: `1a224b2` — feat(05.2-02): implementation passes all tests

## Verification

- `zig build test -Dtest-filter=lexer_keywords` — PASS (88 keyword count, calc/return/require found, sig=null)
- `zig build test -Dtest-filter=lexer_calc` — PASS (all 6 tests including ± single-token and minimal calc def)
- `zig build test -Dtest-filter=lexer_snapshot` — PASS (19 snapshots stable, zero git diff)
- `zig build` — PASS (no exhaustive switch errors)
- `grep -c 'kw_sig' src/keywords.zig` — 0 (D-07 source assertion)
- `cargo test --workspace` — 144/145 tests pass; 1 pre-existing failure (`check_workspace_writes_index_json_with_alphabetical_keys` — pre-existing stdlib path issue, confirmed failing on HEAD before our changes)
- `zig build test` — 76/77 tests pass; 1 pre-existing failure (`sema.dimensional.regression_pins` — documented Phase-4 carryover in STATE.md)

## Deviations from Plan

None — plan executed exactly as written.

The pre-existing test failures (`sema.dimensional.regression_pins`, `check_workspace_writes_index_json_with_alphabetical_keys`) are documented in STATE.md as Phase-4/Phase-5 carryovers unaffected by this plan.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. The `±` dispatch adds a new lexer path but operates on source bytes already in the buffer — no new trust boundary crossed. T-05.2-W1-01 mitigated by placing the dispatch before (not replacing) the generic high-bit handler.

## Self-Check: PASSED

- FOUND: tests/unit/lexer_calc.zig (created)
- FOUND: src/lexer.zig (plus_minus: 3 occurrences)
- FOUND: src/keywords.zig (kw_calc: 1 occurrence)
- FOUND: RED commit 6358476
- FOUND: GREEN commit 1a224b2
