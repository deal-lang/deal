---
phase: 05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com
plan: 04
type: execute
wave: 3
depends_on: ["05.2-03"]
files_modified:
  - src/diagnostics.zig
  - src/sema.zig
  - tests/unit/sema_calc.zig
  - tests/malformed/m25_calc_out_param_expr.deal
  - tests/malformed/m26_calc_no_return.deal
  - tests/malformed/m27_require_not_boolean.deal
  - tests/unit/recovery_corpus.zig
autonomous: true
requirements: [REQ-05.2-D05, REQ-05.2-D06_sema, REQ-05.2-D08]
must_haves:
  truths:
    - "An out-param calc used in expression position emits E2600 (purity rule, D7/D-10)"
    - "A calc body with no return statement emits E2601"
    - "A require expression that does not resolve to Boolean emits E2610"
    - "A ConstraintRef in a return contract that does not resolve to a defined constraint_def emits E2611 (incl. cycle A→B→A)"
    - "Calc return expr + args are dimensionally checked via the existing checkDimensionalExpr (reuse, no new walker)"
    - "Precision spec is parse-and-carry only — sema records precision{kind,value} at ALL THREE attach points (calc return, attribute/feature value, require threshold) and emits NO diagnostic for precision itself (D-06/D-08)"
    - "`deal fmt` normalizes `+/-`→`±` at every precision attach point (calc return, attribute value, require threshold) and round-trips the suffix byte-stably (D-06/D-09)"
    - "The existing showcase_clean dimensional loop stays ZERO E25xx (no regression)"
  artifacts:
    - path: "src/diagnostics.zig"
      provides: "E2600..E2699 calc/constraint/precision diagnostic band"
      contains: "E2600"
    - path: "tests/unit/sema_calc.zig"
      provides: "E2600/E2601/E2610/E2611 sema assertions"
      contains: "E2600"
    - path: "tests/malformed/m25_calc_out_param_expr.deal"
      provides: "E2600 purity-violation corpus fixture"
  key_links:
    - from: "src/sema.zig checkCalcDef"
      to: "checkDimensionalExpr (sema.zig:1109)"
      via: "return-expr-vs-return-type + arg-vs-param-type reuse"
      pattern: "checkDimensionalExpr"
    - from: "src/sema.zig checkConstraintRefCycle"
      to: "checkSpecializationCycle (sema.zig:993) analog"
      via: "visited-set cursor walk → E2611 on cycle"
      pattern: "E2611"
    - from: "src/sema.zig recordPrecision"
      to: "the generic precision slot on calc return / attribute value / require threshold"
      via: "parse-and-carry record, no enforcement (D-06/D-08)"
      pattern: "precision"
---

<objective>
Wave 3: add the calc/constraint semantic checks in the fresh E2600..E2699 diagnostic band, reusing the existing dimensional and cycle-detection machinery, and pin the new error paths with malformed-corpus fixtures (m25/m26/m27). The `+/-`→`±` fmt normalization landed in Wave 2 (fmt writeNode) at ALL THREE precision attach points; this wave verifies it via fmt round-trip (calc return + attribute value + require threshold) and adds the sema layer that records the generic precision slot at every attach point WITHOUT enforcement (D-06/D-08).

Purpose: D-10 must-have sema checks — purity rule, dimensional check on calc return/args, require→Boolean, ConstraintRef resolution/cycle. D-08 keeps precision parse-and-carry only (no enforcement) and D-06 generalizes the carried slot to attribute values and require thresholds, not only calc returns. The existing dimensional showcase_clean loop must stay green (no E25xx regression).

Output: E2600..E2699 band; checkCalcDef/checkConstraintDef sema checks; generic-precision recording at all three attach points (no enforcement); new sema_calc.zig test; m25/m26/m27 malformed fixtures + corpus pins; fmt round-trip verifying `+/-`→`±` at every attach point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-CONTEXT.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-PATTERNS.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-RESEARCH.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-VALIDATION.md
@src/sema.zig
@src/diagnostics.zig

<interfaces>
<!-- Anchors extracted from PATTERNS.md / RESEARCH.md (live-verified). -->
- diagnostics.zig:160–173 E2500..E2599 band declaration (analog for the E2600..E2699 block); module doc comment 7–21
- sema.zig:1109–1127 checkDimensionalExpr(a, type_node: ?*ast.Node, expr: *ast.Node, span) — reuse AS-IS for calc return + args
- sema.zig:993–1041 checkSpecializationCycle — visited-set cursor-walk template for checkConstraintRefCycle (E2611)
- sema.zig:826–861 checkDefinition / dispatch insertion point — switch on node.payload: .calc_def => checkCalcDef, .constraint_def => checkConstraintDef
- ElementUsage.precision (ast.zig:310, added Wave 2) + RequireStatement.precision (added Wave 2) — the generic D-06/D-08 slots sema records but does not enforce
- E-code numbering (RESEARCH Open Q2 recommendation — RESOLVED, Claude's discretion per D-10):
    E2600 purity violation (out-param calc in expression position)
    E2601 calc body missing return
    E2602 out-param assignment body (parse error, emitted in Wave 2 parser)
    E2610 require expression not Boolean
    E2611 ConstraintRef not resolved / cycle
- Existing showcase_clean loop lives in tests/unit/sema_dimensional.zig — must stay ZERO E25xx.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reserve E2600..E2699 band + implement checkCalcDef/checkConstraintDef sema checks + record generic precision at all three attach points</name>
  <read_first>
    - src/diagnostics.zig (E2500..E2599 band 160–173; module doc 7–21)
    - src/sema.zig (checkDimensionalExpr 1109–1127; checkSpecializationCycle 993–1041; checkDefinition 826–861)
    - .planning/phases/05.2-.../05.2-PATTERNS.md § "Wave 3: src/diagnostics.zig" + § "Wave 3: src/sema.zig" (copy-ready emit + cycle excerpts)
    - .planning/phases/05.2-.../05.2-RESEARCH.md § "Wave 3: Fmt + Sema — Detailed Investigation" + Open Q2 (RESOLVED)
    - `git -C spec show <merge>:grammar/2026-06-08-calc-constraint-grammar-design.md` §6 (sema notes) D7 purity
  </read_first>
  <behavior>
    - E2600: a calc with any `out`/`inout` param is statement-valued; using it in expression position (inside a Binary/Call tree) emits E2600.
    - E2601: a calc body with no ReturnStatement emits E2601 (note: also surfaced by the Wave-2 parser; sema is the authoritative semantic check).
    - E2610: a `require` condition whose resolved type is non-Boolean emits E2610.
    - E2611: a ConstraintRef in a return contract that resolves to nothing — or forms a cycle A→B→A — emits E2611.
    - Dimensional: calc return expr checked vs declared return type; each arg vs declared param type — via checkDimensionalExpr (NO new walker). Existing E2500..E2503 behavior unchanged.
    - Precision (D-06/D-08): sema records precision{kind,value} for the generic slot at ALL THREE attach points — calc return, attribute/feature value (ElementUsage.precision), and require threshold (RequireStatement.precision) — and emits NO diagnostic for precision (parse-and-carry only; enforcement deferred to numeric-model Lane A).
  </behavior>
  <action>
    In `src/diagnostics.zig`: add the E2600..E2699 block after line 173 (analog: the E2500 block). Declare `e_calc_purity_violation="E2600"`, `e_calc_missing_return="E2601"`, `e_calc_out_param_assignment_body="E2602"`, `e_require_not_boolean="E2610"`, `e_constraint_ref_not_found="E2611"`. Add the `E2600..E2699 semantic — calc/constraint/precision (Phase 05.2 D-10)` line to the module doc comment (7–21). E-codes are compile-time string constants — never runtime data (emitFmt pattern).

    In `src/sema.zig`: wire `checkCalcDef` and `checkConstraintDef` into the definition dispatch (826–861) by switching on `node.payload` (`.calc_def => checkCalcDef`, `.constraint_def => checkConstraintDef`, else → existing elemDefOf path). Implement:
      - Check #8a purity (E2600): mark a calc statement-valued if any ParameterDecl direction is .out/.inout; emit E2600 if such a calc appears in expression position.
      - Check #8b return dimensional: `checkDimensionalExpr(a, calc.type_node, return_stmt.expr, return_stmt.span)`.
      - Check #8c arg dimensional: at each .call resolving to a calc_def, `checkDimensionalExpr(a, param.type_node, arg, arg.span)` per arg.
      - Check #8d require Boolean (E2610): each require_statement condition must resolve to Boolean; else E2610.
      - Check #8e ConstraintRef resolution + cycle (E2611): each ConstraintRef must resolve to a constraint_def in scope; mirror `checkSpecializationCycle`'s visited-set cursor walk to detect A→B→A cycles → E2611 (threat T-W3).
      - Precision (D-06/D-08): add a small `recordPrecision(slot: ?*ast.Node)` helper called from THREE places — the calc return contract, each attribute/feature value's `ElementUsage.precision`, and each `RequireStatement.precision`. It records the precision{kind,value} into the sema model and emits NOTHING (no enforcement). This makes the generic slot uniformly carried at all attach points (D-06), not only on calc returns.

    Copy emit + cycle shapes from PATTERNS.md § Wave 3; no fenced code in this action.
  </action>
  <verify>
    <automated>zig build test -Dtest-filter=sema_calc && zig build test -Dtest-filter=sema_dimensional</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/sema_calc.zig` exists and `zig build test -Dtest-filter=sema_calc` passes, asserting E2600 (purity), E2601 (missing return), E2610 (non-boolean require), E2611 (unresolved/cyclic ConstraintRef), AND that an attribute-value precision contract (`attribute mass : Mass = kg(5) => sig 4;`) and a require-value precision contract are RECORDED but produce ZERO diagnostics (D-06/D-08 parse-and-carry)
    - `zig build test -Dtest-filter=sema_dimensional` passes — existing showcase_clean loop emits ZERO E25xx (no regression from the new checks)
    - `grep -v '^#' src/diagnostics.zig | grep -c 'E2600'` ≥ 1 and the band E2600..E2699 is documented in the module doc comment
    - `grep -c 'checkDimensionalExpr' src/sema.zig` increased (reuse, not a new walker — source assertion for "Don't Hand-Roll")
    - `grep -c 'recordPrecision\|\.precision' src/sema.zig` ≥ 1 (generic precision recorded at the attribute + require attach points, not only calc returns — D-06 source assertion)
  </acceptance_criteria>
  <done>E2600..E2699 reserved; all five calc/constraint sema checks implemented via reuse; the generic precision slot is recorded at all three attach points with no enforcement; no E25xx regression.</done>
</task>

<task type="auto">
  <name>Task 2: Add m25/m26/m27 malformed fixtures + corpus pins; verify fmt +/- → ± round-trip at all three precision attach points</name>
  <read_first>
    - tests/unit/recovery_corpus.zig (the per-file pin table; 05.1 m21–m24 pattern)
    - tests/malformed/ (existing m01..m24 fixtures for the format)
    - tests/unit/fmt_roundtrip.zig (existing fmt round-trip harness)
    - .planning/phases/05.2-.../05.2-VALIDATION.md § "Wave 3 — Fmt + Sema" + § "Wave 0 Requirements" (the 3 net-new malformed fixtures)
    - .planning/phases/05.2-.../05.2-RESEARCH.md § "Wave 3" malformed-corpus pins + Open Q2 (RESOLVED)
  </read_first>
  <action>
    Create three malformed fixtures and pin each in `recovery_corpus.zig` with its expected primary diagnostic code (05.1 m21–m24 pinning pattern — per-file expected code matches the first emitted diagnostic):
      - `tests/malformed/m25_calc_out_param_expr.deal` — an out-param calc used in expression position → expected E2600.
      - `tests/malformed/m26_calc_no_return.deal` — a calc body with no return → expected E2601.
      - `tests/malformed/m27_require_not_boolean.deal` — a require whose condition is non-Boolean → expected E2610.
    (If the empirical first-emitted code diverges from the intended code — as happened with several 05.1 pins — pin the OBSERVED code and document the divergence inline next to the pin, matching the established empirical-truth pinning policy.)

    Verify the D-06/D-09 normalization end-to-end at ALL THREE precision attach points: `zig build test -Dtest-filter=fmt_roundtrip` must show that a source using the `+/-` ASCII alias formats to `±` for (a) a calc return contract, (b) an attribute-value precision suffix (`=> ± percent(1)` on an attribute), and (c) a require-value precision suffix; and that `deal fmt` on the already-canonical oracle files (calcs.deal AND precision.deal) is a no-op (idempotent). If the existing fmt_roundtrip harness does not already include attribute/require precision cases via precision.deal, add a small fixture or extend the harness so each attach point is exercised.
  </action>
  <verify>
    <automated>zig build test -Dtest-filter=recovery_corpus && zig build test -Dtest-filter=fmt_roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - The three fixtures exist; `zig build test -Dtest-filter=recovery_corpus` passes with m25→E2600, m26→E2601, m27→E2610 pins (or documented empirical-truth equivalents)
    - `zig build test -Dtest-filter=fmt_roundtrip` passes, including a `+/-` → `±` normalization case at EACH of the three attach points (calc return, attribute value, require threshold) and `deal fmt` idempotency on both calcs.deal and precision.deal
    - `zig build test && cargo test --workspace` green (wave-merge sampling)
  </acceptance_criteria>
  <done>Three E26xx malformed fixtures pinned; fmt normalizes +/- to ± and is idempotent on canonical source at all three precision attach points.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed AST → sema | Untrusted calc/constraint structures (cyclic ConstraintRefs, non-Boolean requires) reach semantic analysis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-W3-01 | Tampering | ConstraintRef cycle A→B→A | mitigate | `checkConstraintRefCycle` bounded by `max_hops = table.entries.count()+1` with a visited-set (checkSpecializationCycle analog) → E2611, terminates (RESEARCH Security Domain row 4) |
| T-05.2-W3-02 | DoS | malformed calc/require body in sema | mitigate | m25/m26/m27 corpus pins exercise the error paths without panic; sema operates on already-recovered AST (Wave-2 sync) |
| T-05.2-W3-03 | Tampering | new sema checks regress existing dimensional checks | mitigate | sema_dimensional showcase_clean loop asserts ZERO E25xx — explicit no-regression gate |
| T-05.2-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH audit: none) |
</threat_model>

<verification>
- `zig build test -Dtest-filter=sema_calc` + `=sema_dimensional` + `=recovery_corpus` + `=fmt_roundtrip` all pass.
- `zig build test && cargo test --workspace` green.
</verification>

<success_criteria>
- E2600/E2601/E2610/E2611 emitted on the right conditions; precision parse-and-carry only (D-08), recorded at all three attach points (D-06).
- Calc return/args dimensionally checked via reuse; no E25xx regression.
- m25/m26/m27 pinned; +/- → ± normalization verified at calc return, attribute value, and require threshold.
</success_criteria>

<output>
Create `.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-04-SUMMARY.md` when done.
</output>
