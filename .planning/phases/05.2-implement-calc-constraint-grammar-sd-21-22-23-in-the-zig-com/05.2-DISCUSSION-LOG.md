# Phase 05.2: Implement calc/constraint grammar (SD-21/22/23) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-09
**Phase:** 05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com
**Areas discussed:** Spec re-pin strategy, Phase scope / Wave 5, sig keyword classification (→ generalized precision), out/inout param fidelity (→ multi-out via deal-sim)

---

## Spec re-pin strategy

### How to bring 7ce59bc onto current spec main (74dfea5, on origin)?

| Option | Description | Selected |
|--------|-------------|----------|
| Merge 7ce59bc into main | git merge onto origin/main; new files clean, resolve 3 additive conflicts + .dealview/.dealx rename overlap by hand; faithful to locked authorship | ✓ |
| Cherry-pick grammar content | Re-apply additive productions + 2 new files, skip incidental .dealview edits | |
| Let me handle the merge | User does the spec merge/push out-of-band | |

**User's choice:** Merge 7ce59bc into main.

### How to sequence/verify the merge within the phase?

| Option | Description | Selected |
|--------|-------------|----------|
| Wave 0 blocking gate | Dedicated plan: merge + push + gitlink bump; verify 19 showcase files parse + sims/v0 intact + new analysis files present (expected parse-fail until Waves 1–4); nothing else starts until green | ✓ |
| Fold into Wave 1 | Do merge as first step of lexer plan, no gate | |
| You decide at planning | Leave plan boundary to planner | |

**User's choice:** Wave 0 blocking gate.
**Notes:** Discovered during scout that 7ce59bc is local-only (must push); current pin 74dfea5 is already on origin/main and is the further-along canonical line (sims/v0 + 05.1 reconcile). Merge conflict surface confirmed small/additive (deal.ebnf, DESIGN-DECISIONS.md, README.md) + a .dealview→.dealx rename overlap.

---

## Phase scope / Wave 5

### Include Wave 5 (editor surfaces) or stop at codegen?

| Option | Description | Selected |
|--------|-------------|----------|
| Stop at codegen (Waves 0-4) | Deliver lexer→codegen + goldens; defer editor surfaces to Phase 6 | |
| Include Wave 5 editor surfaces | Add tree-sitter/vscode/LSP support this phase | ✓ |
| Wave 5 as optional tail | Waves 0-4 committed, Wave 5 best-effort droppable | |

**User's choice:** Include Wave 5 editor surfaces.

### How deep should Wave 5 go?

| Option | Description | Selected |
|--------|-------------|----------|
| All three, keyword-level | tree-sitter + vscode TextMate + LSP semantic-token classification for the 5 keywords + ± | |
| All three + rich LSP | Keyword highlighting + LSP signature/return-type hover + go-to-def for calc/constraint refs | ✓ |
| tree-sitter + vscode only | Highlighting now, defer all LSP to Phase 6 | |

**User's choice:** All three + rich LSP.
**Notes:** Flagged that rich LSP overlaps Phase 6's LSP build-out — plan must define a clean seam to avoid duplication.

---

## sig keyword classification → generalized precision

### Global vs contextual `sig`?

| Option | Description | Selected |
|--------|-------------|----------|
| Contextual (parse-time) | Lexer emits IDENT for sig; parser recognizes only in a precision contract; sig stays a usable identifier | (implied ✓ via expansion) |
| Global reserved word | Add sig to global_keywords; simplest but reserves sig everywhere | |
| You decide | Pick cleanest impl | |

**User's choice (free text):** "It needs to be more usable than that as it could be applied to any numerical value." → settles `sig` as non-reserved/contextual AND expands scope: precision should attach to any numerical value, not just calc returns.

### How much of the broader precision surface lands this phase?

| Option | Description | Selected |
|--------|-------------|----------|
| Generalize the production now | Extend grammar so PrecisionSpec attaches to attribute/feature values and more — still parse-and-carry; SD-23 amendment + new fixtures | ✓ |
| Build calc-contract now, design IR for general | Implement SD-23 as-is, keep sig non-reserved + generic IR slot, defer broader surface | |
| Decide after a spec amendment | Pause until design doc amended | |

**User's choice:** Generalize the production now.

### Which positions carry a precision spec?

| Option | Description | Selected |
|--------|-------------|----------|
| Attribute/feature values | `attribute mass : Mass = kg(5) => sig 4;` | ✓ |
| Calc return contracts | Original SD-23 surface | ✓ |
| Bare literals in expressions | Inline on a literal mid-expression | ✗ (user: only on the result) |
| Constraint/require values | Precision on require thresholds/result values | ✓ |

**User's choice (multi + free text):** Attribute/feature values + Calc return contracts + Constraint/require values, plus "For any expressions, it should really only be allowed on the result." → result-valued declarations only; never bare sub-literals. Claude agreed (precision is a property of a result, not its intermediates).

---

## out/inout param fidelity → multi-out via deal-sim

### How to handle out/inout calc params this phase?

| Option | Description | Selected |
|--------|-------------|----------|
| Accept in list, reject body | Accept out/inout in ParameterDecl (KerML hook); reject assignment-style body (E26xx); bodies stay LocalBinding* + return | ✓ |
| Reject out/inout entirely | Smaller surface, less KerML fidelity | |
| Implement out-param bodies now | Resolve deferred sub-decision, build assignment-style multi-out body | |

**User's choice (free text):** "I like choice 1, however I'd like our decision to essentially handle multi-in, multi-out clearly via deal-sim usage and be able to map that properly to KerML by essentially collapsing a deal-sim into a calc."

### Direction for the deferred out-param body?

| Option | Description | Selected |
|--------|-------------|----------|
| Route multi-out to deal-sim | out-param calc = typed interface, deal-sim = body; no in-language body planned; shape IR to coincide with deal-sim→KerML mapping for zero-rework collapse; binding mechanism = forward phase | ✓ |
| Keep both paths open | Same parse behavior, don't commit direction | |
| Just choice 1, defer the rest | Accept/reject only, no IR-shape constraint | |

**User's choice:** Route multi-out to deal-sim.
**Notes:** Claude assessed the unification as sound at the KerML representation layer (design §7 already maps out-param calc → Function with directed result features, same shape as a sim's I/O). This phase shapes the IR; the binding/collapse mechanism is deferred.

## Claude's Discretion

- Exact `=>` placement on attribute declarations, per-position FIRST-set proofs, E26xx sub-code numbering — left to planner/grammar-architect within D-06/D-09/D-10 constraints.
- Constraint-header `=>` contract stays calc-only (design §10 default).

## Deferred Ideas

- Precision *enforcement* (numeric-model Lane A, N-01..N-04).
- calc↔deal-sim binding/collapse *mechanism* (forward phase; this phase only shapes the IR).
- In-language out-param assignment body (superseded in direction by deal-sim routing).
- Constraint-header `=>` contract.
- Broader Phase 6 LSP build-out (rich calc/constraint LSP lands here; define the seam).
