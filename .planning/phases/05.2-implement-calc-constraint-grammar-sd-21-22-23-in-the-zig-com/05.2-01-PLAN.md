---
phase: 05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - spec/grammar/deal.ebnf
  - spec/grammar/DESIGN-DECISIONS.md
  - spec/grammar/README.md
  - spec/grammar/lexical.ebnf
  - spec/grammar/2026-06-08-calc-constraint-grammar-design.md
  - spec/examples/showcase/packages/analysis/calcs.deal
  - spec/examples/showcase/packages/analysis/constraints.deal
  - spec/examples/showcase/packages/analysis/precision.deal
  - build.zig
  - scripts/verify-fresh-worktree.sh
  - .gitmodules
autonomous: false
requirements: [REQ-05.2-D01, REQ-05.2-D02]
must_haves:
  truths:
    - "D-01: Spec origin/main contains both the SD-15a/15b line (74dfea5) and the calc/constraint line (7ce59bc) as a single merge commit, with the 3 additive conflicts (deal.ebnf, DESIGN-DECISIONS.md, README.md) and the .dealview→.dealx model overlap hand-resolved"
    - "D-02: The merged spec commit is PUSHED to spec origin/main and the parent deal gitlink is bumped, gated by a dedicated Wave 0 blocking step BEFORE any Zig work — a fresh worktree of deal with submodule init contains tests/showcase/packages/analysis/calcs.deal, constraints.deal, and precision.deal"
    - "D-05: This Wave 0 spec gate precedes the committed wave spine (W1 lexer → W2 AST/parser → W3 fmt/sema → W4 IR/lowering/codegen → W5 editor); the 19 pre-existing showcase files still parse clean with the current (pre-Wave-1) compiler"
    - "deal parse on the three new analysis/*.deal files FAILS (calc/±/=> precision not yet lexed/parsed) — confirming oracle files are present and are the Wave 1-3 acceptance target"
    - "precision.deal demonstrates attribute-value precision (`attribute mass : Mass = kg(5) => sig 4;`) and require-value precision (D-03/D-06), as a DEDICATED fixture file outside calcs.deal/constraints.deal"
  artifacts:
    - path: "spec/examples/showcase/packages/analysis/calcs.deal"
      provides: "calc-def parse oracle (MarginOfSafety, Drag, KineticEnergy)"
      contains: "calc def"
    - path: "spec/examples/showcase/packages/analysis/constraints.deal"
      provides: "constraint-def parse oracle (PositiveForce, BatteryLimits, ThermalWindow)"
      contains: "constraint def"
    - path: "spec/examples/showcase/packages/analysis/precision.deal"
      provides: "D-03 generalized-precision oracle: attribute-value precision + require-value precision (D-06)"
      contains: "=> sig"
    - path: "build.zig"
      provides: "phase-05.2-wave0-gate build step"
      contains: "phase-05.2-wave0-gate"
  key_links:
    - from: ".gitmodules / deal gitlink"
      to: "spec merge commit on origin/main"
      via: "git submodule pin bump"
      pattern: "spec"
---

<objective>
Land the calc/constraint grammar line into the spec submodule and gate it, BEFORE any Zig work begins. This is a BLOCKING precondition (D-02): merge spec commit `7ce59bc` into spec `origin/main` (current pin `74dfea5`), resolve the 3 additive hand-conflicts, carry the D-06 SD-23 precision-generalization amendment and the new D-03 oracle fixtures, push the merge to spec origin/main (7ce59bc is currently local-only), bump the parent `deal` gitlink, and install a `phase-05.2-wave0-gate` build step that verifies the merged state.

Purpose: Without the spec merge + push + gitlink bump, fresh-worktree submodule init fails and no downstream wave has the oracle files (calcs.deal/constraints.deal/precision.deal) to parse against. Nothing else in Phase 05.2 starts until this gate is green.

Output: Merged + pushed spec origin/main; bumped deal gitlink; three new analysis/*.deal oracle files in-tree (calcs.deal, constraints.deal, precision.deal); D-06 SD-23 amendment + new precision fixtures; `phase-05.2-wave0-gate` build step; `scripts/verify-fresh-worktree.sh` extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-CONTEXT.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-RESEARCH.md
@.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-VALIDATION.md

<notes>
- The design doc lives ONLY on spec commit 7ce59bc until this merge lands. Read it via `git -C spec show 7ce59bc:grammar/2026-06-08-calc-constraint-grammar-design.md`.
- 05.1 Plan 01 established the spec-push + gitlink-bump pattern (D-05/D-06 of 05.1). Follow the same flow.
- `scripts/verify-fresh-worktree.sh` is generalized (Phase 1.5) and accepts a gate-step name argument; build.zig `phase-1.5-gate-fresh` shows the wiring pattern (build.zig lines ~287–293).
- Merge-base is `fe98f89`; `git merge-tree fe98f89 74dfea5 7ce59bc` confirmed exactly 3 conflict files.
- The D-03 generalized-precision oracle goes in a DEDICATED file `spec/examples/showcase/packages/analysis/precision.deal` — NOT inside calcs.deal/constraints.deal — so that Plan 05.2-03 Task 3's snapshot scope is the THREE analysis files (calcs.deal, constraints.deal, precision.deal). Attribute-value precision is parsed in Plan 05.2-03 alongside calc/constraint, so all three become parse-oracle GREEN in the same wave.
</notes>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Merge spec 7ce59bc into spec origin/main, resolve 3 conflicts, push, bump deal gitlink</name>
  <read_first>
    - spec/grammar/deal.ebnf (current 74dfea5 working-tree state — the AnnotationKey I1/I2 side of the conflict)
    - spec/grammar/DESIGN-DECISIONS.md (current SD-15a/15b side of the conflict)
    - spec/grammar/README.md (current stat-refresh side)
    - `git -C spec show 7ce59bc:grammar/2026-06-08-calc-constraint-grammar-design.md` (the locked design — authoritative; read in full)
    - `git -C spec show 7ce59bc:grammar/deal.ebnf` (the calc/constraint productions side of the deal.ebnf conflict)
    - `git -C spec show 7ce59bc:grammar/DESIGN-DECISIONS.md` (SD-21/22/23 side)
    - .planning/phases/05.2-.../05.2-RESEARCH.md § "Wave 0: Spec Submodule Merge — Detailed Investigation"
    - .planning/todos/pending/2026-06-08-implement-calc-constraint-grammar-in-zig.md § "Spec submodule recall" + "Push note"
  </read_first>
  <action>
    This task mutates git history across the spec submodule and the parent deal gitlink — it cannot be unit-tested, so it is a blocking human-action checkpoint (D-01/D-02; VALIDATION.md Manual-Only row 1). Execute autonomously where the CLI permits, pause only if push auth fails.

    In the `spec/` submodule (origin `git@github.com:deal-lang/spec.git`):
    1. Confirm `7ce59bc` exists locally and `74dfea5` is on origin/main (`git -C spec log --oneline -1 origin/main`). Merge-base is `fe98f89`.
    2. `git -C spec checkout main && git -C spec merge 7ce59bc`. Resolve EXACTLY 3 additive conflicts BY HAND, preserving BOTH sides faithfully:
       - `grammar/deal.ebnf` — keep 74dfea5's `AnnotationKey` (I1/I2) change in the AnnotationField section AND 7ce59bc's 13 calc/constraint productions (CalcDefinition, ConstraintDefinition, ParameterList, ParameterDecl, ReturnContract, PrecisionSpec, ConstraintRef, ConstraintBody, CalcBody, ReturnStatement, RequireStatement, etc.). Different sections — no semantic overlap.
       - `grammar/DESIGN-DECISIONS.md` — keep both SD-15a/SD-15b (from 74dfea5) and SD-21/SD-22/SD-23 (from 7ce59bc).
       - `grammar/README.md` — combine both stat refreshes (production count + DESIGN-DECISIONS.md path).
    3. The `.dealview`→`.dealx` rename/edit overlap on `examples/showcase/model/{traceability,performance,sedan,vehicle}.*` is NOT a real conflict — git rename detection treats 7ce59bc's edits as content edits on top of 74dfea5's renames. Verify it resolved automatically; do not hand-edit unless git flags it.
    4. Carry the D-06 SD-23 amendment (generalized precision attach points) into BOTH `grammar/deal.ebnf` AND `grammar/DESIGN-DECISIONS.md` SD-23. The amendment states: a PrecisionContract `=> ContractItem ( "," ContractItem )*` attaches to ANY result-valued declaration — calc return, attribute/feature value, AND require threshold — result-only, never bare sub-literals (D-06). In `deal.ebnf`, add the optional `( "=>" ReturnContract )?` suffix to the AttributeUsage/value-binding production (placed BEFORE the terminating `;`, per RESEARCH Open Q1) and to the RequireStatement production (on the require result value).
    5. Create the D-03 generalized-precision oracle as a DEDICATED file `spec/examples/showcase/packages/analysis/precision.deal` (NOT inside calcs.deal/constraints.deal). It MUST demonstrate: (a) attribute-value precision — `attribute mass : Mass = kg(5) => sig 4;` and a relative-tolerance form e.g. `derived attribute q : Pressure = ... => ± percent(1);`; and (b) require-value precision — a `require` whose result value carries a `=> sig N` or `=> ± Tolerance` contract. Keep it inside the `analysis` package so the parser snapshot scan in Plan 05.2-03 Task 3 covers exactly the three analysis files.
    6. Commit the merge. Push to spec origin/main: `git -C spec push origin main`. If push auth fails, PAUSE and surface the error (this is the only legitimate pause point).
    7. In the parent `deal` repo, bump the gitlink: `git add spec` so `.gitmodules`/the gitlink points at the new merge commit SHA. Confirm `git -C spec rev-parse HEAD` matches the staged gitlink.
    8. The `tests/showcase` symlink (→ `../spec/examples/showcase`) now automatically exposes the three new `packages/analysis/*.deal` files. Confirm they appear.
  </action>
  <acceptance_criteria>
    - `git -C spec log --oneline origin/main | head -3` shows the merge commit with both `7ce59bc` and `74dfea5` as parents (`git -C spec log --merges -1 origin/main` non-empty)
    - `git -C spec show HEAD:grammar/DESIGN-DECISIONS.md` contains both "SD-15a" AND "SD-21" (grep both, exit 0)
    - `git -C spec show HEAD:grammar/deal.ebnf` contains both "AnnotationKey" AND "CalcDefinition"
    - `ls spec/examples/showcase/packages/analysis/calcs.deal spec/examples/showcase/packages/analysis/constraints.deal spec/examples/showcase/packages/analysis/precision.deal` all three exist (exit 0)
    - `grep -c '=> sig' spec/examples/showcase/packages/analysis/precision.deal` ≥ 1 AND `grep -c 'attribute' spec/examples/showcase/packages/analysis/precision.deal` ≥ 1 (D-03 attribute-value precision present)
    - `git -C spec rev-parse HEAD` equals the SHA staged for the parent `deal` gitlink (`git ls-files -s spec` shows the same SHA)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the merge is pushed to spec origin/main and the deal gitlink is bumped, or describe the push/auth issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add phase-05.2-wave0-gate build step + extend verify-fresh-worktree.sh</name>
  <read_first>
    - build.zig (lines ~209–340 — the phase-1-gate / phase-1.5-gate / phase-1.5-gate-fresh / phase-2-gate-fresh step definitions are the exact analog)
    - scripts/verify-fresh-worktree.sh (the generalized gate-step-argument script)
    - .planning/phases/05.2-.../05.2-RESEARCH.md § "Wave 0 Gate Verification" + § "Validation Architecture > Wave 0 — Spec Merge Gate"
    - .planning/phases/05.2-.../05.2-VALIDATION.md § "Wave 0 Requirements"
  </read_first>
  <action>
    Add a `phase-05.2-wave0-gate` build step in `build.zig` (analog: the `phase-1.5-gate` step at build.zig ~259 and the `phase-2-gate` step at ~317). The step runs, in order:
    1. `zig build test -Dtest-filter=parser_deal_snapshot` — all 19 pre-existing showcase files still parse clean (regression).
    2. A shell/run-artifact step asserting `tests/showcase/packages/analysis/calcs.deal`, `tests/showcase/packages/analysis/constraints.deal`, AND `tests/showcase/packages/analysis/precision.deal` EXIST in the worktree.
    3. A `deal parse tests/showcase/packages/analysis/calcs.deal` invocation that MUST exit NON-ZERO (the file uses `calc`/`±` which are not yet lexed pre-Wave-1) — i.e. the gate asserts the oracle file is present AND currently fails, confirming it is the Wave 1-3 acceptance target. Use the run-step exit-code-inversion pattern (expect failure); do NOT mark the gate green on a parse success here.
    4. `zig build phase-5-gate` — prior pipeline regression stays green.

    Then extend `scripts/verify-fresh-worktree.sh` so `phase-05.2-wave0-gate` is an accepted gate-step argument (it already takes a positional gate-step name — confirm the new name routes through, mirroring how `phase-1.5-gate` is passed at build.zig ~293). Add a `phase-05.2-wave0-gate-fresh` build step that shells out to the script with the new gate-step, mirroring `phase-1.5-gate-fresh` (build.zig ~287).

    Do NOT place fenced code in this action; copy the step-construction shape directly from the existing phase-N-gate steps in build.zig.
  </action>
  <verify>
    <automated>zig build phase-05.2-wave0-gate</automated>
  </verify>
  <acceptance_criteria>
    - `zig build phase-05.2-wave0-gate` exits 0 (the gate itself is green: 19 showcase parse clean, three oracle files present, oracle parse fails as expected, phase-5-gate green)
    - `grep -v '^#' build.zig | grep -c 'phase-05.2-wave0-gate'` ≥ 1
    - `grep -c 'phase-05.2-wave0-gate' scripts/verify-fresh-worktree.sh` ≥ 1 OR the script's positional-argument routing demonstrably accepts the new name (documented in SUMMARY)
    - `zig build phase-05.2-wave0-gate-fresh` exits 0 (runs in an ephemeral worktree with submodule init against the PUSHED spec merge)
  </acceptance_criteria>
  <done>The wave-0 gate is green in both the main checkout and a fresh ephemeral worktree, proving the spec merge is pushed and reachable and the three oracle files are present-but-not-yet-parseable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local spec repo → spec origin/main | Local-only commit 7ce59bc must be pushed; until pushed, fresh-worktree consumers cannot fetch it |
| deal gitlink → spec submodule SHA | Gitlink must point at a commit reachable on origin, or `submodule update --init` fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-W0-01 | Tampering | spec gitlink points at unpushed/local SHA | mitigate | `phase-05.2-wave0-gate-fresh` runs in an ephemeral worktree with `submodule update --init` — fails fast if the merge SHA is not on origin |
| T-05.2-W0-02 | Repudiation | merge silently drops one side's grammar additions | mitigate | Acceptance grep asserts BOTH "SD-15a"+"SD-21" and "AnnotationKey"+"CalcDefinition" survive the merge |
| T-05.2-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `zig build phase-05.2-wave0-gate` and `zig build phase-05.2-wave0-gate-fresh` both exit 0.
- Spec origin/main carries the merge; deal gitlink bumped to the merge SHA.
- Three oracle files present (calcs.deal, constraints.deal, precision.deal), currently un-parseable (expected) — Wave 1-3 acceptance target locked in.
</verification>

<success_criteria>
- Spec merge committed + pushed to origin/main with both lines preserved (SD-15a/b + SD-21/22/23, AnnotationKey + CalcDefinition).
- D-06 SD-23 amendment (generalized precision attach points in deal.ebnf) + D-03 new precision fixture (precision.deal) carried in the merge.
- deal gitlink bumped; fresh-worktree gate green.
- `phase-05.2-wave0-gate` build step + `verify-fresh-worktree.sh` extension in place.
- 19 existing showcase files parse clean; 3 new analysis files present and (expectedly) fail parse.
</success_criteria>

<output>
Create `.planning/phases/05.2-implement-calc-constraint-grammar-sd-21-22-23-in-the-zig-com/05.2-01-SUMMARY.md` when done.
</output>
