---
phase: 02-prove-the-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - deal/src/sema.zig
  - deal/src/diagnostics.zig
  - deal/src/json.zig
  - deal/src/lib.zig
  - deal/tests/unit/sema_corpus.zig
  - deal/tests/unit/_all.zig
  - deal/tests/regressions/sema/01-name-resolution.deal
  - deal/tests/regressions/sema/02-type-check.deal
  - deal/tests/regressions/sema/03-multiplicity.deal
  - deal/tests/regressions/sema/04-specializes.deal
  - deal/tests/regressions/sema/05-trace.deal
  - deal/tests/regressions/sema/06-import.deal
  - deal/cli/src/main.rs
  - deal/cli/src/render.rs
  - deal/cli/tests/check_subcommand.rs
autonomous: true
requirements:
  - REQ-phase-2-1-semantic-analyzer
user_setup: []

must_haves:
  truths:
    - "src/sema.zig runs all 6 blocking checks against AST in the parser arena per D-20 (Zig owns the semantic analyzer)"
    - "diagnostics.zig has E2000..E2499 band declared per D-33 (with one-line doc comments)"
    - "All 19 showcase files pass sema (deal check exits 0)"
    - "All 6 regression fixtures (tests/regressions/sema/01..06.deal) fail sema with their pinned E2xxx code"
    - ".deal/index.json is generated with alphabetical-key shape per D-18 / RESEARCH Pattern 4"
    - "deal check subcommand renders human diagnostics via owo-colors + anstream"
    - "deal check --json emits the D-32 envelope: {v, command, deal_version, diagnostics, summary}"
    - "deal check returns exit 0 on clean, 1 on user error (any error-severity diagnostic), 2 on internal error"
  artifacts:
    - path: "deal/src/sema.zig"
      provides: "Semantic analyzer with 6 blocking checks + index.json writer"
    - path: "deal/src/diagnostics.zig"
      provides: "Codes namespace extended with E2000..E2499 band"
      contains: "E2000"
    - path: "deal/tests/unit/sema_corpus.zig"
      provides: "Per-file sema pin table + showcase-clean check + index.json materialization + alphabetical-key assertion"
      contains: "sema_pins"
    - path: "deal/cli/src/render.rs"
      provides: "Diagnostic rendering (human mode) with owo-colors + anstream"
      contains: "render_diagnostic"
    - path: "deal/cli/src/main.rs"
      provides: "deal check subcommand wired to FFI"
      contains: "Command::Check"
  key_links:
    - from: "deal/src/sema.zig"
      to: "deal/src/diagnostics.zig E2xxx codes"
      via: "DiagnosticCollector.emitFmt(Codes.e_*, .err, span, ...)"
      pattern: "Codes\\.e_(name|type|multiplicity|specialization|trace|unresolved)"
    - from: "deal/cli/src/main.rs"
      to: "deal_diagnostics_json via FFI"
      via: "extern \"C\" call + envelope wrap"
      pattern: "deal_diagnostics_json"
    - from: "deal/tests/regressions/sema/*.deal"
      to: "deal/tests/unit/sema_corpus.zig sema_pins"
      via: "per-file expected_code pin (analog of recovery_corpus.zig)"
      pattern: "sema_pins"
---

<objective>
Implement the semantic analyzer (D-20) and wire the `deal check` subcommand end-to-end. This is the first blocking-check surface — REQ-phase-2-1-semantic-analyzer is a single requirement that owns: name resolution, type checking, multiplicity enforcement, `<<specializes>>` compatibility, `@trace` reference validation, import resolution, `.deal/index.json` generation, and E2xxx error code allocation per D-33.

Purpose: SPEC.md REQ #1 acceptance says ALL 6 checks block (none warn-only). The 6 regression fixtures (RESEARCH §Recommended Project Structure lines 343-351 + CONTEXT §Specifics) prove each check independently. The showcase MUST exit 0 — if any showcase file has a latent sema bug, the planner of this plan is responsible for fixing it (showcase is the integration oracle per CONTEXT §canonical_refs).

Output: A `src/sema.zig` Zig module that walks the AST, populates a workspace-wide symbol table, runs 6 blocking checks, writes `.deal/index.json`; an extended `Codes` namespace with E2000..E2499; a `deal check` CLI subcommand that calls into the FFI, wraps the D-32 envelope when `--json` is set, and renders human-readable diagnostics via owo-colors when `--json` is unset. Plan 02-03's IR lowering depends on this plan's symbol table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-prove-the-pipeline/02-CONTEXT.md
@.planning/phases/02-prove-the-pipeline/02-RESEARCH.md
@.planning/phases/02-prove-the-pipeline/02-PATTERNS.md
@.planning/phases/02-prove-the-pipeline/02-SPEC.md
@.planning/phases/02-prove-the-pipeline/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts for the executor. -->

From deal/src/diagnostics.zig (Phase 1 + 1.5):
- `pub const Codes` namespace with E0001..E0122 currently allocated.
- `pub const DiagnosticCollector` with `emitFmt(code, severity, span, comptime fmt, args)` (src/diagnostics.zig:123-189).
- D-33 LOCKED bands: E2000..E2099 name resolution; E2100..E2199 type checking; E2200..E2299 multiplicity; E2300..E2349 specializes; E2350..E2399 @trace; E2400..E2499 import resolution.

From deal/src/ast.zig (Phase 1 + Plan 02-01):
- `Node`, `NodeKind`, `Span` (lines 14-50).
- `DocComment` struct at line 393.
- All declaration variants now carry leading_comments / trailing_comments / doc_comment (Plan 02-01).

From deal/src/lib.zig (Phase 1):
- `DealHandle` struct (lines 36-45) — Plan 02-03 will add ir_root + cached_ir_json + cached_fmt_bytes; this plan does NOT touch DealHandle.
- `deal_parse` (lines 92-160) + `deal_diagnostics_json` (lines 306-323) — this plan extends the diagnostics path to call sema before serialization.
- Existing `tests/unit` arena-leak pattern via `std.testing.allocator`.

From RESEARCH.md §Pattern 4 (name resolution) + §Pattern 5 (cycle detection):
- Two-pass: Pass A populates symbol table; Pass B resolves references.
- DEAL qualified-path uses `.` separator (D-23/PS-2). When emitting SysML JSON (Plan 02-04), translate `.` → `::`.
- Cycle detection: visited-set during `<<specializes>>` chain traversal. On re-entry, emit E2300..E2349 with both spans.
- `.deal/index.json` shape sketch in RESEARCH §Pattern 4 lines 477-498.

From RESEARCH §Anti-Patterns:
- DO NOT use println! — go through anstream::stdout()/stderr() so --color={auto,always,never} works.
- DO NOT enable serde_json/preserve_order — alphabetical-key invariant must hold.
- DO NOT re-serialize Phase 1's diagnostic shape from typed Rust structs (key-order drift) — pass through Zig's deal_diagnostics_json verbatim then envelope-wrap.

From CONTEXT.md decisions:
- D-32 --json envelope: {v, command, deal_version, diagnostics, summary} alphabetical keys.
- D-34 exit codes: 0 success, 1 user error, 2 internal error.

From PATTERNS.md Lane 1 (sema.zig analog) + S-3 (DiagnosticCollector pattern):
- Allocate into the parser arena (D-02 per-handle arena).
- Use DiagnosticCollector.emitFmt — never build Diagnostic literals.
- comptime fmt string (T-DiagInjection mitigation per S-3 excerpt).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Allocate E2xxx codes + build src/sema.zig with 6 blocking checks + .deal/index.json writer + sema_corpus.zig + 6 regression fixtures</name>

  <read_first>
    - deal/src/diagnostics.zig (lines 7-13 D-16 doc comment block; lines 60-98 Codes namespace; lines 123-189 DiagnosticCollector)
    - deal/src/parser_deal.zig (lines 361-382 parseFile entry; pattern for arena + diag collector wiring)
    - deal/src/ast.zig (NodeKind tagged union; declaration variants — list ALL kinds the analyzer must walk)
    - deal/src/json.zig (lines 28-105 emitAst, lines 1143-1173 appendJsonStringEscaped, lines 804-861 writeDiagnostic — alphabetical-key reference)
    - deal/tests/unit/recovery_corpus.zig (lines 31-78 — the Pin struct + per-file enforcement loop is the EXACT template for sema_corpus.zig)
    - deal/tests/unit/strictness_m08_m18_m19.zig (lines 48-66 countCode + firstWithCode helpers)
    - deal/tests/malformed/m01..m20.*.deal — shape pattern for the 6 regression fixtures
    - .planning/phases/02-prove-the-pipeline/02-CONTEXT.md D-20 (Zig owns sema), D-23 (qualified-path IDs), D-33 (E2xxx bands)
    - .planning/phases/02-prove-the-pipeline/02-RESEARCH.md §Pattern 4 (name resolution + scope), §Pattern 5 (cycle detection), §"Pitfall 5" (SysON UUID — informational for Plan 04), Standard Stack Zig core lines 140-148
    - .planning/phases/02-prove-the-pipeline/02-PATTERNS.md Lane 1 (sema.zig analog) + S-1 (arena alloc) + S-3 (DiagnosticCollector) + S-9 (showcase iteration)
    - .planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-03-SUMMARY.md — m_pins table pattern in recovery_corpus
    - tests/showcase/ — the 19 files MUST exit 0 under sema (integration oracle)
    - tests/showcase/deal.toml — workspace + aliases + build.targets blocks (parsed for import resolution per RESEARCH §canonical_refs)
  </read_first>

  <files>
    - deal/src/diagnostics.zig (MODIFIED — append E2xxx Codes block AFTER the existing E0xxx block; update D-16 doc comment lines 7-13 with new ranges)
    - deal/src/sema.zig (NEW — 6 blocking checks + .deal/index.json writer)
    - deal/src/json.zig (MODIFIED — add writeIndexJson() helper using alphabetical-key emitter; reuse appendJsonStringEscaped)
    - deal/src/lib.zig (MODIFIED — call sema.analyze() inside deal_parse() AFTER parser completes; populate diagnostics list with sema findings; add `index_root: ?*sema.SymbolTable` field to DealHandle — Plan 02-03 needs symbol table for lowering; OPTIONAL: add a Zig-internal test helper `deal_write_index_json_to_disk(handle, path)` that writeIndexJson()'s output is written to `<path>/.deal/index.json` so sema_corpus.zig can exercise the on-disk artifact contract)
    - deal/tests/unit/sema_corpus.zig (NEW)
    - deal/tests/unit/_all.zig (MODIFIED — register sema_corpus)
    - deal/tests/regressions/sema/01-name-resolution.deal (NEW — minimal fixture trips E2000..E2099)
    - deal/tests/regressions/sema/02-type-check.deal (NEW — trips E2100..E2199)
    - deal/tests/regressions/sema/03-multiplicity.deal (NEW — trips E2200..E2299)
    - deal/tests/regressions/sema/04-specializes.deal (NEW — trips E2300..E2349 — cycle or type-incompatibility)
    - deal/tests/regressions/sema/05-trace.deal (NEW — trips E2350..E2399 — @trace to non-existent requirement)
    - deal/tests/regressions/sema/06-import.deal (NEW — trips E2400..E2499 — unresolved import path)
  </files>

  <action>
    Sub-step A — E2xxx allocation (commit 1, small):
    (1) Extend deal/src/diagnostics.zig `Codes` namespace AFTER the existing E0xxx block (after `h_did_you_mean`). Add a header comment block: `// Semantic — E2000..E2499 (Phase 2 D-33; new band, never renumbered)`. Allocate:
        - E2000 `e_name_not_found` — "name `%s` not found in scope"
        - E2001 `e_ambiguous_name` — "name `%s` is ambiguous; resolves to %s"
        - E2002 `e_duplicate_declaration` — "duplicate declaration of `%s`; first declared at %d:%d"
        - E2100 `e_type_mismatch` — "type mismatch: expected `%s`, got `%s`"
        - E2101 `e_type_annotation_required` — "type annotation required for `%s`"
        - E2200 `e_multiplicity_violation` — "expected %d..%d, got %d"
        - E2201 `e_multiplicity_required_missing` — "required prop `%s` (multiplicity [1]) missing"
        - E2300 `e_specialization_cycle` — "specialization cycle: %s -> %s"
        - E2301 `e_specialization_type_mismatch` — "specializes target type incompatible: %s vs %s"
        - E2350 `e_trace_target_not_found` — "@trace target `%s` not found"
        - E2400 `e_unresolved_import` — "unresolved import path `%s`"
        - E2401 `e_circular_import` — "circular import: %s -> %s"
        - Each constant carries a one-line doc comment per the existing E0xxx style citing the SPEC acceptance fixture (e.g., `/// Check #1 — tests/regressions/sema/01-name-resolution.deal`).
        Update the `// D-16` documentation comment at lines 7-13 to add the E2xxx band ranges. DO NOT renumber existing codes (D-16 stability).

    Sub-step B — Build sema.zig + index.json (commit 2):
    (2) Create deal/src/sema.zig with the structure from PATTERNS Lane 1:
        - `pub fn analyze(arena: std.mem.Allocator, root: *ast.Node, diag_list: *std.ArrayList(diagnostics.Diagnostic)) !?*SymbolTable`
        - `pub const SymbolTable = struct { entries: std.StringHashMap(*SymbolEntry), imports_graph: []ImportEdge };`
        - `pub const SymbolEntry = struct { id: []const u8 (qualified path), kind: SymbolKind, span: ast.Span, source_file: []const u8, scope_chain: []const []const u8 };`
        - Two-pass walker (RESEARCH Pattern 4):
            - Pass A: walk every declaration; compute qualified path per D-23 (`.` separator between packages and members); insert into symbol table. Emit E2002 on duplicate.
            - Pass B: walk references (identifier, member_access, type_annotation, <<specializes>>, @trace, import). For each: look up qualified path; emit E2000/E2400/E2350 on miss.
        - Cycle detection (RESEARCH Pattern 5): visited-set during <<specializes>> chain; emit E2300 with both spans.
        - Multiplicity check: walk composition usages (.dealx [<tag>]); enforce SD-14 / CS-8 — emit E2200 / E2201 on count out of range. Multiplicity declared on the type annotation per SD-14 (`[1..*]` etc.).
        - Type check: walk type_annotation nodes; verify the annotated type resolves to a declaration of compatible kind. Emit E2100 on `attribute foo : Bar = baz(...)` where Bar is a port_def (kind mismatch).
        - All allocations through `arena` per S-1 (D-02 per-handle arena).
        - All diagnostic emissions through DiagnosticCollector per S-3 (comptime fmt string).
    (3) Add `writeIndexJson(buf, symbol_table)` to deal/src/json.zig. Shape per RESEARCH §Pattern 4 lines 477-498: top-level `{"v":1, "deal_version":"...", "elements":{...}, "imports_graph":[...]}` with alphabetical-key ordering throughout. Use existing appendJsonStringEscaped. DO NOT use std.json.Stringify (Pitfall 1). The `elements` object MUST emit its inner keys in alphabetical (lexicographic) order so the on-disk `.deal/index.json` round-trips identically across two sema runs.
    (4) Modify deal/src/lib.zig: in `deal_parse()` (or a near successor), after parser completes successfully, call `sema.analyze(handle.arena.allocator(), handle.ast_root, &handle.diagnostics)`. Store result in `handle.index_root: ?*sema.SymbolTable`. Sema diagnostics flow into the same handle.diagnostics list (deal_diagnostics_json picks them up unchanged). DO NOT add a new C ABI export for sema in this plan — Plan 02-03 owns the new C ABI work. Sema runs synchronously inside deal_parse for simplicity in Phase 2. Optionally expose an internal test-only helper (no `export`, no `callconv(.c)`) `pub fn write_index_json_to_path(handle: *DealHandle, out_dir: []const u8) !void` that materializes `.deal/index.json` under `out_dir` — sema_corpus.zig (step 6 below) uses this to assert the on-disk alphabetical-key contract.

    Sub-step C — Tests + regression fixtures (commit 3):
    (5) Create 6 regression fixtures in deal/tests/regressions/sema/. Each is hand-crafted minimal `.deal` file that trips ONE specific check:
        - 01-name-resolution.deal: declares `attribute mass : NonExistentType;` (target not declared anywhere) → expects E2000.
        - 02-type-check.deal: declares `port def P { in flow : Power; }` then uses `attribute foo : P;` (port used as attribute type — kind mismatch) → expects E2100.
        - 03-multiplicity.deal + a sibling .dealx that exercises CS-8 prop count violation → expects E2200. (If multiplicity is naturally checked in .dealx, use a .dealx fixture instead; rename file to .dealx in that case and adjust sema_pins.)
        - 04-specializes.deal: declares `part def A <<specializes>> B { } part def B <<specializes>> A { }` (cycle) → expects E2300.
        - 05-trace.deal: declares `requirement def REQ_X { } @trace:<<satisfies>> nonexistent.REQ_Y` → expects E2350.
        - 06-import.deal: declares `import nonexistent.package.{Foo};` → expects E2400.
        Each fixture is otherwise minimal — a header + package + the smallest construct that trips the check. Filename encodes the check number.
    (6) Create deal/tests/unit/sema_corpus.zig per PATTERNS Lane 1 sema_corpus + S-6 two-outcome contract:
        - `const Pin = struct { name, expected_code, adr_basename };` (mirror recovery_corpus.zig:41-47)
        - `const sema_pins = [_]Pin{ ... 6 entries ... }` for the 6 regression fixtures.
        - Per-file loop: parse + sema each fixture; assert FIRST emitted diagnostic code equals pin.expected_code. Reuse `countCode` / `firstWithCode` from strictness_m08_m18_m19.zig.
        - Second loop (S-9 showcase iteration): parse + sema ALL 19 showcase files; assert deal_has_errors() == false (clean).
        - Use deal_parse_internal (test-only helper at src/lib.zig:173-242) so std.testing.allocator catches leaks.
        - **Index materialization sub-test (`sema_corpus.index_json_alphabetical_keys`)** — per WARNING-01: pick ONE showcase file (e.g., `tests/showcase/m01-blocks-and-ports.deal` or the smallest .deal in the corpus); parse + sema via the internal helper; call `lib.write_index_json_to_path(handle, tmpdir)`; read back `tmpdir/.deal/index.json`; assert (a) the file exists, (b) it parses as valid JSON via `std.json.parseFromSlice`, (c) its top-level keys are emitted in alphabetical order (`deal_version, elements, imports_graph, v`), (d) within the `elements` object, the inner keys are alphabetically sorted (compare each adjacent pair and assert lexicographic ordering). This Zig-side gate mirrors the cli/tests/key_order.rs Rust-side defensive test (Plan 02-01 Task 1b) — together they guard the alphabetical-key invariant (D-18) on both sides of the FFI boundary.
    (7) Register sema_corpus in deal/tests/unit/_all.zig.

    CRITICAL: ALL 19 showcase files MUST exit 0 under sema. If a showcase file has a latent issue that this plan's sema flags as an error, FIX the showcase file (it's the integration oracle) OR FIX the sema check (false positive). Do NOT skip-list any showcase file.

    Per D-23: qualified-path IDs use `.` separator. RESEARCH §Pattern 4 line 469 notes "DEAL uses `.` per D-23/PS-2" — when Plan 04 emits SysML JSON, translate `.` → `::` for the `qualifiedName` SysML field. THIS plan does NOT do the translation.

    Per Pitfall 1: writeIndexJson MUST hand-roll alphabetical-key emission per S-2 (D-18) — do NOT use std.json.Stringify.

    Per S-3: every diagnostic emission uses DiagnosticCollector.emitFmt with comptime fmt string (security: T-DiagInjection mitigation).
  </action>

  <verify>
    <automated>cd deal && zig build 2>&1 | tail -10 && echo "BUILD OK"</automated>
    <automated>cd deal && zig build test 2>&1 | tail -20 && echo "ALL TESTS OK"</automated>
    <automated>cd deal && zig build test -Dtest-filter=sema_corpus 2>&1 | grep -q 'passed' && echo "SEMA TESTS OK"</automated>
    <automated>cd deal && zig build phase-1.5-gate 2>&1 | tail -5 && echo "PHASE 1.5 GATE OK"</automated>
    <!-- Defensive grep: filter comment lines before counting tokens -->
    <automated>grep -v '^//' deal/src/diagnostics.zig | grep -cE 'E2[0-9]{3}' | awk '{ if ($1 >= 10) print "E2xxx CODES OK"; else { print "MISSING CODES: " $1; exit 1 } }'</automated>
    <automated>ls deal/tests/regressions/sema/0{1,2,3,4,5,6}-*.deal 2>/dev/null | wc -l | awk '{ if ($1 == 6) print "6 FIXTURES OK"; else { print "MISSING FIXTURES: " $1; exit 1 } }'</automated>
    <!-- WARNING-01 mitigation: end-to-end gate that materializes .deal/index.json from sema_corpus's helper and asserts alphabetical-key shape on disk. -->
    <!-- The sema_corpus.zig sub-test (step 6) writes index.json into the test's tmpdir; this command re-validates the contract via a second-line Python check, providing visible CI evidence independent of Zig's std.json assertion. -->
    <automated>cd deal && rm -rf /tmp/deal-index-gate && mkdir -p /tmp/deal-index-gate && zig build test -Dtest-filter=sema_corpus.index_json_alphabetical_keys -DINDEX_OUT_DIR=/tmp/deal-index-gate 2>&1 | grep -q 'passed' && python3 -c "import json,sys; d=json.load(open('/tmp/deal-index-gate/.deal/index.json')); ks=list(d.keys()); assert ks==sorted(ks), f'top-level keys not alphabetically sorted: {ks}'; elements=d.get('elements', {}); ek=list(elements.keys()); assert ek==sorted(ek), f'elements keys not alphabetically sorted: {ek}'; print('INDEX.JSON ALPHABETICAL KEYS OK')"</automated>
  </verify>

  <acceptance_criteria>
    - deal/src/diagnostics.zig declares E2000..E2499 codes (≥10 constants); D-16 doc comment updated with new bands
    - deal/src/sema.zig implements 6 blocking checks via two-pass walk per RESEARCH Pattern 4
    - Cycle detection in <<specializes>> chain via visited-set per RESEARCH Pattern 5
    - writeIndexJson in src/json.zig emits alphabetical-key {v, deal_version, elements, imports_graph} per D-18; on-disk `.deal/index.json` round-trips through JSON parser with both top-level and `elements`-inner keys alphabetically sorted
    - deal_parse() calls sema.analyze() after parser; sema diagnostics flow into handle.diagnostics
    - sema_corpus.zig passes: all 6 regression fixtures trip pinned E2xxx codes; all 19 showcase files exit 0; index.json alphabetical-key sub-test (step 6) passes
    - phase-1.5-gate still GREEN (regression invariant)
    - No `>= 0` vacuous assertions in any new test (Phase 1.5 invariant)
  </acceptance_criteria>

  <done>
    Sema lands as a self-contained Zig module. 6 regression fixtures each fail with their pinned code. 19 showcase files exit 0. Symbol table available on DealHandle for Plan 02-03's lowering pass. `.deal/index.json` materializes with alphabetical-key shape on disk (D-18 enforced on the Zig side of the FFI boundary, mirroring Plan 02-01 Task 1b's Rust-side key_order.rs).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire `deal check` subcommand + diagnostic renderer + --json envelope (D-32) + exit codes (D-34)</name>

  <read_first>
    - deal/cli/src/main.rs (Plan 02-01 — Check subcommand currently stubbed to exit 2 with "not yet implemented")
    - deal/cli/src/ffi.rs (Plan 02-01 — declares the 6 Phase 1 exports including deal_parse + deal_diagnostics_json + deal_has_errors)
    - deal/cli/src/render.rs (Plan 02-01 — stub with unimplemented!() — fill in body)
    - deal/src/json.zig (lines 776-784 — Diagnostic shape doc comment; lines 804-861 writeDiagnostic — the canonical JSON shape Rust consumes)
    - .planning/phases/02-prove-the-pipeline/02-CONTEXT.md D-32 (--json envelope), D-34 (exit codes 0/1/2)
    - .planning/phases/02-prove-the-pipeline/02-RESEARCH.md §"Pitfall 8: CLI exit code conflation" (lines 786-798), §Anti-Patterns (avoid println!, use anstream)
    - .planning/phases/02-prove-the-pipeline/02-PATTERNS.md Lane 7 (cli/src/render.rs — owo-colors + anstream)
    - .planning/phases/02-prove-the-pipeline/02-VALIDATION.md §Manual-Only Verifications row "Diagnostic visual quality"
  </read_first>

  <files>
    - deal/cli/src/main.rs (MODIFIED — implement Check subcommand body; remove Plan 02-01 "not yet implemented" stub for Check only)
    - deal/cli/src/render.rs (MODIFIED — implement render_diagnostic body)
    - deal/cli/tests/check_subcommand.rs (NEW)
  </files>

  <action>
    Wire `deal check` end-to-end. Implementation steps:

    (1) Implement deal/cli/src/render.rs::render_diagnostic per PATTERNS Lane 7. Signature: `pub fn render_diagnostic<W: std::io::Write>(out: &mut anstream::AutoStream<W>, source_by_file: &HashMap<&str, &str>, diag: &serde_json::Value) -> std::io::Result<()>`. Body:
        - Parse the Phase 1 Diagnostic JSON shape (from src/json.zig:776-784 doc comment): code, fix_it, message, notes, secondary_spans, severity, span.
        - Render: `<severity-color>severity</color>: <message> [<code>]`. severity color: "err" → red.bold, "warn" → yellow.bold, "info" → blue.bold, "hint" → green.bold (per Phase 1 convention).
        - Emit source snippet with carets pointing at span. (Hand-rolled per RESEARCH §Alternatives row — do NOT pull codespan-reporting / miette / ariadne in Phase 2.)
        - Render secondary_spans below with labels.
        - Render notes after the snippet.
        - Render fix_it as a "help: ..." line if present.
        - NEVER use `println!` — always go through the anstream writer (RESEARCH §Anti-Patterns).
    (2) Implement deal/cli/src/main.rs Check subcommand body:
        - For each path in `paths`: read file bytes, call ffi::deal_parse(bytes, mode), check deal_has_errors, fetch deal_diagnostics_json (returns alphabetical-key JSON), Cow-clone the bytes BEFORE deal_free per Pitfall 3.
        - If `--json` is set: assemble the D-32 envelope manually as a string concat (DO NOT round-trip through serde_json::Value — Pitfall 1 risk + RESEARCH §Don't-Hand-Roll line 687 says pass through Zig's diagnostics JSON verbatim). Envelope shape per D-32:
            ```
            {"command":"check","deal_version":"<version>","diagnostics":<raw_diagnostics_array>,"summary":{"errors":N,"hints":N,"warnings":N},"v":1}
            ```
          Alphabetical keys at top-level (command, deal_version, diagnostics, summary, v). Compute summary by counting severity values in the diagnostics array (parse the JSON to count, but emit the raw JSON for diagnostics — do NOT re-serialize).
        - If `--json` is NOT set: for each diagnostic, call render_diagnostic(stderr, source_map, &diag_value). Print a summary line at the end: "X errors, Y warnings".
        - Exit code per D-34: 0 if zero error-severity diagnostics; 1 if ≥1 error-severity (User); 2 only if FFI call fails (Internal).
    (3) Create deal/cli/tests/check_subcommand.rs:
        - Test: `deal check tests/showcase/<a-deal-file>` exits 0 with no diagnostics.
        - Test: `deal check tests/regressions/sema/01-name-resolution.deal` exits 1; stderr contains "E2000".
        - Test: `deal check --json tests/regressions/sema/01-name-resolution.deal` exits 1; stdout is parseable JSON with keys [command, deal_version, diagnostics, summary, v] in alphabetical order (defensive check); diagnostics[0].code == "E2000".
        - Test: `deal check tests/regressions/sema/06-import.deal` exits 1; stderr contains "E2400".
        - Test: `deal check /tmp/nonexistent.deal` exits 2 with "internal error" in stderr (file-not-found is internal per D-34, NOT user error).
        - Test: `deal check --color=always tests/regressions/sema/01-name-resolution.deal` produces stderr that contains ANSI escape codes (e.g., `\x1b[`); `--color=never` produces stderr WITHOUT ANSI codes.
    (4) Verify no `println!` macros in cli/src/render.rs or cli/src/main.rs (Pitfall: RESEARCH §Anti-Patterns).

    The Plan 02-01 stub for Check is REMOVED in this plan; the stubs for Parse / Fmt / Build remain (Plans 02-03/04/05 own those).
  </action>

  <verify>
    <automated>cd deal && cargo build --workspace 2>&1 | tail -10 && echo "CARGO BUILD OK"</automated>
    <automated>cd deal && cargo test --workspace -- check_subcommand 2>&1 | grep -q 'test result: ok' && echo "CHECK SUBCOMMAND TESTS OK"</automated>
    <automated>cd deal && cargo run --bin deal -- check tests/showcase/<any-deal-file>.deal 2>&1; test $? -eq 0 && echo "SHOWCASE CHECK CLEAN OK"</automated>
    <automated>cd deal && cargo run --bin deal -- check tests/regressions/sema/01-name-resolution.deal 2>&1; test $? -eq 1 && echo "EXIT 1 ON SEMA ERROR OK"</automated>
    <automated>cd deal && cargo run --bin deal -- check --json tests/regressions/sema/01-name-resolution.deal | python3 -c 'import json,sys; d=json.loads(sys.stdin.read()); ks=list(d.keys()); assert ks==sorted(ks), f"keys not alphabetical: {ks}"; assert d["diagnostics"][0]["code"]=="E2000"; print("ENVELOPE OK")'</automated>
    <!-- Defensive grep: ensure no println! macros leaked into render/main (Pitfall: RESEARCH §Anti-Patterns) -->
    <automated>grep -v '^//\|^\s*//' deal/cli/src/render.rs deal/cli/src/main.rs | grep -c 'println!' | awk '{ if ($1 == 0) print "NO PRINTLN OK"; else { print "PRINTLN FOUND: " $1; exit 1 } }'</automated>
  </verify>

  <acceptance_criteria>
    - render_diagnostic implemented using owo-colors + anstream (no println!)
    - deal check subcommand parses + analyzes + renders diagnostics
    - --json envelope keys in alphabetical order: command, deal_version, diagnostics, summary, v
    - --json envelope's diagnostics field passes through Zig output verbatim (no re-serialization)
    - Exit 0 on clean, exit 1 on any error-severity diagnostic, exit 2 on FFI/internal error (D-34)
    - --color={always,never,auto} controls ANSI output via anstream (verified by escape-code grep)
    - check_subcommand.rs passes all 6 tests
  </acceptance_criteria>

  <done>
    `deal check` is the first user-facing pipeline surface. It catches sema errors with rustc-quality diagnostics, supports machine-readable --json envelope (D-32), and respects exit-code semantics (D-34).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted DEAL source → sema analyzer | Adversarial inputs (deeply nested specializes chains, large symbol tables) must not panic or run unbounded |
| Sema diagnostic strings → CLI rendering | Format-string injection risk via DiagnosticCollector |
| FFI byte buffer → Rust diagnostic JSON parse | Use-after-free if Rust holds pointer past deal_free |
| `.deal/index.json` write → filesystem | Path traversal risk if file paths from source bleed into the index |
| `--json` envelope → consumer (CI, Phase 3 LSP) | Schema drift breaks downstream consumers (public contract per CONTEXT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Denial of service | <<specializes>> cycle in adversarial input | mitigate | Cycle detection visited-set per RESEARCH §Pattern 5; emit E2300 on re-entry; max chain depth bounded by symbol-table size (no unbounded recursion) |
| T-02-09 | Denial of service | Deeply-nested name resolution (10K-level scope chain) | accept | Phase 1 parser already bounds nesting via fixed-depth parser stack; sema walks the AST tree which is depth-bounded by the parser |
| T-02-10 | Tampering | Format-string injection via diagnostic messages | mitigate | DiagnosticCollector.emitFmt takes comptime fmt string (Pattern S-3) — runtime user input cannot become format directives |
| T-02-11 | Information disclosure | `.deal/index.json` leaking absolute paths outside workspace | mitigate | sema records source_file as workspace-relative path (strip the workspace root before persisting); verified by grep gate in sema_corpus.zig |
| T-02-12 | Tampering | `--json` envelope key reordering breaking downstream consumers | mitigate | Hand-rolled string concat at the top level (D-32) + serde_json BTreeMap default for inner; key_order.rs test (Plan 02-01) catches preserve_order leaks; integration test in check_subcommand.rs asserts top-level keys are sorted; sema_corpus.zig sub-test asserts on-disk index.json alphabetical-key shape (Zig-side mirror) |
| T-02-13 | Spoofing | Use-after-free of diagnostic JSON pointer past deal_free | mitigate | Pitfall 3 mitigation: Rust Cow-clones FFI byte buffers BEFORE any deal_free call; mirror Phase 1.5 Plan 04 "gpa.dupe-before-free" |
| T-02-14 | Repudiation | Exit code conflation (panic returns 1 instead of 2) | mitigate | CliError enum splits User/Internal (Plan 02-01); check_subcommand.rs asserts file-not-found exits 2, sema error exits 1; per Pitfall 8 |
| T-02-SC | Tampering | npm/pip/cargo installs (Supply-Chain) | mitigate | Plan 02-01 already approved Cargo workspace deps via Package Legitimacy Audit; this plan adds no new package installs (only uses workspace-pinned crates) |
</threat_model>

<verification>
- `zig build` exits 0
- `zig build test` exits 0 including sema_corpus.zig
- `zig build phase-1.5-gate` exits 0 (regression invariant)
- `cargo build --workspace` exits 0
- `cargo test --workspace` exits 0 including check_subcommand.rs
- `deal check tests/showcase/*.deal` exits 0 for all 19 showcase files
- `deal check tests/regressions/sema/01..06.deal` exits 1 with the pinned E2xxx code in stderr
- `deal check --json tests/regressions/sema/01-name-resolution.deal` emits JSON with alphabetical top-level keys
- `deal check --color=always/never` controls ANSI output
- No `println!` in cli/src/{render,main}.rs (grep gate)
- No `>= 0` vacuous assertions in sema_corpus.zig (Phase 1.5 invariant)
- `.deal/index.json` materialized by sema_corpus sub-test has alphabetical top-level + alphabetical `elements`-inner keys (D-18 enforced on-disk)
</verification>

<success_criteria>
1. src/sema.zig implements all 6 blocking checks (REQ-phase-2-1-semantic-analyzer)
2. E2000..E2499 codes declared in diagnostics.zig per D-33 (no renumbering of existing codes)
3. .deal/index.json written with alphabetical-key shape (RESEARCH Pattern 4); WARNING-01 gate passes on disk
4. 6 regression fixtures each fail with pinned E2xxx code; 19 showcase files exit 0
5. `deal check` CLI wired with human-mode rendering + --json envelope + D-34 exit codes
6. Phase 1.5 gate still GREEN
</success_criteria>

<output>
Create `.planning/phases/02-prove-the-pipeline/02-02-SUMMARY.md` when done. Summary must record: (a) exact list of E2xxx codes allocated; (b) regression fixture filenames + expected codes; (c) confirmation that all 19 showcase files exit 0 under `deal check`; (d) example --json envelope output for a sample fixture; (e) example top-level keys of the on-disk `.deal/index.json` (asserting alphabetical order); (f) Phase 1.5 gate still GREEN.
</output>
</output>
