---
phase: 02-prove-the-pipeline
plan: 05
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - deal/src/fmt.zig
  - deal/src/lib.zig
  - deal/include/deal.h
  - deal/cli/src/ffi.rs
  - deal/cli/src/main.rs
  - deal/tests/unit/fmt_roundtrip.zig
  - deal/tests/unit/_all.zig
  - deal/cli/tests/fmt_subcommand.rs
autonomous: true
requirements:
  - REQ-phase-2-4-formatter
user_setup: []

must_haves:
  truths:
    - "src/fmt.zig walks the AST + attached comments and emits canonical source bytes"
    - "deal_format C ABI export added; total exports = 8 per CONTEXT D-21/D-22"
    - "All 19 showcase files round-trip: parse → format → parse → byte-equal AST JSON"
    - "Every comment from the original file survives at its original attachment point after format"
    - "deal fmt CLI subcommand wired; --json mode wraps the D-32 envelope around any emitted diagnostics"
    - "Round-trip invariants enforced: one-space-around-binaries, four-space indent, one-blank-line between top-decls, LM-3 single→double quote normalization, NO trailing commas (E0122)"
  artifacts:
    - path: "deal/src/fmt.zig"
      provides: "AST → canonical source pretty-printer with comment preservation"
      min_lines: 500
    - path: "deal/src/lib.zig"
      provides: "deal_format C ABI export + cached_fmt_bytes on DealHandle"
      contains: "deal_format"
    - path: "deal/include/deal.h"
      provides: "deal_format declaration with doxygen"
      contains: "deal_format"
    - path: "deal/cli/src/ffi.rs"
      provides: "deal_format extern decl"
      contains: "deal_format"
    - path: "deal/tests/unit/fmt_roundtrip.zig"
      provides: "Parse→format→parse byte-equality test over 19 showcase files"
      contains: "fmt_roundtrip"
  key_links:
    - from: "deal/src/fmt.zig"
      to: "deal/src/ast.zig comment fields (Plan 02-01)"
      via: "Emit leading_comments + doc_comment + decl + trailing_comments in order"
      pattern: "leading_comments|trailing_comments|doc_comment"
    - from: "deal/src/lib.zig deal_format"
      to: "deal/src/fmt.zig emitFormatted"
      via: "lazy emission + cached_fmt_bytes field on DealHandle"
      pattern: "cached_fmt_bytes|emitFormatted"
    - from: "deal/cli/src/main.rs Fmt subcommand"
      to: "deal_format via FFI"
      via: "extern \"C\" call + write to stdout or in-place file"
      pattern: "deal_format"
---

<objective>
Implement `deal fmt` end-to-end: a Zig pretty-printer walking AST + attached comments (D-21), a new `deal_format` C ABI export (D-22 — brings total to 8), a 19-file round-trip test that proves parse→format→parse produces byte-equal ASTs with every comment surviving at its original attachment point, and a Rust CLI subcommand that wires it together with stdin/stdout + in-place editing.

Purpose: REQ-phase-2-4-formatter is one of the 9 locked Phase 2 requirements. The round-trip invariant is THE single hardest correctness property in Phase 2 — any edge case in comment attachment (Plan 02-01) or formatting (this plan) breaks it. Plan 02-01 attached comments structurally per D-28 so this plan's printer never has to scan original source for "missed" comments (RESEARCH §Pattern 3 — Phase 2 simplification).

Output: A new `src/fmt.zig` module (~500-800 LOC); a new `deal_format` C ABI export raising the surface to 8; an updated `deal.h` + ffi.rs; a `fmt_roundtrip.zig` test over all 19 showcase files; a `deal fmt` subcommand wired with stdin/stdout + path arguments + --json envelope mode.

This plan runs in parallel with Plan 02-04 (both depend on Plan 02-03 IR-LOCK; both touch the cli/ crate but on disjoint file sets — Plan 02-04 owns sysml_v2.rs + schema_registry.rs + Build subcommand; this plan owns fmt.zig + Fmt subcommand). The shared touchpoint is `cli/src/main.rs` + `cli/src/ffi.rs` — both plans append to these files; merge conflicts must be resolved by the executor in whichever plan lands second. See the dedicated `<merge_protocol>` block below for the 3-command post-merge gate (per WARNING-04 mitigation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-prove-the-pipeline/02-CONTEXT.md
@.planning/phases/02-prove-the-pipeline/02-RESEARCH.md
@.planning/phases/02-prove-the-pipeline/02-PATTERNS.md
@.planning/phases/02-prove-the-pipeline/02-SPEC.md
@.planning/phases/02-prove-the-pipeline/02-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts for the executor. -->

From CONTEXT D-21 + D-22:
- deal_format(handle, out_ptr, out_len) — Zig-owned printer; Rust CLI calls via FFI.
- Total C ABI exports rises 7 → 8 (Plan 02-03 added deal_ir_json; this plan adds deal_format).
- Length-prefixed UTF-8 (D-11); arena-owned bytes; NO NUL-terminator.

From CONTEXT D-25:
- IR is comment-free. fmt walks AST (NOT IR).
- Comments live on AST (leading_comments / trailing_comments / doc_comment per Plan 02-01).

From RESEARCH §Pattern 3 (AST → canonical source pretty-printing):
- Walk AST. At each declaration: emit leading_comments → doc_comment → declaration body → trailing_comments.
- Whitespace: 1 space around binary operators, 4-space indent, 1 blank line between top-level decls.
- LM-3: normalize single quotes to double quotes (SD-12 confirms).
- NO trailing commas — grammar §_ArgumentList disallows them; E0122 fires on re-parse if introduced (Phase 1.5 invariant).
- Comments emitted verbatim — DO NOT re-flow /** */ content (Phase 2 simplification per RESEARCH §Pattern 3).
- Phase 2 simplification per RESEARCH §Pattern 3 line 453: "because D-28 attaches comments STRUCTURALLY to AST nodes (not as missed spans), the printer never needs to scan the original source for 'missed' comments — it just emits the attached fields in order. Rustfmt's missed-spans complexity is avoided."

From CONTEXT Claude's Discretion (resolved here):
- deal_format returns bytes via out_ptr/out_len (NOT a caller-supplied path) per LSP-friendliness — matches deal_ast_json shape.

From PATTERNS.md Lane 3 §src/fmt.zig:
- Writer pattern: function signature `emitFormatted(allocator, root) ![]const u8` — same as emitAst.
- Internal helpers take `(allocator, *std.ArrayList(u8), node)` — same as writeNode.
- Final toOwnedSlice returns arena-owned bytes — same lifetime contract.

From PATTERNS.md Lane 3 §tests/unit/fmt_roundtrip.zig:
- Iterate 19 showcase files (S-9).
- Per file: parse → format → re-parse → compare AST JSON byte-equal.
- Additionally: walk both ASTs to assert every comment from ast_a survives in ast_b at the same attachment point (counts + spans match modulo formatting normalization).

From PATTERNS S-1 (per-handle arena): all fmt allocations go through handle.arena.allocator().

From PATTERNS S-7 (test-only internal helpers): may add deal_format_internal for std.testing.allocator leak detection.

From RESEARCH §Anti-Patterns: no println! in CLI rendering; use anstream.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build src/fmt.zig + deal_format C ABI export + fmt_roundtrip.zig (19-file byte-equality test)</name>

  <read_first>
    - deal/src/ast.zig (post-Plan 02-01 — declaration variants carry leading_comments / trailing_comments / doc_comment; DocComment struct at line 393)
    - deal/src/json.zig (lines 28-105 emitAst — writer-pattern template per PATTERNS Lane 3; lines 1143-1173 appendJsonStringEscaped — reuse for string escaping inside comment text)
    - deal/src/lib.zig (lines 36-45 DealHandle — extend with cached_fmt_bytes; lines 285-302 deal_ast_json — exact template for deal_format export)
    - deal/include/deal.h (lines 229-276 deal_ast_json doxygen — template for deal_format doc block)
    - deal/cli/src/ffi.rs (post-Plan 02-03 — extends with deal_format after Plan 04 correction; Plan 02-04 fixed the comment to say "Plan 02-05")
    - deal/tests/unit/parser_deal_snapshot.zig (lines 20-36 — 15 .deal file iteration; copy as basis for fmt_roundtrip)
    - deal/tests/unit/parser_dealx_snapshot.zig (4 .dealx file iteration)
    - deal/tests/unit/determinism_parse_twice.zig (lines 14-28 — buffer lifetime contract; gpa.dupe-before-free pattern)
    - .planning/phases/02-prove-the-pipeline/02-CONTEXT.md D-21 (Zig owns printer), D-25 (IR comment-free; fmt walks AST), D-28..D-31 (comment attachment surfaces)
    - .planning/phases/02-prove-the-pipeline/02-RESEARCH.md §Pattern 3 (lines 432-455 — AST → source rules), §Anti-Patterns (lines 670-671 — don't re-flow /** */ content)
    - .planning/phases/02-prove-the-pipeline/02-PATTERNS.md Lane 3 (full section — fmt.zig analog patterns)
    - spec/grammar/deal.ebnf (one-paragraph spot read of the production list to remind the executor of every declaration kind the printer must handle — port_def, part_def, requirement_def, allocation_def, etc.)
    - spec/grammar/dealx.ebnf (one-paragraph spot read of the system / connect / expose production shapes)
  </read_first>

  <files>
    - deal/src/fmt.zig (NEW — ~500-800 LOC; AST + comments → canonical source bytes)
    - deal/src/lib.zig (MODIFIED — add cached_fmt_bytes field to DealHandle; add deal_format export mirroring deal_ast_json; add deal_format_internal test-only helper per S-7)
    - deal/include/deal.h (MODIFIED — append deal_format declaration with doxygen mirroring deal_ast_json)
    - deal/cli/src/ffi.rs (MODIFIED — append deal_format extern decl; remove the "Plan 02-05 will append" comment since it's now resolved)
    - deal/tests/unit/fmt_roundtrip.zig (NEW)
    - deal/tests/unit/_all.zig (MODIFIED — register fmt_roundtrip)
  </files>

  <action>
    Sub-step A — Build the printer (commit 1):
    (1) Create deal/src/fmt.zig per PATTERNS Lane 3 §src/fmt.zig:
        - `pub fn emitFormatted(allocator: std.mem.Allocator, root: *ast.Node) ![]const u8` — signature mirrors json.emitAst.
        - Internal helpers: `fn writeNode(allocator, *std.ArrayList(u8), node, indent: u32) !void` — mirror json.writeNode shape.
        - Track current indent (u32, increments by 4 per nesting level).
        - **At every declaration node**, emit in this order (per RESEARCH §Pattern 3 lines 434-440):
            1. `leading_comments[]` — one per line, indented to declaration's column.
            2. `doc_comment` if present — verbatim, no re-flow (RESEARCH §Anti-Patterns).
            3. The declaration body itself, recursively.
            4. `trailing_comments[]` — single same-line trailer if present (only one trailing per line; multiple would be an unreachable invariant).
        - **Declaration body emission** — dispatch on NodeKind, calling per-kind writers:
            * `writePartDef`, `writePortDef`, `writeRequirementDef`, `writeAttributeDef`, `writeActionDef`, etc. — one per declaration kind.
            * For .dealx tags: `writeSystem`, `writeConnect`, `writeExpose`, `writeTraceabilityBlock`, etc.
        - **Round-trip invariants** (RESEARCH §Pattern 3 lines 442-446 + §Anti-Patterns):
            * Exactly 1 space around binary operators (no extra spaces).
            * 4-space indent (NOT tabs); track via `indent` parameter.
            * Exactly 1 blank line between top-level declarations.
            * Single quotes → double quotes per LM-3 / SD-12. String content escaped per existing src/json.zig appendJsonStringEscaped helper IF the string contains characters needing escapes.
            * NEVER emit trailing commas (Phase 1.5 E0122 will fire on re-parse). Argument lists, member lists, tag attribute lists — comma-separated WITHOUT a trailing comma.
            * Backtick strings stay backticked; double-quote strings stay double-quoted; single-quote strings become double-quoted (normalize per LM-3).
        - Wrap output: `var buf: std.ArrayList(u8) = .empty;` allocated in arena; `return try buf.toOwnedSlice(allocator);` per S-1 + PATTERNS Lane 3.
        - DO NOT walk IR — fmt walks AST per D-25. The IR Document from Plan 02-03 is irrelevant here; this printer reads ast.Node directly.

    Sub-step B — C ABI export + lib.zig wiring (commit 2):
    (2) Extend deal/src/lib.zig DealHandle (lines 36-45) with `cached_fmt_bytes: ?[]const u8 = null` field.
    (3) Add `pub export fn deal_format(handle_ptr, out_ptr, out_len) callconv(.c) bool` to deal/src/lib.zig mirroring `deal_ast_json` (lines 285-302):
        - Cast handle; if cached_fmt_bytes is null, call `fmt.emitFormatted(handle.arena.allocator(), handle.ast_root)`; cache the result.
        - Set out_ptr.* and out_len.*; return true.
        - On any allocation failure, return false (caller treats as internal error / exit 2 per D-34).
    (4) Add `pub fn deal_format_internal(allocator, source) ![]const u8` test-only helper per S-7 — NO `export`, NO `callconv(.c)`. For std.testing.allocator leak detection inside fmt_roundtrip.zig.
    (5) Modify deal/include/deal.h — append `deal_format` declaration with doxygen mirroring `deal_ast_json` (lines 229-276 template). Update the symbol-count assertion comment if it mentions a count.
    (6) Modify deal/cli/src/ffi.rs — append the `deal_format` extern declaration mirroring deal_ast_json shape; REMOVE the "Plan 02-05 will append deal_format" forward-reference comment Plan 02-04 left.
    (7) Verify nm count is now 8:
        ```
        nm -gU deal/zig-out/lib/libdeal.a | grep -cE 'T _?deal_(parse|free|has_errors|diagnostics_count|ast_json|diagnostics_json|ir_json|format)'
        ```
        MUST return 8. NOT 7, NOT 9.
    (8) Verify deal_format_internal is NOT in nm (S-7 invariant).

    Sub-step C — Round-trip test (commit 3):
    (9) Create deal/tests/unit/fmt_roundtrip.zig per PATTERNS Lane 3 §fmt_roundtrip:
        - Iterate the 19 showcase files (15 .deal + 4 .dealx) per S-9. Use the verbatim file list from determinism_parse_twice.zig:36-56.
        - For each file:
            * `const source = try std.fs.cwd().readFileAlloc(testing.allocator, path, max_size);`
            * `const handle_a = lib.deal_parse(source.ptr, source.len, mode);` — assert non-null.
            * `var fmt_ptr: [*]const u8 = undefined; var fmt_len: usize = 0;` then `try expect(lib.deal_format(handle_a, &fmt_ptr, &fmt_len));`
            * `const formatted = try testing.allocator.dupe(u8, fmt_ptr[0..fmt_len]);` — gpa.dupe-before-free per Pitfall 3.
            * `const ast_a_json = ...emit AST JSON from handle_a ...dupe...`
            * `lib.deal_free(handle_a);`
            * `const handle_b = lib.deal_parse(formatted.ptr, formatted.len, mode);`
            * `const ast_b_json = ...emit AST JSON from handle_b ...dupe...`
            * `lib.deal_free(handle_b);`
            * `try expectEqualStrings(ast_a_json, ast_b_json);` — byte-equal ASTs prove structural round-trip.
            * Comment-preservation check: count leading_comments + trailing_comments + doc_comment entries in ast_a_json vs ast_b_json; counts MUST be equal per file. (Spans may shift if formatting changes whitespace; counts and per-declaration ATTACHMENT must be preserved.)
        - Test names: `fmt_roundtrip.showcase_<basename>` per file (19 tests).
        - Use deal_parse_internal / deal_format_internal where possible to leverage std.testing.allocator leak detection. Public-ABI path (deal_parse + deal_format) is also tested via at least one file per Pitfall 3 (real-world memory contract).
    (10) Register fmt_roundtrip in deal/tests/unit/_all.zig.

    Sub-step D — Integration check (commit 4 or rolled into commit 3):
    (11) Run `zig build phase-1.5-gate` to verify the regression invariant holds.
    (12) Run `zig build test -Dtest-filter=fmt_roundtrip` and confirm all 19 sub-tests pass. If any showcase file fails round-trip:
        - DO NOT modify the showcase file (it's the integration oracle).
        - Diagnose: was the failure in comment attachment (Plan 02-01 bug — escalate)? In the formatter (this plan — fix in src/fmt.zig)? In quote normalization (LM-3 over-applied to a backtick string)? In an unexpected trailing comma the grammar accepts (Phase 1.5 E0122 says no — grammar is the authority)?
        - Document any showcase-revealed grammar ambiguity in 02-05-SUMMARY.md as a deferred follow-up; do NOT defer the round-trip failure itself.

    Anti-pattern (Pitfall): NEVER re-flow doc-comment content; emit verbatim per RESEARCH §Anti-Patterns line 670.
    Anti-pattern: NO println! in fmt.zig — it's a Zig module emitting bytes via std.ArrayList, not stdout.
    Anti-pattern: NO walking IR in fmt.zig — fmt walks AST per D-25 (Plan 02-03 IR is irrelevant here).
  </action>

  <verify>
    <automated>cd deal && zig build 2>&1 | tail -10 && echo "BUILD OK"</automated>
    <automated>cd deal && zig build test 2>&1 | tail -20 && echo "ALL TESTS OK"</automated>
    <automated>cd deal && zig build test -Dtest-filter=fmt_roundtrip 2>&1 | grep -q 'passed' && echo "FMT ROUND-TRIP OK"</automated>
    <automated>cd deal && zig build phase-1.5-gate 2>&1 | tail -5 && echo "PHASE 1.5 GATE OK"</automated>
    <automated>cd deal && nm -gU zig-out/lib/libdeal.a 2>/dev/null | grep -cE 'T _?deal_(parse|free|has_errors|diagnostics_count|ast_json|diagnostics_json|ir_json|format)' | awk '{ if ($1 == 8) print "8 EXPORTS OK"; else { print "EXPORT COUNT WRONG: " $1; exit 1 } }'</automated>
    <automated>! nm -gU deal/zig-out/lib/libdeal.a | grep -qE 'deal_format_internal|deal_lower_internal' && echo "INTERNAL HELPERS HIDDEN OK"</automated>
    <automated>wc -l deal/src/fmt.zig | awk '{ if ($1 >= 300) print "FMT SIZE OK"; else { print "FMT TOO SHORT: " $1; exit 1 } }'</automated>
    <!-- Defensive grep: ensure no IR walking leaked into fmt.zig (D-25) -->
    <automated>grep -v '^//\|^\s*//' deal/src/fmt.zig | grep -cE 'ir\.|Document|lowering' | awk '{ if ($1 == 0) print "NO IR LEAK IN FMT OK"; else { print "FMT WALKS IR (D-25 VIOLATION): " $1; exit 1 } }'</automated>
    <!-- Defensive grep: ensure doc-comment content is not re-flowed (Anti-Pattern) -->
    <automated>grep -v '^//\|^\s*//' deal/src/fmt.zig | grep -cE 'reflow|reFlow|wrap_doc|wrapDoc' | awk '{ if ($1 == 0) print "NO DOC REFLOW OK"; else { print "DOC REFLOW DETECTED (Anti-Pattern)"; exit 1 } }'</automated>
  </verify>

  <acceptance_criteria>
    - src/fmt.zig ≥300 LOC; walks AST + comments per RESEARCH §Pattern 3
    - fmt.zig does NOT reference ir/lowering modules (D-25 — fmt walks AST only)
    - deal_format C ABI export added; nm count is exactly 8 (Plan 02-03 brought to 7; this plan adds the 8th)
    - deal_format_internal NOT in nm (S-7)
    - deal.h has doxygen for deal_format mirroring deal_ast_json template
    - cli/src/ffi.rs declares deal_format extern; the "Plan 02-05 will append" comment is removed
    - fmt_roundtrip.zig passes for ALL 19 showcase files (no skip-list)
    - Comment-preservation check passes per file (count match)
    - Quote normalization works: single-quoted strings → double-quoted on round-trip; backticks unchanged; double-quoted unchanged
    - NO trailing commas in formatter output (E0122 doesn't fire on re-parse)
    - phase-1.5-gate still GREEN
  </acceptance_criteria>

  <done>
    Formatter lands as Zig module. C ABI raises to 8 exports. Round-trip enforced over all 19 showcase files with comment preservation. Ready for CLI wiring.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire `deal fmt` CLI subcommand + stdin/stdout + in-place editing + tests</name>

  <read_first>
    - deal/cli/src/main.rs (Plan 02-02 — Check subcommand wired; Plan 02-04 — Build subcommand wired; Fmt stub STILL exits 2 with "not yet implemented")
    - deal/cli/src/ffi.rs (Task 1 — declares deal_format extern)
    - deal/cli/src/render.rs (Plan 02-02 — render_diagnostic implementation; fmt subcommand uses it for any sema diagnostics during pre-format check)
    - .planning/phases/02-prove-the-pipeline/02-CONTEXT.md D-21 (deal_format returns bytes per LSP-friendliness), D-32 (--json envelope), D-34 (exit codes)
    - .planning/phases/02-prove-the-pipeline/02-RESEARCH.md §Pitfall 3 (FFI lifetime — Cow-clone bytes BEFORE deal_free)
    - .planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-04-SUMMARY.md (gpa.dupe-before-free lifetime pattern)
  </read_first>

  <files>
    - deal/cli/src/main.rs (MODIFIED — implement Fmt subcommand body; remove "not yet implemented" stub for Fmt)
    - deal/cli/tests/fmt_subcommand.rs (NEW)
  </files>

  <action>
    Implement `deal fmt` subcommand:

    (1) Modify deal/cli/src/main.rs Fmt subcommand body (REMOVE the stub):
        - Accept `paths: Vec<PathBuf>` (existing in Plan 02-01 stub). Also accept an implicit "stdin if paths is empty OR a single `-` path" mode.
        - Add a `--check` flag (does NOT modify files; exits 1 if any file would be reformatted; matches `gofmt -l` semantics) — declare in clap derive.
        - Add a `--stdout` flag (writes formatted output to stdout instead of in-place editing) — useful for piping; default behavior with one path is in-place editing.
        - Behavior matrix:
            * `deal fmt foo.deal` → reads file, formats, writes back in-place atomically (rename-from-temp pattern).
            * `deal fmt foo.deal bar.deal` → both formatted in-place.
            * `deal fmt --stdout foo.deal` → formatted output to stdout; file untouched.
            * `deal fmt -` or `cat foo.deal | deal fmt` → stdin in, stdout out.
            * `deal fmt --check foo.deal` → no modification; exit 0 if already formatted, exit 1 if would change.
            * `deal fmt --json foo.deal` → if any diagnostics fire pre-format (sema errors), emit D-32 envelope to stdout; on success, no stdout output unless --stdout is also set (in which case formatted bytes go to stdout AND envelope is suppressed — formatter is silent on success).
        - Per file: read bytes; call ffi::deal_parse; if deal_has_errors, render diagnostics via render_diagnostic (or D-32 envelope) and exit 1 (D-34); otherwise call ffi::deal_format; Cow-clone formatted bytes BEFORE ffi::deal_free (Pitfall 3); write to destination (in-place, stdout, or compare for --check).
        - Refuse to format if `deal config user.name/email` is unconfigured per PROJECT.md FS-3? **Decision**: FS-3 says `deal fmt refuses to run without configured identity`. Implement this. Check for `git config user.name` (and/or a future `deal config` namespace — for now use git config as proxy). If unset, exit 2 with stderr "deal fmt requires git config user.name/email to be set per FS-3". (Phase 4 may add a deal-specific config namespace; for Phase 2 git config is the source of truth.)
        - Exit codes per D-34: 0 success (no reformat needed or reformat applied); 1 user error (any sema error OR --check found a file that needed reformat); 2 internal (FFI failure, IO failure, missing git config).

    (2) Create deal/cli/tests/fmt_subcommand.rs:
        - Test: `deal fmt --stdout tests/showcase/<file>.deal` exits 0; stdout is the formatted source.
        - Test: `deal fmt --check tests/showcase/<file>.deal` exits 0 IF the showcase is already canonically formatted (this is the expected state — showcase is hand-curated). If exits 1, the showcase file needs a one-shot `deal fmt` pass committed first (record in summary as a follow-up; do NOT skip-list).
        - Test: `echo 'package foo;' | deal fmt -` exits 0; stdout contains `package foo;\n`.
        - Test: `deal fmt --json tests/regressions/sema/01-name-resolution.deal` exits 1; stdout is parseable JSON with D-32 envelope; diagnostics[0].code == "E2000".
        - Test: in-place editing — create a tmpfile with `package foo;\n`, run `deal fmt <tmpfile>`, assert file contents are formatted bytes; assert exit 0; assert atomic write semantics (file is replaced by rename, not truncated-then-written — verify by tail-appended-byte check between read and write).
        - Test: `deal fmt /tmp/nonexistent.deal` exits 2 with stderr "internal error" (file-not-found is internal per D-34).
        - Test (FS-3 identity gate): temporarily unset `git config user.name`; `deal fmt foo.deal` exits 2 with stderr mentioning "FS-3" or "user.name". Restore git config after.

    Pitfall 3 mitigation: Rust side MUST Cow-clone formatted bytes BEFORE deal_free; verified by leak-detection tests where available, otherwise by code review against the gpa.dupe pattern.
    LSP friendliness (D-21): deal_format returning bytes (not writing to a path) is the right primitive — Phase 3 LSP will call deal_format directly for textDocument/formatting without touching the filesystem.
    Atomic in-place edit: write to a temp filename then rename to the destination — ensures no partial-write corruption on power-loss.
  </action>

  <verify>
    <automated>cd deal && cargo build --workspace 2>&1 | tail -10 && echo "BUILD OK"</automated>
    <automated>cd deal && cargo test --workspace -- fmt_subcommand 2>&1 | grep -q 'test result: ok' && echo "FMT SUBCOMMAND TESTS OK"</automated>
    <automated>cd deal && cargo run --bin deal -- fmt --stdout tests/showcase/$(ls tests/showcase/*.deal | head -1 | xargs basename) 2>&1 | head -5 && echo "STDOUT FMT OK"</automated>
    <automated>cd deal && echo 'package foo;' | cargo run --bin deal -- fmt - 2>/dev/null | grep -q 'package foo' && echo "STDIN FMT OK"</automated>
    <automated>cd deal && cargo run --bin deal -- fmt --json tests/regressions/sema/01-name-resolution.deal 2>/dev/null | python3 -c 'import json,sys; d=json.loads(sys.stdin.read()); assert d["diagnostics"][0]["code"]=="E2000"; print("FMT --JSON ENVELOPE OK")' || echo "Note: may exit 1 — that is expected"</automated>
    <automated>grep -v '^//\|^\s*//' deal/cli/src/main.rs | grep -c 'println!' | awk '{ if ($1 == 0) print "NO PRINTLN OK"; else { print "PRINTLN LEAK: " $1; exit 1 } }'</automated>
  </verify>

  <acceptance_criteria>
    - `deal fmt foo.deal` formats in-place atomically (rename-from-temp)
    - `deal fmt --stdout foo.deal` writes to stdout
    - `deal fmt -` (or empty paths) reads stdin
    - `deal fmt --check foo.deal` exits 0 if already formatted, 1 if would change
    - `deal fmt --json` emits D-32 envelope for diagnostics; suppressed on success
    - Exit codes per D-34: 0 success, 1 user error (sema fail or --check mismatch), 2 internal
    - FS-3 identity gate: refuses to run without git config user.name/email
    - All fmt_subcommand.rs tests pass
    - Showcase round-trips already (REQ-phase-2-4-formatter exit criterion met via Task 1 fmt_roundtrip.zig)
  </acceptance_criteria>

  <done>
    `deal fmt` is the third user-facing pipeline surface (after `deal check` and `deal build`). LSP-friendly via byte-returning C ABI. FS-3 identity gate enforced. All 19 showcase files round-trip.
  </done>
</task>

</tasks>

<merge_protocol>
## Parallel-Wave Merge Protocol (Plans 02-04 and 02-05)

This plan and Plan 02-04 are BOTH Wave 4 and BOTH depend only on Plan 02-03. They run in parallel by design (PATTERNS S-8). They share two touchpoint files: `deal/cli/src/main.rs` (this plan implements Fmt subcommand body; Plan 02-04 implements Build subcommand body) and `deal/cli/src/ffi.rs` (this plan appends the `deal_format` extern decl and removes the Plan 02-04-left forward-reference comment; Plan 02-04 fixed the Plan 02-03 forward-reference comment). Both files take ADDITIVE changes — no shared edits to the same code regions are expected — but auto-merge may still produce conflicts in the import block, the clap `Commands` enum, or the dispatch `match` arms.

**After git auto-merge resolves (or after manual conflict resolution), the executor of WHICHEVER plan lands SECOND in CI/local execution MUST run all three of the following commands in this exact order, before proceeding to the next task in either plan:**

1. **`cd deal && zig build phase-1.5-gate`** — proves the Zig side still compiles and the existing Phase 1.5 invariants (ABI count, determinism, span-containment) are intact. This plan adds `deal_format` (8th export); Plan 02-04 does NOT touch the Zig side. After merge, the Zig artifact must still be coherent.

2. **`cd deal && cargo build --workspace`** — proves the Rust side still compiles after merging this plan's `Fmt` match arm + new `deal_format` extern in `cli/src/ffi.rs` with Plan 02-04's `Build` match arm + module declarations. Any duplicate-import, duplicate-mod, or missing-arm conflict surfaces here.

3. **`cd deal && cargo test --workspace`** — proves no integration-test regressions. This includes `check_subcommand` (Plan 02-02), `sysml_validate` (Plan 02-04), `golden_sysml_v2` (Plan 02-04), `golden_fixture_schema_validity` (Plan 02-04), `fmt_subcommand` (this plan), and all `tests/ffi` tests. A regression here means the merge silently broke a behavior contract.

**If any of these three fail post-merge, the executor MUST resolve the conflict by hand (do NOT proceed to the next task in either plan) and re-run all three until they all pass.** The principle: parallel work is fine, but the union of two parallel plans is not validated until ALL THREE post-merge commands exit 0.

**Option B was considered (serialize: change this plan's `depends_on: [02-03]` to `[02-04]` so Plan 05 sees Plan 04's merged state before starting). Rejected** because:
- The merge surface is small and additive (additional clap variants + additional extern decl + comment edit).
- Both plans are self-contained until the final wire-up step inside `cli/src/main.rs`.
- Serializing would cost ~30–40% of a context window (this plan is heavy — Zig printer + 19-file round-trip test + 7 Rust subcommand tests) and the merge-protocol cost is two `git merge` + three CLI invocations.
- The post-merge gate (3-command verification) is mechanical and cheap (<60s wall).

**Choice: Option A (parallel + post-merge gate)** — recorded here per WARNING-04 mitigation. The identical block appears in Plan 02-04 — both plans must converge on the same protocol so whichever lands second knows what to run.
</merge_protocol>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted DEAL source → fmt.zig pretty-printer | Malformed AST shapes (after Phase 1 hardening) must not panic; comment text could be adversarial |
| deal_format FFI byte buffer → Rust CLI | Use-after-free risk if Rust holds pointer past deal_free (Pitfall 3) |
| In-place file write → filesystem | Power-loss / race conditions during write |
| git config probe → environment | Subprocess invocation; argument injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-28 | Tampering | Adversarial comment text containing newlines / format codes | mitigate | Comment text is emitted verbatim through the byte writer; format-string injection is impossible because writer takes &[]u8 not format strings; newlines inside comments are preserved (multi-line block comments are a legitimate authoring construct) |
| T-02-29 | Spoofing | Use-after-free of deal_format buffer | mitigate | Pitfall 3 mitigation: cli/src/main.rs Cow-clones formatted bytes BEFORE deal_free; same gpa.dupe-before-free pattern as Plan 02-03 + Plan 02-04 |
| T-02-30 | Tampering | Non-atomic in-place write corrupts source file on power-loss | mitigate | Write to a temp filename then rename; std::fs::rename is atomic on POSIX within same filesystem |
| T-02-31 | Denial of service | Pathological comment count (10K comments on one decl) | accept | Phase 1 parser already bounds input size; comments are arena-allocated; emission is O(n) in comment count |
| T-02-32 | Repudiation | Format normalization silently rewriting code that was syntactically valid | mitigate | Round-trip test fmt_roundtrip.zig asserts byte-equal AST after format; any silent rewrite that changes AST shape is caught |
| T-02-33 | Tampering | Trailing comma reintroduction (E0122 regression) | mitigate | fmt.zig MUST NOT emit trailing commas; fmt_roundtrip re-parses the formatted output and would surface E0122 immediately if introduced |
| T-02-34 | Spoofing | Subprocess invocation of `git config` from CLI | mitigate | Use std::process::Command with explicit args (no shell injection); read stdout only; failure mode is "git not in PATH" → exit 2 with descriptive error |
| T-02-35 | Tampering | LM-3 quote normalization corrupting strings containing literal quotes | mitigate | Single→double normalization MUST escape any embedded double quotes via the existing src/json.zig appendJsonStringEscaped helper; fmt_roundtrip catches any escape-failure via byte-equal AST check (string literals with bad escaping would fail to re-parse identically) |
| T-02-SC | Tampering | npm/pip/cargo installs (Supply-Chain) | mitigate | This plan adds NO new crate dependencies; uses workspace deps from Plans 02-01 / 02-04 |
</threat_model>

<verification>
- `zig build` exits 0
- `zig build test` exits 0 including fmt_roundtrip on all 19 showcase files
- `zig build phase-1.5-gate` exits 0 (regression invariant)
- `cargo build --workspace` exits 0
- `cargo test --workspace` exits 0 including fmt_subcommand
- nm count on libdeal.a is exactly 8 (Plan 02-03 → 7; this plan → 8)
- deal_format_internal NOT in nm (S-7)
- fmt.zig does NOT reference ir/lowering modules (D-25)
- No `println!` in cli/src/main.rs (Anti-Pattern guard)
- `deal fmt` respects FS-3 identity gate
- Atomic in-place edit verified
- Post-merge with Plan 02-04 verified per `<merge_protocol>` (3-command gate)
</verification>

<success_criteria>
1. src/fmt.zig walks AST + attached comments; emits canonical source bytes per RESEARCH §Pattern 3
2. deal_format C ABI export added; total exports = 8 (REQ-phase-2-4-formatter acceptance)
3. fmt_roundtrip.zig passes for ALL 19 showcase files: parse → format → parse → byte-equal AST
4. Every comment from original file survives at original attachment point (counts match)
5. `deal fmt` CLI subcommand wired with stdin/stdout + in-place edit + --check + --json
6. FS-3 identity gate enforced
7. Phase 1.5 gate still GREEN
8. Plan 02-04 ran in parallel (this plan is its sibling); the cli/src/{main.rs, ffi.rs} merge points resolved per `<merge_protocol>` (3-command post-merge gate)
</success_criteria>

<output>
Create `.planning/phases/02-prove-the-pipeline/02-05-SUMMARY.md` when done. Summary must record: (a) fmt.zig final LOC; (b) updated nm count = 8 (list all 8 exports by name); (c) fmt_roundtrip per-file pass/fail status for all 19 files; (d) any showcase file that needed a one-shot `deal fmt` commit to converge to canonical form (note as a follow-up issue if any); (e) post-merge gate outcome (record whichever plan landed second + the three command exit codes); (f) Phase 1.5 gate still GREEN.
</output>
