---
phase: 02-prove-the-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deal/src/lexer.zig
  - deal/src/ast.zig
  - deal/src/parser_deal.zig
  - deal/src/parser_dealx.zig
  - deal/src/json.zig
  - deal/tests/snapshots/ast/
  - deal/tests/unit/_all.zig
  - deal/tests/unit/comment_attachment.zig
  - deal/scripts/diff-only-new-fields.sh
  - deal/Cargo.toml
  - deal/cli/Cargo.toml
  - deal/cli/build.rs
  - deal/cli/src/main.rs
  - deal/cli/src/ffi.rs
  - deal/cli/src/render.rs
  - deal/cli/tests/key_order.rs
  - deal/cli/tests/cli_smoke.rs
  - deal/tests/ffi/Cargo.toml
  - tests/schemas/SysML.json
  - tests/schemas/KerML.json
  - tests/schemas/README.md
autonomous: true
requirements:
  - REQ-phase-2-5-cli-shell
  - REQ-phase-2-prove-pipeline
user_setup: []

must_haves:
  truths:
    - "Lexer emits //, /* */, and /** */ as separate tokens (not skipped trivia) per D-29"
    - "Every declaration node carries leading_comments, trailing_comments, doc_comment fields per D-29"
    - "D-31: Comment attachment lands in Wave 1 (Plan 02-01) before sema, IR lowering, or fmt work begins"
    - "All 19 AST snapshots regenerated in one dedicated commit; only new comment fields appear in the diff per D-30"
    - "Phase 1.5 determinism + property-span-containment tests still pass on refreshed snapshots (per D-30 — AST JSON stays at v=1)"
    - "Cargo workspace at deal/Cargo.toml with members cli/ and tests/ffi/; cargo build --workspace exits 0"
    - "deal --version prints the version and exits 0"
    - "deal parse/check/fmt/build subcommand stubs print not-yet-implemented and exit 2 per D-34 (internal-error band)"
    - "Bundled SysML.json + KerML.json schemas live at tests/schemas/ with provenance README"
    - "key_order.rs defensive test asserts serde_json emits alphabetical keys (no preserve_order leak)"
  artifacts:
    - path: "deal/Cargo.toml"
      provides: "Workspace root + workspace.dependencies pinning"
      contains: "[workspace]"
    - path: "deal/cli/Cargo.toml"
      provides: "CLI crate manifest"
      contains: "name = \"deal\""
    - path: "deal/cli/src/main.rs"
      provides: "clap entry point + 4 subcommand stubs + global flags"
      contains: "#[derive(Parser)]"
    - path: "deal/cli/src/ffi.rs"
      provides: "extern \"C\" decls for the 6 existing Phase 1 exports"
      contains: "extern \"C\""
    - path: "deal/cli/build.rs"
      provides: "Invokes zig build, links static libdeal"
      contains: "cargo::rustc-link-lib=static=deal"
    - path: "deal/src/ast.zig"
      provides: "leading_comments, trailing_comments, doc_comment fields on declaration variants"
      contains: "leading_comments"
    - path: "deal/src/lexer.zig"
      provides: "Comment tokens emitted (line, block, doc) per D-28"
      contains: "comment_line"
    - path: "tests/schemas/SysML.json"
      provides: "Bundled OMG SysML v2 schema (frozen copy with SHA256)"
    - path: "tests/schemas/KerML.json"
      provides: "Bundled OMG KerML schema"
    - path: "deal/cli/tests/key_order.rs"
      provides: "Defensive test that serde_json keys are alphabetical"
  key_links:
    - from: "deal/Cargo.toml"
      to: "deal/cli/Cargo.toml + deal/tests/ffi/Cargo.toml"
      via: "workspace members array"
      pattern: "members\\s*=\\s*\\[.*\"cli\".*\"tests/ffi\".*\\]"
    - from: "deal/src/parser_deal.zig"
      to: "deal/src/ast.zig comment fields"
      via: "attachComments() helper per gofmt blank-line rule (D-28)"
      pattern: "leading_comments|trailing_comments|doc_comment"
    - from: "deal/cli/build.rs"
      to: "deal/zig-out/lib/libdeal.a"
      via: "Command::new(\"zig\").arg(\"build\")"
      pattern: "cargo::rustc-link-search"
---

<objective>
Land the two foundational prerequisites that every later Plan 02-NN consumes:
(1) Comment attachment to AST per D-28..D-31 (lexer emits comment tokens, parser attaches them to declarations, AST gains three new fields, all 19 snapshots regenerate in ONE dedicated commit, determinism + span-containment tests stay GREEN);
(2) Cargo workspace + clap CLI scaffold per D-19/REQ #8 (workspace root at deal/Cargo.toml, cli/ crate with subcommand stubs, FFI bindings for the existing 6 Phase 1 exports, defensive key-order test, bundled schemas pre-staged for Plan 04).

Purpose: D-31 mandates that comment attachment lands FIRST so every downstream consumer (sema, IR lowering, fmt, codegen) sees the final AST shape from day one — mid-phase AST churn is explicitly forbidden. The CLI scaffold lands in this same plan so Plan 02-02 has a `deal check` subcommand to wire into, Plan 02-04 has `deal build` to wire into, and the workspace lock-file lands in ONE commit (avoids scattered `cargo add` calls). Plans 02-02 through 02-06 will EXTEND this scaffold; none of them create the workspace.

Output: AST that carries comments structurally, snapshots refreshed and reviewed, Cargo workspace operational with key_order defensive test, FFI bindings for current 6 exports (Plan 03 will add 2 more), 4 subcommand stubs that error out cleanly with exit code 2 (D-34 "not yet implemented"), schema bundle copied into tests/schemas/ for Plan 04 to consume.

Task slicing rationale (per BLOCKER-01 revision): the original Task 1 bundled workspace scaffold + ffi.rs + key_order test + 9 numbered implementation steps + cli_smoke + schema bundle + README — 12 files and 11 numbered steps. The revision splits the scaffold along a natural seam: Task 1a stands up the Cargo workspace + CLI scaffold + 4 subcommand stubs (compile + run gates), Task 1b lands the bundled schema artifacts + defensive tests (key_order + cli_smoke). Task 2 (comment attachment) is unchanged. Resulting plan: 3 atomic tasks, each ≤7 files and ≤7 numbered implementation steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-prove-the-pipeline/02-CONTEXT.md
@.planning/phases/02-prove-the-pipeline/02-RESEARCH.md
@.planning/phases/02-prove-the-pipeline/02-PATTERNS.md
@.planning/phases/02-prove-the-pipeline/02-SPEC.md
@.planning/phases/01-zig-compiler-core/01-06-SUMMARY.md
@.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-04-SUMMARY.md
@.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-05-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase + RESEARCH.md. -->

From deal/src/ast.zig (Phase 1 — DO NOT CHANGE existing shape; ADD comment fields):
- `pub const Span = extern struct { start: u32, end: u32 };`
- `pub const DocComment = struct { ... }` declared at src/ast.zig:393 (REUSE for D-29; do not duplicate)
- `pub const NodeKind` tagged union — declaration variants (`part_def`, `port_def`, `requirement_def`, etc.) gain three new optional fields per D-28/D-29
- Existing arm `doc_comment` at src/ast.zig:602 STAYS (floating-doc-comment edge case)

From deal/src/lexer.zig (Phase 1):
- `Tag` enum currently has no comment tokens (skipped via skipTrivia). D-28 requires three new tags: `comment_line`, `comment_block`, `doc_comment`.
- Existing field `unterminated_block_comment: ?ast.Span` (Phase 1.5 Plan 02 wiring) — DO NOT touch; just keep it functioning.

From deal/tests/ffi/Cargo.toml (Phase 1 standalone — to be migrated into workspace):
- `[package] name = "deal-ffi-tests"` — current name; keep
- `links = "deal"` field at line ~10 — CRITICAL: Cargo refuses two crates claiming the same native lib. Resolution per PATTERNS.md Lane 8 option (b): DROP `links = "deal"` from tests/ffi/Cargo.toml and put it on a new `deal/cli/ffi/` sub-crate, OR (option (c), simpler): drop from tests/ffi and let cli/ claim it. THIS PLAN PICKS OPTION (c) — single binding crate cli/, tests/ffi/ depends on cli for FFI symbols. If complexity warrants, switch to (b) in a follow-up.

From deal/include/deal.h (Phase 1):
- 6 exports declared: deal_parse, deal_free, deal_has_errors, deal_diagnostics_count, deal_ast_json, deal_diagnostics_json
- Plan 03 will add deal_ir_json + deal_format (8 total). This plan does NOT modify deal.h.

From RESEARCH.md §Pattern 1 (Cargo workspace template — verbatim contract):
The workspace root pins `clap = { version = "4.6", features = ["derive"] }`, `serde = { version = "1", features = ["derive"] }`, `serde_json = "1"` (NO preserve_order feature), `jsonschema = { version = "0.46", default-features = false }`, `anstream = "1"`, `owo-colors = "4"`, `anyhow = "1"`, `thiserror = "2"`, `insta = { version = "1.47", features = ["json"] }`. Resolver "2". Members `["cli", "tests/ffi"]`. (Exact Toml template is in 02-RESEARCH.md §Pattern 1; copy verbatim into deal/Cargo.toml.)

From RESEARCH.md §Pattern 2 (Gofmt comment attachment rules — verbatim):
- Leading: comment immediately precedes declaration (no blank line) → leading_comments
- Trailing: comment on the SAME line as the declaration's last token → trailing_comments
- Same line as declaration close: trailing on previous
- Separated by blank line from prior decl: leading on next OR floating if at file start
- /** */ immediately preceding declaration (no blank) → promoted to doc_comment field
- /** */ separated by blank → remains as floating NodeKind.doc_comment arm (rare)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1a: Cargo workspace root + CLI crate skeleton + 4 subcommand stubs (compile + run gates)</name>

  <read_first>
    - deal/tests/ffi/Cargo.toml — current standalone manifest (will become workspace member; per PATTERNS Lane 8 option (c) drop `links = "deal"` here)
    - deal/tests/ffi/build.rs — exact template for cli/build.rs per PATTERNS lines 391-419
    - deal/tests/ffi/src/lib.rs:14-42 — extern block for existing 6 symbols (verbatim copy into cli/src/ffi.rs; remove the marker comment "Phase 1" and add a comment noting Plan 03 will append deal_ir_json + deal_format)
    - .planning/phases/02-prove-the-pipeline/02-RESEARCH.md §Pattern 1 (lines 384-397) — workspace template verbatim
    - .planning/phases/02-prove-the-pipeline/02-RESEARCH.md §Example 1 (lines 808-866) — clap derive subcommand template
    - .planning/phases/02-prove-the-pipeline/02-CONTEXT.md decisions D-19 (Rust owns SysML emitter + CLI), D-22 (8 total exports — currently 6; Plan 03 adds 2), D-32 (--json envelope shape), D-34 (exit codes 0/1/2)
    - .planning/phases/02-prove-the-pipeline/02-PATTERNS.md Lane 4 (build.rs analog) + Lane 8 (Cargo workspace, links field resolution)
  </read_first>

  <files>
    - deal/Cargo.toml (NEW — workspace root)
    - deal/cli/Cargo.toml (NEW)
    - deal/cli/build.rs (NEW)
    - deal/cli/src/main.rs (NEW)
    - deal/cli/src/ffi.rs (NEW — 6 extern decls; comments note Plan 03 extends to 8)
    - deal/cli/src/render.rs (NEW — minimal stub; full implementation in Plan 02-02)
    - deal/tests/ffi/Cargo.toml (MODIFIED — become workspace member; remove `links = "deal"` per Lane 8 option (c) so cli/ owns the native-lib claim)
  </files>

  <action>
    Stand up the Cargo workspace scaffold and the CLI crate skeleton with the 4 subcommand stubs. This task ends when `cargo build --workspace` exits 0 and the binary runs. Defensive tests (key_order, cli_smoke) and the schema bundle land in Task 1b.

    Resolve the `links = "deal"` conflict per PATTERNS Lane 8 option (c) — DECISION RECORDED IN PLAN: cli/ claims the native lib; tests/ffi drops the field. If a future plan needs tests/ffi to claim a different native lib symbol set, switch to option (b) in a follow-up ADR. Document this decision in the SUMMARY when the task completes.

    Implementation steps in order:

    (1) Create deal/Cargo.toml as a workspace root with `resolver = "2"`, `members = ["cli", "tests/ffi"]`, and the `[workspace.dependencies]` block from RESEARCH.md §Pattern 1 verbatim (versions pinned: clap 4.6, serde 1, serde_json 1 with NO `preserve_order` feature, jsonschema 0.46 with `default-features = false`, anstream 1, owo-colors 4, anyhow 1, thiserror 2, insta 1.47 with `features = ["json"]`). DO NOT add any other crates here — later plans will add their own with documented rationale.

    (2) Create deal/cli/Cargo.toml. `[package] name = "deal"`, `version` from workspace, `edition = "2021"`. `[[bin]] name = "deal"`. `links = "deal"` (per the decision above). Dependencies all reference `{ workspace = true }`. `[dev-dependencies] insta = { workspace = true }`.

    (3) Create deal/cli/build.rs as a verbatim copy of deal/tests/ffi/build.rs (per PATTERNS Lane 4) with ONE adjustment: cli/ is one level below deal/ (not two like tests/ffi/), so the `.join("..")` count is 1 instead of 2. Keep the `cargo::rustc-link-search=native=…` + `cargo::rustc-link-lib=static=deal` + `cargo::rerun-if-changed=…` invocations exactly as in tests/ffi/build.rs.

    (4) Create deal/cli/src/ffi.rs. Copy the extern block from deal/tests/ffi/src/lib.rs:14-42 verbatim (declares the 6 Phase 1 exports). Add `#![allow(non_camel_case_types)]`. Define `#[repr(C)] pub struct DealHandle { _opaque: [u8; 0], _marker: PhantomData<(*mut u8, PhantomPinned)> }` to keep the handle `!Send` / `!Sync` (D-13). Add a top-of-file comment noting "Plan 02-03 will append `deal_ir_json` and `deal_format` declarations here (D-22, D-21)" — DO NOT pre-declare them.

    (5) Create deal/cli/src/main.rs from the RESEARCH §Example 1 template (lines 808-866) verbatim. Add `pub mod ffi;` and `pub mod render;` module declarations. Implement `fn run(cli: Cli) -> Result<(), CliError>` where `CliError` is a typed enum with two variants: `CliError::User(String)` (exit 1 per D-34) and `CliError::Internal(anyhow::Error)` (exit 2). For each of the 4 subcommands (parse/check/fmt/build), the stub body MUST return `Err(CliError::Internal(anyhow::anyhow!("not yet implemented: {} — see Plan 02-{02..06}", subcommand_name)))` so the exit code is 2. Wire `--json`/`--color`/`--verbose` as global flags per the template. DO NOT implement the --json envelope (D-32 — that lands when Plan 02-02 wires `deal check` to the FFI).

    (6) Create deal/cli/src/render.rs as a stub: declare `pub fn render_diagnostic(...)` with the body `unimplemented!("Plan 02-02 implements diagnostic rendering")`. The signature should accept `&mut anstream::AutoStream<std::io::Stderr>`, a `&str` source, and a `&serde_json::Value` (Phase 1 diagnostic shape). Plan 02-02 will fill in the body using owo-colors per RESEARCH §Anti-Patterns.

    (7) Modify deal/tests/ffi/Cargo.toml: remove the `links = "deal"` line (option (c) per Lane 8 — cli/ now claims the native lib). Add a top-of-file comment "Workspace member; links inherited from deal/cli". Verify the existing 9 FFI tests still pass after `cargo test --workspace -p deal-ffi-tests`.

    Per D-34: subcommand stubs MUST exit 2 (internal error — not yet implemented), NOT exit 1 (user error). Task 1b adds the cli_smoke.rs test that verifies this distinction; this task only needs the binary to build + run + exit 2 on subcommand invocation.

    NO `--json` envelope assembly in this task. NO diagnostic rendering body. NO schema bundle (Task 1b). NO defensive key-order test (Task 1b).
  </action>

  <verify>
    <automated>cd deal && cargo build --workspace 2>&1 | tail -20 && echo "WORKSPACE BUILD OK"</automated>
    <automated>cd deal && cargo run --bin deal -- --version 2>&1 | grep -q "^deal " && echo "VERSION OK"</automated>
    <automated>cd deal && cargo run --bin deal -- parse /tmp/nonexistent.deal; test $? -eq 2 && echo "PARSE STUB EXIT 2 OK"</automated>
    <automated>cd deal && cargo run --bin deal -- check /tmp/nonexistent.deal; test $? -eq 2 && echo "CHECK STUB EXIT 2 OK"</automated>
    <automated>cd deal && cargo run --bin deal -- fmt /tmp/nonexistent.deal; test $? -eq 2 && echo "FMT STUB EXIT 2 OK"</automated>
    <automated>cd deal && cargo run --bin deal -- build --target sysml-v2 /tmp/nonexistent.deal; test $? -eq 2 && echo "BUILD STUB EXIT 2 OK"</automated>
    <automated>test -f deal/Cargo.toml && grep -q 'members.*"cli".*"tests/ffi"' deal/Cargo.toml && echo "WORKSPACE MEMBERS OK"</automated>
    <automated>! grep -q 'links *= *"deal"' deal/tests/ffi/Cargo.toml && grep -q 'links *= *"deal"' deal/cli/Cargo.toml && echo "LINKS RESOLUTION OK"</automated>
    <automated>! grep -r 'preserve_order' deal/Cargo.toml deal/cli/Cargo.toml deal/tests/ffi/Cargo.toml && echo "NO PRESERVE_ORDER OK"</automated>
    <automated>cd deal && cargo test --workspace -p deal-ffi-tests 2>&1 | grep -q 'test result: ok' && echo "EXISTING FFI TESTS STILL PASS OK"</automated>
  </verify>

  <acceptance_criteria>
    - deal/Cargo.toml exists with `[workspace]` and members `["cli", "tests/ffi"]`; `cargo build --workspace` exits 0
    - deal/cli/Cargo.toml has `name = "deal"`, `links = "deal"`, dependencies all reference workspace
    - deal/cli/src/main.rs has 4 subcommand stubs (Parse, Check, Fmt, Build) and 3 global flags (--json, --color, --verbose); `deal --version` exits 0; all 4 subcommand stubs exit 2 with stderr containing "not yet implemented"
    - deal/cli/src/ffi.rs declares the 6 Phase 1 exports; the file has a comment marking where Plan 02-03 appends 2 more
    - deal/tests/ffi/Cargo.toml no longer has `links = "deal"`; existing 9 FFI tests still pass via `cargo test --workspace -p deal-ffi-tests`
    - NO crate enables `serde_json/preserve_order` (grep clean)
    - `links = "deal"` conflict resolved in favor of cli/ (option (c) per PATTERNS Lane 8); decision documented in task action
  </acceptance_criteria>

  <done>
    Cargo workspace operational; CLI binary builds and all 4 stubs exit 2; links conflict resolved. Ready for Task 1b (schema bundle + defensive tests).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1b: Bundled OMG schemas + defensive key-order + cli_smoke tests</name>

  <read_first>
    - deal/cli/Cargo.toml (Task 1a output — confirms workspace deps are pinned and reachable)
    - deal/cli/src/main.rs (Task 1a output — confirms the binary the smoke tests will invoke)
    - .planning/phases/02-prove-the-pipeline/02-RESEARCH.md §"Pitfall 1: serde_json non-alphabetical key emission" (lines 695-707) — basis for key_order.rs defensive test
    - .planning/phases/02-prove-the-pipeline/02-RESEARCH.md §Pitfall 5 (informational — schema-bundle SHA256 invariant)
    - .planning/phases/02-prove-the-pipeline/02-VALIDATION.md §Wave 0 Requirements — concrete list of artifacts Task 1b must create
    - spec/references/omg-sysml-v2/SysML.json — source for the bundled copy (126K lines; copy bytes verbatim; record SHA256 in README)
    - spec/references/omg-kerml-v1/KerML.json — source for the bundled copy
    - .planning/phases/02-prove-the-pipeline/02-CONTEXT.md D-18 (alphabetical-key invariant — the underlying contract that key_order.rs defends)
  </read_first>

  <files>
    - tests/schemas/SysML.json (NEW — bundled copy of spec/references/omg-sysml-v2/SysML.json with byte-identical content)
    - tests/schemas/KerML.json (NEW — bundled copy)
    - tests/schemas/README.md (NEW — provenance: OMG release date, source URL, SHA256 of each schema)
    - deal/cli/tests/key_order.rs (NEW — defensive serde_json alphabetical-key test)
    - deal/cli/tests/cli_smoke.rs (NEW — --version + 4 subcommand exit-code tests)
  </files>

  <action>
    Land the schema bundle that Plan 02-04 will consume, plus the two defensive Rust tests that gate the workspace against the two highest-priority pitfalls (Pitfall 1 key reordering, Pitfall 5 schema tampering). Implementation steps in order:

    (1) Copy spec/references/omg-sysml-v2/SysML.json byte-verbatim to tests/schemas/SysML.json. Copy spec/references/omg-kerml-v1/KerML.json to tests/schemas/KerML.json. Use `cp` (not `cat`) to preserve bytes exactly; verify with `cmp` afterward.

    (2) Create tests/schemas/README.md with the following content blocks (executor fills the concrete values from the bytes copied in step 1):
        - **Provenance**: cite the OMG release the schemas come from (look for `version` or `$id` field inside each schema; record the version identifier).
        - **Source URLs**: cite the upstream OMG download links + the local `spec/references/omg-sysml-v2/` + `spec/references/omg-kerml-v1/` paths the bytes came from.
        - **SHA256 pins**: compute via `shasum -a 256 tests/schemas/SysML.json tests/schemas/KerML.json` and paste both digests under a `## SHA256` heading.
        - **Tamper-detection invariant**: note that Plan 02-04's `schema_registry.rs` will SHA256-check these bundle files at validator-construction time and fail loudly on mismatch (T-02-01 mitigation extension).

    (3) Create deal/cli/tests/key_order.rs as a defensive test against Pitfall 1. The single test function `serde_json_emits_alphabetical_keys` asserts that `serde_json::to_string(&serde_json::json!({"z": 1, "a": 2}))` equals exactly `r#"{"a":2,"z":1}"#`. If preserve_order leaks via any transitive dep, this test fails — the alphabetical-key invariant (D-18) is enforced at workspace level on every `cargo test` run. Add a comment block explaining: "This guards Pitfall 1 (RESEARCH.md). If this test ever fails, a transitive dep has flipped `serde_json/preserve_order`. Run `cargo tree -e features --workspace | grep preserve_order` to locate the offender."

    (4) Create deal/cli/tests/cli_smoke.rs with two tests using `assert_cmd` or `std::process::Command` (executor picks; assert_cmd is more idiomatic but adds a dep — if avoiding the dep, hand-roll with Command):
        - `version_flag_exits_zero` — invokes the built binary with `--version`, asserts exit 0 and stdout starts with `deal `.
        - `subcommand_stubs_exit_two` — for each of the 4 subcommands (`parse <path>`, `check <path>`, `fmt <path>`, `build --target sysml-v2 <path>`): invoke with a sentinel input path (e.g., `/tmp/nonexistent.deal` or `nonexistent-fixture.deal`), assert exit code 2, assert stderr contains "not yet implemented". Use `nm`-stable invocations (no piping; pure argv).
        If `assert_cmd` is added, it goes in `[dev-dependencies]` of deal/cli/Cargo.toml AND in workspace dependencies of deal/Cargo.toml (with a documented rationale comment "test-only; not in runtime").

    (5) Verify the bundle integrity: `shasum -a 256 tests/schemas/SysML.json tests/schemas/KerML.json` returns two digests; tests/schemas/README.md contains both digests. The README assertion catches accidental edits to either file in future commits.

    (6) Run `cargo test --workspace --no-fail-fast` and confirm: `serde_json_emits_alphabetical_keys` passes; both cli_smoke tests pass; all 9 existing tests/ffi tests still pass.

    Per D-34: cli_smoke tests verify subcommand stubs exit 2 (not 1) — this enforces the User-vs-Internal exit-code distinction from the task action of Task 1a.

    Per Pitfall 1: key_order.rs is the workspace-level guardrail. Every other Plan 02-NN that adds a dep MUST re-run `cargo test -- key_order` to confirm no regression.

    Per Pitfall 5: tests/schemas/README.md is the source of truth for SHA256; Plan 02-04 will load these pins at runtime.
  </action>

  <verify>
    <automated>shasum -a 256 tests/schemas/SysML.json tests/schemas/KerML.json && grep -qE 'SHA256|sha256' tests/schemas/README.md && echo "SCHEMA BUNDLE + README OK"</automated>
    <automated>test -f tests/schemas/SysML.json && test -f tests/schemas/KerML.json && cmp tests/schemas/SysML.json spec/references/omg-sysml-v2/SysML.json && cmp tests/schemas/KerML.json spec/references/omg-kerml-v1/KerML.json && echo "BYTE-IDENTICAL BUNDLES OK"</automated>
    <automated>cd deal && cargo test --workspace -- key_order 2>&1 | grep -q 'test result: ok' && echo "KEY ORDER DEFENSIVE TEST OK"</automated>
    <automated>cd deal && cargo test --workspace -- cli_smoke 2>&1 | grep -q 'test result: ok' && echo "CLI SMOKE TESTS OK"</automated>
    <automated>cd deal && cargo test --workspace --no-fail-fast 2>&1 | tail -10 && echo "FULL CARGO TEST WORKSPACE OK"</automated>
    <!-- Defensive: README must record both digests literally (not just header text) -->
    <automated>SHA_SYS=$(shasum -a 256 tests/schemas/SysML.json | awk '{print $1}'); SHA_KER=$(shasum -a 256 tests/schemas/KerML.json | awk '{print $1}'); grep -q "$SHA_SYS" tests/schemas/README.md && grep -q "$SHA_KER" tests/schemas/README.md && echo "BOTH DIGESTS RECORDED IN README OK"</automated>
  </verify>

  <acceptance_criteria>
    - tests/schemas/SysML.json + tests/schemas/KerML.json exist with byte-identical content to spec/references/ (cmp confirms)
    - tests/schemas/README.md records both SHA256 digests literally (grep confirms both digests appear in README body)
    - deal/cli/tests/key_order.rs passes; the test asserts serde_json output `{"a":2,"z":1}` for `{"z":1,"a":2}` input (D-18 invariant defended)
    - deal/cli/tests/cli_smoke.rs passes; all 5 smoke tests green (1 version + 4 subcommand stubs)
    - `cargo test --workspace` exits 0 with all tests/ffi tests still passing alongside the new key_order + cli_smoke tests
  </acceptance_criteria>

  <done>
    Schema bundle SHA256-pinned and provenanced. Defensive Rust tests (key_order, cli_smoke) operational. Ready for Task 2 (comment attachment to AST).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comment attachment to AST (lexer + parser + ast + json + tests + snapshot refresh)</name>

  <read_first>
    - deal/src/lexer.zig — current Tag enum + skipTrivia function (REMOVE skipTrivia's comment-discarding behavior; emit comment tokens instead)
    - deal/src/ast.zig (lines 14-50) — NodeKind tagged union; (line 393) existing DocComment struct (REUSE per D-29); (line 602) existing NodeKind.doc_comment arm (KEEP for floating edge case)
    - deal/src/parser_deal.zig (lines 361-382) — parseFile entry; the place to wire attachComments() at declaration boundaries
    - deal/src/parser_dealx.zig — same wiring pattern for .dealx
    - deal/src/json.zig (lines 28-105 emitAst; lines 1143-1173 appendJsonStringEscaped; lines 804-861 writeDiagnostic for alphabetical key reference)
    - deal/tests/unit/parser_deal_snapshot.zig — 15 .deal file iteration; will need snapshot regen
    - deal/tests/unit/parser_dealx_snapshot.zig — 4 .dealx file iteration; will need snapshot regen
    - deal/tests/unit/determinism_parse_twice.zig — MUST stay GREEN after snapshot regen
    - deal/tests/unit/property_span_containment.zig — MUST stay GREEN after snapshot regen
    - .planning/phases/02-prove-the-pipeline/02-CONTEXT.md decisions D-28 (gofmt blank-line rule), D-29 (DocComment reuse), D-30 (no schema bump; one dedicated commit), D-31 (lands FIRST in plan order)
    - .planning/phases/02-prove-the-pipeline/02-RESEARCH.md §Pattern 2 (Gofmt rule verbatim, lines 401-430)
    - .planning/phases/02-prove-the-pipeline/02-PATTERNS.md Lane 2 §src/json.zig (alphabetical comment-field ordering)
    - .planning/phases/02-prove-the-pipeline/02-RESEARCH.md §"Pitfall 2: comment-attachment snapshot churn" (lines 708-720) — basis for diff-only-new-fields.sh script
    - .planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-04-SUMMARY.md — determinism test pattern (gpa.dupe-before-free lifetime)
  </read_first>

  <files>
    - deal/src/lexer.zig (MODIFIED — add comment_line, comment_block, doc_comment tags; replace skipTrivia comment-discard with token emission)
    - deal/src/ast.zig (MODIFIED — add leading_comments: []Comment, trailing_comments: []Comment, doc_comment: ?*DocComment to every declaration payload variant; declare Comment struct)
    - deal/src/parser_deal.zig (MODIFIED — call attachComments(node) after each parseDeclaration; implement gofmt blank-line rule)
    - deal/src/parser_dealx.zig (MODIFIED — same attachComments wiring for [<tag>] declarations)
    - deal/src/json.zig (MODIFIED — emit the three new fields in alphabetical positions within each declaration payload; empty arrays / null still emitted per D-18; reuse appendJsonStringEscaped)
    - deal/tests/unit/comment_attachment.zig (NEW — focused unit test of gofmt rule edge cases)
    - deal/tests/unit/_all.zig (MODIFIED — register comment_attachment.zig)
    - deal/tests/snapshots/ast/showcase__*.json (REGENERATED — all 19 files; one dedicated commit; only the 3 new fields should appear in the diff)
    - deal/scripts/diff-only-new-fields.sh (NEW — helper script that asserts the snapshot-regen diff contains ONLY new comment fields and no structural drift)
  </files>

  <action>
    Land comment attachment to AST in three sub-steps, committing each separately so the snapshot refresh stays isolated per D-30 and Pitfall 2.

    Sub-step A — Lexer + AST shape (commit 1, no snapshot regen yet):
    (1) In deal/src/lexer.zig, add three new Tag variants: `comment_line` (for `//...\n`), `comment_block` (for `/*...*/`), `doc_comment` (for `/**...*/`). Modify the existing skipTrivia function: it currently consumes comments silently — change it so comments are EMITTED as tokens (caller pulls them via next()) rather than skipped. Keep the existing `unterminated_block_comment: ?ast.Span` drain mechanism (Phase 1.5 Plan 02 wiring for E0003) intact — emission and error-drain are independent.
    (2) In deal/src/ast.zig, declare `pub const Comment = struct { kind: enum { line, block }, text: []const u8, span: Span };` (arena-owned text). Add three optional fields to EVERY declaration payload variant (`part_def`, `port_def`, `port_usage`, `requirement_def`, `constraint_def`, `attribute_def`, `attribute_usage`, `item_def`, `interface_def`, `connection_def`, `flow_def`, `allocation_def`, `action_def`, `state_def`, `need_def`, `use_case_def`, `system` / `subsystem` / `connect` / `expose` / `traceability_block` / `allocate` / `satisfy` / `validate` for .dealx): `leading_comments: []Comment = &.{}`, `trailing_comments: []Comment = &.{}`, `doc_comment: ?*DocComment = null`. REUSE the existing DocComment struct at src/ast.zig:393 — do NOT duplicate. KEEP the existing NodeKind.doc_comment arm at line 602 unchanged (handles the floating-doc-comment edge case per D-29).
    (3) Verify zig build still succeeds: `zig build` exits 0 (no parser changes yet, so existing snapshots may temporarily diverge — that's expected; sub-step B fixes it).

    Sub-step B — Parser attachComments + tests (commit 2 — snapshot regen happens here):
    (4) In deal/src/parser_deal.zig, implement a helper `fn attachComments(self: *Parser, node: *ast.Node) !void` that applies the gofmt rule:
        - Collect all comment tokens between the previous declaration's end (or file start) and this declaration's start.
        - Apply gofmt rule (RESEARCH §Pattern 2, lines 401-430 verbatim): comments separated from this declaration by a blank line attach as trailing on the previous decl; comments with no blank line before this decl attach as leading on this decl; `/** */` immediately preceding (no blank) is promoted to `doc_comment` (not added to leading_comments); same-line trailing attaches as trailing.
        - Call attachComments() at every parseDeclaration() return site. For .dealx parser (parser_dealx.zig), do the same at every [<tag>] open + close site.
    (5) In deal/src/json.zig, extend the AST emitter for every declaration payload variant: emit `leading_comments`, `trailing_comments`, `doc_comment` in their alphabetical positions within the payload object (D-18). Use a helper `emitCommentArray(buf, comments)` that writes `[{"kind":"line","span":[s,e],"text":"..."}, ...]` with alphabetical-key inner objects; empty array is `[]`. Use existing appendJsonStringEscaped (src/json.zig:1143-1173) for text content.
    (6) Create deal/tests/unit/comment_attachment.zig as a TDD-first unit test (write tests BEFORE the attachComments implementation is "done"). Tests:
        - Test: leading comment with no blank line attaches to next decl.
        - Test: leading comment with blank line above attaches as trailing on prior decl (or floating if at file start).
        - Test: same-line trailing comment attaches as trailing on current decl.
        - Test: /** */ immediately preceding decl with no blank line is promoted to doc_comment field; leading_comments stays empty.
        - Test: /** */ separated by blank line stays as floating NodeKind.doc_comment arm (file-level node).
        - Test: every comment span is contained within or adjacent to the declaration's span range (interaction with property_span_containment).
    (7) Register comment_attachment.zig in deal/tests/unit/_all.zig.
    (8) Create deal/scripts/diff-only-new-fields.sh: bash script that takes a list of changed snapshot files and asserts every diff hunk contains ONLY lines mentioning `leading_comments`, `trailing_comments`, or `doc_comment`. Exits non-zero if any other line changed (catches structural drift per Pitfall 2).
    (9) Regenerate all 19 snapshots via `zig build test -Dupdate-snapshots` (or the equivalent flag — check deal/build.zig for the snapshot-regen mechanism). Run `bash deal/scripts/diff-only-new-fields.sh deal/tests/snapshots/ast/showcase__*.json` and assert clean exit. If the diff contains structural changes, FIX the parser bug (do NOT accept the diff).
    (10) Verify determinism + property tests still pass after snapshot refresh:
        - `zig build test -Dtest-filter=determinism` exits 0
        - `zig build test -Dtest-filter=property` exits 0
    (11) Commit message MUST be a single dedicated commit per D-30: "phase-2-01: attach comments to AST + regenerate 19 snapshots" — no other source changes ride along.

    Sub-step C — Final integration check (commit 3 — already small if sub-step B is clean):
    (12) Run the full Phase 1.5 gate: `zig build phase-1.5-gate` exits 0. This is the regression-protection invariant per CONTEXT.md (Phase 1.5 ADR binds Phases 2..6). If this fails, the comment attachment broke something downstream — fix before proceeding.

    CRITICAL: NO ad-hoc snapshot acceptance via `--accept`. NO ride-along source changes in the snapshot-regen commit. NO structural diffs hidden inside comment-field diffs. NO duplication of the DocComment struct (D-29 says reuse the existing one).
  </action>

  <verify>
    <automated>cd deal && zig build 2>&1 | tail -10 && echo "BUILD OK"</automated>
    <automated>cd deal && zig build test 2>&1 | tail -20 && echo "ALL TESTS OK"</automated>
    <automated>cd deal && zig build test -Dtest-filter=comment_attachment 2>&1 | grep -q 'passed' && echo "COMMENT TESTS OK"</automated>
    <automated>cd deal && zig build test -Dtest-filter=determinism 2>&1 | grep -q 'passed' && echo "DETERMINISM OK"</automated>
    <automated>cd deal && zig build test -Dtest-filter=property 2>&1 | grep -q 'passed' && echo "PROPERTY OK"</automated>
    <automated>cd deal && zig build phase-1.5-gate 2>&1 | tail -5 && echo "PHASE 1.5 GATE OK"</automated>
    <automated>bash deal/scripts/diff-only-new-fields.sh deal/tests/snapshots/ast/showcase__*.json 2>&1 && echo "DIFF DISCIPLINE OK"</automated>
    <!-- Defensive grep gate against header-prose self-invalidation: filter comment lines, then assert zero matches -->
    <automated>grep -v '^//' deal/src/ast.zig | grep -c 'leading_comments' | awk '{ if ($1 >= 10) print "AST COMMENT FIELDS OK"; else { print "TOO FEW DECLARATION VARIANTS UPDATED: " $1; exit 1 } }'</automated>
    <automated>grep -v '^//' deal/src/lexer.zig | grep -cE 'comment_line|comment_block|doc_comment' | awk '{ if ($1 >= 3) print "LEXER COMMENT TAGS OK"; else { print "MISSING TAGS"; exit 1 } }'</automated>
  </verify>

  <acceptance_criteria>
    - Lexer emits comment_line, comment_block, doc_comment tokens (not skipped); grep confirms 3+ tag references in src/lexer.zig
    - AST declaration variants gain leading_comments, trailing_comments, doc_comment fields (10+ variants updated; grep-counted)
    - DocComment struct at src/ast.zig:393 is REUSED (not duplicated); NodeKind.doc_comment arm at line 602 unchanged
    - parseDeclaration in parser_deal.zig + parser_dealx.zig calls attachComments() per gofmt rule
    - json.zig emits the three new fields in alphabetical positions per D-18; empty arrays / null still emitted
    - comment_attachment.zig unit test passes (6 sub-tests covering gofmt edge cases)
    - All 19 AST snapshots refreshed in ONE dedicated commit; diff-only-new-fields.sh asserts no structural drift
    - determinism_parse_twice test passes on refreshed snapshots
    - property_span_containment test passes on refreshed snapshots
    - phase-1.5-gate still exits 0 (regression invariant)
  </acceptance_criteria>

  <done>
    Comment attachment lands on AST per D-28..D-31. Snapshot refresh is a single dedicated commit. Phase 1.5 gate stays GREEN. Sema (Plan 02-02), IR lowering (Plan 02-03), and fmt (Plan 02-05) can now consume the final AST shape.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User shell → cargo workspace | Untrusted invocation of `cargo build`; package legitimacy must be verified offline |
| Filesystem → tests/schemas/ | Bundled SysML.json + KerML.json are read by Plan 02-04 validator; if tampered, validation outcomes corrupt |
| AST consumer → Comment struct | Comment.text is arena-owned; downstream readers must not free or mutate (Phase 5 sim/IR consumers) |
| CLI arg parsing → process exit | Wrong exit code conflation (D-34) silently corrupts CI signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | tests/schemas/ schema bundle | mitigate | tests/schemas/README.md records SHA256 of each schema (Task 1b); CI re-checks SHA256 against the README pin before any validation run |
| T-02-02 | Tampering | Cargo dependency graph (preserve_order leak) | mitigate | cli/tests/key_order.rs defensive test (Task 1b) fails if any transitive dep flips serde_json/preserve_order; per Pitfall 1 |
| T-02-03 | Tampering | npm/pip/cargo installs | mitigate | All crates approved per RESEARCH §"Package Legitimacy Audit" (16 packages, all `[OK]` via slopcheck); no `[ASSUMED]` or `[SUS]` packages remain (T-02-SC) |
| T-02-04 | Information disclosure | Comment text containing PII/secrets | accept | Comments are author-controlled source; same trust level as code; no automated redaction |
| T-02-05 | Repudiation | CLI exit code conflation (D-34) | mitigate | CliError enum splits User/Internal (Task 1a); cli_smoke.rs (Task 1b) asserts subcommand stubs exit 2 (Internal), not 1 (User); per Pitfall 8 |
| T-02-06 | Denial of service | Parser comment-token flood (malicious 10MB comment) | accept | Phase 1 lexer already bounds-checks input length; comment tokens are arena-allocated (single deal_free releases all); existing 50-file malformed corpus exercises stress paths |
| T-02-07 | Tampering | AST snapshot regen diff (Pitfall 2) | mitigate | diff-only-new-fields.sh (Task 2) asserts the regen diff contains ONLY the 3 new comment fields; structural drift causes script exit non-zero; property_span_containment test catches hidden structural changes |
| T-02-SC | Tampering | npm/pip/cargo installs (Supply-Chain) | mitigate | Package Legitimacy Audit table in RESEARCH.md (lines 185-208) confirms all crates [OK] via slopcheck; SHA256-pinned schema bundle (tests/schemas/README.md) prevents OMG-schema tampering between phases |
</threat_model>

<verification>
- `cargo build --workspace` exits 0 (Task 1a)
- `cargo test --workspace --no-fail-fast` exits 0 (cli tests + tests/ffi tests; Task 1a + 1b)
- `cargo run --bin deal -- --version` exits 0 (Task 1a)
- `cargo run --bin deal -- parse|check|fmt|build ...` exits 2 (D-34 internal error for not-yet-implemented; Task 1a)
- `zig build` exits 0 (Task 2)
- `zig build test` exits 0 including new comment_attachment.zig (Task 2)
- `zig build test -Dtest-filter=determinism` exits 0 (Task 2)
- `zig build test -Dtest-filter=property` exits 0 (Task 2)
- `zig build phase-1.5-gate` exits 0 (regression invariant; Task 2)
- All 19 AST snapshots refreshed; diff-only-new-fields.sh asserts no structural drift (Task 2)
- tests/schemas/{SysML.json,KerML.json,README.md} exist; README documents SHA256 of each (Task 1b)
- No crate uses `preserve_order` (grep clean across deal/Cargo.toml, deal/cli/Cargo.toml, deal/tests/ffi/Cargo.toml; Task 1a)
</verification>

<success_criteria>
1. Comment attachment lands per D-28..D-31; all 19 snapshots regenerate in a SINGLE dedicated commit with only the 3 new fields appearing in the diff
2. Cargo workspace operational with cli/ as binary crate and tests/ffi/ as workspace member; both build under `cargo build --workspace`
3. CLI binary `deal` accepts --version, 4 subcommand stubs, 3 global flags; stubs exit 2 per D-34
4. tests/schemas/{SysML.json,KerML.json} bundled with SHA256 provenance in tests/schemas/README.md
5. cli/tests/key_order.rs passes — alphabetical-key invariant defensively enforced
6. Phase 1.5 gate still GREEN — no regression in Phase 1 / 1.5 invariants
7. FFI bindings for the 6 existing Phase 1 exports declared in cli/src/ffi.rs; comment marks where Plan 02-03 will append 2 more
</success_criteria>

<output>
Create `.planning/phases/02-prove-the-pipeline/02-01-SUMMARY.md` when done. Summary must record: (a) the snapshot regen commit hash; (b) the SHA256 of tests/schemas/SysML.json and KerML.json; (c) the link/no-link resolution chosen (option (c) — links on cli/, removed from tests/ffi/); (d) confirmation that determinism + property tests pass on refreshed snapshots; (e) Phase 1.5 gate still GREEN; (f) task slicing record (Task 1a = workspace scaffold, Task 1b = schemas + defensive tests, Task 2 = comment attachment).
</output>
</output>
