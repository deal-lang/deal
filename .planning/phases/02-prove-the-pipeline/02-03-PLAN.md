---
phase: 02-prove-the-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - deal/src/ir.zig
  - deal/src/lowering.zig
  - deal/src/json.zig
  - deal/src/lib.zig
  - deal/include/deal.h
  - deal/cli/src/ffi.rs
  - deal/tests/unit/determinism_lower_twice.zig
  - deal/tests/unit/property_ir_id_uniqueness.zig
  - deal/tests/unit/_all.zig
  - .planning/decisions/ADR-deal-ir-v0.md
  - spec/ir/v0/schema.json
  - spec/ir/v0/README.md
  - .planning/phases/02-prove-the-pipeline/02-IR-LOCK.md
autonomous: false  # IR-LOCK checkpoint at the end requires recorded human review
requirements:
  - REQ-phase-2-2a-ir-v0
  - REQ-phase-2-2b-ir-lowering
user_setup: []

must_haves:
  truths:
    - "src/ir.zig declares NodeKind + Edge + Document types per D-22..D-27 + RESEARCH §Pattern 6"
    - "src/lowering.zig produces an IR Document for every workspace AST root with all reference edges resolved"
    - "deal_ir_json C ABI export added per D-22; total exports = 7 (Plan 04 adds deal_format → 8)"
    - "Two lowering passes of the same source produce byte-identical IR JSON (determinism per REQ #3 acceptance)"
    - "Every IR node has a unique fully-qualified path ID per D-23"
    - "Backend traversal API (walk/find/references/children/parent) per D-26 LOCKED"
    - "D-25: IR contains no leading_comments/trailing_comments/doc_comment fields — lowering MUST NOT copy comment fields from AST into IR nodes"
    - "ADR-deal-ir-v0.md + spec/ir/v0/schema.json + spec/ir/v0/README.md all land per D-27"
    - "D-37: IR-LOCK checkpoint between Plan 03 and Plan 04 — ADR Status flips PROPOSED → LOCKED only on recorded approval before Plans 04/05 begin"
    - "IR-LOCK checkpoint review recorded in 02-IR-LOCK.md with timestamp + outcome (approved / changes-requested)"
  artifacts:
    - path: "deal/src/ir.zig"
      provides: "IR v0 types + traversal API"
      min_lines: 400
    - path: "deal/src/lowering.zig"
      provides: "AST + symbol table → IR Document"
      min_lines: 600
    - path: "deal/src/json.zig"
      provides: "emitIrJson per D-22 alphabetical-key shape"
      contains: "emitIrJson"
    - path: "deal/include/deal.h"
      provides: "deal_ir_json declaration (deal_format added in Plan 04)"
      contains: "deal_ir_json"
    - path: "spec/ir/v0/schema.json"
      provides: "Normative IR v0 JSON Schema (draft-2020-12)"
    - path: "spec/ir/v0/README.md"
      provides: "Markdown reference for IR v0"
    - path: ".planning/decisions/ADR-deal-ir-v0.md"
      provides: "Rationale + alternatives rejected"
    - path: ".planning/phases/02-prove-the-pipeline/02-IR-LOCK.md"
      provides: "Recorded IR-lock review outcome with timestamp"
  key_links:
    - from: "deal/src/lib.zig deal_ir_json"
      to: "deal/src/json.zig emitIrJson"
      via: "lazy emission + cached_ir_json field on DealHandle"
      pattern: "cached_ir_json|emitIrJson"
    - from: "deal/src/lowering.zig"
      to: "deal/src/sema.zig SymbolTable"
      via: "consumes Pass-A symbol table; reads via handle.index_root"
      pattern: "SymbolTable|index_root"
    - from: "deal/include/deal.h"
      to: "deal/cli/src/ffi.rs"
      via: "extern \"C\" declaration mirrors C header"
      pattern: "deal_ir_json"
---

<objective>
Land DEAL IR v0 as the public contract surface Phases 3-6 will consume. This plan delivers BOTH the IR implementation (Zig: `src/ir.zig` + `src/lowering.zig` + `deal_ir_json` C ABI export) AND the IR v0 specification (ADR + JSON Schema + Markdown reference), then HALTS at a recorded IR-LOCK checkpoint per D-37 / SPEC REQ #9 acceptance #2.

Purpose: D-22..D-27 locked the IR architecture in CONTEXT.md. This plan implements it. The IR is the kernel (FA-1) — once locked, it's the input to SysML codegen (Plan 04), formatter (Plan 05), Phase 4 ReqIF emitter, Phase 5 sim bindings, Phase 6 importers. Mid-phase IR-shape churn is forbidden; SPEC REQ #9 mandates a human-recorded review BEFORE codegen begins. This plan ends with a `checkpoint:human-verify` task that writes 02-IR-LOCK.md with the review outcome.

Output: Two new Zig modules (`ir.zig` + `lowering.zig`); one new C ABI export (`deal_ir_json`); one updated header; two determinism/property tests; three spec artifacts (ADR + schema + README); one recorded IR-LOCK review document. After this plan completes, Plan 04 (codegen) and Plan 05 (formatter) can run in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-prove-the-pipeline/02-CONTEXT.md
@.planning/phases/02-prove-the-pipeline/02-RESEARCH.md
@.planning/phases/02-prove-the-pipeline/02-PATTERNS.md
@.planning/phases/02-prove-the-pipeline/02-SPEC.md
@.planning/phases/02-prove-the-pipeline/02-01-SUMMARY.md
@.planning/phases/02-prove-the-pipeline/02-02-SUMMARY.md
@.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md

<interfaces>
<!-- Key types and contracts for the executor. -->

From RESEARCH.md §Pattern 6 (LOCKED IR v0 shape):
```zig
pub const IrNode = struct {
    id: []const u8,             // fully-qualified path (D-23), arena-owned
    kind: NodeKind,
    span: ast.Span,
    source_file: []const u8,    // workspace-relative
    payload: Payload,
};

pub const NodeKind = enum {
    package,
    part_def, port_def, action_def, state_def,
    requirement_def, constraint_def, attribute_def,
    item_def, interface_def, connection_def, flow_def,
    allocation_def, use_case_def, need_def,
    part_usage, port_usage, attribute_usage,
    system, subsystem, connect, expose,
    traceability_block, allocate, satisfy, validate,
    annotation,
};

pub const Edge = struct { src: []const u8, dst: []const u8, kind: EdgeKind };
pub const EdgeKind = enum {
    specializes, redefines, subsets, satisfies,
    allocated_to, derives_from, imports, traces,
    contains, connects_via, carries,
};
```

From CONTEXT D-26 LOCKED traversal API:
```zig
pub const Document = struct {
    elements: std.StringHashMap(*IrNode),
    edges: []Edge,
    incoming_index: std.StringHashMap([]EdgeRef),
    children_index: std.StringHashMap([]const u8),

    pub fn walk(self: *const Document, visitor: anytype) !void;
    pub fn find(self: *const Document, id: []const u8) ?*IrNode;
    pub fn references(self: *const Document, id: []const u8) []const EdgeRef;
    pub fn children(self: *const Document, id: []const u8) []const []const u8;
    pub fn parent(self: *const Document, id: []const u8) ?[]const u8;
};
```

From CONTEXT D-22 IR JSON envelope:
```
{
  "v": 1,
  "edges": [{"dst": "...", "kind": "...", "src": "..."}, ...],
  "elements": {"id1": {"kind":"...","payload":{...alphabetical...},"source_file":"...","span":[s,e]}, ...},
  "ir_version": "v0"
}
```
Top-level keys MUST be alphabetical (edges, elements, ir_version, v).

From CONTEXT §Claude's Discretion (this plan resolves them):
- Agent-metadata envelope: typed `agent_metadata: {assumes:[], concerns:[], confidence:f64, rationale:str}` field on payload — round-trips showcase usage (verify grep).
- Simulation-binding: `simulation_bindings: []SimBinding` array per RESEARCH §Pattern 6 lines 589-602.
- `deal_format` return shape: bytes via out_ptr/out_len (deferred to Plan 04's plan that ships deal_format).

From deal/src/lib.zig DealHandle:
- Already has: arena, ast_root, mode, source, filename, diagnostics, cached_ast_json, cached_diag_json
- After Plan 02-02: + index_root: ?*sema.SymbolTable
- THIS plan adds: + ir_root: ?*ir.Document + cached_ir_json: ?[]const u8
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author IR v0 specification artifacts (ADR + schema.json + README.md)</name>

  <read_first>
    - .planning/phases/02-prove-the-pipeline/02-CONTEXT.md decisions D-22..D-27 (IR LOCKED contract surface)
    - .planning/phases/02-prove-the-pipeline/02-DISCUSSION-LOG.md — source of "alternatives rejected" for the ADR
    - .planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md — shape template for the new ADR
    - .planning/phases/02-prove-the-pipeline/02-RESEARCH.md §Pattern 6 (lines 509-604) — IR node shape + agent_metadata + simulation_bindings (Claude's Discretion items resolved here)
    - .planning/phases/02-prove-the-pipeline/02-PATTERNS.md §"spec docs" lane (lines 786-816)
    - spec/references/omg-sysml-v2/SysML.json — JSON Schema draft-2020-12 reference shape (top of file shows the dialect)
    - tests/showcase/ — grep for @confidence, @rationale, @assumes, @concerns to verify agent_metadata field shape matches actual usage
  </read_first>

  <files>
    - .planning/decisions/ADR-deal-ir-v0.md (NEW)
    - spec/ir/v0/schema.json (NEW)
    - spec/ir/v0/README.md (NEW)
  </files>

  <action>
    Author the three IR v0 spec artifacts per D-27. These artifacts MUST be authored FIRST in this plan so Tasks 2 and 3 implement against a written contract. Steps:

    (1) Write `.planning/decisions/ADR-deal-ir-v0.md` per the shape contract in PATTERNS Lane "spec docs":
        - Title: "ADR: DEAL IR v0 — public contract for Phases 2-6"
        - Status: PROPOSED (becomes LOCKED after IR-LOCK checkpoint review)
        - Date: today
        - Context: FA-1 (IR is the kernel); Phases 3-6 will all consume this shape; mid-phase churn forbidden by D-31
        - Decision: IR v0 is defined by `spec/ir/v0/schema.json` (normative) + `spec/ir/v0/README.md` (reference); transport is JSON via `deal_ir_json()` C ABI; ID strategy is fully-qualified path strings (D-23); single workspace-wide graph (D-24); comment-free (D-25); traversal API per D-26
        - Alternatives Rejected (from 02-DISCUSSION-LOG.md):
            * FlatBuffers / shared-struct IR transport (D-22 rejected): JSON keeps everything aligned with D-04/D-18 conventions and avoids build-time schema codegen
            * Per-package IR + per-file SysML output (D-24 rejected): single workspace graph aligns with FS-4 system-wide-index intent; multi-file output deferrable to a `--target sysml-v2-per-package` flag in a future phase without changing IR shape
            * MQL query DSL in v0 (D-26 rejected): deferred per PROJECT.md ADR; ad-hoc queries are coded as visitors via `walk(visitor)`
            * AST JSON `"v": 2` schema bump (D-30 rejected): comment-attachment is additive within v: 1
            * Free-floating trivia list (D-28 rejected): structural attachment chosen for round-trip determinism
            * IR carrying doc comments as `documentation` field (D-25 rejected): comment-free IR; doc-comment lowering to SysML deferrable to a follow-up ADR
        - Consequences: Phases 3-6 build against this stable shape; any breaking change requires a new ADR + bump to `ir_version: "v1"`
        - Links: spec/ir/v0/schema.json, spec/ir/v0/README.md

    (2) Write `spec/ir/v0/schema.json` as a JSON Schema draft-2020-12 document with `$schema: "https://json-schema.org/draft/2020-12/schema"`. Cover the envelope + every NodeKind from RESEARCH §Pattern 6:
        - Top-level: `{v: 1, edges: [Edge], elements: {<id>: IrNode}, ir_version: "v0"}`. Alphabetical key order (`edges, elements, ir_version, v`).
        - `$defs.IrNode`: `{kind: NodeKind enum, payload: Payload (oneOf based on kind), source_file: string, span: [u32, u32]}` — note: alphabetical inside each node object too (`kind, payload, source_file, span`).
        - `$defs.Edge`: `{dst: string, kind: EdgeKind enum, src: string}`.
        - `$defs.NodeKind` enum: package, part_def, port_def, action_def, state_def, requirement_def, constraint_def, attribute_def, item_def, interface_def, connection_def, flow_def, allocation_def, use_case_def, need_def, part_usage, port_usage, attribute_usage, system, subsystem, connect, expose, traceability_block, allocate, satisfy, validate, annotation.
        - `$defs.EdgeKind` enum: specializes, redefines, subsets, satisfies, allocated_to, derives_from, imports, traces, contains, connects_via, carries.
        - `$defs.Payload.agent_metadata` (optional): `{assumes: [string], concerns: [string], confidence: number, rationale: string}` — all optional, alphabetical keys.
        - `$defs.Payload.simulation_bindings` (optional): array of `{entry: ?string, equation: ?string, fidelity: ?string, operator: enum["computes","validates against"], target: string, tool: string}` — alphabetical keys.
        - Per-kind payload shapes are referenced via oneOf discriminated by `kind`. Allow `additionalProperties: false` on each payload for strictness.
        Resource budget: keep schema under ~800 lines. Detail the most-common kinds (package, part_def, port_def, requirement_def, attribute_usage, satisfy) fully; simpler kinds (annotation) can reuse a generic payload shape.

    (3) Write `spec/ir/v0/README.md`. Sections per PATTERNS Lane "spec docs" §README authoring guidance:
        - **Overview**: "IR v0 is the kernel data model that DEAL phases 2-6 build against. Lowered from AST + sema symbol table. Transported across the FFI as JSON via `deal_ir_json()`."
        - **ID Strategy (D-23)**: Fully-qualified path strings with `.` separator. Example: `sedan_project.vehicle.electrical.BatteryPack.hvOut`. Translation to SysML v2: `.` → `::` for the `qualifiedName` field; `@id` / `elementId` get a synthesized UUID v5(NAMESPACE_OID, qualified_path) per Pitfall 5.
        - **Span Carry-Over from AST (D-15 + RESEARCH §Pattern S-10)**: Every IR node carries the AST span verbatim (`u32` byte offsets). Used by diagnostic-attach points.
        - **Relationship Graph Contract (D-24, RESEARCH §Pattern 6)**: Edges stored in `edges[]` adjacency list (NOT inline on nodes). `incoming_index` + `children_index` built at lower-end for O(1) `references()`, `children()`, `parent()`. Single workspace-wide graph.
        - **Metadata Envelope Shape**:
            * `agent_metadata` (optional): `{assumes, concerns, confidence, rationale}` — typed envelope, not free-form map. Matches showcase actual usage.
            * `simulation_bindings` (optional): array of `{operator, target, tool, equation?, entry?, fidelity?}` — Phase 5 sim integration reads this; Phase 2 SysML emitter ignores.
        - **Diagnostic Attachment Points**: span + source_file on every node; diagnostics emitted by sema reference these (via DiagnosticCollector).
        - **Backend Traversal API (D-26 LOCKED)**: `walk(visitor)`, `find(id) -> ?Node`, `references(id) -> []EdgeRef`, `children(id) -> []Node`, `parent(id) -> ?Node`. No MQL query DSL in v0.
        - **JSON Envelope (D-22 + D-18)**: top-level alphabetical keys; per-element payload alphabetical; UTF-8; no NUL-terminator (length-prefixed across FFI per D-11).
        - **Stability**: v0 is the PUBLIC contract surface. Breaking changes require an ADR and a bump to `ir_version: "v1"`. Additive fields are allowed within v0 as long as schema.json gets the optional addition + a regen of golden fixtures.
        - **Examples**: include a minimal IR document for `tests/showcase/01-vehicle.deal` (or whatever showcase file is smallest) — actual JSON, ~30-50 lines, with one part_def, one port, one specialization edge.
  </action>

  <verify>
    <automated>test -f .planning/decisions/ADR-deal-ir-v0.md && grep -q 'Alternatives Rejected' .planning/decisions/ADR-deal-ir-v0.md && echo "ADR OK"</automated>
    <automated>test -f spec/ir/v0/schema.json && python3 -c 'import json; d=json.load(open("spec/ir/v0/schema.json")); assert d["$schema"]=="https://json-schema.org/draft/2020-12/schema"; assert "$defs" in d; print("SCHEMA OK")'</automated>
    <automated>test -f spec/ir/v0/README.md && grep -qE 'walk|find|references|children|parent' spec/ir/v0/README.md && grep -q 'agent_metadata' spec/ir/v0/README.md && echo "README OK"</automated>
    <automated>python3 -c 'import json; d=json.load(open("spec/ir/v0/schema.json")); kinds=d["$defs"]["NodeKind"]["enum"]; required=["package","part_def","port_def","requirement_def","attribute_usage","satisfy","traceability_block"]; missing=[k for k in required if k not in kinds]; assert not missing, f"Missing kinds: {missing}"; print("KINDS COVERAGE OK")'</automated>
  </verify>

  <acceptance_criteria>
    - ADR-deal-ir-v0.md exists with Status, Decision, Alternatives Rejected (≥6 alternatives from 02-DISCUSSION-LOG.md), Consequences, Links sections
    - spec/ir/v0/schema.json is a valid JSON Schema draft-2020-12 document; includes $defs for NodeKind (≥20 enum values), EdgeKind (≥10 enum values), Edge, IrNode, agent_metadata, simulation_bindings
    - spec/ir/v0/README.md covers all 6 sections from D-27 (ID strategy, span carry-over, relationship graph, metadata envelope, diagnostic attach points, traversal API)
    - All three files reference one another (cross-linked)
  </acceptance_criteria>

  <done>
    Spec artifacts authored. Tasks 2 and 3 now implement against a written contract.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build src/ir.zig + src/lowering.zig + deal_ir_json C ABI export + determinism test</name>

  <read_first>
    - spec/ir/v0/schema.json (Task 1 output — implementation MUST conform to this schema)
    - spec/ir/v0/README.md (Task 1 output — implementation MUST honor every contract noted)
    - deal/src/ast.zig (lines 14-50 Node + NodeKind + Span; lines 19-22 extern Span struct — REUSE per PATTERNS S-10)
    - deal/src/sema.zig (Plan 02-02 output — provides SymbolTable that lowering consumes for ID resolution)
    - deal/src/parser_deal.zig (lines 361-382 — pattern for arena + diagnostic flow)
    - deal/src/json.zig (lines 28-105 emitAst alphabetical-key emission; lines 1143-1173 appendJsonStringEscaped) — IR emitter MUST follow same hand-rolled pattern per S-2 / Pitfall 1
    - deal/src/lib.zig (lines 36-45 DealHandle struct; lines 285-302 deal_ast_json — exact template for deal_ir_json; lines 306-323 deal_diagnostics_json — same caching pattern)
    - deal/include/deal.h (lines 229-276 deal_ast_json doxygen template; lines 37-68 error-code reference block)
    - deal/tests/unit/determinism_parse_twice.zig — verbatim template for determinism_lower_twice.zig per PATTERNS Lane 2 §determinism_lower_twice (lines 327-335)
    - deal/cli/src/ffi.rs (Plan 02-01 — extern block declares 6 Phase 1 symbols; this plan appends deal_ir_json)
    - .planning/phases/02-prove-the-pipeline/02-RESEARCH.md §Pattern 7 (AST → IR lowering with arena + ID interning), §Example 2 (IR JSON emitter sketch), §Pitfall 3 (arena lifetime crossing FFI)
    - .planning/phases/02-prove-the-pipeline/02-PATTERNS.md Lane 2 §src/ir.zig, §src/lowering.zig, §src/json.zig (IR emitter — hand-rolled, NOT std.json.Stringify per Pitfall 1)
    - .planning/phases/02-prove-the-pipeline/02-CONTEXT.md D-22 (IR JSON shape), D-26 (traversal API LOCKED)
  </read_first>

  <files>
    - deal/src/ir.zig (NEW — ~400-600 LOC; IrNode + Edge + Document + traversal API)
    - deal/src/lowering.zig (NEW — ~600-900 LOC; AST + SymbolTable → IR Document)
    - deal/src/json.zig (MODIFIED — add emitIrJson per Pattern S-2 alphabetical-key hand-rolled emission)
    - deal/src/lib.zig (MODIFIED — add ir_root + cached_ir_json fields to DealHandle; add deal_ir_json export; add lower-internal test-only helper per S-7)
    - deal/include/deal.h (MODIFIED — append deal_ir_json declaration with doxygen mirroring deal_ast_json; update error-code reference block with E2xxx band)
    - deal/cli/src/ffi.rs (MODIFIED — append deal_ir_json extern decl; comment "Plan 04 will append deal_format here")
    - deal/tests/unit/determinism_lower_twice.zig (NEW)
    - deal/tests/unit/property_ir_id_uniqueness.zig (NEW — optional per PATTERNS but recommended)
    - deal/tests/unit/_all.zig (MODIFIED — register both new tests)
  </files>

  <action>
    Implement IR + lowering + FFI export in three sub-steps:

    Sub-step A — Types (commit 1):
    (1) Create deal/src/ir.zig per spec/ir/v0/schema.json + RESEARCH §Pattern 6:
        - `pub const Span = ast.Span;` (reuse per S-10)
        - `pub const IrNode = struct { id, kind, span, source_file, payload }`
        - `pub const NodeKind = enum { … }` — list ALL kinds from schema.json
        - `pub const Edge = struct { src, dst, kind }`
        - `pub const EdgeKind = enum { … }`
        - `pub const Payload = union(NodeKind) { … }` — tagged union per kind; include optional `agent_metadata: ?AgentMetadata` and `simulation_bindings: ?[]SimBinding` on payloads that carry annotations.
        - `pub const AgentMetadata = struct { assumes: []const []const u8, concerns: []const []const u8, confidence: ?f64, rationale: ?[]const u8 }`
        - `pub const SimBinding = struct { entry: ?[]const u8, equation: ?[]const u8, fidelity: ?[]const u8, operator: enum { computes, validates_against }, target: []const u8, tool: ?[]const u8 }`
        - `pub const Document = struct { elements, edges, incoming_index, children_index }` + 5 traversal methods per D-26 LOCKED.
        - Implement `walk(visitor)` as pre-order then post-order calls on `visitor.preOrder(node)` / `visitor.postOrder(node)` using duck-typed `anytype` per Zig idiom.
        - `find(id) -> ?*IrNode` is a `StringHashMap.get(id)`.
        - `references(id) -> []const EdgeRef` reads `incoming_index.get(id)`.
        - `children(id) -> []const []const u8` reads `children_index.get(id)`.
        - `parent(id) -> ?[]const u8` walks `id`'s dot-separated path; returns prefix.

    Sub-step B — Lowering + FFI (commit 2):
    (2) Create deal/src/lowering.zig per RESEARCH §Pattern 7 + PATTERNS Lane 2 §src/lowering.zig:
        - `pub fn lower(arena: std.mem.Allocator, ast_root: *ast.Node, symbols: *const sema.SymbolTable) !*ir.Document`
        - Three-pass walk:
            * Pass 1: enumerate every declaration; build `id_intern: std.StringHashMap([]const u8)` map of qualified-path → interned slice (Pattern 7 ID interning).
            * Pass 2: walk again; for each AST decl, allocate an IrNode in the arena with id from intern, span carried over, source_file from sema's symbol entry (workspace-relative), payload lowered per kind. Build edges as encountered (specializes, redefines, satisfies, allocated_to, derives_from, imports, traces, contains, connects_via, carries).
            * Pass 3: build `incoming_index` + `children_index` from the edges list — adjacency indexes for O(1) traversal-API queries.
        - Allocate ALL into the arena (S-1, D-02).
        - Lower agent_metadata annotations into the typed envelope per Pattern 6: parse the annotation body for @confidence (f64), @rationale (string), @assumes ([]string), @concerns ([]string). Null fields are omitted in IR (per D-18 emission rule).
        - Lower @simulation:<<computes>> and @simulation:<<validates against>> annotations into simulation_bindings per RESEARCH lines 589-602. Phase 2 emitter ignores them; the IR carries them for Phase 5.
    (3) Modify deal/src/json.zig — add `pub fn emitIrJson(allocator, doc: *const ir.Document) ![]const u8` per PATTERNS Lane 2 §emitter pattern. Hand-rolled alphabetical-key emission per S-2 (do NOT use std.json.Stringify — Pitfall 1 + Pitfall 5). Order: `{v: 1, edges: [...], elements: {...}, ir_version: "v0"}` at top level (alphabetical order `edges, elements, ir_version, v` if we put `v` last; or `{v, ...rest}` if we keep v first — schema.json fixes the answer; honor whatever the schema declares). Inside each Edge: `{dst, kind, src}`. Inside each IrNode object: `{kind, payload, source_file, span}`. Inside payload: keys alphabetical; `agent_metadata` and `simulation_bindings` placed alphabetically if present, omitted if null.
    (4) Modify deal/src/lib.zig:
        - Extend DealHandle (lines 36-45) with: `ir_root: ?*ir.Document = null`, `cached_ir_json: ?[]const u8 = null`. (Plan 04 adds `cached_fmt_bytes`.)
        - Add `pub export fn deal_ir_json(handle_ptr, out_ptr, out_len) callconv(.c) bool` mirroring `deal_ast_json` (lines 285-302). Lazy: if `ir_root == null`, call `lowering.lower(...)` first using sema's symbol table (handle.index_root); cache result in cached_ir_json. Length-prefixed UTF-8, NOT NUL-terminated (D-11).
        - Add a test-only internal helper `pub fn deal_lower_internal(allocator, source) !*ir.Document` (NO `export`, NO callconv(.c)) per S-7. Used by determinism_lower_twice.zig with `std.testing.allocator` for leak detection. Verify `nm -gU` will NOT see this symbol (only the C ABI ones).
    (5) Modify deal/include/deal.h — append `deal_ir_json` declaration with doxygen mirroring `deal_ast_json` (lines 229-276 template). Update the error-code reference block (lines 37-68) with the E2xxx band (E2000-E2499). DO NOT add deal_format yet — that's Plan 04's responsibility.
    (6) Modify deal/cli/src/ffi.rs — append the `deal_ir_json` extern declaration mirroring the existing `deal_ast_json` shape per PATTERNS Lane 4 §ffi.rs. Add a comment: "Plan 02-04 will append deal_format here (D-21)."

    Sub-step C — Tests (commit 3):
    (7) Create deal/tests/unit/determinism_lower_twice.zig per PATTERNS Lane 2 §determinism_lower_twice (verbatim from determinism_parse_twice.zig with three changes):
        - Replace `lib.deal_ast_json` with `lib.deal_ir_json`.
        - Test name: `determinism.lower_twice`.
        - Doc-comment cites REQ #3 acceptance ("Two byte-equal lowering passes of the same source produce byte-identical IR").
        - PUBLIC C ABI path (not deal_lower_internal) — same as parse_twice.
        - gpa.dupe-before-free buffer-lifetime pattern from Phase 1.5 Plan 04 (Pitfall 3 mitigation).
        - Iterate all 19 showcase files (S-9).
    (8) Create deal/tests/unit/property_ir_id_uniqueness.zig per PATTERNS Lane 2 §optional test:
        - For each showcase file: lower, walk(visitor), collect all IDs into a set, assert no duplicates.
        - Exhaustive switch over `NodeKind` so a missing arm is a compile error.
    (9) Register both in deal/tests/unit/_all.zig.
    (10) Verify the nm grep gate count: `nm -gU deal/zig-out/lib/libdeal.a | grep -E '^.+ T (_deal|deal)_' | wc -l` returns 7 (6 Phase 1 exports + deal_ir_json). Plan 04 will bump this to 8 (adds deal_format).

    Pitfall 1: NO std.json.Stringify — hand-rolled alphabetical emission only.
    Pitfall 3: Rust ffi.rs MUST Cow-clone IR bytes BEFORE any deal_free call — Plan 04's test will verify this; this plan adds the extern decl + the determinism test in Zig.
    D-25: IR is comment-free. Lowering MUST NOT copy leading_comments / trailing_comments / doc_comment into IR nodes. Phase 2 SysML emitter walks IR; fmt walks AST.
  </action>

  <verify>
    <automated>cd deal && zig build 2>&1 | tail -10 && echo "BUILD OK"</automated>
    <automated>cd deal && zig build test 2>&1 | tail -20 && echo "ALL TESTS OK"</automated>
    <automated>cd deal && zig build test -Dtest-filter=determinism.lower_twice 2>&1 | grep -q 'passed' && echo "IR DETERMINISM OK"</automated>
    <automated>cd deal && zig build test -Dtest-filter=property_ir_id_uniqueness 2>&1 | grep -q 'passed' && echo "ID UNIQUENESS OK"</automated>
    <automated>cd deal && zig build phase-1.5-gate 2>&1 | tail -5 && echo "PHASE 1.5 GATE OK"</automated>
    <automated>cd deal && nm -gU zig-out/lib/libdeal.a 2>/dev/null | grep -cE 'T _?deal_(parse|free|has_errors|diagnostics_count|ast_json|diagnostics_json|ir_json)' | awk '{ if ($1 == 7) print "7 EXPORTS OK (Plan 04 will add deal_format → 8)"; else { print "EXPORT COUNT WRONG: " $1; exit 1 } }'</automated>
    <automated>! nm -gU deal/zig-out/lib/libdeal.a | grep -q 'deal_lower_internal' && echo "INTERNAL HELPER HIDDEN OK"</automated>
    <!-- IR JSON is hand-rolled per Pitfall 1; this grep ensures no Stringify slipped in -->
    <automated>grep -v '^//' deal/src/json.zig | grep -c 'std.json.Stringify\|std.json.stringify' | awk '{ if ($1 == 0) print "NO STRINGIFY OK"; else { print "STRINGIFY FOUND"; exit 1 } }'</automated>
    <automated>cd deal && cargo build --workspace 2>&1 | tail -10 && echo "RUST BUILD OK"</automated>
  </verify>

  <acceptance_criteria>
    - src/ir.zig declares all types from spec/ir/v0/schema.json + the D-26 LOCKED traversal API (walk, find, references, children, parent)
    - src/lowering.zig produces an IR Document for every AST root; consumes sema's SymbolTable
    - Agent metadata + simulation bindings lowered into typed envelopes per RESEARCH §Pattern 6
    - emitIrJson hand-rolls alphabetical emission per Pitfall 1 (no std.json.Stringify)
    - deal_ir_json C ABI export added; nm count is 7 (Plan 04 → 8); deal_lower_internal NOT in nm
    - determinism_lower_twice passes on all 19 showcase files via PUBLIC C ABI
    - property_ir_id_uniqueness passes — no duplicate qualified paths
    - cli/src/ffi.rs declares deal_ir_json; cargo build --workspace exits 0
    - IR is comment-free per D-25 (lowering does NOT copy comment fields)
    - phase-1.5-gate still GREEN
  </acceptance_criteria>

  <done>
    IR + lowering operational; determinism + uniqueness invariants enforced; C ABI extended; specs implemented. Ready for IR-LOCK checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: IR-LOCK CHECKPOINT — recorded human review before Plan 04/05 begin</name>

  <what-built>
    Three IR v0 spec artifacts (ADR + JSON Schema + Markdown reference) PLUS two Zig modules (ir.zig + lowering.zig) PLUS deal_ir_json C ABI export PLUS Rust extern decl PLUS determinism + ID uniqueness tests. The IR shape and the spec for it both exist.
  </what-built>

  <how-to-verify>
    SPEC.md REQ #9 acceptance #2 mandates a recorded review. Steps:

    1. Read `.planning/decisions/ADR-deal-ir-v0.md` end-to-end. Verify the Alternatives Rejected section accurately reflects 02-DISCUSSION-LOG.md. Verify the Decision section is internally consistent with D-22..D-27.

    2. Read `spec/ir/v0/schema.json`. Verify it parses as JSON. Run `python3 -c 'import json; json.load(open("spec/ir/v0/schema.json"))'`. Verify the enum sets for NodeKind and EdgeKind match RESEARCH §Pattern 6 verbatim.

    3. Read `spec/ir/v0/README.md`. Verify all 8 sections present (Overview, ID Strategy, Span Carry-Over, Relationship Graph Contract, Metadata Envelope Shape, Diagnostic Attach Points, Backend Traversal API, JSON Envelope, Stability) plus a worked example.

    4. Inspect the IR JSON produced by ONE showcase file:
       ```bash
       cd deal && cargo run --bin deal -- parse tests/showcase/<smallest>.deal --json 2>/dev/null | python3 -m json.tool | head -40
       ```
       (Note: `deal parse` may not yet emit IR — verify via the FFI directly via a quick test or by reading determinism_lower_twice's emitted bytes from `zig build test -Dtest-filter=determinism.lower_twice -Dverbose`.)

    5. Spot-check ONE workshop element: walk the chain from an `@trace:<<satisfies>>` annotation in a `.dealx` showcase file through the lowering → verify the IR has the corresponding edge with `kind: "satisfies"`.

    6. Verify the C ABI surface: `cd deal && nm -gU zig-out/lib/libdeal.a | grep -E 'T _?deal_' | sort` should list 7 symbols: deal_parse, deal_free, deal_has_errors, deal_diagnostics_count, deal_ast_json, deal_diagnostics_json, deal_ir_json. Plan 04 will add deal_format → 8.

    7. Record the review outcome in `.planning/phases/02-prove-the-pipeline/02-IR-LOCK.md`:

       ```markdown
       # IR-LOCK Checkpoint — Phase 2 Plan 03

       **Date:** YYYY-MM-DD HH:MM
       **Reviewer:** <human name>
       **Outcome:** approved | changes-requested

       ## Verified
       - [x] ADR-deal-ir-v0.md reviewed; alternatives section accurate
       - [x] spec/ir/v0/schema.json parses; NodeKind / EdgeKind sets complete
       - [x] spec/ir/v0/README.md sections present
       - [x] IR JSON spot-check on tests/showcase/<file>.deal passes
       - [x] @trace edge round-trips end-to-end
       - [x] nm count == 7 (Plan 04 will add deal_format → 8)
       - [x] determinism.lower_twice GREEN on all 19 showcase files

       ## Findings (if changes-requested)
       <list itemized changes>

       ## Decision
       <free-form rationale>
       ```

       If outcome is `changes-requested`: list the specific changes; do NOT proceed to Plans 04/05 until a follow-up commit lands the changes and a SECOND review records `approved`.

       If outcome is `approved`: commit `02-IR-LOCK.md` with message "phase-2-03: IR-LOCK approved (see 02-IR-LOCK.md)" and signal that Plans 04 and 05 may now run in parallel.

    8. Update `.planning/decisions/ADR-deal-ir-v0.md` Status field from PROPOSED to LOCKED upon approval. Commit the status change in the same commit as 02-IR-LOCK.md.
  </how-to-verify>

  <resume-signal>
    Type "approved" if the IR shape is acceptable and Plans 04 + 05 can begin in parallel.
    Type "changes-requested: <specifics>" to halt the phase and require revisions.
    Type "blocker: <description>" if the IR cannot be locked without an upstream change.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AST → IR lowering | Adversarial AST (deep nesting, malformed reference targets) must not panic |
| IR JSON → FFI buffer | Length-prefixed UTF-8 buffer ownership crosses Zig→Rust boundary |
| IR schema.json → public contract | Public contract surface for Phases 3-6; breaking changes cascade |
| Lowering pass → arena alloc | All IR data must allocate into per-handle arena (D-02); cross-arena pointers forbidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | IR JSON key-order drift (D-18 violation) | mitigate | emitIrJson hand-rolls alphabetical emission (no std.json.Stringify per Pitfall 1); determinism.lower_twice catches byte-level regression on any reorder |
| T-02-16 | Spoofing | Use-after-free of deal_ir_json buffer (Pitfall 3) | mitigate | Plan 02-04 Cow-clones IR bytes BEFORE deal_free; THIS plan's determinism test exercises the gpa.dupe-before-free lifetime pattern via the PUBLIC C ABI |
| T-02-17 | Repudiation | IR-LOCK approval without record | mitigate | 02-IR-LOCK.md REQUIRED before Plans 04/05 begin; ADR status flips PROPOSED→LOCKED only on approval (SPEC REQ #9 acceptance #2) |
| T-02-18 | Tampering | Per-handle arena overflow on multi-million-element model | accept | Phase 2 scope is 19 showcase files + 5-8 golden fixtures (well under 100K elements); ArenaAllocator scales to GB-class allocations; Phase 6+ concern |
| T-02-19 | Information disclosure | source_file paths in IR leak absolute filesystem paths | mitigate | sema (Plan 02-02) records workspace-relative paths; lowering carries them verbatim into IR; documented as contract in spec/ir/v0/README.md |
| T-02-20 | Tampering | Hidden internal helper exported to C ABI | mitigate | deal_lower_internal declared `pub fn` (no `export`, no callconv); nm grep gate in Task 2 verifies it's NOT visible in libdeal.a symbol table |
| T-02-SC | Tampering | npm/pip/cargo installs (Supply-Chain) | mitigate | This plan adds NO new package installs; uses workspace deps from Plan 02-01 |
</threat_model>

<verification>
- `zig build` exits 0
- `zig build test` exits 0 including determinism_lower_twice + property_ir_id_uniqueness
- `zig build phase-1.5-gate` exits 0
- `cargo build --workspace` exits 0
- nm count on libdeal.a is 7 (Plan 04 will bump to 8)
- deal_lower_internal NOT in nm output (S-7)
- emitIrJson is hand-rolled (no std.json.Stringify references in src/json.zig)
- spec/ir/v0/schema.json parses; declares NodeKind enum with all 27 kinds; declares EdgeKind enum with all 11 kinds
- ADR-deal-ir-v0.md Status flips PROPOSED → LOCKED only on IR-LOCK approval (Task 3)
- 02-IR-LOCK.md exists with reviewer + timestamp + outcome
</verification>

<success_criteria>
1. IR v0 spec artifacts (ADR + schema.json + README.md) land per D-27
2. src/ir.zig + src/lowering.zig implement the IR + traversal API per D-22..D-26
3. deal_ir_json C ABI export added (7 total; Plan 04 → 8)
4. Determinism + ID uniqueness invariants enforced via new property tests
5. IR-LOCK checkpoint review RECORDED in 02-IR-LOCK.md; ADR Status flips to LOCKED on approval
6. Plans 04 and 05 are unblocked (can run in parallel)
</success_criteria>

<output>
Create `.planning/phases/02-prove-the-pipeline/02-03-SUMMARY.md` when done. Summary must record: (a) ir.zig + lowering.zig final LOC; (b) the IR-LOCK review outcome + timestamp (paste from 02-IR-LOCK.md); (c) confirmation that ADR Status flipped PROPOSED → LOCKED; (d) example IR JSON snippet from one showcase file; (e) updated nm count; (f) Phase 1.5 gate still GREEN.
</output>
</output>
