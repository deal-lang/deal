---
phase: 05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit
plan: 01
subsystem: spec
tags: [ebnf, spec, submodule, grammar, deal-lang-spec]

# Dependency graph
requires:
  - phase: 05-simulation-integration
    provides: "spec submodule pin dd3bb78 (sims/v0 + showcase input values)"
provides:
  - "05.1-STEP-0-GATE.md D-10 baseline: all 19 showcase files parse with exit 0"
  - "D-01 confirmed standing: zero category-(c) annotation-body comma separators"
  - "spec submodule 74dfea5: honest-opaque GapBlock (D-05), AnnotationKey keyword acceptance (D-03/I2), ';' canonical (D-01/I1), I6 citation fixes, SD-15a/SD-15b locked"
  - "Parent deal gitlink bumped to 74dfea5 (Wave-A HEAD)"
  - "D-07 honored: 7ce59bc calc/constraint recall excluded from gitlink"
affects: [05.1-02, 05.1-03, 05.1-04, fresh-worktree-gate]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Wave-gated spec reconciliation: empirical gate first (Step 0), then spec-only edits, then push + gitlink bump, then human-verify fresh-worktree gate"
    - "D-07 forward-planning: spec branch from pin dd3bb78 only; calc/constraint 7ce59bc deferred to Phase 6"

key-files:
  created:
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-STEP-0-GATE.md
  modified:
    - spec/grammar/dealx.ebnf
    - spec/grammar/deal.ebnf
    - spec/grammar/lexical.ebnf
    - spec/grammar/DESIGN-DECISIONS.md
    - spec  # (gitlink bumped from dd3bb78 to 74dfea5)

key-decisions:
  - "D-01 confirmed standing: `,` never used as annotation-body field separator in any showcase file; Waves B/C may proceed"
  - "SD-15a locked: `;` is the canonical annotation field terminator; `,` is rejected (E0123)"
  - "SD-15b locked: keyword tokens are valid annotation field keys (AnnotationKey ::= IDENT | _Keyword)"
  - "D-05 applied: GapBlock replaced with honest-opaque _OpaqueBody in dealx.ebnf; structured gap parsing deferred to Wave E"
  - "D-07 honored: spec branch from dd3bb78 only; 7ce59bc (calc/constraint recall) excluded and deferred to Phase 6"
  - "Inherited E2503 failure (sema.dimensional.regression_pins) is a pre-existing Phase-4 carryover unrelated to this plan; not fixed here"

patterns-established:
  - "Step 0 before spec edits: always record the empirical baseline before grammar-reconciliation to prove later strictness changes are intentional"
  - "Push spec then bump gitlink atomically: spec push confirmed reachable via fresh-worktree gate before the plan is marked complete"

requirements-completed: [D-10, D-05, D-06, D-07]

# Metrics
duration: ~1.5h
completed: 2026-06-09
---

# Phase 05.1 Plan 01: Step-0 Gate + Wave-A Spec Reconciliation Summary

**Honest-opaque GapBlock, AnnotationKey keyword acceptance, and `;`-canonical separator locked in spec grammar (74dfea5) after empirical D-01 confirmation across all 19 showcase files**

## Performance

- **Duration:** ~1.5h
- **Started:** 2026-06-09T14:00:00Z
- **Completed:** 2026-06-09T18:11:00Z
- **Tasks:** 3 (2 auto + 1 checkpoint:human-verify)
- **Files modified:** 5 (spec submodule: 4 files + parent gitlink)

## Accomplishments

- D-10 Step 0 baseline recorded: all 19 showcase files parse with exit 0; `,` classified in zero annotation-body-separator (category-c) roles; D-01 confirmed standing.
- Wave-A spec reconciliation committed and pushed (74dfea5): honest-opaque GapBlock (D-05/I5), AnnotationKey keyword acceptance (D-03/I2), `;` canonical field terminator (D-01/I1), stale I6 line citations corrected, SD-15a/SD-15b locked in DESIGN-DECISIONS.md.
- Parent deal gitlink bumped to 74dfea5; fresh-worktree gate (`scripts/verify-fresh-worktree.sh phase-1.5-gate`) confirmed 65/66 tests passing against the pushed spec — D-07 verified (7ce59bc absent from spec history).

## Task Commits

Each task was committed atomically:

1. **Task 1: Step 0 empirical gate** - `d5fdf6d` (docs)
2. **Task 2: Wave A spec edits + push + gitlink bump** - `814b63e` (chore)
3. **Task 3: checkpoint:human-verify** - approved; gate evidence recorded in STATE.md

**Plan metadata:** TBD (docs commit — this SUMMARY + STATE/ROADMAP updates)

## Files Created/Modified

- `.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-STEP-0-GATE.md` — D-10 baseline: per-file parse results + comma classification table for all 19 showcase files
- `spec/grammar/dealx.ebnf` — GapBlock replaced with honest-opaque `_OpaqueBody` (D-05/I5)
- `spec/grammar/deal.ebnf` — AnnotationKey production added; AnnotationField updated to use AnnotationKey and `;` terminator; I6 citations corrected
- `spec/grammar/lexical.ebnf` — `_Keyword` non-terminal defined for use in AnnotationKey
- `spec/grammar/DESIGN-DECISIONS.md` — SD-15a and SD-15b locked decisions added; NC-1 domain fix
- `spec` (gitlink) — bumped from dd3bb78 to 74dfea5

## Decisions Made

- **D-01 standing confirmed:** No `,` annotation-body separators in any showcase file. Waves B/C proceed as planned.
- **SD-15a locked:** `;` is the canonical annotation field terminator. `,` in annotation context will be rejected with E0123 (Wave B, Plan 05.1-03).
- **SD-15b locked:** Keyword tokens are valid annotation field keys. `AnnotationKey ::= IDENT | _Keyword` is now in the spec.
- **D-07 honored:** spec branch from `dd3bb78` only. The calc/constraint recall commit `7ce59bc` is NOT merged and remains deferred to Phase 6.

## Deviations from Plan

None — plan executed exactly as written. The checkpoint was approved with the documented caveat below.

### Checkpoint Gate Note

The fresh-worktree gate ran 65/66 tests. The single failure is the **inherited Phase-4 E2503 regression_pins** (`sema.dimensional.regression_pins` fails only when `../deal-stdlib/packages/units/mod.deal` is absent, which is the case in a phase-1.5 fresh worktree that does not symlink sibling repos). This failure:

- Is a pre-existing Phase-4 carryover tracked in `05-VALIDATION.md` line 100 and `v2.1.0-MILESTONE-AUDIT.md` line 72.
- Is independent of plan 05.1-01's spec-only edits.
- Was present before this plan executed and is not caused by the Wave-A spec changes.
- Transitive `recovery.corpus` failures share the same stdlib-FileNotFound root cause.

The checkpoint's core purpose — proving the pushed spec is reachable and the gitlink resolves in a clean clone — is satisfied.

## Issues Encountered

None beyond the inherited E2503 carryover documented in the checkpoint note above.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- D-10 baseline is recorded; D-01 confirmed; Waves B/C may proceed without re-planning.
- Plan 05.1-02 (E2501/E2502/E2503 CLI integration tests) is unblocked.
- Plan 05.1-03 (Wave B parser strictness: `;` consume + `,` reject E0123) is unblocked.
- Plan 05.1-04 (Wave C array unification) is unblocked after 05.1-03.
- The inherited E2503 dimensional regression pin remains deferred to Phase 6 (stdlib integration in a fresh-worktree context).

---
*Phase: 05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit*
*Completed: 2026-06-09*
