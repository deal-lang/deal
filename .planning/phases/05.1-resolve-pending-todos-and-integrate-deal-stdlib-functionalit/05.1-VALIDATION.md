---
phase: 05.1
slug: resolve-pending-todos-and-integrate-deal-stdlib-functionalit
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-09
validated: 2026-06-09
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `05.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Zig `zig build test` (unit + snapshot + corpus) · Rust `cargo test -p deal` |
| **Config file** | `build.zig` (Zig) · `cli/Cargo.toml` (Rust) — both exist, no Wave-0 install |
| **Quick run command** | `zig build test -Dtest-filter=<subset>` (~5–30s) |
| **Full suite command** | `zig build test && cargo test -p deal` |
| **Snapshot regeneration** | `zig build test -Dupdate-snapshots=true` |
| **Estimated runtime** | ~30–90 seconds full |

---

## Sampling Rate

- **After every task commit:** Run `zig build test -Dtest-filter=<subset>` (or `cargo test -p deal --test e2500_cli` for TODO 2 tasks)
- **After every plan wave:** Run `zig build test && cargo test -p deal`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** ~90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Decision | Observable Signal | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|-------------------|-----------|-------------------|-------------|--------|
| 01-T1 | 05.1-01 | 1 | D-10 | Zero parse errors over showcase files; baseline recorded; D-01 confirmed standing | corpus + record | `zig build run -- parse <each showcase file>` → `05.1-STEP-0-GATE.md` | ✅ existing corpus | ✅ green |
| 01-T2 | 05.1-01 | 1 | D-05/D-06/D-07 | `dealx.ebnf` GapBlock honest-opaque; spec pushed; gitlink bumped; `7ce59bc` excluded | manual + gate | `scripts/verify-fresh-worktree.sh` + `git submodule status` | ✅ | ✅ green (manual gate) |
| 02-T1 | 05.1-02 | 1 | D-08/D-09 (E2501) | `deal check` exits non-zero + `E2501` (mixed-unit same-dim) | integration | `cargo test -p deal --test e2500_cli` | ✅ `test_e2501_mixed_unit_same_dimension` | ✅ green |
| 02-T1 | 05.1-02 | 1 | D-08/D-09 (E2502) | `deal check` exits non-zero + `E2502` (unknown unit literal) | integration | `cargo test -p deal --test e2500_cli` | ✅ `test_e2502_unknown_unit_literal` | ✅ green |
| 02-T1 | 05.1-02 | 1 | D-08/D-09 (E2503) | `deal check` exits non-zero + `E2503` (conversion mismatch); distinct fixture from regression pin | integration | `cargo test -p deal --test e2500_cli` | ✅ `test_e2503_conversion_type_mismatch` | ✅ green |
| 02-T1 | 05.1-02 | 1 | D-09 (E2500 regress) | pre-existing E2500 tests still green | integration | `cargo test -p deal --test e2500_cli` | ✅ 2 tests green | ✅ green |
| 03-T1 | 05.1-03 | 2 | D-01/D-02 | E0123/E0124 constants declared; m21/m22 fixtures + pins exist (RED until 03-T2) | corpus (Wave 0) | `zig build` compiles; `recovery_corpus.zig` references pins | ✅ m21/m22 pinned | ✅ green |
| 03-T2 | 05.1-03 | 2 | D-01 | `;` explicitly consumed (no silent skip); `,` separator → E0123 | unit + corpus | `zig build test -Dtest-filter=strictness` / `-Dtest-filter=recovery` | ✅ strictness test (a) | ✅ green |
| 03-T2 | 05.1-03 | 2 | D-02 | unknown verification key → E0124 | unit + corpus | `zig build test -Dtest-filter=strictness` / `-Dtest-filter=recovery` | ✅ strictness test (b/c) | ✅ green |
| 03-T2 | 05.1-03 | 2 | D-03 | keyword field key (`from:`) → explicit tested accept | unit | `zig build test -Dtest-filter=strictness` | ✅ strictness test (d) | ✅ green |
| 03-T2 | 05.1-03 | 2 | regression | all 50+ malformed-corpus pins still fire; no new silent acceptance | corpus | `zig build test -Dtest-filter=recovery` | ✅ (incl. m23/m24) | ✅ green |
| 04-T1 | 05.1-04 | 3 | D-04 | `array_literal` NodeKind + json arm defined (contract) | compile + grep | `grep array_literal src/ast.zig src/json.zig` | ✅ `ast.zig:138`, `json.zig:447` | ✅ green |
| 04-T2 | 05.1-04 | 3 | D-04 | single `l_bracket` atom in expr.zig; both duplicate parsers deleted; parser-side `__array__` gone | unit + grep | `zig build` + `grep -c __array__ src/parser_*.zig` | ✅ `expr.zig:278`; parsers deleted | ✅ green |
| 04-T3 | 05.1-04 | 3 | D-04 | byte-identical AST snapshots in both array contexts; fmt round-trip; `__array__` fully removed | snapshot + fmt | `zig build test` (regenerate then re-lock); `-Dtest-filter=fmt` | ✅ snapshots byte-stable | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] New malformed-corpus fixtures for D-01 (`,` as annotation separator → E0123) and D-02 (unknown verification key → E0124) — created in Plan 03 Task 1
- [x] `E2501` / `E2502` / `E2503` CLI test cases — added to `cli/tests/e2500_cli.rs` in Plan 02 Task 1
- [x] New `array_literal` NodeKind definition in `src/ast.zig` — Plan 04 Task 1 (Wave C prerequisite)
- [x] New diagnostic codes in `src/diagnostics.zig` (E0123/E0124) for D-01/D-02 — Plan 03 Task 1

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| `dealx.ebnf` GapBlock matches parser byte-scan reality | D-05 | Spec text correctness has no executable assertion | Review `spec/grammar/dealx.ebnf` GapBlock production against `parser_dealx.zig:1229-1252` |
| Spec push + parent gitlink bump | D-06 | Cross-repo submodule state | `git submodule status` shows new pin; `scripts/verify-fresh-worktree.sh` passes against pushed spec (Plan 01 human-verify checkpoint) |

---

## Validation Sign-Off

- [x] All tasks have automated verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (E2501–E2503 tests, D-01/D-02 fixtures, array_literal NodeKind, E0123/E0124 diagnostics)
- [x] No watch-mode flags
- [x] Feedback latency < 90s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planner-signed 2026-06-09

---

## Validation Audit 2026-06-09

State A audit — reconciled stale `⬜ pending` map against live test runs.

| Metric | Count |
|--------|-------|
| Requirements in map | 14 |
| COVERED (automated, green) | 13 |
| Manual-only (gate, documented) | 1 (01-T2 / D-05–D-07) |
| Gaps found | 0 |
| Resolved (tests written) | 0 (none needed) |
| Escalated | 0 |

**Evidence (re-run this audit):**
- `cargo test -p deal --test e2500_cli` → **5 passed, 0 failed** (E2500 ×2, E2501, E2502, E2503).
- `zig build test` → **69/70 passed**. The single failure is `sema.dimensional.regression_pins` for `07-conversion-mismatch.deal` (E2503), caused by the absent sibling `../deal-stdlib/packages/units/mod.deal` in the bare workspace. This is the **documented Phase-4 carryover** (`v2.1.0-MILESTONE-AUDIT.md` line 72), out of Phase 5.1 scope, and is **not** a row in this Per-Task Map.
- All map-referenced fixtures present: `m21`/`m22`/`m23`/`m24` malformed corpus, `parser_deal_strictness.zig` (5 tests), `e2500_cli.rs` (5 `#[test]`), `05.1-STEP-0-GATE.md`, AST snapshot with `array_literal`.

**Outcome:** Phase 05.1 is Nyquist-compliant. Every in-scope decision (D-01..D-11) has automated verification or a documented manual gate; no 3 consecutive tasks lack an automated signal; feedback latency < 90s.
