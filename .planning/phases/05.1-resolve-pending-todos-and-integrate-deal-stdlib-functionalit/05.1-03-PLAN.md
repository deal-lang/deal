---
phase: 05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit
plan: 03
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - src/diagnostics.zig
  - src/parser_deal.zig
  - tests/malformed/m21_annotation_comma_separator.deal
  - tests/malformed/m22_unknown_verification_key.deal
  - tests/unit/recovery_corpus.zig
  - tests/unit/parser_deal_strictness.zig
autonomous: true
requirements: [D-01, D-02, D-03, D-11]
must_haves:
  truths:
    - "`;` is explicitly consumed as the canonical annotation-body field terminator (no silent skip)"
    - "`,` as an annotation-body field separator emits a hard diagnostic (E0123), not silent acceptance"
    - "An unrecognized key inside a verification block emits a hard diagnostic (E0124), not a silent drop"
    - "A keyword field key (e.g. `from:`) is accepted via an explicit, tested path"
    - "All pre-existing 50 malformed-corpus pins still fire; no new silent acceptance"
    - "Step 0 showcase corpus still parses clean after the strictness changes"
  artifacts:
    - path: "src/diagnostics.zig"
      provides: "E0123 e_annotation_comma_separator + E0124 e_unknown_verification_key constants"
      contains: "e_unknown_verification_key"
    - path: "tests/malformed/m21_annotation_comma_separator.deal"
      provides: "D-01 `,`-rejection malformed fixture"
      contains: "package"
    - path: "tests/malformed/m22_unknown_verification_key.deal"
      provides: "D-02 unknown-verification-key malformed fixture"
      contains: "verification"
    - path: "tests/unit/parser_deal_strictness.zig"
      provides: "Explicit accept/reject tests for D-01/D-02/D-03"
      contains: "from"
  key_links:
    - from: "src/parser_deal.zig:1914 annotation-body loop"
      to: "Codes.e_annotation_comma_separator"
      via: "p.diags.append on `,` separator"
      pattern: "e_annotation_comma_separator"
    - from: "src/parser_deal.zig:1956 verification-key else arm"
      to: "Codes.e_unknown_verification_key"
      via: "p.diags.append on unrecognized key"
      pattern: "e_unknown_verification_key"
    - from: "tests/unit/recovery_corpus.zig m_pins"
      to: "m21/m22 fixtures"
      via: "Pin{ .name, .expected_code }"
      pattern: "m21_annotation_comma_separator"
---

<objective>
TODO 1 Wave B — parser strictness hardening. Convert three silent-tolerance regressions in the `.deal` annotation/verification parser into explicit accept-or-diagnostic, restoring the Phase 1.5 strictness contract:

- **D-01 (I1):** Stop silently discarding `;` (the canonical field terminator) via the `key_text.len == 0` path at `parser_deal.zig:1914`. Consume `;` explicitly; reject a `,` annotation-body separator with a hard diagnostic (new E0123) — NOT as an alias.
- **D-02 (I3):** Replace the silent-drop `else => { advance; continue; }` of unknown verification keys at `parser_deal.zig:1956-1959` with a hard diagnostic (new E0124).
- **D-03 (I2):** Keep the already-correct keyword-as-field-key accept at `parser_deal.zig:1908` (`isKeyword(key_tok.tag)`), but add an explicit test proving `from:` produces the correct AST node.

Per D-11: build these as the canonical "ideal forms" the deferred calc/constraint work will inherit.

Purpose: Phase 1.5 made the parser contract asserted, not merely intended; these three drift points re-opened silent tolerance. Closing them keeps the strictness invariant.
Output: two new `Codes` constants; patched annotation-body + verification-key loops; two new malformed fixtures with corpus pins; a strictness unit-test file.

DEPENDS ON Plan 01 (Step 0 gate): if `05.1-STEP-0-GATE.md` carries a `D-01 FLIP` banner, this plan must be re-planned before execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-CONTEXT.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-RESEARCH.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-PATTERNS.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-01-SUMMARY.md

<interfaces>
<!-- Verified 2026-06-09. -->
src/diagnostics.zig — Codes struct, E0100..E0299 parser-deal band:
  - last assigned: e_dangling_dot = "E0121" (:85), e_empty_arg_comma = "E0122" (:87).
  - E0123 and E0124 are FREE — add:
      pub const e_annotation_comma_separator = "E0123";  // `,` as annotation body separator (rejected)
      pub const e_unknown_verification_key   = "E0124";  // unrecognized key in verification block

src/parser_deal.zig anchors (verified):
  - :1908  `if (key_tok.tag == .ident or isKeyword(key_tok.tag)) {`  ← D-03 keyword-key accept (CORRECT, keep)
  - :1914  `if (key_text.len == 0) { _ = p.advance(); continue; }`   ← D-01 silent-skip path (REPLACE)
            ...trailing `if (p.peek().tag == .comma) _ = p.advance();` near :1923 ← REMOVE (`,` no longer tolerated)
  - :1956-1959  verification-key switch `else => { _ = p.advance(); continue; }` ← D-02 silent drop (REPLACE)
  - :1966  `if (p.peek().tag == .semicolon) _ = p.advance();` ← optional `;` in verification (keep behavior)

Diagnostic-emit-then-advance analog (parser_dealx.zig:848-858):
  try p.diags.append(p.arena, .{ .code = Codes.e_<name>, .severity = .err,
      .message = "<msg>", .span = key_tok.span });
  _ = p.advance();
  continue;

Malformed-corpus pin pattern (tests/unit/recovery_corpus.zig):
  - const m_pins = [_]Pin{ ... } (line 64); each entry `.{ .name = "<file>", .expected_code = Codes.<name> }`.
  - test loop (line 268) parses each fixture and asserts first diagnostic == expected_code.
  - existing fixtures live in tests/malformed/ (e.g. m18_dangling_dot.deal — two-line minimal form).

New fixture content (from PATTERNS.md):
  m21_annotation_comma_separator.deal — `@config { voltage: 12, current: 5 }` → E0123
  m22_unknown_verification_key.deal   — `verification { badkey: true; }`      → E0124
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add E0123/E0124 diagnostic constants + create malformed fixtures (Wave 0)</name>
  <read_first>
    - src/diagnostics.zig (Codes struct, E0121/E0122 additions at :84-88 — the analog)
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-PATTERNS.md (§diagnostics.zig — E0123/E0124 declaration; §Malformed corpus fixtures — m21/m22 content + pins)
    - tests/malformed/m18_dangling_dot.deal (existing minimal-fixture form)
    - tests/unit/recovery_corpus.zig (m_pins array :64, Pin struct, test loop :268)
  </read_first>
  <action>
    Add two constants to the `Codes` struct in `src/diagnostics.zig`, immediately after `e_empty_arg_comma` (E0122), in the same comment-then-const style: `e_annotation_comma_separator = "E0123"` and `e_unknown_verification_key = "E0124"`. Confirm E0123/E0124 are unused by scanning the E0100..E0299 band first. Create `tests/malformed/m21_annotation_comma_separator.deal` (the `@config { voltage: 12, current: 5 }` fixture) and `tests/malformed/m22_unknown_verification_key.deal` (the `verification { badkey: true; }` fixture) using the two-line minimal `package <name>;` + construct form from PATTERNS.md. Add the two corresponding `Pin` entries to `m_pins` in `tests/unit/recovery_corpus.zig` referencing `Codes.e_annotation_comma_separator` and `Codes.e_unknown_verification_key`. The constants MUST be declared before the pin array references them or Zig will not compile. The corpus test will fail (RED) until Task 2 emits the diagnostics — that is expected and intentional at this step.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && grep -v '^[[:space:]]*//' src/diagnostics.zig | grep -c 'e_unknown_verification_key' | grep -q 1 && test -f tests/malformed/m21_annotation_comma_separator.deal && test -f tests/malformed/m22_unknown_verification_key.deal && grep -q 'm21_annotation_comma_separator' tests/unit/recovery_corpus.zig && grep -q 'm22_unknown_verification_key' tests/unit/recovery_corpus.zig</automated>
  </verify>
  <acceptance_criteria>
    - `src/diagnostics.zig` declares `e_annotation_comma_separator = "E0123"` and `e_unknown_verification_key = "E0124"` (both in the E0100..E0299 band, not duplicating an existing code).
    - `tests/malformed/m21_annotation_comma_separator.deal` and `m22_unknown_verification_key.deal` exist with the PATTERNS.md content.
    - `tests/unit/recovery_corpus.zig` `m_pins` has entries pinning m21→E0123 and m22→E0124.
    - `zig build` compiles (constants resolve); the corpus test is RED until Task 2.
  </acceptance_criteria>
  <done>Diagnostic codes declared, fixtures created, pins added; project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Patch annotation-body (D-01) + verification-key (D-02) loops; add strictness tests (D-01/D-02/D-03)</name>
  <read_first>
    - src/parser_deal.zig (:1908 keyword-key accept; :1914-1923 silent-skip + trailing-comma path; :1956-1966 verification-key switch + optional-`;`)
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-PATTERNS.md (§parser_deal.zig — D-01/D-02 replacement patterns; §Shared Patterns — diagnostic emission)
    - src/parser_dealx.zig:848-858 (diagnostic-emit-then-advance analog)
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-RESEARCH.md (Q4 anchor confirmations; "Silent token skipping" anti-pattern)
    - spec/examples/showcase/packages/vehicle/behaviors.deal:29 (`from:` keyword field key — D-03 oracle)
    - tests/unit/recovery_corpus.zig (to confirm m21/m22 now go green)
  </read_first>
  <action>
    In `src/parser_deal.zig` annotation-body loop (around :1908-1923): (D-01) before the `key_text.len == 0` path, add an explicit `if (key_tok.tag == .semicolon) { _ = p.advance(); continue; }` so `;` is consumed as the canonical field terminator rather than swallowed by the unknown-token path. Replace the bare `key_text.len == 0` silent `advance; continue;` with a diagnostic-emit path: when the offending token is a `,`, emit `Codes.e_annotation_comma_separator` (E0123) on `key_tok.span`; otherwise emit `Codes.e_expected_token`/appropriate existing code, then advance. REMOVE the trailing `if (p.peek().tag == .comma) _ = p.advance();` near :1923 — `,` is no longer silently tolerated. Keep the keyword-key accept at :1908 unchanged (D-03 is already correct). (D-02) In the verification-key switch (:1956-1959), replace `else => { _ = p.advance(); continue; }` with the emit-then-advance form using `Codes.e_unknown_verification_key` (E0124) on `key_tok.span`, then advance/continue — modeled on parser_dealx.zig:848-858. Leave the optional-`;` consume at :1966 intact. Then create `tests/unit/parser_deal_strictness.zig` with explicit tests: (a) D-01 — `;`-terminated annotation body parses clean (no diagnostic); (b) D-01 — `,`-separated annotation body emits exactly E0123; (c) D-02 — `verification { badkey: ... }` emits exactly E0124; (d) D-03 — `from:` keyword field key parses and produces the expected annotation field AST node. Register the new test file in `build.zig` test aggregation if the project requires explicit registration (follow how recovery_corpus.zig is wired). Run the malformed corpus to confirm m21/m22 now fire their pinned codes and no pre-existing pin regressed.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && zig build test -Dtest-filter=parser_deal 2>&1 | grep -qiv 'error:' && zig build test -Dtest-filter=recovery 2>&1 | tail -3 | grep -qiv 'FAIL' && zig build test -Dtest-filter=strictness 2>&1 | tail -3 | grep -qiv 'FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - `;` in an annotation body is explicitly consumed (no silent-skip path) — D-01 test (a) passes with zero diagnostics.
    - `,` as an annotation-body separator emits exactly `E0123` — D-01 test (b) and corpus pin m21 both green.
    - An unknown verification key emits exactly `E0124` — D-02 test (c) and corpus pin m22 both green.
    - `from:` keyword field key produces the correct annotation-field AST node — D-03 test (d) green.
    - `zig build test -Dtest-filter=recovery` shows all 50+ pre-existing pins still fire (no new silent acceptance, no regressed pin).
    - The trailing `if (p.peek().tag == .comma) _ = p.advance();` at ~:1923 is removed: `grep -v '^[[:space:]]*//' src/parser_deal.zig | grep -c 'peek().tag == .comma) _ = p.advance()'` reflects the removal at the annotation-body site.
  </acceptance_criteria>
  <done>D-01/D-02 hard-error paths emit E0123/E0124; D-03 explicit accept tested; full corpus green; showcase still parses clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.deal` source (incl. adversarial) → annotation/verification parser loops | untrusted input drives the patched token-dispatch loops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-C01 | Denial of Service | new diagnostic path causing an infinite loop on adversarial input | mitigate | Every replacement arm advances the token cursor (`_ = p.advance();`) before `continue;`, matching the parser_dealx.zig:848-858 invariant — no arm can spin without consuming a token. The 50-file malformed corpus regression run exercises adversarial shapes and asserts termination (no panic/hang). |
| T-05.1-C02 | Tampering | silent acceptance re-introduced by an over-broad diagnostic arm | mitigate | The `,`-separator and unknown-key paths emit hard `.err` diagnostics; strictness unit tests assert EXACT codes (E0123/E0124), and the corpus pins lock first-diagnostic identity so a future silent-accept regression fails the gate. |
| T-05.1-C03 | Information Disclosure | diagnostic span pointing outside the source buffer (OOB read) | mitigate | Diagnostics use `key_tok.span`, a token span already produced by the lexer within the source buffer; no new span arithmetic is introduced. |
| T-05.1-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; N/A. RESEARCH.md confirms zero new dependencies. |
</threat_model>

<verification>
- `zig build test -Dtest-filter=parser_deal` and `-Dtest-filter=strictness` pass.
- `zig build test -Dtest-filter=recovery` — all 50+ malformed pins fire, m21→E0123, m22→E0124, no regression.
- Showcase corpus still parses clean (re-run Step 0 spot-check or the snapshot suite).
</verification>

<success_criteria>
- D-01: `;` explicitly consumed; `,` separator → E0123 hard error.
- D-02: unknown verification key → E0124 hard error.
- D-03: keyword field key (`from:`) explicitly accepted and tested.
- D-11: ideal forms built cleanly (single advance-per-arm, hard diagnostics) for downstream calc/constraint inheritance.
- No malformed-corpus regression; showcase parses unchanged.
</success_criteria>

<output>
Create `.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-03-SUMMARY.md` when done.
</output>
