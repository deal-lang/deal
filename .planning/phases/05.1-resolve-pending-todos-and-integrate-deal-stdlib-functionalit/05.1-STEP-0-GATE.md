# Step 0 Empirical Gate (D-10) — Showcase Parse Baseline

**Recorded:** 2026-06-09
**Phase:** 05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit
**Plan:** 01
**Gate status:** PASS — D-01 CONFIRMED STANDING

All non-analysis showcase files parse successfully (exit 0). Zero category-(c) annotation-body `,` separators found. D-01 stands — Waves B/C may proceed.

---

## Tool Invocation

```
target/release/deal parse <file>
```

Binary: `/Users/dunnock/projects/deal-lang/deal/target/release/deal` (built from current HEAD via `zig build`)
Build result: clean (no warnings, no errors)
Invocation: exit 0 = parse succeeded (AST JSON to stdout); exit 1 = parse failed (diagnostics to stderr)

---

## Showcase Corpus

**Total files enumerated:** 19 named DEAL/DEALX source files
(4 `.dealx` + 15 `.deal`)

**Excluded from corpus:**
- `spec/examples/showcase/packages/.deal` — this is a **directory** (not a source file), containing `.deal/index.json` (generated index). Excluded per Open Q3 resolution: it is not DEAL source. No parse attempted.
- No `analysis/*.deal` files exist in the current checkout (calc/constraint deferred TODO 3).

---

## Per-File Parse Results

| # | Path | Extension | Exit Code | Result | First Diagnostic |
|---|------|-----------|-----------|--------|-----------------|
| 1 | `spec/examples/showcase/model/traceability.dealx` | .dealx | 0 | **PASS** | none |
| 2 | `spec/examples/showcase/model/variants/performance.dealx` | .dealx | 0 | **PASS** | none |
| 3 | `spec/examples/showcase/model/variants/sedan.dealx` | .dealx | 0 | **PASS** | none |
| 4 | `spec/examples/showcase/model/vehicle.dealx` | .dealx | 0 | **PASS** | none |
| 5 | `spec/examples/showcase/packages/interfaces/connections.deal` | .deal | 0 | **PASS** | none |
| 6 | `spec/examples/showcase/packages/interfaces/electrical.deal` | .deal | 0 | **PASS** | none |
| 7 | `spec/examples/showcase/packages/interfaces/index.deal` | .deal | 0 | **PASS** | none |
| 8 | `spec/examples/showcase/packages/interfaces/thermal.deal` | .deal | 0 | **PASS** | none |
| 9 | `spec/examples/showcase/packages/requirements/index.deal` | .deal | 0 | **PASS** | none |
| 10 | `spec/examples/showcase/packages/requirements/needs.deal` | .deal | 0 | **PASS** | none |
| 11 | `spec/examples/showcase/packages/requirements/system.deal` | .deal | 0 | **PASS** | none |
| 12 | `spec/examples/showcase/packages/use-cases/charging.deal` | .deal | 0 | **PASS** | none |
| 13 | `spec/examples/showcase/packages/use-cases/driving.deal` | .deal | 0 | **PASS** | none |
| 14 | `spec/examples/showcase/packages/use-cases/index.deal` | .deal | 0 | **PASS** | none |
| 15 | `spec/examples/showcase/packages/vehicle/battery.deal` | .deal | 0 | **PASS** | none |
| 16 | `spec/examples/showcase/packages/vehicle/behaviors.deal` | .deal | 0 | **PASS** | none |
| 17 | `spec/examples/showcase/packages/vehicle/components.deal` | .deal | 0 | **PASS** | none |
| 18 | `spec/examples/showcase/packages/vehicle/index.deal` | .deal | 0 | **PASS** | none |
| 19 | `spec/examples/showcase/packages/vehicle/motor.deal` | .deal | 0 | **PASS** | none |

**Summary: 19 / 19 PASS. 0 failures.**

---

## `,` Classification Table (D-01 standing check)

Every `,` occurrence in the showcase corpus is classified below by category:

| Category | Description | Count | Example |
|----------|-------------|-------|---------|
| **(a) Import-list separator** | `,` separating items inside `import {...}` or `export {...}` braces | many | `import deal.std.units.{kg, kWh, V, A, ...}` |
| **(b) Inside opaque gap block** | `,` inside `gap { missing: { key: "val", key: "val" } }` byte-scan body | 2 | `traceability.dealx:133-134` (inside `missing: { ... }`) |
| **(b) Inside string literal / array element / comment** | `,` separating array items `[simulation, test]` inside verification block array literals, or inside string literals, or in doc comments | many | `system.deal:23: accepts: [simulation, test];` |
| **(c) Annotation-body field separator** | `,` used as a field terminator between annotation-body fields at the same level as `;` | **0** | NONE |

### Evidence for category-(b) gap-block commas (traceability.dealx:133-134)

```dealx
gap {
    description: "30-min target met at 25°C; untested below 0°C";
    missing: {
        chargeTime_neg10C: "Cold weather test scheduled 2026-Q3",   // line 133
        chargeTime_neg30C: "Cold weather test scheduled 2026-Q3"    // line 134
    };
    risk: "high";
    mitigation: "Test facility booked at ALEC, Kapuskasing ON";
}
```

The `,` at line 133 is inside the `missing: { ... }` sub-object within a `gap { ... }` block.
Per D-05/I5, the entire `gap { ... }` block body is parsed as an opaque brace-balanced byte-scan
(`parser_dealx.zig:1229-1252`). The `,` is therefore category-(b): inside an opaque gap block.
**Does NOT flip D-01.**

### Evidence for array-literal commas in system.deal

```deal
verification {
    accepts: [simulation, test];   // commas inside array literal [...]
    rejects: [analysis, inspection];
    threshold: minRange;
    operator: ">=";
}
```

The commas in `[simulation, test]` are inside array literals `[...]` within a verification block.
They are NOT annotation-body field separators — the field terminator is `;` after the closing `]`.
**Does NOT flip D-01.**

---

## D-01 Standing Confirmation

**D-01 STANDS.** The annotation-body separator is `;` (canonical). `,` is NOT used as an
annotation-body field separator anywhere in the showcase corpus. Zero category-(c) occurrences.

Wave B parser strictness hardening (D-01: `;` explicit consume, `,` rejection with diagnostic)
is validated as non-breaking against the current showcase corpus.

---

## Notes

- The `packages/.deal` directory (`spec/examples/showcase/packages/.deal/`) was confirmed to be
  a generated directory containing `index.json`, not a `.deal` source file. Correctly excluded.
- No `analysis/*.deal` files exist in the current checkout (calc/constraint deferred per D-07/TODO 3).
  If they existed and failed, they would be documented as expected failures; they do not exist.
- Parser binary (`target/release/deal`) was built clean from HEAD before any Phase 05.1 source
  changes — this is the mandatory D-10 pre-change baseline.
