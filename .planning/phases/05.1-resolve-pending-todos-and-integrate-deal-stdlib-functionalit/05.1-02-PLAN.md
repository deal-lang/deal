---
phase: 05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/tests/e2500_cli.rs
autonomous: true
requirements: [D-08, D-09]
must_haves:
  truths:
    - "deal check exits non-zero and prints E2501 for a mixed-unit same-dimension scenario"
    - "deal check exits non-zero and prints E2502 for an unknown-unit literal"
    - "deal check exits non-zero and prints E2503 for a conversion type mismatch (distinct fixture from the regression pin)"
    - "All four cross-file E25xx checks (E2500–E2503) have CLI integration coverage, exercising the already-complete analyzeWithExternalTable wiring end-to-end (D-08)"
  artifacts:
    - path: "cli/tests/e2500_cli.rs"
      provides: "E2501/E2502/E2503 CLI integration tests via the stdlib-seed tempdir pattern"
      contains: "E2503"
  key_links:
    - from: "cli/tests/e2500_cli.rs"
      to: "deal check --json CLI path (analyzeWithExternalTable)"
      via: "Command::new(deal_bin()).args([\"check\",\"--json\"])"
      pattern: "check.*--json"
    - from: "cli/tests/e2500_cli.rs MINIMAL_STDLIB seed"
      to: ".deal/deps/deal-stdlib/packages/units/units.deal"
      via: "write_stdlib_seed tempdir"
      pattern: "write_stdlib_seed"
---

<objective>
Close the D-09 acceptance-breadth gap for TODO 2. RESEARCH.md Q2 confirms the CLI wiring is ALREADY COMPLETE (Phase 5 D-88 routed `deal check` through `deal_check_with_stdlib` + `analyzeWithExternalTable` at `cli/src/main.rs:350-388`, with two E2500 CLI tests already green). The ONLY remaining TODO-2 work is adding CLI integration tests for the other three cross-file dimensional checks: E2501 (mixed-unit same-dimension), E2502 (unknown unit), E2503 (conversion type mismatch).

Purpose: D-09 requires CLI-level coverage for ALL FOUR E25xx checks, not just E2500. The dimensional algebra is already proven in `tests/unit/sema_dimensional.zig`; these tests validate the CLI routing layer only.
Output: three new `#[test]` functions in `cli/tests/e2500_cli.rs` plus a `g` (grams) unit added to the `MINIMAL_STDLIB` seed for E2501.

NOTE — do NOT plan any new Zig sema logic or any new CLI wiring. RESEARCH.md Q2 verified both are complete. This is test-breadth only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-CONTEXT.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-RESEARCH.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-PATTERNS.md
@.planning/phases/05-simulation-integration/05-LEARNINGS.md

<interfaces>
<!-- Existing e2500_cli.rs scaffold. Verified 2026-06-09. -->
cli/tests/e2500_cli.rs:
  - const MINIMAL_STDLIB: &str (line 35) — package deal.std.units; Mass + Voltage dimension defs
    (7 si_* fields each) + kg (specializes Mass, si_factor=1.0) + V (specializes Voltage, si_factor=1.0).
  - fn write_stdlib_seed(tempdir) (line 87) — writes MINIMAL_STDLIB to
    <tempdir>/.deal/deps/deal-stdlib/packages/units/units.deal. Reuse unchanged.
  - fn deal_bin() — resolves the built deal binary path (existing helper).
  - fn test_e2500_voltage_assigned_to_mass_attribute (line 111) — the analog: tempdir,
    write_stdlib_seed, write src/<fixture>.deal, Command check --json with current_dir(tempdir),
    assert_ne!(exit_code, 0) + assert!(stdout.contains("E2500")).
  - fn test_e2500_correct_mass_unit_no_error (line 175) — clean-path analog (exit 0).

Run command: cargo test -p deal --test e2500_cli

FFI lifetime landmine (Phase 5 — 05-LEARNINGS.md Clone-Before-Free):
  - deal_check_with_stdlib SHARES (does not copy) entry pointers from stdlib_arena.
  - These CLI tests go through the existing production path (cli/src/main.rs:427-433 already
    guards against deal_index_json on stdlib-check handles) and are safe AS LONG AS they add
    NO new handle consumers. Do NOT call deal_index_json. Use only `deal check --json`.

E2503 regression-pin hazard (RESEARCH Pitfall 4):
  - tests/unit/sema_dimensional.zig E2503 pin uses showcase 07-conversion-mismatch.deal (a
    Phase-4 carryover failing on bare HEAD via sema.dimensional.regression_pins).
  - The E2503 CLI test MUST use a fresh minimal fixture in the tempdir pattern — NOT the
    regression-pin file — so the two layers stay independent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MINIMAL_STDLIB + add E2501/E2502/E2503 CLI tests (D-09)</name>
  <read_first>
    - cli/tests/e2500_cli.rs (full file — MINIMAL_STDLIB:35, write_stdlib_seed:87, test_e2500_voltage_assigned_to_mass_attribute:111)
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-PATTERNS.md (per-test scaffold + E2501/E2502/E2503 fixture sources + MINIMAL_STDLIB `g` extension)
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-RESEARCH.md (Q2 — wiring already complete; Pitfall 2 SIGSEGV; Pitfall 4 regression-pin distinctness)
    - tests/unit/sema_dimensional.zig (the algebra oracle — confirms which scenario triggers each E25xx; do NOT regress)
    - .planning/phases/05-simulation-integration/05-LEARNINGS.md (Clone-Before-Free FFI pattern)
  </read_first>
  <behavior>
    - test_e2501_mixed_unit_same_dimension: src uses `kg(1) + g(500)` → `deal check --json` exits non-zero, stdout contains "E2501".
    - test_e2502_unknown_unit_literal: src uses an undeclared unit (e.g. `lb(500)`) → exits non-zero, stdout contains "E2502".
    - test_e2503_conversion_type_mismatch: src assigns a Mass unit to a Voltage attribute (e.g. `attribute voltage : Voltage = kg(800);`) → exits non-zero, stdout contains "E2503". Fixture must be DISTINCT from showcase 07-conversion-mismatch.deal.
    - Pre-existing test_e2500_* tests remain green (no regression).
  </behavior>
  <action>
    Extend `MINIMAL_STDLIB` with a second mass unit `g` (`<<specializes>> Mass`, `si_factor = 0.001`) so the E2501 mixed-unit scenario has two distinct mass units to combine (per RESEARCH Open Q2 + PATTERNS seed extension). Add three `#[test]` functions following the exact `test_e2500_voltage_assigned_to_mass_attribute` structure (tempdir → `write_stdlib_seed` → write `src/<fixture>.deal` → `Command::new(deal_bin()).args(["check","--json"]).arg(&source_path).current_dir(tempdir.path())` → assert exit non-zero + stdout contains the code). Use the three fixture sources from PATTERNS.md §`cli/tests/e2500_cli.rs`. Each fixture is a fresh minimal file in the tempdir — for E2503 specifically, do NOT reuse `07-conversion-mismatch.deal` (Pitfall 4). Do NOT call `deal_index_json` anywhere (Pitfall 2 SIGSEGV); these tests use only the `deal check --json` production path. Verify the assertion substrings match the exact diagnostic code strings the sema layer emits by cross-checking `tests/unit/sema_dimensional.zig`.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && cargo test -p deal --test e2500_cli 2>&1 | grep -qE 'test result: ok' && cargo test -p deal --test e2500_cli e2501 2>&1 | grep -q 'e2501' && cargo test -p deal --test e2500_cli e2502 2>&1 | grep -q 'e2502' && cargo test -p deal --test e2500_cli e2503 2>&1 | grep -q 'e2503'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p deal --test e2500_cli` exits 0 with all tests passing (the 2 pre-existing E2500 + 3 new).
    - `test_e2501_*` asserts exit code != 0 AND stdout contains "E2501".
    - `test_e2502_*` asserts exit code != 0 AND stdout contains "E2502".
    - `test_e2503_*` asserts exit code != 0 AND stdout contains "E2503", using a fixture file whose name is NOT `07-conversion-mismatch.deal`.
    - `MINIMAL_STDLIB` contains a `g` unit specializing `Mass` with `si_factor = 0.001`.
    - No `deal_index_json` call appears in the new test code: `grep -c 'deal_index_json' cli/tests/e2500_cli.rs` returns 0.
  </acceptance_criteria>
  <done>Three E25xx CLI tests added and green; MINIMAL_STDLIB extended; pre-existing E2500 tests still pass; no SIGSEGV.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI test fixture (`.deal` source) → deal check parser/sema | adversarial-shaped DEAL input crosses into the parser |
| Rust CLI → Zig C ABI (deal_check_with_stdlib) | FFI boundary; arena-owned pointers shared not copied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-B01 | Denial of Service | use-after-free on stdlib_arena handle (Phase 5 SIGSEGV) | mitigate | Tests use only the `deal check --json` production path; the existing guard at cli/src/main.rs:427-433 skips `deal_index_json` on stdlib-check handles. New tests add NO new handle consumers. Grep gate confirms no `deal_index_json` in the test file. |
| T-05.1-B02 | Denial of Service | malformed `.deal` fixture causing unbounded loop/OOB in parser | accept | Fixtures are minimal, well-formed-but-dimension-wrong sources exercising the sema layer, not the parser recovery path; parser robustness is covered by the 50-file malformed corpus (separate plan). Low risk. |
| T-05.1-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; `tempfile` is already a dev-dependency. RESEARCH.md confirms zero new dependencies. |
</threat_model>

<verification>
- `cargo test -p deal --test e2500_cli` exits 0 (5 tests: 2 existing E2500 + E2501/E2502/E2503).
- Each new test asserts non-zero exit + the matching E25xx substring.
- E2503 fixture is distinct from the regression-pin file.
- No `deal_index_json` consumer added (grep gate = 0).
</verification>

<success_criteria>
- D-09 satisfied: all four cross-file E25xx checks (E2500–E2503) have CLI integration coverage.
- D-08 confirmed wired (no new wiring needed — tests prove the existing path).
- No regression to the two pre-existing E2500 tests; no FFI use-after-free.
</success_criteria>

<output>
Create `.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-02-SUMMARY.md` when done.
</output>
