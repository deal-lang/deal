---
phase: 05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit
verified: 2026-06-09T21:00:00Z
status: passed
score: 11/11 must-haves verified
overrides_applied: 0
---

# Phase 05.1: Resolve Pending TODOs and Integrate deal-stdlib Functionality — Verification Report

**Phase Goal:** Resolve two carried-over compiler/stdlib todos before Phase 6 — (TODO 2) close the deal-stdlib dimensional integration gap by adding CLI integration coverage for all four cross-file E25xx checks (E2500–E2503); and (TODO 1) reconcile the EBNF↔parser drift and convert silent-tolerance regressions into explicit accept-or-diagnostic (`;` canonical / `,` rejected, unknown verification key → hard error, keyword field key explicit accept, real `array_literal` AST node), restoring the Phase 1.5 strictness contract with the showcase corpus as oracle.

**Verified:** 2026-06-09T21:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                  | Status     | Evidence                                                                                                      |
|----|--------------------------------------------------------------------------------------------------------|------------|---------------------------------------------------------------------------------------------------------------|
| 1  | D-01: `;` canonical / `,` rejected (E0123 emitted on comma separator)                                 | VERIFIED   | `src/parser_deal.zig:1922-1925` emits `Codes.e_annotation_comma_separator`; corpus pin m21→E0123 green        |
| 2  | D-02: Unknown verification key → hard error (E0124 emit-then-advance)                                  | VERIFIED   | `src/parser_deal.zig:1980` emits `Codes.e_unknown_verification_key`; corpus pin m22→E0124 green               |
| 3  | D-03: Keyword-as-field-key explicit accept (tested)                                                    | VERIFIED   | `parser_deal.zig:1908` `isKeyword` accept path present; `tests/unit/parser_deal_strictness.zig` test (d) green |
| 4  | D-04: Real `array_literal` AST NodeKind; both duplicate parsers deleted                               | VERIFIED   | `src/ast.zig:138` NodeKind declared; `fn parseVerificationArray` and `fn parseArrayLiteral` absent from both parser files; `__array__` appears only in comments |
| 5  | D-05: Honest-opaque GapBlock EBNF in spec (`dealx.ebnf`)                                              | VERIFIED   | `spec/grammar/dealx.ebnf` GapBlock → `"gap" _OpaqueBody`; `_OpaqueBody` defined; structured gap fields absent |
| 6  | D-06: Spec pushed (74dfea5) + parent gitlink bumped                                                   | VERIFIED   | `git submodule status` → `74dfea56f13e67ca7d21bfc71b8d12ed68f3c4a9 spec (heads/main)`; spec HEAD is 74dfea5   |
| 7  | D-07: Calc/constraint commit 7ce59bc NOT merged onto gitlink                                           | VERIFIED   | `git merge-base --is-ancestor 7ce59bc 74dfea5` → non-zero exit (not an ancestor); 74dfea5 is descendant of dd3bb78 only |
| 8  | D-08/D-09: E2500–E2503 CLI integration coverage in `cli/tests/e2500_cli.rs`                           | VERIFIED   | `cargo test -p deal --test e2500_cli` → 5 passed (E2500 ×2, E2501, E2502, E2503); all use `deal check --json` production path |
| 9  | D-10: Step 0 empirical gate recorded (showcase corpus parse baseline)                                 | VERIFIED   | `05.1-STEP-0-GATE.md` exists; 19/19 showcase files PASS exit 0; zero category-(c) annotation commas; no D-01 FLIP banner |
| 10 | D-11: Ideal forms for downstream calc/constraint (`array_literal` single source of truth)              | VERIFIED   | `l_bracket` arm in `src/expr.zig:278` is the sole array entry point; plan frontmatter records D-11 satisfied  |
| 11 | Showcase corpus still parses clean after all strictness changes                                        | VERIFIED   | `zig build test -Dtest-filter=parser_deal` exits 0; per-plan verification confirms showcase parse passes       |

**Score:** 11/11 truths verified

---

### Required Artifacts

| Artifact                                                                                             | Expected                                                            | Status     | Details                                                                              |
|------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------|------------|--------------------------------------------------------------------------------------|
| `src/ast.zig`                                                                                        | `array_literal` NodeKind + `ArrayLiteral` struct + Payload arm      | VERIFIED   | Lines 135-138 NodeKind; struct at ~564; union arm present                            |
| `src/diagnostics.zig`                                                                                | E0123 + E0124 Codes constants                                       | VERIFIED   | Lines 88-91: `e_annotation_comma_separator = "E0123"`, `e_unknown_verification_key = "E0124"` |
| `src/expr.zig`                                                                                       | `l_bracket` arm in `parsePrimary` producing `array_literal` node    | VERIFIED   | Line 278: `.l_bracket =>` arm; makes `.array_literal` node at line 292               |
| `src/fmt.zig`                                                                                        | `writeArrayLiteral` + `.array_literal` dispatch                     | VERIFIED   | Lines 169 dispatch arm; line 765-767 `fn writeArrayLiteral`                          |
| `src/json.zig`                                                                                       | `.array_literal` serialization arm                                  | VERIFIED   | Line 447: `.array_literal => |p| ...`; line 749: `nodekindName` arm                 |
| `src/parser_deal.zig`                                                                                | E0123 emit on `,`; E0124 emit on unknown verification key           | VERIFIED   | Lines 1922-1925 (E0123); line 1980 (E0124); `fn parseVerificationArray` deleted      |
| `src/parser_dealx.zig`                                                                               | `fn parseArrayLiteral` deleted; `[` delegates to `parseExpression`  | VERIFIED   | No match for `fn parseArrayLiteral`; callers delegate to expr                        |
| `spec/grammar/dealx.ebnf`                                                                            | Honest-opaque GapBlock with `_OpaqueBody` (D-05)                    | VERIFIED   | `GapBlock ::= "gap" _OpaqueBody`; `_OpaqueBody` production defined                   |
| `spec/grammar/deal.ebnf`                                                                             | `AnnotationKey ::= IDENT \| _Keyword`; `;` canonical (D-03/D-01)   | VERIFIED   | Line 1391 `AnnotationKey ":" Expression ";"?`; line 1410-1411 `AnnotationKey ::= IDENT \| _Keyword` |
| `spec/grammar/DESIGN-DECISIONS.md`                                                                   | SD-15a + SD-15b locked decisions                                    | VERIFIED   | SD-15a (line 455): `;` canonical / `,` rejected; SD-15b (line 472): keyword keys     |
| `cli/tests/e2500_cli.rs`                                                                             | E2501/E2502/E2503 test functions + `g`/`to_g` in MINIMAL_STDLIB     | VERIFIED   | 3 new `#[test]` functions; MINIMAL_STDLIB at lines 69-77 has `g` (0.001) and `to_g` |
| `tests/malformed/m21_annotation_comma_separator.deal`                                                | D-01 `,`-rejection fixture                                          | VERIFIED   | File exists; `@trace:<<satisfies>> R { voltage: 12, current: 5 }` triggers E0123    |
| `tests/malformed/m22_unknown_verification_key.deal`                                                  | D-02 unknown-verification-key fixture                               | VERIFIED   | File exists; `verification { badkey: true; }` triggers E0124                         |
| `tests/unit/parser_deal_strictness.zig`                                                              | 5 explicit accept/reject tests for D-01/D-02/D-03                  | VERIFIED   | File exists with tests (a)–(d) for D-01/D-02/D-03; all 5 tests green                 |
| `.planning/phases/05.1-.../05.1-STEP-0-GATE.md`                                                     | D-10 showcase parse baseline                                        | VERIFIED   | Exists; 19 files PASS; zero category-(c) commas; no FLIP banner                      |
| `tests/snapshots/ast/showcase__packages__requirements__system.deal.json`                             | `array_literal` nodes, no `__array__`                               | VERIFIED   | Snapshot uses `"k":"array_literal"` throughout; `__array__` absent                   |

---

### Key Link Verification

| From                                        | To                                        | Via                                            | Status   | Details                                                                    |
|---------------------------------------------|-------------------------------------------|------------------------------------------------|----------|----------------------------------------------------------------------------|
| `src/parser_deal.zig` annotation-body loop  | `Codes.e_annotation_comma_separator`      | `p.diags.append` on `,` at line 1922-1925      | WIRED    | Emit path present; malformed corpus pin m21 confirms it fires              |
| `src/parser_deal.zig` verification-key else | `Codes.e_unknown_verification_key`        | `p.diags.append` at line 1980                  | WIRED    | Emit path present; corpus pin m22 confirms it fires                        |
| `src/expr.zig parsePrimary`                 | `ast.array_literal` node                  | `l_bracket` arm → `makeNode(.array_literal)`   | WIRED    | Line 278-294 confirmed; both former parser call sites deleted and delegated |
| `cli/tests/e2500_cli.rs`                    | `deal check --json` (analyzeWithExternalTable) | `Command::new(deal_bin()).args(["check","--json"])` | WIRED | All 5 tests use this path; `grep -c 'deal_index_json'` = 0 (no use-after-free) |
| `spec` submodule HEAD                       | Parent `deal` gitlink                     | `git add spec + commit`                        | WIRED    | Gitlink at 74dfea5; spec at 74dfea5; wave-A commit on top of dd3bb78 only  |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase modifies compiler source (parser, AST, diagnostics), CLI integration tests, and spec EBNF. No dynamic-data UI components. Behavioral spot-checks cover the data-flow concern for CLI tests.

---

### Behavioral Spot-Checks

| Behavior                                     | Command                                              | Result                            | Status  |
|----------------------------------------------|------------------------------------------------------|-----------------------------------|---------|
| All 5 E25xx CLI integration tests pass        | `cargo test -p deal --test e2500_cli`                | 5 passed, 0 failed, 0.61s         | PASS    |
| Full Zig test suite (1 known carryover fail)  | `zig build test`                                     | 65 pass, 1 fail (E2503 stdlib sibling absent — documented Phase-4 carryover) | PASS (known) |
| Spec submodule at 74dfea5, descendant of dd3bb78 | `git submodule status` + `git merge-base` checks  | 74dfea5 confirmed; 7ce59bc NOT ancestor | PASS  |

---

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes declared for this phase. Fresh-worktree gate (`scripts/verify-fresh-worktree.sh`) was executed as a human-verify checkpoint in Plan 01 (65/66 tests; 1 failure is the documented E2503 carryover). Step 7c: SKIPPED (no conventional phase probes; human-verify gate documented in Plan 01 checkpoint).

---

### Requirements Coverage

Phase 05.1 has no formal REQUIREMENTS.md IDs — scope is anchored to CONTEXT decisions D-01..D-11. All 11 decisions verified above.

---

### Anti-Patterns Found

| File                            | Line  | Pattern                                     | Severity | Impact                                             |
|---------------------------------|-------|---------------------------------------------|----------|----------------------------------------------------|
| `cli/tests/e2500_cli.rs`        | 367-369 | Stale doc comment: describes E2500 fixture, test uses E2503 fixture | WARNING  | Misleading to developers; test itself passes correctly (see REVIEW.md WR-02) |
| `src/expr.zig`                  | 288   | Trailing comma in array literal silently accepted; inconsistent with `parseArgList` E0122 | WARNING | Round-trip invariant inconsistency; `[x,]` parsed without diagnostic (REVIEW.md WR-03) |
| `src/expr.zig`                  | 278-294 | Unclosed array emits E0100 instead of dedicated E0402 | WARNING | Diagnostic UX degradation on malformed arrays (REVIEW.md WR-01) |

No `TBD`, `FIXME`, or `XXX` markers found in any file modified by this phase. All three warnings were identified by the code review (05.1-REVIEW.md); none are BLOCKERS against phase goal achievement. WR-01/WR-03 are behavioral edge-case gaps (not correctness regressions); WR-02 is a doc comment error. The `__array__` string appears only in comments (not code), confirming it is dead encoding.

---

### Human Verification Required

None — all must-haves are verifiable programmatically. The Plan 01 checkpoint (fresh-worktree gate) was approved during execution and recorded in STATE.md. No new human verification items identified.

---

### Gaps Summary

No gaps. All 11 decisions (D-01 through D-11) are verified against the actual codebase:

- D-01/D-02/D-03: Hard diagnostics emitted (E0123/E0124), malformed corpus pins green, strictness tests green.
- D-04: Real `array_literal` NodeKind exists end-to-end (ast/json/expr/fmt); both duplicate parsers deleted; snapshots byte-stable.
- D-05: Honest-opaque `GapBlock → _OpaqueBody` in `dealx.ebnf`.
- D-06/D-07: Spec at 74dfea5 (descendant of dd3bb78; 7ce59bc excluded); gitlink bumped.
- D-08/D-09: 5/5 CLI tests pass (`cargo test -p deal --test e2500_cli`).
- D-10: `05.1-STEP-0-GATE.md` records 19/19 PASS, zero category-(c) commas.
- D-11: Clean `array_literal` ideal form in `expr.zig` ready for calc/constraint inheritance.

The single known failing test (`sema.dimensional.regression_pins`) is the documented Phase-4 carryover (stdlib sibling absent in bare workspace) — explicitly out of scope for Phase 5.1 per the phase instructions and `v2.1.0-MILESTONE-AUDIT.md` line 72.

Three code-review warnings (WR-01, WR-02, WR-03 from `05.1-REVIEW.md`) are noted but none block the phase goal. They are candidates for Phase 5.x or Phase 6 follow-up.

---

_Verified: 2026-06-09T21:00:00Z_
_Verifier: Claude (gsd-verifier)_
