---
phase: 05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit
plan: "04"
subsystem: compiler-ast
tags: [zig, ast, parser, fmt, array_literal, D-04, D-11]

requires:
  - phase: 05.1-03
    provides: parser_deal.zig verification strictness (D-01/D-02/D-03) — parallel edits to same file sequenced before this plan

provides:
  - "Real array_literal AST NodeKind replacing synthetic call(__array__, items) encoding"
  - "Single l_bracket atom arm in expr.zig as sole source of truth for [expr, expr, ...]"
  - "Both duplicate array parsers deleted (parser_deal.parseVerificationArray, parser_dealx.parseArrayLiteral)"
  - "fmt.zig writeArrayLiteral replacing __array__ name-check special-case"
  - "AST snapshots regenerated: vehicle.dealx.json + system.deal.json now emit array_literal"

affects:
  - "Phase 6 calc/constraint work — inherits clean array_literal form (D-11)"
  - "Any future phase touching ast.zig NodeKind or exhaustive switches"

tech-stack:
  added: []
  patterns:
    - "Three-part atomic NodeKind addition: enum + payload struct + Payload union arm must all be in NodeKind order"
    - "Exhaustive switch coverage: adding a NodeKind requires updating all switch(node.payload) sites — tests, json nodekindName, fmt writeNode"
    - "Snapshot gate sequence: regenerate with -Dupdate-snapshots=true then confirm byte-stability with plain zig build test"

key-files:
  created: []
  modified:
    - src/ast.zig
    - src/json.zig
    - src/expr.zig
    - src/parser_deal.zig
    - src/parser_dealx.zig
    - src/fmt.zig
    - tests/snapshots/ast/showcase__model__vehicle.dealx.json
    - tests/snapshots/ast/showcase__packages__requirements__system.deal.json
    - tests/unit/parser_deal_coverage.zig
    - tests/unit/parser_dealx_coverage.zig
    - tests/unit/property_span_containment.zig

key-decisions:
  - "D-04 completed: array_literal is the single source of truth; call(__array__,items) fully removed from AST output"
  - "D-11 satisfied: clean ideal array_literal form built for downstream calc/constraint inheritance"
  - "Payload union field order must match NodeKind enum field order in Zig 0.16.0 — array_literal arm placed after interpolation"
  - "Three test files with exhaustive switch(node.payload) required .array_literal arms (Rule 3 auto-fix, not ir.zig/lowering.zig)"
  - "ir.zig and lowering.zig have no exhaustive switch over ast.NodeKind — RESEARCH Q1 verified correct; no stub arms needed"

requirements-completed: [D-04, D-11]

duration: 20min
completed: 2026-06-09
---

# Phase 05.1 Plan 04: Array Unification Summary

**Real `array_literal` AST NodeKind replacing synthetic `call(__array__, items)` encoding — both duplicate parsers deleted, single l_bracket atom in expr.zig, fmt round-trips `[expr, expr]` correctly, snapshots regenerated byte-stable**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-06-09T18:50:00Z
- **Completed:** 2026-06-09T19:09:59Z
- **Tasks:** 3
- **Files modified:** 11

## Accomplishments

- Added `array_literal` NodeKind + `ArrayLiteral{ items: []*Node }` struct + Payload union arm atomically to `src/ast.zig`
- Added `.array_literal` serialization arm in `src/json.zig` emitting `{"k":"array_literal","items":[...]}`
- Added `l_bracket` atom arm to `src/expr.zig:parsePrimary` — single source of truth with EOF/r_bracket DoS guard (T-05.1-D01)
- Deleted `parser_deal.parseVerificationArray` + `parseVerificationArrayItem` (both `.deal` verification arrays now route through expr)
- Deleted `parser_dealx.parseArrayLiteral` (`.dealx` attribute arrays now route through expr)
- Removed `__array__` name-check special-case from `fmt.zig:writeCall`; added `writeArrayLiteral` method + dispatch arm
- Regenerated two affected AST snapshots; confirmed byte-stable on clean re-run; SysML/reqif goldens unchanged

## Task Commits

1. **Task 1: Define array_literal AST contract (ast.zig + json.zig)** - `08f4e89` (feat)
2. **Task 2: Wire l_bracket atom + delete both duplicate parsers (D-04)** - `14a56d6` (feat)
3. **Task 3: Thread array_literal through fmt/json/tests; regenerate snapshots** - `5339d21` (feat)

## Files Created/Modified

- `/Users/dunnock/projects/deal-lang/deal/src/ast.zig` — NodeKind `array_literal`, `ArrayLiteral` struct, Payload union arm (after interpolation)
- `/Users/dunnock/projects/deal-lang/deal/src/json.zig` — `.array_literal` arm in `writeNode` switch + `nodekindName` switch
- `/Users/dunnock/projects/deal-lang/deal/src/expr.zig` — `l_bracket` atom arm in `parsePrimary`
- `/Users/dunnock/projects/deal-lang/deal/src/parser_deal.zig` — `parseVerificationValue` simplified; `parseVerificationArray` + `parseVerificationArrayItem` deleted
- `/Users/dunnock/projects/deal-lang/deal/src/parser_dealx.zig` — `parseArrayLiteral` deleted; caller branch replaced with `expr.parseExpression` delegation; doc comment updated
- `/Users/dunnock/projects/deal-lang/deal/src/fmt.zig` — `writeCall` `__array__` special-case removed; `writeArrayLiteral` added; `.array_literal` dispatch arm added to `writeNode`
- `/Users/dunnock/projects/deal-lang/deal/tests/snapshots/ast/showcase__model__vehicle.dealx.json` — regenerated: `array_literal` replaces `call+__array__`
- `/Users/dunnock/projects/deal-lang/deal/tests/snapshots/ast/showcase__packages__requirements__system.deal.json` — regenerated: `array_literal` replaces `call+__array__`
- `/Users/dunnock/projects/deal-lang/deal/tests/unit/parser_deal_coverage.zig` — `.array_literal` arm added to visit switch
- `/Users/dunnock/projects/deal-lang/deal/tests/unit/parser_dealx_coverage.zig` — `.array_literal` arm added to visit switch
- `/Users/dunnock/projects/deal-lang/deal/tests/unit/property_span_containment.zig` — `.array_literal` arm added to visit switch

## Decisions Made

- **Payload union field order:** Zig 0.16.0 requires union field order to match the tag enum field order exactly. `array_literal` was initially appended at the end of the union but needed to be placed after `interpolation` (its position in the NodeKind enum). Corrected in Task 3.
- **ir.zig / lowering.zig:** RESEARCH Q1 was accurate — neither file has exhaustive `switch (node.payload)` over `ast.NodeKind`. No stub arms were needed. The compile errors surfaced in three test files instead (parser_deal_coverage, parser_dealx_coverage, property_span_containment).
- **nodekindName switch in json.zig:** This separate name-mapping switch also needed `array_literal` — discovered via compile error, fixed inline with Task 3.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Payload union field order must match NodeKind enum order**
- **Found during:** Task 3 (first build attempt)
- **Issue:** `array_literal` union arm was appended at the end (index 72) but its NodeKind enum position is index 62 (after `interpolation`). Zig 0.16.0 exhaustive union enforcement rejects mismatched order.
- **Fix:** Moved `array_literal: ArrayLiteral` arm from the end of the union to immediately after `interpolation: InterpolationNode`.
- **Files modified:** `src/ast.zig`
- **Verification:** `zig build` clean after correction
- **Committed in:** `5339d21` (Task 3 commit)

**2. [Rule 3 - Blocking] nodekindName switch in json.zig missing array_literal**
- **Found during:** Task 3 (second build attempt)
- **Issue:** `json.zig` has a separate `nodekindName()` function with an exhaustive switch over `ast.NodeKind` that maps each kind to its string name. Not mentioned in the plan's interface spec.
- **Fix:** Added `.array_literal => "array_literal"` after `.interpolation` in `nodekindName`.
- **Files modified:** `src/json.zig`
- **Verification:** `zig build` clean after correction
- **Committed in:** `5339d21` (Task 3 commit)

**3. [Rule 3 - Blocking] Three test files with exhaustive switch(node.payload) needed .array_literal arms**
- **Found during:** Task 3 (snapshot regeneration compile errors)
- **Issue:** `tests/unit/parser_deal_coverage.zig`, `tests/unit/parser_dealx_coverage.zig`, and `tests/unit/property_span_containment.zig` all have exhaustive `switch (node.payload)` dispatches that require new arms for every new NodeKind.
- **Fix:** Added `.array_literal => |p| for (p.items) |item| try visit(item, seen)` (or span-passing variant) to each file. Correctly recurses into items rather than being a no-op.
- **Files modified:** all three test files
- **Verification:** `zig build test` compiles and 65/66 tests pass (1 pre-existing E2503 carryover)
- **Committed in:** `5339d21` (Task 3 commit)

---

**Total deviations:** 3 auto-fixed (all Rule 3 — blocking compile errors from exhaustive switch enforcement)
**Impact on plan:** All three were necessary for a compiling build. No scope creep.

## Issues Encountered

- The plan stated "Let the compile error surface the exact sites" for ir.zig/lowering.zig — those files had no errors (RESEARCH Q1 correct). The exhaustive switch errors surfaced in test files instead. Fixed inline per Rule 3.
- Pre-existing `sema.dimensional.regression_pins` failure (E2503, stdlib sibling absent) unaffected — documented Phase-4 carryover per STATE.md.

## Known Stubs

None — array_literal is fully wired end-to-end: parsed, serialized, formatted, and snapshot-verified.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries introduced.

## Next Phase Readiness

- D-04 complete: `array_literal` is the single source of truth; downstream calc/constraint work (Phase 6) inherits the clean form as required by D-11.
- Phase 05.1 Wave 3 (this plan) is the final wave — phase 05.1 is now complete.
- All 4 plans in phase 05.1 done: P01 (Wave A spec), P02 (E2501/E2502/E2503 CLI tests), P03 (parser strictness D-01/D-02/D-03), P04 (array unification D-04/D-11).

---
*Phase: 05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit*
*Completed: 2026-06-09*

## Self-Check: PASSED

- `src/ast.zig` exists and contains `array_literal`: FOUND
- `src/json.zig` exists and contains `array_literal`: FOUND
- `src/expr.zig` exists and contains `l_bracket`: FOUND
- `src/fmt.zig` exists and contains `writeArrayLiteral`: FOUND
- `tests/snapshots/ast/showcase__packages__requirements__system.deal.json` contains `array_literal`: FOUND
- Commits 08f4e89, 14a56d6, 5339d21: all present in git log
