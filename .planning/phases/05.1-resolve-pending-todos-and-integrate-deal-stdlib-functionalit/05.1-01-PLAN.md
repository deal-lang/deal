---
phase: 05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/grammar/deal.ebnf
  - spec/grammar/dealx.ebnf
  - spec/grammar/lexical.ebnf
  - spec/grammar/DESIGN-DECISIONS.md
  - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-STEP-0-GATE.md
autonomous: false            # Task 3 is a blocking human-verify checkpoint (fresh-worktree gate)
requirements: [D-10, D-05, D-06, D-07]
must_haves:
  truths:
    - "deal parse succeeds on every non-analysis showcase file (Step 0 baseline recorded)"
    - "No showcase file uses `,` as an annotation-body field separator (D-01 stands)"
    - "spec/grammar GapBlock production matches the parser's opaque brace-balanced byte-scan"
    - "spec EBNF citations + AnnotationKey + optional-`;` reflect parser reality (I6/I2/I3 spec half)"
    - "Wave-A spec edits are pushed to the spec origin and the parent gitlink is bumped"
    - "Wave-A branches from pin dd3bb78 only — the calc/constraint recall 7ce59bc is NOT merged onto the gitlink this phase (D-07)"
    - "A fresh worktree pulls the pushed submodule and the fresh gate passes"
  artifacts:
    - path: ".planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-STEP-0-GATE.md"
      provides: "Recorded per-file parse result over the showcase corpus (D-10 baseline)"
      contains: "Step 0"
    - path: "spec/grammar/dealx.ebnf"
      provides: "Honest-opaque GapBlock production (D-05)"
      contains: "GapBlock"
    - path: "spec/grammar/DESIGN-DECISIONS.md"
      provides: "Recorded `;`-canonical / `,`-rejected + keyword-key decisions"
      contains: "annotation"
  key_links:
    - from: "spec submodule HEAD"
      to: "parent deal gitlink"
      via: "git add spec + commit + push (D-06)"
      pattern: "spec"
    - from: "scripts/verify-fresh-worktree.sh"
      to: "pushed spec origin"
      via: "git submodule update --init --recursive"
      pattern: "submodule update"
---

<objective>
Establish the two TODO-1 preconditions before any parser source changes:

1. **Step 0 empirical gate (D-10):** Run `deal parse` over the entire showcase corpus and record the result. This is the mandatory baseline that proves the parser-strictness changes in later plans are intentional, not regressions. If anything other than the (absent) calc/constraint `analysis/*.deal` files fails — especially if any showcase file uses `,` as an annotation-body separator — D-01 flips and Waves B/C must be re-planned BEFORE proceeding.
2. **Wave A spec edits (D-05/D-06/D-07):** Reconcile the `spec` submodule EBNF + DESIGN-DECISIONS with parser reality (honest-opaque GapBlock, AnnotationKey keyword acceptance, optional-`;`, citation fixes), commit on top of pin `dd3bb78`, push to the spec origin, and bump the parent gitlink so the fresh-worktree gate exercises the pushed state.

Purpose: Step 0 is the blocking precondition for D-01's strict disposition. D-06 push-and-bump is required so fresh clones do not break on unpushed spec commits.
Output: `05.1-STEP-0-GATE.md` baseline record; updated + pushed `spec` submodule; bumped parent gitlink.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-CONTEXT.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-RESEARCH.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-PATTERNS.md
@.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md

<interfaces>
<!-- Spec submodule state. Verified 2026-06-09. -->
spec submodule pin: dd3bb78 (sims/v0 + showcase input values)
spec origin: git@github.com:deal-lang/spec.git (SSH, fetch + push)
Wave-A branch base: dd3bb78 — do NOT merge calc/constraint 7ce59bc (D-07).

Gap byte-scan parser reality (parser_dealx.zig:1229-1252):
  - `const open = try p.expect(.l_brace);` at line 1229
  - brace-balanced manual depth scan, body captured as raw byte slice `p.source[body_start_pos..body_end_pos]`
  - NO structured field parsing — the gap body is opaque text.

Showcase corpus (verified via find, 2026-06-09): 20 files under spec/examples/showcase/
  - 4 .dealx, 15 named .deal, 1 hidden packages/.deal (verify whether DEAL source — Open Q3)
  - No analysis/*.deal subdirectory exists in the current checkout (calc/constraint — deferred TODO 3)
  - `,` appears in showcase ONLY as: import-list separators and inside opaque gap blocks
    (model/traceability.dealx:133) — NEVER as an annotation-body field separator. D-01 stands.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step 0 empirical gate (D-10) — record showcase parse baseline</name>
  <read_first>
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-RESEARCH.md (Q5 — corpus inventory + `,` scan; Pitfall 3 — ordering)
    - spec/examples/showcase/packages/vehicle/battery.deal (`;`-terminated annotation bodies, lines 23-27, 80-89)
    - spec/examples/showcase/model/traceability.dealx (line 133 — `,` inside opaque gap block only)
    - build.zig (how `zig build run -- parse <file>` is invoked)
  </read_first>
  <action>
    Build the compiler (`zig build`). Enumerate every showcase file under spec/examples/showcase/ (the 4 `.dealx` + the 15 named `.deal`; check the hidden `packages/.deal` content first and EXCLUDE it from the parse corpus if it is not DEAL source — Open Q3). Run `zig build run -- parse <file>` for each and capture exit code + first diagnostic. Write `05.1-STEP-0-GATE.md` recording, per file: path, exit code, PASS/FAIL, and any first diagnostic code. Then grep the corpus for `,` usage and classify each occurrence as (a) import-list separator, (b) inside an opaque gap block, or (c) annotation-body field separator. Per D-10/research Q5 the expectation is ZERO category-(c) occurrences and zero parse failures (no `analysis/*.deal` files exist in the tree). If ANY non-`analysis` showcase file fails to parse, OR any category-(c) `,` separator is found, STOP and emit a `D-01 FLIP` banner at the top of `05.1-STEP-0-GATE.md` — Waves B/C must be re-planned and the executor must halt the phase.
  </action>
  <verify>
    <automated>test -f .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-STEP-0-GATE.md && grep -qi 'Step 0' .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-STEP-0-GATE.md && ! grep -qi 'D-01 FLIP' .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-STEP-0-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `05.1-STEP-0-GATE.md` exists and lists every parsed showcase file with its exit code.
    - Every non-`analysis` showcase file shows exit code 0 (PASS).
    - The `,` classification table shows zero category-(c) annotation-body separators.
    - No `D-01 FLIP` banner present (its presence is a hard stop on the phase).
  </acceptance_criteria>
  <done>Showcase parse baseline recorded; D-01 confirmed standing; no flip banner.</done>
</task>

<task type="auto">
  <name>Task 2: Wave A spec edits + push + gitlink bump (D-05/D-06/D-07)</name>
  <read_first>
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-PATTERNS.md (parser_dealx.zig:1229-1252 gap byte-scan reality; D-05 spec-only note)
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-RESEARCH.md (Q4 anchor table; Pitfall 5 — push without gitlink bump)
    - spec/grammar/dealx.ebnf (current GapBlock production)
    - spec/grammar/deal.ebnf (annotation/verification productions — AnnotationKey, optional-`;`, I6 citations)
    - spec/grammar/lexical.ebnf (name-token / keyword class for AnnotationKey)
    - spec/grammar/DESIGN-DECISIONS.md (where to record the separator + keyword-key decisions)
    - src/parser_deal.zig:1908 (`key_tok.tag == .ident or isKeyword(key_tok.tag)` — the keyword-key reality the spec must catch up to)
  </read_first>
  <action>
    Inside the `spec` submodule (currently pinned `dd3bb78` — do NOT merge calc/constraint `7ce59bc`, per D-07): (1) Redefine the `GapBlock` production in `dealx.ebnf` to honest-opaque — an opaque brace-balanced body, matching the parser_dealx.zig:1229-1252 byte-scan reality; do NOT claim structured gap fields (D-05; structured-gap parsing is deferred Wave E). (2) Update `deal.ebnf` so `AnnotationKey ::= IDENT | _Keyword` reflects the parser's keyword-as-field-key acceptance (I2/D-03 spec half), document that `;` is the canonical field terminator with `,` rejected (I1/D-01 spec half — the EBNF should NOT list `,` as a separator alias), and fix the I6 citation drift the audit flagged. (3) Add the keyword class reference in `lexical.ebnf` if `_Keyword` is not already defined there. (4) Record in `DESIGN-DECISIONS.md`: annotation separator decision (`;` canonical, `,` rejected) and keyword-as-field-key accept. Commit on the spec submodule with message `docs(grammar): reconcile EBNF with parser reality (I1/I2/I3/I5/I6) [phase 05.1]`. Push the spec commit to `origin`. Back in the parent `deal` repo, `git add spec` to bump the gitlink to the new spec HEAD, and commit `chore(spec): bump gitlink for Wave-A grammar reconciliation`. Per Pitfall 5: push spec → bump gitlink → both must land.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && git -C spec log --oneline -1 | grep -qi 'reconcile\|grammar' && git -C spec status --porcelain | grep -q . ; test $? -ne 0 && grep -qi 'GapBlock' spec/grammar/dealx.ebnf && grep -qi 'annotation' spec/grammar/DESIGN-DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `spec/grammar/dealx.ebnf` GapBlock production describes an opaque brace-balanced body (no structured fields).
    - `spec/grammar/deal.ebnf` AnnotationKey includes keyword acceptance and does NOT list `,` as a separator alias.
    - `spec/grammar/DESIGN-DECISIONS.md` records both decisions (separator + keyword key).
    - spec submodule has a new committed HEAD pushed to origin; `git -C spec status` is clean.
    - parent `deal` repo gitlink points at the new spec HEAD (`git submodule status` shows the new pin).
    - spec HEAD is a descendant of `dd3bb78` and does NOT include `7ce59bc` (D-07).
  </acceptance_criteria>
  <done>Spec EBNF + DESIGN-DECISIONS reconciled, committed, pushed; parent gitlink bumped and committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Wave-A spec edits pushed to the spec origin and the parent gitlink bumped (D-06). The fresh-worktree gate must now exercise the pushed submodule state.</what-built>
  <how-to-verify>
    1. Run `scripts/verify-fresh-worktree.sh` (it creates a fresh worktree, runs `git submodule update --init --recursive`, and builds/tests against the pulled submodule).
    2. Confirm it exits 0 — this proves the pushed spec commit is reachable from origin and the bumped gitlink resolves in a clean clone.
    3. Run `git submodule status` and confirm the spec pin is the new Wave-A HEAD (not `dd3bb78`) and is a descendant of `dd3bb78`.
    4. Confirm the spec HEAD does NOT contain calc/constraint commit `7ce59bc` (`git -C spec log --oneline | grep 7ce59bc` returns nothing).
  </how-to-verify>
  <resume-signal>Type "approved" once the fresh-worktree gate passes and the gitlink/D-07 checks hold, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → spec origin (shared remote) | `git push` to the shared spec remote crosses into shared state |
| fresh clone → submodule pull | a clean environment resolves the gitlink from origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-A01 | Tampering | spec push to shared origin (D-06) | mitigate | Branch from `dd3bb78`; push a fast-forward descendant only; verify `git -C spec log` shows the new commit is a child of `dd3bb78` and excludes `7ce59bc` before bumping the gitlink. Do NOT force-push. |
| T-05.1-A02 | Denial of Service | fresh-worktree gate pulling an unreachable pin | mitigate | Human-verify checkpoint runs `verify-fresh-worktree.sh` after push to prove the pushed commit is reachable; gitlink bump is committed only after the spec push succeeds. |
| T-05.1-A03 | Repudiation | gitlink bumped without recording why | accept | Commit message records the Wave-A reconciliation rationale; low risk (single-maintainer repo). |
| T-05.1-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; N/A. RESEARCH.md confirms zero new dependencies. |
</threat_model>

<verification>
- `05.1-STEP-0-GATE.md` exists, records all showcase files PASS, no `D-01 FLIP` banner.
- `spec` submodule has a new pushed HEAD (descendant of `dd3bb78`, excludes `7ce59bc`).
- Parent `deal` gitlink bumped to the new spec HEAD.
- `scripts/verify-fresh-worktree.sh` exits 0 against the pushed state (human-verify checkpoint).
</verification>

<success_criteria>
- D-10 Step 0 baseline recorded with all non-analysis showcase files passing; D-01 confirmed standing.
- D-05 honest-opaque GapBlock landed in `dealx.ebnf`.
- D-06 spec edits pushed + parent gitlink bumped; fresh gate green.
- D-07 honored: `7ce59bc` NOT merged onto the gitlink.
</success_criteria>

<output>
Create `.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-01-SUMMARY.md` when done.
</output>
