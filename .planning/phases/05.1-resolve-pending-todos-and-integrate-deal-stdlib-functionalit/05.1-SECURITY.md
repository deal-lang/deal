---
phase: 05.1
slug: resolve-pending-todos-and-integrate-deal-stdlib-functionalit
status: verified
threats_open: 0
asvs_level: 1
created: 2026-06-09
---

# Phase 05.1 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| local repo → spec origin (shared remote) | `git push` to the shared spec remote crosses into shared state | spec commits / gitlink pin |
| fresh clone → submodule pull | a clean environment resolves the gitlink from origin | submodule commit reachability |
| CLI test fixture (`.deal` source) → deal check parser/sema | adversarial-shaped DEAL input crosses into the parser | untrusted source text |
| Rust CLI → Zig C ABI (`deal_check_with_stdlib`) | FFI boundary; arena-owned pointers shared not copied | arena handle / raw pointers |
| `.deal`/`.dealx` source (incl. adversarial) → parser dispatch loops & l_bracket atom | untrusted bracketed/annotation input drives the new parser arms | untrusted source text |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-05.1-A01 | Tampering | spec push to shared origin (D-06) | mitigate | Fast-forward descendant push only, no force-push. `spec` 74dfea5 is a single-commit FF from dd3bb78; commit 814b63e records the FF claim and 7ce59bc exclusion; no `--force` artifact | closed |
| T-05.1-A02 | Denial of Service | fresh-worktree gate pulling unreachable pin | mitigate | `scripts/verify-fresh-worktree.sh` proves pushed commit reachable (dirty-tree reject L87, `submodule update --init --recursive` L126, materialization check L133, gate-step L221); 01-SUMMARY records gate ran against pushed spec | closed |
| T-05.1-A03 | Repudiation | gitlink bumped without recording why | accept | Commit 814b63e body records full reconciliation rationale; single-maintainer repo | closed |
| T-05.1-B01 | Denial of Service | use-after-free on stdlib_arena handle (Phase 5 SIGSEGV) | mitigate | Guard `if !use_stdlib_check { … deal_index_json … }` at cli/src/main.rs:433; tests use only `deal check --json` path; `grep -c deal_index_json cli/tests/e2500_cli.rs` = 0 | closed |
| T-05.1-B02 | Denial of Service | malformed `.deal` fixture causing unbounded loop/OOB in parser | accept | Fixtures are well-formed-but-dimension-wrong sema-layer sources; parser robustness covered by 55-file malformed corpus | closed |
| T-05.1-C01 | Denial of Service | new diagnostic path causing an infinite loop on adversarial input | mitigate | Every diagnostic arm advances the cursor before `continue`: parser_deal.zig:1938 (comma), parser_deal.zig:1985 (unknown-key else), parser_dealx.zig:834 | closed |
| T-05.1-C02 | Tampering | silent acceptance re-introduced by over-broad diagnostic arm | mitigate | Hard `.err` codes E0123 (diagnostics.zig:89) / E0124 (diagnostics.zig:91); corpus pins lock first-diagnostic identity at recovery_corpus.zig:159-162 | closed |
| T-05.1-C03 | Information Disclosure | diagnostic span pointing outside the source buffer (OOB read) | mitigate | Diagnostics use `key_tok.span` (lexer-produced, within buffer) at parser_deal.zig:1928,1935,1983; no new span arithmetic | closed |
| T-05.1-D01 | Denial of Service | unbounded loop / OOB on unterminated array `[ …` | mitigate | l_bracket loop dual-sentinel `while (.r_bracket and .eof)` expr.zig:283-286; EOF emits E0402 (expr.zig:308-314); `expect(.r_bracket)` expr.zig:316 — terminates at EOF | closed |
| T-05.1-D02 | Tampering | snapshot regeneration masking an unintended AST shape change | mitigate | Regenerate-then-clean-rerun gate; snapshots show 0 `__array__` and 1+ `array_literal`; SysML/reqif goldens contain no `__array__` (no diff) | closed |
| T-05.1-D03 | Information Disclosure | array_literal span pointing outside the source buffer | mitigate | Span `{ .start = tok.span.start, .end = close.span.end }` at expr.zig:317 — both lexer-produced tokens, no new arithmetic beyond the l_paren analog | closed |
| T-05.1-SC | Tampering | npm/pip/cargo/zig installs | mitigate | Zero new dependencies: `build.zig.zon:11` `.dependencies = .{}`; `tempfile` is a pre-existing Phase-4 dev-dependency; no new Cargo/Zig/Python deps | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-05.1-01 | T-05.1-A03 | Gitlink-bump repudiation: commit 814b63e records full Wave-A reconciliation rationale; single-maintainer repo makes attribution-loss risk low | DavidAD | 2026-06-09 |
| AR-05.1-02 | T-05.1-B02 | Malformed `.deal` fixtures: phase fixtures are well-formed-but-dimension-wrong sema sources; parser recovery robustness is covered separately by the 55-file malformed corpus regression | DavidAD | 2026-06-09 |

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-06-09 | 12 | 12 | 0 | gsd-security-auditor (sonnet) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-06-09
