---
phase: 05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit
plan: 04
type: execute
wave: 3
depends_on: ["05.1-03"]
files_modified:
  - src/ast.zig
  - src/json.zig
  - src/expr.zig
  - src/parser_deal.zig
  - src/parser_dealx.zig
  - src/ir.zig
  - src/lowering.zig
  - src/fmt.zig
  - tests/snapshots/ast/showcase__model__vehicle.dealx.json
  - tests/snapshots/ast/showcase__packages__requirements__system.deal.json
autonomous: true
requirements: [D-04, D-11]
must_haves:
  truths:
    - "A real `array_literal` AST NodeKind exists and is the single source of truth for `[expr, expr, ...]` (D-04)"
    - "Both duplicate array parsers (parser_dealx.parseArrayLiteral, parser_deal.parseVerificationArray) are deleted"
    - "The array_literal node is built as a clean ideal form so the deferred calc/constraint work inherits it without rework (D-11)"
    - "The synthetic `call(__array__, items)` encoding no longer appears in AST output"
    - "Array AST output is byte-identical (post-regeneration) in both .dealx carrying and .deal verification contexts"
    - "deal fmt round-trips `[expr, expr]` byte-identically"
    - "Showcase snapshots are byte-stable after regeneration (no drift on a clean re-run)"
  artifacts:
    - path: "src/ast.zig"
      provides: "array_literal NodeKind + ArrayLiteral payload struct + Payload union arm"
      contains: "array_literal"
    - path: "src/json.zig"
      provides: "array_literal serialization arm emitting {\"k\":\"array_literal\",\"items\":[...]}"
      contains: "array_literal"
    - path: "src/expr.zig"
      provides: "l_bracket ArrayLiteral atom case in parsePrimary"
      contains: "l_bracket"
    - path: "src/fmt.zig"
      provides: "writeArrayLiteral branch replacing the __array__ special-case"
      contains: "array_literal"
  key_links:
    - from: "src/expr.zig parsePrimary"
      to: "ast.array_literal node"
      via: "l_bracket atom arm makeNode(.array_literal, ...)"
      pattern: "array_literal"
    - from: "src/parser_deal.zig parseVerificationValue"
      to: "expr.parseExpression (l_bracket)"
      via: "deleted parseVerificationArray, delegate to expr"
      pattern: "parseExpression"
    - from: "src/parser_dealx.zig array call site"
      to: "expr.parseExpression (l_bracket)"
      via: "deleted parseArrayLiteral, delegate to expr"
      pattern: "parseExpression"
---

<objective>
TODO 1 Wave C — array unification (D-04). Replace the synthetic `call(__array__, items)` encoding with a real `array_literal` AST NodeKind, threaded through the full pipeline, and delete both duplicated array parsers. RESEARCH.md Q1 verified `__array__` is an AST-level encoding ONLY — `ir.zig`/`lowering.zig`/`json.zig` have zero `__array__` references — so only the AST snapshot goldens (2 files) are affected, and IR/lowering need only compile-safe stub arms for the new NodeKind.

Decisions:
- **D-04:** Add `array_literal` to `ast.zig` (NodeKind + `ArrayLiteral{ items: []*Node }` payload + Payload union arm). Add the `l_bracket` atom arm to `expr.zig:parsePrimary` as the single source of truth. Delete `parser_dealx.parseArrayLiteral` (:809) and `parser_deal.parseVerificationArray` (:1994), delegating both `[` call sites to `expr.parseExpression`. Add the `json.zig` serialization arm. Replace the `fmt.zig` `__array__` special-case with a `writeArrayLiteral` branch. Add stub `array_literal` arms to any exhaustive `switch (node.kind)` in `ir.zig`/`lowering.zig`. Acceptance: byte-identical output vs. pre-refactor goldens after regenerating the 2 affected AST snapshots.
- **D-11:** Build the ideal `array_literal` form cleanly so the deferred calc/constraint work inherits it.

Interface-first ordering: Task 1 defines the AST contract (ast.zig + json.zig) so the parser/fmt tasks build against a known node shape.

Purpose: a single real AST node removes the two parser duplications and the brittle name-based `__array__` formatter special-case.
Output: unified `array_literal` node; both duplicate parsers deleted; pipeline stub arms; regenerated byte-stable snapshots; fmt round-trip green.

DEPENDS ON Plan 03 (Wave B): both touch `parser_deal.zig` (annotation/verification region). Wave B lands first to keep the diffs sequential and reviewable (research Open Q1 + anti-pattern note).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-CONTEXT.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-RESEARCH.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-PATTERNS.md
@.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-03-SUMMARY.md

<interfaces>
<!-- Verified 2026-06-09. -->
src/ast.zig (three-part atomic addition — exhaustive union enforces all three):
  - NodeKind enum (:136-150 expression group): add `array_literal,` near object_literal:149.
  - Payload struct (analog ObjectLiteral :453-557): add
        pub const ArrayLiteral = struct { items: []*Node };
  - Payload union (:655 object_literal arm): add `array_literal: ArrayLiteral,`.

src/json.zig (.call arm :440-446 analog):
  - add `.array_literal => |p| { try buf.appendSlice(allocator, ",\"items\":");
        try writeNodeArrayMut(allocator, buf, p.items); }`
  - produces {"k":"array_literal","items":[...]} replacing
    {"k":"call","callee":{"k":"identifier","name":"__array__"},"args":[...]}.

src/expr.zig parsePrimary switch:
  - l_paren arm at :265-276 is the consume/parse/consume-close analog.
  - insert l_bracket arm BEFORE the else=> recovery at :278 (so `[` no longer falls through):
    loop parseExpression until r_bracket/eof, optional comma between items, expect(.r_bracket),
    makeNode(.array_literal, span, .{ .array_literal = .{ .items = ... } }).

src/parser_deal.zig:
  - :1986 `return parseVerificationArray(p);` branch in parseVerificationValue — DELETE branch,
    let l_bracket fall through to `expr.parseExpression(p, 0)`.
  - :1994 `fn parseVerificationArray` — DELETE entire fn.

src/parser_dealx.zig:
  - :809 `fn parseArrayLiteral` + its `if (tok.tag == .l_bracket) return parseArrayLiteral(p);`
    caller (~:800) — DELETE; delegate `[` to expr.parseExpression.

src/fmt.zig:
  - writeCall (:754-768) special-cases callee name "__array__" → emit [..]. REMOVE that check.
  - add fn writeArrayLiteral(ctx, p: *const ast.ArrayLiteral, indent) → "[" items join ", " "]".
  - add writeNode dispatch arm `.array_literal => |p| try ctx.writeArrayLiteral(&p, indent),`.

src/ir.zig (282 lines) + src/lowering.zig (729 lines):
  - ZERO __array__ references. Adding the NodeKind breaks their exhaustive `switch (node.kind)`.
  - Add a compile-safe `.array_literal` stub arm at each switch (passthrough / treat-as-items,
    matching the existing .call treatment). Let the compile error surface the exact sites.

Affected AST snapshot goldens (regenerate after ast.zig + json.zig land):
  - tests/snapshots/ast/showcase__model__vehicle.dealx.json (messageIds arrays)
  - tests/snapshots/ast/showcase__packages__requirements__system.deal.json (accepts:/rejects: arrays)
  SysML-v2 + reqif goldens are UNAFFECTED (no __array__ content — IR/lowering unchanged).

Snapshot regeneration (build.zig:28-37):
  zig build test -Dupdate-snapshots=true   # regenerate
  zig build test                            # confirm byte-stability (no flag)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the array_literal AST contract (ast.zig + json.zig)</name>
  <read_first>
    - src/ast.zig (NodeKind :136-150; ObjectLiteral struct :453-557; Payload union :568-656 object_literal arm :655)
    - src/json.zig (.call arm :440-446; writeNodeArrayMut helper)
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-PATTERNS.md (§ast.zig three-part addition; §json.zig array arm)
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-RESEARCH.md (Q1 — no __array__ in ir/lowering/json; Q3 — affected goldens)
  </read_first>
  <action>
    Add the `array_literal` NodeKind to `src/ast.zig` in the expression group (near `object_literal`), define `pub const ArrayLiteral = struct { items: []*Node };` alongside `ObjectLiteral`, and add the `array_literal: ArrayLiteral` arm to the Payload union — all three atomically (the exhaustive union forces this). Add the serialization arm to `src/json.zig` immediately adjacent to the `.call` arm, emitting `{"k":"array_literal","items":[...]}` via `writeNodeArrayMut(allocator, buf, p.items)` (alphabetical field order per D-18). Do NOT yet touch parsers/fmt/ir/lowering — this task only establishes the contract so downstream tasks build against a known shape. The build will not fully compile until exhaustive switches in ir.zig/lowering.zig/fmt.zig get their arms (Task 3); that is expected. Verify the ast.zig + json.zig additions are syntactically consistent.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && grep -q 'array_literal' src/ast.zig && grep -q 'pub const ArrayLiteral' src/ast.zig && grep -q 'array_literal' src/json.zig</automated>
  </verify>
  <acceptance_criteria>
    - `src/ast.zig` declares `array_literal` NodeKind, `ArrayLiteral{ items: []*Node }` payload, and the `array_literal: ArrayLiteral` union arm.
    - `src/json.zig` has an `.array_literal` arm emitting `"k":"array_literal","items":[...]`.
    - The additions follow the ObjectLiteral / `.call` analogs structurally.
  </acceptance_criteria>
  <done>array_literal AST contract + JSON serialization defined; downstream tasks have a known node shape.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the l_bracket parser atom + delete both duplicate array parsers (D-04)</name>
  <read_first>
    - src/expr.zig (parsePrimary switch; l_paren arm :265-276; else=> recovery :278-284)
    - src/parser_deal.zig (parseVerificationValue :1986 l_bracket branch; parseVerificationArray :1994)
    - src/parser_dealx.zig (parseArrayLiteral :809 + its caller ~:800)
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-PATTERNS.md (§expr.zig l_bracket arm; §parser_deal delete; §parser_dealx delete)
  </read_first>
  <action>
    Add the `l_bracket` atom arm to `src/expr.zig:parsePrimary`'s switch, inserted BEFORE the `else =>` recovery at :278 so `[` is parsed instead of falling into error recovery. The arm loops `parseExpression(parser, 0)` collecting items until `.r_bracket`/`.eof`, consumes an optional `.comma` between items, `expect(.r_bracket)`, and returns `makeNode(.array_literal, span, .{ .array_literal = .{ .items = ... } })` — modeled on the `l_paren` arm shape (PATTERNS.md §expr.zig). Then DELETE `parser_deal.parseVerificationArray` (:1994) and its branch in `parseVerificationValue` (:1986) so `[` delegates to `expr.parseExpression(p, 0)`. DELETE `parser_dealx.parseArrayLiteral` (:809) and its caller branch (~:800), delegating `[` to `expr.parseExpression`. After this, both array contexts route through the single `expr.zig` atom. The `__array__` synthetic node is no longer produced by any parser.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && grep -q 'l_bracket' src/expr.zig && ! grep -q 'fn parseVerificationArray' src/parser_deal.zig && ! grep -q 'fn parseArrayLiteral' src/parser_dealx.zig && grep -vc '^[[:space:]]*//' src/parser_deal.zig >/dev/null && test "$(grep -c '__array__' src/parser_deal.zig src/parser_dealx.zig | awk -F: '{s+=$2} END{print s}')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `src/expr.zig:parsePrimary` has an `l_bracket` arm producing `.array_literal`, placed before the `else =>` recovery.
    - `parser_deal.parseVerificationArray` and `parser_dealx.parseArrayLiteral` are both deleted (`grep -q 'fn parseVerificationArray'`/`'fn parseArrayLiteral'` return non-zero).
    - Neither `parser_deal.zig` nor `parser_dealx.zig` references `__array__` anymore (combined count = 0).
    - Both `[` call sites delegate to `expr.parseExpression`.
  </acceptance_criteria>
  <done>Single l_bracket atom in expr.zig; both duplicate parsers deleted; no parser-side __array__.</done>
</task>

<task type="auto">
  <name>Task 3: Thread array_literal through fmt/ir/lowering + regenerate and re-lock snapshots (D-04)</name>
  <read_first>
    - src/fmt.zig (writeCall __array__ special-case :754-768; writeNode dispatch switch)
    - src/ir.zig (exhaustive `switch (node.kind)` sites — let the compile error surface them)
    - src/lowering.zig (exhaustive `switch (node.kind)` sites)
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-PATTERNS.md (§fmt.zig writeArrayLiteral; §ir.zig/lowering.zig stub arm; §Snapshot regeneration gate sequence)
    - .planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-RESEARCH.md (Q1 verdict; Q3 affected goldens; Pitfall 1 — json.zig + snapshot order)
  </read_first>
  <action>
    In `src/fmt.zig`: remove the `__array__` name-check from `writeCall` (:754-768), add `fn writeArrayLiteral(ctx, p: *const ast.ArrayLiteral, indent)` emitting `[` + items joined by `, ` + `]`, and add the `.array_literal => |p| try ctx.writeArrayLiteral(&p, indent),` dispatch arm to `writeNode`'s switch. In `src/ir.zig` and `src/lowering.zig`, add a compile-safe `.array_literal` stub arm to each exhaustive `switch (node.kind)` — since IR/lowering do not process arrays today (RESEARCH Q1: zero `__array__` refs), mirror the existing `.call` treatment (passthrough over items) or a no-op that keeps the switch exhaustive; let the compile errors point to the exact sites. Once `zig build` compiles cleanly, regenerate the two affected AST snapshots with `zig build test -Dupdate-snapshots=true`, then IMMEDIATELY re-run `zig build test` WITHOUT the flag to confirm byte-stability (Pitfall 1 ordering). Verify the regenerated `showcase__model__vehicle.dealx.json` and `showcase__packages__requirements__system.deal.json` now contain `"k":"array_literal","items":[...]` and NO `__array__`. Run the fmt round-trip test to confirm `[expr, expr]` output is byte-identical. Confirm SysML-v2 + reqif goldens are unchanged (no regeneration needed — IR/lowering output shape did not change).
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && zig build test 2>&1 | tail -5 | grep -qiv 'FAIL' && zig build test 2>&1 | grep -qiv 'error:' && ! grep -q '__array__' tests/snapshots/ast/showcase__model__vehicle.dealx.json && ! grep -q '__array__' tests/snapshots/ast/showcase__packages__requirements__system.deal.json && grep -q 'array_literal' tests/snapshots/ast/showcase__packages__requirements__system.deal.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/fmt.zig` no longer special-cases `__array__`; a `writeArrayLiteral` branch handles `.array_literal`.
    - `src/ir.zig` and `src/lowering.zig` compile with `.array_literal` arms in their exhaustive switches.
    - `zig build test` (no flag) passes with byte-stable snapshots after regeneration.
    - The 2 affected AST snapshots contain `"k":"array_literal"` and zero `__array__`.
    - SysML-v2 and reqif goldens are unchanged (git diff shows no modification).
    - fmt round-trip emits `[expr, expr]` byte-identically (fmt test green).
    - No remaining `__array__` anywhere except possibly historical comments: `grep -rl '__array__' src/ tests/snapshots/` is empty.
  </acceptance_criteria>
  <done>array_literal threaded through fmt/ir/lowering; snapshots regenerated and byte-stable; __array__ fully removed; fmt round-trip green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.deal`/`.dealx` source (incl. adversarial arrays) → expr.zig l_bracket atom | untrusted bracketed input drives the new array parser arm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-D01 | Denial of Service | unbounded loop / OOB on adversarial `[ ... ` (unterminated array) | mitigate | The l_bracket loop guards on `.r_bracket` AND `.eof`, and uses `expect(.r_bracket)` (which emits a diagnostic on mismatch) — an unterminated array terminates at EOF, not an infinite loop. The 50-file malformed corpus (Plan 03 regression run) plus the property-span-containment test exercise adversarial shapes for non-panic/termination. |
| T-05.1-D02 | Tampering | snapshot regeneration masking an unintended AST shape change | mitigate | Regenerate-then-re-run-clean gate (Pitfall 1): `-Dupdate-snapshots=true` followed immediately by a flagless `zig build test`. The grep gate asserts the new snapshots contain `array_literal` and zero `__array__`, and SysML/reqif goldens must show no git diff — so the only intended change is the AST-node rename. |
| T-05.1-D03 | Information Disclosure | array_literal span pointing outside the source buffer | mitigate | Span computed from the opening `[` token span start and the closing `]` token span end, both lexer-produced within the source buffer — no new arithmetic beyond the l_paren analog. |
| T-05.1-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; N/A. RESEARCH.md confirms zero new dependencies. |
</threat_model>

<verification>
- `zig build test` (no flag) passes with byte-stable AST snapshots.
- Both affected AST snapshots show `array_literal`, zero `__array__`.
- SysML-v2 + reqif goldens unchanged.
- fmt round-trip on array contexts byte-identical.
- `grep -rl '__array__' src/ tests/snapshots/` is empty (synthetic encoding fully removed).
</verification>

<success_criteria>
- D-04: real `array_literal` node is the single source of truth; both duplicate parsers deleted; `call(__array__, items)` gone from AST; byte-identical (post-regeneration) output in both array contexts; fmt round-trips.
- D-11: clean ideal `array_literal` form for downstream calc/constraint inheritance.
- No IR/lowering output change (SysML/reqif goldens stable).
</success_criteria>

<output>
Create `.planning/phases/05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit/05.1-04-SUMMARY.md` when done.
</output>
