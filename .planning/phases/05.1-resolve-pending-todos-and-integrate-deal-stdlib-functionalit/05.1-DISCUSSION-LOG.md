# Phase 05.1: Resolve pending todos and integrate deal-stdlib functionality - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-09
**Phase:** 05.1-resolve-pending-todos-and-integrate-deal-stdlib-functionalit
**Areas discussed:** Phase scope (todo folding), Strictness disposition (I1/I3), Array unification depth (I4), Spec submodule handling, E2500 wiring breadth (TODO 2)

---

## Phase Scope (todo folding)

The phase was inserted with a TBD goal. Three pending todos matched. multiSelect — which to fold:

| Option | Description | Selected |
|--------|-------------|----------|
| TODO 2 — stdlib E2500 wiring | Cross-file dimensional integration; small/surgical; the "integrate deal-stdlib" clause | ✓ |
| TODO 1 — parser drift/strictness (I1–I6) | Reconcile EBNF↔parser + harden silent-tolerance; medium, ~5 waves | ✓ |
| TODO 3 — calc/constraint grammar (SD-21/22/23) | Full language feature, lexer→codegen, ~Phase-2 sized; todo recommends its own phase | |

**User's choice:** TODO 2 + TODO 1.
**Notes:** TODO 3 deferred to its own dedicated phase, carrying the spec-submodule recall obligation (re-pin `7ce59bc` + `dd3bb78`).

---

## Strictness disposition — I1 (annotation-body separator)

| Option | Description | Selected |
|--------|-------------|----------|
| Accept `,`, fmt-normalize to `;` | Lenient migration; parser accepts both, fmt rewrites `,`→`;` | |
| Reject `,` with a diagnostic | Strict; only `;` valid; `,` separator emits an explicit error | ✓ |

**User's choice:** Reject `,` with a diagnostic.
**Notes:** `;` becomes the canonical explicitly-consumed terminator. Caveat recorded: Step 0 empirical `deal parse` must confirm no showcase file relies on `,`; if one does, this flips and the wave re-plans (D-10).

---

## Strictness disposition — I3 (unknown verification keys)

| Option | Description | Selected |
|--------|-------------|----------|
| Hard error (new diagnostic) | Unknown verification key = explicit error; fully closes silent-drop | ✓ |
| Warning, key preserved | Emit a warning but keep parsing (forward-compat) | |

**User's choice:** Hard error (new diagnostic).
**Notes:** Consistent strict posture with I1.

---

## Array unification depth (I4)

| Option | Description | Selected |
|--------|-------------|----------|
| Real ArrayLiteral AST node | Single expr.zig path, real AST node, delete both duplicated parsers, thread through IR/codegen; regenerate goldens | ✓ |
| Unify path, keep `__array__` | Collapse duplicated parsers but keep synthetic `call(__array__, items)`; minimal blast radius | |

**User's choice:** Real ArrayLiteral AST node.
**Notes:** Full pipeline refactor; byte-identical-output proof vs pre-refactor goldens required. Keep `__array__` only if codegen back-compat genuinely forces it (verify in research).

---

## Spec submodule handling (Wave A)

| Option | Description | Selected |
|--------|-------------|----------|
| Commit + push, bump gitlink | Land Wave-A on top of `dd3bb78`, push to spec origin, bump parent gitlink; fresh gates see the fixes | ✓ |
| Local edits, defer push/bump | Edit locally for in-worktree gates only; spec stays "lying" on remote | |

**User's choice:** Commit + push, bump gitlink.
**Notes:** calc/constraint recall (`7ce59bc`) stays separately deferred; Wave-A branches from `dd3bb78` to keep Phase 5's sims/v0 protocol intact.

---

## E2500 wiring breadth (TODO 2)

| Option | Description | Selected |
|--------|-------------|----------|
| All four cross-file E25xx | Seeding the external table activates E2500–E2503; CLI tests for all four | ✓ |
| E2500 only (deferred scenario) | Wire/test just the 04-HUMAN-UAT Test 2 scenario | |

**User's choice:** All four cross-file E25xx.
**Notes:** Includes the exact 04-HUMAN-UAT Test 2 scenario as a named acceptance case.

---

## Claude's Discretion

- **I2 (keyword-as-field-key):** not a real choice — the showcase oracle requires `from:`, so it must be accepted; captured as an explicit, tested accept (`AnnotationKey ::= IDENT | _Keyword`) rather than silent.
- **I5 (gap block):** adopted the todo's recommended honest-opaque option (EBNF matches parser reality; structured gap deferred to Wave E) without separately asking — fits the user's strictness/no-lying pattern.
- **I6 (stale EBNF citations):** mechanical fix + guard; no decision needed.
- **Wave sequencing (D-11):** no two-pass conflict this phase since calc/constraint is deferred; build I1/I3/I4 ideal forms so future calc/constraint work inherits them.

## Deferred Ideas

- calc/constraint grammar build-out (TODO 3) — its own phase; carries the spec-submodule recall.
- Structured `gap` field parsing (TODO 1 Wave E).
- E2503 dimensional regression pin (`07-conversion-mismatch.deal`) — Phase-4 inherited carryover.
