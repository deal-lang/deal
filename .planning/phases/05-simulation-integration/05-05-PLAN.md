---
phase: 05-simulation-integration
plan: 05
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - cli/src/evidence.rs
  - .gitignore
  - cli/tests/evidence_baseline.rs
autonomous: true
requirements:
  - REQ-phase-5-3-evidence-verification
  - REQ-phase-5-simulation

must_haves:
  truths:
    - "deal evidence capture snapshots .deal/evidence/ working runs into the captured set"
    - "deal evidence baseline v2.1.0 writes a frozen, git-tracked snapshot + manifest to evidence/baselines/v2.1.0/"
    - "The baseline manifest records per-sim content hashes, tool versions, @reproducibility tier, and PASS/FAIL/PARTIAL verdicts at capture time (D-82)"
    - ".deal/ stays gitignored; evidence/baselines/ is tracked (D-81 clean split)"
    - "All evidence JSON uses BTreeMap alphabetical keys (D-18 byte-stable)"
  artifacts:
    - path: "cli/src/evidence.rs"
      provides: "evidence capture + baseline snapshot + manifest generation"
      contains: "fn run_evidence_baseline"
    - path: "cli/tests/evidence_baseline.rs"
      provides: "baseline manifest + frozen snapshot test"
      contains: "manifest"
  key_links:
    - from: "cli/src/evidence.rs"
      to: "evidence/baselines/<tag>/manifest.json"
      via: "BTreeMap-keyed manifest write (D-18)"
      pattern: "manifest"
    - from: ".gitignore"
      to: ".deal/"
      via: "gitignore scopes .deal only, not evidence/baselines"
      pattern: ".deal"
---

<objective>
Wave 3 (depends on the orchestrator, Plan 03): Implement `deal evidence capture` and `deal evidence baseline <tag>`. Capture snapshots the working `.deal/evidence/` runs into a captured set; baseline writes a frozen, git-tracked snapshot plus a self-contained manifest recording per-sim content hashes, tool versions, declared `@reproducibility` tier, and PASS/FAIL/PARTIAL verdicts at capture time (D-82). Enforce the D-81 clean split: working cache in gitignored `.deal/evidence/`, durable baselines in tracked `evidence/baselines/<tag>/`.

Purpose: The baseline is the citable V&V reference for CDRL/audit — readable cold without re-running anything. Plan 06's verify engine reads the captured/baseline evidence to evaluate criteria and compute freshness.
Output: Working `deal evidence` subcommands + a frozen baseline manifest + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-simulation-integration/05-CONTEXT.md
@.planning/phases/05-simulation-integration/05-RESEARCH.md
@.planning/phases/05-simulation-integration/05-PATTERNS.md

<interfaces>
Plan 03 writes working runs to .deal/evidence/<sim-name>/ holding output.json + metadata.json (+ skip.json for skipped sims). metadata.json carries the staleness hash, tool, tool_version, reproducibility_tier.

D-81/D-82 layout: .deal/evidence/<name>/ is the working cache (GITIGNORED, PS-10); evidence/baselines/<tag>/ is the frozen snapshot + manifest.json (TRACKED, repo root, NOT under .deal).

Reuse (PATTERNS.md evidence.rs section): cli/src/main.rs:441-468 BTreeMap-keyed JSON (D-18); cli/src/main.rs:713-749 write_file_atomic (tmp+rename); cli/src/main.rs:469-484 create_dir_all; sha2::Sha256 per-sim content hash; walkdir recursive scan (added Plan 01).

manifest.json shape (D-82, alphabetical keys): per-sim content_hash, reproducibility_tier, tool, tool_version, verdict; plus top-level tag, timestamp, v:1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deal evidence capture — snapshot working runs</name>
  <files>cli/src/evidence.rs, .gitignore</files>
  <read_first>
    - cli/src/evidence.rs (the Plan 01 stub to flesh out)
    - cli/src/main.rs (lines 441-468 BTreeMap JSON; 469-484 create_dir_all; 713-749 write_file_atomic)
    - .planning/phases/05-simulation-integration/05-PATTERNS.md (evidence.rs section — atomic write + BTreeMap + directory creation)
    - .planning/phases/05-simulation-integration/05-RESEARCH.md (Pitfall 3 evidence cache path vs baseline path; D-81/D-84)
    - .gitignore (confirm .deal/ is scoped so evidence/baselines/ is NOT ignored)
  </read_first>
  <action>
    Flesh out run_evidence_capture in cli/src/evidence.rs: scan .deal/evidence/ via walkdir, read each sim output.json + metadata.json (and skip.json if present), and snapshot them into the captured set the verify engine reads. Use write_file_atomic (tmp+rename) and BTreeMap-keyed JSON (D-18) for any aggregate index written. Confirm .gitignore scopes .deal/ only (NOT evidence/); if evidence/ is accidentally covered, add a negation or restructure (Pitfall 3). The Command::Evidence Capture dispatch arm is already wired to run_evidence_capture by Plan 01 — implement the function body only (no main.rs edit).
  </action>
  <verify>
    <automated>cargo test -p deal evidence::tests::capture 2>&1 | tail -6; git check-ignore .deal/evidence/x && echo DEAL_IGNORED; git check-ignore evidence/baselines/x && echo BASELINE_IGNORED || echo BASELINE_TRACKED</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p deal evidence::tests::capture exits 0.
    - git check-ignore .deal/evidence/x succeeds (ignored); git check-ignore evidence/baselines/x fails (tracked) — D-81 split holds.
    - Capture reads output.json + metadata.json (+ skip.json) from .deal/evidence/ and snapshots them.
    - Any aggregate index uses BTreeMap (D-18) and write_file_atomic.
  </acceptance_criteria>
  <done>deal evidence capture snapshots working runs; gitignore split verified (.deal ignored, evidence/baselines tracked).</done>
</task>

<task type="auto">
  <name>Task 2: deal evidence baseline tag — frozen snapshot + manifest</name>
  <files>cli/src/evidence.rs, cli/tests/evidence_baseline.rs</files>
  <read_first>
    - cli/src/evidence.rs (Task 1 output — capture path + helpers)
    - cli/src/main.rs (lines 441-468 BTreeMap JSON; 713-749 write_file_atomic)
    - .planning/phases/05-simulation-integration/05-RESEARCH.md (D-82 manifest contents; D-18 alphabetical-key discipline)
    - .planning/phases/05-simulation-integration/05-PATTERNS.md (D-18 Alphabetical Key Order shared pattern)
    - cli/tests/evidence_baseline.rs (the test to author — manifest + frozen snapshot assertions)
  </read_first>
  <action>
    Implement run_evidence_baseline(tag) in cli/src/evidence.rs: copy the captured output.json set into evidence/baselines/<tag>/ (tracked, repo root — NOT under .deal), and write evidence/baselines/<tag>/manifest.json recording, per sim, a content_hash (sha2::Sha256 over the output.json bytes), reproducibility_tier, tool, tool_version, and the PASS/FAIL/PARTIAL verdict at capture time — plus top-level tag, timestamp, v:1. Verdict source (resolves cross-plan ordering with 05-06, which is Wave 4): read the `verdict` field from each sim's `.deal/evidence/<sim>/metadata.json`, written by the simulate runner with a provisional default of `PARTIAL` until Plan 06's verify engine backfills real PASS/FAIL/PARTIAL values. Do NOT call `crate::verify::evaluate()` from baseline — baseline only transcribes the recorded verdict field, so it never functionally depends on 05-06. Build the manifest with a BTreeMap so keys are alphabetical (D-18) and the artifact is byte-stable. Use write_file_atomic. Author cli/tests/evidence_baseline.rs asserting: a baseline writes manifest.json under evidence/baselines/<tag>/, the manifest contains per-sim content_hash + a verdict field whose value is a valid enum member (PASS | FAIL | PARTIAL), and two baseline runs over identical evidence produce byte-identical manifest.json (D-18). The authoritative end-to-end verdict-correctness assertion lives in Plan 07's smoke test, not here. The Command::Evidence Baseline dispatch arm is already wired to run_evidence_baseline by Plan 01 — implement the function body only (no main.rs edit).
  </action>
  <verify>
    <automated>cargo test -p deal --test evidence_baseline 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p deal --test evidence_baseline exits 0.
    - deal evidence baseline v2.1.0 writes evidence/baselines/v2.1.0/manifest.json with per-sim content_hash, reproducibility_tier, tool, tool_version, and a verdict field that is a valid enum member (PASS | FAIL | PARTIAL), sourced from metadata.json (D-82).
    - Two baseline runs over identical evidence produce byte-identical manifest.json (D-18 byte-stability test passes).
    - manifest.json keys are alphabetical (BTreeMap).
  </acceptance_criteria>
  <done>deal evidence baseline writes a frozen, git-tracked, byte-stable snapshot + manifest with verdicts and hashes (D-82).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working cache to baseline | evidence promoted from gitignored cache into tracked VCS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Repudiation | evidence cache poisoning / undetectable tamper | mitigate | Per-sim SHA-256 content_hash in the git-tracked manifest (D-82/D-83); pre-image resistance sufficient for audit |
| T-05-13 | Tampering | working .deal artifacts accidentally committed | mitigate | .gitignore scopes .deal/ only; git check-ignore assertion in tests confirms the D-81 split |
| T-05-14 | Information Disclosure | non-deterministic manifest leaks ordering into hashes | mitigate | BTreeMap alphabetical keys (D-18); byte-identical-twice test enforces it |
| T-05-SC | Tampering | no new package installs | accept | Uses sha2/walkdir already approved in Plan 01 |
</threat_model>

<verification>
- cargo test -p deal evidence::tests::capture + --test evidence_baseline green.
- gitignore split verified; manifest byte-stable across two runs (D-18).
</verification>

<success_criteria>
- deal evidence capture + baseline tag work (REQ-phase-5-3).
- D-81 clean split (.deal gitignored, evidence/baselines tracked); D-82 manifest with hashes + verdicts; D-18 byte-stable.
</success_criteria>

<output>
Create `.planning/phases/05-simulation-integration/05-05-SUMMARY.md` when done.
</output>
