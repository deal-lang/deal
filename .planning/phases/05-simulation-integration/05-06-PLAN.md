---
phase: 05-simulation-integration
plan: 06
type: execute
wave: 4
depends_on:
  - 05-04
  - 05-05
files_modified:
  - cli/src/verify.rs
  - cli/src/ffi.rs
  - cli/tests/verify_engine.rs
autonomous: true
requirements:
  - REQ-phase-5-3-evidence-verification
  - REQ-phase-5-simulation

must_haves:
  truths:
    - "deal check --verify performs SIM-5 three-level verification: structural completeness, criteria evaluation with PASS/FAIL/PARTIAL + margins, evidence freshness (D-86); the editor hot-path re-runs compute transiently in-process with no per-keystroke disk churn, persisting evidence only on explicit capture (D-74)"
    - "A narrow expression evaluator handles exactly the showcase grammar: >= <= == AND, + - * /, field-path refs, max()"
    - "The per-REQ report answers 'is REQ_BAT_001 verified?' with status + evidence + margins + stale flag (D-87), human-readable + JSON"
    - "Stale evidence reports STALE (orthogonal to PASS/FAIL/PARTIAL) and never auto-runs sims; --run-sims is the explicit opt-in (D-84)"
    - "Unit/dimension compatibility routes through the Zig engine via deal_check_with_stdlib (D-85, no second engine in Rust)"
  artifacts:
    - path: "cli/src/verify.rs"
      provides: "expression evaluator + three-level verdict + per-REQ report"
      contains: "enum Verdict"
    - path: "cli/tests/verify_engine.rs"
      provides: "PASS/FAIL/PARTIAL/STALE + AND + margin tests against the showcase"
      contains: "REQ_BAT_002"
  key_links:
    - from: "cli/src/verify.rs"
      to: ".deal/evidence + evidence/baselines"
      via: "resolve evidence maps to output.json values + staleness compare"
      pattern: "evidence"
    - from: "cli/src/verify.rs"
      to: "deal_check_with_stdlib"
      via: "FFI dimension compatibility check (D-85)"
      pattern: "deal_check_with_stdlib"
---

<objective>
Wave 4 (depends on the C ABI dimension seam from Plan 04 and the evidence cache from Plan 05): Build the `deal check --verify` evaluation engine. Walk the IR satisfy/criteria/compute/evidence nodes, evaluate the narrow showcase expression grammar (Claude's-discretion scope, locked by RESEARCH Pattern 5), resolve evidence bindings to captured output.json values, compute staleness against recorded hashes, route unit/dimension compatibility through the Zig engine via `deal_check_with_stdlib` (D-85), and emit the SIM-5 three-level verdict (PASS/FAIL/PARTIAL + orthogonal STALE, D-86) as a per-requirement report (D-87) in both human-readable and JSON form.

Purpose: This is the PM query (SC#5): "is REQ_BAT_001 verified?" answered by ID with satisfaction status, supporting evidence, margins, and stale flags. It is the verification half of the phase goal.
Output: Working `deal check --verify` + per-REQ report + tests covering PASS/FAIL/PARTIAL/STALE/AND/margins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-simulation-integration/05-CONTEXT.md
@.planning/phases/05-simulation-integration/05-RESEARCH.md
@.planning/phases/05-simulation-integration/05-PATTERNS.md
@spec/examples/showcase/model/traceability.dealx

<interfaces>
Expression grammar (RESEARCH Pattern 5 — the COMPLETE locked scope; defer everything else per D-55 precedent):
  comparisons >= <= == ; logical AND (no OR/NOT in showcase) ; arithmetic + - * / ;
  field-path refs (dot-separated, e.g. REQ_SYS_001.minRange, EnergyStorage.battery.usableCapacity) ;
  one stdlib call max() (REQ_BAT_002 compute). Implement as a recursive-descent evaluator over IR values
  (IR is already structured — NOT a parser); field-path resolution walks the merged IR elements map; max() special-cased.

Verdict rubric (D-86): PASS = all criteria true AND evidence complete AND fresh; FAIL = a criterion false;
  PARTIAL = explicit status="partial" OR unmapped/missing return fields OR a declared gap{} block.
  STALE (D-84) is a SEPARATE flag layered on top, never folded into PARTIAL.

Showcase oracle cases (traceability.dealx): REQ_BAT_001 PASS (design evidence); REQ_BAT_002 PARTIAL
  (status="partial" + gap block, max() in compute); REQ_BAT_003 AND criteria (maxTemp AND minTemp).

Reuse (PATTERNS.md verify.rs section): cli/src/sysml_v2.rs emit/emit_from_bytes IR-walk; D-32 JSON envelope
  (cli/src/main.rs:392-430); Verdict enum #[serde(rename_all="SCREAMING_SNAKE_CASE")]; C ABI Call + Clone-Before-Free
  for deal_check_with_stdlib (ffi.rs extern already added in Plan 04).

D-84: --verify reports STALE and exits non-zero in gate mode; --run-sims is the only way to re-run stale sims
  (calls crate::simulate). NEVER auto-execute on stale.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expression evaluator + field-path resolution (narrow showcase grammar)</name>
  <files>cli/src/verify.rs, cli/tests/verify_engine.rs</files>
  <read_first>
    - spec/examples/showcase/model/traceability.dealx (all 8 satisfy blocks — the exact criteria/compute grammar in use)
    - .planning/phases/05-simulation-integration/05-RESEARCH.md (Pattern 5 grammar inventory table — the complete locked operator set)
    - cli/src/verify.rs (the Plan 01 stub to flesh out)
    - cli/src/sysml_v2.rs (IR-walk entry pattern: elements map access, emit/emit_from_bytes)
    - .planning/phases/05-simulation-integration/05-PATTERNS.md (verify.rs section — IR-walk + Verdict enum)
  </read_first>
  <behavior>
    - Test: `actualRange >= REQ_SYS_001.minRange` evaluates true/false from resolved IR + output values.
    - Test: AND criterion `actualMaxTemp <= REQ_BAT_003.maxTemp AND actualMinTemp >= REQ_BAT_003.minTemp` (REQ_BAT_003).
    - Test: compute `margin = actualRange - REQ_SYS_001.minRange` and `marginPercent = margin / REQ_SYS_001.minRange * 100` yield correct numbers.
    - Test: `max(a, b, c)` returns the maximum (REQ_BAT_002 worstCase).
    - Test: a field-path ref that does not resolve yields an unmapped signal (feeds PARTIAL, not a panic).
    - Test: an unsupported operator (e.g. OR) is rejected with a clear "unsupported in showcase grammar" error (scope guard, D-55 precedent).
  </behavior>
  <action>
    Flesh out cli/src/verify.rs with a recursive-descent evaluator over the IR criteria/compute expression nodes implementing EXACTLY: comparisons >= <= ==, logical AND, arithmetic + - * /, dot-separated field-path resolution against the merged IR elements map + the evidence output.json values, and a special-cased max() built-in. Reject any operator outside this set with an explicit "unsupported in showcase grammar (deferred per D-55)" error. Surface unresolved field-paths as an unmapped signal (consumed by the PARTIAL rule in Task 2), never a panic. Add the Verdict enum (Pass/Fail/Partial, SCREAMING_SNAKE_CASE serde). Make the evaluator unit tests in cli/tests/verify_engine.rs green.
  </action>
  <verify>
    <automated>cargo test -p deal verify::tests::and_criteria verify::tests::margin verify::tests::max_call 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p deal verify::tests for and_criteria, margin, and max_call exit 0.
    - The evaluator handles >= <= == AND + - * / field-path max() and rejects anything else with an explicit scope error.
    - An unresolved field-path produces an unmapped signal (no panic).
    - Verdict enum serializes as SCREAMING_SNAKE_CASE.
  </acceptance_criteria>
  <done>Narrow showcase expression evaluator + field-path resolution implemented and unit-tested; scope guard enforces the locked grammar.</done>
</task>

<task type="auto">
  <name>Task 2: Three-level verdict + STALE + Zig dimension check + per-REQ report</name>
  <files>cli/src/verify.rs, cli/src/ffi.rs, cli/tests/verify_engine.rs</files>
  <read_first>
    - cli/src/verify.rs (Task 1 output — evaluator + Verdict enum)
    - cli/src/ffi.rs (deal_check_with_stdlib extern added in Plan 04)
    - cli/src/main.rs (lines 392-430 D-32 JSON envelope; the FFI Clone-Before-Free call site)
    - .planning/phases/05-simulation-integration/05-RESEARCH.md (D-84 STALE-not-run; D-85 Zig dimension via C ABI; D-86 rubric; D-87 report shape Code Example)
    - .planning/phases/05-simulation-integration/05-PATTERNS.md (C ABI Call + Clone-Before-Free; D-32 envelope; verify.rs report shape)
    - spec/examples/showcase/model/traceability.dealx (REQ_BAT_001 PASS, REQ_BAT_002 PARTIAL+gap, REQ_BAT_003 AND — the verdict oracle)
    - evidence/baselines/ + .deal/evidence/ layout (from Plan 05 — the evidence the engine resolves)
  </read_first>
  <action>
    Extend cli/src/verify.rs into evaluate(ir_json, evidence_dir) -> VerifyReport implementing SIM-5 three levels: (L1) structural completeness — every criterion has a mapped evidence binding; (L2) criteria evaluation producing PASS/FAIL/PARTIAL per D-86 (PARTIAL when status="partial", or unmapped/missing return fields, or a gap{} block present) with margins surfaced from compute{} values; (L3) freshness — recompute the staleness key (Plan 03 compute_staleness_key) and compare to the recorded evidence hash, setting an orthogonal stale:bool flag (D-86), never folding into PARTIAL. For unit/dimension compatibility between criteria operands route through deal_check_with_stdlib via FFI (D-85, clone-before-free) — no Rust-side dimension engine. Build the per-REQ report keyed by REQ_* id (D-87) with verdict, stale, criteria_results, compute_results (margins), evidence bindings, and a summary {pass,fail,partial,stale}; emit both human-readable and a D-32 JSON envelope (command="verify", deal_version, requirements, summary, v:1). Honor D-74 (transient-live): --verify re-runs compute in-process transiently and never writes durable evidence artifacts (no per-keystroke disk churn); durable writes happen only on simulate/capture/gate. Honor D-84: when stale and NOT --run-sims, report STALE and exit non-zero in gate mode; when --run-sims, call crate::simulate to re-run stale sims first. The Check{verify, simulations, run_sims} dispatch arm is already wired by Plan 01 — implement evaluate() and the report only (no main.rs edit; ffi.rs extern was added in Plan 04). Make cli/tests/verify_engine.rs fully green (pass_verdict, partial_verdict, stale_detection).
  </action>
  <verify>
    <automated>cargo build -p deal 2>&1 | tail -3 && cargo test -p deal --test verify_engine 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p deal --test verify_engine exits 0 (pass_verdict, partial_verdict, stale_detection, and_criteria all green).
    - REQ_BAT_001 yields PASS; REQ_BAT_002 yields PARTIAL (status=partial + gap block); a hash mismatch yields STALE as a separate flag (not PARTIAL).
    - Stale + no --run-sims reports STALE and exits non-zero in gate mode; sims are NOT auto-run (D-84).
    - Dimension compatibility flows through deal_check_with_stdlib FFI (grep shows the call; no Rust dimension algebra).
    - JSON report is a D-32 envelope keyed by REQ_* with verdict/stale/margins; human-readable form answers "is REQ_BAT_001 verified?".
  </acceptance_criteria>
  <done>deal check --verify produces the SIM-5 three-level per-REQ report with PASS/FAIL/PARTIAL + orthogonal STALE, margins, and Zig-backed dimension checks; STALE never auto-runs (D-84).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence files to verify engine | captured output.json values (untrusted on disk) drive criteria |
| Rust verify to Zig C ABI | criteria operands cross into deal_check_with_stdlib for dimension check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Elevation of Privilege | silent sim execution during verify | mitigate | D-84 — verify NEVER auto-runs; stale reports STALE; --run-sims is the explicit opt-in calling crate::simulate |
| T-05-16 | Tampering | reusing stale evidence as if fresh | mitigate | L3 freshness recomputes the SHA-256 staleness key (D-83) and flags STALE on mismatch |
| T-05-17 | Tampering | use-after-free across the dimension-check FFI | mitigate | Clone diagnostics bytes before deal_free (Clone-Before-Free pattern) |
| T-05-SC | Tampering | no new package installs | accept | Reuses sha2 + the Plan 04 FFI export; no registry installs |
</threat_model>

<verification>
- cargo test -p deal --test verify_engine green (PASS/FAIL/PARTIAL/STALE/AND/margins).
- Dimension checks via deal_check_with_stdlib; STALE never auto-runs (D-84).
- Per-REQ JSON + human report answer the PM query by ID.
</verification>

<success_criteria>
- deal check --verify three-level SIM-5 verification with PASS/FAIL/PARTIAL + STALE + margins (REQ-phase-5-3, D-86).
- Narrow showcase expression grammar only (D-55 precedent).
- Zig owns dimensions via C ABI (D-85); per-REQ report answers SC#5 (D-87).
</success_criteria>

<output>
Create `.planning/phases/05-simulation-integration/05-06-SUMMARY.md` when done.
</output>
