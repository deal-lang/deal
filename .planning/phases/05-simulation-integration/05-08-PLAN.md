---
phase: "05"
plan: "05-08"
wave: 6
gap_closure: true
autonomous: false
depends_on: ["05-06", "05-07"]
status: pending
objective: >
  Gap closure for the 05-07 human-verify checkpoint. The Phase 5 verify engine
  reports all-PARTIAL against the real showcase because it never resolves
  model-backed evidence to IR values, and the simulate orchestrator cannot
  resolve sim input model_paths (FQPN mismatch + value-less showcase attributes).
  Close BOTH so `deal check --verify` against the showcase yields real verdicts
  (REQ_BAT_001 = PASS), with sims actually running end-to-end.
files_modified:
  - cli/src/verify.rs
  - cli/src/simulate.rs
  - cli/src/ffi.rs
  - cli/tests/verify_engine.rs
  - cli/tests/phase5_e2e.rs
  - scripts/phase-5-smoke.sh
  - spec/examples/showcase/packages/vehicle/battery.deal   # (spec submodule — input values)
  - spec/examples/showcase/packages/vehicle/motor.deal     # (spec submodule — input values)
truths:
  - The showcase model carries the design values needed for analysis/design
    requirements (usableCapacity = kWh(85) at battery.deal:99; minCapacity =
    kWh(85) at system.deal:61), so REQ_BAT_001's criterion `actualCapacity >=
    REQ_BAT_001.minCapacity` is 85 >= 85 = PASS once resolved (D-85, D-87).
  - The verify engine (verify.rs::parse_satisfy_block) only handles
    `evidence simulation { binding: "<sim>" }` and reads only
    `.deal/evidence/<binding>/output.json`; it never parses
    `evidence design/analysis/test { maps { <model_path> -> <field> } }` nor
    resolves `REQ_X.attr` — the root cause of all-PARTIAL (D-86, D-87).
  - simulate.rs::resolve_model_path does a direct key lookup of the registry
    model_path against the merged IR `elements` map; the showcase model_paths
    (`EnergyStorage.battery.packResistance`) do not match the IR element keys
    AND those attributes are value-less (D-71, D-83).
---

# Plan 05-08 — Gap closure: model-IR evidence + simulation input resolution

## Context (diagnosis from the 05-07 human checkpoint)

Running the real PM-query against the showcase
(`deal check --verify model/traceability.dealx`) returns
`0 PASS, 0 FAIL, 8 PARTIAL, 0 STALE / 8 total`. Two independent root causes,
both masked by isolated unit fixtures:

1. **Verify engine has no model-IR resolution.** It only reads sim `output.json`
   via a `binding:` key. Showcase requirements use model-backed evidence
   (`evidence design { maps { EnergyStorage.battery.usableCapacity -> actualCapacity } }`)
   and requirement-attribute refs (`REQ_BAT_001.minCapacity`). Neither is
   resolved → every operand is `Unmapped` → PARTIAL.

2. **Simulate cannot resolve sim inputs.** `resolve_model_path` direct-key-lookup
   fails for the registry model_paths, and the referenced showcase attributes
   (`packResistance`, `totalCurrent`, `coolantSupply.flowRate`, motor inputs)
   have no values → battery_thermal fails input validation (`expected Real, got
   NoneType`).

## Task 1 — Verify engine: resolve model-backed evidence to IR values

In `cli/src/verify.rs` (+ `cli/src/ffi.rs` if a new accessor is needed):

- Parse `evidence design/analysis/test/simulation { maps { <src> -> <field> } }`
  blocks (not just `binding:`). Capture every `src -> field` pair.
- Obtain the workspace IR the same way `simulate.rs::load_project_ir` does
  (`deal_parse` → `deal_ir_json`, merge `elements`). Factor a shared helper if
  practical.
- Resolve a `<src>` that is a model path (e.g.
  `EnergyStorage.battery.usableCapacity`) to its numeric value, and a `<src>`
  that is a sim-output field to the captured `output.json` value.
- Resolve requirement-attribute refs in criteria/compute (`REQ_BAT_001.minCapacity`)
  to the requirement def's attribute value.
- Handle unit-call literal values (`kWh(85)`, `ohm(0.05)`): resolve to a number
  consistently for both sides of a comparison (same canonicalization on both
  operands so `85 kWh >= 85 kWh` compares true). Document the chosen convention.
- Populate `EvalContext` with the resolved values so criteria evaluate to real
  PASS/FAIL; keep `Unmapped` → PARTIAL only for genuinely missing data.

**FIRST determine the actual IR element key naming** (dump the merged elements
map for the showcase) so model_path ↔ IR-key resolution is correct rather than
assumed. Use the same matching strategy in both verify.rs and simulate.rs.

## Task 2 — Simulate: resolve sim inputs end-to-end

- Make `resolve_model_path` actually resolve the registry model_paths against the
  IR element keys (fix the FQPN-matching: align the registry path form with the
  IR key form — suffix/qualified-id match as needed; do not weaken the T-05-06
  path-alphabet guard).
- Add concrete input values to the value-less showcase attributes the sims
  consume, in the `spec` submodule
  (`packResistance`, `totalCurrent`, `coolantSupply.flowRate`, and the motor
  inputs). Commit INSIDE the submodule, then bump the parent gitlink. The new
  spec commit supersedes the temporary `1fb43ca` pin from `7cc0def` — UPDATE the
  Phase-6 recall note (todo `2026-06-08-implement-calc-constraint-grammar-in-zig.md`
  §Spec submodule recall + STATE.md Deferred Items) to the new commit hash.
- `deal simulate --all` against the showcase must run battery_thermal (and any
  other Python sims) for real and produce schema-valid output.json; MATLAB sims
  still graceful-skip (D-72).

## Task 3 — Real end-to-end acceptance + tighten the gate

- Update `cli/tests/phase5_e2e.rs` to assert REAL verdicts (not mere presence):
  REQ_BAT_001 = PASS, REQ_BAT_002 = PARTIAL (its scheduled gap), and at least one
  sim-backed requirement resolves to a real verdict after the sim runs.
- Tighten `scripts/phase-5-smoke.sh`: the `deal check --verify` step must FAIL
  the smoke if REQ_BAT_001 is not PASS (remove the `|| true` swallow on verdict
  correctness; keep presence checks too).
- `cargo test --workspace` stays green; `bash scripts/phase-5-smoke.sh` exits 0
  on real verdicts.

## Acceptance (the real PM-query, not fixtures)

1. `deal simulate --all` against the showcase: battery_thermal runs for real,
   produces output.json; MATLAB graceful-skips.
2. `deal check --verify model/traceability.dealx` against the showcase:
   **REQ_BAT_001 = PASS** (85 kWh ≥ 85 kWh) with traceable supporting criterion +
   margin; REQ_BAT_002 = PARTIAL with its gap; summary is no longer
   "0 PASS / 8 PARTIAL".
3. `deal check --verify --json` keys per REQ with real verdicts/margins.
4. `cargo test --workspace` green; `scripts/phase-5-smoke.sh` exits 0 and now
   gates verdict correctness.
5. spec submodule changes committed inside the submodule + gitlink bumped; Phase-6
   recall note updated to the new spec commit.

## Human checkpoint

Re-run the 05-07 SC#5 human-verify against the corrected report.
