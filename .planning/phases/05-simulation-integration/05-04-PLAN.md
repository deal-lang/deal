---
phase: 05-simulation-integration
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/sim/deal_sim.zig
  - src/sim/zig_runner.zig
  - src/sim/range_model.zig
  - build.zig
  - cli/src/main.rs
  - cli/src/ffi.rs
  - cli/tests/e2500_cli.rs
  - .planning/decisions/ADR-phase-5-matlab-subprocess.md
autonomous: true
requirements:
  - REQ-phase-5-1-deal-sim-sdk
  - REQ-phase-5-2-orchestration
  - REQ-phase-5-3-evidence-verification
  - REQ-phase-5-simulation

must_haves:
  truths:
    - "deal_sim.zig lives in the deal repo and provides a comptime DealSimulation wrapper that applies @setFloatMode from the declared @reproducibility tier (D-73/D-75/D-80)"
    - "A canonical Zig sim runs in-process AND serializes the same run to schema-valid evidence artifacts (one execution, two outputs)"
    - "deal check emits error[E2500] via the CLI for a cross-file dimension mismatch (D-88 carryover)"
    - "An ADR records that ROADMAP SC#2's MATLAB Engine-API wording is superseded by subprocess -batch (D-72)"
  artifacts:
    - path: "src/sim/deal_sim.zig"
      provides: "Zig DealSimulation comptime wrapper + @setFloatMode + sim_run export"
      contains: "setFloatMode"
    - path: "src/sim/zig_runner.zig"
      provides: "in-process call + JSON evidence serialization"
      contains: "pub fn"
    - path: "cli/tests/e2500_cli.rs"
      provides: "D-88 cross-file E2500 CLI integration test"
      contains: "E2500"
    - path: ".planning/decisions/ADR-phase-5-matlab-subprocess.md"
      provides: "Records D-72 supersession of ROADMAP SC#2 Engine-API wording"
      min_lines: 15
  key_links:
    - from: "cli/src/main.rs run_check"
      to: "src/lib.zig deal_check_with_stdlib"
      via: "FFI call replacing per-file deal_parse for cross-file dimension check (D-88)"
      pattern: "deal_check_with_stdlib"
    - from: "src/sim/deal_sim.zig"
      to: "@setFloatMode"
      via: "comptime tier dispatch from @reproducibility annotation"
      pattern: "setFloatMode"
---

<objective>
Track C: Ship the Zig simulation path and close the D-88 E2500 CLI carryover. Build `deal_sim.zig` (the comptime `DealSimulation(T)` wrapper that sets `@setFloatMode` from the declared `@reproducibility` tier per D-73/D-75 and exports a C-callable `sim_run`), `zig_runner.zig` (in-process call + serialize the same run to schema-valid evidence — one execution, two outputs), and one canonical `range_model.zig` Zig sim to prove the path. Then wire the new `deal_check_with_stdlib` C ABI export (landed in Plan 01) into `deal check` so cross-file dimensional E2500 errors surface via the CLI (D-88). Record the D-72 MATLAB-subprocess ADR.

Purpose: D-73/D-80 make Zig a first-class sim language (the strongest proof the protocol is language-neutral). D-88 closes the Phase 4 SC-1 deferred gap: the dimensional algebra is proven in sema_dimensional.zig but deal check does not yet emit E2500 cross-file via the CLI because the single-file path registers imported units as .imported. The fix is the analyzeWithExternalTable wiring — the same seam D-85's verify engine needs.
Output: A working in-process Zig sim + E2500 surfaced via deal check + ADR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-simulation-integration/05-CONTEXT.md
@.planning/phases/05-simulation-integration/05-RESEARCH.md
@.planning/phases/05-simulation-integration/05-PATTERNS.md
@.planning/decisions/ADR-deal-stdlib-numeric-model.md

<interfaces>
src/lib.zig (Plan 01) — pub export fn deal_check_with_stdlib(source, filename, stdlib_ir, out_diag) bool wrapping analyzeWithExternalTable. Rust calls it via FFI; clone bytes before deal_free.

tests/unit/sema_dimensional.zig — proven E2500..E2503 algebra harness; shows how the external (stdlib) unit table is seeded. D-88 acceptance scenario (04-HUMAN-UAT Test 2): import deal.std.units kg V then attribute mass : Mass = V(800) — V is Voltage, mass is Mass, so E2500.

cli/src/main.rs run_check (~line 229 comment: "the current C ABI processes each file independently via deal_parse") — replace the per-file path with the deal_check_with_stdlib call when stdlib deps are resolved (D-49 already builds the merged stdlib table for the workspace index).

ADR-deal-stdlib-numeric-model.md — reproducibility tiers strict/upsize-f128/kahan/tolerant map to @setFloatMode/storage. Phase 5 implements ONLY deferred-decision #5 (tier to orchestrator). @setFloatMode(.strict) is the default (D-73).

PATTERNS.md src/sim/deal_sim.zig + src/sim/zig_runner.zig sections — comptime DealSimulation(T) wrapper + JSON serialization via src/json.zig; build.zig must add the sim source to the libdeal step (RESEARCH Open Q1: add to existing lib step, not a second link target).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deal_sim.zig comptime wrapper + zig_runner + canonical range_model.zig</name>
  <files>src/sim/deal_sim.zig, src/sim/zig_runner.zig, src/sim/range_model.zig, build.zig</files>
  <read_first>
    - .planning/decisions/ADR-deal-stdlib-numeric-model.md (reproducibility tiers to @setFloatMode mapping; deferred-decision #5 scope; A-001)
    - src/sema.zig (lines 240-315 — Zig module pub-fn pattern called from lib.zig)
    - src/lib.zig (lines 533-577 deal_ir_json — JSON serialization via src/json.zig; arena pattern)
    - .planning/phases/05-simulation-integration/05-PATTERNS.md (deal_sim.zig + zig_runner.zig sections; @setFloatMode usage)
    - .planning/phases/05-simulation-integration/05-RESEARCH.md (Pattern 7 in-process Zig mechanism; Open Q1 single libdeal.a vs separate object)
    - spec/examples/showcase/simulations/dynamics/range_model.py (the physics to mirror in the canonical Zig sim)
    - build.zig (the libdeal library step to add the sim source to)
  </read_first>
  <action>
    Create src/sim/deal_sim.zig with pub fn DealSimulation(comptime T: type) type returning a struct that exports sim_run(input_json, input_len, out_json, out_len) callconv(.C) bool: read the comptime T.reproducibility decl (default .strict per D-73), apply @setFloatMode(mode) at function scope before any arithmetic (D-75), deserialize input.json into T's input type, call T.run(...), serialize the output + a metadata envelope (recording the declared tier, D-75) to arena-allocated JSON written to out_json/out_len, return success. Create src/sim/zig_runner.zig exposing pub fn run_zig_sim(...) that calls the sim in-process AND serializes the same run's input/output/metadata to schema-valid .deal/evidence/<name>/ artifacts (D-73 one-execution-two-outputs) — reuse the src/json.zig serialization pattern. Create one canonical src/sim/range_model.zig mirroring range_model.py's physics, declaring reproducibility = .strict. Add the sim sources to the libdeal step in build.zig (RESEARCH Open Q1 — extend existing lib step, compile-time flagged). Add a Zig test asserting sim_run is bit-reproducible across two calls (D-73 strict to bit-reproducible).
  </action>
  <verify>
    <automated>zig build 2>&1 | tail -3 && zig build test -Dtest-filter=sim 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - zig build exits 0 with src/sim/*.zig compiled into the libdeal step.
    - zig build test -Dtest-filter=sim exits 0, including a bit-reproducibility assertion across two sim_run calls (D-73 strict).
    - grep -c "setFloatMode" src/sim/deal_sim.zig >= 1 and the mode derives from T.reproducibility (default .strict).
    - range_model.zig declares reproducibility = .strict and implements run().
    - zig_runner serializes evidence artifacts to .deal/evidence/<name>/ (D-73 second output).
  </acceptance_criteria>
  <done>Zig DealSimulation comptime wrapper + in-process runner + canonical range_model.zig compile and pass a bit-reproducibility test; float mode bound to @reproducibility tier.</done>
</task>

<task type="auto">
  <name>Task 2: Wire deal_check_with_stdlib into deal check (D-88 E2500 CLI) + MATLAB ADR</name>
  <files>cli/src/ffi.rs, cli/src/main.rs, cli/tests/e2500_cli.rs, .planning/decisions/ADR-phase-5-matlab-subprocess.md</files>
  <read_first>
    - cli/src/main.rs (run_check ~line 229 "processes each file independently" comment; the FFI call site ~300-352; the D-49 merged stdlib table build for the workspace index ~441-468)
    - cli/src/ffi.rs (the extern block declaring deal_* symbols — add deal_check_with_stdlib)
    - src/lib.zig (the deal_check_with_stdlib export landed in Plan 01 — its exact arg shape)
    - tests/unit/sema_dimensional.zig (the proven E2500 algebra + how the stdlib table is seeded — the oracle for the CLI test)
    - cli/tests/e2500_cli.rs (the red stub from Plan 01 to make green — un-ignore it)
    - .planning/phases/05-simulation-integration/05-RESEARCH.md (Pitfall 1; State-of-the-Art "update run_check comment"; D-88 acceptance)
    - .planning/phases/05-simulation-integration/05-PATTERNS.md (C ABI Call + Clone-Before-Free shared pattern)
    - ROADMAP.md (Phase 5 SC#2 "MATLAB Engine API" wording the ADR supersedes; D-72)
  </read_first>
  <action>
    In cli/src/ffi.rs add the deal_check_with_stdlib extern declaration matching the Plan 01 export. In cli/src/main.rs run_check, when stdlib deps are resolved (D-49 merged table already built), replace the per-file deal_parse dimension-check path with a call to deal_check_with_stdlib(source, filename, stdlib_ir_bytes, &out_diag) — clone the diagnostics bytes BEFORE deal_free (Clone-Before-Free pattern), parse them into the existing diagnostic envelope, and surface E2500 with the standard non-zero exit. Update the obsolete ~line 229 comment ("processes each file independently") to note cross-file dimension checking now flows through analyzeWithExternalTable. Make cli/tests/e2500_cli.rs green: the 04-HUMAN-UAT Test 2 scenario (import deal.std.units kg V then attribute mass : Mass = V(800)) makes deal check exit non-zero and the stderr/JSON contains error[E2500]. Write .planning/decisions/ADR-phase-5-matlab-subprocess.md recording that ROADMAP SC#2's "MATLAB Engine API for Python" wording is superseded by D-72 (subprocess matlab -batch default; Engine API a future pluggable adapter).
  </action>
  <verify>
    <automated>cargo build -p deal 2>&1 | tail -3 && cargo test -p deal --test e2500_cli 2>&1 | tail -6 && test -f .planning/decisions/ADR-phase-5-matlab-subprocess.md && echo ADR_OK</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p deal --test e2500_cli exits 0 — deal check exits non-zero and emits error[E2500] for the cross-file Mass = V(800) scenario.
    - cli/src/ffi.rs declares deal_check_with_stdlib; cli/src/main.rs calls it and clones diagnostics before deal_free.
    - The obsolete "processes each file independently" comment in run_check is updated.
    - .planning/decisions/ADR-phase-5-matlab-subprocess.md exists (>= 15 lines) recording the D-72 supersession of ROADMAP SC#2.
    - cargo build -p deal exits 0.
  </acceptance_criteria>
  <done>deal check surfaces cross-file E2500 via the new C ABI (D-88 closed); MATLAB-subprocess ADR recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust CLI to Zig C ABI | source + stdlib IR bytes cross into deal_check_with_stdlib |
| in-process Zig sim to evidence | sim_run output serialized to evidence artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | use-after-free of arena diagnostics across FFI | mitigate | Clone diagnostics bytes BEFORE deal_free (Clone-Before-Free pattern); the live copy drives the envelope |
| T-05-11 | Information Disclosure | non-reproducible in-process float results in evidence | mitigate | @setFloatMode(.strict) default makes the Zig fast path bit-reproducible + authoritative (D-73); tier recorded in metadata |
| T-05-SC | Tampering | no new package installs in this plan | accept | Plan adds only repo-local Zig/Rust source; no registry installs |
</threat_model>

<verification>
- zig build + zig build test -Dtest-filter=sim green (in-process Zig sim + bit-reproducibility).
- cargo test -p deal --test e2500_cli green (D-88 cross-file E2500 via CLI).
- MATLAB-subprocess ADR present.
</verification>

<success_criteria>
- Zig is a first-class sim language: comptime DealSimulation + @setFloatMode tier + in-process evidence (D-73/D-75/D-80).
- deal check emits cross-file E2500 via the CLI (D-88 carryover closed).
- ROADMAP SC#2 Engine-API supersession recorded in an ADR (D-72).
</success_criteria>

<output>
Create `.planning/phases/05-simulation-integration/05-04-SUMMARY.md` when done.
</output>
