---
phase: 05-simulation-integration
plan: 07
type: execute
wave: 5
depends_on:
  - 05-02
  - 05-03
  - 05-04
  - 05-05
  - 05-06
files_modified:
  - scripts/phase-5-smoke.sh
  - build.zig
  - cli/src/main.rs
  - cli/tests/phase5_e2e.rs
autonomous: false
requirements:
  - REQ-phase-5-gate
  - REQ-phase-5-simulation
  - REQ-phase-5-2-orchestration
  - REQ-phase-5-3-evidence-verification

must_haves:
  truths:
    - "phase-5-smoke.sh runs the full showcase chain: deal simulate --all, deal evidence capture, deal check --verify"
    - "deal check --simulations validates MATLAB bindings structurally without executing MATLAB (D-72 hard-gate path)"
    - "zig build phase-5-gate exits 0 with Python + Zig sims running for real and MATLAB graceful-skipped"
    - "zig build phase-5-gate-fresh exits 0 in an ephemeral worktree (deal-sim materialized + pip-installed)"
    - "A program manager can run deal check --verify and get a traceable per-REQ answer for REQ_BAT_001"
  artifacts:
    - path: "scripts/phase-5-smoke.sh"
      provides: "end-to-end showcase smoke: simulate, capture, verify"
      contains: "deal check --verify"
    - path: "cli/tests/phase5_e2e.rs"
      provides: "end-to-end test asserting the full chain green on the showcase"
      min_lines: 20
  key_links:
    - from: "scripts/phase-5-smoke.sh"
      to: "deal simulate --all + deal check --verify"
      via: "release binary invoked over the showcase model"
      pattern: "simulate"
    - from: "build.zig phase-5-gate"
      to: "scripts/phase-5-smoke.sh"
      via: "gate step dependsOn smoke"
      pattern: "phase-5-smoke"
---

<objective>
Wave 5 convergence (depends on all tracks): Wire the end-to-end `phase-5-gate` against the showcase model. Fill the `scripts/phase-5-smoke.sh` placeholder (created in Plan 01) with the full chain — `deal simulate --all` (Python + Zig run for real, MATLAB graceful-skips), `deal evidence capture`, `deal evidence baseline v2.1.0`, `deal check --simulations` (structural MATLAB binding validation), and `deal check --verify` (per-REQ report green). Add an end-to-end Rust test, finalize the gate, and run the human-verify checkpoint confirming the PM-query goal.

Purpose: This proves the phase goal: the showcase verification chain is executable end-to-end and a PM gets a traceable "is REQ_BAT_001 verified?" answer. It is the REQ-phase-5-gate exit criterion.
Output: A green `phase-5-gate` + `phase-5-gate-fresh` + an e2e test + the recorded human-verify result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-simulation-integration/05-CONTEXT.md
@.planning/phases/05-simulation-integration/05-RESEARCH.md
@.planning/phases/05-simulation-integration/05-PATTERNS.md

<interfaces>
All Phase 5 commands exist after Plans 02-06: deal simulate <name>/--all/--stale; deal evidence capture;
deal evidence baseline <tag>; deal check --simulations / --verify / --verify --run-sims.

Hard gate vs graceful (D-72): Python + Zig sims MUST run for real and pass; MATLAB is graceful-skip when
absent (skip.json written, no hard fail). deal check --simulations validates the MATLAB bindings structurally
(no execution) so the MATLAB path is proven without a license. MATLAB-backed criteria report STALE under --verify.

Gate scaffolding (Plan 01): build.zig phase-5-gate dependsOn phase-4-gate (D-35) + cargo test --workspace +
deal-sim pip install + python tests + scripts/phase-5-smoke.sh (placeholder to fill here). phase-5-gate-fresh
calls scripts/verify-fresh-worktree.sh (already extended for deal-sim in Plan 01).

Showcase model root for the smoke: spec/examples/showcase/ (deal.sims.toml + model + traceability.dealx).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill phase-5-smoke.sh + e2e test + finalize gate</name>
  <files>scripts/phase-5-smoke.sh, build.zig, cli/tests/phase5_e2e.rs, .planning/ROADMAP.md</files>
  <read_first>
    - scripts/phase-5-smoke.sh (the Plan 01 placeholder to fill)
    - scripts/phase-3-smoke.sh (existing smoke-script precedent — structure + release-binary invocation)
    - build.zig (the phase-5-gate steps from Plan 01 — confirm dependency wiring)
    - spec/examples/showcase/simulations/deal.sims.toml (the registry the smoke runs against)
    - spec/examples/showcase/model/traceability.dealx (the verify oracle the report covers)
    - .planning/phases/05-simulation-integration/05-RESEARCH.md (Pitfall 5 MATLAB-absent gate; D-72 hard-gate = Python+Zig real, MATLAB structural)
  </read_first>
  <action>
    Fill scripts/phase-5-smoke.sh: build the release binary, then over spec/examples/showcase/ run (1) deal simulate --all — assert Python + the canonical Zig sim produce schema-valid output.json and MATLAB sims write skip.json (graceful-skip, not failure); (2) deal evidence capture; (3) deal evidence baseline v2.1.0 — assert evidence/baselines/v2.1.0/manifest.json exists; (4) deal check --simulations — assert MATLAB bindings validate structurally with exit 0; (5) deal check --verify — assert exit reflects the showcase verdicts and the per-REQ report contains REQ_BAT_001 with a verdict, and REQ_BAT_002 PARTIAL. Use set -euo pipefail and clear FAIL/PASS echoes. Author cli/tests/phase5_e2e.rs invoking the release binary over a temp copy of the showcase and asserting the full chain. Confirm build.zig phase-5-gate dependsOn the smoke + cargo test --workspace + deal-sim install; confirm phase-5-gate-fresh wiring. Update ROADMAP.md Phase 5 SC#2 note to reference the D-72 ADR (MATLAB subprocess, not Engine API).
  </action>
  <verify>
    <automated>bash -n scripts/phase-5-smoke.sh && cargo test -p deal --test phase5_e2e 2>&1 | tail -8 && zig build phase-5-gate 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - bash -n scripts/phase-5-smoke.sh parses clean; the script runs simulate/capture/baseline/check --simulations/check --verify over the showcase.
    - cargo test -p deal --test phase5_e2e exits 0.
    - zig build phase-5-gate exits 0: Python + Zig sims run for real; MATLAB graceful-skips (skip.json, no hard fail).
    - deal check --simulations exits 0 (structural MATLAB binding validation).
    - The verify report contains REQ_BAT_001 (verdict) and REQ_BAT_002 (PARTIAL).
  </acceptance_criteria>
  <done>phase-5-smoke.sh + e2e test + phase-5-gate all green; full showcase chain executable end-to-end with MATLAB graceful-skip.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh-worktree gate + closeout</name>
  <files>scripts/verify-fresh-worktree.sh, build.zig</files>
  <read_first>
    - scripts/verify-fresh-worktree.sh (the deal-sim symlink + pip-install added in Plan 01 — confirm it works)
    - build.zig (phase-5-gate-fresh step from Plan 01)
    - .planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md (the invariant the fresh gate must satisfy)
    - .planning/phases/05-simulation-integration/05-RESEARCH.md (Pitfall 2 deal-sim sibling materialization)
  </read_first>
  <action>
    Run zig build phase-5-gate-fresh and resolve any sibling-materialization gaps (deal-sim symlink present, pip install -e ../deal-sim succeeds inside the ephemeral worktree so Python sims import deal_sim). Confirm the fresh gate inherits phase-4-gate-fresh behavior and exits 0. If the showcase baseline (evidence/baselines/v2.1.0/) should be committed as the frozen V&V reference, ensure it is tracked (not gitignored). Record any deviations in the SUMMARY.
  </action>
  <verify>
    <automated>zig build phase-5-gate-fresh 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - zig build phase-5-gate-fresh exits 0 in an ephemeral worktree.
    - deal-sim is symlinked and pip-installed in the fresh environment (no ModuleNotFoundError: deal_sim).
    - evidence/baselines/v2.1.0/ is git-tracked if committed as the frozen reference.
  </acceptance_criteria>
  <done>phase-5-gate-fresh green in an ephemeral worktree with deal-sim materialized; frozen baseline tracked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the end-to-end PM-query report</name>
  <files>spec/examples/showcase (verification target, not modified)</files>
  <action>Pause for human verification of the end-to-end Phase 5 chain against the showcase model; see what-built and how-to-verify below.</action>
  <what-built>The full Phase 5 simulation chain: deal simulate --all, deal evidence capture/baseline, and deal check --verify producing a per-requirement verification report against the showcase model. This checkpoint confirms the SC#5 PM-query goal end-to-end.</what-built>
  <how-to-verify>
    1. From the repo root, run: `./target/release/deal simulate --all spec/examples/showcase` — confirm Python + the Zig sim produce output and MATLAB sims report a graceful skip (not an error).
    2. Run: `./target/release/deal evidence capture spec/examples/showcase` then `./target/release/deal evidence baseline v2.1.0 spec/examples/showcase` — confirm evidence/baselines/v2.1.0/manifest.json is written.
    3. Run: `./target/release/deal check --verify spec/examples/showcase` — confirm the report answers "is REQ_BAT_001 verified?" with a verdict + supporting evidence + margin, shows REQ_BAT_002 as PARTIAL with its gap, and flags any MATLAB-backed criteria as STALE (since MATLAB was skipped).
    4. Run: `./target/release/deal check --verify --json spec/examples/showcase | head -40` — confirm the JSON report is keyed by REQ_* id with verdict/stale/margins.
  </how-to-verify>
  <resume-signal>Type "approved" if the PM-query report is traceable and correct, or describe what is wrong.</resume-signal>
  <verify><human-check>Human confirms the per-REQ verify report is traceable and correct for REQ_BAT_001 and REQ_BAT_002</human-check></verify>
  <done>Human has approved the end-to-end PM-query report (SC#5) against the showcase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script to external tools | smoke spawns python/zig/(matlab) over the showcase |
| fresh worktree to sibling repos | ephemeral worktree symlinks + pip-installs deal-sim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Denial of Service | gate fails hard when MATLAB absent | mitigate | D-72 graceful-skip; gate hard-requirement is Python+Zig real + MATLAB structural via deal check --simulations |
| T-05-19 | Tampering | deal_sim not importable in fresh worktree | mitigate | verify-fresh-worktree.sh symlinks deal-sim + pip install -e (Plan 01); fresh gate asserts import |
| T-05-SC | Tampering | gate installs deal-sim (created-from-scratch, not registry) | mitigate | deal-sim is the local sibling package built in Plan 02, not a PyPI/crates install; no [ASSUMED]/[SUS] registry package introduced |
</threat_model>

<verification>
- zig build phase-5-gate + phase-5-gate-fresh exit 0.
- cargo test -p deal --test phase5_e2e green.
- Human-verify checkpoint confirms the PM-query report (SC#5).
</verification>

<success_criteria>
- Showcase sims run, produce evidence, criteria evaluate (REQ-phase-5-gate).
- A PM gets a traceable per-REQ answer for REQ_BAT_001 (SC#5).
- MATLAB graceful-skip; Python + Zig hard-gate; fresh-worktree gate green.
</success_criteria>

<output>
Create `.planning/phases/05-simulation-integration/05-07-SUMMARY.md` when done.
</output>
