---
phase: 01.5-parser-strictness-test-gap-resolution-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/recovery_statement.zig
  - tests/unit/recovery_definition.zig
  - tests/unit/recovery_dealx_tag.zig
  - tests/unit/property_span_containment.zig
  - tests/unit/_all.zig
autonomous: true
requirements:
  - REQ-phase-1.5-1-test-assertions
  - REQ-phase-1.5-5-property-tests
must_haves:
  truths:
    - "Every recovery_* test case asserts the exact diagnostic code emitted (string-equal, not >= 1) for any malformed input that should produce a diagnostic."
    - "Every recovery_* test case asserts a named structural property of the recovered AST (e.g. df.definitions[1].payload.part_def.name == \"Good\") at the expected position, not just that some definition exists."
    - "The grep gate `grep -nE 'len >= 0' tests/unit/*.zig` returns zero matches against an unsigned-length expression."
    - "A new test `property.span_containment` walks every AST node of every showcase file (19 .deal/.dealx files) plus the synthetic fixtures from parser_deal.coverage and asserts child.span.start >= parent.span.start && child.span.end <= parent.span.end for every parent-child edge."
    - "All new and strengthened tests register through tests/unit/_all.zig and are runnable via `zig build test -Dtest-filter=<name>`."
  artifacts:
    - path: "tests/unit/recovery_statement.zig"
      provides: "Three recovery cases (A: missing-semicolon-between-attributes baseline / well-formed-or-syncs, B: garbage-tokens-inside-body, C: missing-statement-semicolon) each asserting exact diagnostic codes for any malformed source and the recovered AST shape at expected positions."
      forbids: "any `>= 0` assertion against a `.len` value"
    - path: "tests/unit/recovery_definition.zig"
      provides: "Three recovery cases (A: garbage-between-definitions, B: malformed-header-between-definitions, C: truncated-definition-body) each asserting at minimum one diagnostic code by string equality AND the structural shape (e.g. definitions[N].payload.part_def.name == \"Good\")."
      forbids: "any `>= 0` assertion against a `.len` value"
    - path: "tests/unit/recovery_dealx_tag.zig"
      provides: "Three recovery cases (A: stray-tokens-between-tags, B: orphan-close-tag, C: stray-tokens-before-tag) each asserting exact diagnostic code (E0301 for orphan close-tag retained) AND the recovered root_tags[N] kind/name at expected positions."
      forbids: "any `>= 0` assertion against a `.len` value"
    - path: "tests/unit/property_span_containment.zig"
      provides: "Test `property.span_containment` parsing 19 showcase files + parser_deal.coverage synthetic fixtures, walking the AST and asserting child-span-inside-parent-span on every recursive descent."
    - path: "tests/unit/_all.zig"
      provides: "Umbrella registration of property_span_containment.zig via comptime `_ = @import(...)`."
  key_links:
    - from: "tests/unit/recovery_statement.zig"
      to: "src/diagnostics.zig Codes namespace"
      via: "expectEqualStrings against pub const e_* values"
      pattern: "expectEqualStrings\\(\"E[0-9]{4}\""
    - from: "tests/unit/recovery_definition.zig"
      to: "src/parser_deal.zig recovered definitions slice"
      via: "df.definitions[N].payload.part_def.name string comparison"
      pattern: "df\\.definitions\\[[0-9]+\\]\\.payload\\.part_def\\.name"
    - from: "tests/unit/recovery_dealx_tag.zig"
      to: "src/parser_dealx.zig recovered root_tags slice"
      via: "dx.root_tags[N].kind / system_block name access"
      pattern: "dx\\.root_tags\\[[0-9]+\\]"
    - from: "tests/unit/property_span_containment.zig"
      to: "tests/showcase/**/*.deal[x] + parser_deal.coverage synthetic fixtures"
      via: "parser.parseFile + recursive AST walker mirroring parser_deal.coverage::visit"
      pattern: "child\\.span\\.start >= parent\\.span\\.start"
---

<objective>
Replace every vacuous `diags.items.len >= 0` assertion in `tests/unit/` with a named-property check. Strengthen the 9 recovery cases (3 in recovery_statement.zig + 3 in recovery_definition.zig + 3 in recovery_dealx_tag.zig) so each asserts (a) the exact diagnostic code emitted by string-equality against `src/diagnostics.zig` `Codes` constants, and (b) the recovered AST structure at the expected position (e.g. `df.definitions[1].payload.part_def.name == "Good"`), mirroring the existing `parser_dealx.tag_balance` BDD pattern. Then add a new property-style test `property.span_containment` that walks every AST node of every showcase file (19 .deal/.dealx files) plus the parser_deal.coverage synthetic fixtures, asserting `child.span.start >= parent.span.start && child.span.end <= parent.span.end` on every parent-child edge.

Purpose: Phase 1's automated gates are green, but `/gsd:validate-phase 1` proved that recovery test assertions describe *intent* (in comments) more than *contract* (in expects). This plan flips that — every recovery test now asserts the recovery contract by code AND by AST shape, and the new span-containment property guards the locked snapshots against the class of "AST-byte-equal but spans-corrupted" regressions that snapshot byte-equality alone would silently allow.

Output:
  - `tests/unit/recovery_statement.zig` rewritten with exact-code + AST-shape assertions for all 3 cases
  - `tests/unit/recovery_definition.zig` rewritten with exact-code + AST-shape assertions for all 3 cases
  - `tests/unit/recovery_dealx_tag.zig` rewritten with exact-code + AST-shape assertions for all 3 cases
  - `tests/unit/property_span_containment.zig` created — new test `property.span_containment`
  - `tests/unit/_all.zig` updated — registers the new test file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-CONTEXT.md
@.planning/phases/01-zig-compiler-core/01-LEARNINGS.md
@.planning/phases/01-zig-compiler-core/01-VALIDATION.md

# Exemplar BDD pattern to mirror exactly
@deal/tests/unit/parser_dealx_tag_balance.zig

# Files to modify
@deal/tests/unit/recovery_statement.zig
@deal/tests/unit/recovery_definition.zig
@deal/tests/unit/recovery_dealx_tag.zig
@deal/tests/unit/_all.zig

# AST walker pattern to mirror for span-containment
@deal/tests/unit/parser_deal_coverage.zig

# Diagnostic codes namespace (canonical source for code strings)
@deal/src/diagnostics.zig

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/diagnostics.zig (Codes namespace — use these constants by VALUE in expectEqualStrings):
```
Lexer:
  Codes.e_invalid_utf8           = "E0001"
  Codes.e_unterminated_string    = "E0002"
  Codes.e_unterminated_comment   = "E0003"
  Codes.e_source_too_large       = "E0004"
  Codes.e_template_too_deep      = "E0005"

Parser-deal:
  Codes.e_expected_token         = "E0100"
  Codes.e_expected_definition    = "E0101"
  Codes.e_unexpected_eof         = "E0102"
  Codes.e_expected_attribute_value = "E0103"
  Codes.e_expected_expression    = "E0110"
  Codes.e_expected_identifier    = "E0111"
  Codes.e_invalid_modifier       = "E0120"

Parser-dealx:
  Codes.e_unmatched_close_tag    = "E0301"
  Codes.e_mismatched_close_tag   = "E0302"
  Codes.e_nesting_too_deep_tag   = "E0303"
  Codes.e_unclosed_tag_at_eof    = "E0304"

Recovery:
  Codes.e_sync_dropped_tokens    = "E0400"
  Codes.e_unclosed_brace         = "E0401"
  Codes.e_unclosed_bracket       = "E0402"
  Codes.e_nesting_too_deep_expr  = "E0403"
```

From src/parser_deal.zig:
```
pub fn parseFile(
    allocator: std.mem.Allocator,
    source: []const u8,
    diags: *std.ArrayList(diagnostics_mod.Diagnostic),
) std.mem.Allocator.Error!?*ast.Node;
```

From src/parser_dealx.zig:
```
pub fn parseFile(
    allocator: std.mem.Allocator,
    source: []const u8,
    diags: *std.ArrayList(diagnostics_mod.Diagnostic),
) std.mem.Allocator.Error!?*ast.Node;
```

From src/parser.zig (unified entry):
```
pub fn parseFile(
    allocator: std.mem.Allocator,
    source: []const u8,
    mode: ast.Mode,                  // .deal or .dealx
    diags: *std.ArrayList(diagnostics_mod.Diagnostic),
) std.mem.Allocator.Error!?*ast.Node;
```

From src/ast.zig (relevant payload shapes):
```
Node = struct { kind: NodeKind, span: Span, payload: Payload }
Span = struct { start: u32, end: u32 }
Payload union arms accessed in this plan:
  .deal_file => DealFile { definitions: []*Node, ... }
  .dealx_file => DealxFile { root_tags: []*Node, ... }
  .part_def => ElementDef { name: []const u8, members: []*Node, ... }
  .system_block => SystemBlock { name: []const u8, children: []*Node, ... }
```

Helper functions from parser_dealx_tag_balance.zig that should be hoisted or duplicated:
```
fn countCode(diags: []const Diagnostic, code: []const u8) usize
fn firstWithCode(diags: []const Diagnostic, code: []const u8) ?Diagnostic
```

Showcase file list (19 files; mirror parser_deal_coverage.zig showcase_deal_files and append the 4 .dealx files):
  .deal (15):  packages/vehicle/{battery,motor,behaviors,components,index}.deal
               packages/interfaces/{electrical,thermal,connections,index}.deal
               packages/requirements/{system,needs,index}.deal
               packages/use-cases/{driving,charging,index}.deal
  .dealx (4):  model/{traceability,vehicle}.dealx
               model/variants/{performance,sedan}.dealx
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hoist BDD helpers and strengthen all 9 recovery cases</name>
  <files>
    tests/unit/recovery_statement.zig,
    tests/unit/recovery_definition.zig,
    tests/unit/recovery_dealx_tag.zig
  </files>
  <read_first>
    tests/unit/parser_dealx_tag_balance.zig,
    tests/unit/recovery_statement.zig,
    tests/unit/recovery_definition.zig,
    tests/unit/recovery_dealx_tag.zig,
    src/diagnostics.zig,
    src/parser_deal.zig (peek at parseFile + relevant emit sites),
    src/parser_dealx.zig (peek at parseFile + emit sites)
  </read_first>
  <action>
For each of the three test files: replace every `try std.testing.expect(... .len >= 0)` and every `try std.testing.expect(... .len >= 1)` (where the test claims a specific contract in its comment) with the two-axis assertion shape from parser_dealx_tag_balance.zig:
  axis A (diagnostic): `try std.testing.expectEqual(@as(usize, 1), countCode(diags.items, Codes.e_*));` for the SPECIFIC code the contract demands, OR `try std.testing.expect(countCode(diags.items, Codes.e_*) >= 1);` if multiple emissions are legitimate. If the test claims "≥1 diagnostic", state WHICH code by name. Do not assert generic `diags.items.len > 0`.
  axis B (AST shape): `try std.testing.expectEqualStrings("Good", df.definitions[1].payload.part_def.name);` — name the structural property the recovery contract delivers.

Treat the existing helpers `countCode` and `firstWithCode` from parser_dealx_tag_balance.zig as shared utilities. Either (i) duplicate them inline at the top of each test file (acceptable for 9 lines), OR (ii) extract them into a new helper module `tests/unit/_diag_helpers.zig` exported as `pub fn countCode` and `pub fn firstWithCode`, and import from each test file. Pick approach (i) unless extraction reveals a cleaner pattern.

Per-case behavior to assert (use these as the contract — do NOT relax them):

recovery_statement.zig:
  Case A (`"package foo;\npart def P { attribute a : T; attribute b : T; }"`) — this source is WELL-FORMED (per LEARNINGS Surprise #8 finding #2: the comment falsely claims missing-semicolon). Either (i) keep it as a well-formed baseline and rename the case to "well-formed two-attribute body — zero diagnostics, two members" asserting `countCode(diags.items, anything) == 0` AND `df.definitions[0].payload.part_def.members.len == 2`, OR (ii) edit the source to truly omit a semicolon (e.g. `"package foo;\npart def P { attribute a : T attribute b : T; }"`) and assert exactly one E0100 (e_expected_token) with `firstWithCode(diags.items, Codes.e_expected_token).?.span.start` strictly between the byte offset of `T` and the byte offset of the second `attribute`, AND `df.definitions[0].payload.part_def.members.len >= 1`. Choose option (ii) — it preserves the case's name "recovery.statement" and exercises the statement-tier sync.
  Case B (`"package foo;\npart def P { 12345 99999 ; attribute ok : T; }"`) — assert `countCode(diags.items, Codes.e_expected_definition) + countCode(diags.items, Codes.e_expected_token) >= 1` for the garbage tokens, AND the part_def has at least one member whose payload is an attribute_usage named "ok" (`df.definitions[0].payload.part_def.members[N].payload.attribute_usage.name == "ok"` for some N). Use a loop to find the named member (do NOT hardcode index 0).
  Case C (`"package foo;\npart def P { x = 42 y = 7; }"`) — currently has `>= 0`. Replace with: `try std.testing.expect(countCode(diags.items, Codes.e_expected_token) >= 1);` AND assert that the part_def has at least one operator_statement / attribute_usage / member whose payload's name field equals "y" (the second statement recovered after the missing semicolon between `x = 42` and `y = 7`). If the parser cannot currently recover the "y" assignment, document the actual recovered shape (e.g. operator_statement with value=42, then a second member) and assert that exact shape — never reduce to `.len >= 0`.

recovery_definition.zig:
  Case A (`"package foo;\n12345 99999 part def P { }"`) — already does the right thing in spirit; tighten it to `try std.testing.expectEqual(@as(usize, 1), countCode(diags.items, Codes.e_expected_definition));` (or whichever single code the garbage at top-level emits — verify empirically) AND keep the loop that asserts a part_def named "P" exists. Add: `try std.testing.expect(df.definitions.len == 1);` so we know recovery did not produce phantom extra definitions.
  Case B (`"package foo;\npart def A { }\n!!!stray!!!\npart def B { }"`) — currently asserts `>= 0`. Replace with: assert exactly two part_def kinds with names "A" and "B" (already partially in the file — keep `found_a && found_b`), AND assert `countCode(diags.items, Codes.e_expected_definition) + countCode(diags.items, Codes.e_expected_token) >= 1` (the stray `!!!` must produce at least one diagnostic). Drop the trailing `>= 0`.
  Case C (`"package foo;\npart def Broken { attribute x : T\npart def Good { }"`) — replace `df.definitions.len >= 1` with a loop finding the part_def named "Good": `expectEqualStrings("Good", df.definitions[idx].payload.part_def.name)` for some idx. AND assert at least one diagnostic of code Codes.e_expected_token (the missing `;` after `: T`).

recovery_dealx_tag.zig:
  Case A (stray tokens between two systems) — assert `countCode(diags.items, Codes.e_expected_definition) + countCode(diags.items, Codes.e_expected_token) >= 1` for the stray `12345` AND `dx.root_tags.len == 2` (exactly two systems recovered, not just ≥2 — anything more would mean phantom recovery). For each of the two system_block tags, assert its name is "Foo" or "Bar" via a loop.
  Case B (orphan close tag) — already asserts E0301 by code. Strengthen: `try std.testing.expectEqual(@as(usize, 1), countCode(diags.items, Codes.e_unmatched_close_tag));` AND assert `dx.root_tags.len == 0` (no tag recovered from `[</orphan>]`).
  Case C (stray tokens before a valid system) — assert `countCode(diags.items, Codes.e_expected_definition) + countCode(diags.items, Codes.e_expected_token) >= 1` for the stray digits AND `dx.root_tags.len == 1` AND the single tag is system_block named "Foo".

Every test must use `Codes.e_*` constants by name (via `const diagnostics_mod = @import("diagnostics");`) — never inline string literals like `"E0301"` directly. The pinning lives in src/diagnostics.zig; the tests reference it symbolically.

Anti-pattern enforcement: after the edit, `grep -nE 'len >= 0' tests/unit/recovery_statement.zig tests/unit/recovery_definition.zig tests/unit/recovery_dealx_tag.zig` must return zero lines. This is a hard grep gate, not just an aesthetic choice.

Empirical-truth caveat: if any of the assertions above is empirically wrong (the parser emits a different code than predicted, OR recovers to a different shape), DO NOT relax the assertion — instead run `zig build test -Dtest-filter=recovery.statement` (or the corresponding test), READ the failure output verbatim, and update THIS PLAN with an inline note documenting the empirically-confirmed code/shape, then assert the empirical truth. Plans capture the contract; if the contract diverges from reality, that is either a bug to fix in Phase 1.5 or an actual fact about the parser that this test now locks down. Choose the latter unless the divergence reveals a strictness gap (which becomes input for Plan 02).
  </action>
  <verify>
    <automated>cd deal && zig build test -Dtest-filter=recovery.statement && zig build test -Dtest-filter=recovery.definition && zig build test -Dtest-filter=recovery.dealx_tag</automated>
    <automated>cd deal && bash -c 'if grep -nE "len >= 0" tests/unit/recovery_statement.zig tests/unit/recovery_definition.zig tests/unit/recovery_dealx_tag.zig; then echo "GATE FAIL: vacuous assertion remaining"; exit 1; fi'</automated>
    <automated>cd deal && grep -c "expectEqualStrings\|expectEqual.*countCode\|expectEqual.*part_def.name\|expectEqual.*system_block" tests/unit/recovery_statement.zig tests/unit/recovery_definition.zig tests/unit/recovery_dealx_tag.zig | awk -F: '{ s += $2 } END { if (s < 12) { print "GATE FAIL: not enough strict assertions ("s" < 12)"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'len >= 0' tests/unit/recovery_statement.zig tests/unit/recovery_definition.zig tests/unit/recovery_dealx_tag.zig` returns 0 matching lines (exit 0 from grep means matches found — invert: this command should exit 1 from grep's no-match exit code, equivalently `grep -c ... | grep -v ':0$' | wc -l` returns 0).
    - Each of the three test files contains at least one `expectEqualStrings("E0", ...)`-equivalent assertion or `expectEqual(@as(usize, 1), countCode(diags.items, Codes.e_*))` for every test case (3 × 3 = 9 cases minimum).
    - Each of the three test files contains at least one structural-shape assertion per case asserting either a definition name, member name, root_tag count, or root_tag kind (3 × 3 = 9 cases minimum).
    - `zig build test -Dtest-filter=recovery.statement` exits 0.
    - `zig build test -Dtest-filter=recovery.definition` exits 0.
    - `zig build test -Dtest-filter=recovery.dealx_tag` exits 0.
    - No new compile errors anywhere; `zig build` exits 0.
  </acceptance_criteria>
  <done>
All 9 recovery cases (3 per file × 3 files) assert exact diagnostic codes via `Codes.e_*` constants AND a named structural property of the recovered AST. The grep gate `grep -nE 'len >= 0' tests/unit/*.zig` (across these three files) returns zero matches. All three recovery test filters exit 0 under `zig build test`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create property.span_containment test</name>
  <files>tests/unit/property_span_containment.zig, tests/unit/_all.zig</files>
  <read_first>
    tests/unit/parser_deal_coverage.zig (the visit() walker is the EXACT pattern to mirror — copy its switch/payload structure verbatim, change the body from "count NodeKind" to "assert span containment"),
    tests/unit/parser_dealx_coverage.zig (mirror its dealx walker for system_block/comp_connect/etc.),
    tests/unit/_all.zig,
    src/ast.zig (for the full Payload union; the walker switch must be exhaustive),
    src/parser.zig (for parser.parseFile with mode argument)
  </read_first>
  <action>
Create new file `tests/unit/property_span_containment.zig` declaring `test "property.span_containment"`. Structure (file shape mirrors parser_deal_coverage.zig top-to-bottom):

1. Top of file: doc-comment explaining purpose — "Walks every AST node of every showcase file plus parser_deal.coverage synthetic fixtures; asserts child.span.start >= parent.span.start && child.span.end <= parent.span.end on every parent-child edge. Locks the spec-grade snapshot bytes against the regression class 'AST identical but spans corrupted', which snapshot byte-equality alone would not catch (per LEARNINGS Lesson 'Snapshot byte-equality hides initial-correctness bugs')."

2. Imports: `const std = @import("std"); const ast = @import("ast"); const parser = @import("parser"); const diagnostics_mod = @import("diagnostics");`

3. Constants:
   - `showcase_deal_files: [15][]const u8` — copy from parser_deal_coverage.zig:27-43 verbatim
   - `showcase_dealx_files: [4][]const u8` — `model/traceability.dealx`, `model/vehicle.dealx`, `model/variants/performance.dealx`, `model/variants/sedan.dealx`
   - `synthetic_fixtures` — copy from parser_deal_coverage.zig:48-138 verbatim (the same 8 synthetic .deal fixtures) so the walker covers NodeKinds not present in the 19-file corpus (visibility_wrapper, redefinition, real_literal_and_boolean, unary_expression, verification_block, precondition_postcondition, missing_def_kinds, requirement_usage)

4. Recursive walker `fn visit(node: *ast.Node, parent_span: ast.Span) anyerror!void`:
   - First action: `try std.testing.expect(node.span.start >= parent_span.start);` and `try std.testing.expect(node.span.end <= parent_span.end);` — failure prints node.kind, node.span, parent_span via std.debug.print so a failed assertion points at the offending node before exiting.
   - Then mirror parser_deal_coverage.zig::visit's switch over `node.payload` arms EXHAUSTIVELY (every NodeKind variant present in src/ast.zig), recursing with `visit(child, node.span)` for every child. The exhaustiveness is the contract — a non-exhaustive switch would let new NodeKinds slip through silently.
   - For .dealx-specific arms (system_block, subsystem_block, component_instance, comp_connect, expose_tag, traceability_block, allocate_tag, satisfy_block, validate_tag, object_literal), follow parser_dealx_coverage.zig's walker for the children to recurse into (e.g. system_block.children, comp_connect.via_expr, comp_connect.carrying_expr, traceability_block.links). Read parser_dealx_coverage.zig to copy the exact payload-field names.
   - Use `std.mem.Allocator.Error || error{TestExpectedSpanContained}` (or use `anyerror!void` for simplicity) as the return type. std.testing.expect already returns error.TestExpectedEqual on failure — that propagates naturally.

5. Root entry: `fn walkRoot(root: *ast.Node) !void { try visit(root, root.span); }` — the root's "parent span" is its own span; the root passes the assertion trivially, and every recursive child is checked against the root's span before descending.

6. Test body `test "property.span_containment"`:
   - `const gpa = std.testing.allocator;`
   - `const io = std.testing.io;`
   - `const cwd = std.Io.Dir.cwd();`
   - Loop over showcase_deal_files: read each via `cwd.readFileAlloc(io, path, gpa, .unlimited)`, parse with `parser.parseFile(arena.allocator(), source, .deal, &diags)`, if root non-null call `walkRoot(root)`. Defer arena.deinit and gpa.free(source) per iteration.
   - Loop over showcase_dealx_files: same shape with `.dealx` mode.
   - Loop over synthetic_fixtures: same shape with `.deal` mode (these are inline strings — no file read).
   - For each parse, if `diags.items.len > 0` on a SHOWCASE file, print a warning to stderr but DO NOT fail — showcase files are well-formed by Phase 1 contract; if a diagnostic appears, that is a separate regression. The property test only asserts span containment.
   - If `root == null` on any parse: `std.debug.print("property.span_containment: parser returned null on {s}\n", .{path}); return error.TestUnexpectedNull;` — root-null on showcase is a hard failure.

7. Register in tests/unit/_all.zig: add `_ = @import("property_span_containment.zig");` to the comptime block after `_ = @import("recovery_corpus.zig");`. Keep alphabetical-ish ordering for diff cleanliness.

CRITICAL: Do NOT regenerate any snapshots. If this test fails against the current locked AST snapshots, the failure surfaces a Phase 1 bug (a span emitted with start past its parent's end, or end before its parent's start) — fix the bug in Phase 1.5 (will likely be small; spans are 6-byte primitives and the bug, if any, is a missed end-extension in a parseXxx call). Do NOT relax the assertion to "containment OR overlap" or similar — the assertion is intentionally strict.

If a span containment violation is found, document it in the task summary with: file path, NodeKind, child span, parent span, and the source line. Then fix the parser (likely a single-line span-end update in src/parser_deal.zig or src/expr.zig). Snapshots stay byte-identical — span containment must hold on the EXISTING snapshots, otherwise the locked snapshots themselves are wrong (a Phase 1 regression to surface and fix in this phase, per LEARNINGS Lesson "Snapshot byte-equality hides initial-correctness bugs").

Acceptance gate: the test exits 0 against the current locked snapshots without snapshot regeneration. The `-Dtest-filter=property` filter substring-matches the test name "property.span_containment" via the existing build.zig `-Dtest-filter` mechanism.
  </action>
  <verify>
    <automated>cd deal && zig build test -Dtest-filter=property.span_containment</automated>
    <automated>cd deal && ls tests/unit/property_span_containment.zig && grep -c "property_span_containment" tests/unit/_all.zig | awk -F: '{ if ($1+0 < 1) exit 1 }'</automated>
    <automated>cd deal && zig build test</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/property_span_containment.zig` exists.
    - File contains the literal substring `test "property.span_containment"`.
    - File contains `child.span.start >= parent.span.start` or equivalent (e.g. `node.span.start >= parent_span.start`).
    - File walks all 19 showcase files (15 .deal + 4 .dealx) — `grep -c "showcase_deal_files\|showcase_dealx_files" tests/unit/property_span_containment.zig` returns >= 2.
    - File parses synthetic_fixtures from parser_deal.coverage — `grep -c "synthetic_fixtures" tests/unit/property_span_containment.zig` returns >= 2.
    - `tests/unit/_all.zig` contains `_ = @import("property_span_containment.zig");`.
    - `zig build test -Dtest-filter=property.span_containment` exits 0.
    - `zig build test` (full unit suite) exits 0 — no regression to other tests.
    - No AST snapshot file under `tests/snapshots/ast/` has changed bytes (verify with `git diff --stat tests/snapshots/ast/` — must be empty).
  </acceptance_criteria>
  <done>
`tests/unit/property_span_containment.zig` exists with test `property.span_containment` walking all 19 showcase files plus 8 synthetic fixtures, asserting span containment on every parent-child edge. The test is registered in `_all.zig` and exits 0 under `zig build test -Dtest-filter=property.span_containment`. No AST snapshots are regenerated; the existing locked snapshots remain byte-identical.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test source → test runner | Tests are in-tree authored code; same trust level as parser. Strengthening assertions does not cross a new trust boundary. |
| parser output → test assertion | AST and Diagnostic structs are arena-owned; tests read but do not mutate. No new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | tests/unit/recovery_*.zig assertions | mitigate | Replace vacuous `>= 0` with named-property checks. Future test-only changes that re-introduce `len >= 0` would weaken the contract; the grep gate in Plan 04 catches recurrence. |
| T-15-01-02 | Information Disclosure | property.span_containment walker | accept | Walker reads AST nodes (arena-owned) and prints span bytes to stderr on failure. No secrets present; AST is parsed from in-tree showcase test fixtures. |
| T-15-01-03 | Denial of Service | property.span_containment walker recursion | mitigate | Walker is bounded by AST node count. Showcase corpus is 19 files; synthetic fixtures are 8 inline sources. MAX_PARSE_DEPTH=256 from Phase 1 (D-17 sentinel) already bounds recursion depth — the walker inherits that bound transitively. No new DoS surface introduced. |
| T-15-01-04 | Repudiation | n/a | accept | Test results are recorded by `zig build test` exit code; no auditable user action involved. |
</threat_model>

<verification>
After both tasks complete, run the full Zig unit suite to confirm no regression:

```sh
cd deal && zig build test
```

Expected output: every test (lexer.*, expr.*, parser_deal.*, parser_dealx.*, recovery.*, diag.*, c_abi.*, AND the new property.span_containment) passes; exit 0.

Verify the vacuous-assertion grep gate is clean across ALL test files (not just the three modified here — this guards against re-introduction elsewhere):

```sh
cd deal && bash -c 'if grep -nE "len >= 0" tests/unit/*.zig; then exit 1; fi'
```

Expected: command exits 0, no output.
</verification>

<success_criteria>
- All 9 recovery test cases assert exact diagnostic codes via `Codes.e_*` constants AND a named structural property of the recovered AST.
- Grep gate `grep -nE 'len >= 0' tests/unit/*.zig` returns zero matches.
- New test `property.span_containment` walks 19 showcase files + 8 synthetic fixtures asserting child-span-inside-parent-span containment.
- `zig build test -Dtest-filter=property.span_containment` exits 0 against the current locked snapshots.
- `zig build test` (full suite) exits 0 — no regression to existing tests.
- AST snapshots under `tests/snapshots/ast/` remain byte-identical (`git diff --stat tests/snapshots/ast/` is empty).
</success_criteria>

<output>
Create `.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-01-SUMMARY.md` when done. Summary captures:
  - Each of the 9 strengthened cases: the exact diagnostic code now asserted and the exact AST shape now asserted.
  - The synthetic-fixture-or-showcase test that exercised each previously-uncovered NodeKind in the span walker.
  - Any span-containment violation surfaced (if any), with the fix applied to src/parser_*.zig (one-line span-end update if simple).
  - Confirmation that AST snapshots are byte-identical pre/post.
</output>
