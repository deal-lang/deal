---
phase: 01.5
slug: parser-strictness-test-gap-resolution-inserted
status: reconstructed
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-21
reconstructed_from:
  - 01.5-01-SUMMARY.md
  - 01.5-02-SUMMARY.md
  - 01.5-03-SUMMARY.md
  - 01.5-04-SUMMARY.md
  - 01.5-05-SUMMARY.md
  - 01.5-VERIFICATION-PLAN05.md
  - 01.5-UAT.md (status: resolved)
---

# Phase 1.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Phase 1.5
> is complete; this document is reconstructed retrospectively from per-plan
> SUMMARYs and the Plan 05 false-GREEN remediation, so it doubles as the Nyquist
> audit record.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Zig 0.16.0 `std.testing` (in-tree unit tests; no external runner) |
| **Config file** | `deal/build.zig` (unit_tests module + `-Dtest-filter=<substr>` plumbing) |
| **Quick run command** | `zig build test -Dtest-filter=<area>` (e.g. `recovery.corpus`, `determinism`, `property`, `strictness`) |
| **Full suite command** | `zig build test` (33 named tests) |
| **Exit-gate (convenience)** | `zig build phase-1.5-gate` — bundles `phase-1-gate` + full test suite |
| **Exit-gate (authoritative)** | `zig build phase-1.5-gate-fresh` — runs the convenience gate inside an ephemeral worktree (binding per ADR-phase-1.5-fresh-worktree-verification) |
| **Exit-gate (three-step CI)** | `zig build phase-1-gate && zig build test -Dtest-filter=determinism && zig build test -Dtest-filter=property` |
| **Estimated runtime** | ~600 ms full suite; <2 s for the determinism + property filters; ~5–10 s for `phase-1.5-gate-fresh` (adds worktree + submodule init) |

---

## Sampling Rate

- **After every task commit:** `zig build test -Dtest-filter=<area>` for the affected area (e.g. `strictness`, `recovery.statement`, `property`).
- **After every plan wave:** `zig build test` (full unit suite).
- **Before phase-exit verification:** `zig build phase-1.5-gate` AND `zig build phase-1.5-gate-fresh` both exit 0.
- **Max feedback latency:** < 2 s for any filtered subset; < 10 s for the fresh-worktree gate including submodule init.

---

## Per-Task Verification Map

Each entry maps a task (plan-task pair) to its requirement, the test file that asserts the contract, and the automated command that exercises it. Status `✅` means the test exists, has been committed, and passes against current HEAD. `nyquist_compliant: true` requires every row's Status to be `✅`.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.5-01-01 | 01 | 1 | REQ-phase-1.5-1-test-assertions | T-15-01-01 | recovery_statement asserts exact `Codes.e_*` codes + recovered AST shape; no `len >= 0` accepted | unit | `zig build test -Dtest-filter=recovery.statement` | ✅ tests/unit/recovery_statement.zig | ✅ green |
| 01.5-01-01 | 01 | 1 | REQ-phase-1.5-1-test-assertions | T-15-01-01 | recovery_definition asserts exact codes + member-name + part_usage("def") empirical lock | unit | `zig build test -Dtest-filter=recovery.definition` | ✅ tests/unit/recovery_definition.zig | ✅ green |
| 01.5-01-01 | 01 | 1 | REQ-phase-1.5-1-test-assertions | T-15-01-01 | recovery_dealx_tag asserts E0301 + root_tags count and kind | unit | `zig build test -Dtest-filter=recovery.dealx_tag` | ✅ tests/unit/recovery_dealx_tag.zig | ✅ green |
| 01.5-01-01 | 01 | 1 | REQ-phase-1.5-1-test-assertions | T-15-01-01 | No vacuous `len >= 0` assertions cross-file gate | static | `grep -nE 'len >= 0' tests/unit/*.zig` → 0 matches | ✅ enforced post-Plan-01 | ✅ green |
| 01.5-01-02 | 01 | 1 | REQ-phase-1.5-5-property-tests | T-15-01-02, T-15-01-03 | child-span ⊆ parent-span over 19 showcase + 8 synthetic fixtures; exhaustive `union(NodeKind)` switch | property | `zig build test -Dtest-filter=property.span_containment` | ✅ tests/unit/property_span_containment.zig | ✅ green |
| 01.5-02-01 | 02 | 2 | REQ-phase-1.5-4-parser-strictness | T-15-02-01 | `Codes.e_dangling_dot = "E0121"` and `Codes.e_empty_arg_comma = "E0122"` declared in D-16 parser-deal range; EXISTING `e_unterminated_comment = "E0003"` preserved at line 64 | static | `grep -nE 'e_dangling_dot\|e_empty_arg_comma' src/diagnostics.zig` → 2 lines; `grep -nE 'e_unterminated_comment\s*=\s*"E0003"' src/diagnostics.zig` → 1 line | ✅ src/diagnostics.zig | ✅ green |
| 01.5-02-02 | 02 | 2 | REQ-phase-1.5-4-parser-strictness | T-15-02-01, T-15-02-04 | m08 `/* … EOF` emits E0003 at `[13,43)` via lexer→parser drain; no snapshot drift | unit | `zig build test -Dtest-filter=strictness.m08_unterminated_block_comment` | ✅ tests/unit/strictness_m08_m18_m19.zig | ✅ green |
| 01.5-02-02 | 02 | 2 | REQ-phase-1.5-4-parser-strictness | T-15-02-02 | m18 `a.` emits E0121 at the `.` token span; Pratt loop breaks rather than advancing | unit | `zig build test -Dtest-filter=strictness.m18_dangling_dot` | ✅ tests/unit/strictness_m08_m18_m19.zig | ✅ green |
| 01.5-02-02 | 02 | 2 | REQ-phase-1.5-4-parser-strictness | T-15-02-02, T-15-02-03 | m19 `f(,)` emits E0122 at leading-comma span; forward-progress preserved across leading/double/trailing comma | unit | `zig build test -Dtest-filter=strictness.m19_empty_arg_list_comma` | ✅ tests/unit/strictness_m08_m18_m19.zig | ✅ green |
| 01.5-02-03 | 02 | 2 | REQ-phase-1.5-4-parser-strictness | T-15-02-05 | AST + token snapshots byte-identical across the strictness change | static | `git diff --stat tests/snapshots/` → empty | ✅ verified by Plan 02 | ✅ green |
| 01.5-03-02 | 03 | 3 | REQ-phase-1.5-2-malformed-pinning | T-15-03-01, T-15-03-02 | `m_pins[20]` enforced: 19 impl pins via `Codes.e_*` + 1 accepted-no-diag (m01 SD-10) with ADR existence check; soft 60% gate preserved | unit | `zig build test -Dtest-filter=recovery.corpus` | ✅ tests/unit/recovery_corpus.zig | ✅ green |
| 01.5-03-02 | 03 | 3 | REQ-phase-1.5-2-malformed-pinning | — | m01 SD-10 ADR present and referenced symbolically | static | `test -f .planning/decisions/ADR-phase-1.5-m01-sd10-optional-semicolon.md` | ✅ ADR committed | ✅ green |
| 01.5-04-01 | 04 | 4 | REQ-phase-1.5-3-determinism | T-15-04-01, T-15-04-06 | For each of 19 showcase files, parse twice via PUBLIC C ABI (`deal_parse` / `deal_ast_json` / `deal_diagnostics_json` / `deal_free`); assert byte-equal AST AND diagnostics JSON; `gpa.dupe` before first free | unit | `zig build test -Dtest-filter=determinism.parse_twice` | ✅ tests/unit/determinism_parse_twice.zig | ✅ green |
| 01.5-04-01 | 04 | 4 | REQ-phase-1.5-3-determinism | T-15-04-06 | Determinism test does NOT use `deal_parse_internal` (PUBLIC C ABI lock) | static | `grep -c 'deal_parse_internal' tests/unit/determinism_parse_twice.zig` → 0; `grep -c 'lib\.deal_parse(' tests/unit/determinism_parse_twice.zig` → ≥2 | ✅ surface grep gate | ✅ green |
| 01.5-04-02 | 04 | 4 | REQ-phase-1.5-gate | T-15-04-04 | `phase-1.5-gate` build step bundles `phase-1-gate` + full test suite; inline NOTE comment names the three-step CI command on a single line | build | `zig build phase-1.5-gate`; `grep -qE 'NOTE:.*three-step\|zig build phase-1-gate.*test-filter=determinism.*test-filter=property' build.zig` | ✅ build.zig | ✅ green |
| 01.5-04-02 | 04 | 4 | REQ-phase-1.5-gate | — | C ABI symbol surface preserved at exactly 6 exports | static | `nm -gU zig-out/lib/libdeal.a \| grep -c '_deal_'` → 6 | ✅ no new exports | ✅ green |
| 01.5-04-02 | 04 | 4 | REQ-phase-1.5-gate | — | `## Phase 1.5 Resolution` section appended to 01-LEARNINGS.md closing every Surprise #8 sub-item (9 Resolved + 1 Deferred via ADR) | static | `grep -c '## Phase 1.5 Resolution' .planning/phases/01-zig-compiler-core/01-LEARNINGS.md` → 1 | ✅ LEARNINGS amended | ✅ green |
| 01.5-05-02 | 05 | 5 | REQ-phase-1.5-gate | T-15-05-02, T-15-05-05 | `spec/` submodule pinned + `tests/showcase` committed symlink resolves inside the deal repo; no developer-local state can mask missing inputs | static | `git ls-tree HEAD spec` → `160000`; `git ls-tree HEAD tests/showcase` → `120000`; `test -f tests/showcase/packages/vehicle/battery.deal` | ✅ verified by Plan 05 | ✅ green |
| 01.5-05-03 | 05 | 5 | REQ-phase-1.5-gate | T-15-05-04, T-15-05-06 | `phase-1.5-gate-fresh` runs `phase-1.5-gate` inside an ephemeral worktree with `git submodule update --init --recursive`; refuses dirty trees; cleans up via EXIT trap | build | `zig build phase-1.5-gate-fresh` → exit 0, `Build Summary: 33/33 tests passed` | ✅ scripts/verify-fresh-worktree.sh + build.zig step | ✅ green |
| 01.5-05-04 | 05 | 5 | REQ-phase-1.5-gate | — | ADR-phase-1.5-fresh-worktree-verification.md (`status: accepted`) binds Phases 2..6 to add a `-fresh` sibling | static | `grep -q 'status: accepted' .planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md` | ✅ ADR committed | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Phase 1.5 ran without a Wave 0 — Phase 1's `build.zig` test infrastructure (umbrella `tests/unit/_all.zig`, `-Dtest-filter=<substr>`, `phase-1-gate`) was already in place. Phase 1.5 extended it with the four new test files and two new build steps listed below; no test-framework install was required.

- [x] Existing infrastructure (`build.zig` umbrella + `tests/unit/_all.zig`) covers all phase requirements.
- [x] New tests registered through `tests/unit/_all.zig` (the established umbrella convention):
  - `tests/unit/property_span_containment.zig` (Plan 01)
  - `tests/unit/strictness_m08_m18_m19.zig` (Plan 02)
  - `tests/unit/determinism_parse_twice.zig` (Plan 04)
- [x] New build steps in `build.zig`:
  - `phase-1.5-gate` (Plan 04)
  - `phase-1.5-gate-fresh` (Plan 05)
- [x] Submodule + symlink committed for reproducible test inputs (Plan 05):
  - `.gitmodules` + `spec` gitlink pinned at `fe98f893…`
  - `tests/showcase` symlink (mode 120000) → `../spec/examples/showcase`
- [x] `scripts/verify-fresh-worktree.sh` (mode 100755) for the ephemeral-worktree gate.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|

*All phase behaviors have automated verification. No manual-only rows.*

The only behavior that was once manual — confirming verification ran from a clean environment — is now mechanically enforced by `phase-1.5-gate-fresh` per ADR-phase-1.5-fresh-worktree-verification.

---

## Audit Cross-Reference

This phase passed validation retrospectively. Every requirement maps to a committed test file plus an automated command. The Plan 05 false-GREEN remediation (committed 2026-05-21) closed the previously-undocumented dependency on a developer-local symlink; verification was re-run inside a fresh worktree and reported `Build Summary: 33/33 tests passed`.

| Requirement | Test File | Automated Command | Coverage |
|-------------|-----------|--------------------|----------|
| REQ-phase-1.5-1-test-assertions | tests/unit/recovery_statement.zig + recovery_definition.zig + recovery_dealx_tag.zig + cross-file `grep` gate | `zig build test -Dtest-filter=recovery.{statement,definition,dealx_tag}` + grep gate | COVERED |
| REQ-phase-1.5-2-malformed-pinning | tests/unit/recovery_corpus.zig (`m_pins[20]`) + ADR-phase-1.5-m01-sd10-optional-semicolon.md | `zig build test -Dtest-filter=recovery.corpus` | COVERED |
| REQ-phase-1.5-3-determinism | tests/unit/determinism_parse_twice.zig (PUBLIC C ABI) | `zig build test -Dtest-filter=determinism.parse_twice` | COVERED |
| REQ-phase-1.5-4-parser-strictness | src/{diagnostics,lexer,parser_deal,parser_dealx,expr}.zig wiring + tests/unit/strictness_m08_m18_m19.zig (3 named cases) | `zig build test -Dtest-filter=strictness` | COVERED |
| REQ-phase-1.5-5-property-tests | tests/unit/property_span_containment.zig (walker + 19 showcase + 8 synthetic) | `zig build test -Dtest-filter=property.span_containment` | COVERED |
| REQ-phase-1.5-gate | build.zig `phase-1.5-gate` + `phase-1.5-gate-fresh`; scripts/verify-fresh-worktree.sh; ADR-phase-1.5-fresh-worktree-verification.md; 01-LEARNINGS.md `## Phase 1.5 Resolution` | `zig build phase-1.5-gate-fresh` (binding) and `zig build phase-1.5-gate` (convenience) | COVERED |

---

## Validation Audit 2026-05-21 (reconstruction)

| Metric | Count |
|--------|-------|
| Requirements in scope | 6 (5 sub-requirements + 1 gate) |
| Automated coverage | 6 / 6 |
| Gaps found (MISSING) | 0 |
| Gaps found (PARTIAL) | 0 |
| Resolved by reconstruction | 0 (none required) |
| Escalated to Manual-Only | 0 |
| Tests added by Phase 1.5 | 4 new files + 1 extended (`recovery_corpus.zig`) + 3 strengthened (`recovery_*.zig`) |
| Build steps added | 2 (`phase-1.5-gate`, `phase-1.5-gate-fresh`) |
| ADRs accepted | 2 (m01 SD-10, fresh-worktree verification) |
| Surprise #8 sub-items closed | 10 (9 Resolved + 1 Deferred) |
| Phase-exit gate result | `33/33 tests passed`, EXIT 0 (per Plan 05 Task 5 + Plan 05 re-verification 2026-05-21) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify (no manual-only behaviors)
- [x] Sampling continuity: every plan wave has at least one named test filter; no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (Phase 1.5 had none — existing Phase 1 infrastructure was reused)
- [x] No watch-mode flags (Zig `zig build test` is one-shot)
- [x] Feedback latency < 10 s for the binding `phase-1.5-gate-fresh`; < 2 s for any filtered subset
- [x] `nyquist_compliant: true` set in frontmatter
- [x] False-GREEN class of bug structurally prevented (ADR-phase-1.5-fresh-worktree-verification binds Phases 2..6)

**Approval:** approved 2026-05-21 (reconstructed retrospectively from per-plan SUMMARYs and 01.5-VERIFICATION-PLAN05.md; no gaps required filling)
