---
phase: 01.5-parser-strictness-test-gap-resolution-inserted
plan: 05
type: execute
wave: 5
depends_on: [01.5-01, 01.5-02, 01.5-03, 01.5-04]
files_modified:
  - .gitmodules
  - spec
  - tests/showcase
  - build.zig
  - scripts/verify-fresh-worktree.sh
  - README.md
  - .planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-VERIFICATION.md
  - .planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-CONTEXT.md
  - .planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md
autonomous: false
gap_closure: true
requirements:
  - REQ-phase-1.5-gate
  - REQ-phase-1.5-strictness-and-tests
tags:
  - gap-closure
  - submodule
  - verification-hardening
  - phase-1.5
must_haves:
  truths:
    - "On any FRESH `deal/` worktree (no prior local symlink), after `git submodule update --init --recursive`, `zig build phase-1.5-gate` exits 0 and runs all 33 unit tests (the 24 that already passed + the 9 previously gated on tests/showcase)."
    - "`tests/showcase/packages/vehicle/battery.deal` (and every other showcase path the 9 failing tests read) resolves to a real, readable file when the cwd is the deal repo root, via a relative symlink that does not depend on any sibling-directory layout outside the deal repo."
    - "The 9 previously-failing tests each exit 0 when run via `zig build test -Dtest-filter=<name>` from a fresh-worktree checkout (after submodule init): lexer.snapshot, lexer.spans_monotonic, parser_deal.snapshot, parser_deal.coverage, parser_dealx.snapshot, parser_dealx.coverage, property.span_containment, determinism.parse_twice, c_abi.no_leaks."
    - "Future Phase 1.5 (or successor) gates can NEVER be declared GREEN based on a developer's main-repo state — the verification process mechanically exercises a freshly-created worktree (or ephemeral CI runner) that materializes inputs only via committed git state + `git submodule update --init --recursive`."
    - "`tests/showcase` is tracked by the deal repo as a committed symlink (no longer appears under `git status` Untracked files) AND the showcase corpus content lives in a pinned submodule revision (auditable via `git submodule status`)."
    - "The previously-false `01.5-VERIFICATION.md` GREEN claim is amended to document the false-GREEN incident, its root cause, and the new fresh-worktree gate that prevents recurrence."
    - "`README.md` instructs any new clone to run `git submodule update --init --recursive` before `zig build phase-1.5-gate`."
  artifacts:
    - path: ".gitmodules"
      provides: "Declares spec/ as a submodule of deal/ at mount path `spec`, pinned to a specific commit."
      contains: 'path = spec'
    - path: "spec"
      provides: "Submodule mount — git treats this as a gitlink to the pinned spec/ revision."
      kind: "gitlink (mode 160000)"
    - path: "tests/showcase"
      provides: "Committed symbolic link `tests/showcase -> ../spec/examples/showcase`; resolves to the submodule's showcase corpus when cwd is the repo root."
      kind: "symlink (mode 120000)"
      contents_after_resolve: "../spec/examples/showcase"
    - path: "scripts/verify-fresh-worktree.sh"
      provides: "Executable shell script that creates an ephemeral git worktree, runs `git submodule update --init --recursive` inside it, and runs `zig build phase-1.5-gate` from that worktree. Exits 0 iff the gate exits 0 in the ephemeral worktree. Cleans up the worktree on exit (success or failure)."
      min_lines: 40
    - path: "build.zig"
      provides: "New `phase-1.5-gate-fresh` step that shells out to `scripts/verify-fresh-worktree.sh`. Depends on no other build steps (the script is self-contained and creates its own worktree)."
      contains: "phase-1.5-gate-fresh"
    - path: "README.md"
      provides: "Initial-tracked README documenting the submodule-init invariant: `git submodule update --init --recursive` is mandatory after clone."
      contains: "git submodule update --init --recursive"
    - path: ".planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md"
      provides: "Locks the invariant: Phase N exit verification MUST be performed from a freshly-created worktree (or ephemeral CI runner) that materializes all inputs only via committed git state. Records the false-GREEN incident as the motivating evidence."
      status: "accepted"
    - path: ".planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-VERIFICATION.md"
      provides: "Amended verification record: the original 2026-05-20 GREEN claim is annotated as false (root cause + diagnosis link), and the new fresh-worktree-verified GREEN claim replaces it."
      contains: "false-GREEN"
    - path: ".planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-CONTEXT.md"
      provides: "Appended invariant block referencing the new ADR and the fresh-worktree gate."
      contains: "fresh-worktree"
  key_links:
    - from: "tests/unit/property_span_containment.zig:409"
      to: "deal/spec/examples/showcase/packages/vehicle/battery.deal"
      via: "tests/showcase symlink -> ../spec/examples/showcase + submodule materialization"
      pattern: 'readFileAlloc.*tests/showcase'
    - from: "tests/unit/determinism_parse_twice.zig:36-55"
      to: "deal/spec/examples/showcase/packages/**/*.deal[x]"
      via: "Hardcoded `tests/showcase/...` paths resolved through the new symlink + submodule"
      pattern: 'tests/showcase/'
    - from: "scripts/verify-fresh-worktree.sh"
      to: "zig build phase-1.5-gate"
      via: "ephemeral worktree + submodule init + zig invocation"
      pattern: "zig build phase-1.5-gate"
    - from: "build.zig phase-1.5-gate-fresh step"
      to: "scripts/verify-fresh-worktree.sh"
      via: "b.addSystemCommand + step.dependOn"
      pattern: "phase-1.5-gate-fresh"
    - from: "README.md"
      to: ".gitmodules"
      via: "Documented `git submodule update --init --recursive` instruction"
      pattern: "submodule update --init"
---

<objective>
Close the single blocker gap from `01.5-UAT.md`: `tests/showcase/` is unavailable on any fresh `deal/` worktree, causing 9 of 33 unit tests to fail with `error.FileNotFound`. The original Phase 1.5 exit gate was only "green" because verification ran from the developer's main checkout — which contained an UNTRACKED, never-committed local symlink to a sibling git repo. This plan makes the showcase corpus reproducibly available via a committed submodule + committed symlink, and hardens the verification process so future phases cannot be declared green based on a developer's local-only working-tree state.

Purpose: Without this fix, Phase 1.5 cannot legitimately close, and Phase 2 (semantic analysis) would inherit a parser whose acceptance tests cannot be re-run in any clean environment. The fix also installs a structural guard rail (`phase-1.5-gate-fresh` + ADR) preventing the same class of false-GREEN from recurring in Phases 2 through 6.

Output:
  - `.gitmodules` + `spec` gitlink committing spec/ at its current `fe98f89` revision (or whichever commit is current at execution time)
  - `tests/showcase` committed as a symlink pointing into the submodule mount
  - `scripts/verify-fresh-worktree.sh` + `build.zig phase-1.5-gate-fresh` step running the ephemeral-worktree verification
  - `README.md` initially-tracked with the submodule-init instruction
  - `.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md` locking the invariant
  - `01.5-VERIFICATION.md` amended to record the false-GREEN incident and the new authoritative gate
  - `01.5-CONTEXT.md` amended with the fresh-worktree-verification invariant
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-UAT.md
@.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-VERIFICATION.md
@.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-CONTEXT.md
@.planning/debug/phase-1-5-gate-failure.md
@build.zig
@README.md
@.gitignore
@tests/unit/property_span_containment.zig
@tests/unit/determinism_parse_twice.zig

<interfaces>
<!-- Concrete facts extracted from the workspace at PLAN-time. Executor should treat these as authoritative; if any disagrees with the current state, the executor MUST stop and re-read before acting. -->

Repository facts (verified at plan-time, commit bc20009):
  - deal/ git remote: NONE (deal repo is local-only at the moment; `git remote -v` returns empty). The submodule is initialized from a LOCAL path, not a network URL. The spec/ submodule's `url` recorded in `.gitmodules` will use the canonical github URL `git@github.com:deal-lang/spec.git`, but the initial `git submodule add` MUST be performed against the local path `/Users/dunnock/projects/deal-lang/spec` to populate the working tree quickly and avoid SSH key dependence at plan-execution time. The recorded URL in `.gitmodules` is then edited to the canonical github URL before commit. (See Task 1 step-by-step.)
  - spec/ git remote: origin = `git@github.com:deal-lang/spec.git` (fetch+push); HEAD on branch `main` at `fe98f893cefed72a56498f3e0271d459c13a1210`.
  - deal/ has no existing path named `spec` (verified `ls deal/spec` -> ENOENT). Safe to use as the submodule mount.
  - deal/.gitignore lines: `zig-out/`, `.zig-cache/`, `.codegraph/`, `tests/ffi/target/`, `tests/ffi/Cargo.lock`, `.DS_Store`. Nothing ignores `spec/` or `tests/showcase`.
  - Untracked at HEAD bc20009: `.claude/`, `README.md`, `tests/showcase`. (README.md and tests/showcase are EXPECTED to be tracked by this plan; .claude/ stays untracked.)
  - Existing worktrees: main checkout + locked worktree at `.claude/worktrees/agent-a00e64992513aad6e/` (used by the diagnosis agent). DO NOT touch the locked worktree.
  - Showcase corpus = 19 files (verified: `find spec/examples/showcase -type f \( -name '*.deal' -o -name '*.dealx' \) | wc -l = 19`). Matches the file lists in determinism_parse_twice.zig:36-55 and property_span_containment.zig:46-78.

Symlink target math:
  - PRE (current main checkout, untracked): `tests/showcase -> ../../spec/examples/showcase` → resolves from `deal/tests/` to `deal/tests/../../spec/examples/showcase` = `/Users/dunnock/projects/deal-lang/spec/examples/showcase`. Works only because `spec/` is a sibling of `deal/` on this developer's machine.
  - POST (committed, this plan): `tests/showcase -> ../spec/examples/showcase` → resolves from `deal/tests/` to `deal/tests/../spec/examples/showcase` = `deal/spec/examples/showcase`. Works because `deal/spec` is the submodule mount inside the deal repo. Survives any clone, worktree, or CI runner.
  - The tests themselves (property_span_containment.zig:407, determinism_parse_twice.zig:64, c_abi_no_leaks.zig:61,83, all the *_snapshot.zig/*_coverage.zig readers) call `cwd.readFileAlloc(io, "tests/showcase/...", ...)`. As long as cwd is the repo root AND `tests/showcase` is a valid symlink, the existing test code requires ZERO modification. **This plan modifies ZERO test source files.**

Test enumeration (the 9 that currently fail on fresh worktree, confirmed by `.planning/debug/phase-1-5-gate-failure.md` evidence block 2026-05-21T00:18:00Z):
  1. lexer_snapshot.test.lexer.snapshot
  2. lexer_snapshot.test.lexer.spans_monotonic  (filter name: "lexer.snapshot spans monotonic" per build summary; will be matched by `-Dtest-filter=lexer.spans_monotonic` if the test name in source includes that substring — see Task 5 verification step for the exact filters)
  3. parser_deal_snapshot.test.parser_deal.snapshot
  4. parser_deal_coverage.test.parser_deal.coverage
  5. parser_dealx_snapshot.test.parser_dealx.snapshot
  6. parser_dealx_coverage.test.parser_dealx.coverage
  7. property_span_containment.test.property.span_containment
  8. determinism_parse_twice.test.determinism.parse_twice
  9. c_abi_no_leaks.test.c_abi.no_leaks
  All 9 share the same FileNotFound stack on `tests/showcase/*`. Confirming they ALL pass post-fix in a fresh worktree is the capstone verification (Task 5).

Submodule semantics (Zig build does not interact with git, so this is purely a checkout-time invariant):
  - `git submodule add <local-path> spec` creates `.gitmodules` and stages the gitlink. The `<local-path>` form populates the working tree from the local path immediately.
  - After `git submodule add`, edit `.gitmodules` to replace the local URL with the canonical `git@github.com:deal-lang/spec.git` (this is what a fresh clone will use). Re-run `git submodule sync` to update `.git/config`.
  - Commit `.gitmodules` + `spec` gitlink in a single commit.
  - On a fresh clone: `git clone <deal-repo> && cd deal && git submodule update --init --recursive` populates spec/ at the pinned revision.

Plan structure consequence:
  - Tasks 1 and 2 MUST run sequentially in the same git tree (Task 2's symlink only resolves once Task 1's submodule mount exists).
  - Tasks 3 (script + build wiring) and 4 (docs/ADR/VERIFICATION amendment) can be drafted in parallel with each other after Task 2, but are presented sequentially here so the executor sees one clear path.
  - Task 5 is the BLOCKING-HUMAN checkpoint: the executor runs the new `phase-1.5-gate-fresh` step and reports the results; the user verifies that all 33 tests pass in the ephemeral worktree before declaring closure.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1a: Confirm spec/ submodule URL and pinned revision</name>
  <decision>
    Confirm the canonical remote URL for the spec/ submodule recorded in `.gitmodules`, and confirm that pinning at spec's current HEAD (`fe98f893cefed72a56498f3e0271d459c13a1210`, branch `main`) is acceptable. The diagnosis in `01.5-UAT.md` requires the submodule to have a fetchable remote so fresh CI runners can populate it.
  </decision>
  <context>
    Plan-time facts (verified by `git -C /Users/dunnock/projects/deal-lang/spec remote -v`):
      - origin (fetch): `git@github.com:deal-lang/spec.git`
      - origin (push):  `git@github.com:deal-lang/spec.git`
      - HEAD: `fe98f893cefed72a56498f3e0271d459c13a1210` (branch main)
    The deal repo itself has NO remote configured (`git -C deal remote -v` is empty), so this plan does NOT need to choose a deal URL. The submodule URL recorded in `.gitmodules` must be reachable by any future fresh-clone CI runner. The SSH form above requires a key; if the user prefers HTTPS, propose `https://github.com/deal-lang/spec.git` instead. Either is fine; the canonical github URL just needs to be reachable by the intended CI environment.
  </context>
  <options>
    <option id="option-a">
      <name>Use SSH URL `git@github.com:deal-lang/spec.git` (matches current spec/ origin)</name>
      <pros>Already the spec repo's recorded origin. No drift between developer-local and committed config.</pros>
      <cons>CI runners must have an SSH deploy key. Anonymous clones will fail until HTTPS is added (post-Phase 1.5 concern).</cons>
    </option>
    <option id="option-b">
      <name>Use HTTPS URL `https://github.com/deal-lang/spec.git`</name>
      <pros>Anonymous fetch works on CI without keys. Better for public docs / open-source CI.</pros>
      <cons>If `deal-lang/spec` is currently private on GitHub, anonymous HTTPS will 404 until the repo is made public or a token is supplied.</cons>
    </option>
    <option id="option-c">
      <name>Re-pin to a different spec revision (NOT HEAD)</name>
      <pros>Use this only if the user has a specific spec commit they want frozen for Phase 1.5 (e.g. a pre-2026-05-21 commit predating any in-flight Phase 2 work).</pros>
      <cons>Requires the user to name the SHA. Current HEAD `fe98f89` is what the local symlink resolves to right now, so option-a/b+HEAD is the "preserve current behavior exactly" choice.</cons>
    </option>
  </options>
  <resume-signal>Select: `option-a`, `option-b`, or `option-c {sha}`. Default if unanswered after presenting this checkpoint: option-a (matches current spec/ origin exactly).</resume-signal>
</task>

<task type="auto">
  <name>Task 1b: Add spec/ as a git submodule of deal/ at mount path `spec`</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/.gitmodules
    /Users/dunnock/projects/deal-lang/deal/spec
    /Users/dunnock/projects/deal-lang/deal/.git/config
  </files>
  <action>
    Create the submodule mount. ALL commands are absolute-path and assume cwd = `/Users/dunnock/projects/deal-lang/deal`.

    Pre-flight checks (abort with a clear error message if any fail):
      1. `git -C /Users/dunnock/projects/deal-lang/deal status --porcelain` MUST NOT show staged changes for `spec/` or `.gitmodules` (a previous attempt did not finish). If staged, `git rm --cached spec && rm -f .gitmodules` to reset, then retry.
      2. `test ! -e /Users/dunnock/projects/deal-lang/deal/spec` (path is currently free).
      3. `git -C /Users/dunnock/projects/deal-lang/spec rev-parse HEAD` MUST succeed (spec/ exists with a real commit).

    Execute the submodule add (use the LOCAL path to populate quickly, then rewrite the URL to the canonical remote chosen in Task 1a):

      git -C /Users/dunnock/projects/deal-lang/deal submodule add /Users/dunnock/projects/deal-lang/spec spec

    After that succeeds:
      - Verify `.gitmodules` was created and contains `path = spec` and a `url = ` line. The `url` value will currently be `/Users/dunnock/projects/deal-lang/spec` (the local path).
      - Rewrite the URL to the canonical remote chosen by the user in Task 1a (e.g. `git@github.com:deal-lang/spec.git` for option-a):

          git -C /Users/dunnock/projects/deal-lang/deal config -f .gitmodules submodule.spec.url <CHOSEN_URL>
          git -C /Users/dunnock/projects/deal-lang/deal submodule sync spec

      - Verify the working tree at `deal/spec/` is populated with spec's content (look for `deal/spec/examples/showcase/packages/vehicle/battery.deal` — it MUST exist and be a regular file, not a broken symlink).
      - Verify `git -C /Users/dunnock/projects/deal-lang/deal submodule status` reports the submodule at the expected SHA (the HEAD of `/Users/dunnock/projects/deal-lang/spec` at execution time — if the user chose option-c in Task 1a, that SHA instead).
      - Verify `git -C /Users/dunnock/projects/deal-lang/deal ls-files --stage spec` shows mode `160000` (gitlink) at path `spec`.

    DO NOT commit yet. Task 2 stages the symlink change alongside this, and they commit together.

    Revert path (if any verification fails):
      git -C /Users/dunnock/projects/deal-lang/deal submodule deinit -f spec
      git -C /Users/dunnock/projects/deal-lang/deal rm -f spec
      rm -rf /Users/dunnock/projects/deal-lang/deal/.git/modules/spec
      rm -f /Users/dunnock/projects/deal-lang/deal/.gitmodules
      # Working tree is back to its pre-task state. Investigate the failure (likely a stale gitlink or permissions) before retrying.
  </action>
  <verify>
    <automated>
      test -f /Users/dunnock/projects/deal-lang/deal/.gitmodules &amp;&amp; \
      grep -q 'path = spec' /Users/dunnock/projects/deal-lang/deal/.gitmodules &amp;&amp; \
      grep -q '^\s*url = ' /Users/dunnock/projects/deal-lang/deal/.gitmodules &amp;&amp; \
      ! grep -q 'url = /Users/dunnock/projects/deal-lang/spec' /Users/dunnock/projects/deal-lang/deal/.gitmodules &amp;&amp; \
      test -f /Users/dunnock/projects/deal-lang/deal/spec/examples/showcase/packages/vehicle/battery.deal &amp;&amp; \
      git -C /Users/dunnock/projects/deal-lang/deal ls-files --stage spec | grep -E '^160000 [0-9a-f]+ 0\s+spec$' &amp;&amp; \
      git -C /Users/dunnock/projects/deal-lang/deal submodule status spec | grep -E '^[ +-][0-9a-f]+ spec'
    </automated>
  </verify>
  <done>
    `.gitmodules` exists with `path = spec` and a non-local `url`. `deal/spec/examples/showcase/` is populated with real files (the showcase corpus). `git ls-files --stage spec` shows mode 160000 (gitlink). The submodule is staged but not yet committed (commit happens at the end of Task 2 in a single atomic commit).
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-point tests/showcase symlink to the submodule, commit submodule + symlink atomically</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/tests/showcase
    /Users/dunnock/projects/deal-lang/deal/.gitmodules
    /Users/dunnock/projects/deal-lang/deal/spec
  </files>
  <action>
    Replace the existing untracked symlink with a new symlink that resolves through the submodule, then commit `.gitmodules` + `spec` gitlink + `tests/showcase` in ONE atomic commit. All paths absolute; cwd = `/Users/dunnock/projects/deal-lang/deal`.

    Pre-flight checks:
      1. `readlink /Users/dunnock/projects/deal-lang/deal/tests/showcase` MUST return `../../spec/examples/showcase` (the current untracked symlink). If it returns something else, STOP — the symlink has already been altered by another process, investigate before continuing.
      2. `git -C /Users/dunnock/projects/deal-lang/deal status --porcelain tests/showcase` MUST show `?? tests/showcase` (untracked). NOT `M ` (modified) — Task 2 is the first time it is tracked.
      3. `test -d /Users/dunnock/projects/deal-lang/deal/spec/examples/showcase` (submodule populated by Task 1b).

    Replace the symlink (cannot use `git mv` for an untracked symlink):
      rm /Users/dunnock/projects/deal-lang/deal/tests/showcase
      ln -s ../spec/examples/showcase /Users/dunnock/projects/deal-lang/deal/tests/showcase

    Resolution check (CRITICAL — verifies the math in <interfaces>):
      cd /Users/dunnock/projects/deal-lang/deal &amp;&amp; \
      test -f tests/showcase/packages/vehicle/battery.deal &amp;&amp; \
      test -f tests/showcase/model/vehicle.dealx
    Both files MUST exist via the new symlink. If either fails, the symlink target is wrong — verify Task 1b populated `deal/spec/examples/showcase/`.

    Stage and commit (all three changes in one commit so any future checkout sees the symlink AND its target submodule together):
      git -C /Users/dunnock/projects/deal-lang/deal add .gitmodules spec tests/showcase
      git -C /Users/dunnock/projects/deal-lang/deal commit -m "$(cat <<'COMMIT_MSG'
build(01.5): track spec/ as submodule + commit tests/showcase symlink

Closes the Phase 1.5 false-GREEN gap diagnosed in 01.5-UAT.md.

- `.gitmodules` declares spec/ as a submodule of deal/ at mount path `spec`,
  pinned to spec/'s HEAD at plan-time. URL chosen per Task 1a decision.
- `tests/showcase` is now a COMMITTED symlink `-> ../spec/examples/showcase`
  (previously: untracked `-> ../../spec/examples/showcase` resolving outside
  the deal repo via the developer's sibling-directory layout).
- The 9 tests that read `tests/showcase/...` (lexer/parser snapshots,
  coverage, property.span_containment, determinism.parse_twice,
  c_abi.no_leaks) now have inputs available on any fresh worktree after
  `git submodule update --init --recursive`.

This is Plan 01.5-05 Task 2; verification gate runs in Task 5.
COMMIT_MSG
)"

    Post-commit verification:
      git -C /Users/dunnock/projects/deal-lang/deal log -1 --stat
    The diff stat MUST list exactly three paths: `.gitmodules`, `spec` (gitlink change), `tests/showcase` (new symlink).

      git -C /Users/dunnock/projects/deal-lang/deal ls-tree HEAD tests/
    MUST now include a `showcase` entry of mode `120000` (symlink). The pre-fix output was `ffi malformed snapshots unit` ONLY; post-fix MUST be `ffi malformed showcase snapshots unit`.

      git -C /Users/dunnock/projects/deal-lang/deal status --porcelain | grep -E '^\?\?\s+tests/showcase' &amp;&amp; echo FAIL || echo OK
    MUST print `OK` (no longer untracked).

    Revert path (if commit verification fails):
      git -C /Users/dunnock/projects/deal-lang/deal reset --soft HEAD~1
      # Then re-stage and fix the issue. DO NOT `reset --hard` — would lose Task 1b's working-tree state.
  </action>
  <verify>
    <automated>
      readlink /Users/dunnock/projects/deal-lang/deal/tests/showcase | grep -Fxq '../spec/examples/showcase' &amp;&amp; \
      test -f /Users/dunnock/projects/deal-lang/deal/tests/showcase/packages/vehicle/battery.deal &amp;&amp; \
      test -f /Users/dunnock/projects/deal-lang/deal/tests/showcase/model/vehicle.dealx &amp;&amp; \
      git -C /Users/dunnock/projects/deal-lang/deal ls-tree HEAD tests/ | grep -E '^120000 blob [0-9a-f]+\s+tests/showcase$' &amp;&amp; \
      git -C /Users/dunnock/projects/deal-lang/deal ls-tree HEAD spec | grep -E '^160000 commit [0-9a-f]+\s+spec$' &amp;&amp; \
      git -C /Users/dunnock/projects/deal-lang/deal ls-tree HEAD .gitmodules | grep -E '^100644 blob [0-9a-f]+\s+\.gitmodules$' &amp;&amp; \
      ! (git -C /Users/dunnock/projects/deal-lang/deal status --porcelain | grep -qE '^\?\?\s+tests/showcase')
    </automated>
  </verify>
  <done>
    `tests/showcase` is a committed symlink in `git ls-tree HEAD`, target `../spec/examples/showcase`, resolving to a real directory inside the submodule mount. `spec` is a committed gitlink. `.gitmodules` is committed. `git status` no longer shows `tests/showcase` as untracked. `tests/showcase/packages/vehicle/battery.deal` is readable via the symlink — meaning the 9 previously-failing tests' read paths now exist. Test source files were NOT modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add fresh-worktree verification script + build.zig step</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh
    /Users/dunnock/projects/deal-lang/deal/build.zig
  </files>
  <action>
    Add the structural gate that prevents recurrence of the false-GREEN incident (DECISION 2 in the planning context).

    Step A — Create `scripts/verify-fresh-worktree.sh`:
      - Directory: create `/Users/dunnock/projects/deal-lang/deal/scripts/` if it does not exist (`mkdir -p`).
      - Shebang: `#!/usr/bin/env bash`. Use `set -euo pipefail` for fail-fast behavior. Use `IFS=$'\n\t'` for safe word-splitting.
      - Script responsibilities (in order):
          1. Resolve the deal repo root: take it from the script's own location via `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" &amp;&amp; pwd)"; REPO_ROOT="$(cd "$SCRIPT_DIR/.." &amp;&amp; pwd)"`. Refuse to run if `REPO_ROOT/.git` is missing.
          2. Reject running on a dirty tree: `git -C "$REPO_ROOT" diff --quiet &amp;&amp; git -C "$REPO_ROOT" diff --cached --quiet` — if there are uncommitted changes, refuse with a clear error. Rationale: the gate verifies COMMITTED state; running it on a dirty tree would mask exactly the class of bug we are guarding against.
          3. Create an ephemeral worktree: `WORKTREE_DIR="$(mktemp -d -t deal-verify-XXXXXX)"`. Register a `trap` that removes the worktree on EXIT (both success and failure):
                trap 'git -C "$REPO_ROOT" worktree remove --force "$WORKTREE_DIR" 2>/dev/null || true; rm -rf "$WORKTREE_DIR"' EXIT
          4. Attach the worktree at HEAD: `git -C "$REPO_ROOT" worktree add "$WORKTREE_DIR" HEAD`.
          5. Initialize submodules INSIDE the worktree: `git -C "$WORKTREE_DIR" submodule update --init --recursive`. This is the operation that materializes `spec/` and (transitively) `tests/showcase`'s target. If this fails (network error, missing key, missing remote), exit non-zero with a clear message.
          6. Sanity-check the showcase is materialized: `test -f "$WORKTREE_DIR/tests/showcase/packages/vehicle/battery.deal"`. If absent, exit non-zero with "tests/showcase materialization failed — submodule did not populate as expected".
          7. Run the gate: `cd "$WORKTREE_DIR" &amp;&amp; zig build phase-1.5-gate 2>&amp;1 | tee "$REPO_ROOT/.zig-cache/verify-fresh-worktree.log"`. Capture the exit code; on non-zero, print "fresh-worktree gate FAILED — see log" and exit with the captured code.
          8. On success, print "fresh-worktree gate PASSED" with the worktree path and the build summary line, then exit 0. (The trap removes the worktree.)
      - `chmod +x` the script after writing.
      - Inline comment at the top of the script citing this plan + the ADR introduced in Task 4 + the original UAT/debug references, so a future maintainer reading the script understands why it exists.

    Step B — Wire `phase-1.5-gate-fresh` into `build.zig`:
      - Locate the existing `phase-1.5-gate` step (build.zig:206). The new step is declared IMMEDIATELY AFTER it, inside the same `pub fn build` body.
      - Use `b.addSystemCommand` to invoke the script. The command argv is `[_][]const u8{ "bash", "scripts/verify-fresh-worktree.sh" }`. Rationale for using `bash` explicitly rather than relying on `#!/usr/bin/env bash`: build.zig's working directory at `zig build` time is the repo root, but the script must exist there with execute permission; invoking via `bash <path>` works regardless of file mode and is portable across the Zig 0.16.0 builder API.
      - The new step does NOT `dependOn(test_step)` or any other step — the script itself runs the gate inside the ephemeral worktree, and depending on `test_step` would also build it in the MAIN tree which defeats the purpose.
      - Step name: `phase-1.5-gate-fresh`. Description (the second arg to `b.step`): "Run phase-1.5-gate inside a freshly-created ephemeral worktree with submodule init (closes the false-GREEN class of bug per ADR-phase-1.5-fresh-worktree-verification)".
      - Add a NOTE comment above the step block explaining the gate's purpose and how it differs from `phase-1.5-gate` (one sentence each), and explicitly noting that future phase gates (phase-2-gate, etc.) SHOULD have a `-fresh` sibling for the same reason.

    Verification before commit:
      bash -n /Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh   # syntax check
      cd /Users/dunnock/projects/deal-lang/deal &amp;&amp; zig build --help 2>&amp;1 | grep -F 'phase-1.5-gate-fresh'   # step is registered
      # DO NOT actually run `zig build phase-1.5-gate-fresh` here — that is Task 5's job.

    Commit:
      git -C /Users/dunnock/projects/deal-lang/deal add scripts/verify-fresh-worktree.sh build.zig
      git -C /Users/dunnock/projects/deal-lang/deal commit -m "$(cat <<'COMMIT_MSG'
build(01.5): add phase-1.5-gate-fresh + scripts/verify-fresh-worktree.sh

Closes DECISION 2 in 01.5-05-PLAN.md: structural guard rail preventing
the false-GREEN class of bug diagnosed in 01.5-UAT.md.

- `scripts/verify-fresh-worktree.sh`: bash script that creates an ephemeral
  worktree at HEAD, runs `git submodule update --init --recursive` inside
  it, materializes `tests/showcase` via the spec/ submodule, then runs
  `zig build phase-1.5-gate` from the ephemeral tree. Trap-cleans the
  worktree on exit. Refuses to run on a dirty main tree.
- `build.zig phase-1.5-gate-fresh` step: thin wrapper invoking the script.
  Does NOT depend on test_step (would build in the main tree, defeating
  the purpose). Sibling to phase-1.5-gate; future phase gates (2..6)
  SHOULD add their own -fresh sibling per the same pattern.

Plan 01.5-05 Task 3.
COMMIT_MSG
)"

    Revert path (if commit verification fails):
      git -C /Users/dunnock/projects/deal-lang/deal reset --soft HEAD~1
      rm -f /Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh
      # Restore build.zig from the previous commit:
      git -C /Users/dunnock/projects/deal-lang/deal checkout HEAD -- build.zig
  </action>
  <verify>
    <automated>
      test -x /Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh &amp;&amp; \
      bash -n /Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh &amp;&amp; \
      grep -q 'git worktree add' /Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh &amp;&amp; \
      grep -q 'submodule update --init --recursive' /Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh &amp;&amp; \
      grep -q 'phase-1.5-gate' /Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh &amp;&amp; \
      grep -q "set -euo pipefail" /Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh &amp;&amp; \
      grep -q 'phase-1.5-gate-fresh' /Users/dunnock/projects/deal-lang/deal/build.zig &amp;&amp; \
      (cd /Users/dunnock/projects/deal-lang/deal &amp;&amp; zig build --help 2>&amp;1 | grep -Fq 'phase-1.5-gate-fresh')
    </automated>
  </verify>
  <done>
    `scripts/verify-fresh-worktree.sh` exists, is executable, has valid bash syntax, contains the four critical operations (`git worktree add`, `submodule update --init --recursive`, materialization-check on `tests/showcase/packages/vehicle/battery.deal`, `zig build phase-1.5-gate`), and uses `set -euo pipefail` for fail-fast. `build.zig` declares the `phase-1.5-gate-fresh` step, visible in `zig build --help`. NOT YET executed end-to-end — that is Task 5's blocking checkpoint.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update README + author ADR + amend VERIFICATION + amend CONTEXT</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/README.md
    /Users/dunnock/projects/deal-lang/deal/.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-VERIFICATION.md
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-CONTEXT.md
  </files>
  <action>
    Update the documentation surface so the submodule-init invariant + fresh-worktree gate are discoverable by any future agent or human.

    Step A — `README.md`:
      The file currently exists but is UNTRACKED in the deal repo (git status pre-task confirms). This task is the first time README.md is added to git.
      Add a new top-of-file section IMMEDIATELY AFTER the title line `# deal-lang/deal` and before `## Current Phase`. Section heading: `## Repository Layout`. Body (use exactly this prose, adapting only the URL if Task 1a chose option-b):

        ## Repository Layout

        The DEAL grammar specification + showcase corpus lives in the `deal-lang/spec`
        repository, mounted as a git submodule at `./spec` and referenced from the
        test suite via the symlink `tests/showcase -> ../spec/examples/showcase`.

        **After any fresh clone or `git worktree add`, run:**

        ```bash
        git submodule update --init --recursive
        ```

        Without this step, 9 of the 33 unit tests will fail with `error.FileNotFound`
        because `tests/showcase/` resolves through the submodule mount. See
        `.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md` for the
        full background.

        The CI-authoritative gate that mechanically exercises this requirement is:

        ```bash
        zig build phase-1.5-gate-fresh
        ```

        which creates an ephemeral worktree, runs `git submodule update --init --recursive`
        from inside it, and runs `zig build phase-1.5-gate` against that ephemeral
        tree. The convenience single-tree gate `zig build phase-1.5-gate` is still
        available for the developer's main checkout.

      Also update the existing Planned Layout block (currently shows `tests/showcase/ # copied or referenced showcase inputs`) to reflect reality: `tests/showcase/   # symlink -> ../spec/examples/showcase (submodule)`.

    Step B — `.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md`:
      Create a new ADR file. Use the same frontmatter shape as the existing `ADR-phase-1.5-m01-sd10-optional-semicolon.md` (read it first to mirror the convention exactly — fields likely include `status`, `phase`, `date`, `decision`). Required content:
        - status: accepted
        - phase: 01.5
        - date: today's date in ISO 8601
        - Context section: summarize the false-GREEN incident — main-repo verification reported phase-1.5-gate as green (`01.5-VERIFICATION.md:73`), but the gate required `tests/showcase/` which existed only as an untracked local symlink. Cite `01.5-UAT.md` and `.planning/debug/phase-1-5-gate-failure.md`. Include the 9-test failure list verbatim.
        - Decision section (the binding invariant): "Phase N exit verification MUST be performed from a freshly-created git worktree (or ephemeral CI runner) that materializes all inputs only via committed git state plus `git submodule update --init --recursive`. The main developer checkout is NEVER an acceptable verification site for a phase-exit gate. The mechanical implementation for Phase 1.5 is `zig build phase-1.5-gate-fresh`; future phase gates (phase-2-gate, phase-3-gate, ...) MUST add an analogous `-fresh` sibling step."
        - Consequences section: lists the structural guard rails added (`scripts/verify-fresh-worktree.sh`, `phase-1.5-gate-fresh` build step), the committed-state changes (`tests/showcase` symlink + spec submodule), and the doc-surface changes (README + this ADR + amended VERIFICATION + amended CONTEXT). Note that this invariant has NO runtime cost — it is a CI/verification protocol change.
        - Alternatives considered section: the three options listed in `01.5-UAT.md` gap.missing (OPTION A commit-as-is, OPTION C vendor). For each, give a one-sentence rationale for rejection. (The user picked OPTION B — submodule — per `<user_decisions_locked>` in the planning context.)

    Step C — `01.5-VERIFICATION.md` amendment:
      Read the existing file (verified in plan-time context — frontmatter has `status: passed`, `verified: 2026-05-20T22:22:38Z`).
      Modify in-place (do NOT rewrite from scratch):
        - Frontmatter: change `status: passed` to `status: passed-after-amendment`. Add new field `re_verification: 2026-05-21T<execution-time>` (replacing the existing `re_verification: null`). Add new field `false_green_amended: true`.
        - Insert a new top-level section BEFORE the existing `## Goal Achievement` section, titled `## False-GREEN Amendment (2026-05-21)`. Body must:
            * Acknowledge that the 2026-05-20 verification reported GREEN based on the developer's main repo, where `tests/showcase` was a local untracked symlink to a sibling repo not part of the deal git tree.
            * State that 9 of the 33 unit tests (enumerate them by full test name as in the `<interfaces>` block) would have failed in any fresh worktree at that time.
            * Cite `01.5-UAT.md` (status: diagnosed, dated 2026-05-21) and `.planning/debug/phase-1-5-gate-failure.md` as the diagnostic record.
            * Reference the remediation: Plan `01.5-05` (this plan) committed the spec/ submodule + symlink + fresh-worktree gate. Cite the ADR.
            * State the new authoritative gate is `zig build phase-1.5-gate-fresh`, NOT the original `phase-1.5-gate` alone.
            * State that the re-verification timestamp at the top of this file refers to the post-Task-5 execution of `phase-1.5-gate-fresh`.
        - At the bottom of the file (after the existing `## Behavioral Spot-Checks` table), add a new row to the table:
            `| Fresh-worktree gate exits 0 | zig build phase-1.5-gate-fresh | exit 0 | PASS-after-amendment |`
          (Or whatever the actual result is after Task 5 runs; the executor records the real outcome.)
        - DO NOT delete or contradict the existing rows. They are preserved as historical record.

    Step D — `01.5-CONTEXT.md` amendment:
      Append a new top-level block at the END of the file (after the existing `<deferred>` section), titled:
        <invariants_added_post_completion>
        ## Phase 1.5 Post-Completion Invariants
      Body:
        - Reference the new ADR `.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md`.
        - State the invariant verbatim (same prose as the ADR's Decision section).
        - State the new authoritative gate command.
        - Note that this invariant binds future phases (2 through 6) and is therefore relevant to whoever plans Phase 2.
      Close the block with `</invariants_added_post_completion>`.

    Verification before commit:
      grep -q 'submodule update --init --recursive' /Users/dunnock/projects/deal-lang/deal/README.md
      grep -q 'phase-1.5-gate-fresh' /Users/dunnock/projects/deal-lang/deal/README.md
      test -f /Users/dunnock/projects/deal-lang/deal/.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md
      grep -q 'status: accepted' /Users/dunnock/projects/deal-lang/deal/.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md
      grep -q 'False-GREEN Amendment' /Users/dunnock/projects/deal-lang/deal/.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-VERIFICATION.md
      grep -q 'false_green_amended: true' /Users/dunnock/projects/deal-lang/deal/.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-VERIFICATION.md
      grep -q 'invariants_added_post_completion' /Users/dunnock/projects/deal-lang/deal/.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-CONTEXT.md

    Commit:
      git -C /Users/dunnock/projects/deal-lang/deal add README.md \
        .planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md \
        .planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-VERIFICATION.md \
        .planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-CONTEXT.md
      git -C /Users/dunnock/projects/deal-lang/deal commit -m "$(cat <<'COMMIT_MSG'
docs(01.5): README + ADR + amended VERIFICATION/CONTEXT for fresh-worktree gate

- README.md: first-time-tracked; adds Repository Layout section explaining the
  spec/ submodule + tests/showcase symlink + mandatory `git submodule update
  --init --recursive` after any fresh clone. Documents `zig build
  phase-1.5-gate-fresh` as the new CI-authoritative gate.
- ADR-phase-1.5-fresh-worktree-verification.md: locks the invariant that Phase
  N exit verification must run from a freshly-created worktree (or ephemeral
  CI runner) that materializes inputs only via committed git state +
  submodule init. Future phase gates (2..6) must add a `-fresh` sibling.
- 01.5-VERIFICATION.md: amended in-place — frontmatter status flipped to
  `passed-after-amendment`, new False-GREEN Amendment section enumerates
  the 9 tests that would have failed on any fresh worktree pre-Plan-05, cites
  01.5-UAT.md + debug session, references the new ADR + remediation.
- 01.5-CONTEXT.md: appended invariants_added_post_completion block citing
  the ADR and binding future phases.

Plan 01.5-05 Task 4.
COMMIT_MSG
)"

    Revert path: `git -C /Users/dunnock/projects/deal-lang/deal reset --soft HEAD~1 &amp;&amp; git checkout HEAD -- README.md .planning/`
  </action>
  <verify>
    <automated>
      grep -q 'git submodule update --init --recursive' /Users/dunnock/projects/deal-lang/deal/README.md &amp;&amp; \
      grep -q 'phase-1.5-gate-fresh' /Users/dunnock/projects/deal-lang/deal/README.md &amp;&amp; \
      git -C /Users/dunnock/projects/deal-lang/deal ls-tree HEAD README.md | grep -q '^100644' &amp;&amp; \
      test -f /Users/dunnock/projects/deal-lang/deal/.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md &amp;&amp; \
      grep -q 'status: accepted' /Users/dunnock/projects/deal-lang/deal/.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md &amp;&amp; \
      grep -q 'fresh-worktree' /Users/dunnock/projects/deal-lang/deal/.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md &amp;&amp; \
      grep -q 'False-GREEN' /Users/dunnock/projects/deal-lang/deal/.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-VERIFICATION.md &amp;&amp; \
      grep -q 'false_green_amended: true' /Users/dunnock/projects/deal-lang/deal/.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-VERIFICATION.md &amp;&amp; \
      grep -q 'invariants_added_post_completion' /Users/dunnock/projects/deal-lang/deal/.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-CONTEXT.md
    </automated>
  </verify>
  <done>
    README.md is committed (no longer untracked) and contains the submodule-init instruction + phase-1.5-gate-fresh reference. The new ADR exists and is `accepted`. 01.5-VERIFICATION.md is amended in-place with the False-GREEN section + amended frontmatter. 01.5-CONTEXT.md has the appended invariants block.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: Run the new fresh-worktree gate end-to-end and verify all 33 tests pass</name>
  <what-built>
    A committed `tests/showcase` symlink + `spec` submodule that materializes the showcase corpus reproducibly, a `scripts/verify-fresh-worktree.sh` ephemeral-worktree gate, a `phase-1.5-gate-fresh` build step, and updated docs/ADR/VERIFICATION amendments. This checkpoint is the FIRST end-to-end exercise of the new gate and is BLOCKING — Phase 1.5 cannot be re-declared green until this passes.
  </what-built>
  <how-to-verify>
    Run, in order, all of the following from the main checkout at `/Users/dunnock/projects/deal-lang/deal`. Each command MUST succeed exactly as described:

    1. Confirm the committed state:
       ```
       cd /Users/dunnock/projects/deal-lang/deal
       git status --porcelain
       ```
       MUST show NO entries for `.gitmodules`, `spec`, `tests/showcase`, `README.md`, `scripts/verify-fresh-worktree.sh`, `build.zig`, the new ADR, the amended VERIFICATION, or the amended CONTEXT (all are now committed by Tasks 2/3/4). `.claude/` may still appear as untracked — that is expected and unrelated.

    2. Confirm submodule status:
       ```
       git submodule status
       ```
       MUST print one line of the form `<sha> spec (heads/main)` or similar — no leading `-` (would mean uninitialized).

    3. Run the new fresh-worktree gate (THE AUTHORITATIVE TEST):
       ```
       zig build phase-1.5-gate-fresh
       ```
       Expected outcome:
         - The script creates an ephemeral worktree in `$TMPDIR/deal-verify-XXXXXX/`.
         - Runs `git submodule update --init --recursive` inside it (you may briefly see git progress output for the spec/ clone).
         - Runs `zig build phase-1.5-gate` inside the ephemeral worktree.
         - The build summary line MUST report `33/33 tests passed (0 failed)` (or whatever the total is after counting; the critical condition is 0 failed).
         - The script prints `fresh-worktree gate PASSED` and exits 0.
         - The ephemeral worktree is cleaned up automatically by the script's `trap`.

    4. Confirm the 9 previously-failing tests now pass individually IN THE EPHEMERAL WORKTREE (this exercises the targeted filters mentioned in the diagnosis). Re-run the script with a small modification — OR — if simpler, run them in your current main tree (which now has `spec/` initialized too):
       ```
       cd /Users/dunnock/projects/deal-lang/deal
       git submodule update --init --recursive   # ensure spec/ is populated locally
       zig build test -Dtest-filter=lexer.snapshot
       zig build test -Dtest-filter=parser_deal.snapshot
       zig build test -Dtest-filter=parser_deal.coverage
       zig build test -Dtest-filter=parser_dealx.snapshot
       zig build test -Dtest-filter=parser_dealx.coverage
       zig build test -Dtest-filter=property.span_containment
       zig build test -Dtest-filter=determinism.parse_twice
       zig build test -Dtest-filter=c_abi.no_leaks
       ```
       Each MUST exit 0. (If any silently passes with 0 tests run, that is the WR-02 substring-filter risk documented in build.zig — verify by re-running with `-Dtest-filter=lexer` etc. and confirming output mentions a non-zero test count. The unfiltered `phase-1.5-gate-fresh` run from step 3 is the authoritative check and has no silent-skip risk.)

    5. Confirm the original three-step CI-authoritative locked command (from ROADMAP success criterion #6) is also green inside the ephemeral worktree. The fresh-worktree script ran `phase-1.5-gate` only, which transitively covers `phase-1-gate` + the unfiltered suite. To also exercise the three-step form, the simplest path is to re-run the script after temporarily adding the three filtered commands to it OR manually inside the ephemeral worktree path printed by the script (capture the path before cleanup by adding `--debug` mode — but the unfiltered phase-1.5-gate is sufficient for closure; this step is OPTIONAL informational).

    6. Confirm git artifacts are correct:
       ```
       git ls-tree HEAD tests/    # must include `120000 blob ... tests/showcase`
       git ls-tree HEAD spec      # must show mode 160000 (gitlink)
       git ls-tree HEAD .gitmodules
       git ls-tree HEAD README.md
       git log --oneline -5       # last commits are Tasks 2, 3, 4 from this plan
       ```

    Report back:
      - Did `zig build phase-1.5-gate-fresh` exit 0 with all tests passing? (yes/no, attach the build summary line)
      - Did all 8 (or 9) targeted filter runs in step 4 exit 0? (yes/no per filter)
      - Were there any unexpected build warnings or test-output anomalies? (free-form)

    If any check fails:
      - Capture the full stderr + the ephemeral worktree path (the script prints it on the "PASSED"/"FAILED" line).
      - DO NOT amend `01.5-VERIFICATION.md` to GREEN. Instead, append a `## Re-verification failure (2026-05-21)` section noting what failed and create a follow-up gap-closure plan (or re-open this one if the fix is small).
  </how-to-verify>
  <resume-signal>
    Type `approved` (with the build summary line pasted) to declare Phase 1.5 GREEN under the new fresh-worktree gate, OR `failed: <description>` to surface a residual issue for a follow-up plan. The executor MUST NOT proceed to a SUMMARY.md write until this checkpoint is approved.
  </resume-signal>
</task>

</tasks>

<post_conditions>
After Task 5 is approved, the following MUST be true (the executor verifies each before writing SUMMARY.md):

  - `zig build phase-1.5-gate` exits 0 in a FRESHLY CREATED git worktree of the deal repo, after `git submodule update --init --recursive` runs inside that worktree. (Demonstrated by the new `phase-1.5-gate-fresh` step from Task 3.)
  - All 33 unit tests pass — the 24 that were already passing in the worktree the debug agent used, PLUS the 9 that were previously gated on tests/showcase availability.
  - `tests/showcase` is no longer in `git status` Untracked output (it is now `git ls-tree HEAD tests/`-visible as a symlink).
  - The 9 previously-failing tests each exit 0 individually via `-Dtest-filter=<name>` from a fresh-worktree checkout:
      1. `-Dtest-filter=lexer.snapshot` (matches BOTH `lexer.snapshot` and `lexer.snapshot spans monotonic`)
      2. `-Dtest-filter=parser_deal.snapshot`
      3. `-Dtest-filter=parser_deal.coverage`
      4. `-Dtest-filter=parser_dealx.snapshot`
      5. `-Dtest-filter=parser_dealx.coverage`
      6. `-Dtest-filter=property.span_containment`
      7. `-Dtest-filter=determinism.parse_twice`
      8. `-Dtest-filter=c_abi.no_leaks`
      (Filter substring `lexer.spans_monotonic` will match the second lexer_snapshot test by source-side test name; confirm in the ephemeral worktree.)
  - `spec/` submodule is pinned to a specific commit (auditable via `git submodule status`).
  - `tests/showcase` symlink resolves to a real readable directory via the submodule mount (`test -f tests/showcase/packages/vehicle/battery.deal` succeeds).
  - `README.md` is tracked at HEAD and contains the `git submodule update --init --recursive` instruction.
  - `.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md` exists with `status: accepted`.
  - `01.5-VERIFICATION.md` has the False-GREEN Amendment section AND amended frontmatter.
  - `01.5-CONTEXT.md` has the appended `<invariants_added_post_completion>` block.

If ANY post-condition fails, the executor MUST surface it to the user before declaring Phase 1.5 GREEN.
</post_conditions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deal repo → spec submodule | spec/ is an external repo (different remote, separate commit history). Pinned by SHA in deal's gitlink; spec's tip can move without affecting deal until a `git submodule update --remote` is explicit. |
| host filesystem → ephemeral worktree | `mktemp -d` creates a directory in `$TMPDIR`. The script's trap cleans it up, but a hard-kill between worktree-add and worktree-remove would leave a directory + a stale `git worktree list` entry. |
| developer's main repo state → verification result | The whole motivation for this plan: a developer's local working-tree state must NOT influence the verification outcome. The ephemeral worktree is the trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Tampering | spec/ submodule remote URL | mitigate | URL recorded in `.gitmodules` is the canonical github URL (Task 1a). Any future drift requires an explicit `git submodule sync` + commit; signed commits at deal-repo-level are a separate Phase-2 concern. |
| T-15-05-02 | Spoofing  | spec/ submodule content | mitigate | Pinned by SHA in the gitlink (mode 160000). A malicious push to `spec` main does not change what deal/ pulls until someone explicitly bumps the gitlink and commits the bump. |
| T-15-05-03 | Information Disclosure | ephemeral worktree leaks | accept | Worktree contains only committed code + submodule content (no secrets). Trap-cleanup removes it on EXIT. Hard-kill between worktree-add and worktree-remove leaves a directory under `$TMPDIR` — visible only to the local user; acceptable. |
| T-15-05-04 | Denial of Service | repeated ephemeral worktrees fill disk | mitigate | Script uses `trap ... EXIT` to remove the worktree on both success and failure paths. Hard-kill leaks are bounded (one per kill). Documenting in the script's header: a periodic `git worktree prune` cleans stale registrations. |
| T-15-05-05 | Tampering | symlink target rewrite | mitigate | `tests/showcase` is a committed symlink (`120000` mode). A change to its target requires a deliberate `git add tests/showcase` + commit; not a working-tree-only modification. |
| T-15-05-06 | Repudiation | false-GREEN recurrence | mitigate | The new `phase-1.5-gate-fresh` step is the structural guard rail. ADR-phase-1.5-fresh-worktree-verification locks the invariant for future phases. Mechanical (not norm-only) enforcement: any future phase that does NOT add a `-fresh` sibling step is flagged in a future audit. |
| T-15-05-07 | Elevation of Privilege | shell script invocation in build.zig | accept | `b.addSystemCommand(.{"bash", "scripts/verify-fresh-worktree.sh"})` runs a tracked script in the developer's environment. The script does not require elevated privileges. Standard threat model for any build system that shells out. |
| T-15-05-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. The submodule add does not invoke a package manager. |

All threats with disposition `mitigate` have a specific implementation cited above. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages are involved.
</threat_model>

<verification>
## Phase-Level Verification After Plan 05

The original Phase 1.5 success criteria (ROADMAP.md §"Phase 1.5: Parser Strictness & Test-Gap Resolution"):
  1. No vacuous `>= 0` assertions — UNCHANGED by this plan; already verified in 01.5-VERIFICATION.md.
  2. 20 hand-curated m_pins enforced — UNCHANGED.
  3. determinism.parse_twice via PUBLIC C ABI — UNCHANGED (test source not modified; this plan only fixes its INPUT availability).
  4. m08/m18/m19 strictness gaps closed — UNCHANGED.
  5. property.span_containment over 19 showcase + 8 synthetic — UNCHANGED (same: source not modified, input availability fixed).
  6. Phase 1.5 exit gate green — THIS plan makes it ACTUALLY green on a fresh worktree, not just in the developer's main checkout. The authoritative gate is now `zig build phase-1.5-gate-fresh`.
  7. (Added by Plan 05) Fresh-worktree gate `zig build phase-1.5-gate-fresh` exits 0 — the NEW success criterion #7 added to ROADMAP.md Phase 1.5 entry by this plan's update.

Plan-05-specific verification (the post_conditions block above) is the binding gate for closing the gap.
</verification>

<success_criteria>
Plan 01.5-05 is COMPLETE when:
  1. Tasks 1b, 2, 3, 4 all show green automated verifications and committed git history (3 commits total: the atomic submodule+symlink commit from Task 2, the script+build.zig commit from Task 3, and the docs+ADR+amendments commit from Task 4).
  2. Task 5 checkpoint approved with build summary showing all 33 unit tests passing in an ephemeral worktree.
  3. All post_conditions hold.
  4. SUMMARY.md written at `.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-05-SUMMARY.md` per the standard summary template, with the build summary line + ephemeral worktree path captured as evidence.
  5. ROADMAP.md Phase 1.5 entry is flipped back to `[x] Complete` ONLY AFTER Task 5 approval — the planner has already updated ROADMAP.md to mark Phase 1.5 as `[ ]` in-progress (gap closure underway). When Task 5 passes, the executor flips it back to `[x] Complete` AND notes the false-GREEN remediation date alongside the original completion date in the Phase 1.5 list-line, AND updates the Progress table row to `5/5 Complete`. If Task 5 fails, leave ROADMAP unchanged from the in-progress state.
</success_criteria>

<output>
Create `.planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-05-SUMMARY.md` upon Task 5 approval, including:
  - Commit hashes for Tasks 2, 3, 4
  - The ephemeral worktree path captured during Task 5's gate run
  - The final `zig build phase-1.5-gate-fresh` exit code + build summary line verbatim
  - A pointer to ADR-phase-1.5-fresh-worktree-verification.md as the binding artifact for future phases
  - Confirmation that ROADMAP.md Phase 1.5 entry has been flipped back to `[x] Complete` with the false-GREEN remediation date alongside the original completion date, and the Progress table updated to `5/5 Complete`.
</output>
