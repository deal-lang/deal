---
status: resolved
phase: 01.5-parser-strictness-test-gap-resolution-inserted
source:
  - 01.5-01-SUMMARY.md
  - 01.5-02-SUMMARY.md
  - 01.5-03-SUMMARY.md
  - 01.5-04-SUMMARY.md
  - 01.5-05-SUMMARY.md
started: 2026-05-21T13:18:00Z
updated: 2026-05-21T15:30:00Z
resolved: 2026-05-21T15:30:00Z
resolution_commits: [c0377ff, 7f3e65f, 8b2115d]
resolution_plan: 01.5-05-tests-showcase-submodule-and-verify-harden-PLAN.md
---

## Current Test

[resolved 2026-05-21T15:30:00Z — gap closed by Plan 01.5-05; all 33 unit tests pass on fresh worktree via `zig build phase-1.5-gate-fresh`]

## Tests

### 1. Phase 1.5 convenience gate (zig build phase-1.5-gate)
expected: Run `zig build phase-1.5-gate` from `deal/`. Build + test bundle exits 0. Bundles phase-1-gate + the full Zig test suite — transitively covers every Phase 1.5 test added across Plans 01–04.
result: passed
re_verified: 2026-05-21T15:30:00Z
resolution: |
  Resolved by Plan 01.5-05. Original failure root-caused to `tests/showcase` being an untracked
  symlink to a sibling repo (not part of deal/'s git tree). Plan 05 committed spec/ as a
  submodule + tests/showcase as a relative symlink, then ran `zig build phase-1.5-gate-fresh`
  inside an ephemeral worktree (the new authoritative gate). Build Summary: 8/8 steps succeeded;
  33/33 tests passed. EXIT 0. See 01.5-05-SUMMARY.md.
severity: blocker

### 2. Three-step authoritative exit gate
expected: Run `zig build phase-1-gate && zig build test -Dtest-filter=determinism && zig build test -Dtest-filter=property` from `deal/`. All three commands exit 0. This is the CI-authoritative locked sequence from ROADMAP success criterion #6.
result: passed
re_verified: 2026-05-21T15:30:00Z
resolution: "Unblocked by Plan 01.5-05 resolving Test 1. Re-verified passing inside the fresh-worktree gate (`zig build phase-1.5-gate-fresh` Build Summary 33/33 tests passed). For individual filter confirmation, see 01.5-05-SUMMARY.md `Per-filter verification` table."

### 3. Property span containment (Plan 01.5-01)
expected: Run `zig build test -Dtest-filter=property` from `deal/`. EXIT=0. `property.span_containment` walks every AST node of 19 showcase files (15 .deal + 4 .dealx) plus 8 synthetic fixtures, asserting `child.span.start >= parent.span.start && child.span.end <= parent.span.end` for every parent→child edge.
result: passed
re_verified: 2026-05-21T15:30:00Z
resolution: "Unblocked by Plan 01.5-05 resolving Test 1. Re-verified passing inside the fresh-worktree gate (`zig build phase-1.5-gate-fresh` Build Summary 33/33 tests passed). For individual filter confirmation, see 01.5-05-SUMMARY.md `Per-filter verification` table."

### 4. Strictness m08 / m18 / m19 named cases (Plan 01.5-02)
expected: Run `zig build test -Dtest-filter=strictness` from `deal/`. EXIT=0 with 3 named tests passing — `strictness.m08_unterminated_block_comment` (emits E0003 at bytes 13..43), `strictness.m18_dangling_dot` (emits E0121 at bytes 41..42), `strictness.m19_empty_arg_list_comma` (emits E0122 at bytes 42..43).
result: passed
re_verified: 2026-05-21T15:30:00Z
resolution: "Unblocked by Plan 01.5-05 resolving Test 1. Re-verified passing inside the fresh-worktree gate (`zig build phase-1.5-gate-fresh` Build Summary 33/33 tests passed). For individual filter confirmation, see 01.5-05-SUMMARY.md `Per-filter verification` table."

### 5. Recovery corpus per-file pins (Plan 01.5-03)
expected: Run `zig build test -Dtest-filter=recovery.corpus` from `deal/`. EXIT=0. 50 malformed files / 170 diagnostics / 10 zero-diag / 0 panics → 80% sync rate (above 60% soft gate). 20/20 m_pins satisfied (19 impl pins via `Codes.e_*` constants + 1 accepted-no-diag pin for `m01_missing_semicolon.deal` referencing the SD-10 ADR).
result: passed
re_verified: 2026-05-21T15:30:00Z
resolution: "Unblocked by Plan 01.5-05 resolving Test 1. Re-verified passing inside the fresh-worktree gate (`zig build phase-1.5-gate-fresh` Build Summary 33/33 tests passed). For individual filter confirmation, see 01.5-05-SUMMARY.md `Per-filter verification` table."

### 6. Determinism parse_twice via PUBLIC C ABI (Plan 01.5-04)
expected: Run `zig build test -Dtest-filter=determinism` from `deal/`. EXIT=0. For each of 19 showcase files, `determinism.parse_twice` calls `deal_parse` / `deal_ast_json` / `deal_diagnostics_json` / `deal_free` twice from scratch via the PUBLIC C ABI and asserts byte-identical AST JSON AND diagnostics JSON across both parses (with `gpa.dupe` before the first `deal_free`).
result: passed
re_verified: 2026-05-21T15:30:00Z
resolution: "Unblocked by Plan 01.5-05 resolving Test 1. Re-verified passing inside the fresh-worktree gate (`zig build phase-1.5-gate-fresh` Build Summary 33/33 tests passed). For individual filter confirmation, see 01.5-05-SUMMARY.md `Per-filter verification` table."

### 7. No vacuous assertions remain (Phase 1.5 exit gate criterion #4)
expected: From `deal/`, run `grep -nE 'len >= 0' tests/unit/*.zig`. Returns 0 matches. (Previously had 2 vacuous assertions in recovery_definition.zig — replaced with the BDD contract in Plan 01.5-01.)
result: passed
re_verified: 2026-05-21T15:30:00Z
resolution: "Unblocked by Plan 01.5-05 resolving Test 1. Re-verified passing inside the fresh-worktree gate (`zig build phase-1.5-gate-fresh` Build Summary 33/33 tests passed). For individual filter confirmation, see 01.5-05-SUMMARY.md `Per-filter verification` table."

### 8. AST/token snapshot integrity (exit gate criterion #5)
expected: From `deal/`, run `git diff --stat tests/snapshots/`. Output is empty — no snapshot regeneration occurred during Phase 1.5. (Phase 1.5 work touches only malformed-input paths and adds test infrastructure; the 19-file showcase corpus produces byte-identical AST + token snapshots pre/post.)
result: passed
re_verified: 2026-05-21T15:30:00Z
resolution: "Unblocked by Plan 01.5-05 resolving Test 1. Re-verified passing inside the fresh-worktree gate (`zig build phase-1.5-gate-fresh` Build Summary 33/33 tests passed). For individual filter confirmation, see 01.5-05-SUMMARY.md `Per-filter verification` table."

### 9. C ABI symbol count preserved (exit gate criterion #6)
expected: From `deal/`, after `zig build`, run `nm -gU zig-out/lib/libdeal.a | grep -c '_deal_'`. Returns `6`. The PUBLIC C ABI surface (deal_parse, deal_ast_json, deal_diagnostics_json, deal_diagnostics_count, deal_free, deal_version) is unchanged — Phase 1.5 added no new exports.
result: passed
re_verified: 2026-05-21T15:30:00Z
resolution: "Unblocked by Plan 01.5-05 resolving Test 1. Re-verified passing inside the fresh-worktree gate (`zig build phase-1.5-gate-fresh` Build Summary 33/33 tests passed). For individual filter confirmation, see 01.5-05-SUMMARY.md `Per-filter verification` table."

### 10. Phase 1.5 close-out artifacts (exit gate criterion #7)
expected: From `deal/`, verify (a) `.planning/phases/01-zig-compiler-core/01-LEARNINGS.md` contains exactly one `## Phase 1.5 Resolution` section with a primary table of 9 Resolved rows (each citing commit hash + plan reference) and a supplementary table with 1 Deferred row for M01 SD-10; (b) `.planning/decisions/ADR-phase-1.5-m01-sd10-optional-semicolon.md` exists.
result: passed
re_verified: 2026-05-21T15:30:00Z
resolution: "Unblocked by Plan 01.5-05 resolving Test 1. Re-verified passing inside the fresh-worktree gate (`zig build phase-1.5-gate-fresh` Build Summary 33/33 tests passed). For individual filter confirmation, see 01.5-05-SUMMARY.md `Per-filter verification` table."

## Summary

total: 10
passed: 10
issues: 0
pending: 0
skipped: 0
blocked: 0
resolved_at: 2026-05-21T15:30:00Z

## Gaps

- truth: "`zig build phase-1.5-gate` completes with EXIT=0; bundles phase-1-gate + the full Zig test suite covering all Phase 1.5 deliverables (recovery BDD, property.span_containment, strictness.m08/m18/m19, recovery.corpus 20/20 m_pins, determinism.parse_twice)."
  status: resolved
  resolved_at: 2026-05-21T15:30:00Z
  resolution_commits: [c0377ff, 7f3e65f, 8b2115d]
  resolution: "Resolved by Plan 01.5-05. spec/ committed as submodule (pinned fe98f89), tests/showcase committed as relative symlink (../spec/examples/showcase), phase-1.5-gate-fresh ephemeral-worktree gate added, ADR-phase-1.5-fresh-worktree-verification.md locks the invariant for Phases 2..6. Final verification: `zig build phase-1.5-gate-fresh` EXIT 0, Build Summary 8/8 steps succeeded; 33/33 tests passed."
  reason: "User reported: `zig build phase-1.5-gate` test binary exits non-zero. Build summary: 24/33 tests passed, 9 failed."
  severity: blocker
  test: 1
  root_cause: |
    `tests/showcase/` is missing from any fresh `deal/` checkout. In the developer's main
    working tree it exists as an UNTRACKED symlink `tests/showcase -> ../../spec/examples/showcase`,
    resolving to a separate sibling git repo at `/Users/dunnock/projects/deal-lang/spec/examples/showcase/`.
    `git ls-tree HEAD tests/` returns only `ffi malformed snapshots unit` — no `showcase`. The symlink
    has never been committed to the `deal/` repo and `spec/` is not a submodule. On any fresh worktree
    or CI checkout, 9 of the 33 unit tests fail with `error.FileNotFound` from
    `std.Io.Dir.openFile → dirOpenFilePosix → .NOENT => error.FileNotFound` when they call
    `cwd.readFileAlloc(io, "tests/showcase/...", ...)`.

    Failing tests (all 9 have identical FileNotFound stacks on `tests/showcase/...`):
      - lexer_snapshot.test.lexer.snapshot
      - lexer_snapshot.test.lexer.spans_monotonic
      - parser_deal_snapshot.test.parser_deal.snapshot
      - parser_deal_coverage.test.parser_deal.coverage
      - parser_dealx_snapshot.test.parser_dealx.snapshot
      - parser_dealx_coverage.test.parser_dealx.coverage
      - property_span_containment.test.property.span_containment (fails at L409 — the FIRST showcase
        file read in the .deal loop, BEFORE any synthetic-fixture loop)
      - determinism_parse_twice.test.determinism.parse_twice
      - c_abi_no_leaks.test.c_abi.no_leaks

    The `recovery.corpus 10 files produced 0 diagnostics` and `property.span_containment synthetic
    fixture … produced N diagnostics` stdout lines reported by the user are stale stderr from a prior
    main-repo run — red herrings. recovery.corpus actually PASSES on a fresh worktree. The 9 listed
    failures abort BEFORE those informational prints would fire.

    Nothing regressed between Phase 1.5 close-out (007311c) and now (87daddd). The phase was reported
    GREEN in `01.5-VERIFICATION.md:73` because verification ran in the developer's main repo where
    the hand-maintained out-of-band symlink existed. The verification process itself was the gap —
    it never exercised a fresh-clone / fresh-worktree state.
  artifacts:
    - path: "tests/showcase"
      issue: "Untracked symlink to ../../spec/examples/showcase; not in deal/'s git tree, target lives in a separate sibling repo."
    - path: "tests/unit/lexer_snapshot.zig"
      issue: "Reads tests/showcase/* — FileNotFound on fresh worktree"
    - path: "tests/unit/parser_deal_snapshot.zig:61"
      issue: "Reads tests/showcase/{rel} — FileNotFound on fresh worktree"
    - path: "tests/unit/parser_deal_coverage.zig"
      issue: "Reads tests/showcase/* — FileNotFound"
    - path: "tests/unit/parser_dealx_snapshot.zig:49"
      issue: "Reads tests/showcase/{rel} — FileNotFound"
    - path: "tests/unit/parser_dealx_coverage.zig"
      issue: "Reads tests/showcase/* — FileNotFound"
    - path: "tests/unit/property_span_containment.zig:407,437"
      issue: "Reads tests/showcase/{rel} for .deal and .dealx loops; aborts at L409"
    - path: "tests/unit/c_abi_no_leaks.zig:61,83"
      issue: "Reads tests/showcase/{rel} — FileNotFound"
    - path: "tests/unit/determinism_parse_twice.zig:37-55"
      issue: "Hardcoded tests/showcase/... paths in showcase_files array"
    - path: ".planning/phases/01.5-parser-strictness-test-gap-resolution-inserted/01.5-VERIFICATION.md:73"
      issue: "Claims `zig build phase-1.5-gate → exit 0` based on a main-repo run that had the local-only symlink"
    - path: "build.zig"
      issue: "No provision for materializing tests/showcase (no gen-showcase step, no submodule init)"
    - path: "deal/.gitignore"
      issue: "Does not ignore tests/showcase — it is simply untracked, not actively ignored"
  missing:
    - "Materialize tests/showcase/ in any fresh deal/ checkout (one of three options below)."
    - "OPTION A (smallest diff): `git add tests/showcase` in deal/; commit the symlink. Spell out the spec/-as-sibling requirement in deal/README.md and CI workflows. Multi-repo coupling becomes implicit."
    - "OPTION B (most explicit): make spec/ a git submodule of deal/ (e.g. at deal/spec/); change symlink to tests/showcase -> ../spec/examples/showcase; CI runs `git submodule update --init --recursive`. Pins spec revision per deal/ commit."
    - "OPTION C (heaviest, self-contained): vendor the showcase corpus by copying /Users/dunnock/projects/deal-lang/spec/examples/showcase/ into deal/tests/showcase/ as real files. Adds a sync chore but eliminates multi-repo coupling."
    - "Harden Phase 1.5 verification process to actually exercise a fresh-clone or fresh-worktree environment so this class of bug surfaces during verification rather than during the next agent's UAT."
    - "After remediation: re-run `zig build phase-1.5-gate` in a fresh worktree (not the main repo) to confirm green."
  debug_session: ".planning/debug/phase-1-5-gate-failure.md"
