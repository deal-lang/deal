---
phase: 03-editor-intelligence
plan: 03
plan_id: 03-03
name: deal-lsp Scaffold, Diagnostics, and Formatting
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/dunnock/projects/deal-lang/deal/Cargo.toml
  - /Users/dunnock/projects/deal-lang/deal/deal-ffi/Cargo.toml
  - /Users/dunnock/projects/deal-lang/deal/deal-ffi/build.rs
  - /Users/dunnock/projects/deal-lang/deal/deal-ffi/src/lib.rs
  - /Users/dunnock/projects/deal-lang/deal/cli/Cargo.toml
  - /Users/dunnock/projects/deal-lang/deal/cli/src/main.rs
  - /Users/dunnock/projects/deal-lang/deal/cli/src/ffi.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/Cargo.toml
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/main.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/backend.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/documents.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/diagnostics.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/formatting.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/debounce.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/tests/showcase.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/tests/golden/formatted/BatteryPack.deal.expected
  - /Users/dunnock/projects/deal-lang/deal/lsp/README.md
autonomous: true
requirements:
  - REQ-phase-3-3-lsp-server
must_haves:
  truths:
    - "`deal-lsp` binary builds and links libdeal.a via the shared `deal-ffi` crate (D-49 architectural split: Rust workspace tooling, Zig core single-file-scoped)."
    - "On textDocument/didOpen, deal-lsp calls deal_parse and stores the returned DealHandle* in a per-document table keyed by URI (D-42)."
    - "On textDocument/didChange, the prior handle is freed and a fresh handle is created with the new buffer, after a 300 ms debounce window (D-43, D-45 per-handle Mutex)."
    - "Diagnostics are pushed to the client via textDocument/publishDiagnostics after each (re-)parse, carrying E2xxx codes from the Phase 2 envelope (D-32, D-33, RESEARCH §11)."
    - "textDocument/formatting reuses the live in-memory handle, calls deal_format, and returns a single TextEdit replacing the document range — no save round-trip needed (D-21 in-memory semantics, CONTEXT.md Claude's Discretion note)."
    - "did_close is a no-op per D-44 + RESEARCH Open Q2 — handles persist for workspace lifetime."
    - "FFI ownership invariant (Pitfall 3 / T-02-29) is encapsulated inside deal-ffi's safe wrappers: every clone-before-free is owned by deal-ffi, not duplicated at LSP call sites."
  artifacts:
    - path: deal/Cargo.toml
      provides: workspace root declaring 4 members (cli, lsp, deal-ffi, tests/ffi) with tower-lsp/tokio/tower/dashmap/ropey workspace deps
      contains: 'members = ["cli", "lsp", "deal-ffi", "tests/ffi"]'
    - path: deal/deal-ffi/Cargo.toml
      provides: shared FFI crate with `links = "deal"` claim (moved from cli/)
      contains: 'links = "deal"'
    - path: deal/deal-ffi/src/lib.rs
      provides: 9 extern "C" declarations + safe Rust wrappers + OwnedDealHandle newtype with Send + Drop impls (D-45)
      contains: 'pub fn deal_parse'
    - path: deal/lsp/src/main.rs
      provides: tokio::main entrypoint that boots LspService::new + Server::new on stdin/stdout
      contains: 'tokio::main'
    - path: deal/lsp/src/backend.rs
      provides: Backend struct implementing LanguageServer trait with initialize/initialized/shutdown/did_open/did_change/did_close/formatting
      contains: 'impl LanguageServer for Backend'
    - path: deal/lsp/src/documents.rs
      provides: DashMap<Url, Arc<Mutex<OwnedDealHandle>>> per-document table (D-42, D-44, D-45)
      contains: 'DashMap'
    - path: deal/lsp/src/debounce.rs
      provides: 300 ms re-parse debounce scheduler keyed by URI (D-43)
    - path: deal/lsp/tests/showcase.rs
      provides: tokio::test harness spawning deal-lsp via in-process channels; covers diagnostics_match_phase_2_codes, format_round_trip, debounce_collapses_rapid_changes
    - path: deal/lsp/tests/golden/formatted/BatteryPack.deal.expected
      provides: committed byte-exact golden output for the format_round_trip regression test; generated once by the deal CLI fmt subcommand (per Issue 6 resolution; replaces in-test tautology of computing expected output via deal_ffi::safe::format inside the test)
  key_links:
    - from: deal/lsp/src/backend.rs
      to: deal/deal-ffi/src/lib.rs
      via: deal_ffi::{parse, format, diagnostics_json} safe wrappers
      pattern: 'use deal_ffi'
    - from: deal/lsp/src/diagnostics.rs
      to: deal/deal-ffi/src/lib.rs
      via: deal_diagnostics_json → serde_json::from_slice → tower_lsp::lsp_types::Diagnostic
      pattern: 'serde_json::from_slice'
    - from: deal/Cargo.toml
      to: deal/deal-ffi/Cargo.toml
      via: workspace members entry + single-claim links="deal" enforcement
      pattern: 'deal-ffi'
    - from: deal/lsp/tests/showcase.rs
      to: deal/lsp/tests/golden/formatted/BatteryPack.deal.expected
      via: include_str! at test compile time; byte-for-byte equality assertion
      pattern: 'include_str!'
---

<objective>
Stand up the `deal-lsp` Rust crate inside the existing `deal/` Cargo workspace, extract the FFI surface into a shared `deal-ffi` crate, and ship the first three LSP capabilities backed by the Phase 2 C ABI: per-document handle lifecycle (`did_open`/`did_change`/`did_close`), push-mode diagnostics (`publishDiagnostics`), and `textDocument/formatting`. After this plan ships, `cargo test -p deal-lsp` exits 0 with three integration tests passing: `diagnostics_match_phase_2_codes`, `format_round_trip`, `debounce_collapses_rapid_changes`.

Purpose: Deliver the diagnostic + formatting subset of REQ-phase-3-3-lsp-server. Lock the per-document architecture (D-42..D-45) and the shared FFI crate layout (RESEARCH §10). Plan 04 layers completion / hover / definition / semantic tokens on top of this scaffold.

Output: Three new crates in the `deal/` workspace (`lsp`, `deal-ffi`), CLI migrated to consume `deal-ffi` (no duplicated `extern "C"` blocks), and a working `deal-lsp` binary that round-trips diagnostics and formatting against the showcase.

Wave assignment: This plan is `wave: 1` with empty `depends_on` — it is parallel-safe with Plans 01 and 02. Their `files_modified` sets are disjoint (Plan 01 = TextMate grammars + vscode-deal manifest scaffolding, Plan 02 = tree-sitter-deal grammar, Plan 03 = deal/Cargo.toml workspace + Rust crates). All three can execute concurrently in Wave 1; downstream plans (04+) consume Plan 03's Rust outputs and are scheduled accordingly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dunnock/projects/deal-lang/deal/.planning/PROJECT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/ROADMAP.md
@/Users/dunnock/projects/deal-lang/deal/.planning/STATE.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/02-prove-the-pipeline/02-CONTEXT.md
@/Users/dunnock/projects/deal-lang/deal/Cargo.toml
@/Users/dunnock/projects/deal-lang/deal/cli/Cargo.toml
@/Users/dunnock/projects/deal-lang/deal/cli/build.rs
@/Users/dunnock/projects/deal-lang/deal/cli/src/main.rs
@/Users/dunnock/projects/deal-lang/deal/cli/src/ffi.rs
@/Users/dunnock/projects/deal-lang/deal/include/deal.h
@/Users/dunnock/projects/deal-lang/deal/tests/ffi/tests/parse.rs

<interfaces>
<!-- FFI C ABI surface (9 exports, locked at Phase 2 closeout — do NOT add new exports).
     deal-ffi MUST mirror these exactly per /Users/dunnock/projects/deal-lang/deal/include/deal.h. -->

extern "C" pub fn deal_parse(source_ptr: *const u8, source_len: usize, filename_ptr: *const u8, filename_len: usize) -> *mut DealHandle;
extern "C" pub fn deal_free(handle: *mut DealHandle);
extern "C" pub fn deal_has_errors(handle: *mut DealHandle) -> bool;
extern "C" pub fn deal_diagnostics_count(handle: *mut DealHandle) -> usize;
extern "C" pub fn deal_diagnostics_json(handle: *mut DealHandle, out_ptr: *mut *const u8, out_len: *mut usize) -> bool;
extern "C" pub fn deal_ast_json(handle: *mut DealHandle, out_ptr: *mut *const u8, out_len: *mut usize) -> bool;
extern "C" pub fn deal_ir_json(handle: *mut DealHandle, out_ptr: *mut *const u8, out_len: *mut usize) -> bool;
extern "C" pub fn deal_format(handle: *mut DealHandle, out_ptr: *mut *const u8, out_len: *mut usize) -> bool;
extern "C" pub fn deal_index_json(handle: *mut DealHandle, out_ptr: *mut *const u8, out_len: *mut usize) -> bool;

<!-- D-45 concurrency wrapper (new in Phase 3): -->
pub struct OwnedDealHandle(*mut DealHandle);
unsafe impl Send for OwnedDealHandle {}
impl Drop for OwnedDealHandle { fn drop(&mut self) { unsafe { deal_free(self.0); } } }

<!-- D-32 Phase 2 diagnostic envelope (consume verbatim — see 02-CONTEXT.md): -->
{
  "v": 1,
  "diagnostics": [
    { "code": "E2001", "severity": "error", "message": "...", "file": "...", "range": { "start": {"line": L, "character": C}, "end": {"line": L, "character": C} } }
  ]
}

<!-- tower-lsp 0.20.0 LanguageServer trait surface (from RESEARCH §1 — pin set per Issue 1 resolution): -->
#[async_trait::async_trait]
impl LanguageServer for Backend {
    async fn initialize(&self, params: InitializeParams) -> Result<InitializeResult>;
    async fn initialized(&self, params: InitializedParams);
    async fn shutdown(&self) -> Result<()>;
    async fn did_open(&self, params: DidOpenTextDocumentParams);
    async fn did_change(&self, params: DidChangeTextDocumentParams);
    async fn did_close(&self, params: DidCloseTextDocumentParams);
    async fn formatting(&self, params: DocumentFormattingParams) -> Result<Option<Vec<TextEdit>>>;
}

<!-- Phase 2 CLI FFI call-site pattern (PATTERNS.md lines 339–378 — clone-before-free invariant): -->
1. deal_parse → handle
2. deal_has_errors / deal_diagnostics_json into out_ptr + out_len
3. CLONE bytes into Vec<u8> via std::slice::from_raw_parts(out_ptr, out_len).to_vec()
4. deal_free(handle) — bytes already cloned
This pattern MUST be encapsulated inside deal-ffi/src/lib.rs safe wrappers — LSP call sites should never see raw pointers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract deal-ffi crate; update workspace; migrate cli/ to depend on deal-ffi</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/Cargo.toml,
    /Users/dunnock/projects/deal-lang/deal/deal-ffi/Cargo.toml,
    /Users/dunnock/projects/deal-lang/deal/deal-ffi/build.rs,
    /Users/dunnock/projects/deal-lang/deal/deal-ffi/src/lib.rs,
    /Users/dunnock/projects/deal-lang/deal/cli/Cargo.toml,
    /Users/dunnock/projects/deal-lang/deal/cli/src/main.rs,
    /Users/dunnock/projects/deal-lang/deal/cli/src/ffi.rs
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/deal/Cargo.toml,
    /Users/dunnock/projects/deal-lang/deal/cli/Cargo.toml,
    /Users/dunnock/projects/deal-lang/deal/cli/build.rs,
    /Users/dunnock/projects/deal-lang/deal/cli/src/ffi.rs,
    /Users/dunnock/projects/deal-lang/deal/cli/src/main.rs,
    /Users/dunnock/projects/deal-lang/deal/include/deal.h,
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md (sections "Plan 03 / deal-ffi crate", "Plan 03 / deal/Cargo.toml"),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (§10 Cargo workspace layout)
  </read_first>
  <behavior>
    - deal/Cargo.toml workspace members becomes exactly ["cli", "lsp", "deal-ffi", "tests/ffi"].
    - deal/Cargo.toml [workspace.dependencies] gains tower-lsp = { version = "0.20.0", features = ["runtime-tokio"] }, tokio = { version = "1", features = ["full"] }, tower = "0.5", dashmap = "6", ropey = "1.6", async-trait = "0.1", tracing = "0.1", tracing-subscriber = { version = "0.3", features = ["env-filter"] }.
    - Existing workspace.dependencies entries (clap, serde, serde_json, jsonschema, anstream, owo-colors, anyhow, thiserror, insta, uuid) preserved unchanged.
    - tower-lsp version is concretely pinned to "0.20.0" (the current actively-maintained line per RESEARCH §1 training baseline; Open Question Q1 RESOLVED per Issue 2). The `cargo search tower-lsp` step remains as a Wave 1 sanity check (Task 1 first action) — if the registry shows 0.20.0 was yanked or unpublished, the executor escalates to a plan-revision; absent that signal the pin is committed as written.
    - deal-ffi/Cargo.toml: name "deal-ffi", version "0.3.0", edition "2021", publish = false; declares `links = "deal"` (single workspace claimant per Cargo rule); [lib] name = "deal_ffi" crate-type = ["rlib"]; no [dependencies] except where the safe-wrapper layer needs them (likely just std).
    - deal-ffi/build.rs is a verbatim copy of cli/build.rs lines 20–52, single `.join("..")` adjusted to reflect deal-ffi/ being at the same depth as cli/ (one level below deal/) — confirm via `manifest_dir.join("..").canonicalize()`.
    - deal-ffi/src/lib.rs contains the 9 extern "C" declarations matching deal.h verbatim, all type names per PATTERNS.md lines 186–215 (DealHandle opaque struct with PhantomData<*mut u8, PhantomPinned>) PLUS the new D-45 `OwnedDealHandle` newtype with Send + Drop impls PLUS safe Rust wrapper functions encapsulating the clone-before-free pattern.
    - Safe wrappers in deal-ffi exposed for downstream consumers:
      - `pub fn parse(source: &[u8], filename: &str) -> Option<OwnedDealHandle>` — returns None on null pointer (OOM).
      - `pub fn has_errors(h: &OwnedDealHandle) -> bool` — borrows handle by ref.
      - `pub fn diagnostics_json(h: &OwnedDealHandle) -> Option<Vec<u8>>` — clones bytes before returning; None on Zig-side failure.
      - `pub fn ast_json(h: &OwnedDealHandle) -> Option<Vec<u8>>`
      - `pub fn ir_json(h: &OwnedDealHandle) -> Option<Vec<u8>>`
      - `pub fn index_json(h: &OwnedDealHandle) -> Option<Vec<u8>>`
      - `pub fn format(h: &OwnedDealHandle) -> Option<Vec<u8>>`
      - `pub fn diagnostics_count(h: &OwnedDealHandle) -> usize`
    - cli/Cargo.toml drops `links = "deal"` line; adds `deal-ffi = { path = "../deal-ffi" }` to [dependencies]; removes any direct C-side build script references that are now deal-ffi's responsibility — KEEP cli/build.rs only if it still has CLI-specific build steps (e.g., schema embedding) — otherwise simplify or delete.
    - cli/src/ffi.rs is deleted.
    - cli/src/main.rs: replace `mod ffi;` (or `pub mod ffi;`) declaration with `use deal_ffi as ffi;` re-export so all existing `ffi::deal_*` call-sites continue to work without modification. Where the existing code does raw `unsafe { ffi::deal_parse(...) }` then clone-before-free, optionally migrate one or two call sites to use the new safe wrapper helpers — but DO NOT refactor all CLI code in this plan (out of scope; do that in a future cleanup).
    - All existing `cargo test --workspace` tests in deal/cli + deal/tests/ffi continue to pass after migration (no behavior change, only crate restructuring).
  </behavior>
  <action>
    Move the FFI surface into its own crate per RESEARCH §10 (Cargo workspace layout) and PATTERNS.md (Plan 03 / deal-ffi crate section).

    Sequence:
    1. Sanity-check the tower-lsp pin: run `cargo search tower-lsp | head -3`. The latest published 0.20.x line MUST appear; the plan commits "0.20.0" per Issue 1 resolution. If the registry no longer lists 0.20.0 (yanked or unpublished), stop and escalate via a plan revision — do NOT silently bump to 0.21.x.
    2. Create deal-ffi/Cargo.toml mirroring the shape from PATTERNS.md lines 120–155: [package] name="deal-ffi" version="0.3.0" edition="2021" publish=false links="deal"; [lib] name="deal_ffi" crate-type=["rlib"]; [build-dependencies] empty (NO bindgen per RESEARCH §10 A7 risk note — the hand-written 109-line extern block already works). The `links = "deal"` claim MUST move here, AND be removed from cli/Cargo.toml — Cargo enforces single-claim per native lib across the workspace.
    3. Create deal-ffi/build.rs as a verbatim copy of cli/build.rs lines 20–52. The path resolution `manifest_dir.join("..")` is unchanged because deal-ffi lives at the same depth as cli (both one level below deal/).
    4. Create deal-ffi/src/lib.rs by moving cli/src/ffi.rs wholesale (the 109-line module) and adding at the bottom:
       - The OwnedDealHandle newtype (PATTERNS.md lines 224–229).
       - Safe wrapper module `pub mod safe { ... }` containing the 8 helper functions named above. Each wrapper encapsulates Pitfall 3's clone-before-free invariant by:
         (a) calling the extern function with raw pointers,
         (b) cloning the output via `std::slice::from_raw_parts(out_ptr, out_len).to_vec()` BEFORE the OwnedDealHandle Drop fires,
         (c) returning the owned Vec<u8>.
    5. Update deal/Cargo.toml workspace [workspace] members array to ["cli", "lsp", "deal-ffi", "tests/ffi"] and extend [workspace.dependencies] with the 7 new entries listed in the behavior section (tower-lsp pinned to exactly "0.20.0" per Issue 1). Preserve the existing entries verbatim.
    6. Update cli/Cargo.toml: remove `links = "deal"`; add `deal-ffi = { path = "../deal-ffi" }` to [dependencies]; if cli/build.rs still exists, strip the libdeal link logic (now deal-ffi's responsibility) — keep only CLI-specific build steps; delete cli/build.rs entirely if no CLI-specific build steps remain.
    7. cli/src/main.rs: replace the `mod ffi;` or `pub mod ffi;` declaration on the line referenced by PATTERNS.md (around line 17) with `use deal_ffi as ffi;`. All 1500+ existing `ffi::deal_*` call sites continue to work.
    8. Delete cli/src/ffi.rs.

    Verification (in this task): `cd deal && cargo build --workspace --release` succeeds; `cargo test --workspace --lib` succeeds (no behavior regressions); `nm -gU target/release/deal-lsp || true` is acceptable to error since deal-lsp crate doesn't exist yet — Task 2 creates it.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && cargo build --workspace --release && cargo test --workspace --lib && grep -q '^links = "deal"' deal-ffi/Cargo.toml && ! grep -q '^links = "deal"' cli/Cargo.toml && test ! -f cli/src/ffi.rs && grep -q 'use deal_ffi' cli/src/main.rs && grep -q 'tower-lsp = { version = "0.20.0"' Cargo.toml && grep -q 'tokio' Cargo.toml && grep -qE '"deal-ffi"' Cargo.toml && echo "EXTRACTION OK"</automated>
  </verify>
  <done>
    Workspace builds; cli + tests/ffi tests pass; `links = "deal"` claim moved from cli to deal-ffi; cli/src/ffi.rs deleted; cli uses `deal_ffi` re-export; workspace.dependencies gains tower-lsp pinned to "0.20.0", tokio, tower, dashmap, ropey, async-trait, tracing, tracing-subscriber.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold deal-lsp crate with backend.rs implementing initialize/shutdown/did_open/did_change/did_close + diagnostics push + 300 ms debounce</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/lsp/Cargo.toml,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/main.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/backend.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/documents.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/diagnostics.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/debounce.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/README.md
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/deal/deal-ffi/src/lib.rs (from Task 1),
    /Users/dunnock/projects/deal-lang/deal/cli/Cargo.toml,
    /Users/dunnock/projects/deal-lang/deal/cli/src/main.rs (lines 173–244 — parse + diagnostics + free sequence pattern),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md (sections "Plan 03 / deal/lsp/Cargo.toml", "Plan 03 / lsp/src/backend.rs"),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (sections §1 tower-lsp, §2 LSP token types — only the diagnostic-related parts here, §11 push-mode diagnostics),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/02-prove-the-pipeline/02-CONTEXT.md (D-32 diagnostic envelope, D-33 E2xxx codes)
  </read_first>
  <behavior>
    - lsp/Cargo.toml shape per PATTERNS.md lines 244–280: name "deal-lsp", version "0.3.0", edition "2021"; [[bin]] name="deal-lsp" path="src/main.rs"; [dependencies] deal-ffi = { path = "../deal-ffi" }, tower-lsp = { workspace = true }, tokio = { workspace = true }, tower = { workspace = true }, dashmap = { workspace = true }, ropey = { workspace = true }, async-trait = { workspace = true }, serde = { workspace = true }, serde_json = { workspace = true }, anyhow = { workspace = true }, thiserror = { workspace = true }, tracing = { workspace = true }, tracing-subscriber = { workspace = true }; [dev-dependencies] insta = { workspace = true }.
    - lsp/Cargo.toml MUST NOT declare `links = "deal"` (deal-ffi owns it).
    - lsp/src/main.rs has `#[tokio::main] async fn main() { ... }` per RESEARCH §1 lines 161–167: initialize tracing-subscriber from env, build LspService with `LspService::new(|client| Backend::new(client))`, run Server::new(stdin, stdout, socket).serve(service).await.
    - lsp/src/backend.rs defines `pub struct Backend { client: Client, documents: Arc<Documents>, ... }` with `pub fn new(client: Client) -> Self`.
    - Backend implements LanguageServer trait with these methods (Plan 04 adds the rest):
      - initialize: returns InitializeResult with ServerCapabilities declaring text_document_sync = FULL, document_formatting_provider = Some(OneOf::Left(true)), workspace_symbol_provider = None (Plan 04). Stub completion_provider / hover_provider / definition_provider / semantic_tokens_provider as None — Plan 04 fills them.
      - initialized: log "deal-lsp ready" via tracing; eager workspace parse (D-47) is Plan 04's responsibility.
      - shutdown: return Ok(()).
      - did_open: extract URI + source text from params; call documents.open(uri, text) which parses + stores OwnedDealHandle + publishes diagnostics via client.publish_diagnostics().
      - did_change: extract URI + new text; debounce 300 ms (via debounce.rs); on debounce-fire, documents.update(uri, text) which frees old handle + parses new + publishes diagnostics.
      - did_close: no-op per D-44 / RESEARCH Open Q2 (workspace lifetime, not editor lifetime).
      - formatting: lookup handle for URI; call deal_ffi::safe::format(handle); compute full-document range from stored buffer (via ropey rope); return single TextEdit { range, new_text }.
    - lsp/src/documents.rs defines `pub struct Documents { handles: DashMap<Url, Arc<TokioMutex<OwnedDealHandle>>>, buffers: DashMap<Url, Rope> }` per D-42, D-44, D-45.
      - `pub async fn open(&self, uri: Url, text: String, client: &Client) -> Result<()>` — parses, stores handle + Rope, publishes diagnostics.
      - `pub async fn update(&self, uri: Url, text: String, client: &Client) -> Result<()>` — drops prior handle (Drop fires deal_free), parses new, replaces Rope, publishes diagnostics.
      - `pub fn get_handle(&self, uri: &Url) -> Option<Arc<TokioMutex<OwnedDealHandle>>>` — for formatting.rs and Plan 04.
      - `pub fn get_buffer(&self, uri: &Url) -> Option<Rope>` — for formatting.rs full-document range computation.
    - lsp/src/diagnostics.rs: `pub fn parse_diagnostics(json_bytes: &[u8]) -> Vec<tower_lsp::lsp_types::Diagnostic>` — consumes the D-32 envelope, maps E2xxx codes to LSP Diagnostic.code (NumberOrString::String), maps range to LSP Range, severity to DiagnosticSeverity::ERROR/WARNING.
    - lsp/src/debounce.rs: `pub struct Debouncer { schedules: DashMap<Url, JoinHandle<()>> }` with `pub async fn schedule(&self, uri: Url, delay: Duration, action: impl FnOnce() -> Future + Send + 'static)`. Each schedule cancels the prior one for the same URI (D-43: keystrokes within 300 ms collapse into one re-parse).
    - lsp/README.md: written from scratch citing RESEARCH §10 and the new architecture. DO NOT inherit content from the stale `/Users/dunnock/projects/deal-lang/lsp/README.md` (TypeScript design); Plan 07 retires that file. The new README documents: tower-lsp 0.20.0 dependency (concrete pin per Issue 1), 9-export FFI consumption, per-document handle model (D-42), 300 ms debounce (D-43), push-mode diagnostics (RESEARCH §11), in-memory formatting (CONTEXT.md Claude's Discretion).
  </behavior>
  <action>
    Scaffold deal/lsp/ as a sibling crate to deal/cli/ following the manifest shape in PATTERNS.md lines 244–280 and the LanguageServer trait shape in RESEARCH §1 lines 107–153.

    Cargo.toml: copy the manifest pattern from cli/Cargo.toml as the structural template; replace name and bin entries; replace dependencies with the deal-lsp dependency set listed in the behavior section. CRITICAL: do NOT add `links = "deal"` — deal-ffi owns it per Task 1.

    main.rs: implement the tokio::main entrypoint per RESEARCH §1 lines 161–167. Initialize tracing-subscriber via `tracing_subscriber::fmt().with_env_filter(EnvFilter::from_env("DEAL_LSP_LOG")).init()`. Spawn LspService over stdio. The main function is ~15 lines.

    backend.rs: define Backend struct and implement LanguageServer trait. The initialize method returns a capabilities-stub that declares only what THIS plan ships: text_document_sync = FULL, document_formatting_provider = true. Plan 04 will add completion/hover/definition/semantic_tokens. Use the trait shape from RESEARCH §1 lines 107–153 (verbatim adaptation; only fill in the bodies of methods this plan owns; leave the others as `Ok(None)` or `unimplemented!()` placeholders — but DO NOT declare them in capabilities since the client will ignore unsupported requests).

    documents.rs: model the per-document handle table per D-42. Use `dashmap::DashMap<Url, Arc<tokio::sync::Mutex<deal_ffi::OwnedDealHandle>>>` for handles. Use `dashmap::DashMap<Url, ropey::Rope>` for buffers (needed for formatting.rs to compute full-document ranges and for Plan 04 incremental position lookups). The `open` method calls deal_ffi::safe::parse, stores the handle, calls deal_ffi::safe::diagnostics_json, parses via diagnostics::parse_diagnostics, then `client.publish_diagnostics(uri, diagnostics, Some(version)).await`. The `update` method does the same but the old handle's Arc drops out (Mutex guard released, Drop runs deal_free).

    diagnostics.rs: implement parse_diagnostics. Input: byte slice from deal_diagnostics_json. Use `serde_json::from_slice::<DiagnosticEnvelope>(bytes)` where DiagnosticEnvelope mirrors D-32. Map each record to tower_lsp::lsp_types::Diagnostic with: source = Some("deal-lsp"), code = Some(NumberOrString::String(record.code)), severity = match record.severity { "error" => ERROR, "warning" => WARNING, ... }, range = Range { start: Position { line, character }, end: ... }, message = record.message. NumberOrString and the lsp_types path are re-exported by tower_lsp::lsp_types.

    debounce.rs: implement a per-URI debouncer using `tokio::time::sleep` + `tokio::task::JoinHandle`. `schedule(uri, delay, action)` cancels any existing JoinHandle for that URI (via JoinHandle::abort()) then spawns a new task that sleeps for `delay` then runs `action.await`. Use a Mutex-protected DashMap to atomically cancel-and-replace.

    Wire-up in backend.rs::did_change: extract uri + full text (DocumentSyncKind::FULL means the client sends the entire buffer per change), call `self.debouncer.schedule(uri.clone(), Duration::from_millis(300), { let docs = self.documents.clone(); let client = self.client.clone(); async move { docs.update(uri, text, &client).await.ok(); } })`. The 300 ms value is the D-43 lock — write it as a `const DEBOUNCE_MS: u64 = 300` at the top of backend.rs with a `// D-43` comment.

    formatting.rs (created later in Task 3, but its skeleton stub here in Task 2): `pub async fn handle_formatting(documents: &Documents, uri: &Url) -> Result<Option<Vec<TextEdit>>>` — looks up handle + Rope, calls deal_ffi::safe::format, computes the full-document Range from Rope length, returns Ok(Some(vec![TextEdit { range, new_text: String::from_utf8(bytes)? }])).

    README.md: write 40–80 lines documenting purpose, architecture (consume the 9-export FFI), capabilities served (Plan 03 ships diagnostics + formatting; Plan 04 adds completion/hover/definition/semantic-tokens), build instructions (`cargo build -p deal-lsp --release`), test instructions (`cargo test -p deal-lsp`), and a "NOTE: this supersedes the stale `/Users/dunnock/projects/deal-lang/lsp/README.md`" line that Plan 07 will action.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && cargo build -p deal-lsp --release && test -f target/release/deal-lsp && nm -gU target/release/deal-lsp 2>/dev/null | head -5 ; cargo test -p deal-lsp --lib && grep -q 'DEBOUNCE_MS: u64 = 300' lsp/src/backend.rs && grep -q 'impl LanguageServer for Backend' lsp/src/backend.rs && grep -q 'DashMap<Url' lsp/src/documents.rs && grep -q 'OwnedDealHandle' lsp/src/documents.rs && ! grep -q '^links = "deal"' lsp/Cargo.toml && echo "deal-lsp scaffold OK"</automated>
  </verify>
  <done>
    deal-lsp crate compiles and links libdeal.a via deal-ffi; main.rs boots LspService; backend.rs implements LanguageServer with initialize + shutdown + did_open + did_change + did_close + formatting; documents.rs holds per-URI handles in DashMap; diagnostics.rs converts D-32 envelope to LSP Diagnostic; debounce.rs schedules 300 ms re-parse with cancellation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement formatting.rs + tests/showcase.rs integration harness; verify diagnostics + format + debounce against committed golden file</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/lsp/src/formatting.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/tests/showcase.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/tests/golden/formatted/BatteryPack.deal.expected
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/deal/lsp/src/backend.rs (from Task 2),
    /Users/dunnock/projects/deal-lang/deal/lsp/src/documents.rs (from Task 2),
    /Users/dunnock/projects/deal-lang/deal/cli/src/main.rs (lines 480–565 — invoke_formatter pattern),
    /Users/dunnock/projects/deal-lang/deal/tests/ffi/tests/parse.rs (showcase-driven harness pattern),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md (sections "Plan 03 / formatting.rs", "Plan 03 / tests/showcase.rs"),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-VALIDATION.md (LSP integration tests section)
  </read_first>
  <behavior>
    - formatting.rs encapsulates the in-memory format flow: take a URI, look up the live OwnedDealHandle (do NOT re-parse), acquire the per-handle tokio Mutex (D-45), call deal_ffi::safe::format(handle), compute the full-document range from the stored Rope (`Range { start: Position { line: 0, character: 0 }, end: Position { line: rope.len_lines() saturating-1, character: <last line char count> } }`), return `Vec<TextEdit>` with a single replacement.
    - If deal_has_errors(handle) is true, formatting.rs returns Ok(None) (do not format invalid syntax; diagnostics already published).
    - The handle is borrowed for the duration of the format call; Mutex is released before TextEdit is constructed.
    - The committed golden file `lsp/tests/golden/formatted/BatteryPack.deal.expected` is generated ONCE before this test runs by invoking the deal CLI's fmt subcommand on the canonical showcase file:
      `./target/release/deal fmt tests/showcase/sedan_project/vehicle/electrical/BatteryPack.deal > lsp/tests/golden/formatted/BatteryPack.deal.expected`
      It is then committed to git. Subsequent test runs compare byte-for-byte against this committed expected output — NOT against an in-test recomputation of `deal_ffi::safe::format`. This breaks the tautology flagged in checker Issue 6 (Plan 03 Task 3): the test now asserts that the LSP formatter agrees with a frozen reference produced via an independent codepath (CLI binary), making the test a regression detector rather than a self-equality check. Phase 3's gate (Plan 07 phase-3-smoke.sh) is the snapshot owner — if the formatter intentionally changes, the gate maintainer regenerates the golden via the same CLI command and re-commits.
    - tests/showcase.rs has at least 3 tokio::tests:
      - `diagnostics_match_phase_2_codes` — opens a known-bad showcase file (or a synthetic test fixture with syntax errors); asserts received diagnostics carry the expected E2xxx codes and ranges from the D-32 envelope.
      - `format_round_trip` — opens `tests/showcase/sedan_project/vehicle/electrical/BatteryPack.deal`, sends formatting request, asserts returned bytes match `include_str!("golden/formatted/BatteryPack.deal.expected")` byte-for-byte. No call to `deal_ffi::safe::format` inside the test; no UPDATE_GOLDEN escape hatch.
      - `debounce_collapses_rapid_changes` — fires 5 did_change events with sub-100ms gaps, then waits 400 ms, asserts diagnostics were published exactly once (debounce coalesced). Implementation: instrument documents.rs or use a counter in test client.
    - Tests spawn deal-lsp in-process via tower_lsp::LspService::new(...).serve(reader, writer) over tokio duplex pipes, sending JSON-RPC envelopes constructed via tower_lsp::lsp_types::* request/response types.
    - Test harness reads showcase files via `std::env::var("CARGO_MANIFEST_DIR")`-relative path `../tests/showcase/` (the committed symlink — PATTERNS.md lines 495–500 pattern).
    - All 3 tests pass via `cargo test -p deal-lsp --test showcase`.
  </behavior>
  <action>
    Implement formatting.rs per the call-pattern from PATTERNS.md lines 437–475 (cli/src/main.rs invoke_formatter pattern), with one adaptation: DO NOT re-parse — Plan 03 holds the live handle per D-42.

    Skeleton:
    1. `pub async fn handle_formatting(documents: &Documents, uri: &Url) -> Result<Option<Vec<TextEdit>>>`
    2. Look up handle_arc + rope via documents.get_handle(uri) + documents.get_buffer(uri). If either is None, return Ok(None).
    3. Acquire `let handle_guard = handle_arc.lock().await;`
    4. If deal_ffi::safe::has_errors(&*handle_guard), return Ok(None).
    5. Let formatted_bytes = deal_ffi::safe::format(&*handle_guard).ok_or_else(|| LspError::from("format failed"))?;
    6. Drop handle_guard.
    7. Compute Range from rope: `let last_line = rope.len_lines().saturating_sub(1); let last_col = rope.line(last_line).len_chars() as u32; let range = Range { start: Position { line: 0, character: 0 }, end: Position { line: last_line as u32, character: last_col } };`
    8. Return Ok(Some(vec![TextEdit { range, new_text: String::from_utf8(formatted_bytes)? }])).

    In backend.rs::formatting, replace the placeholder stub with `formatting::handle_formatting(&self.documents, &params.text_document.uri).await`.

    Golden file generation (one-time, executor runs this once during Task 3 before committing):
    - After Task 1's `cargo build --workspace --release` produces `target/release/deal`, run:
      `cd /Users/dunnock/projects/deal-lang/deal && mkdir -p lsp/tests/golden/formatted && ./target/release/deal fmt tests/showcase/sedan_project/vehicle/electrical/BatteryPack.deal > lsp/tests/golden/formatted/BatteryPack.deal.expected`
    - Commit `BatteryPack.deal.expected` to git as part of Task 3's commit.
    - The CLI's `deal fmt` subcommand is the snapshot-producing tool; the LSP formatter is the system under test.

    tests/showcase.rs: write the three integration tests using tower_lsp's in-process testing pattern.

    For `diagnostics_match_phase_2_codes`:
    - Construct a small malformed .deal source (e.g., `part def Foo { port def bar` missing the closing brace) OR pick a Phase 1.5 malformed-corpus file with known E2xxx codes.
    - did_open the file via DidOpenTextDocumentParams.
    - Wait for publishDiagnostics notification (with timeout 1s).
    - Assert diagnostics.len() > 0 and the first diagnostic.code matches the expected E2xxx code.

    For `format_round_trip` (Issue 6 resolution):
    - Read the showcase file `tests/showcase/sedan_project/vehicle/electrical/BatteryPack.deal` into source text.
    - Embed the committed expected via `const EXPECTED: &str = include_str!("golden/formatted/BatteryPack.deal.expected");` at the top of the test module.
    - Send didOpen + formatting request to the in-process LSP service.
    - Assert the returned TextEdit.new_text equals EXPECTED byte-for-byte: `assert_eq!(edit.new_text.as_bytes(), EXPECTED.as_bytes())`.
    - No call to `deal_ffi::safe::format` inside the test body. No UPDATE_GOLDEN env-var branch. If the formatter output diverges from EXPECTED, the test fails — the phase-gate maintainer regenerates the golden via the documented CLI command and re-commits.

    For `debounce_collapses_rapid_changes`:
    - Instrument Documents to expose a `pub fn parse_count(&self) -> usize` counter (atomic).
    - did_open a file → parse_count becomes 1.
    - Fire 5 did_change events at 50 ms intervals (within the 300 ms debounce window) → parse_count stays at 1.
    - Wait 400 ms → parse_count becomes 2 (one debounced re-parse fired).
    - Assert parse_count == 2.

    Use the `tower-lsp` test patterns demonstrated in `tower-lsp-boilerplate` (RESEARCH §1 line 173 reference). If those patterns are not directly usable, fall back to spawning the deal-lsp binary as a subprocess and communicating via stdin/stdout JSON-RPC — but in-process is preferred for speed.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && test -f lsp/tests/golden/formatted/BatteryPack.deal.expected && cargo test -p deal-lsp --test showcase -- --test-threads=1 && grep -q 'handle_formatting' lsp/src/formatting.rs && grep -q 'diagnostics_match_phase_2_codes' lsp/tests/showcase.rs && grep -q 'format_round_trip' lsp/tests/showcase.rs && grep -q 'include_str!.*BatteryPack.deal.expected' lsp/tests/showcase.rs && ! grep -q 'UPDATE_GOLDEN' lsp/tests/showcase.rs && grep -q 'debounce_collapses_rapid_changes' lsp/tests/showcase.rs && echo "integration tests OK"</automated>
  </verify>
  <done>
    `cargo test -p deal-lsp --test showcase` exits 0; formatting.rs handle_formatting works against the live handle (no re-parse); three integration tests cover diagnostics, formatting, and debounce; `lsp/tests/golden/formatted/BatteryPack.deal.expected` is committed and the format_round_trip test asserts byte-equality against it via `include_str!` (Issue 6 resolution — no in-test recomputation tautology); deal-lsp serves the showcase via in-memory formatting and push-mode diagnostics.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LSP client (VS Code, etc.) ↔ deal-lsp stdio | Malformed JSON-RPC, pathological input, oversized buffers crossing untrusted boundary. |
| deal-lsp ↔ Zig core via 9-export C ABI | Raw pointer surface; one mis-clone-before-free corrupts memory. |
| cargo registry ↔ workspace | tower-lsp, tokio, dashmap, ropey, async-trait, tracing pulled from crates.io. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | Tampering (supply-chain — cargo) | tower-lsp (pinned 0.20.0), tokio, dashmap, ropey, async-trait, tracing | mitigate | Pin exact versions in [workspace.dependencies] (tower-lsp = "0.20.0" per Issue 1 resolution); run `cargo audit` before commit; all 6 packages are `[ASSUMED]` per RESEARCH §1199 audit and have 5+ year publish history with well-known maintainers (tokio-rs, xacrimon, helix-editor, dtolnay). |
| T-3-04 | Denial-of-Service (malformed input) | tower-lsp JSON-RPC handler | mitigate | tower-lsp validates JSON-RPC envelope; per-document Mutex (D-45) guarantees one in-flight request per URI; OwnedDealHandle Drop fires on panic so memory is freed even on error paths. |
| T-3-05 | Tampering (FFI safety) | OwnedDealHandle / deal-ffi safe wrappers | mitigate | Clone-before-free invariant encapsulated inside deal-ffi::safe::* functions per PATTERNS.md Pitfall 3 (T-02-29). Each wrapper takes &OwnedDealHandle (borrow), clones output bytes before returning, never exposes raw pointer to LSP call sites. Drop impl on OwnedDealHandle ensures deal_free runs even on panic. `unsafe impl Send` justified by Phase 1 D-02/D-13: each handle owns an independent arena and per-handle Mutex (D-45) prevents concurrent access. |

**T-3-SC supply-chain checkpoint:** `slopcheck install tower-lsp tokio dashmap ropey async-trait tracing tracing-subscriber` (or `cargo search <pkg>` + manual repo review if slopcheck unavailable) BEFORE `cargo build`. All packages in RESEARCH §1199 audit table are `[ASSUMED]` with 5+ year history; none `[SUS]` or `[SLOP]`. tower-lsp pin is "0.20.0" — verify availability via `cargo search tower-lsp` at Task 1 start.
</threat_model>

<verification>
- `cd /Users/dunnock/projects/deal-lang/deal && cargo build --workspace --release` exits 0.
- `cd /Users/dunnock/projects/deal-lang/deal && cargo test --workspace` exits 0.
- `cd /Users/dunnock/projects/deal-lang/deal && cargo test -p deal-lsp --test showcase` exits 0 (3 integration tests pass).
- `nm -gU /Users/dunnock/projects/deal-lang/deal/target/release/deal-lsp` exists and is non-empty (binary built).
- `grep -c "links = \"deal\"" /Users/dunnock/projects/deal-lang/deal/{cli,lsp,deal-ffi,tests/ffi}/Cargo.toml` shows exactly 1 hit (deal-ffi only).
- `grep -q 'tower-lsp = { version = "0.20.0"' /Users/dunnock/projects/deal-lang/deal/Cargo.toml` confirms concrete pin (Issue 1).
- `test -f /Users/dunnock/projects/deal-lang/deal/lsp/tests/golden/formatted/BatteryPack.deal.expected` confirms golden committed (Issue 6).
- Grep gate: `grep -v '^//' deal/lsp/src/backend.rs | grep -c 'DEBOUNCE_MS' ` ≥ 1 (D-43 lock constant present); `grep -v '^//' deal/lsp/src/documents.rs | grep -c 'OwnedDealHandle'` ≥ 2 (handle table + helpers).
</verification>

<success_criteria>
- REQ-phase-3-3-lsp-server diagnostic + formatting subset satisfied: deal-lsp serves textDocument/publishDiagnostics and textDocument/formatting against the showcase.
- D-42 honored: per-document DealHandle* lifecycle on did_open / did_change.
- D-43 honored: 300 ms debounce with cancellation.
- D-44 honored: did_close is a no-op; handles persist for workspace lifetime.
- D-45 honored: per-handle tokio Mutex; OwnedDealHandle is Send.
- RESEARCH §10 honored: deal-ffi extracted as shared crate; `links = "deal"` single-claimant; cli migrated.
- RESEARCH §11 honored: push-mode diagnostics via publishDiagnostics.
- D-21 in-memory formatting honored: formatting reuses live handle, no save round-trip.
- tower-lsp pinned to concrete version "0.20.0" (Issue 1 resolution).
- format_round_trip test asserts against committed golden file (Issue 6 resolution — no in-test tautology).
- Wave 1 placement honored (Issue 4 resolution option a): parallel-safe with Plans 01 + 02 — no file-overlap with their files_modified sets.
- Plan 04 can layer completion / hover / definition / semantic-tokens on top of the Backend + Documents primitives without re-architecting.
</success_criteria>

<output>
Create `.planning/phases/03-editor-intelligence/03-03-SUMMARY.md` on completion with: tower-lsp version pinned ("0.20.0" — Issue 1), deal-ffi extraction status (lines moved verbatim vs. modified), integration test results (3 passing), committed golden-file size for format_round_trip (Issue 6), and any deviations from D-42/D-43/D-45 design (e.g., if Mutex granularity needed adjustment).
</output>
