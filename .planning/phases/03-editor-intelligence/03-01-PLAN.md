---
phase: 03-editor-intelligence
plan: 01
plan_id: 03-01
name: TextMate Grammars and VS Code Scaffold
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/dunnock/projects/deal-lang/vscode-deal/package.json
  - /Users/dunnock/projects/deal-lang/vscode-deal/language-configuration.json
  - /Users/dunnock/projects/deal-lang/vscode-deal/syntaxes/deal.tmLanguage.json
  - /Users/dunnock/projects/deal-lang/vscode-deal/syntaxes/dealx.tmLanguage.json
  - /Users/dunnock/projects/deal-lang/vscode-deal/snippets/deal.json
  - /Users/dunnock/projects/deal-lang/vscode-deal/snippets/dealx.json
  - /Users/dunnock/projects/deal-lang/vscode-deal/icons/deal-light.svg
  - /Users/dunnock/projects/deal-lang/vscode-deal/icons/deal-dark.svg
  - /Users/dunnock/projects/deal-lang/vscode-deal/icons/dealx-light.svg
  - /Users/dunnock/projects/deal-lang/vscode-deal/icons/dealx-dark.svg
  - /Users/dunnock/projects/deal-lang/vscode-deal/COLOR-CATEGORIES.md
  - /Users/dunnock/projects/deal-lang/vscode-deal/tsconfig.json
  - /Users/dunnock/projects/deal-lang/vscode-deal/.gitignore
  - /Users/dunnock/projects/deal-lang/vscode-deal/.vscodeignore
  - /Users/dunnock/projects/deal-lang/vscode-deal/test/tmLanguage.snapshot.test.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/test/scope-cases/*.deal
autonomous: true
requirements:
  - REQ-phase-3-1-textmate
must_haves:
  truths:
    - "Opening a .deal file in a VS Code dev host renders element keywords (part def, port def, ...), <<operator>> tokens, [<tag>] composition tags, multiplicity [1..*], and @annotation categories with distinct colors per RESEARCH §6 scope picks (D-38 TextMate primary)."
    - "Opening a .dealx file in a VS Code dev host renders composition-tag scopes (entity.other.attribute-name.composition.deal) and inherits all common scopes from the shared category set (D-41 visual parity)."
    - "Typing snippet prefix `partdef` (and the other 18 prefixes per RESEARCH §14) expands to the full element body."
    - "deal.toml present in the workspace triggers extension activation via workspaceContains glob (PS-1)."
    - "TextMate scope categories match the 20-row table in RESEARCH §8 verbatim so tree-sitter @-captures (Plan 02) can share names for D-41 parity."
  artifacts:
    - path: vscode-deal/package.json
      provides: Extension manifest with languages/grammars/snippets/icons/configuration contributions
      contains: '"id": "deal"'
    - path: vscode-deal/syntaxes/deal.tmLanguage.json
      provides: TextMate grammar for .deal files (scopeName source.deal)
      contains: '"scopeName": "source.deal"'
    - path: vscode-deal/syntaxes/dealx.tmLanguage.json
      provides: TextMate grammar for .dealx files (scopeName source.dealx)
      contains: '"scopeName": "source.dealx"'
    - path: vscode-deal/snippets/deal.json
      provides: 14 snippets (1-11 element defs + 17-19 header/import/package)
    - path: vscode-deal/snippets/dealx.json
      provides: 8 snippets (12-16 composition tags + 17-19 shared)
    - path: vscode-deal/COLOR-CATEGORIES.md
      provides: D-41 shared color-category lookup table (TextMate scope ↔ tree-sitter capture)
      contains: 'keyword.control.element.deal'
  key_links:
    - from: vscode-deal/package.json
      to: vscode-deal/syntaxes/deal.tmLanguage.json
      via: contributes.grammars[0].path
      pattern: '"path": "./syntaxes/deal.tmLanguage.json"'
    - from: vscode-deal/package.json
      to: vscode-deal/snippets/deal.json
      via: contributes.snippets[0].path
      pattern: '"path": "./snippets/deal.json"'
    - from: vscode-deal/COLOR-CATEGORIES.md
      to: tree-sitter-deal/queries/highlights.scm
      via: shared category names (D-41)
      pattern: '@keyword.element'
---

<objective>
Stand up the publishable shape of the `vscode-deal` extension with TextMate-only highlighting, snippet library, language configuration, file icons, and the shared color-category lock file. After this plan ships, F5 in VS Code launches a dev host that renders the 19-file showcase with distinct colors for every category in RESEARCH §8, and the extension is structurally ready for Plan 05's LanguageClient wiring.

Purpose: Deliver REQ-phase-3-1-textmate. Lock D-38 (TextMate is the primary VS Code highlight surface) and D-41 (color-category names shared with tree-sitter). No LSP code is added in this plan — Plan 05 wires the LanguageClient on top of this scaffold.

Output: A `vscode-deal/` package whose `npx vsce package` succeeds, whose dev host renders the showcase, and whose `COLOR-CATEGORIES.md` is the canonical scope-name reference that Plan 02's tree-sitter grammar consumes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dunnock/projects/deal-lang/deal/.planning/PROJECT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/ROADMAP.md
@/Users/dunnock/projects/deal-lang/deal/.planning/STATE.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md
@/Users/dunnock/projects/deal-lang/spec/grammar/lexical.ebnf
@/Users/dunnock/projects/deal-lang/spec/grammar/deal.ebnf
@/Users/dunnock/projects/deal-lang/spec/grammar/dealx.ebnf
@/Users/dunnock/projects/deal-lang/vscode-deal/README.md
@/Users/dunnock/projects/deal-lang/deal/include/deal.h

<interfaces>
<!-- Color category contract: every TextMate scope assigned here MUST exactly match the
     value in RESEARCH §8 column "TextMate scope". Plan 02's tree-sitter highlights.scm
     uses the matching @capture from the same row. -->

RESEARCH §6 table (source of truth for scope picks):
- `part def`/`port def`/etc → keyword.control.element.deal
- `in`/`out`/`inout` → keyword.other.direction.deal
- `abstract`/`variation` → storage.modifier.deal
- `import`/`package` → keyword.control.import.deal
- Definition name (declaration) → entity.name.type.deal
- Definition reference → entity.name.type.reference.deal
- Package path segments → entity.name.namespace.deal
- `<<specializes>>` etc → keyword.operator.relationship.deal
- `@trace`/`@simulation`/etc → support.type.annotation.deal
- `[1..*]` multiplicity → constant.numeric.multiplicity.deal
- `[<system>]` composition tag → entity.other.attribute-name.composition.deal
- `/** */` doc → comment.block.documentation.deal
- `//` → comment.line.double-slash.deal
- `/* */` → comment.block.deal
- `"..."` → string.quoted.double.deal
- integers → constant.numeric.integer.deal
- `{ }` → punctuation.section.block.{begin,end}.deal
- `[< >]` brackets → punctuation.definition.tag.{begin,end}.deal
- `<< >>` brackets → punctuation.definition.operator.{begin,end}.deal

RESEARCH §14 snippet table (verbatim):
- 11 element-def snippets (partdef, portdef, actiondef, statedef, reqdef, constraintdef, attrdef, itemdef, ifacedef, conndef, flowdef)
- 5 composition-tag snippets (tagsys, tagcon, tagsat, tagalloc, tagtrace)
- 3 shared snippets (header, import, pkg)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold extension manifest, language configuration, file icons, snippets, and COLOR-CATEGORIES.md</name>
  <files>
    /Users/dunnock/projects/deal-lang/vscode-deal/package.json,
    /Users/dunnock/projects/deal-lang/vscode-deal/language-configuration.json,
    /Users/dunnock/projects/deal-lang/vscode-deal/snippets/deal.json,
    /Users/dunnock/projects/deal-lang/vscode-deal/snippets/dealx.json,
    /Users/dunnock/projects/deal-lang/vscode-deal/icons/deal-light.svg,
    /Users/dunnock/projects/deal-lang/vscode-deal/icons/deal-dark.svg,
    /Users/dunnock/projects/deal-lang/vscode-deal/icons/dealx-light.svg,
    /Users/dunnock/projects/deal-lang/vscode-deal/icons/dealx-dark.svg,
    /Users/dunnock/projects/deal-lang/vscode-deal/COLOR-CATEGORIES.md,
    /Users/dunnock/projects/deal-lang/vscode-deal/tsconfig.json,
    /Users/dunnock/projects/deal-lang/vscode-deal/.gitignore,
    /Users/dunnock/projects/deal-lang/vscode-deal/.vscodeignore
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/vscode-deal/README.md,
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (sections §5, §6, §8, §14),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md (Plan 01 file table),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md (D-38, D-40, D-41 sections, <specifics> snippet starting set, file icon iconography)
  </read_first>
  <behavior>
    - package.json declares both languages (id=deal, id=dealx) with extensions [.deal] and [.dealx], `configuration` pointing at language-configuration.json, and per-language icon objects.
    - package.json activationEvents includes exactly: "onLanguage:deal", "onLanguage:dealx", "workspaceContains:**/deal.toml" (per PS-1, RESEARCH §5).
    - package.json contributes.snippets entries reference ./snippets/deal.json and ./snippets/dealx.json.
    - package.json engines.vscode is "^1.95.0" per RESEARCH §5 baseline assumption.
    - package.json publisher is "deal-lang"; name is "vscode-deal"; displayName is "DEAL Language"; version starts at "0.3.0" matching the deal-lsp pin (D-52).
    - package.json contributes.configuration declares "deal.lsp.path" (string, default "") and "deal.lsp.trace" (enum off|messages|verbose, default "off") — stubs for Plan 05.
    - language-configuration.json declares comments line "//" and block ["/*", "*/"]; brackets [["{","}"], ["[","]"], ["<<",">>"], ["[<",">]"]]; autoClosingPairs covering the brace, bracket, and `[<` / `<<` pairs; surroundingPairs analogous.
    - snippets/deal.json contains exactly the 14 entries from RESEARCH §14: partdef, portdef, actiondef, statedef, reqdef, constraintdef, attrdef, itemdef, ifacedef, conndef, flowdef, header, import, pkg.
    - snippets/dealx.json contains exactly: tagsys, tagcon, tagsat, tagalloc, tagtrace, header, import, pkg.
    - Each snippet entry uses VS Code snippet JSON shape {"prefix": "...", "body": ["..."], "description": "..."} per RESEARCH §14 "Body summary" column.
    - icons/*.svg are valid SVG files (parseable by xmllint) with viewBox attribute and at least one `<path>` or `<rect>` child: deal-* renders a blueprint-sheet motif; dealx-* renders a JSX-style angle-bracket motif. Each pair has a light and dark variant.
    - COLOR-CATEGORIES.md contains a markdown table with exactly the 20 rows from RESEARCH §8, headers "| Category | TextMate scope | Tree-sitter capture |". Plan 02 reads this file as the source of truth.
    - tsconfig.json targets ES2020, module commonjs, outDir "./out", strict true, rootDir "./src" (src/ added in Plan 05).
    - .gitignore excludes node_modules, out, *.vsix, server/.
    - .vscodeignore excludes test/, src/test/, **/*.ts.map, **/*.test.ts, tsconfig.json, but does NOT exclude syntaxes/, snippets/, icons/, COLOR-CATEGORIES.md, language-configuration.json.
  </behavior>
  <action>
    Create the manifest, language configuration, snippet libraries, file icons, and COLOR-CATEGORIES.md per the locked shapes in RESEARCH §5 (package.json), §6/§8 (scope categories), §14 (snippet table), and CONTEXT.md `<specifics>` (icon iconography, snippet picks).

    package.json contributions table:
    - languages: two entries `{id, aliases, extensions, configuration, icon: {light, dark}}` for deal and dealx; deal aliases ["DEAL"]; dealx aliases ["DEAL Composition"].
    - grammars: two entries pointing at `./syntaxes/deal.tmLanguage.json` (scopeName `source.deal`) and `./syntaxes/dealx.tmLanguage.json` (scopeName `source.dealx`); Plan creates these grammar files in Task 2.
    - snippets: two entries pointing at the deal/dealx snippet JSONs created here.
    - configuration: title "DEAL", properties `deal.lsp.path` and `deal.lsp.trace` per RESEARCH §5 lines 497–501.
    - commands: two entries `deal.restartServer` and `deal.showOutput` (Plan 05 implements handlers; this plan declares the contributions only).

    language-configuration.json: include the `[< >]` and `<< >>` bracket pairs in `brackets`, `autoClosingPairs`, and `surroundingPairs` because TextMate's auto-close behavior depends on this file (REQ-phase-3-1-textmate calls out "bracket matching + auto-close for `[< >]`").

    snippets/deal.json + snippets/dealx.json: emit the 14 + 8 snippet bodies verbatim from RESEARCH §14 "Body summary" column. Use tab-stop placeholders `$1`/`$2`/`$0` exactly as written.

    Icons (Claude's Discretion per CONTEXT.md `<specifics>`): hand-author 4 small SVGs (≤2 KB each) using simple primitives. `.deal` = solid blueprint sheet (rectangle outline + grid lines suggesting an engineering drawing); `.dealx` = JSX-style angle brackets framing a composition badge. Use distinct light/dark fill colors so the icon-theme contract renders correctly.

    COLOR-CATEGORIES.md: emit the 20-row table from RESEARCH §8 verbatim plus a short preamble citing D-41 and D-38, and a footer pointing readers at Plan 02 (`tree-sitter-deal/queries/highlights.scm`) for the consumer side.

    tsconfig.json + .gitignore + .vscodeignore: emit shapes per the standard VS Code TypeScript extension template — these are minimal scaffolding that Plan 05 builds on.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/vscode-deal && node -e "const m=require('./package.json'); if(m.name!=='vscode-deal'||!m.activationEvents.includes('workspaceContains:**/deal.toml')||m.contributes.languages.length!==2||m.contributes.grammars.length!==2||m.contributes.snippets.length!==2||!m.contributes.configuration.properties['deal.lsp.path']) throw new Error('manifest invariants violated'); console.log('manifest OK');" && node -e "const d=require('./snippets/deal.json'),x=require('./snippets/dealx.json'); const need=['partdef','portdef','actiondef','statedef','reqdef','constraintdef','attrdef','itemdef','ifacedef','conndef','flowdef','header','import','pkg']; for(const k of need) if(!d[k]) throw new Error('deal snippet missing: '+k); const xneed=['tagsys','tagcon','tagsat','tagalloc','tagtrace','header','import','pkg']; for(const k of xneed) if(!x[k]) throw new Error('dealx snippet missing: '+k); console.log('snippets OK');" && grep -c 'keyword.control.element.deal' COLOR-CATEGORIES.md | grep -qv '^0$' && for f in icons/deal-light.svg icons/deal-dark.svg icons/dealx-light.svg icons/dealx-dark.svg; do xmllint --noout "$f" || exit 1; done && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    package.json, language-configuration.json, snippets/{deal,dealx}.json, icons/*.svg, COLOR-CATEGORIES.md, tsconfig.json, .gitignore, .vscodeignore all exist; manifest invariants pass (correct activationEvents, languages, grammars, snippets, configuration); 14 + 8 snippet keys present; 4 SVG icons parse with xmllint; COLOR-CATEGORIES.md contains the 20-row §8 table.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author deal.tmLanguage.json and dealx.tmLanguage.json plus snapshot test harness</name>
  <files>
    /Users/dunnock/projects/deal-lang/vscode-deal/syntaxes/deal.tmLanguage.json,
    /Users/dunnock/projects/deal-lang/vscode-deal/syntaxes/dealx.tmLanguage.json,
    /Users/dunnock/projects/deal-lang/vscode-deal/test/tmLanguage.snapshot.test.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/test/scope-cases/element-keywords.deal,
    /Users/dunnock/projects/deal-lang/vscode-deal/test/scope-cases/operators.deal,
    /Users/dunnock/projects/deal-lang/vscode-deal/test/scope-cases/annotations.deal,
    /Users/dunnock/projects/deal-lang/vscode-deal/test/scope-cases/composition-tags.dealx,
    /Users/dunnock/projects/deal-lang/vscode-deal/test/scope-cases/multiplicity.deal,
    /Users/dunnock/projects/deal-lang/vscode-deal/test/__snapshots__/.gitkeep
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/vscode-deal/COLOR-CATEGORIES.md (from Task 1),
    /Users/dunnock/projects/deal-lang/spec/grammar/lexical.ebnf,
    /Users/dunnock/projects/deal-lang/spec/examples/showcase/packages/vehicle/electrical (look at any battery.deal),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (sections §3 lexical patterns, §6 scope-pick table, §8 parity table)
  </read_first>
  <behavior>
    - deal.tmLanguage.json declares scopeName "source.deal", name "DEAL", fileTypes ["deal"]; patterns include includes for: comments (line, block, doc), strings, numbers, multiplicity, keywords, modifiers, annotations, operators, namespaces, type-references, definition-declarations, punctuation.
    - dealx.tmLanguage.json declares scopeName "source.dealx", name "DEAL Composition", fileTypes ["dealx"]; reuses 80% of deal.tmLanguage.json patterns plus dealx-specific composition-tag rules using `entity.other.attribute-name.composition.deal` scope.
    - Every scope name in both grammars matches RESEARCH §6/§8 table (no orphan scopes; no scope outside the 20-row category set).
    - Element keywords `part def`, `port def`, `action def`, `state def`, `requirement def`, `constraint def`, `attribute def`, `item def`, `interface def`, `connection def`, `flow def` are matched as multi-word tokens (space between `part` and `def` required) and assigned `keyword.control.element.deal`.
    - Direction keywords `in`, `out`, `inout` → `keyword.other.direction.deal`; modifier keywords `abstract`, `variation` → `storage.modifier.deal`.
    - Operators `<<specializes>>`, `<<redefines>>`, `<<conjugates>>`, `<<derives>>` matched as a single regex group with scope `keyword.operator.relationship.deal`; bracket pair `<<`/`>>` gets `punctuation.definition.operator.begin.deal` / `.end.deal`.
    - Composition tag `[<identifier>]` matched in dealx.tmLanguage.json as `entity.other.attribute-name.composition.deal` with `punctuation.definition.tag.begin.deal` / `.end.deal` on the `[<` and `>]` brackets.
    - Multiplicity `[1..*]`, `[0..1]`, `[N]` (where N is integer or `*`) matched as `constant.numeric.multiplicity.deal`.
    - Doc comments `/** … */` → `comment.block.documentation.deal`; line comments `//` → `comment.line.double-slash.deal`; block comments `/* */` → `comment.block.deal`.
    - Strings `"…"` → `string.quoted.double.deal`.
    - Annotation use `@identifier` (followed optionally by `:` and `<<operator>>`) → `support.type.annotation.deal` on the `@identifier` part.
    - Test harness `tmLanguage.snapshot.test.ts` uses `vscode-textmate` (devDep) to tokenize each `test/scope-cases/*.deal` and `*.dealx` file; assert byte-stable JSON snapshot of `[startIndex, endIndex, scopes[]]` triples. Snapshot lives in `test/__snapshots__/` (gitkept directory).
    - Test cases cover each row of RESEARCH §6: element keywords (5 lines, 5 different element kinds), operators (one line per `<<specializes>>`/`<<redefines>>`/`<<conjugates>>`/`<<derives>>`), annotations (`@trace`, `@simulation:<<computes>>`, `@header`), composition tags (`[<system>]`, `[<connect from={a} to={b}/>]`), multiplicity (`[1..*]`, `[0..1]`, `[3]`).
  </behavior>
  <action>
    Author both TextMate grammar JSON files using oniguruma regex patterns appropriate to VS Code's grammar engine. Structure each grammar as `{$schema, name, scopeName, fileTypes, patterns, repository}` with `patterns` referencing named entries in `repository`. Use the RESEARCH §6 / §8 scope names exactly — every assignment must match a row in the table.

    Pattern shape per repository entry:
    - `comments`: nested `begin/end` patterns for `/** */` (doc), `/* */` (block), `//.*$` (line).
    - `element-keyword`: regex `(?<![\w-])(part|port|action|state|requirement|constraint|attribute|item|interface|connection|flow)\s+def(?![\w-])`.
    - `direction-keyword`: regex `(?<![\w-])(in|out|inout)(?![\w-])`.
    - `modifier-keyword`: regex `(?<![\w-])(abstract|variation)(?![\w-])`.
    - `import-keyword`: regex `(?<![\w-])(import|package|as)(?![\w-])`.
    - `relationship-operator`: regex `<<(specializes|redefines|conjugates|derives)>>` with `captures` assigning `keyword.operator.relationship.deal` to the inner group and `punctuation.definition.operator.{begin,end}.deal` to the `<<`/`>>` outer groups.
    - `composition-tag` (dealx only): regex `\[<([A-Za-z_][A-Za-z0-9_]*)` with the inner group → `entity.other.attribute-name.composition.deal` and `[<` → punctuation; closing `>]` matched separately.
    - `annotation`: regex `@[A-Za-z_][A-Za-z0-9_]*` → `support.type.annotation.deal`.
    - `multiplicity`: regex `\[\s*(\d+|\*)(\s*\.\.\s*(\d+|\*))?\s*\]` → `constant.numeric.multiplicity.deal`.
    - `string`: standard double-quoted with `\\.` escape support.
    - `number`: integer regex `\b\d+\b` → `constant.numeric.integer.deal`.
    - `namespace`: qualified path segments (the dotted identifiers in `import sedan_project.vehicle.electrical;`) → `entity.name.namespace.deal`.
    - `type-definition` (declaration site): match an identifier following an `element-keyword` capture; scope `entity.name.type.deal` with modifier `entity.name.type.deal definition`. Note TextMate does not have modifiers; use the scope name alone — semantic-tokens overlay (Plan 04) adds the `definition` modifier.
    - `type-reference`: identifiers after `:` in `voltage : Real` → `entity.name.type.reference.deal` (best-effort regex match; semantic tokens will refine).
    - `punctuation`: braces `{ }` → `punctuation.section.block.{begin,end}.deal`.

    dealx.tmLanguage.json includes all of the above plus the composition-tag pattern (deal.tmLanguage.json does NOT include this — `[< >]` is dealx-only per Phase 1 lexer mode selection D-04/lexical.ebnf).

    Test harness `test/tmLanguage.snapshot.test.ts`:
    - Use `vscode-textmate` (add to package.json devDependencies via Task 1's manifest if missed — otherwise add here) + `vscode-oniguruma`.
    - Load each `test/scope-cases/*.deal` and `*.dealx`, tokenize line-by-line, emit JSON `[{line, tokens:[{start,end,scopes}]}]`, snapshot via `mocha` + `chai` `expect(JSON.stringify(actual)).to.equal(snapshot)`. If snapshot file is absent, create it on first run and mark as committed.

    Add `vscode-textmate` and `vscode-oniguruma` to `package.json` devDependencies if Task 1 did not include them.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/vscode-deal && node -e "const d=require('./syntaxes/deal.tmLanguage.json'); if(d.scopeName!=='source.deal'||!d.patterns||!d.repository) throw new Error('deal grammar invariants violated'); const allScopes=JSON.stringify(d); for(const s of ['keyword.control.element.deal','keyword.other.direction.deal','storage.modifier.deal','keyword.operator.relationship.deal','support.type.annotation.deal','constant.numeric.multiplicity.deal','comment.block.documentation.deal','entity.name.type.deal']) if(!allScopes.includes(s)) throw new Error('missing scope: '+s); console.log('deal grammar OK');" && node -e "const x=require('./syntaxes/dealx.tmLanguage.json'); if(x.scopeName!=='source.dealx') throw new Error('dealx scope wrong'); if(!JSON.stringify(x).includes('entity.other.attribute-name.composition.deal')) throw new Error('missing composition tag scope'); console.log('dealx grammar OK');" && npm install --no-audit --no-fund --silent && npx mocha --require ts-node/register test/tmLanguage.snapshot.test.ts</automated>
  </verify>
  <done>
    Both TextMate grammar files exist with correct scopeName; all 20 §8 categories present in deal.tmLanguage.json (or dealx-specific ones in dealx.tmLanguage.json); snapshot test passes on all 5 scope-case files; `npx vsce package` from the Plan-01 directory completes without error (informational — gate enforced in Plan 07).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VS Code shell ↔ TextMate grammar JSON | Grammars are loaded by VS Code; malformed regex can hang the tokenizer (catastrophic backtracking). |
| npm install ↔ dev dependencies | `vscode-textmate`, `vscode-oniguruma`, ts-node, mocha pulled from npm registry. |
| .vsix install ↔ user machine | Marketplace package surface (production code paths). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | Tampering (supply-chain — npm) | vscode-deal devDependencies | mitigate | Pin top-level deps in package.json to exact versions (no `^`/`~`); CI runs `npm audit --omit=dev --audit-level=high` once Plan 05 adds the test script. Plan 07 closes by verifying lockfile presence. |
| T-3-06 | Denial-of-Service | TextMate regex patterns | mitigate | Avoid unbounded backtracking: every regex in repository uses bounded quantifiers and negated character classes (no `.*` followed by `.*`). Snapshot tests in Task 2 cover the 20 categories — if backtracking ever spikes, mocha timeout fires. |

**T-3-SC supply-chain checkpoint:** All npm packages added by this plan (`vscode-textmate`, `vscode-oniguruma`, `ts-node`, `mocha`, `chai`, `@types/*`) are `[ASSUMED]` per RESEARCH §1199 Package Legitimacy Audit and resolve to long-lived (5+ yr) Microsoft / community packages. Pre-`npm install`, the executor SHOULD run `slopcheck install <package list>` per RESEARCH §1228 planner action; if slopcheck is unavailable, a manual `npm view <pkg>` review of publisher + last-publish date is acceptable. None of these packages are flagged `[SUS]` or `[SLOP]` in research.
</threat_model>

<verification>
- `cd /Users/dunnock/projects/deal-lang/vscode-deal && npm install` exits 0.
- `cd /Users/dunnock/projects/deal-lang/vscode-deal && npx mocha --require ts-node/register test/tmLanguage.snapshot.test.ts` exits 0.
- `cd /Users/dunnock/projects/deal-lang/vscode-deal && npx vsce package --allow-missing-repository --skip-license -o /tmp/vscode-deal-plan-01.vsix` produces a `.vsix` (build-time sanity; full publish gated in Plan 06).
- Grep gates: `grep -c 'keyword.control.element.deal' syntaxes/deal.tmLanguage.json` ≥ 1; `grep -c 'entity.other.attribute-name.composition.deal' syntaxes/dealx.tmLanguage.json` ≥ 1; `grep -c '|' COLOR-CATEGORIES.md` ≥ 20 (table rows).
</verification>

<success_criteria>
- REQ-phase-3-1-textmate fully satisfied: TextMate grammars, snippets, language config, file icons all exist and load in VS Code; snapshot tests pass.
- D-38 honored: TextMate is the only highlight source contributed in this plan; no LSP wiring code is added (Plan 05's responsibility).
- D-41 honored: COLOR-CATEGORIES.md contains the canonical 20-row table; Plan 02 will reference the same names from `queries/highlights.scm`.
- D-40 prerequisite: `deal.lsp.path` and `deal.lsp.trace` settings declared but no LSP launch logic yet — Plan 05 fills the activation handler.
- Phase-3-gate dependency: this plan's `vscode-deal/` directory must `vsce package` successfully when Plan 07 runs `npm run package`.
</success_criteria>

<output>
Create `.planning/phases/03-editor-intelligence/03-01-SUMMARY.md` on completion with: files created (count), snapshot test count, scope categories covered (20 expected), and notes on any RESEARCH §6 regex assumptions that needed revision against the showcase corpus.
</output>
