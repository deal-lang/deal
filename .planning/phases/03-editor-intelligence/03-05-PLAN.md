---
phase: 03-editor-intelligence
plan: 05
plan_id: 03-05
name: vscode-deal LSP Wiring (LanguageClient + Status Bar + Commands)
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-03
files_modified:
  - /Users/dunnock/projects/deal-lang/vscode-deal/package.json
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/extension.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/client.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/status.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/status_bar.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/commands.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/binary.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/test/runTest.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/test/suite/index.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/test/suite/activation.test.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/test/unit/status.test.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/.vscode-test.mjs
  - /Users/dunnock/projects/deal-lang/vscode-deal/.mocharc.unit.json
autonomous: true
requirements:
  - REQ-phase-3-4-vscode-lsp
must_haves:
  truths:
    - "Opening a workspace containing deal.toml triggers extension activation (PS-1, RESEARCH §5 activationEvents)."
    - "extension.ts launches deal-lsp via vscode-languageclient/node with stdio transport; clientOptions.documentSelector covers both deal and dealx schemes."
    - "Status bar transitions starting… → ready → error per D-40 silent-fallback policy; no modal/toast on LSP failure."
    - "Command palette exposes deal.restartServer and deal.showOutput commands; restartServer stops + restarts the LanguageClient; showOutput focuses the LSP output channel."
    - "Semantic tokens render in the editor automatically (vscode-languageclient registers the provider when the server declares SemanticTokensServerCapabilities in initialize — no extra TS wiring needed)."
    - "deal.lsp.path configuration override is honored: if set to a non-empty string, that path is used instead of the bundled/downloaded binary (configurable-path escape hatch per D-40)."
    - "@vscode/test-electron suite passes 3 tests: activates_on_deal_toml, language_client_connects, format_on_save_invokes_lsp (per Issue 5, status_bar_transitions moves to the pure-mocha unit suite)."
    - "Pure mocha unit suite passes 1 test: status_bar_transitions — verifies updateStatusBar(state) → {text, tooltip} mapping with no electron boot, no xvfb (~1s feedback vs ~30s electron — Issue 5 resolution)."
    - "Binary resolution is stubbed (binary.ts): looks at deal.lsp.path first, then a bundled `server/` directory; auto-download (full bootstrap.ts) is Plan 06's responsibility — this plan leaves a TODO for the download path."
  artifacts:
    - path: vscode-deal/src/extension.ts
      provides: activate/deactivate entrypoints; instantiates LanguageClient
      contains: "vscode.ExtensionContext"
    - path: vscode-deal/src/client.ts
      provides: LanguageClient factory + start/stop helpers + restart helper
      contains: "vscode-languageclient/node"
    - path: vscode-deal/src/status.ts
      provides: pure-function status-bar state mapping `updateStatusBar(state: State): { text: string; tooltip: string }` — extracted per Issue 5 so the state mapping logic is unit-testable without electron/xvfb (~1s per-commit feedback)
      contains: "updateStatusBar"
    - path: vscode-deal/src/status_bar.ts
      provides: thin VS Code integration layer that owns the StatusBarItem and wires client.onDidChangeState to status.ts's updateStatusBar
      contains: "vscode.StatusBarItem"
    - path: vscode-deal/src/commands.ts
      provides: deal.restartServer + deal.showOutput command handlers
    - path: vscode-deal/src/binary.ts
      provides: binary resolution stub (deal.lsp.path > bundled > TODO[Plan 06] auto-download)
    - path: vscode-deal/src/test/suite/activation.test.ts
      provides: @vscode/test-electron tests covering 3 behaviors (activation, client-connect, format-on-save)
    - path: vscode-deal/src/test/unit/status.test.ts
      provides: pure-mocha unit test for status.ts updateStatusBar — runs without electron/xvfb (Issue 5 resolution; ~1s vs ~30s)
    - path: vscode-deal/.vscode-test.mjs
      provides: electron test runner config (workspace = showcase, extension under test = vscode-deal)
    - path: vscode-deal/.mocharc.unit.json
      provides: pure-mocha config for the unit suite (spec = out/test/unit/**/*.test.js; no electron)
  key_links:
    - from: vscode-deal/src/extension.ts
      to: vscode-deal/src/client.ts
      via: createClient(context) factory call
      pattern: "createClient"
    - from: vscode-deal/src/client.ts
      to: deal-lsp binary (resolved via binary.ts)
      via: ServerOptions { run: { command: lspPath, transport: stdio } }
      pattern: "TransportKind.stdio"
    - from: vscode-deal/src/status_bar.ts
      to: vscode-deal/src/status.ts
      via: client.onDidChangeState → updateStatusBar(state) → apply {text, tooltip} to StatusBarItem (Issue 5 logic seam)
      pattern: "updateStatusBar"
    - from: vscode-deal/src/test/unit/status.test.ts
      to: vscode-deal/src/status.ts
      via: direct import + assert {text, tooltip} for each State value; no electron
      pattern: "updateStatusBar"
---

<objective>
Wire `vscode-deal` to consume the `deal-lsp` server built in Plans 03–04. After this plan ships: opening a workspace containing `deal.toml` activates the extension, launches `deal-lsp` via stdio, connects a LanguageClient that handles all 5 capabilities + semantic tokens transparently, and surfaces lifecycle state through a status bar item per the D-40 silent-fallback model. The auto-download path is stubbed — Plan 06 implements the actual bootstrap.ts.

Purpose: Deliver the LSP-wiring half of REQ-phase-3-4-vscode-lsp. Lock D-40 (silent TextMate fallback + status-bar indicator) into the TypeScript extension. Plan 01 delivered TextMate + manifest scaffolding; Plan 03/04 delivered the server; this plan is the glue.

Output: A vscode-deal extension that, when run with a local deal-lsp binary on PATH (or via the `deal.lsp.path` setting), provides full LSP intelligence on the showcase project. Test suite passes 3 electron tests + 1 unit test (Issue 5 split — status_bar logic moves from electron suite to pure-mocha unit suite for ~30x feedback speedup).

Wave assignment: This plan is `wave: 2` with `depends_on: ["03-01", "03-03"]`. Both dependencies are wave 1 after the Issue 4 resolution (Plan 03 moved to wave 1; Plan 01 was already wave 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dunnock/projects/deal-lang/deal/.planning/PROJECT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md
@/Users/dunnock/projects/deal-lang/vscode-deal/package.json
@/Users/dunnock/projects/deal-lang/vscode-deal/tsconfig.json
@/Users/dunnock/projects/deal-lang/vscode-deal/README.md

<interfaces>
<!-- vscode-languageclient/node API surface (RESEARCH §5 lines 513–547): -->
import { LanguageClient, LanguageClientOptions, ServerOptions, TransportKind, State } from 'vscode-languageclient/node';

ServerOptions = {
  run:   { command: string, args?: string[], transport: TransportKind.stdio },
  debug: { command: string, args?: string[], transport: TransportKind.stdio },
};

LanguageClientOptions = {
  documentSelector: [
    { scheme: 'file', language: 'deal' },
    { scheme: 'file', language: 'dealx' }
  ],
  synchronize: {
    fileEvents: vscode.workspace.createFileSystemWatcher('**/deal.toml'),
  },
  outputChannelName: 'DEAL Language Server',
};

new LanguageClient('deal', 'DEAL Language Server', serverOptions, clientOptions);
await client.start();
client.onDidChangeState(e => { ... });  // State.Stopped | State.Starting | State.Running

<!-- D-40 status-bar state machine (consume verbatim from RESEARCH §5 lines 552–567): -->
State.Stopped  → text "$(error) DEAL LSP", tooltip "DEAL LSP error — click for output"
State.Starting → text "$(sync~spin) DEAL LSP", tooltip "DEAL LSP starting…"
State.Running  → text "$(check) DEAL LSP", tooltip "DEAL LSP ready"

<!-- Issue 5 logic seam (extracted pure function in status.ts): -->
export type LspState = 'stopped' | 'starting' | 'running';
export interface StatusBarView { text: string; tooltip: string }
export function updateStatusBar(state: LspState): StatusBarView;

<!-- VS Code Extension API entry points: -->
export async function activate(context: vscode.ExtensionContext): Promise<void>;
export function deactivate(): Thenable<void> | undefined;

<!-- D-40 silent-fallback policy: -->
No modal dialogs.
No vscode.window.showWarningMessage / showErrorMessage popups on LSP failure.
Status bar IS the failure-surface.
TextMate keeps rendering even when LSP is down (handled by VS Code core — extension does nothing extra).

<!-- Plan 01's package.json (Wave 1 output) provides activationEvents, language IDs, commands. -->
<!-- Plan 06 will replace binary.ts's TODO with a real bootstrap.ts implementing D-50..D-53. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend package.json with TypeScript build pipeline + LSP deps; scaffold src/extension.ts + src/client.ts + src/binary.ts</name>
  <files>
    /Users/dunnock/projects/deal-lang/vscode-deal/package.json,
    /Users/dunnock/projects/deal-lang/vscode-deal/src/extension.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/src/client.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/src/binary.ts
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/vscode-deal/package.json (after Plan 01 — has manifest contributions, no main entry yet),
    /Users/dunnock/projects/deal-lang/vscode-deal/tsconfig.json (from Plan 01),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (§5 LanguageClient setup, §10 — note `out/` output directory),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md (Plan 05 file table)
  </read_first>
  <behavior>
    - package.json gains `"main": "./out/extension.js"` (already declared by Plan 01 manifest contributions — confirm it points to ./out/extension.js).
    - package.json gains scripts.compile = "tsc -p ./", scripts.watch = "tsc -watch -p ./", scripts.test = "vscode-test", scripts.test:unit = "mocha --config .mocharc.unit.json", scripts.lint = "eslint src --ext ts".
    - package.json devDependencies gains exact-pinned versions: @types/node ^20, @types/vscode ^1.95.0, @types/mocha ^10, @types/chai ^4, @vscode/test-electron ^2.4, @vscode/test-cli ^0.0.10, mocha ^10, chai ^4, sinon ^17, @types/sinon ^17, typescript ^5.4, eslint ^9, @typescript-eslint/eslint-plugin ^7, @typescript-eslint/parser ^7.
    - package.json dependencies gains "vscode-languageclient" pinned to ^9 (latest LTS series compatible with VS Code 1.95+ engine; verify exact version via `npm view vscode-languageclient version` before commit).
    - package.json contributes.commands entries from Plan 01 (deal.restartServer, deal.showOutput) remain unchanged — Task 2 wires their handlers.
    - src/extension.ts exports `activate(context: vscode.ExtensionContext): Promise<void>` and `deactivate(): Thenable<void> | undefined`.
    - activate() flow:
      1. Resolve the LSP binary path via binary.resolveLspPath(context). If null, log "deal-lsp binary not found — TextMate-only mode" via the LSP output channel and return early (status bar shows error state).
      2. Call client.createClient(context, lspPath) → returns LanguageClient.
      3. Call client.start() with try/catch — on failure, log via output channel and set status bar to error; do NOT throw or showError (D-40).
      4. Instantiate status bar via status_bar.installStatusBar(client) and push to context.subscriptions.
      5. Register commands via commands.registerCommands(context, client).
    - deactivate() calls client.stop().
    - src/client.ts:
      - `export async function createClient(context: vscode.ExtensionContext, lspPath: string): Promise<LanguageClient>` — builds ServerOptions { run: { command: lspPath, args: [], transport: TransportKind.stdio }, debug: { command: lspPath, args: ['--log=debug'], transport: TransportKind.stdio } } and LanguageClientOptions with documentSelector covering deal + dealx, synchronize.fileEvents = workspace.createFileSystemWatcher('**/deal.toml'), outputChannelName 'DEAL Language Server'.
      - Returns `new LanguageClient('deal', 'DEAL Language Server', serverOptions, clientOptions)`.
    - src/binary.ts:
      - `export interface BinaryResolution { path: string; source: 'configured' | 'bundled' | 'downloaded' | 'path' }`.
      - `export async function resolveLspPath(context: vscode.ExtensionContext): Promise<BinaryResolution | null>`:
        1. Check `vscode.workspace.getConfiguration('deal').get<string>('lsp.path')` — if non-empty AND fs.existsSync, return { path, source: 'configured' }.
        2. Check `vscode.Uri.joinPath(context.extensionUri, 'server', binaryFilename())` — if exists (bundled offline .vsix per D-51), return { path, source: 'bundled' }.
        3. TODO [Plan 06]: auto-download from GitHub Releases. For now, return null and emit a debug log.
      - `binaryFilename()` returns 'deal-lsp.exe' on win32, 'deal-lsp' otherwise.
      - Add a comment block in binary.ts explicitly noting "Plan 06 replaces the TODO with bootstrap.ts auto-download per D-50, D-52".
    - All TypeScript compiles via `tsc -p ./` — no type errors; strict mode enabled per Plan 01's tsconfig.
  </behavior>
  <action>
    Update package.json to wire the TypeScript build pipeline and LSP client dependency. Add the `vscode-languageclient` runtime dep and the devDeps listed in the behavior section. Pin exact versions via `npm view <pkg> version` at execution time (do not use `^` ranges that allow major drift).

    Scaffold the three new source files in vscode-deal/src/:

    extension.ts shape (~50 LOC): activate function resolves binary, creates language client, installs status bar, registers commands, then awaits client.start() inside a try/catch (catch logs but does not modal — D-40). deactivate awaits client.stop().

    client.ts shape (~30 LOC): pure factory; ServerOptions + LanguageClientOptions setup per RESEARCH §5.

    binary.ts shape (~60 LOC): three-tier resolution (configured path > bundled > TODO auto-download). Use `vscode.workspace.fs.stat(uri)` to check existence asynchronously (works on virtual file systems, unlike sync fs.existsSync — though sync is acceptable here since we're touching local extension storage).

    package.json updates: keep all existing contributions from Plan 01 unchanged; add the scripts block, dependencies block (vscode-languageclient), devDependencies block. Add scripts.test:unit pointing at the new mocha config that Task 2 will commit. Run `npm install` after editing to populate node_modules — verify install completes.

    Pin policy: every new dep gets an exact version (`^` allowed for major.minor only if the upstream maintains tight SemVer — for safety prefer exact pins on the security-sensitive ones: vscode-languageclient and @vscode/test-electron).

    No status.ts, status_bar.ts, commands.ts, or test files in this task — those come in Task 2.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/vscode-deal && npm install --no-audit --no-fund --silent && npx tsc -p ./ --noEmit && test -f src/extension.ts && test -f src/client.ts && test -f src/binary.ts && grep -q "vscode-languageclient/node" src/client.ts && grep -q "TransportKind.stdio" src/client.ts && grep -q "documentSelector" src/client.ts && grep -q "resolveLspPath" src/binary.ts && node -e "const m=require('./package.json'); if(!m.dependencies['vscode-languageclient']) throw new Error('vscode-languageclient missing from dependencies'); if(!m.devDependencies['@vscode/test-electron']) throw new Error('@vscode/test-electron missing'); if(!m.scripts || !m.scripts['test:unit']) throw new Error('test:unit script missing'); console.log('package.json deps + scripts OK');"</automated>
  </verify>
  <done>
    package.json declares vscode-languageclient + @vscode/test-electron + typescript build scripts (compile, watch, test, test:unit); src/extension.ts activates with binary resolution + LanguageClient start + silent-fallback policy; src/client.ts builds the LanguageClient per RESEARCH §5; src/binary.ts implements three-tier resolution with Plan 06 TODO clearly marked; `tsc -p ./ --noEmit` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement status.ts (pure logic) + status_bar.ts (VS Code wiring) + commands.ts; split status-bar unit test from electron suite (Issue 5)</name>
  <files>
    /Users/dunnock/projects/deal-lang/vscode-deal/src/status.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/src/status_bar.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/src/commands.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/src/test/runTest.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/src/test/suite/index.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/src/test/suite/activation.test.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/src/test/unit/status.test.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/.vscode-test.mjs,
    /Users/dunnock/projects/deal-lang/vscode-deal/.mocharc.unit.json
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/vscode-deal/src/extension.ts (from Task 1),
    /Users/dunnock/projects/deal-lang/vscode-deal/src/client.ts (from Task 1),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (§5 status bar + test runner sections),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-VALIDATION.md (vscode-deal extension tests section)
  </read_first>
  <behavior>
    - status.ts (NEW PER ISSUE 5) defines:
      - `export type LspState = 'stopped' | 'starting' | 'running';` (normalized form of vscode-languageclient's State enum — abstracted so unit tests don't pull in vscode-languageclient/node runtime).
      - `export interface StatusBarView { text: string; tooltip: string }`.
      - `export function updateStatusBar(state: LspState): StatusBarView` — pure function with exhaustive switch:
        - 'stopped'  → { text: "$(error) DEAL LSP",        tooltip: "DEAL LSP error — click for output" }
        - 'starting' → { text: "$(sync~spin) DEAL LSP",    tooltip: "DEAL LSP starting…" }
        - 'running'  → { text: "$(check) DEAL LSP",        tooltip: "DEAL LSP ready" }
      - `export function fromLspState(state: number): LspState` — maps vscode-languageclient `State` enum (1=Stopped, 2=Starting, 3=Running) to the normalized string form. Lives here (not in status_bar.ts) so the State→LspState bridge is also unit-testable, but does NOT import from vscode-languageclient — it accepts the raw numeric value and asserts the range.
      - Zero imports from `vscode` or `vscode-languageclient` in status.ts — this is the key seam that makes the unit test runnable in pure mocha without electron (Issue 5).
    - status_bar.ts (THIN WRAPPER POST ISSUE 5) `export function installStatusBar(client: LanguageClient): vscode.Disposable`:
      - Creates `vscode.window.createStatusBarItem(vscode.StatusBarAlignment.Right, 100)`.
      - item.command = 'deal.showOutput' (clicking opens the output channel — D-40 affordance).
      - Sets initial state by calling `applyView(item, updateStatusBar('starting'))`.
      - Subscribes to `client.onDidChangeState(e => applyView(item, updateStatusBar(fromLspState(e.newState))))` where applyView is a tiny private helper assigning `item.text = view.text; item.tooltip = view.tooltip`.
      - Returns the StatusBarItem (which is itself Disposable).
      - All state→view mapping lives in status.ts; status_bar.ts only owns the VS Code-side I/O.
    - commands.ts `export function registerCommands(context: vscode.ExtensionContext, client: LanguageClient, output: vscode.OutputChannel): void`:
      - Registers `vscode.commands.registerCommand('deal.restartServer', async () => { await client.stop(); await client.start(); })`.
      - Registers `vscode.commands.registerCommand('deal.showOutput', () => { output.show(true); })`.
      - Pushes both disposables to context.subscriptions.
    - .vscode-test.mjs uses the @vscode/test-cli config shape (electron suite only):
      ```js
      import { defineConfig } from '@vscode/test-cli';
      export default defineConfig({
        files: 'out/test/suite/**/*.test.js',
        workspaceFolder: '<absolute path to spec/examples/showcase>',
        mocha: { ui: 'bdd', timeout: 20000 },
      });
      ```
    - .mocharc.unit.json (NEW PER ISSUE 5) configures the pure-mocha unit suite (no electron):
      ```json
      { "spec": "out/test/unit/**/*.test.js", "ui": "bdd", "timeout": 5000, "require": [] }
      ```
    - src/test/runTest.ts is the @vscode/test-electron entrypoint when test-cli is not used (kept for compatibility) — uses `runTests({ extensionDevelopmentPath, extensionTestsPath })`.
    - src/test/suite/index.ts is the Mocha runner glue — registers all .test.js files in the suite directory.
    - activation.test.ts (3 electron tests — `status_bar_transitions` REMOVED from this suite per Issue 5):
      - `activates_on_deal_toml` — opens the showcase workspace; uses `vscode.extensions.getExtension('deal-lang.vscode-deal')` to fetch the extension; asserts `extension.isActive === true` within 10 s of test start.
      - `language_client_connects` — after activation, polls `await extension.activate()` then asserts the LanguageClient reaches State.Running within 5 s. To check state, the test needs access to the client — expose it via the extension's exports: in extension.ts, `return { client }` from activate() so tests can read `extension.exports.client`.
      - `format_on_save_invokes_lsp` — opens a showcase .deal file; makes a whitespace-only edit; saves; asserts the document content matches a known-formatted version (which equals the output of `deal fmt` from Plan 02 / Phase 2 CLI).
    - src/test/unit/status.test.ts (NEW PER ISSUE 5 — runs WITHOUT electron, ~1s feedback):
      - `import { updateStatusBar, fromLspState } from '../../status';` (no vscode/vscode-languageclient imports).
      - `status_bar_transitions` test: for each of 'stopped', 'starting', 'running', call updateStatusBar(state) and assert exact { text, tooltip } values per the D-40 mapping.
      - `fromLspState_maps_enum_correctly` test: assert fromLspState(1) === 'stopped', fromLspState(2) === 'starting', fromLspState(3) === 'running'.
      - Uses chai's `expect` + a plain `describe`/`it` structure. No sinon, no stubs needed — pure-function tests against the extracted state mapping.
    - All 3 electron tests pass via `xvfb-run -a npm test` on Linux or `npm test` on macOS/Windows; the `.vscode-test.mjs` config points workspaceFolder at the showcase.
    - The unit suite passes via `npm run test:unit` with no display server / electron download — total runtime ~1 second on any platform (Issue 5 — per-commit feedback on status-bar logic edits drops from ~30s electron to ~1s).
    - The deal-lsp binary needed by the electron tests is built by Plan 03 (cargo build -p deal-lsp --release); the electron tests resolve it via `deal.lsp.path` set in workspace settings or by symlinking the cargo target into `vscode-deal/server/deal-lsp` for the test run.
  </behavior>
  <action>
    Issue 5 resolution: extract the state-update logic into a pure module `vscode-deal/src/status.ts` that has zero vscode/vscode-languageclient imports. Implement it as two pure functions: `updateStatusBar(state: LspState) → {text, tooltip}` and `fromLspState(n: number) → LspState`.

    status.ts (~40 LOC):
    - Define LspState union type, StatusBarView interface.
    - updateStatusBar uses an exhaustive switch with type-narrowing — TypeScript's noFallthroughCasesInSwitch + the strict union exhaustively forces compile-time completeness.
    - fromLspState handles 1/2/3 with a default that throws (unreachable in practice; vscode-languageclient State enum is closed). Document the mapping in a comment referencing RESEARCH §5 lines 552–567.

    status_bar.ts (~35 LOC, post-extraction):
    - Imports updateStatusBar + fromLspState from './status'.
    - installStatusBar creates the StatusBarItem, attaches command, subscribes to onDidChangeState, and on each state transition applies the view returned by updateStatusBar to the item via a small private `applyView(item, view)` helper.

    commands.ts (~25 LOC):
    - Each command returns a vscode.Disposable; push to context.subscriptions.

    Test harness setup:
    - .vscode-test.mjs at the repo root (electron suite — use @vscode/test-cli's defineConfig).
    - .mocharc.unit.json at the repo root (unit suite — no electron; spec = out/test/unit/**/*.test.js).
    - src/test/runTest.ts as a fallback for @vscode/test-electron direct invocation.
    - src/test/suite/index.ts as the Mocha glue for electron suite (~30 LOC).
    - src/test/suite/activation.test.ts (~70 LOC for 3 tests — status_bar_transitions excluded per Issue 5).
    - src/test/unit/status.test.ts (~30 LOC for 2 tests — pure mocha, no electron).

    Electron test prerequisites:
    - The deal-lsp binary must be built before electron tests run. Add a npm script `pretest: "cd ../deal && cargo build -p deal-lsp --release"` to package.json (or document the requirement in vscode-deal/README.md).
    - The test workspace settings file (`.vscode/settings.json` inside spec/examples/showcase) must set `"deal.lsp.path": "<absolute path to deal/target/release/deal-lsp>"`. Generate this file dynamically in runTest.ts before invoking VS Code so it's not committed.

    Unit test prerequisites: NONE. The unit suite has zero external prerequisites — `npm run test:unit` works on bare CI runners without xvfb, electron download, or deal-lsp binary. This is the entire point of Issue 5's split.

    Extension exports for testability: update extension.ts to return `{ client, output, statusBar }` from activate() so electron tests can introspect the LSP state. This is a standard VS Code extension testing pattern.

    activation.test.ts (electron suite):
    - `activates_on_deal_toml`: vscode.workspace.openWorkspace(showcase URI) is not directly available — the workspace is set via .vscode-test.mjs's workspaceFolder. The test asserts that the showcase workspace is open AND `vscode.extensions.getExtension('deal-lang.vscode-deal').isActive` becomes true after a brief poll.
    - `language_client_connects`: poll for `extension.exports.client.state === State.Running` every 200 ms up to 10 s; assert state == Running at end.
    - `format_on_save_invokes_lsp`: open a showcase .deal file via `vscode.workspace.openTextDocument(uri)` + `vscode.window.showTextDocument(doc)`; introduce a known formatting violation (e.g., extra trailing whitespace); execute `vscode.commands.executeCommand('editor.action.formatDocument')`; assert document.getText() equals the expected formatted output.

    status.test.ts (unit suite — pure mocha):
    - `describe('updateStatusBar', () => { it('starting →', ...); it('running →', ...); it('stopped →', ...); })` — three sub-tests asserting exact text + tooltip per the D-40 mapping.
    - `describe('fromLspState', () => { it('maps 1 → stopped', ...); it('maps 2 → starting', ...); it('maps 3 → running', ...); })` — verifies the numeric enum bridge.
    - Run via `npm run test:unit`. Expected runtime: ~1 second total.

    Ensure all TypeScript compiles to `out/` (matching `tsconfig.json` outDir from Plan 01). Compile via `npm run compile`. Both the electron suite source files (out/test/suite/*) and the unit suite source files (out/test/unit/*) must end up under out/ via the standard tsc rootDir = src layout.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/vscode-deal && npx tsc -p ./ && test -f out/extension.js && test -f out/client.js && test -f out/status.js && test -f out/status_bar.js && test -f out/commands.js && test -f out/test/suite/activation.test.js && test -f out/test/unit/status.test.js && grep -q "updateStatusBar" src/status.ts && grep -q "fromLspState" src/status.ts && ! grep -q "vscode-languageclient" src/status.ts && ! grep -q "from 'vscode'" src/status.ts && grep -q "updateStatusBar" src/status_bar.ts && grep -q "onDidChangeState" src/status_bar.ts && grep -q "deal.restartServer" src/commands.ts && grep -q "deal.showOutput" src/commands.ts && grep -q "activates_on_deal_toml" src/test/suite/activation.test.ts && grep -q "language_client_connects" src/test/suite/activation.test.ts && grep -q "format_on_save_invokes_lsp" src/test/suite/activation.test.ts && ! grep -q "status_bar_transitions" src/test/suite/activation.test.ts && grep -q "status_bar_transitions\|updateStatusBar" src/test/unit/status.test.ts && test -f .mocharc.unit.json && npm run test:unit && echo "unit suite OK"</automated>
    <automated>cd /Users/dunnock/projects/deal-lang/vscode-deal && (cd /Users/dunnock/projects/deal-lang/deal && cargo build -p deal-lsp --release) && (xvfb-run -a npm test 2>/dev/null || npm test) && echo "vscode-deal electron suite OK"</automated>
  </verify>
  <done>
    status.ts is a pure-logic module with zero vscode/vscode-languageclient imports (Issue 5 logic seam); status_bar.ts is a thin VS Code wrapper that imports from status.ts; commands.ts registers restartServer + showOutput; 3 electron tests pass (activation, client-connect, format-on-save); 1 unit test pair passes in pure mocha (updateStatusBar + fromLspState) with ~1s feedback (vs ~30s electron) — Issue 5 resolution; .vscode-test.mjs points electron workspace at the showcase; .mocharc.unit.json drives the unit suite; deal-lsp binary is available to the test run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VS Code extension JS ↔ deal-lsp child process | Spawns deal-lsp as a subprocess with stdio transport — child process inherits extension's user permissions. |
| `deal.lsp.path` setting ↔ extension | User-controlled string used as `child_process` command — could be set to any path on disk. |
| npm install ↔ vscode-deal node_modules | vscode-languageclient + test deps pulled from npm. |
| @vscode/test-electron download ↔ test runner | First test run downloads a VS Code binary from update.code.visualstudio.com. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | Tampering (supply-chain — npm) | vscode-languageclient, @vscode/test-electron, mocha, chai, sinon, eslint, typescript | mitigate | Pin exact versions in package.json; CI runs `npm audit --omit=dev --audit-level=high` (will be added by Plan 06 release workflow). vscode-languageclient is Microsoft official (10+ year history); @vscode/test-electron is Microsoft official. All packages `[ASSUMED]` per RESEARCH §1199 — not `[SUS]` or `[SLOP]`. |
| T-3-04 | Elevation of Privilege (deal.lsp.path user-controlled) | binary.ts resolveLspPath | accept | `deal.lsp.path` is a user-set configuration (VS Code workspace or user settings). The user already controls VS Code execution; pointing at an arbitrary binary on their machine is not a new attack surface — it's their machine, their settings. Document the trust model in vscode-deal/README.md (Plan 06 closeout) so airgapped/security-conscious deployments know to fix `deal.lsp.path` via workspace settings only. |
| T-3-01 | Tampering (binary integrity) | binary.ts bundled-path resolution | mitigate (deferred to Plan 06) | This plan stubs the auto-download path; Plan 06's bootstrap.ts implements SHA-256 verification per D-50 / D-52. For bundled binaries (offline .vsix per D-51), the integrity is anchored at .vsix signing — Marketplace + sigstore. Status: gate is Plan 06's responsibility; this plan documents the seam. |

**T-3-SC supply-chain checkpoint:** Run `slopcheck install vscode-languageclient @vscode/test-electron @vscode/test-cli mocha chai sinon typescript eslint @types/vscode @types/mocha @types/chai @types/sinon @types/node @typescript-eslint/eslint-plugin @typescript-eslint/parser` BEFORE `npm install`. If slopcheck unavailable: each Microsoft-prefixed and well-known package has documented multi-year history per RESEARCH §1199 audit — manually confirm via `npm view <pkg> maintainers` that the publisher is Microsoft, mocha team, or sinonjs.
</threat_model>

<verification>
- `cd /Users/dunnock/projects/deal-lang/vscode-deal && npm install` exits 0.
- `cd /Users/dunnock/projects/deal-lang/vscode-deal && npx tsc -p ./ --noEmit` exits 0.
- `cd /Users/dunnock/projects/deal-lang/vscode-deal && npm run compile` produces out/extension.js + dependencies including out/status.js and out/test/unit/status.test.js.
- `cd /Users/dunnock/projects/deal-lang/vscode-deal && npm run test:unit` exits 0 in ~1 second (pure mocha; no electron).
- `cd /Users/dunnock/projects/deal-lang/deal && cargo build -p deal-lsp --release` exits 0 (electron test prerequisite).
- `cd /Users/dunnock/projects/deal-lang/vscode-deal && (xvfb-run -a npm test 2>/dev/null || npm test)` exits 0 with 3 electron tests passing.
- Grep gates: `grep -c "vscode-languageclient" package.json` ≥ 1; `grep -c "TransportKind.stdio" src/client.ts` ≥ 1; `grep -c "updateStatusBar" src/status.ts` ≥ 1; `grep -c "updateStatusBar" src/status_bar.ts` ≥ 1 (status_bar.ts consumes status.ts); `! grep -q "from 'vscode'" src/status.ts` (Issue 5 seam invariant — status.ts must be pure-logic); `grep -c "deal.restartServer\\|deal.showOutput" src/commands.ts` ≥ 2.
</verification>

<success_criteria>
- REQ-phase-3-4-vscode-lsp LSP-wiring half satisfied: extension activates on deal.toml, launches deal-lsp via stdio, connects LanguageClient, registers status bar + commands.
- D-40 honored: silent TextMate fallback + status-bar state machine (starting → ready → error); no modals on LSP failure.
- Semantic tokens auto-register via vscode-languageclient (no extra wiring needed — Plan 04's server-side declaration drives it).
- Issue 5 resolution: status.ts is a pure-logic module; status_bar_transitions test moved out of @vscode/test-electron suite into pure-mocha unit suite (`npm run test:unit` ~1s); 3 electron tests pass; total feedback budget for status-bar logic edits drops from ~30s (electron) to ~1s (unit suite).
- Issue 4 resolution: wave: 2 (after Plan 03 moved to wave 1); depends_on stays at ["03-01", "03-03"].
- Binary auto-download seam (binary.ts) is stubbed and clearly marked TODO[Plan 06] — Plan 06 replaces the third-tier branch with bootstrap.ts.
- Configurable-path escape hatch (`deal.lsp.path`) honored: user-set path bypasses bundled/downloaded lookup.
</success_criteria>

<output>
Create `.planning/phases/03-editor-intelligence/03-05-SUMMARY.md` on completion with: package.json final dep list, electron test count (3 expected), unit test count (2 expected — updateStatusBar + fromLspState), unit suite wall-clock runtime (Issue 5 — should be ~1s), electron suite wall-clock runtime, Plan 06 TODO surface count in binary.ts, and any test infrastructure deviations (e.g., xvfb required on Linux CI, deal.lsp.path injection via runTest.ts pre-step).
</output>
