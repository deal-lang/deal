---
phase: 03-editor-intelligence
plan: 07
plan_id: 03-07
name: Phase 3 Gate (phase-3-gate + phase-3-gate-fresh) and Closeout
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
  - 03-05
  - 03-06
files_modified:
  - /Users/dunnock/projects/deal-lang/deal/build.zig
  - /Users/dunnock/projects/deal-lang/deal/scripts/phase-3-smoke.sh
  - /Users/dunnock/projects/deal-lang/deal/lsp-smoke/Cargo.toml
  - /Users/dunnock/projects/deal-lang/deal/lsp-smoke/src/main.rs
  - /Users/dunnock/projects/deal-lang/deal/Cargo.toml
  - /Users/dunnock/projects/deal-lang/deal/.planning/REQUIREMENTS.md
  - /Users/dunnock/projects/deal-lang/deal/.planning/STATE.md
  - /Users/dunnock/projects/deal-lang/deal/.planning/ROADMAP.md
  - /Users/dunnock/projects/deal-lang/lsp/README.md
  - /Users/dunnock/projects/deal-lang/deal/.github/workflows/release.yml
autonomous: true
requirements:
  - REQ-phase-3-gate
  - REQ-phase-3-editor-intelligence
must_haves:
  truths:
    - "`zig build phase-3-gate` exits 0 inside a clean worktree: runs phase-2-gate (regression) + cargo build --workspace + cargo test --workspace + tree-sitter test + xvfb-run npm test (vscode-deal) + vsce package (sanity) + phase-3-smoke.sh."
    - "`zig build phase-3-gate-fresh` exits 0 inside an ephemeral worktree created by scripts/verify-fresh-worktree.sh (ADR-phase-1.5-fresh-worktree-verification binding invariant)."
    - "phase-3-smoke.sh spawns deal-lsp, opens the 19-file showcase, exercises the 5 capabilities + semantic tokens, and asserts golden outputs match. Per RESEARCH section 13."
    - "lsp-smoke harness binary (deal/lsp-smoke/) implements the smoke checks; the script wraps cargo run."
    - "REQUIREMENTS.md DEFER-textmate-vs-treesitter-vscode entry is updated to resolved citing D-38."
    - "STATE.md is updated: Phase 3 COMPLETE; carries forward all D-codes through D-53."
    - "ROADMAP.md Phase 3 checkbox is marked [x]; success criteria 1-5 all verified."
    - "/Users/dunnock/projects/deal-lang/lsp/README.md is rewritten to point at deal/lsp/README.md (the new Rust LSP) -- the stale TypeScript design is retired (CONTEXT.md deferred ideas line 200)."
    - "release.yml's `zig build phase-3-gate-fresh` step flips from continue-on-error: true to required (since the gate now exists)."
  artifacts:
    - path: deal/build.zig
      provides: phase-3-gate and phase-3-gate-fresh step definitions (verbatim mirror of phase-2-gate lines 264-287)
      contains: "phase-3-gate"
    - path: deal/scripts/phase-3-smoke.sh
      provides: end-to-end smoke script invoked by phase-3-gate step 8
      contains: "deal-lsp"
    - path: deal/lsp-smoke/src/main.rs
      provides: dev-binary that performs the 5-capability smoke against the showcase
    - path: lsp/README.md (top-level — STALE TS scaffold being retired)
      provides: updated to redirect to deal/lsp/README.md (Plan 03 created the canonical README)
  key_links:
    - from: deal/build.zig
      to: deal/scripts/phase-3-smoke.sh
      via: phase-3-gate dependency chain step 8 (per RESEARCH section 13 line 965)
      pattern: "phase-3-smoke.sh"
    - from: deal/build.zig
      to: deal/scripts/verify-fresh-worktree.sh
      via: phase-3-gate-fresh wraps the script with argument "phase-3-gate" (RESEARCH section 13 lines 989-993)
      pattern: "verify-fresh-worktree.sh"
    - from: deal/.planning/REQUIREMENTS.md
      to: D-38 (CONTEXT.md)
      via: DEFER-textmate-vs-treesitter-vscode resolution citing the locked decision
      pattern: "D-38"
---

<objective>
Close out Phase 3 by adding the `phase-3-gate` and `phase-3-gate-fresh` build steps to `deal/build.zig`, the `phase-3-smoke.sh` integration script + `lsp-smoke` dev-binary harness that exercises the 5 capabilities + semantic tokens against the showcase, and the documentation closeouts (REQUIREMENTS.md DEFER resolution, STATE.md + ROADMAP.md updates, stale `lsp/README.md` retirement, release.yml gate-required flip).

Purpose: Deliver REQ-phase-3-gate and close REQ-phase-3-editor-intelligence (the umbrella). After this plan ships, `zig build phase-3-gate` and `zig build phase-3-gate-fresh` both exit 0; Phase 3 is shippable.

Output: The complete Phase 3 acceptance surface -- gate steps, smoke harness, governance updates -- ready for `/gsd:verify-phase 3` to certify completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dunnock/projects/deal-lang/deal/.planning/PROJECT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/ROADMAP.md
@/Users/dunnock/projects/deal-lang/deal/.planning/STATE.md
@/Users/dunnock/projects/deal-lang/deal/.planning/REQUIREMENTS.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md
@/Users/dunnock/projects/deal-lang/deal/.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md
@/Users/dunnock/projects/deal-lang/deal/build.zig
@/Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh
@/Users/dunnock/projects/deal-lang/lsp/README.md
@/Users/dunnock/projects/deal-lang/deal/.github/workflows/release.yml

<interfaces>
RESEARCH section 13 -- phase-3-gate step list (consume verbatim from RESEARCH lines 955-966):

```
const phase_3_gate = b.step("phase-3-gate", "Phase 3 acceptance gate");
phase_3_gate.dependOn(&b.addRunArtifact(zig_test_runner).step);                                                                   // (1) Zig core
phase_3_gate.dependOn(b.step("phase-2-gate", ""));                                                                                // (2) Phase 2 regression
phase_3_gate.dependOn(&b.addSystemCommand(&.{ "cargo", "build", "--workspace", "--release", "--manifest-path", "Cargo.toml" }).step);  // (3) Rust build
phase_3_gate.dependOn(&b.addSystemCommand(&.{ "cargo", "test", "--workspace", "--manifest-path", "Cargo.toml" }).step);                  // (4) Rust tests
phase_3_gate.dependOn(&b.addSystemCommand(&.{ "bash", "-c", "cd ../tree-sitter-deal && npm ci && npm test" }).step);                     // (5) tree-sitter corpus
phase_3_gate.dependOn(&b.addSystemCommand(&.{ "bash", "-c", "cd ../vscode-deal && npm ci && xvfb-run -a npm test" }).step);             // (6) vscode-deal extension tests
phase_3_gate.dependOn(&b.addSystemCommand(&.{ "bash", "-c", "cd ../vscode-deal && npm run package" }).step);                            // (7) .vsix sanity build
phase_3_gate.dependOn(&b.addSystemCommand(&.{ "bash", "scripts/phase-3-smoke.sh" }).step);                                              // (8) integration smoke
```

phase-2-gate pattern from existing deal/build.zig (verbatim mirror per PATTERNS.md line 101):

```
const phase_2_gate = b.step("phase-2-gate", "...");
phase_2_gate.dependOn(...);
const phase_2_gate_fresh = b.step("phase-2-gate-fresh", "...");
phase_2_gate_fresh.dependOn(&b.addSystemCommand(&.{ "bash", "scripts/verify-fresh-worktree.sh", "phase-2-gate" }).step);
```

phase-3-gate-fresh:
```
const phase_3_gate_fresh = b.step("phase-3-gate-fresh", "Phase 3 gate inside fresh worktree");
phase_3_gate_fresh.dependOn(&b.addSystemCommand(&.{ "bash", "scripts/verify-fresh-worktree.sh", "phase-3-gate" }).step);
```

verify-fresh-worktree.sh contract (PATTERNS.md line 104 -- already generic):
- argv[1] = gate name (e.g., "phase-3-gate")
- Creates ephemeral git worktree at a temp path.
- Runs `git submodule update --init --recursive` (mounts spec/ -> tests/showcase symlink works -- Phase 1.5 Plan 05).
- Runs `zig build $argv[1]` inside the ephemeral worktree.
- Cleans up.

phase-3-smoke.sh contract (per RESEARCH section 13 lines 968-987):
- set -euo pipefail.
- cd to deal/.
- cargo build --release -p deal-lsp.
- cargo run --release -p deal-lsp-smoke -- tests/showcase.

lsp-smoke binary contract:
- Spawns deal-lsp as a subprocess over stdio (or in-process via LspService).
- Sends initialize + initialized; waits for index population.
- did_open each .deal/.dealx in tests/showcase.
- Sends textDocument/completion at a known position; asserts candidates contain element keywords.
- Sends textDocument/definition on a <<specializes>> reference; asserts location.
- Sends textDocument/hover on a declaration with /** */; asserts markdown payload.
- Sends textDocument/formatting; asserts byte-match against golden.
- Sends textDocument/semanticTokens/full; asserts encoded data matches golden.
- Exit 0 on full pass.

REQUIREMENTS.md DEFER section -- update entry from CONTEXT.md line 81:
- Original: "DEFER-textmate-vs-treesitter-vscode *(evaluated at Phase 3 gate)*"
- Updated to resolved entry: "DEFER-textmate-vs-treesitter-vscode -- RESOLVED 2026-05-22 by Phase 3 D-38 (reject experimental tree-sitter; ship TextMate + LSP semantic tokens)."

Stale top-level lsp/README.md (per CONTEXT.md line 115 + line 200) -- rewrite to a 5-line redirect pointing at /Users/dunnock/projects/deal-lang/deal/lsp/README.md (the Rust LSP -- Plan 03's canonical doc).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add phase-3-gate and phase-3-gate-fresh to deal/build.zig; create lsp-smoke binary + scripts/phase-3-smoke.sh</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/build.zig,
    /Users/dunnock/projects/deal-lang/deal/scripts/phase-3-smoke.sh,
    /Users/dunnock/projects/deal-lang/deal/lsp-smoke/Cargo.toml,
    /Users/dunnock/projects/deal-lang/deal/lsp-smoke/src/main.rs,
    /Users/dunnock/projects/deal-lang/deal/Cargo.toml
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/deal/build.zig (existing phase-2-gate block; pattern source),
    /Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh (generic argv[1]=gate-name contract),
    /Users/dunnock/projects/deal-lang/deal/Cargo.toml (workspace members; add lsp-smoke),
    /Users/dunnock/projects/deal-lang/deal/lsp/tests/showcase.rs (8 integration tests from Plans 03+04 -- smoke harness adapts the same patterns),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (section 13 phase-3-gate design),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md (Plan 07 file table -- build.zig and lsp-smoke entries),
    /Users/dunnock/projects/deal-lang/deal/.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md (the binding ADR)
  </read_first>
  <behavior>
    - deal/build.zig gains two new step blocks appended near the existing phase-2-gate block (around line 287):
      1. phase-3-gate: 8 dependencies per RESEARCH section 13 (Zig core, phase-2-gate, cargo build, cargo test, tree-sitter test, vscode-deal test, vscode-deal package, phase-3-smoke).
      2. phase-3-gate-fresh: single dependency on `bash scripts/verify-fresh-worktree.sh phase-3-gate`.
    - The phase-3-gate dependency on `phase-2-gate` ensures regression coverage (per ADR-phase-1.5-fresh-worktree-verification binding invariant).
    - The xvfb-run prefix in step (6) handles headless VS Code testing on Linux CI; on macOS/Windows the xvfb-run binary is absent -- write the command as `xvfb-run -a npm test 2>/dev/null || npm test` to fall back gracefully.
    - phase-3-smoke.sh is an executable bash script (chmod +x) with set -euo pipefail. Resolves the deal/ root via `cd "$(dirname "$0")/.."`. Runs cargo build --release -p deal-lsp, then cargo run --release -p lsp-smoke -- tests/showcase.
    - lsp-smoke is a new Cargo workspace member -- add "lsp-smoke" to deal/Cargo.toml [workspace] members array. The crate is publish = false.
    - lsp-smoke/Cargo.toml: name "lsp-smoke", version "0.3.0", edition "2021", publish = false; [[bin]] name "deal-lsp-smoke" path "src/main.rs"; [dependencies] tower-lsp = { workspace = true }, tokio = { workspace = true }, deal-ffi = { path = "../deal-ffi" }, serde = { workspace = true }, serde_json = { workspace = true }, anyhow = { workspace = true }; [dev-dependencies] none.
    - lsp-smoke/src/main.rs implements the 5-capability smoke via tokio::main:
      1. Read argv[1] as the workspace path (showcase).
      2. Spawn deal-lsp as a tokio process via tokio::process::Command -- spawn the binary from `deal/target/release/deal-lsp` over stdio.
      3. Wrap stdin/stdout in tower_lsp::Client / Server channels (or use a manual JSON-RPC framing if tower-lsp's client API is not directly usable -- in that case construct + send JSON-RPC envelopes via tokio::io::AsyncWriteExt with the LSP \r\n\r\nContent-Length framing).
      4. Send initialize with workspace_folders = [showcase]; await response.
      5. Send initialized.
      6. Wait for the index to populate (poll for 2s or send workspace/symbol query when implemented; for Phase 3, wait a fixed 2s as a smoke).
      7. did_open every .deal and .dealx under the showcase path (find via walkdir).
      8. For each of the 5 capabilities, exercise once on a known showcase position. Hard-code positions for one canonical file (e.g., spec/examples/showcase/packages/vehicle/electrical/BatteryPack.deal).
      9. Compare semantic-tokens output against a golden file at deal/lsp-smoke/golden/semantic_tokens.json (or reuse the Plan 04 golden file path).
      10. Print "PHASE-3-SMOKE: PASS" + exit 0 on success; print specific failure messages + exit non-zero on any check.
    - The smoke harness is intentionally informal -- it's a regression-style end-to-end check that the LSP plumbing connects. The detailed assertions live in deal/lsp/tests/showcase.rs (8 cargo tests). The smoke is the "everything works together" check.
    - Workflow integration: release.yml's `zig build phase-3-gate-fresh` step (linux-x64-gnu row) flips from continue-on-error: true to the default (required). Edit Task 2 of Plan 06's release.yml file accordingly.
  </behavior>
  <action>
    Append the phase-3-gate + phase-3-gate-fresh blocks to deal/build.zig immediately after the existing phase-2-gate-fresh block (around line 287, per PATTERNS.md line 101).

    Use the RESEARCH section 13 verbatim shape, adapting:
    - The `phase-2-gate` dependency: in build.zig, the existing phase-2-gate step is constructed via `b.step("phase-2-gate", ...)`. Re-fetch it by name (or by saving the variable when phase-2-gate is constructed). The RESEARCH snippet uses `b.step("phase-2-gate", "")` as a fetcher -- verify this is the correct API for the Zig version in use; if not, switch to a direct dependency on the captured `phase_2_gate` variable.
    - The bash -c invocations resolve cd'ing to ../tree-sitter-deal and ../vscode-deal because the workflow runs from deal/ root.

    phase-3-gate-fresh is a one-line dependency on `bash scripts/verify-fresh-worktree.sh phase-3-gate` -- per PATTERNS.md the verify-fresh-worktree.sh contract is already generic over the gate-name argv[1].

    Create deal/scripts/phase-3-smoke.sh:
    - Shebang #!/usr/bin/env bash, set -euo pipefail.
    - cd to the deal/ root.
    - Build: cargo build --release -p deal-lsp; cargo build --release -p lsp-smoke.
    - Run: cargo run --release -p lsp-smoke -- tests/showcase.
    - chmod +x via `git update-index --chmod=+x` (or rely on git config core.fileMode; document either way in the script header).

    Create deal/lsp-smoke/ as a new workspace member:
    - Cargo.toml: name "lsp-smoke", version "0.3.0", edition "2021", publish = false; [[bin]] name "deal-lsp-smoke"; deps as listed.
    - src/main.rs (~150-200 LOC): implement the spawn + LSP envelope exchange + 5-capability checks.

    For the JSON-RPC framing: implement a minimal AsyncRead/AsyncWrite-based framer manually if tower-lsp's client API does not directly support outgoing requests in this configuration. The LSP framing is Content-Length-prefixed (e.g., "Content-Length: 123\r\n\r\n{json}"). Use serde_json to (de)serialize the bodies.

    For golden comparison: read deal/lsp/tests/golden/semantic_tokens.json (Plan 04's golden file) and assert the encoded data matches. If lsp-smoke's golden lookup is awkward, copy the golden into deal/lsp-smoke/golden/ as a sibling -- duplication is acceptable for the smoke.

    Update deal/Cargo.toml workspace members: extend to ["cli", "lsp", "deal-ffi", "lsp-smoke", "tests/ffi"] (insert lsp-smoke between deal-ffi and tests/ffi alphabetically).

    Also in this task: edit deal/.github/workflows/release.yml (created by Plan 06) to flip the `zig build phase-3-gate-fresh` step's continue-on-error: true line to be removed (so the step is required). Add a comment `# phase-3-gate-fresh required as of Plan 07`.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && grep -q 'phase-3-gate' build.zig && grep -q 'phase-3-gate-fresh' build.zig && grep -q '"phase-2-gate"' build.zig && grep -q 'verify-fresh-worktree.sh' build.zig && test -x scripts/phase-3-smoke.sh && test -f lsp-smoke/Cargo.toml && test -f lsp-smoke/src/main.rs && grep -q '"lsp-smoke"' Cargo.toml && cargo build --release -p lsp-smoke && cargo build --release -p deal-lsp && bash scripts/phase-3-smoke.sh && ! grep -A 2 'phase-3-gate-fresh' .github/workflows/release.yml | grep -q 'continue-on-error: true' && echo "phase-3-gate plumbing OK"</automated>
  </verify>
  <done>
    deal/build.zig has phase-3-gate (8 deps) + phase-3-gate-fresh (1 dep on verify-fresh-worktree.sh); phase-3-smoke.sh + lsp-smoke binary build and run successfully against the showcase; release.yml's phase-3-gate-fresh step is required (not continue-on-error).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run phase-3-gate end-to-end; update REQUIREMENTS.md / STATE.md / ROADMAP.md; retire stale lsp/README.md</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/.planning/REQUIREMENTS.md,
    /Users/dunnock/projects/deal-lang/deal/.planning/STATE.md,
    /Users/dunnock/projects/deal-lang/deal/.planning/ROADMAP.md,
    /Users/dunnock/projects/deal-lang/lsp/README.md
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/deal/build.zig (after Task 1),
    /Users/dunnock/projects/deal-lang/deal/.planning/REQUIREMENTS.md (find DEFER-textmate-vs-treesitter-vscode entry),
    /Users/dunnock/projects/deal-lang/deal/.planning/STATE.md (find Phase 3 status entry),
    /Users/dunnock/projects/deal-lang/deal/.planning/ROADMAP.md (find Phase 3 checkbox + success criteria),
    /Users/dunnock/projects/deal-lang/lsp/README.md (stale TS scaffold -- to be retired),
    /Users/dunnock/projects/deal-lang/deal/lsp/README.md (Plan 03 created the canonical README -- redirect target),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md (deferred ideas section line 200)
  </read_first>
  <behavior>
    - First, run `cd /Users/dunnock/projects/deal-lang/deal && zig build phase-3-gate` end-to-end and capture the output. The gate MUST exit 0; if any step fails, fix it before proceeding (escalate to a follow-up Plan if structural issues emerge).
    - Then run `cd /Users/dunnock/projects/deal-lang/deal && zig build phase-3-gate-fresh`. Same requirement: exit 0. The fresh-worktree gate creates an ephemeral worktree via scripts/verify-fresh-worktree.sh, runs `git submodule update --init --recursive` to mount spec/, then runs phase-3-gate. This is the binding ADR-phase-1.5 invariant.
    - REQUIREMENTS.md updates:
      1. Find the DEFER-textmate-vs-treesitter-vscode entry (CONTEXT.md line 81 reference).
      2. Replace the line `- **DEFER-textmate-vs-treesitter-vscode** *(evaluated at Phase 3 gate)* — Whether VS Code's experimental tree-sitter support is stable enough to replace TextMate in the VS Code extension by Phase 3.` with `- **DEFER-textmate-vs-treesitter-vscode** -- RESOLVED 2026-05-22 by D-38 (Phase 3 ships TextMate + LSP semantic tokens; experimental tree-sitter rejected -- redundant with tree-sitter-deal package and would create dual parser sources of truth).`
      3. Find the Phase 3 requirements table at the bottom of REQUIREMENTS.md (lines listing REQ-phase-3-* entries with "Pending" status).
      4. Update each row's status to "Completed":
        - REQ-phase-3-editor-intelligence: Completed
        - REQ-phase-3-1-textmate: Completed
        - REQ-phase-3-2-treesitter: Completed
        - REQ-phase-3-3-lsp-server: Completed
        - REQ-phase-3-4-vscode-lsp: Completed
        - REQ-phase-3-gate: Completed
    - STATE.md updates:
      1. Find the section recording phase progression (carries forward from Phase 2 closeout).
      2. Append a new section: "Phase 3 COMPLETE 2026-05-22; 7/7 plans complete; carries forward D-codes D-01..D-53." With a brief 5-7 line summary of what shipped (TextMate + tree-sitter + deal-lsp Rust crate with 5 capabilities + semantic tokens + vscode-deal LanguageClient + release pipeline + phase-3-gate + phase-3-gate-fresh).
      3. Update the project HEAD commit reference if STATE.md uses one (some projects pin the gate-passing HEAD).
    - ROADMAP.md updates:
      1. Find `- [ ] **Phase 3: Editor Intelligence** -- ...` in the phases list (around line 32 from the grep output).
      2. Replace `- [ ]` with `- [x]` and append " (completed 2026-05-22)".
      3. Find the "Phase 3: Editor Intelligence" detailed section and append, after the Success Criteria list, a "Status: COMPLETE 2026-05-22" line.
    - /Users/dunnock/projects/deal-lang/lsp/README.md (stale TypeScript LSP scaffold) is REPLACED with a short redirect document:
      ```
      # DEAL LSP

      This directory is retained as a historical scaffold from before Phase 3.
      The active DEAL Language Server is implemented in Rust at:

      - Source: /Users/dunnock/projects/deal-lang/deal/lsp/
      - Documentation: /Users/dunnock/projects/deal-lang/deal/lsp/README.md
      - Architecture: tower-lsp framework, consumes the Zig core via the 9-export C ABI.

      The TypeScript LSP design previously documented here is superseded by D-38 (locked Phase 3 decision: Rust + tower-lsp + TextMate + LSP semantic tokens; experimental VS Code tree-sitter rejected).
      ```
    - Cross-reference closure: all 6 REQ IDs in plan frontmatter `requirements` arrays across Plans 01-07 must appear at least once with Completed status in REQUIREMENTS.md.
  </behavior>
  <action>
    Execute the closeout in order:

    Step A: Run the gates end-to-end.
    - `cd /Users/dunnock/projects/deal-lang/deal && zig build phase-3-gate` and verify exit 0.
    - `cd /Users/dunnock/projects/deal-lang/deal && zig build phase-3-gate-fresh` and verify exit 0.
    - If either fails, do not proceed to governance updates -- escalate via a follow-up plan or fix the underlying issue.

    Step B: REQUIREMENTS.md updates.
    - Use the Edit tool to replace the DEFER-textmate-vs-treesitter-vscode line with the resolved version above. Cite D-38 verbatim.
    - Use the Edit tool to flip each of the 6 Phase 3 REQ rows from "Pending" to "Completed" in the requirements table.

    Step C: STATE.md updates.
    - Append the Phase 3 completion section. Mirror the format used for Phase 2's completion entry (look at the existing Phase 2 COMPLETE line in STATE.md for shape).

    Step D: ROADMAP.md updates.
    - Flip the Phase 3 checkbox to `[x]` with the completion date.
    - Append the Status: COMPLETE line to the Phase 3 detail section.

    Step E: Retire the stale lsp/README.md.
    - Replace its contents with the redirect document above. This is destructive of the existing scaffold's TypeScript design -- per CONTEXT.md line 200, that's intentional.

    Step F: Final verification.
    - Re-run `cd deal && zig build phase-3-gate` after the doc edits to confirm no doc-related test caught the changes.
    - Run `git status` to confirm all expected files are modified; commit via the standard execute-phase workflow.

    No code changes in this task -- only documentation + gate execution.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && zig build phase-3-gate && zig build phase-3-gate-fresh && grep -q 'DEFER-textmate-vs-treesitter-vscode.*RESOLVED' .planning/REQUIREMENTS.md && grep -q 'D-38' .planning/REQUIREMENTS.md && grep -c 'REQ-phase-3.*Completed' .planning/REQUIREMENTS.md | awk '{ if ($1 < 6) { print "Expected 6 completed REQ rows; got " $1; exit 1 } else print "REQ rows OK: " $1 }' && grep -q 'Phase 3 COMPLETE 2026-05-22' .planning/STATE.md && grep -q '- \[x\] \*\*Phase 3' .planning/ROADMAP.md && grep -q 'deal/lsp/README.md' /Users/dunnock/projects/deal-lang/lsp/README.md && ! grep -q 'TypeScript LSP' /Users/dunnock/projects/deal-lang/lsp/README.md && echo "Phase 3 closeout OK"</automated>
  </verify>
  <done>
    `zig build phase-3-gate` and `zig build phase-3-gate-fresh` both exit 0; REQUIREMENTS.md DEFER entry resolved citing D-38; 6 Phase 3 REQ rows marked Completed; STATE.md records Phase 3 COMPLETE 2026-05-22; ROADMAP.md checkbox flipped to [x]; stale lsp/README.md replaced with redirect to deal/lsp/README.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deal/build.zig ↔ shell commands | The phase-3-gate step shells out to bash for tree-sitter test + vscode-deal test + smoke; the commands are hardcoded (no user-supplied strings) -- shell-injection surface is zero. |
| scripts/verify-fresh-worktree.sh ↔ git | The fresh-worktree gate creates a temp worktree + clones submodules; assumes git is trusted on the build host. |
| Documentation updates ↔ governance | REQUIREMENTS.md / STATE.md / ROADMAP.md edits change the project's verifiable status; an attacker editing these could mark a non-shipping phase as shipped, but the gate-execution requirement (Task 2 Step A) prevents this -- the docs are only updated after the gate exits 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-SC | Tampering (supply-chain via gate execution) | phase-3-gate shells out to npm + cargo + vsce | mitigate | All transitive supply-chain risks are inherited from Plans 01-06 (each plan's threat model addresses its own packages). This plan adds no new packages -- only the lsp-smoke crate uses workspace-only deps (tower-lsp, tokio, deal-ffi, serde, serde_json, anyhow -- all already pinned in Plan 03). |
| T-3-07 | Tampering (false gate-pass) | phase-3-gate / phase-3-gate-fresh execution | mitigate | The gate is the trust anchor -- documentation updates in Task 2 are conditional on Task 2 Step A's gate execution exiting 0. If the gate passes, the documentation reflects reality. The fresh-worktree variant (binding ADR-phase-1.5-fresh-worktree-verification) prevents dev-local untracked-file masking (the same failure mode that previously caused a false-GREEN in Phase 1.5). |
</threat_model>

<verification>
- `cd /Users/dunnock/projects/deal-lang/deal && zig build phase-3-gate` exits 0.
- `cd /Users/dunnock/projects/deal-lang/deal && zig build phase-3-gate-fresh` exits 0.
- `cd /Users/dunnock/projects/deal-lang/deal && cargo build --release -p lsp-smoke && cargo run --release -p lsp-smoke -- tests/showcase` exits 0.
- `grep -c "Completed" /Users/dunnock/projects/deal-lang/deal/.planning/REQUIREMENTS.md` increases by 6.
- `grep -c "RESOLVED" /Users/dunnock/projects/deal-lang/deal/.planning/REQUIREMENTS.md` increases by 1 (DEFER-textmate-vs-treesitter-vscode).
- `grep -c "Phase 3 COMPLETE" /Users/dunnock/projects/deal-lang/deal/.planning/STATE.md` >= 1.
- `grep -c "- \[x\] \*\*Phase 3" /Users/dunnock/projects/deal-lang/deal/.planning/ROADMAP.md` >= 1.
- `grep -c "TypeScript LSP" /Users/dunnock/projects/deal-lang/lsp/README.md` == 0 (the stale design wording is removed).
- `git status` after the final commit is clean.
</verification>

<success_criteria>
- REQ-phase-3-gate satisfied: phase-3-gate + phase-3-gate-fresh both exit 0.
- REQ-phase-3-editor-intelligence (umbrella) satisfied: all 5 ROADMAP success criteria verified by the gate steps + the underlying Plan-01..06 deliverables.
- ADR-phase-1.5-fresh-worktree-verification honored: phase-3-gate-fresh executes inside an ephemeral worktree per the binding invariant.
- DEFER-textmate-vs-treesitter-vscode resolved in REQUIREMENTS.md citing D-38.
- Stale /Users/dunnock/projects/deal-lang/lsp/README.md retired -- now a redirect to deal/lsp/README.md.
- All governance docs (STATE.md, ROADMAP.md, REQUIREMENTS.md) reflect Phase 3 COMPLETE.
- release.yml's phase-3-gate-fresh step is required (continue-on-error removed).
- Phase 3 is shippable; /gsd:verify-phase 3 can certify completion.
</success_criteria>

<output>
Create `.planning/phases/03-editor-intelligence/03-07-SUMMARY.md` on completion with: phase-3-gate runtime (wall-clock), phase-3-gate-fresh runtime, lsp-smoke pass/fail breakdown by capability, total Phase 3 plans (7), total Phase 3 REQ IDs covered (6), total D-codes inherited (D-01..D-53 = 53), governance update file count (4: REQUIREMENTS.md, STATE.md, ROADMAP.md, lsp/README.md).
</output>
