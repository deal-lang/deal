---
phase: 03-editor-intelligence
plan: 06
plan_id: 03-06
name: Binary Distribution and Release Pipeline
type: execute
wave: 3
depends_on:
  - 03-03
  - 03-04
  - 03-05
files_modified:
  - /Users/dunnock/projects/deal-lang/deal/.github/workflows/release.yml
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/bootstrap.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/sha256.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/binary.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/src/test/suite/auto_download.test.ts
  - /Users/dunnock/projects/deal-lang/vscode-deal/scripts/patch-bootstrap-sha.js
  - /Users/dunnock/projects/deal-lang/vscode-deal/package.json
  - /Users/dunnock/projects/deal-lang/deal/SHA256SUMS.template
autonomous: false
user_setup:
  - service: github-actions-secrets
    why: Marketplace publishing step requires a VS Code Marketplace Personal Access Token (vsce PAT) stored as GitHub secret VSCE_PAT
    env_vars:
      - name: VSCE_PAT
        source: "Azure DevOps user settings -> Personal access tokens -> New (scope Marketplace > Publish)"
    dashboard_config:
      - task: "Add VSCE_PAT secret"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret"
      - task: "Create marketplace environment with required reviewer (the user)"
        location: "GitHub repo -> Settings -> Environments -> New environment marketplace -> Required reviewers"
requirements:
  - REQ-phase-3-4-vscode-lsp
must_haves:
  truths:
    - ".github/workflows/release.yml has 5 jobs: build-libdeal (cross-compile via Zig on linux-x64), build-deal-lsp (native matrix of 4 OS runners), package-vsix (one main .vsix + 4 offline .vsix), publish-release (GH Releases), publish-marketplace (manual-approval gated). Per RESEARCH section 12."
    - "build-libdeal job cross-compiles libdeal.a for aarch64-macos, x86_64-macos, x86_64-linux-gnu, x86_64-linux-musl, x86_64-windows via Zig 0.16+ from one Linux runner (D-53)."
    - "build-deal-lsp matrix links each cross-built libdeal.a into deal-lsp on the corresponding native runner (4 OS targets: darwin-arm64, darwin-x64, linux-x64-gnu, win-x64; linux-x64-musl runs on the linux-x64-gnu runner with a separate Rust target)."
    - "package-vsix produces one Marketplace .vsix (no bundled binary, auto-download per D-50) + 4 offline .vsix variants (one per platform with bundled binary per D-51)."
    - "publish-release uploads all artifacts to GitHub Releases AND generates a SHA256SUMS file containing the per-platform deal-lsp binary hashes."
    - "publish-marketplace job is gated behind a manual-approval marketplace GitHub environment; runs vsce publish for the main and offline .vsix variants."
    - "bootstrap.ts implements the D-50/D-52 auto-download path: pin URL with embedded version, fetch with progress, verify SHA-256 against compiled-in manifest, chmod +x on Unix, cache under context.globalStorageUri/v{X}/."
    - "bootstrap.ts is consumed by binary.ts as the third-tier (post-bundled, post-configured) resolution path, replacing the Plan-05 TODO."
    - "auto_download.test.ts mocks the HTTP layer + filesystem and verifies: correct URL contains the pinned version + platform triple; SHA-256 mismatch causes the cached file to be deleted + an error to be thrown."
    - "Pinned deal-lsp version constant in bootstrap.ts matches the vscode-deal version (D-52 lockstep)."
  artifacts:
    - path: deal/.github/workflows/release.yml
      provides: 5-job GitHub Actions release pipeline
      contains: "jobs:"
    - path: vscode-deal/src/bootstrap.ts
      provides: rust-analyzer-paraphrase auto-download with SHA-256 verification + first-run dialog + progress UI
      contains: "DEAL_LSP_VERSION"
    - path: vscode-deal/src/sha256.ts
      provides: streaming SHA-256 helper (Node crypto createHash)
      contains: "createHash"
    - path: vscode-deal/src/binary.ts
      provides: updated to call bootstrap.ensureDealLspBinary in the third-tier slot
    - path: vscode-deal/scripts/patch-bootstrap-sha.js
      provides: node script invoked by package-vsix job to substitute real sha256 values into bootstrap.ts SHA256_MANIFEST
    - path: deal/SHA256SUMS.template
      provides: template structure for the SHA256SUMS file generated by publish-release
  key_links:
    - from: vscode-deal/src/binary.ts
      to: vscode-deal/src/bootstrap.ts
      via: import + invoke at third-tier resolution
      pattern: "ensureDealLspBinary"
    - from: vscode-deal/src/bootstrap.ts
      to: GitHub Releases URL
      via: pinned URL github.com/deal-lang/deal/releases/download/v{X}/deal-lsp-{triple}.tar.gz
      pattern: "releases/download"
    - from: deal/.github/workflows/release.yml
      to: vscode-deal/.vsix artifacts
      via: package-vsix job builds main + 4 offline .vsix variants
      pattern: "vsce package"
---

<objective>
Build the complete release pipeline for `deal-lsp` and `vscode-deal`. After this plan ships, a tag push `v0.3.0` triggers a 5-job GitHub Actions workflow that cross-compiles `libdeal.a` for all 4 supported targets via Zig (D-53), links `deal-lsp` natively on each target's runner, packages a main `.vsix` (Marketplace, auto-download path) plus 4 per-platform offline `.vsix` variants (airgap path per D-51), uploads to GitHub Releases with a SHA-256 manifest, and waits for manual approval before publishing to the VS Code Marketplace. The extension's `bootstrap.ts` implements the D-50/D-52 auto-download with checksum verification.

Purpose: Complete REQ-phase-3-4-vscode-lsp (binary distribution half). Lock D-50..D-53 into shipping infrastructure. Plan 07's phase gate verifies the pipeline by dry-running it in a fresh worktree.

Output: A CI workflow that produces release artifacts on tag push and a vscode-deal extension that auto-downloads + verifies the matching deal-lsp binary on first activation. The Marketplace publish step requires a one-time `VSCE_PAT` secret + `marketplace` environment configuration (recorded in `user_setup`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dunnock/projects/deal-lang/deal/.planning/PROJECT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md
@/Users/dunnock/projects/deal-lang/vscode-deal/src/binary.ts
@/Users/dunnock/projects/deal-lang/vscode-deal/src/extension.ts
@/Users/dunnock/projects/deal-lang/vscode-deal/package.json
@/Users/dunnock/projects/deal-lang/deal/build.zig
@/Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh

<interfaces>
RESEARCH section 12 -- 5-job CI pipeline shape (consume verbatim, adapt to repo layout):

Job 1 build-libdeal: ubuntu-22.04 with mlugg/setup-zig (zig 0.16.0); strategy matrix targets [aarch64-macos, x86_64-macos, x86_64-linux-gnu, x86_64-linux-musl, x86_64-windows]; runs `cd deal && zig build -Doptimize=ReleaseSafe -Dtarget=$matrix-target`; uploads libdeal.a as artifact.
Job 2 build-deal-lsp: needs build-libdeal; matrix include 4 entries (os=macos-14 triple=darwin-arm64 libdeal-target=aarch64-macos, os=macos-13 triple=darwin-x64 libdeal-target=x86_64-macos, os=ubuntu-22.04 triple=linux-x64-gnu libdeal-target=x86_64-linux-gnu, os=windows-2022 triple=win-x64 libdeal-target=x86_64-windows); downloads libdeal-$libdeal-target artifact; runs `cargo build --release -p deal-lsp` + `cargo test --workspace`; on linux-x64-gnu also runs `zig build phase-3-gate-fresh` (Plan 07); uploads deal-lsp binary as artifact.
Job 3 package-vsix: needs build-deal-lsp; ubuntu-22.04 with setup-node node 20; downloads all deal-lsp-* artifacts; runs `npm ci` + `npm run compile`; patches bootstrap.ts SHA256_MANIFEST inline via patch-bootstrap-sha.js; runs `npx vsce package -o vscode-deal.vsix` (main, no bundled binary); for each triple in [darwin-arm64, darwin-x64, linux-x64-gnu, win-x64] copies deal-lsp into vscode-deal/server/ then runs `npx vsce package --target $vsce-target -o vscode-deal-$triple-offline.vsix`; uploads .vsix artifacts.
Job 4 publish-release: needs package-vsix; ubuntu-22.04; downloads all artifacts; generates SHA256SUMS via `sha256sum` over the binary files; softprops/action-gh-release uploads binaries + .vsix + SHA256SUMS.
Job 5 publish-marketplace: needs publish-release; environment=marketplace (manual-approval); ubuntu-22.04; downloads vsix artifact; runs `npx vsce publish --packagePath $vsix --pat $VSCE_PAT` for each.

RESEARCH section 4 -- bootstrap.ts shape (rust-analyzer paraphrase, ~120 LOC):

```
const DEAL_LSP_VERSION = '0.3.0';  // D-52: hardcoded pin matching vscode-deal version
const SHA256_MANIFEST: Record<string, string> = { 'darwin-arm64': '<sha>', ... };
function platformTriple(): string { ... }  // process.platform + process.arch + libc detection
export async function ensureDealLspBinary(ctx: vscode.ExtensionContext): Promise<string> {
  // 1. Bundled binary present? Return early (D-51 offline .vsix path).
  // 2. Cached download + matching SHA256? Return cached path.
  // 3. First-run dialog (Download / Cancel).
  // 4. Download with vscode.window.withProgress.
  // 5. Verify SHA-256; mismatch -> delete + throw.
  // 6. chmod +x on Unix.
  // 7. Return absolute path.
}
```

vsce CLI surface:
- `npx vsce package` bundles current dir into a .vsix
- `npx vsce package --target <triple>` Marketplace per-platform target; triples darwin-arm64, darwin-x64, linux-x64, win32-x64 (vsce uses VS Code's own triple format)
- `npx vsce publish --packagePath <vsix> --pat $TOKEN` publishes to Marketplace

Existing deal/build.zig surface (Plan 07 extends this; this plan only consumes it via cargo + zig build invocations):
- `zig build` produces zig-out/lib/libdeal.a
- `zig build phase-2-gate-fresh` exists (regression check)
- `zig build phase-3-gate-fresh` will be added by Plan 07
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement bootstrap.ts auto-download + sha256.ts helper; wire binary.ts to consume bootstrap as third-tier; add auto_download.test.ts</name>
  <files>
    /Users/dunnock/projects/deal-lang/vscode-deal/src/bootstrap.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/src/sha256.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/src/binary.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/src/test/suite/auto_download.test.ts,
    /Users/dunnock/projects/deal-lang/vscode-deal/package.json
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/vscode-deal/src/binary.ts (from Plan 05, has the Plan-06 TODO marker),
    /Users/dunnock/projects/deal-lang/vscode-deal/src/extension.ts (from Plan 05),
    /Users/dunnock/projects/deal-lang/vscode-deal/package.json (from Plan 05),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (sections 4 rust-analyzer bootstrap.ts pattern, 1212 supply-chain audit),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md (D-50, D-51, D-52)
  </read_first>
  <behavior>
    - bootstrap.ts exports `const DEAL_LSP_VERSION = '0.3.0'` (matching vscode-deal's version per D-52) and `const SHA256_MANIFEST: Record<TripleKey, string>` with placeholder hashes that the CI workflow patches before vsce package (Task 2 implements the patch step).
    - bootstrap.ts `function platformTriple(): TripleKey` maps process.platform + process.arch to one of 'darwin-arm64' | 'darwin-x64' | 'linux-x64-gnu' | 'linux-x64-musl' | 'win-x64'. For linux x64, calls `detectLinuxLibc()` which probes the system to distinguish musl from gnu. Unsupported platform throws an Error with the unsupported triple.
    - bootstrap.ts `export async function ensureDealLspBinary(ctx: vscode.ExtensionContext): Promise<string>` follows RESEARCH section 4 lines 406-448 verbatim:
      1. Compute triple + filename ('deal-lsp.exe' on win32, 'deal-lsp' otherwise).
      2. Compute dest URI: `vscode.Uri.joinPath(ctx.globalStorageUri, DEAL_LSP_VERSION, filename)`.
      3. Bundled-binary check: `vscode.Uri.joinPath(ctx.extensionUri, 'server', filename)` -- if exists, return its fsPath (offline .vsix path per D-51).
      4. Cached-download check: if dest exists AND sha256.sha256File(dest.fsPath) === SHA256_MANIFEST[triple], return dest.fsPath.
      5. First-run dialog via vscode.window.showInformationMessage. If user picks Cancel, throw an Error -- extension.ts catches and falls back silently per D-40.
      6. Download with progress via vscode.window.withProgress(ProgressLocation.Notification, ...).
      7. URL shape: const url = `https://github.com/deal-lang/deal/releases/download/v${DEAL_LSP_VERSION}/deal-lsp-${triple}.tar.gz`.
      8. After download, re-compute SHA-256; if mismatch, delete dest and throw.
      9. chmod +x on Unix (fs.chmodSync(dest.fsPath, 0o755) if process.platform !== 'win32').
      10. Return dest.fsPath.
    - bootstrap.ts `downloadAndExtract(url, dest, progress)` uses Node's native fetch (Node 20+) -- fetch the .tar.gz, save to a temp file, extract via the `tar` npm package (add to dependencies). The `tar` package is well-known (10+ year history per RESEARCH section 1199 audit shape).
    - sha256.ts exports `export function sha256File(path: string): Promise<string>` -- streams the file through Node's crypto createHash 'sha256', returns hex digest.
    - sha256.ts also exports `export function sha256Buffer(buf: Buffer): string` -- synchronous version for in-memory bytes.
    - binary.ts is updated: tier 3 now calls `bootstrap.ensureDealLspBinary(context)` instead of returning null. The TODO comment from Plan 05 is removed.
    - binary.ts returns `{ path, source: 'downloaded' }` from tier 3 on success.
    - binary.ts wraps the bootstrap call in try/catch -- on error (user cancelled, network failure, checksum mismatch), returns null. extension.ts already handles null per Plan 05's D-40 silent-fallback policy.
    - package.json gains "tar" runtime dependency at an exact-pinned version (run `npm view tar version` at execution time and pin).
    - auto_download.test.ts (in src/test/suite/) has 2 tests:
      - `auto_download_url_correct` -- invokes bootstrap.ts with a mocked fetch (via sinon stub on the fetch shim, or by injecting a httpClient interface). Asserts the constructed URL contains `v${DEAL_LSP_VERSION}` and `deal-lsp-${platformTriple()}.tar.gz`.
      - `sha256_mismatch_rejects` -- mocks the download to write known-bad bytes to dest, mocks SHA256_MANIFEST with the expected (different) hash; invokes ensureDealLspBinary; asserts the dest file was deleted and an Error was thrown.
  </behavior>
  <action>
    Implement bootstrap.ts per RESEARCH section 4 lines 380-448 (rust-analyzer paraphrase) and CONTEXT.md D-50..D-52.

    The SHA-256 manifest is populated by the CI workflow on each release tag -- at this plan's implementation time, fill it with placeholder strings like `'<sha256-populated-by-CI-on-release-tag>'` and document that the release pipeline (Task 2) is responsible for replacing these. This is the chicken-and-egg of a self-bootstrapping release: the .vsix bundles a manifest the matching release tag must produce.

    Strategy: the release workflow patches bootstrap.ts in-place before vsce package, baking real hashes for the freshly-built binaries. The patch script `vscode-deal/scripts/patch-bootstrap-sha.js` (committed in Task 2) handles the substitutions.

    Until Task 2 lands, bootstrap.ts has obvious placeholder values that fail SHA-256 verification -- but extension.ts catches the error per Plan 05's D-40 policy, so a user who installs the un-released-yet .vsix sees TextMate fallback (acceptable for development; never shipped).

    Detailed shape of sha256.ts:
    - Imports from 'crypto', 'fs', 'stream/promises'.
    - `sha256File(path)`: create a Hash; pipeline a readStream through it; return digest('hex').
    - `sha256Buffer(buf)`: createHash 'sha256', update(buf), digest('hex'). Synchronous.

    Detailed shape of bootstrap.ts (~120 LOC). Use Node 20's native `fetch` for the download (no node-fetch dep). Extract the .tar.gz via the `tar` package's `tar.extract({ file, cwd })` API. Avoid shelling out -- pure Node modules only.

    Mocking strategy for auto_download.test.ts: refactor bootstrap.ts to take an injectable `httpClient: { fetch(url): Promise<Response> }` and `fsLayer: { exists, chmod, unlink }` so tests can inject stubs. Production code uses the real fetch + fs module; tests pass sinon stubs.

    Wire-up in binary.ts: import `ensureDealLspBinary` from bootstrap.ts; in resolveLspPath, replace the Plan-05 TODO with:
    - try { const path = await ensureDealLspBinary(context); return { path, source: 'downloaded' }; }
    - catch { /* User cancelled, network failure, or SHA-256 mismatch -- log to output channel and fall through to null. */ return null; }

    No vscode.window.showWarningMessage on failure -- D-40 forbids modals; use the output channel. Refactor resolveLspPath to take the output channel as a second argument so binary.ts can log there.

    Add `tar` to package.json dependencies (not devDependencies -- it's used at extension runtime). Pin exact version via `npm view tar version` at execution time.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/vscode-deal && npm install --no-audit --no-fund --silent && npx tsc -p ./ --noEmit && test -f src/bootstrap.ts && test -f src/sha256.ts && grep -q "DEAL_LSP_VERSION" src/bootstrap.ts && grep -q "releases/download" src/bootstrap.ts && grep -q "platformTriple" src/bootstrap.ts && grep -q "ensureDealLspBinary" src/bootstrap.ts && grep -q "createHash" src/sha256.ts && grep -q "ensureDealLspBinary" src/binary.ts && ! grep -q "TODO .Plan 06." src/binary.ts && grep -q "auto_download_url_correct" src/test/suite/auto_download.test.ts && grep -q "sha256_mismatch_rejects" src/test/suite/auto_download.test.ts && (cd /Users/dunnock/projects/deal-lang/deal && cargo build -p deal-lsp --release) && (xvfb-run -a npm test 2>/dev/null || npm test) && echo "bootstrap + auto_download tests OK"</automated>
  </verify>
  <done>
    bootstrap.ts implements D-50/D-52 auto-download with SHA-256 verification + first-run dialog + progress; sha256.ts streams hashes correctly; binary.ts no longer carries the Plan-05 TODO; 2 new auto_download tests pass (in addition to Plan 05's 4); package.json includes the `tar` runtime dep.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author .github/workflows/release.yml (5-job pipeline); add patch-bootstrap-sha.js and SHA256SUMS.template</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/.github/workflows/release.yml,
    /Users/dunnock/projects/deal-lang/deal/SHA256SUMS.template,
    /Users/dunnock/projects/deal-lang/vscode-deal/scripts/patch-bootstrap-sha.js
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (section 12 CI matrix verbatim),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md (D-50, D-51, D-52, D-53),
    /Users/dunnock/projects/deal-lang/deal/build.zig (Plan 07 will add phase-3-gate-fresh; this workflow's linux-x64-gnu row invokes it),
    /Users/dunnock/projects/deal-lang/deal/scripts/verify-fresh-worktree.sh (referenced by the gate step),
    /Users/dunnock/projects/deal-lang/vscode-deal/src/bootstrap.ts (from Task 1; package-vsix job patches its SHA256_MANIFEST)
  </read_first>
  <behavior>
    - release.yml lives at /Users/dunnock/projects/deal-lang/deal/.github/workflows/release.yml (within the deal/ submodule). Triggers: on push tags 'v*.*.*' and on workflow_dispatch.
    - Job 1 build-libdeal per RESEARCH section 12 lines 826-848:
      - runs-on ubuntu-22.04.
      - strategy.matrix.target = [aarch64-macos, x86_64-macos, x86_64-linux-gnu, x86_64-linux-musl, x86_64-windows].
      - Steps: actions/checkout@<sha> with submodules recursive; mlugg/setup-zig@<sha> with version 0.16.0; `cd deal && zig build -Doptimize=ReleaseSafe -Dtarget=$matrix-target`; emit sha256 of zig-out/lib/libdeal.a into libdeal-$target.sha256; upload-artifact deal/zig-out/lib/libdeal.a + .sha256 as libdeal-$target.
      - Pin actions to exact commit SHAs (not @v4) for supply-chain T-3-07 mitigation.
    - Job 2 build-deal-lsp per RESEARCH section 12 lines 852-886:
      - needs build-libdeal.
      - strategy.matrix.include = 5 entries (4 from RESEARCH + 1 musl row): {os: macos-14, triple: darwin-arm64, libdeal-target: aarch64-macos}, {os: macos-13, triple: darwin-x64, libdeal-target: x86_64-macos}, {os: ubuntu-22.04, triple: linux-x64-gnu, libdeal-target: x86_64-linux-gnu}, {os: ubuntu-22.04, triple: linux-x64-musl, libdeal-target: x86_64-linux-musl, rust-target: x86_64-unknown-linux-musl}, {os: windows-2022, triple: win-x64, libdeal-target: x86_64-windows}.
      - Steps: checkout with submodules recursive; download-artifact libdeal-$libdeal-target into deal/zig-out/lib/; dtolnay/rust-toolchain@<sha>; for the musl row only, `rustup target add x86_64-unknown-linux-musl`; `cd deal && cargo build --release -p deal-lsp` (with --target flag for musl row); `cd deal && cargo test --workspace`; if matrix.triple == linux-x64-gnu, `cd deal && zig build phase-3-gate-fresh` (continue-on-error: true until Plan 07 lands the gate); upload-artifact deal/target/release/deal-lsp* (or deal/target/$rust-target/release/deal-lsp for musl) as deal-lsp-$triple.
    - Job 3 package-vsix per RESEARCH section 12 lines 890-913:
      - needs build-deal-lsp.
      - runs-on ubuntu-22.04.
      - Steps: checkout; actions/setup-node@<sha> with node-version 20; download-artifact pattern 'deal-lsp-*' into artifacts/; `cd vscode-deal && npm ci`; `cd vscode-deal && npm run compile`.
      - NEW STEP: patch bootstrap.ts SHA256_MANIFEST via `node vscode-deal/scripts/patch-bootstrap-sha.js artifacts/` (Task 2 commits this script).
      - Run `cd vscode-deal && npx vsce package -o vscode-deal.vsix` (main, no bundled binary).
      - For each triple in [darwin-arm64, darwin-x64, linux-x64-gnu, win-x64]: copy deal-lsp into vscode-deal/server/; run `cd vscode-deal && npx vsce package --target $vsce-target -o vscode-deal-$triple-offline.vsix` (where vsce-target maps darwin-arm64 -> darwin-arm64, darwin-x64 -> darwin-x64, linux-x64-gnu -> linux-x64, win-x64 -> win32-x64); rm -rf vscode-deal/server.
      - Upload-artifact pattern vscode-deal/*.vsix as vsix.
    - Job 4 publish-release per RESEARCH section 12 lines 917-931:
      - needs package-vsix.
      - runs-on ubuntu-22.04.
      - Steps: download all artifacts; generate SHA256SUMS by `find . -name 'deal-lsp-*' -type d | xargs -I{} bash -c 'cd {} && sha256sum deal-lsp* >> ../SHA256SUMS'` and also sha256sum the .vsix files; softprops/action-gh-release@<sha> with files: deal-lsp-*/deal-lsp*, vsix/*.vsix, SHA256SUMS.
    - Job 5 publish-marketplace per RESEARCH section 12 lines 935-948:
      - needs publish-release.
      - environment: marketplace (manual-approval gate via GitHub Environment with required reviewer).
      - runs-on ubuntu-22.04.
      - Steps: download vsix artifact; for each .vsix run `npx vsce publish --packagePath $vsix --pat ${{ secrets.VSCE_PAT }}`.
    - SHA256SUMS.template documents the format for the SHA256SUMS file generated by Job 4: one line per binary, sha256 then asterisk then filename, sorted alphabetically.
    - All actions pinned to commit SHAs (not version tags) for supply-chain T-3-02/T-3-07 mitigation.
    - The `act` CLI (local GitHub Actions runner) can dry-run the workflow on the user's machine -- document the command in a comment at the top of release.yml.
    - patch-bootstrap-sha.js: a small node script (~40 LOC) that:
      1. Takes the artifacts/ directory path as argv[2].
      2. For each triple in [darwin-arm64, darwin-x64, linux-x64-gnu, linux-x64-musl, win-x64], computes sha256 of artifacts/deal-lsp-$triple/deal-lsp* via the crypto module.
      3. Reads vscode-deal/src/bootstrap.ts.
      4. For each triple, runs a string replace on the placeholder pattern (e.g., `'darwin-arm64': '<sha256-populated-by-CI-on-release-tag>'` -> `'darwin-arm64': '<real-sha>'`).
      5. Writes back. Validates that all 5 placeholders were replaced (count check).
  </behavior>
  <action>
    Write release.yml as a single YAML file (~280 lines) implementing the 5-job pipeline per RESEARCH section 12. Honor these architectural locks:
    - D-53: Job 1 cross-compiles via Zig on a single Linux runner for all 4 deal-lsp targets + an extra musl variant.
    - D-50: Job 4 publishes the main .vsix to GitHub Releases -- vscode-deal users auto-download deal-lsp from this URL.
    - D-51: Job 3 produces 4 per-platform offline .vsix variants with bundled binaries.
    - D-52: Job 3 patches bootstrap.ts's SHA256_MANIFEST inline so the released .vsix carries the exact hashes of the binaries in the same release.

    Action pinning policy: use commit SHAs for all third-party actions per T-3-07. Lookups (perform at execution time):
    - actions/checkout, actions/setup-node, actions/upload-artifact, actions/download-artifact: latest stable SHA (these are GitHub-owned per RESEARCH section 1224, lower-risk -- @v4 is acceptable here but SHA pin preferred).
    - mlugg/setup-zig: latest stable SHA from github.com/mlugg/setup-zig.
    - dtolnay/rust-toolchain: latest stable SHA from github.com/dtolnay/rust-toolchain.
    - softprops/action-gh-release: latest stable SHA from github.com/softprops/action-gh-release.

    The bootstrap.ts SHA-256 patching step: write a small node script at vscode-deal/scripts/patch-bootstrap-sha.js that reads `artifacts/deal-lsp-*/deal-lsp*`, computes sha256 of each via the crypto module, parses bootstrap.ts as a string, replaces the 5 placeholders with the real hashes, writes back. Invoke it in package-vsix job before `vsce package`.

    Manual-approval environment: the user must one-time configure a `marketplace` GitHub Environment with required reviewer (themselves). The `environment: marketplace` line in Job 5 blocks until approval is granted in the GitHub UI. This requirement is recorded in the plan's user_setup frontmatter.

    SHA256SUMS.template at /Users/dunnock/projects/deal-lang/deal/SHA256SUMS.template:
    - Header comment line explaining the file is generated by Job 4.
    - Format comment: <sha256> *<filename>.
    - Sort: alphabetical by filename.
    - Example body lines (with placeholder sha256 = `<sha256>`):
      - `<sha256> *deal-lsp-darwin-arm64`
      - `<sha256> *deal-lsp-darwin-x64`
      - `<sha256> *deal-lsp-linux-x64-gnu`
      - `<sha256> *deal-lsp-linux-x64-musl`
      - `<sha256> *deal-lsp-win-x64.exe`
      - `<sha256> *vscode-deal.vsix`
      - `<sha256> *vscode-deal-darwin-arm64-offline.vsix`
      - `<sha256> *vscode-deal-darwin-x64-offline.vsix`
      - `<sha256> *vscode-deal-linux-x64-gnu-offline.vsix`
      - `<sha256> *vscode-deal-win-x64-offline.vsix`

    Note on phase-3-gate-fresh dependency: Plan 07 adds the build step. Until then, the workflow line `zig build phase-3-gate-fresh` will fail. Use `continue-on-error: true` on that step for this plan's commit; Plan 07 flips it to required by removing the continue-on-error annotation.

    Note on user_setup: this plan's frontmatter declares the GitHub Actions secret + Environment setup. The `autonomous: false` flag tells execute-phase to pause for the user to confirm those configurations before tagging a release. Plan 07's gate does NOT require an actual release run -- it only verifies the workflow YAML parses and matches the locked shape.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && test -f .github/workflows/release.yml && test -f SHA256SUMS.template && test -f /Users/dunnock/projects/deal-lang/vscode-deal/scripts/patch-bootstrap-sha.js && python3 -c "import yaml; y=yaml.safe_load(open('.github/workflows/release.yml')); jobs=y.get('jobs',{}); needed=['build-libdeal','build-deal-lsp','package-vsix','publish-release','publish-marketplace']; missing=[j for j in needed if j not in jobs]; raise SystemExit('missing jobs: ' + ','.join(missing)) if missing else print('all 5 jobs present')" && grep -q "aarch64-macos" .github/workflows/release.yml && grep -q "x86_64-linux-musl" .github/workflows/release.yml && grep -q "darwin-arm64" .github/workflows/release.yml && grep -q "win-x64" .github/workflows/release.yml && grep -q "environment: marketplace" .github/workflows/release.yml && grep -q "VSCE_PAT" .github/workflows/release.yml && grep -q "sha256sum" .github/workflows/release.yml && grep -q "phase-3-gate-fresh" .github/workflows/release.yml && grep -q "patch-bootstrap-sha" .github/workflows/release.yml && node /Users/dunnock/projects/deal-lang/vscode-deal/scripts/patch-bootstrap-sha.js --help 2>/dev/null || node -e "require.resolve('/Users/dunnock/projects/deal-lang/vscode-deal/scripts/patch-bootstrap-sha.js'); console.log('patch script exists')" && echo "release.yml structure OK"</automated>
  </verify>
  <done>
    .github/workflows/release.yml has 5 jobs with the locked architectural shape (cross-compile via Zig + per-target Rust matrix + 4 offline .vsix + GH Release + Marketplace gate); SHA256SUMS.template documents the manifest format; patch-bootstrap-sha.js patches the SHA256_MANIFEST inline before vsce package; the workflow parses as valid YAML; all D-50..D-53 decisions visible in the YAML.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runners ↔ release artifacts | CI runners produce the production deal-lsp binaries + .vsix; compromise = compromised releases. |
| GitHub Releases CDN ↔ vscode-deal users | Auto-download path: every vscode-deal install pulls deal-lsp from GitHub Releases. |
| VS Code Marketplace ↔ vscode-deal users | Marketplace .vsix install path; Marketplace token (VSCE_PAT) is the only auth. |
| bootstrap.ts ↔ extracted deal-lsp binary | The downloaded binary runs as user-permission subprocess; SHA-256 check is the only integrity gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering (binary in-transit/at-rest) | bootstrap.ts download path | mitigate | SHA-256 manifest baked into bootstrap.ts at .vsix-build time (Task 2 patches it during package-vsix); mismatch deletes the file + throws an error. Manifest is pinned per D-52 -- the .vsix and deal-lsp binary always release together. |
| T-3-02 | Tampering (Marketplace token compromise) | publish-marketplace job | mitigate | Marketplace publish job is gated behind a manual-approval GitHub Environment (`marketplace`) with required reviewer = the user. A compromised CI token cannot push a release without manual approval. VSCE_PAT scope is "Marketplace > Publish" only (no other permissions). |
| T-3-03 | Tampering (npm supply-chain via vsce, tar) | package-vsix + extension runtime | mitigate | `tar` runtime dep is added in Task 1; well-known 10+ year package per RESEARCH section 1199 audit. `vsce` is Microsoft official. Pin both to exact versions. |
| T-3-07 | Cross-compile artifact poisoning | build-libdeal job artifact -> build-deal-lsp matrix | mitigate | All actions pinned to commit SHAs (not version tags). Job 1's libdeal.a artifacts are retained per the release tag (artifact retention defaults to 90 days). SHA-256 of libdeal.a SHOULD be recorded alongside the binary in the release notes -- a step in Job 1 emits libdeal-$target.sha256 alongside the .a artifact. |

**T-3-SC supply-chain checkpoint:** Before merging release.yml, run `slopcheck install` (or manual verification) on every action: actions/checkout, actions/upload-artifact, actions/download-artifact, actions/setup-node, mlugg/setup-zig, dtolnay/rust-toolchain, softprops/action-gh-release. All have multi-year history per RESEARCH section 1199. Pin all to exact commit SHAs at the time of release.yml authoring.
</threat_model>

<verification>
- `cd /Users/dunnock/projects/deal-lang/vscode-deal && npm install && npx tsc -p ./ --noEmit` exits 0.
- `cd /Users/dunnock/projects/deal-lang/vscode-deal && (xvfb-run -a npm test 2>/dev/null || npm test)` exits 0 with 6 tests passing (4 from Plan 05 + 2 new from this plan).
- `python3 -c "import yaml; yaml.safe_load(open('/Users/dunnock/projects/deal-lang/deal/.github/workflows/release.yml'))"` exits 0 (valid YAML).
- Grep gates on release.yml: `grep -c 'aarch64-macos\\|x86_64-macos\\|x86_64-linux-gnu\\|x86_64-linux-musl\\|x86_64-windows'` >= 5; `grep -c 'environment: marketplace'` >= 1; `grep -c 'VSCE_PAT'` >= 1; `grep -c 'sha256sum'` >= 1.
- Manual checkpoint (after merging): user adds VSCE_PAT secret + creates `marketplace` Environment with self as required reviewer per user_setup; user verifies a dry-run via `act` locally produces all 5 build outputs.
</verification>

<success_criteria>
- REQ-phase-3-4-vscode-lsp binary-distribution half satisfied: release.yml produces all release artifacts on tag push.
- D-50 honored: bootstrap.ts auto-download from GitHub Releases on first activation; SHA-256 verified.
- D-51 honored: 4 per-platform offline .vsix variants with bundled binaries.
- D-52 honored: bootstrap.ts DEAL_LSP_VERSION matches vscode-deal version; SHA256_MANIFEST patched at package-vsix time.
- D-53 honored: Job 1 cross-compiles libdeal.a on linux-x64-gnu runner; Jobs 2's matrix links per-target natively.
- 2 new tests pass (auto_download_url_correct, sha256_mismatch_rejects); total vscode-deal test count now 6.
- Pipeline is verifiable via `act` dry-run locally; full run waits on Plan 07's phase-3-gate-fresh to flip from continue-on-error.
- User has been notified of one-time GitHub secret + Environment setup via user_setup.
</success_criteria>

<output>
Create `.planning/phases/03-editor-intelligence/03-06-SUMMARY.md` on completion with: final action SHA pins used, deal-lsp triple coverage (5: arm64-darwin + x64-darwin + linux-gnu + linux-musl + win64), workflow YAML LOC, and any RESEARCH section 12 deviations (especially the musl + manual-approval handling).
</output>
