---
phase: 03-editor-intelligence
plan: 04
plan_id: 03-04
name: deal-lsp Completion, Hover, Definition, Workspace Index, Semantic Tokens
type: execute
wave: 2
depends_on:
  - 03-03
files_modified:
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/backend.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/workspace.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/index.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/completion.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/hover.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/definition.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/semantic_tokens.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/src/bin/gen_golden.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/Cargo.toml
  - /Users/dunnock/projects/deal-lang/deal/lsp/tests/showcase.rs
  - /Users/dunnock/projects/deal-lang/deal/lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json
autonomous: true
requirements:
  - REQ-phase-3-3-lsp-server
must_haves:
  truths:
    - "On LSP initialized, deal-lsp walks the workspace, parses every .deal/.dealx under packages/ and model/, and populates an in-memory HashMap<PathString, (FileUri, Range)> (D-46, D-47)."
    - "textDocument/definition on a <<specializes>> reference returns the target Location from the in-memory index (D-49 path-string resolver, cross-file resolution)."
    - "textDocument/hover on a declaration with a preceding /** */ doc comment returns Markdown content containing the doc text (LM-2)."
    - "textDocument/completion at trigger characters {., :, <} returns candidates including the 11 element keywords plus workspace types from the in-memory index."
    - "textDocument/semanticTokens/full returns encoded data:[number[]] following the §7 mapping table (D-39 full structural overlay)."
    - "textDocument/semanticTokens/full/delta returns SemanticTokensDelta against the prior result token-set for the same document."
    - "Workspace alias resolution (PS-5 [workspace.aliases]) layers on top of the merged HashMap in Rust without modifying the Zig core (D-49)."
    - "LSP does NOT write back to .deal/index.json (D-48 — disk index remains the CLI's artifact)."
  artifacts:
    - path: deal/lsp/src/workspace.rs
      provides: Workspace walker that discovers deal.toml, enumerates packages/ + model/ files, eager-parses on initialized
      contains: "deal.toml"
    - path: deal/lsp/src/index.rs
      provides: in-memory HashMap<PathString, (FileUri, Range)> with merge logic + alias resolution
      contains: "HashMap"
    - path: deal/lsp/src/completion.rs
      provides: completion provider returning element keywords + workspace types
    - path: deal/lsp/src/hover.rs
      provides: hover provider rendering LM-2 doc comments as Markdown
    - path: deal/lsp/src/definition.rs
      provides: definition provider with O(1) HashMap lookup
    - path: deal/lsp/src/semantic_tokens.rs
      provides: semantic_tokens encoder implementing the §7 DEAL→token mapping table (D-39)
      contains: "SemanticTokensLegend"
    - path: deal/lsp/src/bin/gen_golden.rs
      provides: dedicated cargo test-binary `deal-lsp gen-golden` (target name `gen-golden`) that emits the semantic-tokens golden JSON deterministically (per Issue 6 — replaces in-test UPDATE_GOLDEN escape hatch); invoked once during Task 3 to produce the committed golden file
      contains: "fn main"
    - path: deal/lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json
      provides: frozen committed golden output for the semantic_tokens_match_golden regression test; byte-for-byte comparison target; produced by gen-golden binary, never recomputed inside the test (Issue 6 resolution)
  key_links:
    - from: deal/lsp/src/backend.rs
      to: deal/lsp/src/workspace.rs
      via: initialized handler invokes workspace::eager_parse(deal_toml_path)
      pattern: "eager_parse"
    - from: deal/lsp/src/definition.rs
      to: deal/lsp/src/index.rs
      via: in-memory HashMap O(1) lookup keyed on fully-qualified path-string ID (D-23)
      pattern: "index.get"
    - from: deal/lsp/src/semantic_tokens.rs
      to: deal/deal-ffi/src/lib.rs
      via: deal_ast_json walk to derive token positions (PATTERNS.md lines 70–73 — cli/src/main.rs lines 644–700 pattern)
      pattern: "ast_json"
    - from: deal/lsp/tests/showcase.rs
      to: deal/lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json
      via: include_str! at test compile time; byte-for-byte comparison after result_id normalization
      pattern: "include_str!"
---

<objective>
Complete REQ-phase-3-3-lsp-server by adding the remaining four LSP capabilities on top of Plan 03's `Backend`/`Documents` primitives: cross-file go-to-definition (D-46/D-47/D-49), hover with LM-2 doc-comment rendering, completion (element keywords + workspace types), and structural semantic tokens (D-39 per RESEARCH §7 mapping table). Eager workspace parse on `initialized` populates an in-memory `HashMap<PathString, (FileUri, Range)>`; subsequent `did_change` events update the per-file slice of the index. The disk `.deal/index.json` is consulted as a warm-cache hint only — LSP never writes back (D-48).

Purpose: Close out REQ-phase-3-3-lsp-server (Plan 03 covered diagnostics + formatting; this plan completes the 5-capability set + semantic tokens). Provide the workspace-symbol index that REQ-phase-3-gate's "cross-file go-to-definition comparable to TypeScript in VS Code" depends on.

Output: deal-lsp serves all 5 required capabilities plus full/delta semantic tokens against the showcase. `cargo test -p deal-lsp --test showcase` passes all 8 integration tests from RESEARCH §1056 (Plan 03 covered 3; this plan adds 5 more).

Wave assignment: This plan is `wave: 2` with `depends_on: ["03-03"]`. After Plan 03 moved to wave 1 (Issue 4 resolution option a), Plan 04 shifts up by one wave. The data dependency on 03-03 is unchanged — 04 still consumes 03's deal-ffi safe wrappers, Backend struct, and Documents primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dunnock/projects/deal-lang/deal/.planning/PROJECT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/02-prove-the-pipeline/02-CONTEXT.md
@/Users/dunnock/projects/deal-lang/deal/lsp/src/backend.rs
@/Users/dunnock/projects/deal-lang/deal/lsp/src/documents.rs
@/Users/dunnock/projects/deal-lang/deal/lsp/src/diagnostics.rs
@/Users/dunnock/projects/deal-lang/deal/cli/src/main.rs
@/Users/dunnock/projects/deal-lang/deal/include/deal.h
@/Users/dunnock/projects/deal-lang/spec/ir/v0/schema.json

<interfaces>
<!-- D-23 fully-qualified path-string IDs (locked at Phase 2): -->
PathString = String  // e.g., "sedan_project.vehicle.electrical.BatteryPack.hvOut"

<!-- D-46 in-memory workspace index: -->
HashMap<PathString, (FileUri, Range)>

<!-- deal_index_json output shape (per Phase 2 D-23, D-26): array of records:
{
  "v": 1,
  "symbols": [
    { "path": "sedan_project.vehicle.electrical.BatteryPack", "file": "packages/vehicle/electrical/BatteryPack.deal", "range": {"start":{"line":N,"character":N}, "end":{"line":N,"character":N}} },
    ...
  ]
}
-->

<!-- D-39 SemanticTokensLegend (from RESEARCH §2 lines 188–206): -->
SEMANTIC_TOKEN_TYPES = [
  SemanticTokenType::KEYWORD,        // 0
  SemanticTokenType::TYPE,           // 1
  SemanticTokenType::PARAMETER,      // 2
  SemanticTokenType::VARIABLE,       // 3
  SemanticTokenType::PROPERTY,       // 4
  SemanticTokenType::NAMESPACE,      // 5
  SemanticTokenType::OPERATOR,       // 6
  SemanticTokenType::ENUM_MEMBER,    // 7
  SemanticTokenType::REGEXP,         // 8
]
SEMANTIC_TOKEN_MODIFIERS = [
  SemanticTokenModifier::DECLARATION,
  SemanticTokenModifier::DEFINITION,
  SemanticTokenModifier::READONLY,
  SemanticTokenModifier::STATIC,
  SemanticTokenModifier::DEPRECATED,
]

<!-- §7 DEAL→token mapping (consume verbatim from RESEARCH.md §7 table): -->
| DEAL construct | token_type | modifiers |
| element keyword (11 keywords) | KEYWORD | DECLARATION |
| direction (in/out/inout) | KEYWORD | — |
| modifier (abstract/variation) | KEYWORD | STATIC |
| import/package/as | KEYWORD | — |
| definition name (declaration) | TYPE | DECLARATION, DEFINITION |
| definition reference | TYPE | — |
| package path segment | NAMESPACE | — |
| <<specializes>>/<<redefines>>/<<conjugates>>/<<derives>> | OPERATOR | — |
| @trace/@simulation/@connection/@header/@doc | ENUM_MEMBER | — |
| multiplicity [1..*] | REGEXP | — |
| composition tag [<system>] | ENUM_MEMBER | — |
| property declaration | PROPERTY | DECLARATION |
| property reference | PROPERTY | — |
| parameter | PARAMETER | DECLARATION |
| readonly feature | PROPERTY | READONLY |
| deprecated feature | (inherit type) | DEPRECATED |
| /** */ doc comment | (NONE — falls through to TextMate) | DOCUMENTATION (where applicable) |

<!-- LSP encoded token format (LSP §3.18): 5-tuple deltas
[deltaLine, deltaStart, length, tokenType, tokenModifiersBitset]
Tokens MUST be sorted by (line, character) ascending.
-->

<!-- Plan 03 primitives consumed: -->
Backend { client, documents, debouncer, ... }  // from backend.rs
Documents { handles, buffers, parse_count }    // from documents.rs
OwnedDealHandle                                 // from deal-ffi
deal_ffi::safe::{parse, ast_json, ir_json, index_json, ...}  // safe wrappers from Plan 03

<!-- PS-5 [workspace.aliases] shape (from PROJECT.md / Phase 1 inheritance): -->
[workspace.aliases]
short = "very.long.qualified.path"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Workspace walker + in-memory index + PS-5 alias resolution</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/lsp/src/workspace.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/index.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/backend.rs
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/deal/lsp/src/backend.rs (from Plan 03),
    /Users/dunnock/projects/deal-lang/deal/lsp/src/documents.rs (from Plan 03),
    /Users/dunnock/projects/deal-lang/deal/cli/src/main.rs (lines 215–223 — index merge pattern),
    /Users/dunnock/projects/deal-lang/spec/examples/showcase/deal.toml,
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (section §10 workspace + §16 Plan 04 description),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md (D-46, D-47, D-48, D-49 + PS-5),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md (Plan 04 file table — workspace.rs, index.rs entries)
  </read_first>
  <behavior>
    - workspace.rs has `pub struct Workspace { root: PathBuf, aliases: HashMap<String, String> }` and `pub fn discover(root: &Path) -> Result<Workspace>` that reads `<root>/deal.toml`, parses the TOML via `toml = "0.8"` (add to workspace deps), extracts `[workspace.aliases]` map into `Workspace::aliases`.
    - workspace.rs `pub fn enumerate_files(&self) -> Vec<PathBuf>` walks `<root>/packages/**/*.deal` and `<root>/model/**/*.dealx` via `walkdir = "2"` (add to workspace deps), returning file paths in deterministic order (sorted).
    - workspace.rs `pub async fn eager_parse(workspace: &Workspace, documents: &Documents, index: &Index, client: &Client) -> Result<()>` iterates enumerated files, calls documents.open_silent(uri, contents) (parses and stores handle WITHOUT publishing diagnostics — eager parse is silent), calls index.update_from_handle(uri, &handle) to populate the symbol map. Per D-47, this runs on `initialized`.
    - index.rs `pub struct Index { symbols: DashMap<PathString, (Url, Range)>, aliases: HashMap<String, String> }`.
    - index.rs `pub fn update_from_handle(&self, uri: &Url, handle: &OwnedDealHandle)` calls `deal_ffi::safe::index_json(handle)`, parses the IndexEnvelope (`{v, symbols: [{path, file, range}]}`), inserts each `(path, (uri, range))` into the symbols map.
    - index.rs `pub fn update_from_file(&self, uri: &Url)` — when called, first REMOVES all entries with `value.0 == uri` (stale entries from prior parse), then proceeds as update_from_handle.
    - index.rs `pub fn lookup(&self, path_string: &str) -> Option<(Url, Range)>` — direct HashMap lookup with alias resolution: if `path_string` is in `aliases`, expand to the canonical PathString first.
    - index.rs `pub fn resolve_with_alias(&self, possibly_short: &str) -> String` — expands `[workspace.aliases]` short → long form (PS-5).
    - backend.rs::initialized handler is updated to:
      1. Discover workspace from the first workspace folder URI (or fall back to cwd).
      2. Spawn a tokio task that runs `workspace::eager_parse(&ws, &self.documents, &self.index, &self.client).await`.
      3. Log `tracing::info!("workspace index populated: {} symbols", index.len())` when done.
    - backend.rs::initialize is updated to declare `workspace_symbol_provider: Some(OneOf::Left(true))` so clients know cross-file lookup is available.
    - backend.rs::did_change debounce callback now also calls `index.update_from_file(uri)` after the re-parse so the in-memory index stays current (D-46 invariant: in-memory map is authoritative, not disk).
    - Documents gets a new method: `pub async fn open_silent(&self, uri: Url, text: String) -> Result<Arc<TokioMutex<OwnedDealHandle>>>` — same as open() but does NOT publish diagnostics (used by eager workspace parse to avoid spamming the client during initialize).
    - LSP must NOT call any write-back to `.deal/index.json` (D-48): grep gate in verify confirms `fs::write` or equivalent is absent from index.rs / workspace.rs.
  </behavior>
  <action>
    Add `toml = "0.8"` and `walkdir = "2"` to deal/Cargo.toml `[workspace.dependencies]`; pin exact patch versions after running `cargo search` (both packages have multi-year history; `[ASSUMED]` per RESEARCH §1199 supply-chain context — verify via cargo search).

    Implement workspace.rs:
    - `discover(root)`: read `<root>/deal.toml`; parse via `let manifest: toml::Value = toml::from_str(&contents)?`; extract `manifest.get("workspace").and_then(|w| w.get("aliases")).and_then(|a| a.as_table())` into `HashMap<String, String>`.
    - `enumerate_files(&self) -> Vec<PathBuf>`: use walkdir over `<root>/packages` (extension `.deal`) and `<root>/model` (extension `.dealx`); collect into a Vec; sort by lossy_string() for determinism.
    - `eager_parse(...)`: iterate enumerated paths; for each path, read contents (`std::fs::read_to_string`); construct `Url::from_file_path(path)`; call `documents.open_silent(uri, text).await`; on success, `index.update_from_handle(uri, &*handle_guard.lock().await)`.

    Implement index.rs:
    - DashMap<String, (Url, Range)> for symbols (concurrent access from per-document update + reads from definition/completion).
    - update_from_handle: deserialize the deal_index_json envelope via serde_json into a typed struct `struct IndexEnvelope { v: u32, symbols: Vec<SymbolEntry> }` with `struct SymbolEntry { path: String, file: String, range: lsp_types::Range }`. Insert each into the DashMap keyed on path.
    - update_from_file: first iterate-and-remove entries with `value.0 == *uri` (use `retain` with `|k, v| &v.0 != uri`), then re-insert from the freshly-parsed handle.
    - lookup: first check aliases for expansion, then HashMap::get.
    - resolve_with_alias: if `self.aliases.contains_key(path)`, return cloned alias value; else return path unchanged.

    Wire backend.rs::initialized:
    - Extract workspace folder URI from params (or fall back to env's CWD).
    - tokio::spawn the eager_parse future so initialized returns immediately (per LSP spec: long-running work belongs in tasks).
    - Log progress via tracing.

    Wire backend.rs::did_change (inside the debounce callback): after `documents.update(uri.clone(), text, &client).await`, fetch the handle and call `self.index.update_from_file(&uri)`.

    Add Documents::open_silent and Documents::get_handle_arc (already exists from Plan 03 as get_handle — confirm the name) so workspace.rs can share the same handle table the LSP uses.

    File ownership: this task touches backend.rs (already created in Plan 03). Be precise — append new methods and update initialized + did_change handlers; do NOT replace the entire file.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && cargo build -p deal-lsp --release && cargo test -p deal-lsp --lib && grep -q 'DashMap<String' lsp/src/index.rs && grep -q 'eager_parse' lsp/src/workspace.rs && grep -q 'open_silent' lsp/src/documents.rs && grep -q 'self.index.update_from_file' lsp/src/backend.rs && ! grep -nE 'fs::write|tokio::fs::write|writeln!\(.*index' lsp/src/index.rs lsp/src/workspace.rs && grep -q 'workspace_symbol_provider' lsp/src/backend.rs && echo "workspace + index OK"</automated>
  </verify>
  <done>
    workspace.rs discovers deal.toml + enumerates .deal/.dealx files; index.rs maintains in-memory HashMap with D-46/D-47/D-49 semantics + PS-5 alias resolution; backend::initialized triggers eager_parse; did_change refreshes index per file; D-48 invariant holds (no fs::write to .deal/index.json from LSP code).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Completion, hover, definition providers</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/lsp/src/completion.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/hover.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/definition.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/backend.rs
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/deal/lsp/src/backend.rs (after Task 1),
    /Users/dunnock/projects/deal-lang/deal/lsp/src/documents.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/index.rs (from Task 1),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (section §1 hover/completion/definition trait signatures),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md (LM-2 doc-comment rendering; D-23 path-string IDs),
    /Users/dunnock/projects/deal-lang/deal/include/deal.h (comment-attachment fields per CONTEXT.md line 380 reference),
    /Users/dunnock/projects/deal-lang/spec/ir/v0/schema.json (IR v0 schema for hover content lookup)
  </read_first>
  <behavior>
    - completion.rs `pub async fn handle_completion(documents: &Documents, index: &Index, params: CompletionParams) -> Result<Option<CompletionResponse>>`:
      - Always returns the 11 element keywords (`part def`, `port def`, ..., `flow def`) as CompletionItems with kind = CompletionItemKind::KEYWORD.
      - Additionally returns workspace types: iterate `index.symbols`, for each entry with a PathString ending in a TYPE definition (e.g., the last segment starts with uppercase), emit a CompletionItem with kind = CompletionItemKind::CLASS, label = last-segment, detail = full PathString.
      - Returns CompletionResponse::Array(items) wrapped in Ok(Some(...)).
      - Trigger characters per RESEARCH §1 line 120: ".", ":", "<". Backend's initialize declares these in CompletionOptions.
    - hover.rs `pub async fn handle_hover(documents: &Documents, index: &Index, params: HoverParams) -> Result<Option<Hover>>`:
      - Get URI + position from params.
      - Look up the handle via documents.get_handle(uri); call deal_ffi::safe::ast_json(handle).
      - Walk the AST JSON to find the node containing (line, character).
      - If the node has an attached `/** */` doc comment (Phase 1 LM-2 — comment-attachment field on the AST node, per deal.h line 380 reference), extract the doc text.
      - Return `Hover { contents: HoverContents::Markup(MarkupContent { kind: MarkupKind::Markdown, value: doc_text }), range: Some(node_range) }`.
      - If no doc comment is present, return Ok(None).
    - definition.rs `pub async fn handle_definition(documents: &Documents, index: &Index, params: GotoDefinitionParams) -> Result<Option<GotoDefinitionResponse>>`:
      - Get URI + position from params.
      - Look up the handle; call deal_ffi::safe::ast_json (or ir_json — whichever is faster for symbol-reference resolution).
      - Walk the AST/IR to find the identifier at (line, character).
      - If the identifier is a definition reference (e.g., type-reference, qualified-name segment), compute its fully-qualified PathString — apply alias expansion via `index.resolve_with_alias`.
      - Call `index.lookup(path_string)` → Option<(Url, Range)>.
      - Return `GotoDefinitionResponse::Scalar(Location { uri, range })`.
      - If the identifier is itself a declaration (definition site), return self-location (some editors expect this).
    - backend.rs::initialize is updated to declare the three new capabilities:
      - completion_provider: Some(CompletionOptions { trigger_characters: Some(vec![".".into(), ":".into(), "<".into()]), ..Default::default() })
      - hover_provider: Some(HoverProviderCapability::Simple(true))
      - definition_provider: Some(OneOf::Left(true))
    - backend.rs methods completion, hover, goto_definition delegate to the three new handler functions, passing &self.documents and &self.index.
    - All three providers tolerate the case where documents/index are not yet populated for a URI (e.g., during initialize race) — return Ok(None) gracefully.
  </behavior>
  <action>
    Implement the three provider modules using the safe wrappers from deal-ffi and the in-memory Index from Task 1.

    completion.rs is straightforward — emit the 11 keywords + iterate workspace types from index. Use `CompletionItem { label, kind, detail, documentation, insert_text, ..Default::default() }`. For element-keyword items, use `insert_text` matching the snippet prefix (e.g., for `part def` set `insert_text = "part def $1 { $0 }"` and `insert_text_format = Some(InsertTextFormat::SNIPPET)`).

    hover.rs requires AST walking. The key data sources:
    - `deal_ffi::safe::ast_json(handle)` returns a JSON byte slice of the parsed AST per Phase 1 D-04 (alphabetical-key invariant — keys are predictable).
    - The AST records nodes with `kind`, `range` (start/end positions), and (per CONTEXT.md / deal.h line 380) `doc_comment` attachment fields on ElementDef / ElementUsage.
    - Strategy: deserialize the full AST into a `serde_json::Value`, walk it recursively looking for any node whose `range` contains the hover position. The first match wins. Extract `doc_comment` from that node if present.
    - Convert the doc text to Markdown: strip the `/** */` delimiters, leading `*` on each line, then trim. Wrap in a CodeBlock for the declaration signature if available (e.g., `part def BatteryPack {`).

    definition.rs requires symbol resolution. The key data sources:
    - `deal_ffi::safe::ast_json(handle)` to find the identifier at position.
    - `deal_ffi::safe::ir_json(handle)` (Phase 2 D-22 IR v0) provides resolved references — each reference carries the fully-qualified PathString of its referent. Prefer ir_json over ast_json for definition lookup because IR is already resolved (cli/src/main.rs lines 215–223 demonstrates the index_json + IR walk pattern).
    - Strategy: deserialize ir_json into a serde_json::Value, walk for the node whose `source_range` matches the request position, extract its `resolves_to: PathString`, then `index.lookup(path_string)` returns the target Location.

    backend.rs delegation: in the existing initialize method declaration, add the three capability fields. In the existing completion/hover/goto_definition methods (Plan 03 left them as placeholders returning Ok(None)), replace the bodies with delegations:
    - `async fn completion(&self, params: CompletionParams) -> Result<Option<CompletionResponse>> { completion::handle_completion(&self.documents, &self.index, params).await.map_err(...) }`
    - Analogous for hover and goto_definition.

    Test fixtures: pick known-good showcase positions for the new integration tests in Task 4 — e.g., the position of `BatteryPack` in `<<specializes>>` clause inside `packages/vehicle/electrical/HighVoltageBatteryPack.deal`.

    Error handling: convert anyhow::Error / serde_json::Error to tower_lsp::jsonrpc::Error via `.map_err(|e| jsonrpc::Error { code: ErrorCode::InternalError, message: e.to_string().into(), data: None })`.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && cargo build -p deal-lsp --release && cargo test -p deal-lsp --lib && grep -q 'CompletionItemKind' lsp/src/completion.rs && grep -q 'HoverContents' lsp/src/hover.rs && grep -q 'GotoDefinitionResponse' lsp/src/definition.rs && grep -q 'completion_provider' lsp/src/backend.rs && grep -q 'hover_provider' lsp/src/backend.rs && grep -q 'definition_provider' lsp/src/backend.rs && grep -q 'trigger_characters' lsp/src/backend.rs && echo "providers OK"</automated>
  </verify>
  <done>
    completion.rs returns element-keyword items + workspace-type items; hover.rs renders LM-2 doc comments as Markdown; definition.rs performs O(1) HashMap lookup with alias resolution; backend.rs declares all three capabilities with correct trigger characters.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Semantic tokens encoder (full + delta) implementing the §7 mapping table; committed golden file produced by dedicated gen-golden binary</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/lsp/src/semantic_tokens.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/backend.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/bin/gen_golden.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/Cargo.toml,
    /Users/dunnock/projects/deal-lang/deal/lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/deal/lsp/src/documents.rs,
    /Users/dunnock/projects/deal-lang/deal/cli/src/main.rs (lines 644–700 — deal_ast_json walk pattern per PATTERNS.md),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (sections §2 SemanticTokensLegend encoding + §7 DEAL→token mapping table),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md (D-39 lock-in)
  </read_first>
  <behavior>
    - semantic_tokens.rs declares two static slices: `pub const SEMANTIC_TOKEN_TYPES: &[SemanticTokenType] = &[KEYWORD, TYPE, PARAMETER, VARIABLE, PROPERTY, NAMESPACE, OPERATOR, ENUM_MEMBER, REGEXP]` (9 types) and `pub const SEMANTIC_TOKEN_MODIFIERS: &[SemanticTokenModifier] = &[DECLARATION, DEFINITION, READONLY, STATIC, DEPRECATED]` (5 modifiers).
    - semantic_tokens.rs declares `pub fn semantic_tokens_legend() -> SemanticTokensLegend` returning a Legend built from those two slices.
    - semantic_tokens.rs exposes a pure-function entry `pub fn encode_for_source(source: &[u8], filename: &str) -> SemanticTokens` that calls `deal_ffi::safe::parse`, walks the AST, encodes the tokens, and returns the SemanticTokens result with a DETERMINISTIC result_id (e.g., the constant string `"golden"` when the env var DEAL_LSP_DETERMINISTIC_RESULT_ID is set, otherwise a UUID). This entry is what the gen-golden binary calls; the LSP runtime path uses a thin wrapper that injects a UUID result_id.
    - `pub async fn handle_full(documents: &Documents, uri: &Url) -> Result<Option<SemanticTokensResult>>`:
      - Look up handle; call deal_ffi::safe::ast_json(handle).
      - Walk the AST; for each node matching a row in the §7 table, emit a Token { line, start, length, token_type_index, modifier_bitset }.
      - Sort tokens by (line, start) ascending (REQUIRED by LSP spec).
      - Encode as 5-tuple deltas per LSP §3.18: for each token, `[line_delta_from_prev, start_delta_within_line_or_from_prev_start, length, type_index, modifier_bitset]`.
      - Return `SemanticTokensResult::Tokens(SemanticTokens { result_id: Some(next_id()), data: encoded })`.
      - Cache the encoded result_id → tokens in Documents so the delta handler can compute differences.
    - `pub async fn handle_full_delta(documents: &Documents, uri: &Url, previous_result_id: String) -> Result<Option<SemanticTokensFullDeltaResult>>`:
      - Compute the new full token set.
      - Look up the cached previous token set by result_id.
      - Compute SemanticTokensEdit array (per LSP §3.18 — array splice operations: { start, delete_count, data }).
      - If the previous result_id is unknown (cache eviction), return the full token set instead of a delta.
      - Return `SemanticTokensFullDeltaResult::TokensDelta(SemanticTokensDelta { result_id, edits })`.
    - Documents gets a new field `previous_tokens: DashMap<Url, (String, Vec<u32>)>` (result_id → encoded data) and `pub fn cache_tokens(uri, result_id, data)` / `pub fn get_cached_tokens(uri, result_id)`.
    - The §7 mapping table is implemented as a match on AST node kind. Pseudo-code shape (illustrative):
      - Match node.kind {
        "ElementDef" → emit token at node.keyword_range with KEYWORD + DECLARATION; emit token at node.name_range with TYPE + DECLARATION + DEFINITION.
        "ImportDecl" → emit KEYWORD at "import" range; emit NAMESPACE for each path segment.
        "RelationshipOperator" → emit OPERATOR.
        "AnnotationUse" → emit ENUM_MEMBER.
        "CompositionTag" → emit ENUM_MEMBER for tag name (per §7 row "[<system>] composition tags").
        "Multiplicity" → emit REGEXP (per §7 row "multiplicity").
        "PropertyDecl" → emit PROPERTY + DECLARATION.
        "TypeReference" → emit TYPE (no modifiers).
        "Parameter" → emit PARAMETER + DECLARATION.
        "DocComment" → SKIP (per §7 — falls through to TextMate per ambiguity-resolution note in RESEARCH §651).
      }
    - backend.rs::initialize declares `semantic_tokens_provider: Some(SemanticTokensServerCapabilities::SemanticTokensOptions(SemanticTokensOptions { legend: semantic_tokens::semantic_tokens_legend(), full: Some(SemanticTokensFullOptions::Delta { delta: Some(true) }), range: Some(false), work_done_progress_options: Default::default() }))`.
    - backend.rs::semantic_tokens_full and ::semantic_tokens_full_delta methods delegate to the handler functions.
    - lsp/src/bin/gen_golden.rs is a dedicated cargo `[[bin]]` target named `gen-golden` (configured in lsp/Cargo.toml as `[[bin]] name = "deal-lsp-gen-golden" path = "src/bin/gen_golden.rs"`). It is invoked manually by the gate maintainer (Plan 07 phase-3-gate snapshot owner) to (re)generate the committed golden when intentional changes happen. The binary:
      1. Reads argv[1] as the showcase file path (default: tests/showcase/sedan_project/vehicle/electrical/BatteryPack.deal).
      2. Reads argv[2] as the output golden path (default: lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json).
      3. Calls `semantic_tokens::encode_for_source(source, filename)` with DEAL_LSP_DETERMINISTIC_RESULT_ID set so result_id == "golden".
      4. Serializes the SemanticTokens to canonical JSON via serde_json::to_string_pretty (sorted keys for determinism).
      5. Writes to the output path with a trailing newline.
    - lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json is committed to git (Issue 6 — REPLACES the inline UPDATE_GOLDEN escape hatch). Generated by running `cargo run --release -p deal-lsp --bin deal-lsp-gen-golden` once during Task 3 execution; never re-generated by the test itself.
  </behavior>
  <action>
    Build the semantic-tokens encoder per RESEARCH §7 + §2. The encoder is purely a transform from AST JSON to the LSP encoded representation; no FFI churn beyond ast_json.

    AST walk strategy:
    1. Deserialize ast_json bytes into serde_json::Value.
    2. Traverse the tree recursively. The Phase 1 AST shape (per D-04) is a tagged union — each node has a `kind` discriminator string plus structural fields.
    3. For each node kind matching a §7 table row, emit one or more `RawToken { line: u32, start: u32, length: u32, token_type: u32, modifiers: u32 }` records using the node's `range` field (start.line, start.character, computed length from end - start on same line; if multi-line, emit one token per line — but most DEAL constructs are single-line tokens so the simple case dominates).
    4. After collecting all RawTokens, sort by (line, start) ascending. LSP REQUIRES sorted order.
    5. Encode as deltas:
       ```
       prev_line = 0
       prev_start = 0
       for token in sorted_tokens:
         delta_line = token.line - prev_line
         delta_start = if delta_line == 0 then token.start - prev_start else token.start
         data.push(delta_line); data.push(delta_start); data.push(token.length); data.push(token.token_type); data.push(token.modifiers)
         prev_line = token.line
         prev_start = token.start
       ```
    6. Wrap in `SemanticTokens { result_id: Some(result_id_value), data }` where `result_id_value` is `"golden"` when DEAL_LSP_DETERMINISTIC_RESULT_ID is set, else `uuid::Uuid::new_v4().to_string()`.

    Modifier bitset encoding: modifiers are an OR'd bitset of indices into `SEMANTIC_TOKEN_MODIFIERS`. `DECLARATION = bit 0`, `DEFINITION = bit 1`, `READONLY = bit 2`, `STATIC = bit 3`, `DEPRECATED = bit 4`. A token with both DECLARATION and DEFINITION has modifier bitset = `0b00011 = 3`.

    Delta computation:
    - Plan-04 first delivers full only; delta is a follow-on. If the implementation budget is tight, ship `full_delta` as "fall back to full" (LSP spec allows the server to return full results from a delta handler if delta computation fails or is unimplemented). Document the trade-off in code and add a TODO for incremental delta support in Phase 3.x.
    - For a basic delta: compare two `Vec<u32>` token arrays; emit a single SemanticTokensEdit { start: 0, delete_count: prev.len(), data: new } as a "replace everything" delta. This is the LSP-spec-valid worst case; clients still benefit from the result_id stability.

    Caching:
    - Add `previous_tokens: DashMap<Url, (String, Vec<u32>)>` to Documents.
    - On handle_full: generate result_id (UUID), encode tokens, cache (uri, result_id, data), return.
    - On handle_full_delta with `previous_result_id`: check if cached result_id matches; if yes, compute diff; if no, return full set as recovery.

    Backend wire-up: add the two methods (semantic_tokens_full, semantic_tokens_full_delta) to the LanguageServer impl in backend.rs; update initialize to declare the provider; both delegate to handler functions.

    Golden generation pipeline (Issue 6 resolution):
    1. Add a new `[[bin]]` entry to deal/lsp/Cargo.toml: `name = "deal-lsp-gen-golden"`, `path = "src/bin/gen_golden.rs"`.
    2. Implement src/bin/gen_golden.rs as a small main (~50 LOC): read the source file, set the deterministic-result-id env var, call `semantic_tokens::encode_for_source`, serialize to pretty JSON, write to the output path.
    3. Run the binary ONCE during Task 3 execution to produce the committed golden:
       `cd /Users/dunnock/projects/deal-lang/deal && mkdir -p lsp/tests/golden/semantic_tokens && cargo run --release -p deal-lsp --bin deal-lsp-gen-golden -- tests/showcase/sedan_project/vehicle/electrical/BatteryPack.deal lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json`
    4. Commit `BatteryPack.deal.expected.json` to git as part of Task 3's commit. The test in Task 4 reads this committed file via `include_str!` and compares byte-for-byte.

    The committed golden file shape (formatted JSON, alphabetical keys, sorted by serde_json::to_string_pretty):
    ```json
    {
      "data": [0, 0, 8, 0, 1, 0, 9, 11, 1, 3, ...],
      "result_id": "golden"
    }
    ```

    Future regeneration: when the formatter, AST, or §7 mapping table intentionally changes, the gate maintainer (Plan 07) re-runs `cargo run --release -p deal-lsp --bin deal-lsp-gen-golden` and re-commits. No code path in normal test execution rewrites the golden — eliminating the Issue-6 inline UPDATE_GOLDEN tautology.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && cargo build -p deal-lsp --release && cargo build --release -p deal-lsp --bin deal-lsp-gen-golden && cargo test -p deal-lsp --lib && grep -q 'SEMANTIC_TOKEN_TYPES' lsp/src/semantic_tokens.rs && grep -q 'SemanticTokenType::KEYWORD' lsp/src/semantic_tokens.rs && grep -q 'SemanticTokenType::OPERATOR' lsp/src/semantic_tokens.rs && grep -q 'SemanticTokensFullOptions::Delta' lsp/src/backend.rs && grep -q 'semantic_tokens_full' lsp/src/backend.rs && grep -q 'semantic_tokens_full_delta' lsp/src/backend.rs && grep -q 'encode_for_source' lsp/src/semantic_tokens.rs && grep -q 'DEAL_LSP_DETERMINISTIC_RESULT_ID' lsp/src/semantic_tokens.rs && test -f lsp/src/bin/gen_golden.rs && grep -q 'deal-lsp-gen-golden' lsp/Cargo.toml && cargo run --release -p deal-lsp --bin deal-lsp-gen-golden -- tests/showcase/sedan_project/vehicle/electrical/BatteryPack.deal lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json && test -f lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json && echo "semantic tokens encoder + committed golden OK"</automated>
  </verify>
  <done>
    semantic_tokens.rs implements the §7 mapping table; SemanticTokensLegend exposed with 9 types + 5 modifiers per D-39; backend declares the provider with delta = true; both full and full/delta handlers wired; gen-golden binary produces the committed golden file (Issue 6 — no in-test recomputation); committed `lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json` exists in git.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extend tests/showcase.rs with 5 new integration tests; verify cross-file go-to-definition works end-to-end</name>
  <files>
    /Users/dunnock/projects/deal-lang/deal/lsp/tests/showcase.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/deal/lsp/tests/showcase.rs (from Plan 03 — 3 existing tests),
    /Users/dunnock/projects/deal-lang/deal/lsp/src/workspace.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/index.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/completion.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/hover.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/definition.rs,
    /Users/dunnock/projects/deal-lang/deal/lsp/src/semantic_tokens.rs,
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-VALIDATION.md (LSP integration tests section — the 8-test list)
  </read_first>
  <behavior>
    - tests/showcase.rs gains 5 new tokio::tests (running total: 8, matching RESEARCH §1056):
      - `completion_returns_element_keywords` — open a showcase file; send CompletionRequest at position inside a body; assert response contains 11 element keywords AND at least 3 workspace-type candidates.
      - `definition_lookup_specializes` — open `packages/vehicle/electrical/HighVoltageBatteryPack.deal` (or any showcase file with a `<<specializes>>` reference); send GotoDefinitionRequest at the reference position; assert response Location.uri points at the parent type's defining file AND range matches its declaration.
      - `hover_renders_jsdoc` — open a showcase file with a `/** */` doc comment; send HoverRequest at the declaration position; assert returned MarkupContent.value contains the expected doc text.
      - `semantic_tokens_match_golden` — open `tests/showcase/sedan_project/vehicle/electrical/BatteryPack.deal`; send SemanticTokensRequest; compare encoded `data` array to `include_str!("golden/semantic_tokens/BatteryPack.deal.expected.json")` byte-for-byte (after parsing both via serde_json::from_str and comparing the `data` arrays). No UPDATE_GOLDEN env var branch. No in-test recomputation. The committed golden produced by gen-golden (Task 3) is the regression baseline (Issue 6 resolution).
      - `workspace_index_populated_after_initialize` — initialize with the showcase workspace; wait for the eager-parse spawned task to complete (poll index.len() until > expected_threshold or 5s timeout); assert index has > 50 entries (showcase has ~15 .deal × ~5 symbols each minimum).
    - All 8 tests (3 from Plan 03 + 5 from Plan 04) pass via `cargo test -p deal-lsp --test showcase`.
    - For cross-file go-to-definition, the test workspace MUST be the actual showcase (mounted at deal/tests/showcase symlink). Set the LSP root URI to that path on initialize; wait for eager_parse to populate the index; then exercise definition request.
  </behavior>
  <action>
    Extend the existing tests/showcase.rs (created in Plan 03) with the 5 additional tokio::tests. Reuse the in-process LSP harness pattern established in Plan 03 (LspService over duplex pipes).

    For `completion_returns_element_keywords`:
    - did_open a showcase file (e.g., packages/vehicle/electrical/BatteryPack.deal).
    - Wait briefly (50 ms) for diagnostics.
    - Send CompletionParams { text_document: TextDocumentIdentifier { uri }, position: Position { line: <known body line>, character: 4 } }.
    - Parse response; assert response.items contains items with labels matching all 11 element-keyword prefixes.
    - Assert response.items also contains at least 3 items with kind = CLASS (workspace types).

    For `definition_lookup_specializes`:
    - Initialize the LSP with the showcase workspace folder URI.
    - Wait for eager_parse to complete (poll index.len() or wait 1s).
    - did_open the file containing the `<<specializes>>` reference.
    - Send GotoDefinitionParams with position pointing at the parent-type identifier (right after `<<specializes>>`).
    - Assert response is Location { uri, range } where uri points at the parent type's defining file and range is in that file (not the requesting file).

    For `hover_renders_jsdoc`:
    - Identify a showcase declaration that has a `/** */` doc comment above it (e.g., any well-documented `part def` — check spec/examples/showcase/ for one with a leading `/** */`; if none exists, add one to a test fixture file under deal/lsp/tests/fixtures/ instead).
    - did_open the file.
    - Send HoverParams at the declaration identifier position.
    - Assert response.contents matches MarkupContent { kind: Markdown, value: <expected> } where value contains a known substring from the doc comment.

    For `semantic_tokens_match_golden` (Issue 6 resolution):
    - At the top of the test module: `const GOLDEN_JSON: &str = include_str!("golden/semantic_tokens/BatteryPack.deal.expected.json");`
    - At test runtime, deserialize: `let golden: SemanticTokens = serde_json::from_str(GOLDEN_JSON).unwrap();`
    - did_open `tests/showcase/sedan_project/vehicle/electrical/BatteryPack.deal`.
    - Send SemanticTokensParams.
    - Receive `SemanticTokensResult::Tokens(actual)`.
    - Normalize actual.result_id (LSP runtime produces UUID; golden has "golden" sentinel) — compare only `actual.data == golden.data` byte-for-byte.
    - Assert `assert_eq!(actual.data, golden.data, "semantic-tokens data drift — regenerate via: cargo run --release -p deal-lsp --bin deal-lsp-gen-golden tests/showcase/.../BatteryPack.deal lsp/tests/golden/...")`.
    - DO NOT write golden back from the test under any env var. Eliminate the inline UPDATE_GOLDEN escape hatch (Issue 6).

    For `workspace_index_populated_after_initialize`:
    - Construct InitializeParams with workspace_folders = Some(vec![WorkspaceFolder { uri: showcase_uri, name: "showcase" }]).
    - Send initialize → expect Ok response.
    - Send initialized.
    - Poll index.len() (via a test-only `pub fn len(&self) -> usize` on Index) every 100 ms until > 50 OR 5 s elapsed.
    - Assert len > 50 (showcase symbol count threshold).

    Test fixtures: if no showcase file is suitable for a given test (e.g., none has a `/** */` doc comment in a known position), create a minimal `deal/lsp/tests/fixtures/<name>.deal` file with the required structure and use it.

    Index::len() helper: add `pub fn len(&self) -> usize { self.symbols.len() }` to index.rs for the workspace_index_populated test.

    Run `cargo test -p deal-lsp --test showcase -- --test-threads=1` to ensure tests do not race on the in-process LSP service.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && cargo test -p deal-lsp --test showcase -- --test-threads=1 && grep -c 'tokio::test' lsp/tests/showcase.rs | awk '{ if ($1 < 8) { print "Expected at least 8 tokio::tests, got " $1; exit 1 } else print "test count OK: " $1 }' && grep -q 'completion_returns_element_keywords' lsp/tests/showcase.rs && grep -q 'definition_lookup_specializes' lsp/tests/showcase.rs && grep -q 'hover_renders_jsdoc' lsp/tests/showcase.rs && grep -q 'semantic_tokens_match_golden' lsp/tests/showcase.rs && grep -q 'workspace_index_populated_after_initialize' lsp/tests/showcase.rs && grep -q 'include_str!.*golden/semantic_tokens' lsp/tests/showcase.rs && ! grep -q 'UPDATE_GOLDEN' lsp/tests/showcase.rs && test -f lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json && echo "8 integration tests OK + committed golden"</automated>
  </verify>
  <done>
    All 8 integration tests pass; committed `lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json` is the regression baseline; semantic_tokens_match_golden uses `include_str!` + byte-equality (no in-test recomputation; no UPDATE_GOLDEN escape hatch — Issue 6 resolution); cross-file go-to-definition exercises the eager workspace parse + in-memory index end-to-end; REQ-phase-3-gate's "cross-file go-to-definition comparable to TypeScript" claim is testable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LSP client ↔ deal-lsp (concurrent requests) | Multiple in-flight requests (completion, hover, definition, semantic tokens) may target the same document. |
| AST/IR JSON parsing in Rust | Untrusted byte slices from FFI deserialized via serde_json — malformed JSON would panic without graceful handling. |
| Workspace walker ↔ filesystem | walkdir crosses untrusted file boundaries; symlink loops or large workspaces could DoS the eager_parse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04 | Denial-of-Service (workspace size) | workspace::eager_parse | mitigate | walkdir bounded to packages/ and model/ subtrees (not arbitrary depths); sort + iterate in deterministic order; eager_parse is spawned in a background task so initialized returns immediately (LSP spec). For Phase 3 the showcase is the worst case (19 files); Phase 5+ may add a workspace-size cap if a real project exposes a DoS surface. |
| T-3-04 | Denial-of-Service (malformed AST/IR JSON) | hover.rs, definition.rs, semantic_tokens.rs | mitigate | All serde_json::from_slice calls wrapped in Result + map_err returning Ok(None) on parse failure rather than panic. Pitfall 3 (clone-before-free) is enforced inside deal-ffi safe wrappers (Plan 03) so AST/IR JSON bytes are owned Vec<u8>, not arena-borrowed slices. |
| T-3-05 | Tampering (concurrent FFI access) | Documents::handles DashMap + per-URI Mutex | mitigate | Per-handle tokio Mutex (D-45 from Plan 03) serializes same-URI access; different URIs serve concurrently. Semantic-tokens cache uses a separate DashMap, independent of handle access. |

**T-3-SC supply-chain checkpoint:** New packages `toml`, `walkdir`, `uuid` (already in workspace from Phase 2) added by this plan. Run `slopcheck install toml walkdir` before `cargo build`. Both have multi-year history and are well-known.
</threat_model>

<verification>
- `cd /Users/dunnock/projects/deal-lang/deal && cargo build -p deal-lsp --release` exits 0.
- `cd /Users/dunnock/projects/deal-lang/deal && cargo build --release -p deal-lsp --bin deal-lsp-gen-golden` exits 0.
- `cd /Users/dunnock/projects/deal-lang/deal && cargo test -p deal-lsp --test showcase -- --test-threads=1` exits 0 with 8 tests passing.
- `grep -c "tokio::test" /Users/dunnock/projects/deal-lang/deal/lsp/tests/showcase.rs` ≥ 8.
- `test -f /Users/dunnock/projects/deal-lang/deal/lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json` confirms committed golden (Issue 6).
- `! grep -q 'UPDATE_GOLDEN' /Users/dunnock/projects/deal-lang/deal/lsp/tests/showcase.rs` confirms inline escape hatch removed (Issue 6).
- `nm -gU /Users/dunnock/projects/deal-lang/deal/target/release/deal-lsp` non-empty (binary still builds).
- Grep gates: `grep -v '^//' deal/lsp/src/semantic_tokens.rs | grep -c 'SemanticTokenType::'` ≥ 9 (all 9 token types referenced); `grep -v '^//' deal/lsp/src/index.rs | grep -c 'aliases'` ≥ 2 (PS-5 alias resolution wired); `grep -nE 'fs::write|tokio::fs::write' deal/lsp/src/index.rs deal/lsp/src/workspace.rs | grep -v 'index.json' || true` should not contain any write to .deal/index.json (D-48).
</verification>

<success_criteria>
- REQ-phase-3-3-lsp-server complete: 5 capabilities served (diagnostic via Plan 03, completion, hover, definition, formatting via Plan 03) + semantic tokens.
- D-39 honored: 9 token types + 5 modifiers per §7 mapping table; SemanticTokensFullOptions::Delta with delta=true declared.
- D-46/D-47/D-48/D-49 honored: in-memory HashMap is authoritative; disk .deal/index.json never written by LSP; eager parse on initialized; PS-5 alias resolution in Rust.
- LM-2 honored: hover renders /** */ doc comments as Markdown.
- REQ-phase-3-gate cross-file go-to-definition claim is testable: the definition_lookup_specializes test exercises the full eager_parse → index → lookup chain on real showcase content.
- Wave 2 placement honored (Issue 4 resolution): plan's depends_on stays at ["03-03"]; wave shifts from 3 → 2 to reflect Plan 03's move to wave 1.
- Issue 6 resolution: committed `lsp/tests/golden/semantic_tokens/BatteryPack.deal.expected.json` is the regression baseline; gen-golden binary produced it; semantic_tokens_match_golden uses include_str! byte-equality; no UPDATE_GOLDEN escape hatch remains in the test source.
- Plan 05 can wire the vscode-deal LanguageClient to consume all 5 capabilities + semantic tokens without further deal-lsp changes.
</success_criteria>

<output>
Create `.planning/phases/03-editor-intelligence/03-04-SUMMARY.md` on completion with: 8 integration tests passing, committed golden file size (Issue 6), gen-golden binary invocation command, in-memory index symbol count on showcase open, completion candidate count, and any §7 mapping table refinements (e.g., AST node-kind name corrections against the actual Phase 1 schema).
</output>
