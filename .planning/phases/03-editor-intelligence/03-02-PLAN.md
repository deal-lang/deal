---
phase: 03-editor-intelligence
plan: 02
plan_id: 03-02
name: Tree-sitter Grammar and Corpus Tests
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/package.json
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/grammar.js
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/queries/highlights.scm
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/queries/injections.scm
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/queries/indents.scm
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/corpus/element-defs.txt
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/corpus/operators.txt
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/corpus/annotations.txt
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/corpus/composition-tags.txt
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/corpus/multiplicity.txt
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/corpus/showcase-snapshot.txt
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/highlight-snapshots/.gitkeep
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/.gitignore
  - /Users/dunnock/projects/deal-lang/tree-sitter-deal/README.md
autonomous: true
requirements:
  - REQ-phase-3-2-treesitter
must_haves:
  truths:
    - "`npx tree-sitter generate` produces src/parser.c without errors (grammar.js is a valid tree-sitter DSL spec)."
    - "`npx tree-sitter test` passes against all 6 corpus files; corpus parses match expected AST shapes."
    - "All 19 showcase files parse via `tree-sitter parse <file>` with zero ERROR nodes (full structural coverage of the showcase)."
    - "queries/highlights.scm assigns the 20 RESEARCH §8 @captures verbatim — visual parity with vscode-deal/COLOR-CATEGORIES.md (D-41)."
    - "Grammar is lean per RESEARCH §9 — ≤900 lines grammar.js; only structural anchors needed by highlights.scm/indents.scm are present (D-38 trade-off: VS Code uses TextMate; tree-sitter targets Neovim/Helix/Zed/GitHub)."
  artifacts:
    - path: tree-sitter-deal/grammar.js
      provides: tree-sitter DSL grammar for .deal + .dealx (lean highlight-focused per RESEARCH §9)
      contains: "name: 'deal'"
    - path: tree-sitter-deal/queries/highlights.scm
      provides: 20 @captures matching the §8 parity table
      contains: "@keyword.element"
    - path: tree-sitter-deal/queries/indents.scm
      provides: indent.begin / indent.end / indent.branch captures
      contains: "@indent.begin"
    - path: tree-sitter-deal/test/corpus/*.txt
      provides: 6 corpus files exercising every grammar rule and the showcase snapshot
    - path: tree-sitter-deal/package.json
      provides: npm package with tree-sitter-cli devDep + test scripts
      contains: "tree-sitter-cli"
  key_links:
    - from: tree-sitter-deal/queries/highlights.scm
      to: /Users/dunnock/projects/deal-lang/vscode-deal/COLOR-CATEGORIES.md
      via: shared color-category names (D-41)
      pattern: "@keyword.element"
    - from: tree-sitter-deal/grammar.js
      to: /Users/dunnock/projects/deal-lang/spec/grammar/lexical.ebnf
      via: lexical tokens MUST match the LOCKED 0.1.0-draft EBNF spec
      pattern: "identifier"
---

<objective>
Stand up the publishable shape of the `tree-sitter-deal` npm package: lean highlight-focused grammar (RESEARCH §9), 3 query files (`highlights.scm`/`injections.scm`/`indents.scm`), 6 corpus test files, and a `tree-sitter parse` smoke pass over all 19 showcase files. After this plan ships, `npx tree-sitter test` passes, `npx tree-sitter highlight` produces colored output on any showcase file, and the package is structurally ready for Linguist submission (deferred per RESEARCH Open Q3).

Purpose: Deliver REQ-phase-3-2-treesitter. Lock D-41 (color-category parity) by emitting @captures whose names exactly match the `vscode-deal/COLOR-CATEGORIES.md` rows from Plan 01.

Output: A `tree-sitter-deal/` package whose `npm test` exits 0 and whose grammar parses the 19-file showcase without ERROR nodes. Plan 01's COLOR-CATEGORIES.md and this plan's highlights.scm together satisfy D-41.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dunnock/projects/deal-lang/deal/.planning/PROJECT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/ROADMAP.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-CONTEXT.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md
@/Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md
@/Users/dunnock/projects/deal-lang/spec/grammar/lexical.ebnf
@/Users/dunnock/projects/deal-lang/spec/grammar/deal.ebnf
@/Users/dunnock/projects/deal-lang/spec/grammar/dealx.ebnf
@/Users/dunnock/projects/deal-lang/tree-sitter-deal/README.md
@/Users/dunnock/projects/deal-lang/deal/tests/showcase

<interfaces>
<!-- Tree-sitter capture name contract: every @capture in queries/highlights.scm
     MUST match a tree-sitter capture cell in the RESEARCH §8 / COLOR-CATEGORIES.md table. -->

RESEARCH §8 — tree-sitter @captures (consume verbatim):
- @keyword.element        (element keyword)
- @keyword.direction      (direction keyword)
- @keyword.modifier       (modifier)
- @keyword.import         (import/package)
- @type.definition        (definition name at declaration site)
- @type                   (definition reference)
- @namespace              (package path segments)
- @operator.relationship  (<<specializes>> etc)
- @attribute.annotation   (@trace etc)
- @tag.composition        (composition tag)
- @number.multiplicity    (multiplicity)
- @property.declaration   (property declaration)
- @property               (property reference)
- @parameter              (action def parameter)
- @comment.documentation  (/** */)
- @comment                (// + /* */)
- @string                 (string literal)
- @number                 (integer literal)
- @punctuation.bracket    (braces, brackets)
- @punctuation.delimiter  (commas, semicolons, dots)

Grammar lean-scope (RESEARCH §9): cover lexical productions (lexical.ebnf 758 lines) + structural rules only sufficient to discriminate the 20 categories. NOT a full 87+43-production EBNF mirror.

Source-of-truth EBNF (consume verbatim — DO NOT modify):
- /Users/dunnock/projects/deal-lang/spec/grammar/lexical.ebnf — tokens
- /Users/dunnock/projects/deal-lang/spec/grammar/deal.ebnf — 87 productions for .deal
- /Users/dunnock/projects/deal-lang/spec/grammar/dealx.ebnf — 43 productions for .dealx

Showcase corpus (read-only — Phase 1.5 ADR locks it as integration oracle):
- /Users/dunnock/projects/deal-lang/spec/examples/showcase/ — 15 .deal + 4 .dealx files
- /Users/dunnock/projects/deal-lang/deal/tests/showcase → committed symlink to spec/examples/showcase/
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold npm package and lean grammar.js</name>
  <files>
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/package.json,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/grammar.js,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/.gitignore,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/README.md
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/README.md,
    /Users/dunnock/projects/deal-lang/spec/grammar/lexical.ebnf,
    /Users/dunnock/projects/deal-lang/spec/grammar/deal.ebnf,
    /Users/dunnock/projects/deal-lang/spec/grammar/dealx.ebnf,
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (sections §3 grammar.js DSL, §9 lean grammar rationale + scope),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-PATTERNS.md (Plan 02 table)
  </read_first>
  <behavior>
    - package.json fields: name "tree-sitter-deal", version "0.3.0", description matches REQ-phase-3-2-treesitter, main "bindings/node", scripts.test "tree-sitter test", scripts.generate "tree-sitter generate", scripts.parse "tree-sitter parse", devDependencies includes "tree-sitter-cli" pinned to exact version (e.g., "0.24.3"), no dependencies (grammar.js consumes tree-sitter-cli at build time).
    - package.json files array includes grammar.js, queries/, src/, bindings/, README.md (for npm publish).
    - grammar.js exports a single grammar({...}) call with name "deal".
    - grammar.js declares `extras` covering whitespace + line_comment + block_comment + doc_comment.
    - grammar.js declares `word: $ => $.identifier` so keywords resolve correctly.
    - grammar.js handles BOTH `.deal` and `.dealx` content via a unified `source_file` rule (per RESEARCH §3); composition tags `[<...>]` are valid only inside dealx parts of the rule tree but tolerated everywhere (lean grammar — recognize tokens, do not enforce file-extension lexer mode like Phase 1 does).
    - Rules cover: package_decl, import_decl, element_def (with optional relationship_clause and body), header_block (@header { ... }), annotation_use, composition_tag, composition_tag_block, multiplicity, qualified_name, body, property_decl, action_def_params, element_keyword (choice of the 11 keywords), direction_keyword, modifier_keyword, relationship_operator (choice of the 4 operators), identifier, integer, line_comment, block_comment, doc_comment, string_literal.
    - Grammar.js LOC budget per RESEARCH §9: 600–900 lines.
    - .gitignore excludes node_modules/, build/, src/parser.c, src/grammar.json, src/node-types.json (generated artifacts), bindings/{node,rust} (generated), *.tgz.
    - README.md gets updated to point at the lean-grammar scope per RESEARCH §9 and to cross-reference vscode-deal/COLOR-CATEGORIES.md for the parity table.
  </behavior>
  <action>
    Author grammar.js as a CommonJS module exporting `grammar({...})` per RESEARCH §3. Mirror the EBNF specs at `/Users/dunnock/projects/deal-lang/spec/grammar/lexical.ebnf` for token shape (identifier, integer, comments, strings) and the structural subset from deal.ebnf + dealx.ebnf needed to discriminate the §8 categories.

    Element keyword rule MUST list all 11 keywords as separate string alternatives (SD-1 surface): `part def`, `port def`, `action def`, `state def`, `requirement def`, `constraint def`, `attribute def`, `item def`, `interface def`, `connection def`, `flow def`. Use tree-sitter's `seq('part', 'def')` shape OR a single string `'part def'` — pick the one that survives `tree-sitter generate` (RESEARCH §3 uses the single-string form).

    Relationship operator rule: `choice('<<specializes>>', '<<redefines>>', '<<conjugates>>', '<<derives>>')` — single tokens, NOT separate `<<` + identifier + `>>` (this matches the §6 / §8 single-color treatment per CONTEXT D-39 ambiguity resolution).

    Composition tag rule: `seq('[<', field('name', $.identifier), optional(seq('=', $.composition_tag_attr_value)), repeat($.composition_tag_attr), '>]')` — supports both bare `[<system>]` and attribute forms `[<connect from={...} to={...}/>]`. Attribute value forms may use `field('value', /[^>}]*/)` shorthand for `{...}` blocks — lean grammar does NOT need to fully parse the JS-like expression inside the braces (per RESEARCH §9 trade-off).

    Multiplicity rule: `seq('[', $.multiplicity_range, ']')` with `multiplicity_range: choice(seq($.integer, '..', choice($.integer, '*')), $.integer, '*')`.

    Annotation rule: `seq('@', $.identifier, optional(seq(':', $.relationship_operator)))`. The `:` + operator form matches `@simulation:<<computes>>`.

    Doc comment rule: `token(seq('/**', /[^*]*\*+([^/*][^*]*\*+)*/, '/'))` — must NOT collide with block_comment rule (use precedence or rule ordering to make doc_comment win on `/**` prefix).

    Whitespace + comments go in `extras` so they can appear anywhere; identifier word rule allows keyword resolution.

    package.json must specify `tree-sitter-cli` devDependency with an exact version pin (no `^` / `~`). Use the latest stable per the `tree-sitter-cli` npm registry at planning time — the executor SHOULD `npm view tree-sitter-cli version` and use that exact value (verify it is published by the official `tree-sitter` org).

    README.md updates: keep the existing scaffold description; add a "Status: Phase 3 implementation in progress" section; cross-link to `vscode-deal/COLOR-CATEGORIES.md` for the parity table; cite D-41 and RESEARCH §9 as the design rationale.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/tree-sitter-deal && npm install --no-audit --no-fund --silent && npx tree-sitter generate && test -f src/parser.c && test -f src/grammar.json && node -e "const g=require('./grammar.js'); /* grammar.js executes without throwing */ console.log('grammar.js OK');" && wc -l grammar.js | awk '{ if ($1 < 200 || $1 > 1200) { print \"grammar.js LOC out of range: \" $1; exit 1 } else print \"grammar.js LOC OK: \" $1 }'</automated>
  </verify>
  <done>
    package.json with tree-sitter-cli pinned; grammar.js parses, generates src/parser.c without errors; LOC count in expected range (200–1200, per RESEARCH §9 sweet spot of 600–900 plus headroom); README updated.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author queries (highlights.scm, injections.scm, indents.scm), corpus tests, and showcase smoke</name>
  <files>
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/queries/highlights.scm,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/queries/injections.scm,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/queries/indents.scm,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/corpus/element-defs.txt,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/corpus/operators.txt,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/corpus/annotations.txt,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/corpus/composition-tags.txt,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/corpus/multiplicity.txt,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/corpus/showcase-snapshot.txt,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/test/highlight-snapshots/.gitkeep,
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/scripts/showcase-parse.sh
  </files>
  <read_first>
    /Users/dunnock/projects/deal-lang/tree-sitter-deal/grammar.js (from Task 1),
    /Users/dunnock/projects/deal-lang/vscode-deal/COLOR-CATEGORIES.md (from Plan 01),
    /Users/dunnock/projects/deal-lang/deal/.planning/phases/03-editor-intelligence/03-RESEARCH.md (sections §3 query syntax, §8 capture parity table),
    /Users/dunnock/projects/deal-lang/spec/examples/showcase/ (look at 2-3 representative .deal and .dealx files for corpus extraction)
  </read_first>
  <behavior>
    - queries/highlights.scm assigns exactly the 20 @captures from the §8 table. Captures use the tree-sitter convention `(node_name) @capture` or `(node_name field: (sub_node) @capture)`.
    - queries/highlights.scm assignments (verbatim from §8 right column):
      - `(element_keyword)` → @keyword.element
      - `(direction_keyword)` → @keyword.direction
      - `(modifier_keyword)` → @keyword.modifier
      - `(import_keyword)` → @keyword.import
      - `(element_def name: (identifier) @type.definition)`
      - `(qualified_name (identifier) @type)` at type-reference sites (or a separate `(type_reference)` rule per the grammar choice)
      - `(qualified_name (identifier) @namespace)` at package-path sites — distinguish via grammar field or order
      - `(relationship_operator)` → @operator.relationship
      - `(annotation_use)` → @attribute.annotation
      - `(composition_tag name: (identifier) @tag.composition)`
      - `(multiplicity)` → @number.multiplicity
      - `(property_decl name: (identifier) @property.declaration)`
      - `(property_ref name: (identifier) @property)` — if grammar has it; otherwise omit and document
      - `(parameter)` → @parameter (if action def parameters are modeled)
      - `(doc_comment)` → @comment.documentation
      - `(line_comment)` / `(block_comment)` → @comment
      - `(string_literal)` → @string
      - `(integer)` → @number
      - Punctuation captures: `["{" "}" "[" "]" "(" ")"]` → @punctuation.bracket; `[";" "," "."]` → @punctuation.delimiter
    - queries/injections.scm: empty file with a single comment `; No host-language injections in Phase 3 (no embedded languages — JS-like via={...} blocks are not parsed)` per RESEARCH §3 lines 314–317.
    - queries/indents.scm: `[(body) (header_block) (composition_tag_block)] @indent.begin`; `"}" @indent.end`; `"]" @indent.end`; `"[" @indent.branch` — per RESEARCH §3 lines 320–331.
    - Each corpus file follows tree-sitter test format: heading separator `==========`, name, `==========`, source code, `---`, expected AST sexpr.
    - element-defs.txt: 6+ test cases covering each of the 11 element keywords; minimum cases for `part def`, `port def`, `action def`, `state def`, `requirement def`, `connection def`.
    - operators.txt: 4 test cases — one per `<<specializes>>`, `<<redefines>>`, `<<conjugates>>`, `<<derives>>`.
    - annotations.txt: 3+ cases — `@trace`, `@simulation:<<computes>>`, `@header { ... }`.
    - composition-tags.txt: 4 cases — `[<system>]`, `[<connect from={a} to={b}/>]`, `[<satisfy req={r}>] ... [</satisfy>]`, `[<traceability from={a} to={b}/>]`.
    - multiplicity.txt: 4 cases — `[1..*]`, `[0..1]`, `[3]`, `[*]`.
    - showcase-snapshot.txt: at least one case per showcase file family — small excerpt (10–20 lines) verifying it parses with no ERROR. Goal is a smoke that the grammar handles real showcase patterns.
    - scripts/showcase-parse.sh runs `npx tree-sitter parse` against every file in `/Users/dunnock/projects/deal-lang/spec/examples/showcase/` (or via the deal/tests/showcase symlink if it's the canonical mounted path) and exits non-zero if ANY file produces an ERROR node.
    - Highlight snapshot directory exists (.gitkeep); first `npx tree-sitter highlight` run will populate it — Plan 07's gate ensures the snapshots remain stable.
  </behavior>
  <action>
    Author queries/highlights.scm with the 20 @captures from RESEARCH §8 (third column). Use the exact grammar node names from Task 1's grammar.js — the executor must read grammar.js to confirm the names align (`element_def`, `element_keyword`, `relationship_operator`, etc.).

    For ambiguous captures where the same identifier-shape resolves to different categories (e.g., qualified_name segments are sometimes namespaces and sometimes type references), use grammar fields to discriminate: `(element_def name: (identifier) @type.definition)` vs `(qualified_name . (identifier) @namespace)`. If the grammar lacks a needed discriminator (because the lean approach skipped it), pick the most-common interpretation and document the trade-off in highlights.scm comments — visual parity in §8 is the bar, not 100% semantic accuracy (RESEARCH §9 trade-off).

    Authoring discipline: every assignment in highlights.scm must be reachable by some node in grammar.js (`tree-sitter test` will detect orphan captures). Run `npx tree-sitter test` iteratively while writing.

    Corpus files: each file = multiple test cases separated by the `==========` heading delimiters per RESEARCH §3 lines 343–363. The sexpr-form expected tree below each `---` MUST match the actual tree-sitter parse — generate by running `npx tree-sitter parse <inline>` and copy-pasting the output (no manual sexpr crafting).

    showcase-snapshot.txt strategy: pick 8 representative excerpts (one per package/folder of the showcase, e.g., one from `packages/vehicle/electrical/BatteryPack.deal`, one from `model/SedanProject.dealx`, etc.). Each excerpt is 5–15 lines; expected sexpr is what `tree-sitter parse` produces. The point is regression coverage, not exhaustive showcase mirroring (that responsibility is covered by `scripts/showcase-parse.sh`).

    scripts/showcase-parse.sh: bash script with `set -euo pipefail`; resolves the showcase path via `tree-sitter-deal/../spec/examples/showcase` (sibling-package relative); iterates `find -type f \( -name '*.deal' -o -name '*.dealx' \)`; runs `npx tree-sitter parse --quiet <file>`; counts ERROR occurrences in output; exits non-zero on any error. The Plan 07 phase gate calls this script.

    queries/indents.scm authoring: emit the 4-line block per RESEARCH §3 lines 320–331 verbatim, with `(composition_tag_block)` substituted by whatever the actual grammar rule name is from Task 1.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/tree-sitter-deal && npx tree-sitter generate && npx tree-sitter test && bash scripts/showcase-parse.sh && grep -cE '@keyword\.element|@type\.definition|@operator\.relationship|@attribute\.annotation|@tag\.composition|@number\.multiplicity|@comment\.documentation|@namespace|@punctuation\.bracket' queries/highlights.scm | awk '{ if ($1 < 9) { print "highlights.scm capture coverage too low: " $1; exit 1 } else print "highlights.scm coverage OK: " $1 }'</automated>
  </verify>
  <done>
    `npx tree-sitter test` passes on all 6 corpus files; `scripts/showcase-parse.sh` exits 0 across all 19 showcase files (no ERROR nodes); highlights.scm contains all 20 §8 @captures (at minimum 9 distinctive ones verified by grep); indents.scm + injections.scm exist with correct shape.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry ↔ tree-sitter-deal | `tree-sitter-cli` is the sole dependency (devDep); compromise affects parser generation at build time. |
| Showcase corpus (spec/examples/showcase) ↔ grammar tests | Showcase files are read-only inputs from a git submodule; tampering would invalidate test gates but is detected by git submodule SHA. |
| Generated src/parser.c ↔ downstream consumers (Neovim/Helix/Zed/GitHub web) | Compromised parser would mis-tokenize user code in third-party editors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | Tampering (supply-chain — npm) | tree-sitter-cli devDependency | mitigate | Pin tree-sitter-cli to exact version (no semver range); verify publisher is `tree-sitter` org via `npm view tree-sitter-cli` before install. |
| T-3-06 | Denial-of-Service | tree-sitter generated parser | mitigate | Lean grammar (RESEARCH §9) keeps PEG choice points minimal; corpus tests in this plan cover the 19-file showcase as a regression baseline; pathological-input tests can be added in Phase 3.x if a real DoS emerges. |

**T-3-SC supply-chain checkpoint:** Per RESEARCH §1228 planner action, run `slopcheck install tree-sitter-cli` before `npm install`. Fallback: `npm view tree-sitter-cli` confirms publisher = `tree-sitter` org and last-publish date is recent (8+ year package history per audit). `tree-sitter-cli` is `[ASSUMED]` per audit; not `[SUS]` or `[SLOP]`.
</threat_model>

<verification>
- `cd /Users/dunnock/projects/deal-lang/tree-sitter-deal && npm install` exits 0.
- `cd /Users/dunnock/projects/deal-lang/tree-sitter-deal && npx tree-sitter generate` produces src/parser.c.
- `cd /Users/dunnock/projects/deal-lang/tree-sitter-deal && npx tree-sitter test` exits 0 (all corpus tests pass).
- `cd /Users/dunnock/projects/deal-lang/tree-sitter-deal && bash scripts/showcase-parse.sh` exits 0 (all 19 showcase files parse without ERROR).
- Grep gate: `grep -v '^;' queries/highlights.scm | grep -cE '@keyword|@type|@operator|@attribute|@tag|@number|@comment|@namespace|@string|@punctuation|@property|@parameter'` ≥ 18 (every category from §8 represented).
</verification>

<success_criteria>
- REQ-phase-3-2-treesitter satisfied: grammar.js + 3 query files + 6 corpus files + showcase-parse smoke + npm package.
- D-41 honored: highlights.scm @captures parallel the §8 table; visual parity with vscode-deal/COLOR-CATEGORIES.md is testable by Plan 07's gate.
- RESEARCH §9 lean-grammar trade-off honored: grammar.js LOC in 200–1200 range; structural rules limited to what highlights.scm and indents.scm need.
- Linguist submission (RESEARCH §3 lines 365–371) is unblocked but deferred to Phase 4 per Open Question 3.
- Plan 07's `phase-3-gate` `tree-sitter test` step has a passing baseline.
</success_criteria>

<output>
Create `.planning/phases/03-editor-intelligence/03-02-SUMMARY.md` on completion with: grammar.js LOC count, corpus test count, showcase parse coverage (19/19), highlights.scm @capture count, and any RESEARCH §9 trade-offs deferred (e.g., property_ref disambiguation, action_def parameter modeling).
</output>
