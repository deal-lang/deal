---
phase: 04-ecosystem
plan: 09
type: execute
wave: 5
depends_on:
  - 04-04
  - 04-05
  - 04-06
  - 04-07
  - 04-08
files_modified:
  - build.zig
  - scripts/verify-fresh-worktree.sh
  - scripts/phase-4-smoke.sh
  - spec/grammar/tmp-references/DESIGN-DECISIONS.md
  - spec/grammar/DESIGN-DECISIONS.md
  - spec/grammar/README.md
autonomous: false
requirements:
  - REQ-phase-4-gate
  - REQ-phase-4-ecosystem
user_setup: []

must_haves:
  truths:
    - "`zig build phase-4-gate` runs the full Phase 4 exit suite: Zig tests + cargo workspace + stdlib check + init/install/check/build E2E + docs build + snippet gate, inheriting phase-3-gate"
    - "`zig build phase-4-gate-fresh` runs phase-4-gate inside an ephemeral worktree with deal-stdlib + deal-lang.org siblings symlinked (ADR-phase-1.5)"
    - "The end-to-end new-user flow works: deal init → deal install → deal check → deal build --target sysml-v2 AND --target reqif all exit 0 on a freshly scaffolded project"
    - "The carried-over closeout tasks are done: DESIGN-DECISIONS.md promoted out of tmp-references/, spec/grammar/README.md line-count drift fixed"
  artifacts:
    - path: "build.zig"
      provides: "phase-4-gate + phase-4-gate-fresh steps (mirrors phase-3-gate)"
      contains: "phase-4-gate"
    - path: "scripts/phase-4-smoke.sh"
      provides: "deal init → install → check → build both targets E2E smoke (D-69 new-user moment)"
    - path: "scripts/verify-fresh-worktree.sh"
      provides: "Sibling symlink list extended with deal-stdlib + deal-lang.org"
  key_links:
    - from: "build.zig"
      to: "scripts/phase-4-smoke.sh"
      via: "gate_4_init_smoke addSystemCommand runs the E2E smoke"
      pattern: "phase-4-smoke"
    - from: "scripts/verify-fresh-worktree.sh"
      to: "deal-stdlib"
      via: "sibling symlink materialization for the fresh gate"
      pattern: "deal-stdlib"
---

<objective>
Wire the Phase 4 exit gate (REQ-phase-4-gate): `phase-4-gate` + `phase-4-gate-fresh` build steps that prove the integrated minimum-viable-language end-to-end — `deal init` → `deal install` → `deal check` → `deal build --target sysml-v2` AND `--target reqif`, plus the deal-stdlib check, the docs build, and the snippet gate — inheriting phase-3-gate (D-35 cumulative). Extend the fresh-worktree script with the new sibling repos (ADR-phase-1.5), and clear the two carried-over Phase 2 closeout tasks (promote DESIGN-DECISIONS.md, fix README line-count drift).

Purpose: REQ-phase-4-gate is the minimum-viable-language gate and REQ-phase-4-ecosystem is the umbrella integration requirement (a real project authored end-to-end across all four streams). This plan delivers the integration verification that makes Success Criterion 5 true and closes the phase.
Output: extended `build.zig`, `scripts/phase-4-smoke.sh`, extended `scripts/verify-fresh-worktree.sh`, promoted DESIGN-DECISIONS.md, fixed README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ecosystem/04-CONTEXT.md
@.planning/phases/04-ecosystem/04-RESEARCH.md
@.planning/phases/04-ecosystem/04-PATTERNS.md
@.planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md
@.planning/phases/04-ecosystem/04-04-SUMMARY.md
@.planning/phases/04-ecosystem/04-06-SUMMARY.md

<interfaces>
<!-- build.zig phase-3-gate block (lines 322-407 — the exact analog; PATTERNS.md §build.zig):
   gate_3_step = b.step("phase-3-gate", "..."); gate_3_step.dependOn(gate_2_step);  // cumulative inherit
   addSystemCommand for each sub-step (cargo build/test --workspace; cd ../sibling && npm ...; bash scripts/phase-3-smoke.sh)
   gate_3_fresh_step = b.step("phase-3-gate-fresh", "..."); addSystemCommand bash scripts/verify-fresh-worktree.sh phase-3-gate.
   Phase 4 sub-steps (PATTERNS.md lines 410-440):
     (N)  cargo test --workspace        (reqif + resolver integration tests)
     (N+1) cd ../deal-stdlib && deal check packages/   (stdlib parses+checks)
     (N+2) bash scripts/phase-4-smoke.sh               (init→install→check→build E2E)
     (N+3) cd ../deal-lang.org && npm ci && npm run build
     (N+4) cd ../deal-lang.org && bash scripts/check-snippets.sh -->

<!-- verify-fresh-worktree.sh sibling loop (PATTERNS.md lines 461-477):
   for sibling in tree-sitter-deal vscode-deal deal-stdlib deal-lang.org; do ln -sfn ...; done
   EXIT trap extended to rm the new symlinks. The gate only READS siblings (does not mutate). -->

<!-- D-69 new-user smoke (04-CONTEXT §specifics line 156): scaffold into a temp dir, install, check,
   build BOTH targets. `deal check` MUST pass immediately after init+install. -->

<!-- Closeout (04-CONTEXT canonical_refs lines 83/91 + specifics line 158):
   (a) promote spec/grammar/tmp-references/DESIGN-DECISIONS.md -> spec/grammar/DESIGN-DECISIONS.md (flagged since Phase 2)
   (b) fix spec/grammar/README.md 370->758 line-count drift (flagged since Phase 2). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/phase-4-smoke.sh — deal init → install → check → build both targets E2E</name>
  <files>scripts/phase-4-smoke.sh</files>
  <read_first>
    - scripts/phase-3-smoke.sh (the analog smoke script structure — temp dir, deal binary invocation, cleanup trap)
    - cli/src/main.rs (init/install/check/build subcommand surface + the DEFAULT_STDLIB_TAG and deal-std git dependency wiring from Plan 02)
    - .planning/phases/04-ecosystem/04-CONTEXT.md D-69 (full new-user flow) + D-67 (deal-std git dependency)
    - .planning/phases/04-ecosystem/04-02-SUMMARY.md + 04-03-SUMMARY.md (the deal-std tag wired into init; the stdlib units the scaffold imports)
  </read_first>
  <action>
    Write `scripts/phase-4-smoke.sh` mirroring `scripts/phase-3-smoke.sh` structure (strict bash: `set -euo pipefail`; mktemp working dir; EXIT trap cleanup). Build the deal binary if needed (`cargo build -p deal --release`) and use `DEAL_BIN="${DEAL_BIN:-$(pwd)/target/release/deal}"`. Steps:
    1. `deal init smoke-proj` in the temp dir → assert `smoke-proj/deal.toml` exists with `[dependencies] deal-std = { git = ... }`.
    2. `cd smoke-proj && deal install` → assert `.deal/deps/deal-std/` is populated and `deal.lock` exists (resolves the deal-stdlib sibling — use a `file://` or local-path override of the deal-std dependency so the smoke does NOT require network: point the scaffolded dependency at the local `../deal-stdlib` via a path dep or a `file://` git URL so the gate is air-gap-safe and deterministic).
    3. `deal check` → assert exit 0 (D-69: check passes immediately after init+install; the starter model authored by Plan 02/06's init scaffold + its imports of deal.std.units resolve cleanly).
    4. `deal build --target sysml-v2` → assert exit 0 + output JSON produced.
    5. `deal build --target reqif` → assert exit 0 + `.reqifz` produced (Plan 06 emitter).
    Each failing step exits non-zero with a clear message. The script is the literal D-69 new-user moment and the REQ-phase-4-gate acceptance evidence.
    NOTE on the starter model: if Plan 02's `deal init` scaffolds only the directory tree + deal.toml (model files deferred), this smoke must still produce a check-clean project — author a minimal real starter model file inside the smoke (or confirm init now scaffolds one) so `deal check` passes. Coordinate with the 04-02-SUMMARY: if the starter model files are NOT yet scaffolded by init, add them to `deal init` here is OUT OF SCOPE (that file is owned by Plan 02) — instead the smoke writes a minimal `packages/example.deal` into the scaffolded project before `deal check`. Record this in the SUMMARY as a gap for a follow-up if init should own the starter model.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && cargo build -p deal --release 2>&1 | tail -2 && bash scripts/phase-4-smoke.sh 2>&1 | tail -15 && echo SMOKE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/phase-4-smoke.sh` exits 0
    - The smoke runs `deal init`, `deal install`, `deal check`, `deal build --target sysml-v2`, `deal build --target reqif` in sequence and asserts each exits 0
    - The deal-std dependency resolves WITHOUT network (local path or file:// to ../deal-stdlib) so the smoke is air-gap-safe
    - `deal check` exits 0 on the scaffolded+installed project (D-69)
    - Both a SysML v2 JSON output and a `.reqifz` output are produced and asserted present
  </acceptance_criteria>
  <done>The end-to-end new-user smoke (init→install→check→build both targets) passes offline against the deal-stdlib sibling — REQ-phase-4-gate's core E2E evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add phase-4-gate + phase-4-gate-fresh to build.zig; extend sibling symlinks</name>
  <files>build.zig, scripts/verify-fresh-worktree.sh</files>
  <read_first>
    - build.zig (lines 322-407 — phase-3-gate + phase-3-gate-fresh block, the exact analog)
    - scripts/verify-fresh-worktree.sh (lines ~163-177 — sibling symlink loop + EXIT trap)
    - scripts/phase-4-smoke.sh (Task 1 — invoked by the gate)
    - .planning/phases/04-ecosystem/04-PATTERNS.md §`build.zig` + §`scripts/verify-fresh-worktree.sh`
    - .planning/decisions/ADR-phase-1.5-fresh-worktree-verification.md (the binding fresh-gate invariant)
  </read_first>
  <action>
    In `build.zig`, after the phase-3-gate block, add a `gate_4_step = b.step("phase-4-gate", "Run the full Phase 4 exit-gate suite (Zig + cargo + stdlib + reqif + docs + init/install E2E)")` that `dependOn(gate_3_step)` (D-35 cumulative inherit). Add `addSystemCommand` sub-steps per PATTERNS.md lines 410-440:
    - cargo build + test `--workspace` (covers reqif + resolver integration tests).
    - `bash -c "cd ../deal-stdlib && cargo run --manifest-path ../deal/Cargo.toml -p deal -- check packages/"` (stdlib units + interfaces parse+check clean).
    - `bash scripts/phase-4-smoke.sh` (the Task-1 E2E smoke).
    - `bash -c "cd ../deal-lang.org && npm ci && npm run build"` (docs build).
    - `bash -c "cd ../deal-lang.org && DEAL_BIN=../deal/target/release/deal bash scripts/check-snippets.sh"` (snippet + Shiki scope gate from Plan 08).
    Add `gate_4_fresh_step = b.step("phase-4-gate-fresh", "...per ADR-phase-1.5...")` that `addSystemCommand` `bash scripts/verify-fresh-worktree.sh phase-4-gate` (copy the phase-3-gate-fresh block, change the gate name).
    In `scripts/verify-fresh-worktree.sh`, extend the sibling-symlink loop to `for sibling in tree-sitter-deal vscode-deal deal-stdlib deal-lang.org; do ...` (PATTERNS.md lines 461-477) and extend the EXIT trap to remove the two new symlinks. The gate only reads the siblings.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && grep -q 'phase-4-gate' build.zig && grep -q 'phase-4-gate-fresh' build.zig && grep -q 'deal-stdlib' scripts/verify-fresh-worktree.sh && grep -q 'deal-lang.org' scripts/verify-fresh-worktree.sh && zig build phase-4-gate 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `zig build phase-4-gate` exits 0 (full suite green: cargo + stdlib check + smoke + docs build + snippet gate, inheriting phase-3-gate)
    - `build.zig` declares both `phase-4-gate` and `phase-4-gate-fresh` steps
    - `phase-4-gate` `dependOn(gate_3_step)` (cumulative inherit — grep confirms the dependency)
    - `scripts/verify-fresh-worktree.sh` sibling loop includes `deal-stdlib` and `deal-lang.org`, and the EXIT trap removes both
    - `zig build phase-4-gate-fresh` exits 0 in the ephemeral worktree (siblings symlinked, ADR-phase-1.5)
  </acceptance_criteria>
  <done>phase-4-gate + phase-4-gate-fresh are wired, inherit phase-3-gate, exercise all four streams end-to-end, and pass in both the normal and fresh-worktree forms.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Closeout — promote DESIGN-DECISIONS.md, fix README drift, confirm phase-4-gate-fresh green</name>
  <files>spec/grammar/tmp-references/DESIGN-DECISIONS.md, spec/grammar/DESIGN-DECISIONS.md, spec/grammar/README.md</files>
  <read_first>
    - spec/grammar/tmp-references/DESIGN-DECISIONS.md (the file to promote — flagged since Phase 2)
    - spec/grammar/README.md (the 370→758 line-count drift to fix — flagged since Phase 2)
    - .planning/phases/04-ecosystem/04-CONTEXT.md §canonical_refs (the two carried-over closeout tasks) + §specifics line 158
  </read_first>
  <action>
    Promote `spec/grammar/tmp-references/DESIGN-DECISIONS.md` to `spec/grammar/DESIGN-DECISIONS.md` (git-move it out of `tmp-references/`; update any in-repo references to the old path — grep for `tmp-references/DESIGN-DECISIONS` and fix). This was earmarked "during Phase 4 docs work" since Phase 2.
    Fix the `spec/grammar/README.md` line-count drift: the README references a grammar line count of 370 that has drifted to the actual ~758; update the README to the correct current line count(s) of the grammar files (verify the real count with `wc -l spec/grammar/deal.ebnf` etc. and update the README figures to match reality).
    This is a `checkpoint:human-verify` (gate="blocking") because phase closeout requires a human to confirm the integration gate is genuinely green and the two long-carried closeout debts are cleared before the phase is declared complete (the false-GREEN risk that ADR-phase-1.5 exists to prevent — a human eyeballs `zig build phase-4-gate-fresh` output and the promoted/fixed docs). The human runs `zig build phase-4-gate-fresh` and confirms exit 0, then confirms DESIGN-DECISIONS.md is promoted and README line counts match `wc -l`.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && test -f spec/grammar/DESIGN-DECISIONS.md && test ! -f spec/grammar/tmp-references/DESIGN-DECISIONS.md && ! grep -rq 'tmp-references/DESIGN-DECISIONS' spec/ .planning/ 2>/dev/null && echo CLOSEOUT_FILES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `spec/grammar/DESIGN-DECISIONS.md` exists and `spec/grammar/tmp-references/DESIGN-DECISIONS.md` no longer exists (promoted)
    - No remaining references to `tmp-references/DESIGN-DECISIONS` in `spec/` or `.planning/`
    - `spec/grammar/README.md` line-count figures match the actual `wc -l` of the grammar files (no stale 370 figure)
    - Human confirms `zig build phase-4-gate-fresh` exits 0 before resume
  </acceptance_criteria>
  <done>The two carried-over Phase 2 closeout debts are cleared; the integration gate is human-confirmed green in the fresh worktree; Phase 4 is ready to close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ephemeral worktree → sibling repos | The fresh gate symlinks sibling repos it reads but must not mutate |
| CI binary download → gate | The deal binary used by the docs snippet gate in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-21 | Tampering | false-GREEN gate — dev-local untracked files mask failures | mitigate | `phase-4-gate-fresh` runs in an ephemeral worktree with submodule init (ADR-phase-1.5); the binding invariant against false-GREEN; human confirms exit 0 |
| T-4-22 | Tampering | fresh gate mutates a sibling repo via symlink | mitigate | The gate only READS siblings (deal check / npm build / parse); EXIT trap removes the symlinks; documented read-only contract |
| T-4-23 | Denial of Service | smoke `deal install` hangs on network for the deal-std git dependency | mitigate | The smoke uses a local-path/file:// override for deal-std (Task 1) so it resolves offline — no network dependency in the gate |
</threat_model>

<verification>
- `zig build phase-4-gate` exits 0 (cargo + stdlib check + E2E smoke + docs build + snippet gate, inheriting phase-3-gate)
- `zig build phase-4-gate-fresh` exits 0 in an ephemeral worktree with deal-stdlib + deal-lang.org symlinked
- `bash scripts/phase-4-smoke.sh` exits 0 (init→install→check→build both targets, offline)
- DESIGN-DECISIONS.md promoted; README line-count drift fixed
</verification>

<success_criteria>
- REQ-phase-4-gate: the full minimum-viable-language exit gate passes in both normal and fresh-worktree forms
- REQ-phase-4-ecosystem: a real project is authored end-to-end (init→author→check→export SysML v2 JSON + ReqIF XML) with readable docs — the integrated capability across all four streams (Success Criterion 5)
- The two carried-over Phase 2 closeout debts are cleared
</success_criteria>

<output>
Create `.planning/phases/04-ecosystem/04-09-SUMMARY.md` when done. Record the phase-4-gate sub-step list and whether `deal init` should own the starter model (if the smoke had to author it).
</output>
