---
phase: 04-ecosystem
plan: 08
type: execute
wave: 4
depends_on:
  - 04-07
  - 04-04
  - 04-06
files_modified:
  - ../deal-lang.org/src/content/docs/reference/definitions.mdx
  - ../deal-lang.org/src/content/docs/reference/compositions.mdx
  - ../deal-lang.org/src/content/docs/reference/requirements.mdx
  - ../deal-lang.org/src/content/docs/reference/traceability.mdx
  - ../deal-lang.org/src/content/docs/reference/imports.mdx
  - ../deal-lang.org/src/content/docs/reference/annotations.mdx
  - ../deal-lang.org/src/content/docs/reference/units.mdx
  - ../deal-lang.org/src/content/docs/reference/deal-toml.mdx
  - ../deal-lang.org/src/content/docs/cli/overview.mdx
  - ../deal-lang.org/src/content/docs/cli/vscode-setup.mdx
  - ../deal-lang.org/scripts/extract_deal_blocks.py
  - ../deal-lang.org/scripts/check-snippets.sh
  - ../deal-lang.org/.github/workflows/deploy.yml
autonomous: true
requirements:
  - REQ-phase-4-3-docs-site
user_setup: []

must_haves:
  truths:
    - "The 8 language-reference pages and 2 CLI pages exist, each showcase-derived and parse-clean (D-62)"
    - "check-snippets.sh extracts every .deal/.dealx fenced block and runs `deal parse`, skipping error-expected blocks (D-65)"
    - "The Shiki scope gate fails the build when a .deal/.dealx block renders with zero highlighted tokens (UI-SPEC)"
    - "The deploy workflow runs the snippet parse gate in CI against the real deal binary"
    - "`npm run build` plus check-snippets.sh both pass on the full docs site"
  artifacts:
    - path: "../deal-lang.org/src/content/docs/reference/units.mdx"
      provides: "Units + dimensional algebra reference (D-55..D-57), examples from deal-stdlib"
    - path: "../deal-lang.org/scripts/check-snippets.sh"
      provides: "D-65 parse gate: extract + deal parse every snippet"
    - path: "../deal-lang.org/scripts/extract_deal_blocks.py"
      provides: "Fenced-block extractor honoring the error-expected marker + Shiki scope gate"
    - path: "../deal-lang.org/.github/workflows/deploy.yml"
      provides: "CI snippet gate step wired into the deploy workflow"
  key_links:
    - from: "../deal-lang.org/scripts/check-snippets.sh"
      to: "deal parse"
      via: "runs the deal binary on each extracted snippet"
      pattern: "parse"
    - from: "../deal-lang.org/.github/workflows/deploy.yml"
      to: "../deal-lang.org/scripts/check-snippets.sh"
      via: "CI build job invokes the snippet gate"
      pattern: "check-snippets"
---

<objective>
Complete the `deal-lang.org` docs site (REQ-phase-4-3): author the 8 language-reference topic pages + 2 CLI pages (all showcase-derived, D-62), and implement the D-65 CI snippet gate (extract every `.deal`/`.dealx` fenced block, run `deal parse`, skip `error-expected` blocks) plus the Shiki scope gate (fail on unhighlighted fences), wired into the deploy workflow.

Purpose: Plan 07 delivered the site skeleton + landing/getting-started + Shiki grammar loading. This plan delivers the reference content AND the two CI gates that make Success Criterion 3 true ("every code block renders with actual Shiki syntax highlighting; CI fails on placeholders or missing scopes") and enforces D-65 (docs never drift from the grammar).
Output: 10 reference/CLI pages, `extract_deal_blocks.py`, `check-snippets.sh`, deploy.yml snippet-gate step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ecosystem/04-CONTEXT.md
@.planning/phases/04-ecosystem/04-RESEARCH.md
@.planning/phases/04-ecosystem/04-PATTERNS.md
@.planning/phases/04-ecosystem/04-UI-SPEC.md
@.planning/phases/04-ecosystem/04-07-SUMMARY.md
@spec/examples/showcase/packages/requirements/system.deal

<interfaces>
<!-- Sidebar slugs (set in Plan 07 astro.config.mjs / UI-SPEC §Sidebar Structure):
   reference/{definitions,compositions,requirements,traceability,imports,annotations,units,deal-toml}
   cli/{overview,vscode-setup}. Each page's frontmatter `title` matches the sidebar label. -->

<!-- D-62 page content sources (the 19-file showcase is the oracle — tests/showcase/):
   definitions  -> part def / port def / attribute def  (showcase packages/vehicle/*, interfaces/*)
   compositions -> .dealx composition files
   requirements -> requirement def + verification {} (showcase packages/requirements/system.deal)
   traceability -> @trace:<<satisfies>>/<<derives from>>, [<satisfy>]/[<allocate>] (showcase requirements)
   imports      -> the five PS-4 import forms + deal.toml [dependencies] resolution
   annotations  -> @header/@confidence/@rationale/@assumes/@simulation (showcase)
   units        -> deal.std.units + dimensional algebra D-55..D-57 (examples from deal-stdlib units package, Plan 03)
                   + E2500/E2501 error examples MARKED ```deal error-expected (they intentionally fail check, not parse)
   deal-toml    -> [project]/[workspace]/[dependencies]/[build.targets] (showcase deal.toml) -->

<!-- CLI pages: overview = deal init/install/check/fmt/build (--target sysml-v2|reqif) with the exact
   UI-SPEC §CLI Copywriting messages; vscode-setup = installing the vscode-deal extension (Phase 3 / D-50..D-53 releases). -->

<!-- D-65 snippet gate (RESEARCH §Code Examples lines 921-943 + Docs CI Snippet Gate lines 766-772):
   check-snippets.sh extracts every ```deal/```dealx block (NOT marked error-expected) and runs `deal parse`.
   extract_deal_blocks.py is the extractor (RESEARCH script references scripts/extract_deal_blocks.py).
   Fence markers (UI-SPEC §Fence Markers): ```deal must parse; ```deal error-expected -> CI SKIPS parse.
   Shiki scope gate (UI-SPEC §Shiki Scope Gate + RESEARCH OQ-2): a post-build check over dist/ HTML —
   any deal/dealx code block where every token span carries NO scope class (all plain) = grammar not loaded = FAIL.
   DEAL_BIN env var selects the binary (default `deal`); CI downloads it from GitHub Releases (RESEARCH Pattern 4 lines 459-462). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 8 language-reference pages from showcase examples</name>
  <files>../deal-lang.org/src/content/docs/reference/definitions.mdx, ../deal-lang.org/src/content/docs/reference/compositions.mdx, ../deal-lang.org/src/content/docs/reference/requirements.mdx, ../deal-lang.org/src/content/docs/reference/traceability.mdx, ../deal-lang.org/src/content/docs/reference/imports.mdx, ../deal-lang.org/src/content/docs/reference/annotations.mdx, ../deal-lang.org/src/content/docs/reference/units.mdx, ../deal-lang.org/src/content/docs/reference/deal-toml.mdx</files>
  <read_first>
    - .planning/phases/04-ecosystem/04-UI-SPEC.md §Sidebar Structure + §Code Block Contract (fence markers/language IDs) + §Copywriting
    - spec/examples/showcase/packages/requirements/system.deal (requirements/traceability/annotations examples)
    - spec/examples/showcase/packages/interfaces/electrical.deal (definitions: part/port/interface def examples)
    - spec/examples/showcase/deal.toml (deal-toml page source)
    - ../deal-stdlib/packages/units/si.deal + dimensions.deal (units page examples — Plan 03's authored units)
    - .planning/decisions/ADR-phase-4-dimension-metadata-syntax.md (units page: explain the dimensional metadata encoding)
    - .planning/phases/04-ecosystem/04-CONTEXT.md D-55..D-57 (dimensional algebra rules for the units page)
  </read_first>
  <action>
    Author 8 MDX pages under `src/content/docs/reference/`, each with Starlight frontmatter `title:` matching the UI-SPEC sidebar label, and EVERY code example LIFTED from or DERIVED FROM the 19-file showcase / the Plan-03 deal-stdlib units (D-62 — real, parseable):
    - `definitions.mdx`: `part def`, `port def`, `interface def`, `attribute def` — examples from showcase interfaces/vehicle files.
    - `compositions.mdx`: `.dealx` composition syntax (use a `dealx` fence); derive from a showcase composition file.
    - `requirements.mdx`: `requirement def` / `need def` + `verification {}` blocks (threshold/operator/accepts/rejects) — from system.deal.
    - `traceability.mdx`: `@trace:<<satisfies>>` / `<<derives from>>` and `[<satisfy>]` / `[<allocate>]` — from system.deal.
    - `imports.mdx`: the five PS-4 import forms + how `import deal.std.units.{kg}` resolves through `deal.toml [dependencies]` (D-66 vendoring).
    - `annotations.mdx`: `@header`, `@confidence`, `@rationale`, `@assumes`, `@simulation` — from showcase.
    - `units.mdx`: `deal.std.units` usage + the dimensional algebra rules (D-55 derived-unit arithmetic e.g. `km(200) / hr(1)` → Speed; D-56 data-driven; D-57 explicit conversions only). Include at least one INTENTIONAL-ERROR example (e.g. `mass : Mass = V(800)` → E2500, or a mixed-unit comparison → E2501) marked with a `deal error-expected` fence meta so the parse gate skips it (these fail CHECK, not PARSE, but mark them error-expected to document the diagnostic; explain the E-code in prose).
    - `deal-toml.mdx`: `[project]`/`[workspace]`/`[dependencies]`/`[build.targets]` from showcase deal.toml; show the `deal-std` git dependency form (D-67).
    Use the correct Shiki language IDs per UI-SPEC §Language Identifiers (`deal`, `dealx`, `toml`, `bash`, `xml`). All non-error `deal`/`dealx` blocks MUST parse with `deal parse` (Task 3 enforces). Keep examples focused and minimal.
  </action>
  <verify>
    <automated>cd ../deal-lang.org && ls src/content/docs/reference/*.mdx | wc -l | grep -q 8 && npm run build 2>&1 | tail -8 && echo REF_PAGES_OK</automated>
  </verify>
  <acceptance_criteria>
    - All 8 reference pages exist (`ls src/content/docs/reference/*.mdx` returns 8 files)
    - `cd ../deal-lang.org && npm run build` exits 0 with the reference pages
    - `units.mdx` covers D-55 derived-unit arithmetic AND contains at least one `deal error-expected` block for a dimensional error (E2500 or E2501)
    - `requirements.mdx` contains a `verification {` block lifted from the showcase
    - `deal-toml.mdx` shows the `deal-std = { git = ` dependency form
    - Each page's title matches its UI-SPEC sidebar label
  </acceptance_criteria>
  <done>The 8 language-reference pages are authored from real showcase/stdlib examples, build cleanly, and use the correct Shiki language IDs + fence markers.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 2 CLI reference pages (overview + VS Code setup)</name>
  <files>../deal-lang.org/src/content/docs/cli/overview.mdx, ../deal-lang.org/src/content/docs/cli/vscode-setup.mdx</files>
  <read_first>
    - .planning/phases/04-ecosystem/04-UI-SPEC.md §CLI Copywriting Contract (exact messages) + §Sidebar Structure
    - cli/src/main.rs (the actual subcommand surface: parse/check/fmt/build/init/install — overview must match reality)
    - .planning/phases/04-ecosystem/04-CONTEXT.md D-50..D-53 (vscode-deal release pipeline — install instructions) + D-69 (init flow)
    - .planning/phases/04-ecosystem/04-07-SUMMARY.md (installation page already authored in Plan 07 — link, do not duplicate)
  </read_first>
  <action>
    Author `cli/overview.mdx`: document each `deal` subcommand — `deal init`, `deal install`, `deal check`, `deal fmt`, `deal parse`, `deal build --target sysml-v2|reqif` — using `bash` fences for invocations and the EXACT UI-SPEC §CLI Copywriting output messages (e.g. `deal init: project '{name}' created in ./{name}/`, `deal install: {N} dependencies resolved, deal.lock updated`, `error[E2402]: dependency '{name}' not resolved — run 'deal install'`, `deal build: wrote build/reqif/model.reqifz ({N} requirements, {M} relations)`). Match the subcommands that actually exist in `cli/src/main.rs` (do not document flags that do not exist).
    Author `cli/vscode-setup.mdx`: how to install the `vscode-deal` extension (from the marketplace / GitHub Releases .vsix per D-50..D-53), what it provides (syntax highlighting, LSP diagnostics, completion), and a forward link to the language reference.
    Any DEAL code shown uses `deal` / `dealx` fences (parse-clean) or a `bash` fence for commands. No unhighlighted fences.
  </action>
  <verify>
    <automated>cd ../deal-lang.org && ls src/content/docs/cli/overview.mdx src/content/docs/cli/vscode-setup.mdx && npm run build 2>&1 | tail -6 && grep -q 'deal init' src/content/docs/cli/overview.mdx && echo CLI_PAGES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cli/overview.mdx` and `cli/vscode-setup.mdx` both exist and the site builds
    - `overview.mdx` documents `deal init`, `deal install`, `deal check`, `deal build` and contains the exact UI-SPEC success/error message strings
    - `overview.mdx` does not document subcommands/flags absent from `cli/src/main.rs`
    - `vscode-setup.mdx` covers installing the vscode-deal extension
  </acceptance_criteria>
  <done>The CLI overview + VS Code setup pages are authored, match the real CLI surface + UI-SPEC copy, and build cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the snippet parse gate + Shiki scope gate, wire into deploy CI</name>
  <files>../deal-lang.org/scripts/extract_deal_blocks.py, ../deal-lang.org/scripts/check-snippets.sh, ../deal-lang.org/.github/workflows/deploy.yml</files>
  <read_first>
    - .planning/phases/04-ecosystem/04-RESEARCH.md §Code Examples (check-snippets.sh + extractor lines 921-943) + §Docs CI Snippet Gate (lines 766-772) + OQ-2 (Shiki scope gate mechanism)
    - .planning/phases/04-ecosystem/04-UI-SPEC.md §Fence Markers + §Shiki Scope Gate
    - .planning/phases/04-ecosystem/04-PATTERNS.md §`deal-lang.org/scripts/check-snippets.sh`
    - ../deal-lang.org/.github/workflows/deploy.yml (Plan 07's deploy workflow — append the gate step)
    - cli/src/main.rs (the `deal parse` subcommand contract — exit 0 on parse success)
  </read_first>
  <action>
    Write `scripts/extract_deal_blocks.py`: parse an MDX file, extract every fenced block whose info-string starts with `deal` or `dealx`, SKIP any block whose info-string includes the `error-expected` meta (UI-SPEC §Fence Markers), and write each remaining block to a temp `.deal`/`.dealx` file (by language), printing the temp paths. Determinism: process blocks in document order.
    Write `scripts/check-snippets.sh` (RESEARCH §Code Examples): `DEAL_BIN="${DEAL_BIN:-deal}"`; for every `src/content/docs/**/*.mdx`, run `extract_deal_blocks.py`, then `"$DEAL_BIN" parse` each extracted snippet; accumulate failures; exit non-zero if any snippet fails to parse (D-65 parse gate). Use a robust counter (avoid the subshell-loses-counter bug — accumulate failures into a temp file or use a process-substitution `while` loop, not a pipe-to-while that drops the count).
    Add the Shiki scope gate: after `npm run build`, scan `dist/**/*.html` for code blocks tagged as `deal`/`dealx` language where EVERY token span carries no scope/color class (all plain text) — that indicates the grammar did not load (UI-SPEC §Shiki Scope Gate, RESEARCH OQ-2 fallback: zero colored tokens = grammar not loaded). Implement this as a step in `check-snippets.sh` (or a sibling `check-highlighting.sh` invoked by it) that greps the built HTML and fails if a deal/dealx block has no highlighted tokens. Document the detection heuristic in a comment.
    Append a step to `.github/workflows/deploy.yml` build job (after the astro build): download the `deal` binary from GitHub Releases (RESEARCH Pattern 4 lines 459-462: `curl -sSL .../deal-linux-x86_64.tar.gz | tar xz`), set `DEAL_BIN`, and run `bash scripts/check-snippets.sh`. Build fails (and deploy is blocked) if the gate fails.
    Run the gate locally against the pages authored in Plan 07 + Tasks 1-2 using a locally-built `deal` (`DEAL_BIN=$(pwd)/../deal/target/release/deal`); fix any snippet that fails to parse (a failing snippet is a docs bug, NOT a waiver — fix the snippet).
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && cargo build -p deal --release 2>&1 | tail -2 && cd ../deal-lang.org && npm run build 2>&1 | tail -4 && DEAL_BIN="$(pwd)/../deal/target/release/deal" bash scripts/check-snippets.sh && echo SNIPPET_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/extract_deal_blocks.py` and `scripts/check-snippets.sh` exist
    - `DEAL_BIN=<local deal> bash scripts/check-snippets.sh` exits 0 (every non-error-expected snippet parses)
    - `check-snippets.sh` SKIPS blocks marked `error-expected` (verified by a fixture block that would fail parse but is marked error-expected → gate still passes)
    - The Shiki scope gate fails if a deal/dealx block renders with zero highlighted tokens (assert by temporarily breaking the grammar load → gate fails, then restore)
    - `.github/workflows/deploy.yml` invokes `check-snippets.sh` in the build job and downloads the deal binary
    - The counter does not flake: a deliberately broken snippet causes a non-zero exit (no subshell-counter loss)
  </acceptance_criteria>
  <done>The D-65 parse gate + Shiki scope gate are implemented, pass on the full docs site, and are wired into the deploy CI so the build fails on unparseable or unhighlighted snippets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX snippet content → deal parse | Docs code blocks are executed through the `deal parse` binary in CI |
| GitHub Releases binary → CI | The deal binary downloaded into CI is trusted to run the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-18 | Tampering | docs drift — a snippet silently stops matching the grammar | mitigate | check-snippets.sh runs `deal parse` on every block in CI (D-65); build fails on drift; this IS the docs oracle |
| T-4-19 | Tampering | error-expected marker abused to hide genuinely broken non-error snippets | mitigate | error-expected is only honored on intentional-error examples; reviewers see the marker in diff; the marker skips parse but the prose must explain the expected diagnostic |
| T-4-20 | Tampering | downloaded deal binary in CI replaced/corrupted | accept | CI downloads from the project's own GitHub Releases (same trust domain as the repo); release artifacts carry SHA256SUMS from the Phase 3 pipeline; pinning the release tag is a future hardening |
| T-4-SC | Tampering | `deal parse` running arbitrary snippet content | mitigate | `deal parse` is a local binary, reads files only, no network, bounded by grammar (RESEARCH §Security Domain) |
</threat_model>

<verification>
- All 10 reference/CLI pages exist and `npm run build` exits 0
- `DEAL_BIN=<local> bash scripts/check-snippets.sh` exits 0 (parse gate green)
- error-expected blocks are skipped; broken snippets fail the gate (no counter flake)
- Shiki scope gate fails on unhighlighted deal/dealx blocks
- deploy.yml runs the snippet gate in CI
</verification>

<success_criteria>
- The full language + CLI reference (10 pages) is authored from real showcase/stdlib examples (REQ-phase-4-3, D-62)
- The D-65 parse gate + Shiki scope gate enforce that every snippet parses and every fence highlights — CI fails otherwise (Success Criterion 3)
</success_criteria>

<output>
Create `.planning/phases/04-ecosystem/04-08-SUMMARY.md` when done. Record the Shiki scope-gate detection heuristic used.
</output>
