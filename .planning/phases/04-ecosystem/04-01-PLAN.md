---
phase: 04-ecosystem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/references/omg-reqif/reqif.xsd
  - spec/references/omg-reqif/driver.xsd
  - spec/references/omg-reqif/SHA256SUMS
  - spec/references/omg-reqif/README.md
  - src/diagnostics.zig
  - tests/golden/reqif/README.md
  - tests/golden/reqif/01-requirement-def.deal
  - tests/golden/reqif/01-requirement-def.expected.reqif
  - tests/golden/reqif/02-trace-relation.deal
  - tests/golden/reqif/02-trace-relation.expected.reqif
  - tests/unit/sema_dimensional.zig
  - tests/regressions/sema/07-dimensional-mismatch.deal
  - tests/regressions/sema/07-mixed-unit.deal
  - tests/regressions/sema/07-unknown-unit.deal
autonomous: false
requirements:
  - REQ-phase-4-2-reqif-codegen
  - REQ-phase-4-1-stdlib
user_setup: []

must_haves:
  truths:
    - "The OMG ReqIF 1.2 XSD validation target exists locally and is SHA256-verified (D-60)"
    - "Downstream plans can reference `Codes.e_dimension_mismatch` (E2500) and `Codes.e_dependency_not_resolved` (E2402) without compile errors — the E-code contract for Plans 02 and 04 is established"
    - "Wave 0 test scaffolds exist so downstream tasks have a failing-first target"
  artifacts:
    - path: "spec/references/omg-reqif/reqif.xsd"
      provides: "ReqIF 1.2 main XSD — the emitter's hard validation gate"
    - path: "spec/references/omg-reqif/SHA256SUMS"
      provides: "Tamper-detection manifest for the XSD bundle"
    - path: "src/diagnostics.zig"
      provides: "E2500..E2503 + E2402 code constants and band documentation"
      contains: "e_dimension_mismatch"
    - path: "tests/unit/sema_dimensional.zig"
      provides: "Dimensional algebra test harness (regression pins + showcase-clean loop)"
    - path: "tests/golden/reqif/01-requirement-def.deal"
      provides: "First ReqIF golden fixture input"
  key_links:
    - from: "src/diagnostics.zig"
      to: "tests/unit/sema_dimensional.zig"
      via: "Codes.e_dimension_mismatch constant referenced by regression pins"
      pattern: "e_dimension_mismatch"
---

<objective>
Establish the Phase 4 foundation: acquire the OMG ReqIF 1.2 XSD validation target (D-60, hard prerequisite — no emitter work starts before this exists), reserve the dimensional-algebra and dependency-resolution E-code bands in diagnostics.zig, and create the Wave 0 test scaffolds that every downstream plan needs as a failing-first target.

Purpose: D-60 makes XSD acquisition the gating first task; the Validation strategy lists five Wave 0 test gaps that block Nyquist-compliant downstream tasks. This plan removes both blockers.
Output: `spec/references/omg-reqif/` bundle + SHA256SUMS; reserved E-codes; ReqIF golden-fixture skeleton; dimensional sema test harness + regression fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ecosystem/04-CONTEXT.md
@.planning/phases/04-ecosystem/04-RESEARCH.md
@.planning/phases/04-ecosystem/04-PATTERNS.md

<interfaces>
<!-- Existing E-code band documentation block in src/diagnostics.zig (lines 8-19):
//!   E0001..E0099  lexer
//!   E2000..E2499  semantic (Phase 2 D-33), sub-banded by check
   New band to ADD (per D-33 extension, RESEARCH §E-Code Band Recommendation):
//!   E2500..E2599  semantic — dimensional algebra / unit checks (Phase 4)

   Existing Codes struct convention: `pub const e_<name> = "EXXXX";` with a doc comment.
   Existing semantic codes already present: e_name_not_found="E2000", e_type_mismatch="E2100",
   e_multiplicity_violation="E2200". Import-resolution band is E2400..E2499. -->

<!-- ReqIF XSD facts (RESEARCH §ReqIF Domain, VERIFIED 2026-06-05):
   Main XSD: https://www.omg.org/spec/ReqIF/20110401/reqif.xsd  (892 lines, application/xml)
   Driver XSD (XHTML import): http://www.omg.org/spec/ReqIF/20110402/driver.xsd
   Existing SHA256SUMS convention: spec/references/SHA256SUMS and the omg-sysml-v2 bundle. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Acquire and verify the OMG ReqIF 1.2 XSD bundle (D-60)</name>
  <files>spec/references/omg-reqif/reqif.xsd, spec/references/omg-reqif/driver.xsd, spec/references/omg-reqif/SHA256SUMS, spec/references/omg-reqif/README.md</files>
  <read_first>
    - spec/references/omg-sysml-v2/ (directory listing — the existing bundle convention to mirror)
    - spec/references/SHA256SUMS (existing manifest format)
    - .planning/phases/04-ecosystem/04-RESEARCH.md §ReqIF Domain (XSD URLs + facts)
  </read_first>
  <action>
    Download the OMG ReqIF 1.2 schema files into a new `spec/references/omg-reqif/` directory:
    (1) `reqif.xsd` from `https://www.omg.org/spec/ReqIF/20110401/reqif.xsd`;
    (2) `driver.xsd` (the XHTML import) from `http://www.omg.org/spec/ReqIF/20110402/driver.xsd` — this URL is referenced inside reqif.xsd via `xsd:import schemaLocation` and MUST be bundled offline (RESEARCH Pitfall 1; air-gap constraint).
    Compute SHA256 for both files and write `spec/references/omg-reqif/SHA256SUMS` in the same `<hash>  <filename>` format used by the existing manifests (use `shasum -a 256 reqif.xsd driver.xsd > SHA256SUMS` from inside the directory).
    Write `spec/references/omg-reqif/README.md` documenting: source URLs, OMG formal document number (formal/2016-07-01, ReqIF 1.2), acquisition date, and the offline-bundling rationale.
    This is a `checkpoint:human-verify` with `gate="blocking-human"` because it fetches external network artifacts (supply-chain — threat T-4-SC). The human confirms the downloaded XSD is the genuine OMG schema (correct namespace `http://www.omg.org/spec/ReqIF/20110401/reqif.xsd`, ~892 lines) before any emitter work proceeds.
  </action>
  <verify>
    <automated>cd spec/references/omg-reqif && shasum -a 256 -c SHA256SUMS</automated>
  </verify>
  <acceptance_criteria>
    - `spec/references/omg-reqif/reqif.xsd` exists and contains the string `http://www.omg.org/spec/ReqIF/20110401/reqif.xsd`
    - `spec/references/omg-reqif/driver.xsd` exists (the XHTML import target)
    - `cd spec/references/omg-reqif && shasum -a 256 -c SHA256SUMS` exits 0
    - `spec/references/omg-reqif/README.md` contains `formal/2016-07-01` and both source URLs
    - Human confirms the XSD is the genuine OMG ReqIF 1.2 schema before resume
  </acceptance_criteria>
  <done>The ReqIF XSD bundle exists, is SHA256-verified, documented, and human-confirmed as authentic — the emitter's validation target now exists per D-60.</done>
</task>

<task type="auto">
  <name>Task 2: Reserve dimensional-algebra and dependency-resolution E-codes in diagnostics.zig</name>
  <files>src/diagnostics.zig</files>
  <read_first>
    - src/diagnostics.zig (full file — current band documentation at lines 8-19 and the Codes struct)
  </read_first>
  <action>
    Extend the band-documentation comment block (currently ending at the E2400..E2499 import-resolution line) with one new line:
    `//!   E2500..E2599  semantic — dimensional algebra / unit checks (Phase 4 D-33 extension)`.
    Add the following code constants to the `Codes` struct, each with a doc comment, after the existing semantic codes:
    - `e_dependency_not_resolved = "E2402"` (import-resolution band; `[dependencies]` entry present in deal.toml but `.deal/deps/<name>/` absent — emitted by check, suggests `deal install`; per RESEARCH Pitfall 4)
    - `e_dimension_mismatch = "E2500"` (expected dimension ≠ actual, e.g. `mass : Mass = V(800)`; per D-55)
    - `e_mixed_unit_comparison = "E2501"` (mixed-unit same-dimension expression without explicit conversion; per D-57)
    - `e_unknown_unit = "E2502"` (unit literal not resolvable to a declared dimension)
    - `e_conversion_type_mismatch = "E2503"` (wrong source/target dimension in a conversion call)
    Do not implement any emission logic here — this plan only reserves the codes (Plan 04 emits them). Reference the regression fixture path `tests/regressions/sema/07-dimensional*.deal` in the band comment so the codes are traceable to their tests.
  </action>
  <verify>
    <automated>zig build test -Dtest-filter=diagnostics 2>&1 | tail -5; grep -c 'e_dimension_mismatch\|e_mixed_unit_comparison\|e_unknown_unit\|e_conversion_type_mismatch\|e_dependency_not_resolved' src/diagnostics.zig</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/diagnostics.zig | grep -c 'e_dimension_mismatch = "E2500"'` returns 1
    - src/diagnostics.zig contains `e_mixed_unit_comparison = "E2501"`, `e_unknown_unit = "E2502"`, `e_conversion_type_mismatch = "E2503"`, `e_dependency_not_resolved = "E2402"`
    - src/diagnostics.zig band comment contains `E2500..E2599`
    - `zig build` succeeds (the file still compiles)
  </acceptance_criteria>
  <done>E2402 + E2500..E2503 are declared constants with documentation; diagnostics.zig compiles; codes are reserved for Plan 04 to emit.</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 test scaffolds — ReqIF golden skeleton + dimensional sema harness</name>
  <files>tests/golden/reqif/README.md, tests/golden/reqif/01-requirement-def.deal, tests/golden/reqif/01-requirement-def.expected.reqif, tests/golden/reqif/02-trace-relation.deal, tests/golden/reqif/02-trace-relation.expected.reqif, tests/unit/sema_dimensional.zig, tests/regressions/sema/07-dimensional-mismatch.deal, tests/regressions/sema/07-mixed-unit.deal, tests/regressions/sema/07-unknown-unit.deal</files>
  <read_first>
    - tests/golden/sysml-v2/05-requirement-def.deal (analog input fixture)
    - tests/golden/sysml-v2/05-requirement-def.expected.json (analog expected-output shape)
    - tests/unit/sema_corpus.zig (analog test-harness structure)
    - tests/regressions/sema/06-import.deal (analog regression fixture)
    - spec/grammar/deal.ebnf (requirement def + verification block productions, to author valid .deal fixtures)
  </read_first>
  <action>
    Create the ReqIF golden-fixture directory with two minimal input/expected pairs:
    - `01-requirement-def.deal`: a single `requirement def` with a JSDoc doc comment and a `verification {}` block (mirror the structure of `tests/golden/sysml-v2/05-requirement-def.deal` — it MUST parse under the locked grammar).
    - `01-requirement-def.expected.reqif`: an empty placeholder with a comment `<!-- EXPECTED: hand-written ReqIF XML — to be authored in Plan 04-05 -->`. Plan 05 fills the real expected XML.
    - `02-trace-relation.deal`: two requirements linked by `@trace:<<satisfies>>` (mirror showcase trace usage).
    - `02-trace-relation.expected.reqif`: same placeholder comment.
    - `README.md`: documents the fixture convention (`.deal` input → `.expected.reqif` raw XML; the test runner extracts XML from the emitted `.reqifz` and diffs it), mirroring `tests/golden/sysml-v2/`.
    Create `tests/unit/sema_dimensional.zig` as a test harness with TWO loops (mirror `tests/unit/sema_corpus.zig` structure): (1) a `regression_pins` array mapping each `07-*.deal` fixture to its expected `Codes.e_*` code and asserting that code is the first dimensional diagnostic emitted; (2) a showcase-clean loop asserting ZERO E25xx diagnostics across the 19 showcase files. Because Plan 04 has not implemented emission yet, gate the harness body behind a clearly-commented `// SCAFFOLD: assertions become live once Plan 04 implements checkDimensionalExpr` — the file MUST compile and the test MUST be registered in build.zig but may be a no-op skip until Plan 04. Reference `Codes.e_dimension_mismatch` etc. so it fails to compile if Plan 02's codes are missing.
    Create the three regression fixtures as valid-parsing `.deal` files that each contain exactly one dimensional violation:
    - `07-dimensional-mismatch.deal`: `attribute mass : Mass = V(800);` (Voltage assigned to Mass).
    - `07-mixed-unit.deal`: an expression comparing a `kg` value to an `lb` value without conversion.
    - `07-unknown-unit.deal`: a unit literal with no declared dimension.
    These fixtures must PARSE cleanly (only sema, not parse, should flag them).
  </action>
  <verify>
    <automated>zig build test -Dtest-filter=sema 2>&1 | tail -10; ls tests/golden/reqif/ tests/regressions/sema/07-*.deal</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/sema_dimensional.zig` exists, references `Codes.e_dimension_mismatch`, and `zig build` compiles it (registered in build.zig)
    - `tests/golden/reqif/01-requirement-def.deal` and `02-trace-relation.deal` parse: `cargo run -p deal -- parse tests/golden/reqif/01-requirement-def.deal` exits 0
    - `tests/regressions/sema/07-dimensional-mismatch.deal`, `07-mixed-unit.deal`, `07-unknown-unit.deal` all exist and each `deal parse` exits 0 (parse-clean, sema-dirty)
    - `tests/golden/reqif/README.md` documents the `.deal` → `.expected.reqif` fixture convention
  </acceptance_criteria>
  <done>ReqIF golden skeleton + dimensional sema harness + three regression fixtures exist; harness compiles and is build-registered; all fixtures parse cleanly — Plan 04 and Plan 05 have failing-first targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OMG network → local repo | Downloaded XSD crosses from an external host into the trusted schema bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-SC | Tampering | XSD download (reqif.xsd / driver.xsd) | mitigate | Blocking-human checkpoint (Task 1) confirms authenticity; SHA256SUMS manifest pins the bytes; offline bundling prevents runtime fetch (mirrors T-02-21 schema_registry pattern) |
| T-4-01 | Tampering | spec/references/omg-reqif bundle integrity over time | mitigate | SHA256SUMS verified by `shasum -a 256 -c`; Plan 05's reqif_schema.rs re-verifies at load time |
</threat_model>

<verification>
- `cd spec/references/omg-reqif && shasum -a 256 -c SHA256SUMS` exits 0
- `zig build` succeeds with the new E-codes and the new test file registered
- `cargo run -p deal -- parse` exits 0 on all five new `.deal` fixtures
</verification>

<success_criteria>
- ReqIF XSD validation target exists, SHA256-verified, human-confirmed authentic (D-60 unblocked)
- E2402 + E2500..E2503 reserved in diagnostics.zig
- ReqIF golden skeleton + dimensional sema harness + 3 regression fixtures exist and compile/parse
</success_criteria>

<output>
Create `.planning/phases/04-ecosystem/04-01-SUMMARY.md` when done
</output>
