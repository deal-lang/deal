---
phase: 04-ecosystem
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ../deal-stdlib/deal.toml
  - ../deal-stdlib/README.md
  - ../deal-stdlib/packages/units/dimensions.deal
  - ../deal-stdlib/packages/units/si.deal
  - ../deal-stdlib/packages/units/imperial.deal
  - ../deal-stdlib/packages/units/mod.deal
  - .planning/decisions/ADR-phase-4-dimension-metadata-syntax.md
autonomous: false
requirements:
  - REQ-phase-4-1-stdlib
user_setup: []

must_haves:
  truths:
    - "deal-stdlib declares SI base dimensions carrying a 7-exponent SI vector using grammar-legal annotation syntax"
    - "deal-stdlib declares SI + imperial units linked to their base dimension with a conversion factor"
    - "deal-stdlib ships explicit conversion call forms (D-57) so mixed-unit expressions can be written"
    - "deal check parses all deal-stdlib units sources cleanly with the locked 0.1.0-draft grammar"
    - "deal-stdlib ships the Phase-4 REQ scope only — units + interfaces; the README marks RF/protocol/standards packages as planned for Phase 6 (D-54)"
  artifacts:
    - path: "../deal-stdlib/packages/units/dimensions.deal"
      provides: "SI base dimension definitions with 7-exponent metadata the Zig sema reads (D-56)"
      contains: "package deal.std.units"
    - path: "../deal-stdlib/packages/units/si.deal"
      provides: "SI base + derived unit definitions (kg, m, s, A, K, mol, cd, N, J, W, V, ...)"
    - path: "../deal-stdlib/packages/units/imperial.deal"
      provides: "Imperial unit definitions + conversion factors + explicit conversion forms"
    - path: ".planning/decisions/ADR-phase-4-dimension-metadata-syntax.md"
      provides: "Locks the grammar-legal encoding for dimension/unit metadata (the contract Plan 04 sema reads)"
  key_links:
    - from: "../deal-stdlib/packages/units/si.deal"
      to: "../deal-stdlib/packages/units/dimensions.deal"
      via: "unit definitions reference base dimensions"
      pattern: "Mass|Length|Time"
---

<objective>
Design the grammar-legal encoding for dimensional metadata (D-56) and author the `deal-stdlib` `units/` package: SI base dimensions carrying a 7-exponent SI vector, SI + imperial units linked to their dimension with a conversion factor, and explicit conversion call forms (D-57). This plan owns the metadata-syntax CONTRACT that Plan 04's Zig sema reads, so the syntax decision must be locked here against the actual locked grammar.

Purpose: D-54/D-56 require data-driven dimensional knowledge declared in DEAL source (no hardcoded compiler knowledge). RESEARCH assumption A1 proposed `@si_base_dimension[M:1, ...]` bracket-arglist syntax — but the locked `spec/grammar/deal.ebnf` StandaloneAnnotation production only permits `@keyword: Expression` (SD-15), NOT a bracketed arg list. The exact encoding must therefore be verified against the grammar and locked in an ADR before authoring.
Output: `deal-stdlib/packages/units/*.deal`, `deal-stdlib/deal.toml`, updated README, and the metadata-syntax ADR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ecosystem/04-CONTEXT.md
@.planning/phases/04-ecosystem/04-RESEARCH.md
@.planning/phases/04-ecosystem/04-PATTERNS.md
@spec/grammar/deal.ebnf
@spec/examples/showcase/packages/requirements/system.deal

<interfaces>
<!-- LOCKED grammar facts (spec/grammar/deal.ebnf, verified during planning):
   StandaloneAnnotation ::= ANNOTATION ( ":" AnnotationValue )? NL?
   AnnotationValue ::= Expression          <- ONE expression after the colon
   AnnotationBody ::= "{" AnnotationField* "}"     <- ONLY attaches to CategoryAnnotation (@cat:<<op>> ... { })
   AnnotationField ::= IDENT ":" Expression NL?
   CategoryAnnotation ::= ANNOTATION_PREFIX DELIMITED_OPERATOR NamespacePath? AnnotationBody?

   CONSEQUENCE: `@si_base_dimension[M:1, L:0, ...]` (bracket arg list) is NOT grammar-legal.
   Legal encodings to evaluate in Task 1 (all use existing productions):
     (A) attribute-def body with member attributes carrying integer defaults:
           attribute def Mass { attribute si_M : Int = 1; attribute si_L : Int = 0; ... }
     (B) a single AnnotationValue expression that is an array/tuple literal, IF the Expression
         grammar admits `[1,0,0,0,0,0,0]` array literals (verify against Expression productions).
     (C) category-annotation body form `@dimension:<<si>> Mass { M: 1  L: 0 ... }` IF a suitable
         DELIMITED_OPERATOR is available without inventing new grammar.
   Task 1 picks ONE legal encoding and records it in the ADR. Do NOT invent new grammar tokens. -->

<!-- Showcase annotation usage (real, parseable):
   @confidence: 0.95     @rationale: "..."     @simulation:<<computes>> name { equation: "..." tool: "..." }
   attribute def style + @header block + verification {} block — see system.deal -->

<!-- deal-stdlib/deal.toml target (PATTERNS.md):
   [project] name="deal-std" version="<TAG>" schema="deal/0.1" description="..."
   [workspace] packages=["packages/*"]   — no [dependencies], no [build.targets] -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Lock the grammar-legal dimension/unit metadata encoding (ADR)</name>
  <decision>Which existing locked-grammar construct encodes the 7-exponent SI dimension vector and the unit→dimension+factor metadata that Plan 04's Zig sema will read?</decision>
  <context>
    D-56 requires dimensional knowledge declared in DEAL source as data the compiler reads (FA-2, no hardcoded SI knowledge). RESEARCH A1 proposed `@si_base_dimension[M:1, ...]`, but the locked `spec/grammar/deal.ebnf` does NOT permit a bracketed annotation arg list — `StandaloneAnnotation` is `@keyword: Expression` only, and `AnnotationBody {...}` attaches only to category annotations. The chosen encoding becomes the contract Plan 04 reads, so it must be locked before authoring stdlib OR sema. Choosing a non-grammar-legal form would force a grammar change (out of scope — grammar is LOCKED at 0.1.0-draft).
  </context>
  <read_first>
    - spec/grammar/deal.ebnf (Expression productions — confirm whether array/tuple literals like `[1,0,0]` are admissible as an AnnotationValue Expression; AnnotationField; AnnotationBody; StandaloneAnnotation; CategoryAnnotation; available DELIMITED_OPERATOR tokens)
    - spec/examples/showcase/packages/requirements/system.deal (working annotation + attribute def + body examples)
    - spec/examples/showcase/packages/vehicle/battery.deal (real @confidence/@rationale/@simulation usage)
  </read_first>
  <options>
    <option id="attr-def-body">
      <name>attribute-def body with integer member defaults</name>
      <pros>Uses only the universally-available attribute-def + member + DefaultValue productions; trivially parseable; sema reads named members si_M..si_J; units add si_dim (name) + si_factor (real)</pros>
      <cons>Verbose (7 members per dimension); slightly less annotation-like</cons>
    </option>
    <option id="annotation-array">
      <name>standalone annotation whose value is an array-literal Expression</name>
      <pros>Compact `@si_dim: [1,0,0,0,0,0,0]`; matches RESEARCH intent closely</pros>
      <cons>ONLY valid if the Expression grammar admits array literals as an AnnotationValue — must be VERIFIED against deal.ebnf; if not admissible this option is disqualified</cons>
    </option>
    <option id="category-body">
      <name>category annotation with AnnotationBody key:value fields</name>
      <pros>`@dimension:<<...>> Mass { M: 1  L: 0 ... }` reads naturally; AnnotationBody is grammar-supported</pros>
      <cons>Requires an existing DELIMITED_OPERATOR with appropriate meaning; inventing one is out of scope</cons>
    </option>
  </options>
  <action>
    Verify each option against `spec/grammar/deal.ebnf` by writing a tiny probe `.deal` file for the candidate encoding and running `deal parse` on it (the parser is the oracle). Select the encoding that (a) parses cleanly under the locked grammar with NO grammar changes, and (b) gives the Zig sema a clear, stable place to read the 7 exponents per dimension plus `dim` + `factor` per unit. Write `.planning/decisions/ADR-phase-4-dimension-metadata-syntax.md` recording: the chosen encoding with a concrete parseable example for both a dimension and a unit, the exact field/member names the sema must read (so Plan 04 has an unambiguous contract), the explicit conversion call form for D-57 (e.g. `to_kg(lb(3300))` realized as an `attribute def` or function-form per PS-6/SD-13 — verify it parses), and the rejected options with the grammar reason. This ADR is the source of truth Plan 04 reads.
  </action>
  <resume-signal>Select: attr-def-body, annotation-array, category-body (or specify another grammar-legal encoding); the executor verifies the choice parses before locking the ADR.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author deal-stdlib units package (dimensions, SI, imperial, conversions) + deal.toml + README</name>
  <files>../deal-stdlib/deal.toml, ../deal-stdlib/README.md, ../deal-stdlib/packages/units/dimensions.deal, ../deal-stdlib/packages/units/si.deal, ../deal-stdlib/packages/units/imperial.deal, ../deal-stdlib/packages/units/mod.deal</files>
  <read_first>
    - .planning/decisions/ADR-phase-4-dimension-metadata-syntax.md (the encoding locked in Task 1 — the contract to author against)
    - ../deal-stdlib/README.md (current package-tree sketch to update per D-54)
    - spec/examples/showcase/deal.toml (deal.toml format)
    - spec/examples/showcase/packages/requirements/system.deal (@header block + package + attribute def style)
    - .planning/phases/04-ecosystem/04-RESEARCH.md §Dimensional Algebra Domain (SI base vector table + derived unit vectors + BIPM/NIST factors)
  </read_first>
  <action>
    Create `../deal-stdlib/deal.toml`: `[project] name = "deal-std"`, `version = "<TAG>"` (choose the first tag per OQ-3 — recommend `v0.4.0` to lockstep with the Phase-4 toolchain, matching D-52's pairing precedent; record the chosen tag in the SUMMARY so Plan 04 wires the same tag into `deal init`'s `DEFAULT_STDLIB_TAG`), `schema = "deal/0.1"`, `description`. Add `[workspace] packages = ["packages/*"]`. No `[dependencies]`, no `[build.targets]`.
    Author the units package using the locked Task-1 encoding, each file opening with an `@header { }` block + `package deal.std.units;`:
    - `dimensions.deal`: the 7 SI base dimensions (Mass M, Length L, Time T, Current I, Temperature Θ, Amount N, Luminosity J) each carrying its 7-exponent vector, PLUS the common derived dimensions needed by the showcase and interfaces (Force, Energy, Power, Voltage, Speed, Resistance, Charge) with their exponent vectors from RESEARCH §Derived Unit Vectors.
    - `si.deal`: SI units mapped to dimensions with factor 1.0 for base units and correct factors for derived/prefixed units — at minimum kg, g, m, km, s, hr, A, K, mol, cd, N, J, W, V, ohm, C, plus the units the 19-file showcase imports (`import deal.std.units.{kg, kWh, V, A, kW, degC, ohm}` per showcase battery.deal — every imported symbol MUST exist).
    - `imperial.deal`: imperial units with SI conversion factors (lb→0.453592 kg, etc.) AND the explicit conversion call forms (D-57) using the form locked in the ADR (e.g. `to_kg(...)`), so mixed-unit expressions are writable.
    - `mod.deal`: `export` the public unit/dimension surface (follow showcase export style).
    Update `../deal-stdlib/README.md`: mark `rf/`, `protocols/`, `standards/`, `patterns/` packages as "Phase 6 — deferred" per D-54; document the units package as shipped.
    Every authored `.deal` file MUST parse under the locked grammar (the parser is the oracle).
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && for f in ../deal-stdlib/packages/units/*.deal; do cargo run -q -p deal -- parse "$f" >/dev/null || { echo "PARSE FAIL: $f"; exit 1; }; done && echo UNITS_PARSE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Every file in `../deal-stdlib/packages/units/*.deal` passes `deal parse` with exit 0
    - `dimensions.deal` contains `package deal.std.units` and declares all 7 SI base dimensions plus Force/Energy/Power/Voltage/Speed using the ADR-locked encoding
    - `si.deal` declares every unit imported by the showcase (`kg, kWh, V, A, kW, degC, ohm` at minimum) — `grep -o 'kg\|kWh\|V\|A\|kW\|degC\|ohm'` confirms each appears as a definition
    - `imperial.deal` contains at least one explicit conversion call form matching the ADR (e.g. `to_kg`)
    - `../deal-stdlib/README.md` marks rf/protocols/standards/patterns as "Phase 6 — deferred"
    - `../deal-stdlib/deal.toml` has `name = "deal-std"` and a concrete `version`
  </acceptance_criteria>
  <done>deal-stdlib units package authored against the locked metadata encoding; all unit sources parse; README updated to D-54 scope; deal.toml present with chosen version tag recorded for Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdlib DEAL source → Zig sema | Exponent metadata declared in stdlib is read and trusted by the dimensional algebra at check time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-06 | Tampering | malformed/contradictory exponent metadata in stdlib could mislead sema | accept | Stdlib is authored in-repo, reviewed, and parse-gated; a future stdlib-self-consistency check is out of Phase 4 scope; the parser rejects malformed declarations |
| T-4-07 | Information disclosure | none — units are public reference data (BIPM/NIST), no secrets | accept | Unit definitions carry no sensitive data |
</threat_model>

<verification>
- All `../deal-stdlib/packages/units/*.deal` parse with `deal parse` exit 0
- ADR exists and documents the parseable encoding + the field names Plan 04 sema reads
- README reflects D-54 deferred-package scope
</verification>

<success_criteria>
- The dimension/unit metadata encoding is locked in an ADR and is grammar-legal (no grammar changes)
- deal-stdlib units package (dimensions, SI, imperial, conversions) is authored and parses cleanly (REQ-phase-4-1 units half)
- Every showcase-imported unit symbol exists in the stdlib (the showcase remains the oracle)
</success_criteria>

<output>
Create `.planning/phases/04-ecosystem/04-03-SUMMARY.md` when done. Record the chosen stdlib version tag so Plan 04 wires `DEFAULT_STDLIB_TAG`.
</output>
