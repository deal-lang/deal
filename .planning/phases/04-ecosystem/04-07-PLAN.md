---
phase: 04-ecosystem
plan: 07
type: execute
wave: 2
depends_on:
  - 04-03
files_modified:
  - ../deal-lang.org/package.json
  - ../deal-lang.org/astro.config.mjs
  - ../deal-lang.org/public/CNAME
  - ../deal-lang.org/src/styles/custom.css
  - ../deal-lang.org/src/grammars/deal.tmLanguage.json
  - ../deal-lang.org/src/grammars/dealx.tmLanguage.json
  - ../deal-lang.org/src/content/docs/index.mdx
  - ../deal-lang.org/src/content/docs/getting-started/installation.mdx
  - ../deal-lang.org/src/content/docs/getting-started/first-project.mdx
  - ../deal-lang.org/src/content/docs/getting-started/concepts.mdx
  - ../deal-lang.org/.github/workflows/deploy.yml
  - .planning/decisions/ADR-phase-4-nc1-domain-amendment.md
autonomous: false
requirements:
  - REQ-phase-4-3-docs-site
user_setup:
  - service: github-pages
    why: "Custom domain DNS for deal-lang.org → GitHub Pages (D-63/D-64)"
    dashboard_config:
      - task: "Set DNS CNAME for deal-lang.org → GitHub Pages and enable Pages with custom domain in the deal-lang.org repo settings"
        location: "Domain registrar DNS + GitHub repo Settings → Pages"

must_haves:
  truths:
    - "deal-lang.org runs Astro + Starlight and `npm run build` exits 0"
    - "The DEAL TextMate grammars load as Shiki custom languages so `.deal`/`.dealx` fences render with real highlighting (not unstyled)"
    - "Landing + getting-started pages exist with showcase-derived code examples that match the UI-SPEC copywriting and accent contract"
    - "GitHub Pages deploy workflow exists and a public/CNAME pins deal-lang.org (D-63)"
    - "The NC-1 domain amendment (deal-lang.org → deal-lang.org) is recorded in an ADR"
  artifacts:
    - path: "../deal-lang.org/astro.config.mjs"
      provides: "Starlight config: site=deal-lang.org, sidebar, expressiveCode.shiki.langs = [deal, dealx]"
      contains: "deal-lang.org"
    - path: "../deal-lang.org/src/styles/custom.css"
      provides: "Accent override #0E7490 per UI-SPEC"
    - path: "../deal-lang.org/public/CNAME"
      provides: "Custom domain pin (deal-lang.org)"
    - path: "../deal-lang.org/.github/workflows/deploy.yml"
      provides: "withastro/action@v6 → GitHub Pages deploy"
    - path: ".planning/decisions/ADR-phase-4-nc1-domain-amendment.md"
      provides: "NC-1 amendment record (D-64)"
  key_links:
    - from: "../deal-lang.org/astro.config.mjs"
      to: "../deal-lang.org/src/grammars/deal.tmLanguage.json"
      via: "expressiveCode.shiki.langs reads the parsed grammar JSON"
      pattern: "shiki"
---

<objective>
Stand up the `deal-lang.org` docs site on Astro 6 + Starlight (REQ-phase-4-3 foundation): scaffold the site, load the DEAL/DEALX TextMate grammars as Shiki custom languages (D-41 parity → real highlighting), author the landing + getting-started pages from showcase-derived examples, apply the UI-SPEC accent + copywriting contract, wire the GitHub Pages deploy workflow with a `deal-lang.org` CNAME, and record the NC-1 domain amendment ADR (D-64).

Purpose: Success Criterion 3 requires `deal-lang.org` live on Astro + Starlight with real Shiki highlighting. This plan delivers the site skeleton + highlighting + landing/getting-started + deploy; Plan 08 adds the language/CLI reference pages and the snippet CI gate.
Output: Astro/Starlight project in `../deal-lang.org/`, Shiki grammar wiring, landing + 3 getting-started pages, deploy workflow + CNAME, NC-1 ADR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ecosystem/04-CONTEXT.md
@.planning/phases/04-ecosystem/04-RESEARCH.md
@.planning/phases/04-ecosystem/04-PATTERNS.md
@.planning/phases/04-ecosystem/04-UI-SPEC.md
@spec/examples/showcase/packages/requirements/system.deal

<interfaces>
<!-- Astro/Starlight stack (RESEARCH §Docs Site Domain, VERIFIED 2026-06-05):
   astro 6.4.4, @astrojs/starlight 0.39.3, shiki 4.2.0. Scaffold: `npm create astro@latest -- --template starlight`.
   GitHub Pages: NO adapter package; withastro/action@v6 builds+deploys (RESEARCH State of the Art).
   Custom domain: public/CNAME contains `deal-lang.org`; REMOVE `base` from astro.config (Pitfall 7). -->

<!-- Shiki custom grammar (RESEARCH Pattern 1 lines 332-364 — authoritative; PATTERNS.md §astro.config.mjs):
   expressiveCode: { shiki: { langs: [dealGrammar, dealxGrammar] } }
   dealGrammar = JSON.parse(readFileSync('src/grammars/deal.tmLanguage.json'))  -- PARSED object.
   ANTI-PATTERN (Pitfall 2): { path: './deal.tmLanguage.json' } — removed in Shiki v1.0. Pass parsed JSON, never a path.
   Grammar SOURCE: ../vscode-deal/syntaxes/deal.tmLanguage.json + dealx.tmLanguage.json (copy into src/grammars/). -->

<!-- UI-SPEC contract (04-UI-SPEC.md — load it, it is the visual oracle):
   site title: "DEAL — Digital Engineering Authoring Language"; landing h1 "DEAL"; subtitle "A text-first language for systems engineering";
   primary CTA "Get started" -> /getting-started/installation/; secondary "View on GitHub".
   Accent ONLY #0E7490 via src/styles/custom.css (--sl-color-accent / -high #164E63 / -low #ECFEFF).
   Sidebar structure is canonical (UI-SPEC §Sidebar Structure) — executor must not deviate.
   Typography: do NOT override Starlight defaults except landing h1 36px/700; weight palette 400+700 ONLY.
   Fence markers: ```deal must parse; ```deal error-expected is skipped by CI (Plan 08 gate). -->

<!-- D-62/D-65: every code example is lifted from / derived from the 19-file showcase (tests/showcase/),
   so it is real + parseable. Use spec/examples/showcase/packages/requirements/system.deal for requirement/annotation examples. -->

<!-- GH Pages deploy (RESEARCH Pattern 4 lines 434-474): name=Deploy; on push main + workflow_dispatch;
   permissions pages:write + id-token:write; build job uses withastro/action@v6; deploy job uses actions/deploy-pages@v4.
   The snippet-check CI step is added in Plan 08 (it needs the deal binary + reference pages). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro + Starlight, load DEAL/DEALX Shiki grammars, apply accent + sidebar config</name>
  <files>../deal-lang.org/package.json, ../deal-lang.org/astro.config.mjs, ../deal-lang.org/public/CNAME, ../deal-lang.org/src/styles/custom.css, ../deal-lang.org/src/grammars/deal.tmLanguage.json, ../deal-lang.org/src/grammars/dealx.tmLanguage.json</files>
  <read_first>
    - .planning/phases/04-ecosystem/04-RESEARCH.md §Docs Site Domain + Pattern 1 (astro.config) + Pitfalls 2/7
    - .planning/phases/04-ecosystem/04-PATTERNS.md §`deal-lang.org/astro.config.mjs` (authoritative config block)
    - .planning/phases/04-ecosystem/04-UI-SPEC.md §Sidebar Structure + §Color (accent override CSS) + §Copywriting (site title)
    - ../vscode-deal/syntaxes/deal.tmLanguage.json (grammar source to copy)
    - ../vscode-deal/syntaxes/dealx.tmLanguage.json (grammar source to copy)
    - ../deal-lang.org/README.md (current repo state — README-only)
  </read_first>
  <action>
    Scaffold Astro + Starlight in `../deal-lang.org/` (`npm create astro@latest -- --template starlight` semantics; pin `astro@6.4.4`, `@astrojs/starlight@0.39.3` in package.json per RESEARCH §Standard Stack). Commit a `package-lock.json` so CI can `npm ci`.
    Copy `../vscode-deal/syntaxes/deal.tmLanguage.json` and `dealx.tmLanguage.json` into `../deal-lang.org/src/grammars/` (D-41 source-of-truth grammars; copy, do not symlink, so the docs build is self-contained).
    Write `astro.config.mjs` per RESEARCH Pattern 1 / PATTERNS.md: `site: 'https://deal-lang.org'` (D-64), NO `base` (Pitfall 7), `expressiveCode.shiki.langs = [dealGrammar, dealxGrammar]` where each is `JSON.parse(readFileSync('src/grammars/<x>.tmLanguage.json','utf-8'))` (PARSED objects — never `{ path: ... }`, Pitfall 2). Set Starlight `title: 'DEAL — Digital Engineering Authoring Language'` and declare the sidebar EXACTLY per UI-SPEC §Sidebar Structure (getting-started: installation/first-project/concepts; reference: definitions/compositions/requirements/traceability/imports/annotations/units/deal-toml; cli: overview/vscode-setup). Reference custom.css via Starlight `customCss`.
    Write `src/styles/custom.css` with the accent override ONLY: `--sl-color-accent: #0E7490; --sl-color-accent-high: #164E63; --sl-color-accent-low: #ECFEFF;` (UI-SPEC §Color). Do NOT add other theme overrides.
    Write `public/CNAME` containing exactly `deal-lang.org` (Pitfall 7).
    Verify a `.deal` fenced block highlights: create a throwaway test MDX with a deal fenced block, run `npm run build`, confirm the build succeeds with no "Unknown language" error, then remove the throwaway file (Task 2 adds the real landing page).
  </action>
  <verify>
    <automated>cd ../deal-lang.org && npm ci && npm run build 2>&1 | tail -15 && test -f public/CNAME && grep -qx 'deal-lang.org' public/CNAME && grep -q 'deal-lang.org' astro.config.mjs && echo SITE_BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd ../deal-lang.org && npm ci && npm run build` exits 0
    - `astro.config.mjs` contains `https://deal-lang.org`, `expressiveCode`, and `shiki` with `langs` (grep confirms), and does NOT contain `path:` inside the langs config (Shiki v1.0 anti-pattern absent)
    - `src/grammars/deal.tmLanguage.json` and `dealx.tmLanguage.json` exist (copied from vscode-deal)
    - `public/CNAME` contains exactly `deal-lang.org`
    - `src/styles/custom.css` contains `--sl-color-accent: #0E7490`
    - `astro.config.mjs` sidebar matches UI-SPEC §Sidebar Structure (getting-started + reference + cli groups present)
  </acceptance_criteria>
  <done>Astro + Starlight builds; DEAL/DEALX grammars load as Shiki languages; accent + sidebar + CNAME match the UI-SPEC; site title set.</done>
</task>

<task type="auto">
  <name>Task 2: Author landing + getting-started pages from showcase examples; record NC-1 ADR</name>
  <files>../deal-lang.org/src/content/docs/index.mdx, ../deal-lang.org/src/content/docs/getting-started/installation.mdx, ../deal-lang.org/src/content/docs/getting-started/first-project.mdx, ../deal-lang.org/src/content/docs/getting-started/concepts.mdx, .planning/decisions/ADR-phase-4-nc1-domain-amendment.md</files>
  <read_first>
    - ../deal-lang.org/astro.config.mjs (the sidebar slugs the pages must match — Task 1)
    - .planning/phases/04-ecosystem/04-UI-SPEC.md §Copywriting (landing h1/subtitle/CTAs exact strings) + §Typography (landing h1 only gets 36px/700) + §Code Block Contract (fence markers)
    - spec/examples/showcase/packages/requirements/system.deal (real requirement/annotation source for examples — D-62 showcase-derived)
    - spec/examples/showcase/deal.toml (deal.toml example for first-project page)
    - .planning/phases/04-ecosystem/04-CONTEXT.md D-64 + D-69 (init→install→check new-user flow for first-project)
  </read_first>
  <action>
    Author the landing page `src/content/docs/index.mdx` using Starlight's splash/hero frontmatter: h1 `DEAL` (the ONLY 36px/700 display element per UI-SPEC focal point), tagline `A text-first language for systems engineering`, primary CTA `Get started` → `/getting-started/installation/`, secondary CTA `View on GitHub` → `https://github.com/deal-lang/deal`. Include at least one deal fenced example LIFTED from the showcase (so it parses) demonstrating a `requirement def` with a verification block — copy a real snippet from `spec/examples/showcase/packages/requirements/system.deal`.
    Author `getting-started/installation.mdx`: how to install the `deal` CLI (link to GitHub Releases per the Phase 3 pipeline) and the VS Code extension (forward-link to /cli/vscode-setup/, authored in Plan 08).
    Author `getting-started/first-project.mdx`: the canonical new-user flow per D-69 — `deal init my-project` → `cd my-project && deal install` → `deal check` → `deal build --target sysml-v2` / `--target reqif`, with the scaffolded `deal.toml` shown as a toml fenced block (derive from spec/examples/showcase/deal.toml + the D-67 deal-std git dependency).
    Author `getting-started/concepts.mdx`: the core DEAL concepts (definitions, compositions, requirements, traceability, units) at a high level, each with one showcase-derived deal fenced example. Keep examples minimal and parseable.
    EVERY deal/dealx fenced block MUST be real DEAL that parses with `deal parse` (D-65 — Plan 08's CI gate enforces this; author them parse-clean now). Intentional-error examples must be marked with the `error-expected` fence meta per UI-SPEC §Fence Markers.
    Write `.planning/decisions/ADR-phase-4-nc1-domain-amendment.md`: a short ADR recording that NC-1's `deal-lang.org` is amended to `deal-lang.org` (user owns .org; .dev was aspirational) per D-64, the sibling repo directory name `deal-lang.org/` is retained while the deployed domain is `deal-lang.org`, and the DNS CNAME is a user-side manual step (captured in this plan's user_setup).
  </action>
  <verify>
    <automated>cd ../deal-lang.org && npm run build 2>&1 | tail -8 && ls src/content/docs/index.mdx src/content/docs/getting-started/installation.mdx src/content/docs/getting-started/first-project.mdx src/content/docs/getting-started/concepts.mdx && test -f /Users/dunnock/projects/deal-lang/deal/.planning/decisions/ADR-phase-4-nc1-domain-amendment.md && echo PAGES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd ../deal-lang.org && npm run build` exits 0 with all four pages present
    - `index.mdx` contains the h1 `DEAL`, the tagline `A text-first language for systems engineering`, a `Get started` CTA, and a `View on GitHub` CTA (UI-SPEC §Copywriting exact strings)
    - `index.mdx` and `concepts.mdx` each contain at least one deal fenced block lifted/derived from the showcase
    - `first-project.mdx` shows the `deal init` → `deal install` → `deal check` flow and a toml deal.toml block with the deal-std git dependency
    - `.planning/decisions/ADR-phase-4-nc1-domain-amendment.md` exists and states the deal-lang.org → deal-lang.org amendment per D-64
  </acceptance_criteria>
  <done>Landing + 3 getting-started pages authored from real showcase examples matching the UI-SPEC copy/accent; NC-1 amendment ADR recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Wire GitHub Pages deploy workflow (withastro/action@v6 → deploy-pages)</name>
  <files>../deal-lang.org/.github/workflows/deploy.yml</files>
  <read_first>
    - .planning/phases/04-ecosystem/04-RESEARCH.md §Pattern 4 (deploy.yml — authoritative) + Pitfall 7 (CNAME)
    - ../deal-lang.org/astro.config.mjs (site config the deploy relies on)
    - ../deal-lang.org/public/CNAME (custom domain pin from Task 1)
  </read_first>
  <action>
    Write `.github/workflows/deploy.yml` per RESEARCH Pattern 4: `name: Deploy to GitHub Pages`; trigger on `push` to `main` + `workflow_dispatch`; `permissions: contents: read, pages: write, id-token: write`; `concurrency: group "pages", cancel-in-progress: false`. Build job on `ubuntu-latest`: `actions/checkout@v4` then `withastro/action@v6` (handles npm install + astro build; no adapter package needed per RESEARCH State of the Art). Deploy job `needs: build`, environment `github-pages`, `actions/deploy-pages@v4`.
    Do NOT add the snippet-check step here — that requires the deal binary and the reference pages and is added by Plan 08 (which also adds the Shiki scope gate). Leave a YAML comment noting the snippet-check step is appended by Plan 08.
    The `public/CNAME` (Task 1) is copied to `dist/` by Astro automatically (Pitfall 7) — no extra deploy step needed for the custom domain.
  </action>
  <verify>
    <automated>cd ../deal-lang.org && test -f .github/workflows/deploy.yml && grep -q 'withastro/action@v6' .github/workflows/deploy.yml && grep -q 'actions/deploy-pages@v4' .github/workflows/deploy.yml && grep -q 'pages: write' .github/workflows/deploy.yml && echo DEPLOY_WF_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` exists with `withastro/action@v6` and `actions/deploy-pages@v4`
    - The workflow declares `pages: write` and `id-token: write` permissions
    - Triggers include `push` to `main` and `workflow_dispatch`
    - A comment marks where Plan 08 appends the snippet-check + Shiki scope gate
  </acceptance_criteria>
  <done>The GitHub Pages deploy workflow is wired (build + deploy jobs); the custom domain is pinned via public/CNAME.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → docs build | Third-party Astro/Starlight packages enter the docs toolchain |
| GitHub Actions → GitHub Pages | The deploy pipeline publishes the static site to the public domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-15 | Tampering | npm supply chain (astro/starlight transitive deps) | mitigate | `package-lock.json` committed; CI uses `npm ci` (lockfile-pinned installs); packages vetted in RESEARCH §Package Legitimacy Audit (astro/starlight approved) |
| T-4-16 | Tampering | grammar drift — docs Shiki grammar diverges from the canonical vscode-deal grammar | accept | Grammars are COPIED from vscode-deal at authoring time; a future sync check is out of Phase 4 scope; the Plan 08 snippet parse-gate catches grammar/grammar-vs-parser mismatch on real snippets |
| T-4-17 | Elevation/Tampering | GitHub Pages deploy token (GITHUB_TOKEN) scope | mitigate | Workflow uses least-privilege `permissions` (only pages:write + id-token:write); no PAT stored; OIDC id-token for deploy-pages |
</threat_model>

<verification>
- `cd ../deal-lang.org && npm ci && npm run build` exits 0
- DEAL/DEALX grammars load as Shiki langs (no Unknown-language error)
- Landing + 3 getting-started pages present; deal fenced examples are showcase-derived
- `public/CNAME` = deal-lang.org; accent override present
- deploy.yml uses withastro/action@v6 + deploy-pages@v4
- NC-1 amendment ADR exists
</verification>

<success_criteria>
- `deal-lang.org` builds on Astro + Starlight with real DEAL/DEALX Shiki highlighting (REQ-phase-4-3 foundation; Success Criterion 3 highlighting half)
- Landing + getting-started pages match the UI-SPEC copy/accent contract
- GitHub Pages deploy wired; custom domain `deal-lang.org` pinned; NC-1 amendment recorded
</success_criteria>

<output>
Create `.planning/phases/04-ecosystem/04-07-SUMMARY.md` when done. Note the deal fenced snippets you authored so Plan 08's CI gate covers them.
</output>
