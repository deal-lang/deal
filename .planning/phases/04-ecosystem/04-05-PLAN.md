---
phase: 04-ecosystem
plan: 05
type: execute
wave: 2
depends_on:
  - 04-03
files_modified:
  - ../deal-stdlib/packages/interfaces/electrical/rj45.deal
  - ../deal-stdlib/packages/interfaces/electrical/usb_c.deal
  - ../deal-stdlib/packages/interfaces/electrical/can.deal
  - ../deal-stdlib/packages/interfaces/electrical/rs422.deal
  - ../deal-stdlib/packages/interfaces/electrical/mod.deal
  - ../deal-stdlib/packages/interfaces/mechanical/bolt_patterns.deal
  - ../deal-stdlib/packages/interfaces/mechanical/mod.deal
autonomous: true
requirements:
  - REQ-phase-4-1-stdlib
user_setup: []

must_haves:
  truths:
    - "deal-stdlib declares electrical interfaces RJ45, USB-C, CAN, RS-422 as DEAL interface/port definitions"
    - "deal-stdlib declares mechanical bolt-pattern interfaces"
    - "Every interface source imports its units from deal.std.units (the units package authored in Plan 03) and resolves cleanly"
    - "deal check parses all deal-stdlib interfaces sources cleanly with the locked 0.1.0-draft grammar"
  artifacts:
    - path: "../deal-stdlib/packages/interfaces/electrical/rj45.deal"
      provides: "RJ45 Ethernet connector interface (pins, signals)"
      contains: "package deal.std.interfaces.electrical"
    - path: "../deal-stdlib/packages/interfaces/electrical/usb_c.deal"
      provides: "USB-C connector interface"
    - path: "../deal-stdlib/packages/interfaces/electrical/can.deal"
      provides: "CAN bus interface (baud rate, node count)"
    - path: "../deal-stdlib/packages/interfaces/electrical/rs422.deal"
      provides: "RS-422 differential serial interface"
    - path: "../deal-stdlib/packages/interfaces/mechanical/bolt_patterns.deal"
      provides: "Common bolt-pattern mechanical interface definitions"
  key_links:
    - from: "../deal-stdlib/packages/interfaces/electrical/usb_c.deal"
      to: "../deal-stdlib/packages/units/si.deal"
      via: "import deal.std.units.{V, A, W}"
      pattern: "import deal\\.std\\.units"
---

<objective>
Author the `deal-stdlib` interface packages (D-54 REQ scope): `interfaces/electrical/` (RJ45, USB-C, CAN, RS-422) and `interfaces/mechanical/` (common bolt patterns) as real DEAL `interface def` / `port def` source files that import their units from `deal.std.units` and parse cleanly under the locked grammar.

Purpose: REQ-phase-4-1-stdlib and Success Criterion 1 require BOTH units AND electrical/mechanical interfaces in the stdlib. Plan 03 delivered `units/`; this plan delivers the `interfaces/` half so a downstream project can `import deal.std.interfaces.electrical.{RJ45}`.
Output: `../deal-stdlib/packages/interfaces/electrical/*.deal` (4 interfaces + mod), `../deal-stdlib/packages/interfaces/mechanical/*.deal` (bolt patterns + mod).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ecosystem/04-CONTEXT.md
@.planning/phases/04-ecosystem/04-RESEARCH.md
@.planning/phases/04-ecosystem/04-PATTERNS.md
@spec/examples/showcase/packages/interfaces/electrical.deal
@.planning/phases/04-ecosystem/04-03-SUMMARY.md

<interfaces>
<!-- Interface/port def style (spec/examples/showcase/packages/interfaces/electrical.deal — the exact analog):
   @header { path: ... schema: deal/0.1 ... status: ... marking: Unclassified }
   package <dotted.path>;
   import deal.std.units.{V, A, W, kW, ohm};
   /** JSDoc doc comment */
   port def <Name> { @assumes: "..." public ( attribute <name> : <Dim> [<mult>] [= <unit>(<value>)]; derived attribute <name> : <Dim> = <expr>; ) }
   interface def <Name> { public ( attribute <name> : Integer [1] = <int>; ) }
   <<specializes>> for inheritance. -->

<!-- Units available from Plan 03's deal.std.units package (read the 04-03-SUMMARY for the exact authored symbol list):
   At minimum: kg, g, m, km, s, hr, A, K, mol, cd, N, J, W, V, ohm, C, plus showcase imports kWh/kW/degC.
   Dimensions available: Mass, Length, Time, Current, Voltage, Power, Force, Energy, Speed, Resistance, Charge.
   Use ONLY unit/dimension symbols that Plan 03 actually authored — the 04-03-SUMMARY is the source of truth. -->

<!-- Package namespace contract: interface files live under packages/interfaces/<group>/
   and declare `package deal.std.interfaces.electrical;` / `package deal.std.interfaces.mechanical;`
   so consumers write `import deal.std.interfaces.electrical.{RJ45};`.
   deal.toml [workspace] packages = ["packages/*"] was authored in Plan 03 — DO NOT modify deal.toml or README here. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author electrical interface package (RJ45, USB-C, CAN, RS-422) + mod</name>
  <files>../deal-stdlib/packages/interfaces/electrical/rj45.deal, ../deal-stdlib/packages/interfaces/electrical/usb_c.deal, ../deal-stdlib/packages/interfaces/electrical/can.deal, ../deal-stdlib/packages/interfaces/electrical/rs422.deal, ../deal-stdlib/packages/interfaces/electrical/mod.deal</files>
  <read_first>
    - spec/examples/showcase/packages/interfaces/electrical.deal (the exact analog — @header + port def/interface def + import deal.std.units style)
    - .planning/phases/04-ecosystem/04-03-SUMMARY.md (the authored unit/dimension symbol list — only use symbols that exist)
    - ../deal-stdlib/packages/units/si.deal (confirm the actual unit symbols available to import)
    - ../deal-stdlib/packages/units/mod.deal (the export surface — what is importable)
    - spec/grammar/deal.ebnf (interface def / port def productions, to author grammar-legal sources)
  </read_first>
  <action>
    Create four electrical interface source files under `../deal-stdlib/packages/interfaces/electrical/`, each opening with an `@header { }` block (mirror showcase electrical.deal header; `marking: Unclassified`, `status: release`) and `package deal.std.interfaces.electrical;`, importing units via `import deal.std.units.{V, A, ...};` using ONLY symbols Plan 03 authored:
    - `rj45.deal`: `interface def RJ45` for an 8-pin Ethernet connector. Model the four twisted pairs (TX/RX) as `port def` signal ports or member attributes; include a `baudRate`/`dataRate` attribute typed as an Integer or a stdlib Speed/frequency-adjacent dimension only if that dimension exists in Plan 03 (otherwise use `Integer`). Provide a JSDoc `/** ... */` doc comment.
    - `usb_c.deal`: `interface def USBC` with power-delivery attributes — `voltage : Voltage` and `maxCurrent : Current` with sensible defaults (e.g. `V(20)`, `A(5)`) and a `derived attribute maxPower : Power = voltage * maxCurrent;` mirroring the showcase HVDCPort `maxPower` pattern.
    - `can.deal`: `interface def CANBus` mirroring the showcase CANBus (`baudRate : Integer [1] = 500000`, `maxNodes : Integer [1] = 32`) plus the `@assumes:` annotation form already used in the showcase.
    - `rs422.deal`: `interface def RS422` differential serial — `voltage : Voltage` differential level and an Integer `maxDistance`/`dataRate` attribute.
    - `mod.deal`: `export` the public interface surface (`RJ45`, `USBC`, `CANBus`, `RS422`) following the showcase/Plan-03 `mod.deal` export style.
    Every file MUST parse under the locked grammar (the parser is the oracle). Use the showcase electrical.deal annotation/attribute syntax verbatim where applicable — do NOT invent new grammar constructs. If a unit/dimension you want does not exist in Plan 03's units package, either fall back to `Integer`/`Real`/an existing dimension, or note the gap in the SUMMARY (do NOT add to the units package here — that file is owned by Plan 03).
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && for f in ../deal-stdlib/packages/interfaces/electrical/*.deal; do cargo run -q -p deal -- parse "$f" >/dev/null || { echo "PARSE FAIL: $f"; exit 1; }; done && echo ELEC_PARSE_OK</automated>
  </verify>
  <acceptance_criteria>
    - All four electrical `.deal` files + `mod.deal` pass `deal parse` with exit 0
    - `rj45.deal`, `usb_c.deal`, `can.deal`, `rs422.deal` each contain `package deal.std.interfaces.electrical`
    - `grep -l 'interface def RJ45' ../deal-stdlib/packages/interfaces/electrical/rj45.deal` matches; `interface def USBC`, `interface def CANBus`, `interface def RS422` each present in their file
    - At least one electrical file contains `import deal.std.units.` (units are consumed, not redeclared)
    - `mod.deal` exports RJ45, USBC, CANBus, RS422
  </acceptance_criteria>
  <done>The electrical interface package (RJ45, USB-C, CAN, RS-422) is authored, imports its units from deal.std.units, and every source parses cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Author mechanical bolt-pattern interface package + mod</name>
  <files>../deal-stdlib/packages/interfaces/mechanical/bolt_patterns.deal, ../deal-stdlib/packages/interfaces/mechanical/mod.deal</files>
  <read_first>
    - spec/examples/showcase/packages/interfaces/electrical.deal (interface/port def + @header + import style — reuse for mechanical)
    - ../deal-stdlib/packages/units/si.deal (Length/Force/Torque unit symbols available — only use what exists)
    - .planning/phases/04-ecosystem/04-03-SUMMARY.md (authored dimension list — confirm Length exists; Torque/AngularVelocity may NOT exist)
    - spec/grammar/deal.ebnf (interface def / attribute def productions)
  </read_first>
  <action>
    Create `../deal-stdlib/packages/interfaces/mechanical/bolt_patterns.deal` with an `@header { }` block and `package deal.std.interfaces.mechanical;`. Author common bolt-pattern interface definitions as `interface def`s (JSDoc doc-commented):
    - A generic `interface def BoltPattern` carrying `boltCount : Integer [1]`, `boltCircleDiameter : Length [1]` (use the stdlib `Length` dimension with an `mm`/`m` unit that exists in Plan 03 — if only `m`/`km` exist, use `m`), and a `boltSize` Integer/String attribute.
    - At least two concrete patterns specializing or instantiating the generic via the `<<specializes>>` form the showcase uses (e.g. `BoltPattern4x100`, `BoltPattern5x114_3`) with concrete default values for boltCount and boltCircleDiameter.
    Create `mod.deal` exporting the public mechanical interface surface.
    Use ONLY dimensions/units Plan 03 authored. Torque/AngularVelocity from the showcase ShaftPort are NOT guaranteed to exist — do NOT use them unless the 04-03-SUMMARY confirms they were authored; fall back to `Length` + `Integer`/`Real`. Every file MUST parse under the locked grammar.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && for f in ../deal-stdlib/packages/interfaces/mechanical/*.deal; do cargo run -q -p deal -- parse "$f" >/dev/null || { echo "PARSE FAIL: $f"; exit 1; }; done && echo MECH_PARSE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bolt_patterns.deal` and `mod.deal` both pass `deal parse` with exit 0
    - `bolt_patterns.deal` contains `package deal.std.interfaces.mechanical` and `interface def BoltPattern`
    - At least two concrete bolt-pattern definitions exist (grep for two distinct `interface def BoltPattern` identifiers)
    - `bolt_patterns.deal` uses only dimensions/units present in `../deal-stdlib/packages/units/` (no undefined-symbol references — confirmed by parse + a manual grep that no `Torque`/`AngularVelocity` appears unless 04-03-SUMMARY lists them)
    - `mod.deal` exports the mechanical interface surface
  </acceptance_criteria>
  <done>The mechanical bolt-pattern interface package is authored against the Plan-03 units, with at least two concrete patterns, and every source parses cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdlib DEAL source → consumer projects | Interface definitions become a public dependency surface consumed by external projects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-10 | Tampering | malformed interface declaration mis-typing a consumer's ports | accept | Stdlib is authored in-repo and parse-gated; the parser + sema (Plan 04) reject malformed declarations; interface data carries no secrets |
| T-4-11 | Information disclosure | none — interface definitions are public reference data (connector pinouts, bolt patterns) | accept | No sensitive data; all marking `Unclassified` |
</threat_model>

<verification>
- All `../deal-stdlib/packages/interfaces/electrical/*.deal` and `mechanical/*.deal` parse with `deal parse` exit 0
- Electrical package declares RJ45, USB-C (USBC), CAN (CANBus), RS-422 (RS422)
- Mechanical package declares BoltPattern + at least two concrete patterns
- Interface files import units from deal.std.units rather than redeclaring them
</verification>

<success_criteria>
- deal-stdlib interfaces package (electrical RJ45/USB-C/CAN/RS-422 + mechanical bolt patterns) authored and parses cleanly (REQ-phase-4-1 interfaces half — completes Success Criterion 1's "electrical interfaces, mechanical interfaces")
- Interfaces consume units from the Plan-03 units package (no duplicated unit knowledge)
</success_criteria>

<output>
Create `.planning/phases/04-ecosystem/04-05-SUMMARY.md` when done. Record any unit/dimension symbol you wanted but Plan 03 did not author (so the gate plan or a follow-up can reconcile).
</output>
