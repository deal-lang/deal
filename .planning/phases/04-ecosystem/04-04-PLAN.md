---
phase: 04-ecosystem
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - src/sema.zig
  - tests/unit/sema_dimensional.zig
  - cli/src/main.rs
  - tests/regressions/sema/07-conversion-mismatch.deal
autonomous: true
requirements:
  - REQ-phase-4-1-stdlib
must_haves:
  truths:
    - "deal check evaluates unit expressions and computes their SI dimension vector from stdlib-declared metadata (D-55/D-56)"
    - "A dimension mismatch (e.g. mass : Mass = V(800)) is reported as E2500"
    - "A mixed-unit same-dimension expression without explicit conversion is reported as E2501"
    - "The Zig core gains zero hardcoded stdlib unit knowledge — all exponents come from DEAL-source metadata"
    - "deal check on the 19-file showcase emits zero dimensional diagnostics"
  artifacts:
    - path: "src/sema.zig"
      provides: "Check #7: dimensional algebra over a generic 7-exponent SI vector reading stdlib metadata"
      contains: "DimVector"
    - path: "tests/unit/sema_dimensional.zig"
      provides: "Live regression pins (E2500/E2501/E2502/E2503) + showcase-clean assertion"
    - path: "cli/src/main.rs"
      provides: "run_check assembles vendored stdlib sources from .deal/deps/ into the FFI file set"
  key_links:
    - from: "src/sema.zig"
      to: "deal-stdlib metadata annotations"
      via: "reads exponent metadata from resolved unit/dimension declarations"
      pattern: "DimVector"
    - from: "cli/src/main.rs"
      to: ".deal/deps/<dep>/packages"
      via: "run_check feeds vendored sources to deal_parse/sema"
      pattern: "\\.deal/deps"
---

<objective>
Implement Check #7 in the Zig sema: a generic 7-exponent SI base-vector dimensional algebra (M, L, T, I, Θ, N, J) that reads exponent metadata from stdlib-declared dimensions/units (the encoding locked in Plan 03's ADR) and validates unit expressions (D-55/D-56) — emitting E2500..E2503. Wire the Rust CLI to assemble vendored stdlib sources from `.deal/deps/` into the FFI file set so `import deal.std.units.{kg}` resolves (Plan 02's resolver + this).

Purpose: D-55/D-56 require full dimensional algebra evaluated at check time, data-driven from stdlib (no hardcoded SI knowledge), with explicit-conversion-only enforcement (D-57). This is the most novel work in Phase 4.
Output: extended `src/sema.zig` (Check #7), live `tests/unit/sema_dimensional.zig`, extended `run_check` cross-file resolution, one added conversion-mismatch fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ecosystem/04-CONTEXT.md
@.planning/phases/04-ecosystem/04-RESEARCH.md
@.planning/phases/04-ecosystem/04-PATTERNS.md
@.planning/decisions/ADR-phase-4-dimension-metadata-syntax.md
@.planning/phases/04-ecosystem/04-01-SUMMARY.md
@.planning/phases/04-ecosystem/04-02-SUMMARY.md
@.planning/phases/04-ecosystem/04-03-SUMMARY.md

<interfaces>
<!-- E-codes RESERVED by Plan 01 (src/diagnostics.zig):
   Codes.e_dimension_mismatch = "E2500"
   Codes.e_mixed_unit_comparison = "E2501"
   Codes.e_unknown_unit = "E2502"
   Codes.e_conversion_type_mismatch = "E2503"
   Codes.e_dependency_not_resolved = "E2402"

   DiagnosticCollector emit pattern (src/diagnostics.zig ~lines 174-214):
     try dc.emitFmt(Codes.e_dimension_mismatch, span, "comptime fmt {s}", .{args});  (T-02-10 comptime fmt)

   SI base vector index map (RESEARCH §Dimensional Algebra Domain):
     0=Mass(M) 1=Length(L) 2=Time(T) 3=Current(I) 4=Temp(Θ) 5=Amount(N) 6=Luminosity(J)
   DimVector = [7]i8 (PATTERNS.md Pattern 3). Derived: N=[1,1,-2,0,0,0,0], V=[1,2,-3,-1,0,0,0], speed=[0,1,-1,0,0,0,0]. -->

<!-- Metadata-read contract: read field/member names from Plan 03's ADR, NOT from RESEARCH's
   illustrative @si_base_dimension[...] (which is not grammar-legal). The ADR specifies the exact
   parseable encoding and the names the sema reads. -->

<!-- Cross-file resolution (RESEARCH §Import Resolution through Dependencies, D-49):
   Zig core stays workspace-agnostic. Rust run_check assembles the file set:
   project sources + .deal/deps/<dep>/packages/**/*.deal, passes them to the FFI entry point.
   Existing run_check FFI call pattern is in cli/src/main.rs ~lines 196-232. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Check #7 dimensional algebra in src/sema.zig</name>
  <files>src/sema.zig, tests/regressions/sema/07-conversion-mismatch.deal</files>
  <behavior>
    - `attribute mass : Mass = V(800);` → emits E2500 (Voltage vector ≠ Mass vector)
    - a `kg` value compared/combined with an `lb` value (both Mass) without a conversion call → emits E2501
    - a unit literal whose name has no declared dimension metadata → emits E2502
    - `to_kg(<non-mass expr>)` (wrong source dimension into a conversion) → emits E2503
    - `km(200) / hr(1)` evaluates to a Speed vector [0,1,-1,0,0,0,0] and assigning it to a `Speed`-typed attribute is clean (no diagnostic)
    - all 19 showcase files emit ZERO E25xx diagnostics
  </behavior>
  <read_first>
    - src/sema.zig (full file — existing check enumeration header, SymbolKind enum, check-function structure, DiagnosticCollector usage)
    - src/diagnostics.zig (the E2500..E2503 codes reserved by Plan 01; emitFmt signature)
    - .planning/decisions/ADR-phase-4-dimension-metadata-syntax.md (EXACT field/member names + encoding the sema must read)
    - tests/unit/sema_dimensional.zig (the scaffold harness from Plan 01 — its regression_pins drive this)
    - .planning/phases/04-ecosystem/04-RESEARCH.md §Dimensional Algebra Domain (vector tables) + Pattern 3
  </read_first>
  <action>
    Add `pub const DimVector = [7]i8;` and named constants (DIM_DIMENSIONLESS, DIM_MASS, DIM_LENGTH, DIM_TIME, ...) near the top of `src/sema.zig` after existing type declarations (PATTERNS.md Pattern 3). Index map per RESEARCH: 0=M,1=L,2=T,3=I,4=Θ,5=N,6=J.
    Extend `SymbolKind` with `dimension_def` and `unit_def` (PATTERNS.md). During the existing symbol-collection pass, when a declaration carries the dimension/unit metadata encoding (per the ADR), record its `DimVector` (for dimensions) or its `{ dim_name, factor }` (for units) in the symbol table — reading ONLY the ADR-specified fields. NO unit names are hardcoded; an empty stdlib means no units are known (graceful, not a crash).
    Implement `checkDimensionalExpr(sema, dc, expr, expected_dim)`: evaluate a unit expression into a `DimVector` using vector arithmetic — multiplication adds exponent vectors, division subtracts, a unit literal `kg(x)` resolves to its declared dimension's vector. Compare against the declared type's vector:
    - vectors differ → `dc.emitFmt(Codes.e_dimension_mismatch, expr.span, ...)` (E2500)
    - two same-dimension operands with DIFFERENT unit names combined/compared without a conversion call → E2501 (D-57 explicit-only)
    - unit literal with no metadata → E2502
    - conversion call with wrong source/target dimension → E2503
    Wire `checkDimensionalExpr` into the sema's expression-checking traversal for attribute default values and verification thresholds (the places unit expressions appear). Extend the check-enumeration header comment with "7. Dimensional algebra (E2500..E2599)".
    Add `tests/regressions/sema/07-conversion-mismatch.deal`: an `attribute def`-resolved expression that calls a conversion with the wrong source dimension, parse-clean, triggering E2503.
  </action>
  <verify>
    <automated>zig build test -Dtest-filter=sema 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub const DimVector' src/sema.zig` returns 1
    - `grep -c 'Codes.e_dimension_mismatch\|Codes.e_mixed_unit_comparison\|Codes.e_unknown_unit\|Codes.e_conversion_type_mismatch' src/sema.zig` returns >= 4 (all four emitted)
    - No hardcoded SI unit name table: `grep -inE '"(kg|meter|newton|volt)"' src/sema.zig` returns 0 (unit knowledge comes from metadata, not literals)
    - `zig build test -Dtest-filter=sema` exits 0
  </acceptance_criteria>
  <done>Check #7 computes SI dimension vectors from stdlib metadata, emits E2500..E2503, hardcodes no unit names; sema builds and dimensional tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Activate the dimensional test harness + wire vendored-stdlib resolution into run_check</name>
  <files>tests/unit/sema_dimensional.zig, cli/src/main.rs</files>
  <read_first>
    - tests/unit/sema_dimensional.zig (the Plan 01 scaffold — flip SCAFFOLD no-ops to live assertions)
    - cli/src/main.rs (run_check FFI call pattern ~lines 196-232; the E2402 guard added by Plan 02; resolver exports)
    - ../deal-stdlib/packages/units/ (the authored stdlib sources from Plan 03)
    - .planning/decisions/ADR-phase-4-dimension-metadata-syntax.md
  </read_first>
  <action>
    In `tests/unit/sema_dimensional.zig`, remove the `// SCAFFOLD` no-op guards so both loops run live: (1) the `regression_pins` loop now asserts each `07-*.deal` fixture emits its pinned `Codes.e_*` as the first dimensional diagnostic (07-dimensional-mismatch→E2500, 07-mixed-unit→E2501, 07-unknown-unit→E2502, 07-conversion-mismatch→E2503); (2) the showcase-clean loop asserts ZERO E25xx across all 19 showcase files. The showcase-clean loop must resolve unit imports against the authored stdlib — load the stdlib unit sources alongside each showcase file in the test (the test owns its file set, mirroring how run_check assembles sources).
    In `cli/src/main.rs run_check`: after the E2402 not-installed guard (Plan 02), assemble the FFI file set to include vendored stdlib sources — for each resolved dependency, glob `.deal/deps/<dep>/packages/**/*.deal` and pass them to the parse/sema entry point alongside the project sources, so `import deal.std.units.{kg}` resolves to the real declarations (D-49: Rust assembles the set, Zig stays workspace-agnostic). Preserve the existing clone-before-free FFI discipline (PATTERNS.md Shared Pattern). If a dependency is declared and present in `.deal/deps/` but contains no parseable sources, surface a clear diagnostic rather than silently passing.
    Also in `cli/src/main.rs`: update `const DEFAULT_STDLIB_TAG` (placeholder defined by Plan 02) to the real stdlib version tag recorded in `.planning/phases/04-ecosystem/04-03-SUMMARY.md` — this is the single place the tag is wired so `deal init` scaffolds a `deal.toml` pointing at the released deal-stdlib tag (D-67).
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/deal-lang/deal && zig build test -Dtest-filter=sema.dimensional 2>&1 | tail -10 && cargo build -p deal 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/sema_dimensional.zig` contains no `SCAFFOLD` no-op guard (`grep -c 'SCAFFOLD' tests/unit/sema_dimensional.zig` returns 0)
    - `zig build test -Dtest-filter=sema.dimensional` exits 0 with all four E25xx regression pins live and the showcase-clean loop passing
    - `cli/src/main.rs run_check` globs `.deal/deps/<dep>/packages` into the FFI file set (`grep -c '.deal/deps' cli/src/main.rs` returns >= 1)
    - An end-to-end check: a temp project importing `deal.std.units.{kg}` with the stdlib vendored resolves without E2402/E2502
    - `const DEFAULT_STDLIB_TAG` in cli/src/main.rs equals the tag recorded in 04-03-SUMMARY.md (no `<DEFAULT_STDLIB_TAG>`/placeholder value remains)
  </acceptance_criteria>
  <done>The dimensional harness is live (4 regression pins + showcase-clean); run_check resolves external imports through vendored stdlib sources; cross-boundary wiring complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored stdlib sources → sema | Externally-resolved DEAL sources are parsed and their metadata trusted by Check #7 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-08 | Denial of Service | pathological unit expression depth crashing sema | mitigate | Reuse existing parser nesting/recursion bounds; Check #7 traverses bounded AST; no unbounded recursion in vector evaluation |
| T-4-09 | Tampering | vendored stdlib swapped for one with wrong exponents | accept | deal.lock SHA pins the exact stdlib commit (Plan 02); reproducibility is the control; check only parses (no code execution) |
</threat_model>

<verification>
- `zig build test -Dtest-filter=sema.dimensional` exits 0 (4 live E25xx pins + showcase-clean)
- `cargo build -p deal` succeeds with the cross-file resolution in run_check
- End-to-end: temp project importing a stdlib unit resolves without spurious E2402/E2502
</verification>

<success_criteria>
- deal check computes unit-expression dimensions from stdlib metadata and emits E2500..E2503 correctly (D-55/D-56/D-57)
- Zig core hardcodes zero unit names (data-driven; FA-2)
- 19-file showcase stays dimensionally clean
</success_criteria>

<output>
Create `.planning/phases/04-ecosystem/04-04-SUMMARY.md` when done
</output>
